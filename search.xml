<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2026/01/24/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>pytorch 软件环境搭建</title>
    <url>/2024/08/29/pytorch-%E8%BD%AF%E4%BB%B6%E7%8E%AF%E5%A2%83%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<h1 id="Pytorch-环境搭建"><a href="#Pytorch-环境搭建" class="headerlink" title="Pytorch 环境搭建"></a>Pytorch 环境搭建</h1><h2 id="conda安装"><a href="#conda安装" class="headerlink" title="conda安装"></a>conda安装</h2><p>conda是一个开源的包、环境管理器，可以用于在同一个机器上安装不同版本的软件包及其依赖，并能够在不同的环境之间切换。我强烈推荐你使用他，他的作用类似于java中的maven和我们平时使用的虚拟机，他能够保证的项目之间互相是隔离的。举个简单的例子，如果你同时有两个项目，你一个使用的是pytorch1.8，一个用的是pytorch1.10，这样一个环境肯定就不够了，这个时候anaconda就派上大用场了，他可以创建两个环境，各用各的，互不影响，而且同过不同项目加载不同环境，也可以使项目加载更快更轻便。 </p>
<p>Anaconda有完整版和min版，完整版过于臃肿，因此推荐min版。下载地址推荐<a href="https://mirrors.tuna.tsinghua.edu.cn/anaconda/miniconda/">清华大学开源镜像网站</a>,如果不想自己挑选版本也可以点击这个链接下载楼主的版本<a href="https://mirrors.tuna.tsinghua.edu.cn/anaconda/miniconda/Miniconda3-py39_4.9.2-Windows-x86_64.exe">Miniconda3</a><br>不管下载那个版本的，请注意不要安装到C盘，这是因为conda的使用过程会占据大量内存，下载C盘会使得C盘内存紧张。<br>安装的时候，需要添加到系统路径这个选项也请务必选上，后面使用起来会带来很多便捷，并且这里的安装位置请你一定要记得，后面我们在Pycharm中将会使用到。  </p>
<p><img src="/pic/conda%E5%AE%89%E8%A3%85%E9%80%89%E6%8B%A9.png" alt="conda安装选择">  </p>
<h2 id="conda-虚拟环境创建"><a href="#conda-虚拟环境创建" class="headerlink" title="conda 虚拟环境创建"></a>conda 虚拟环境创建</h2><p>首先，我们要根据项目需求来创建一个环境，通过下面的指令创建并激活虚拟环境<br>本文这里以Python版本3.8.5为例子进行创建一个名为Pytorch的虚拟环境  </p>
<p><code>conda create -n Pytorch python==3.8.5</code><br><code>conda activate Pytorch</code>  </p>
<p><img src="/pic/%E8%BF%9B%E5%85%A5%E8%99%9A%E6%8B%9F%E7%8E%AF%E5%A2%83.png" alt="conda安装选择">  </p>
<p>创建环境过程中需要下载，由于下载服务器不稳定，可能会出现下载错误。导致虚拟环境创建失败，创建成功后并进入虚拟环境，命令行出现Pytorch着表明环境创建成功。<br>一定要出现“Pytorch”没出现着表面没进入虚拟环境，会导致后续安装的包的都在基础环境中。  </p>
<h2 id="Pytorch-安装"><a href="#Pytorch-安装" class="headerlink" title="Pytorch 安装"></a>Pytorch 安装</h2><p>在安装Pytorch前需要缺确认电脑cuda驱动，cuda驱动不用自己安装，一般都随电脑自带的。可以在命令行中输入<code>nvidia-smi</code>  </p>
<p><img src="/pic/nvidia.png" alt="nvidia版本">  </p>
<p>正常出现这个输出着证明cuda驱动没问题，我们还需要记住 CUDA version 版本。上面的截图的版本是12.6.<br>后面pytorch安装需要更具你的cuda版本进行安装，cuda版本可以向下兼容，向上着会出现安装报错。<br>安装有两个选择，一个是去<a href="https://pytorch.org/get-started/locally/">官网</a>根据版本环境需求下载，但是由于服务器的原因下载并不稳定。本文后续介绍其它方法。</p>
<h3 id="Pytorch-CPU版本安装"><a href="#Pytorch-CPU版本安装" class="headerlink" title="Pytorch CPU版本安装"></a>Pytorch CPU版本安装</h3><p><code>conda install pytorch torchvision torchaudio cpuonly -c pytorch</code></p>
<h3 id="Pytroch-GPU版本安装"><a href="#Pytroch-GPU版本安装" class="headerlink" title="Pytroch GPU版本安装"></a>Pytroch GPU版本安装</h3><p><code>conda install pytorch==1.12.0 torchvision==0.13.0 torchaudio==0.12.0 cudatoolkit=11.3 -c https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/pytorch/win-64/</code></p>
<h2 id="Pycharm-IED-安装"><a href="#Pycharm-IED-安装" class="headerlink" title="Pycharm IED 安装"></a>Pycharm IED 安装</h2><p>Pycharm IED是python集成编辑器，目前<a href="https://www.jetbrains.com/pycharm/">Pycharm官网</a>有两个版本社区版和专业版。通常来说初学者使用<a href="https://download-cdn.jetbrains.com.cn/python/pycharm-community-2024.2.1.exe">Pycharm社区版</a>完全够用。另外如想使用专业版，由于专业版有使用期限，但学生可以利用学生邮箱（edu后缀）去省钱使用，可以免费白嫖！！！<br>安装Pycharm过程十分简单这里就过多赘述，仅建议安装过程在选择Pycharm需求时全选。</p>
<h3 id="conda-环境引入"><a href="#conda-环境引入" class="headerlink" title="conda 环境引入"></a>conda 环境引入</h3><p><img src="/pic/pytorchstep1.png" alt="step1"><br><img src="/pic/pytorchstep2.png" alt="step2">  </p>
<h2 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h2><p>验证在Pycharm中新建一个项目，新建项目安装前一掌将conda引入。<br>新建一个py文件并输入下面代码<br><code>import torch</code><br><code>print(torch.__version__)</code><br><code>print(torch.version.cuda)</code><br><code>print(torch.cuda. is_available())</code>  </p>
<p>进行运行，输出结果如下<br><img src="/pic/pytorch%E6%A3%80%E9%AA%8C.png" alt="检验结果"> </p>
<h2 id="完结撒花"><a href="#完结撒花" class="headerlink" title="完结撒花"></a>完结撒花</h2><p>第一片技术类文章完结撒花，总耗时4小时</p>
]]></content>
      <categories>
        <category>软件</category>
      </categories>
      <tags>
        <tag>Pytorch</tag>
      </tags>
  </entry>
  <entry>
    <title>torchvision路径应用失败问题</title>
    <url>/2024/09/05/torchvision%E8%B7%AF%E5%BE%84%E5%BA%94%E7%94%A8%E5%A4%B1%E8%B4%A5%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<h1 id="torchvision模块路径引用问题"><a href="#torchvision模块路径引用问题" class="headerlink" title="torchvision模块路径引用问题"></a>torchvision模块路径引用问题</h1><p>前言：安装pytorch后可能会出现torch模块引用成功，而torchvision模块引用失败，失败原因为torchvision模块路径丢失。可能是三个问题导致的： </p>
<ol>
<li>torch和torchvision版本不匹配</li>
<li>pillow版本和torchvision版本不匹配</li>
</ol>
<h1 id="torch和torchvision版本不匹配"><a href="#torch和torchvision版本不匹配" class="headerlink" title="torch和torchvision版本不匹配"></a>torch和torchvision版本不匹配</h1><p>建议从pytorch官网重新下载新版pytorch，或者查询历史版本重新选择一个版本进行下载</p>
<h1 id="pillow版本和torchvision版本不匹配"><a href="#pillow版本和torchvision版本不匹配" class="headerlink" title="pillow版本和torchvision版本不匹配"></a>pillow版本和torchvision版本不匹配</h1><p>删除pillow 在重新下载pillow最新版</p>
]]></content>
      <categories>
        <category>软件</category>
      </categories>
      <tags>
        <tag>Pytorch</tag>
      </tags>
  </entry>
  <entry>
    <title>GAN生成式对抗网络</title>
    <url>/2024/09/13/GAN%E7%BD%91%E7%BB%9C/</url>
    <content><![CDATA[<h1 id="GAN生成式对抗网络"><a href="#GAN生成式对抗网络" class="headerlink" title="GAN生成式对抗网络"></a>GAN生成式对抗网络</h1><p>GAN生成式对抗网络，GAN网络由生成网络和判别网络两个网络组成，生成式网络通过学习图片后将噪声转换为图片，而判别式网络将区分数据集图片和生成图片进行判别。生成式与判别式网络类似与小偷与警察，通过不断的博弈互相升级，在这场游戏中我们更加希望小偷取得胜利。既是生成式网络生成的图片可以让判别器难以判断。</p>
<h2 id="网络结构"><a href="#网络结构" class="headerlink" title="网络结构"></a>网络结构</h2><h3 id="网络模型"><a href="#网络模型" class="headerlink" title="网络模型"></a>网络模型</h3><h3 id="损失函数"><a href="#损失函数" class="headerlink" title="损失函数"></a>损失函数</h3><h3 id="评价指标"><a href="#评价指标" class="headerlink" title="评价指标"></a>评价指标</h3><h2 id="pytorch实现"><a href="#pytorch实现" class="headerlink" title="pytorch实现"></a>pytorch实现</h2><h2 id="GAN缺陷与改进"><a href="#GAN缺陷与改进" class="headerlink" title="GAN缺陷与改进"></a>GAN缺陷与改进</h2>]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>pytorh</tag>
      </tags>
  </entry>
  <entry>
    <title>新的开始 第一篇博客</title>
    <url>/2024/08/24/%E6%96%B0%E7%9A%84%E5%BC%80%E5%A7%8B-%E7%AC%AC%E4%B8%80%E7%AF%87%E5%8D%9A%E5%AE%A2/</url>
    <content><![CDATA[<h1 id="博客的创建"><a href="#博客的创建" class="headerlink" title="博客的创建"></a>博客的创建</h1><p>当我写下这篇博客的时候，我很高兴我完成了我的第一篇博客的创作，虽然博客网站的运行等注意事项还没彻底掌握，但我很高兴至少自己完成了博客的初步制作。<br>以前在网络查询资料解决各种各式的问题，尝尝觉得这些大佬们技术叹为观止。不免在我脑海中产生一个问题，我能够成为像他们一样的人吗？  </p>
<h1 id="展望"><a href="#展望" class="headerlink" title="展望"></a>展望</h1><p>个人博客的创建是为了记录我即将踏上的三年研究生生涯的点点滴滴，希望我能以梦为马，不负韶华。<br>先定下个小目标实现个人博客的月更和不断探索博客个性化希望等三年后的某一天回首这段时间，我能不留遗憾</p>
]]></content>
      <categories>
        <category>生活</category>
      </categories>
      <tags>
        <tag>随想</tag>
      </tags>
  </entry>
  <entry>
    <title>一、预备知识</title>
    <url>/2024/09/05/%E4%B8%80%E3%80%81%E9%A2%84%E5%A4%87%E7%9F%A5%E8%AF%86/</url>
    <content><![CDATA[<h1 id="预备知识"><a href="#预备知识" class="headerlink" title="预备知识"></a>预备知识</h1><h2 id="基础"><a href="#基础" class="headerlink" title="基础"></a>基础</h2><h2 id="重点"><a href="#重点" class="headerlink" title="重点"></a>重点</h2><h3 id="范数"><a href="#范数" class="headerlink" title="范数"></a>范数</h3><h3 id="矩阵求导"><a href="#矩阵求导" class="headerlink" title="矩阵求导"></a>矩阵求导</h3><h3 id="反向传播（sum函数）"><a href="#反向传播（sum函数）" class="headerlink" title="反向传播（sum函数）"></a>反向传播（sum函数）</h3><p>&#96;&#96;<a href="https://blog.csdn.net/qq_43722079/article/details/136583592">https://blog.csdn.net/qq_43722079/article/details/136583592</a></p>
]]></content>
      <categories>
        <category>动手深度学习</category>
      </categories>
      <tags>
        <tag>Pytorch</tag>
      </tags>
  </entry>
  <entry>
    <title>研一第一月</title>
    <url>/2024/09/01/%E7%A0%94%E4%B8%80%E7%AC%AC%E4%B8%80%E6%9C%88/</url>
    <content><![CDATA[<h1 id="回顾"><a href="#回顾" class="headerlink" title="回顾"></a>回顾</h1><p>结束暑假，开始新学期</p>
<h2 id="学习"><a href="#学习" class="headerlink" title="学习"></a>学习</h2><p>暑假期间初步入门了深度学习和python，完成了小土堆深度学习入门和python，后续需要进一步加强</p>
<h2 id="生活"><a href="#生活" class="headerlink" title="生活"></a>生活</h2><h1 id="展望"><a href="#展望" class="headerlink" title="展望"></a>展望</h1><h2 id="学习-1"><a href="#学习-1" class="headerlink" title="学习"></a>学习</h2><p>继续学习深度学习和python，目前感觉深学习入门还是比较容易，希望学期结束能完成《动手深度学习》</p>
<h2 id="生活-1"><a href="#生活-1" class="headerlink" title="生活"></a>生活</h2><p>准备广东博物馆、广东图书馆游玩</p>
<h1 id="计划"><a href="#计划" class="headerlink" title="计划"></a>计划</h1><ol>
<li>博客一个星期更新一次，一个月更新一次技术类型文章</li>
<li>每月进行总结</li>
<li>精读《动手深度学习》并将每章进行笔记记录，计划本周完成前三章的精读</li>
<li>学习《流程使用python》</li>
</ol>
]]></content>
      <categories>
        <category>生活</category>
      </categories>
      <tags>
        <tag>日记</tag>
      </tags>
  </entry>
  <entry>
    <title>计划</title>
    <url>//notes/%E8%AE%A1%E5%88%92/</url>
    <content><![CDATA[<p>标是把 MySQL&#x2F;Redis 从“会用命令”变成“会做后端设计”。</p>
<p>重点学：索引设计、EXPLAIN、慢查询、事务隔离级别、死锁、幂等、Redis 的缓存一致性、热点 key、大 key、pipeline、限流、分布式锁安全性<br>练习任务：用 Drogon + MySQL + Redis 做一个小服务，至少包含 &#x2F;health、&#x2F;tasks&#x2F;{id}、状态缓存、数据库落盘<br>验收标准：你能说清什么时候查 Redis，什么时候落 MySQL；能解释为什么某个查询要加索引；能讲出缓存击穿和一致性处理思路<br>第 4 周：服务工程化与可靠性<br>目标是把你的项目从“能跑”补成“像后端项目”。</p>
<p>重点学：日志、配置管理、错误码设计、超时、重试、熔断、限流、连接池、线程池、优雅退出、gtest&#x2F;gmock、ASan&#x2F;UBSan&#x2F;TSan、Docker<br>练习任务：把第 3 周的小服务补上日志、配置、单元测试、Docker 启动和一个简单压测<br>验收标准：你能讲清服务异常怎么定位，进程退出怎么保证资源释放，接口超时和重试怎么设计，测试覆盖了什么风险</p>
]]></content>
      <categories>
        <category>学习</category>
      </categories>
  </entry>
  <entry>
    <title>Hexo 博客部署与推送</title>
    <url>/2026/03/30/%E6%9D%82%E4%B8%83%E6%9D%82%E5%85%AB/Hexo%E5%8D%9A%E5%AE%A2%E9%83%A8%E7%BD%B2%E4%B8%8E%E6%8E%A8%E9%80%81/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>这篇文章整理一下我当前博客的部署方式，避免以后每次改完博客都要重新回忆一遍。<br>这套方案的核心思路很简单：</p>
<ol>
<li><code>hexo</code> 分支保存博客源码</li>
<li><code>main</code> 分支保存生成后的静态页面</li>
<li>平时写文章、改主题、换头像，都是在源码分支完成</li>
<li>最后通过 <code>hexo deploy</code> 把静态文件发布到 GitHub Pages</li>
</ol>
<hr>
<h1 id="当前博客结构"><a href="#当前博客结构" class="headerlink" title="当前博客结构"></a>当前博客结构</h1><p>我现在这个仓库的配置大致如下：</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># _config.yml</span></span><br><span class="line"><span class="attr">theme:</span> <span class="string">next</span></span><br><span class="line"></span><br><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repository:</span> <span class="string">https://github.com/ChutianDuan/ChutianDuan.github.io.git</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">main</span></span><br></pre></td></tr></table></figure>

<p>也就是说：</p>
<ul>
<li><code>hexo</code> 分支负责维护源码</li>
<li><code>main</code> 分支负责网站发布</li>
<li>GitHub Pages 最终读取的是 <code>main</code> 分支内容</li>
</ul>
<p>这种做法的好处是源码和生成产物分离，日常维护会清晰很多。</p>
<hr>
<h1 id="需要的环境"><a href="#需要的环境" class="headerlink" title="需要的环境"></a>需要的环境</h1><p>先保证本地安装好 Node.js 和 npm。<br>然后在博客目录执行依赖安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install</span><br></pre></td></tr></table></figure>

<p>当前项目里比较关键的依赖有：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="attr">&quot;hexo&quot;</span><span class="punctuation">:</span> <span class="string">&quot;^7.3.0&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="attr">&quot;hexo-deployer-git&quot;</span><span class="punctuation">:</span> <span class="string">&quot;^4.0.0&quot;</span><span class="punctuation">,</span></span><br><span class="line"><span class="attr">&quot;hexo-theme-next&quot;</span><span class="punctuation">:</span> <span class="string">&quot;^8.27.0&quot;</span></span><br></pre></td></tr></table></figure>

<p>其中 <code>hexo-deployer-git</code> 很重要，如果没有它，执行部署时会报找不到 <code>git deployer</code>。</p>
<hr>
<h1 id="日常写博客的流程"><a href="#日常写博客的流程" class="headerlink" title="日常写博客的流程"></a>日常写博客的流程</h1><h2 id="1-编写或修改文章"><a href="#1-编写或修改文章" class="headerlink" title="1. 编写或修改文章"></a>1. 编写或修改文章</h2><p>文章都放在 <code>source/_posts/</code> 下面，例如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span>/_posts/hello-world.md</span><br><span class="line"><span class="built_in">source</span>/_posts/杂七杂八/内网穿透.md</span><br></pre></td></tr></table></figure>

<p>如果是图片资源，一般放在：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span>/img/</span><br></pre></td></tr></table></figure>

<p>比如头像目前就是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span>/img/me.jpg</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2-本地预览"><a href="#2-本地预览" class="headerlink" title="2. 本地预览"></a>2. 本地预览</h2><p>写完文章后，可以先在本地启动预览服务：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm run server</span><br></pre></td></tr></table></figure>

<p>然后浏览器打开：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">http://localhost:4000</span><br></pre></td></tr></table></figure>

<p>这样可以先检查下面这些内容有没有问题：</p>
<ul>
<li>标题和分类是否正常</li>
<li>封面图是否能显示</li>
<li>Markdown 排版是否错乱</li>
<li>代码块高亮是否正常</li>
<li>图片路径是否写对</li>
</ul>
<hr>
<h2 id="3-生成静态页面"><a href="#3-生成静态页面" class="headerlink" title="3. 生成静态页面"></a>3. 生成静态页面</h2><p>确认内容没问题后，先执行一次生成：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm run build</span><br></pre></td></tr></table></figure>

<p>它实际对应的是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo generate</span><br></pre></td></tr></table></figure>

<p>这一步主要是提前发现问题，比如：</p>
<ul>
<li>配置文件写错</li>
<li>文章 front matter 格式错误</li>
<li>主题模板引用异常</li>
<li>图片路径或资源处理问题</li>
</ul>
<p>如果这里都能通过，后面的部署一般就比较稳。</p>
<hr>
<h2 id="4-提交源码分支"><a href="#4-提交源码分支" class="headerlink" title="4. 提交源码分支"></a>4. 提交源码分支</h2><p>这一步不要省。<br>很多人只执行了部署，却忘了把源码推到远程，结果换台电脑后博客改动全没了。</p>
<p>我的源码分支是 <code>hexo</code>，所以常用流程如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git status</span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;更新博客文章&quot;</span></span><br><span class="line">git push origin hexo</span><br></pre></td></tr></table></figure>

<p>这一步推送的是：</p>
<ul>
<li>文章源码</li>
<li>主题配置</li>
<li>页面配置</li>
<li>图片资源</li>
<li>其他博客工程文件</li>
</ul>
<p>也就是说，这一步是在“保存你的工作过程”。</p>
<hr>
<h2 id="5-部署到-GitHub-Pages"><a href="#5-部署到-GitHub-Pages" class="headerlink" title="5. 部署到 GitHub Pages"></a>5. 部署到 GitHub Pages</h2><p>源码推送完成后，再执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm run deploy</span><br></pre></td></tr></table></figure>

<p>它实际对应的是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo deploy</span><br></pre></td></tr></table></figure>

<p>根据当前配置，Hexo 会做下面几件事：</p>
<ol>
<li>读取 <code>public/</code> 里的生成结果</li>
<li>把这些静态文件提交到部署目录</li>
<li>推送到远程仓库的 <code>main</code> 分支</li>
</ol>
<p>这一步完成后，GitHub Pages 就会从 <code>main</code> 分支发布网站。</p>
<hr>
<h1 id="一套完整命令"><a href="#一套完整命令" class="headerlink" title="一套完整命令"></a>一套完整命令</h1><p>如果只是日常更新一篇文章，我现在一般直接按这个顺序执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm run build</span><br><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;更新博客&quot;</span></span><br><span class="line">git push origin hexo</span><br><span class="line">npm run deploy</span><br></pre></td></tr></table></figure>

<p>可以理解成两次推送：</p>
<ul>
<li>第一次把源码推到 <code>hexo</code></li>
<li>第二次把生成后的静态站点推到 <code>main</code></li>
</ul>
<hr>
<h1 id="为什么要分两个分支"><a href="#为什么要分两个分支" class="headerlink" title="为什么要分两个分支"></a>为什么要分两个分支</h1><p>这个问题很常见。</p>
<p>如果把源码和静态页面都混在一个分支里，会出现几个明显问题：</p>
<ul>
<li>仓库里会同时出现 Hexo 源码和大量生成后的静态文件</li>
<li>每次提交都会混入很多无关改动</li>
<li>后续维护主题、文章、配置时容易混乱</li>
<li>回滚也不方便</li>
</ul>
<p>分开后逻辑就很清楚：</p>
<ul>
<li><code>hexo</code> 是“项目源代码”</li>
<li><code>main</code> 是“构建产物”</li>
</ul>
<p>这和普通前端项目里“源码目录”和“打包产物目录”分离是一样的道理。</p>
<hr>
<h1 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h1><h2 id="1-执行-hexo-deploy-失败"><a href="#1-执行-hexo-deploy-失败" class="headerlink" title="1. 执行 hexo deploy 失败"></a>1. 执行 <code>hexo deploy</code> 失败</h2><p>优先检查有没有安装部署插件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<p>然后确认 <code>_config.yml</code> 里的 <code>deploy</code> 配置是否正确。</p>
<hr>
<h2 id="2-页面没有立刻更新"><a href="#2-页面没有立刻更新" class="headerlink" title="2. 页面没有立刻更新"></a>2. 页面没有立刻更新</h2><p>这通常不是部署失败，而是 GitHub Pages 还没刷新完成。<br>一般等几十秒到几分钟即可。</p>
<p>还可以检查：</p>
<ul>
<li><code>main</code> 分支是否已经收到最新提交</li>
<li>GitHub Pages 配置是否指向正确分支</li>
<li>浏览器是否有缓存</li>
</ul>
<hr>
<h2 id="3-本地构建成功，但线上图片不显示"><a href="#3-本地构建成功，但线上图片不显示" class="headerlink" title="3. 本地构建成功，但线上图片不显示"></a>3. 本地构建成功，但线上图片不显示</h2><p>这种问题通常出在路径。</p>
<p>例如站点资源推荐写成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">/img/me.jpg</span><br></pre></td></tr></table></figure>

<p>而不是本地绝对路径，也不要写成 Windows 文件系统路径。</p>
<hr>
<h2 id="4-只部署了页面，没有保存源码"><a href="#4-只部署了页面，没有保存源码" class="headerlink" title="4. 只部署了页面，没有保存源码"></a>4. 只部署了页面，没有保存源码</h2><p>这是最容易踩的坑。<br>如果你只执行了 <code>npm run deploy</code>，远程网站可能已经更新，但源码分支并没有保存。</p>
<p>正确顺序应该是：</p>
<ol>
<li>先提交并推送 <code>hexo</code> 分支</li>
<li>再部署到 <code>main</code></li>
</ol>
<hr>
<h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><p>对于个人博客来说，这套流程已经足够稳定：</p>
<ul>
<li>平时在 <code>hexo</code> 分支维护文章和配置</li>
<li>用 <code>npm run build</code> 检查生成结果</li>
<li>用 <code>git push origin hexo</code> 保存源码</li>
<li>用 <code>npm run deploy</code> 发布站点</li>
</ul>
<p>这样做的优点是简单、清晰、容易回溯，后续不管是换主题、换头像，还是新增文章，都能沿着同一套流程继续维护。</p>
]]></content>
      <categories>
        <category>杂七杂八</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>GitHub Pages</tag>
        <tag>环境配置</tag>
      </tags>
  </entry>
  <entry>
    <title>第二次组会</title>
    <url>/2024/09/13/%E7%AC%AC%E4%BA%8C%E6%AC%A1%E7%BB%84%E4%BC%9A/</url>
    <content><![CDATA[<h1 id="第二次组会"><a href="#第二次组会" class="headerlink" title="第二次组会"></a>第二次组会</h1><p>一共四个人汇报，其中在投文献一篇方向为拉曼，三篇论汇报拉曼两片，层析一篇</p>
<h2 id="在投文献"><a href="#在投文献" class="headerlink" title="在投文献"></a>在投文献</h2><p>深度学习预测单细胞H2O2含量</p>
<h3 id="数据集"><a href="#数据集" class="headerlink" title="数据集"></a>数据集</h3><p>细胞的数据集在预测问题上可以看作为时间序列，或者弱化其时间的数据看为序列。这种序列和文字序列有一定的相识性，在处理这方面数据可以更多的考虑循环神经网络和注意力机制进行处理</p>
<h3 id="网络"><a href="#网络" class="headerlink" title="网络"></a>网络</h3><p>注意力机制</p>
<h3 id="损失函数"><a href="#损失函数" class="headerlink" title="损失函数"></a>损失函数</h3><p>未知</p>
<h3 id="评价指标"><a href="#评价指标" class="headerlink" title="评价指标"></a>评价指标</h3><p>R^2</p>
<h2 id="论文问题"><a href="#论文问题" class="headerlink" title="论文问题"></a>论文问题</h2><p>网络结构不清晰，深度学习三要素 网络模型、损失函数和评价指标的选取有问题</p>
<h2 id="论文汇报"><a href="#论文汇报" class="headerlink" title="论文汇报"></a>论文汇报</h2><h3 id="transformer-RNN-超高清重建图像"><a href="#transformer-RNN-超高清重建图像" class="headerlink" title="transformer+RNN 超高清重建图像"></a>transformer+RNN 超高清重建图像</h3><h4 id="网络-1"><a href="#网络-1" class="headerlink" title="网络"></a>网络</h4><p>RNN(head)</p>
]]></content>
      <categories>
        <category>生活</category>
      </categories>
      <tags>
        <tag>组会</tag>
      </tags>
  </entry>
  <entry>
    <title>内网穿透</title>
    <url>/2026/02/05/%E6%9D%82%E4%B8%83%E6%9D%82%E5%85%AB/%E5%86%85%E7%BD%91%E7%A9%BF%E9%80%8F/</url>
    <content><![CDATA[<h1 id="内网穿透方案-ZeroTier"><a href="#内网穿透方案-ZeroTier" class="headerlink" title="内网穿透方案_ZeroTier"></a>内网穿透方案_ZeroTier</h1><h2 id="Liunx-配置"><a href="#Liunx-配置" class="headerlink" title="Liunx 配置"></a>Liunx 配置</h2><ol>
<li>安装</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -s https://install.zerotier.com | <span class="built_in">sudo</span> bash</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>启动并设为开机自启</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> systemctl <span class="built_in">enable</span> --now zerotier-one</span><br><span class="line"><span class="built_in">sudo</span> systemctl status zerotier-one --no-pager</span><br></pre></td></tr></table></figure>

<p>) 加入网络（Network ID）</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zerotier-cli <span class="built_in">join</span> &lt;NETWORK_ID&gt;</span><br></pre></td></tr></table></figure>


<ol start="4">
<li><p>去 Central 授权</p>
</li>
<li><p>验证是否拿到 Managed IP</p>
</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> zerotier-cli status</span><br><span class="line"><span class="built_in">sudo</span> zerotier-cli listnetworks</span><br><span class="line">ip a | grep -n <span class="string">&quot;zt&quot;</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="Windows（Win10-Win11）"><a href="#Windows（Win10-Win11）" class="headerlink" title="Windows（Win10&#x2F;Win11）"></a>Windows（Win10&#x2F;Win11）</h2><ol>
<li>安装</li>
</ol>
<ol>
<li>去 ZeroTier 官网下载 Windows Installer（.msi &#x2F; .exe）</li>
<li>双击安装（默认一路 Next 即可）</li>
</ol>
<ol start="2">
<li>启动方式</li>
</ol>
<ul>
<li>安装后通常会自动启动服务，并在右下角托盘出现 <strong>ZeroTier</strong> 图标</li>
<li>若没看到托盘图标：开始菜单打开 <strong>ZeroTier One</strong>（会拉起托盘）</li>
<li>或检查服务：<code>Win + R</code> → <code>services.msc</code> → 找到 <strong>ZeroTier One</strong>（或类似名字）→ 启动</li>
</ul>
<ol start="3">
<li>加入网络（GUI）</li>
</ol>
<ol>
<li>右下角托盘 ZeroTier 图标右键</li>
<li><strong>Join New Network…</strong></li>
<li>粘贴 <code>&lt;NETWORK_ID&gt;</code> → Join</li>
</ol>
<h2 id="最小联通测试"><a href="#最小联通测试" class="headerlink" title="最小联通测试"></a>最小联通测试</h2><p>在任意一端用对方 Managed IP：</p>
<ul>
<li>测试连通：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ping &lt;对方Managed_IP&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li>SSH（Linux 目标）：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh user@&lt;对方Managed_IP&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li>RDP（Windows 目标，默认 3389）：<br><code>mstsc /v:&lt;对方Managed_IP&gt;</code></li>
</ul>
]]></content>
      <categories>
        <category>杂七杂八</category>
      </categories>
      <tags>
        <tag>环境配置</tag>
        <tag>构建系统</tag>
      </tags>
  </entry>
  <entry>
    <title>cmake环境构建注意事项</title>
    <url>/2025/12/25/%E6%9D%82%E4%B8%83%E6%9D%82%E5%85%AB/cmake%E7%8E%AF%E5%A2%83%E6%9E%84%E5%BB%BA%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9/</url>
    <content><![CDATA[<h1 id="cmake环境构建注意事项"><a href="#cmake环境构建注意事项" class="headerlink" title="cmake环境构建注意事项"></a>cmake环境构建注意事项</h1><p>后续要装更多包：通用“镜像下载 + 安装模板”</p>
<p>你以后新增包基本分三类：CMake &#x2F; Autotools &#x2F; 纯 Make。建议统一规则：</p>
<p>下载：都落到 &#x2F;opt&#x2F;tp-downloads</p>
<p>解压构建：都在 &#x2F;opt&#x2F;src_clean&#x2F;<pkg></p>
<p>安装：统一 –prefix&#x3D;&#x2F;opt&#x2F;tp 或 -DCMAKE_INSTALL_PREFIX&#x3D;&#x2F;opt&#x2F;tp</p>
<p>使用：工程端统一 CMAKE_PREFIX_PATH&#x3D;&#x2F;opt&#x2F;tp</p>
<p>7.1 通用下载函数（带镜像）</p>
<p>建议你写个小函数（放到 ~&#x2F;.bashrc 或单独脚本）：</p>
<p>fetch_gh () {</p>
<h1 id="用法：fetch-gh-owner-repo-ref-output-tar-gz"><a href="#用法：fetch-gh-owner-repo-ref-output-tar-gz" class="headerlink" title="用法：fetch_gh owner repo ref output.tar.gz"></a>用法：fetch_gh owner repo ref output.tar.gz</h1><h1 id="例：fetch-gh-sewenew-redis-plus-plus-refs-tags-1-3-15-redis-plus-plus-1-3-15-tar-gz"><a href="#例：fetch-gh-sewenew-redis-plus-plus-refs-tags-1-3-15-redis-plus-plus-1-3-15-tar-gz" class="headerlink" title="例：fetch_gh sewenew redis-plus-plus refs&#x2F;tags&#x2F;1.3.15 redis-plus-plus-1.3.15.tar.gz"></a>例：fetch_gh sewenew redis-plus-plus refs&#x2F;tags&#x2F;1.3.15 redis-plus-plus-1.3.15.tar.gz</h1><p>  local owner&#x3D;”$1” repo&#x3D;”$2” ref&#x3D;”$3” out&#x3D;”$4”<br>  mkdir -p &#x2F;opt&#x2F;tp-downloads<br>  curl -L –fail <br>    “<a href="https://gh-proxy.org/https://github.com/$%7Bowner%7D/$%7Brepo%7D/archive/$%7Bref%7D.tar.gz">https://gh-proxy.org/https://github.com/${owner}/${repo}/archive/${ref}.tar.gz</a>“ <br>    -o “&#x2F;opt&#x2F;tp-downloads&#x2F;${out}”<br>}</p>
<p>7.2 CMake 类包安装模板<br>source &#x2F;opt&#x2F;tp&#x2F;env.sh<br>cd &#x2F;opt&#x2F;src_clean&#x2F;<pkg><br>rm -rf build &amp;&amp; mkdir build &amp;&amp; cd build<br>&#x2F;opt&#x2F;tp&#x2F;bin&#x2F;cmake .. -G Ninja <br>  -DCMAKE_BUILD_TYPE&#x3D;Release <br>  -DCMAKE_INSTALL_PREFIX&#x3D;&#x2F;opt&#x2F;tp <br>  -DCMAKE_PREFIX_PATH&#x3D;&#x2F;opt&#x2F;tp<br>&#x2F;opt&#x2F;tp&#x2F;bin&#x2F;cmake –build . -j”$(nproc)”<br>&#x2F;opt&#x2F;tp&#x2F;bin&#x2F;cmake –install .</p>
<p>7.3 Autotools 类包安装模板<br>.&#x2F;configure –prefix&#x3D;&#x2F;opt&#x2F;tp<br>make -j”$(nproc)”<br>make install</p>
<ol>
<li>下载 redis-plus-plus（走 GitHub 镜像）</li>
</ol>
<p>redis-plus-plus 最新 release&#x2F;tag 是 1.3.15。<br>GitHub</p>
<p>mkdir -p &#x2F;opt&#x2F;tp-downloads<br>cd &#x2F;opt&#x2F;tp-downloads</p>
<p>curl -L –fail <br>  “<a href="https://gh-proxy.org/https://github.com/sewenew/redis-plus-plus/archive/refs/tags/1.3.15.tar.gz">https://gh-proxy.org/https://github.com/sewenew/redis-plus-plus/archive/refs/tags/1.3.15.tar.gz</a>“ <br>  -o redis-plus-plus-1.3.15.tar.gz</p>
<ol start="2">
<li>解压、编译、安装到 &#x2F;opt&#x2F;tp</li>
</ol>
<p>关键 CMake 选项：</p>
<p>-DREDIS_PLUS_PLUS_BUILD_TEST&#x3D;OFF：不编测试，加速且避免额外依赖<br>GitHub<br>+1</p>
<p>-DCMAKE_PREFIX_PATH&#x3D;&#x2F;opt&#x2F;tp：让它找到你已装的 hiredis（以及未来其他依赖）</p>
<p>source &#x2F;opt&#x2F;tp&#x2F;env.sh</p>
<p>cd &#x2F;opt&#x2F;src_clean<br>rm -rf redis-plus-plus-1.3.15<br>tar -xf &#x2F;opt&#x2F;tp-downloads&#x2F;redis-plus-plus-1.3.15.tar.gz<br>cd redis-plus-plus-1.3.15</p>
<p>rm -rf build<br>mkdir -p build<br>cd build</p>
<p>&#x2F;opt&#x2F;tp&#x2F;bin&#x2F;cmake .. -G Ninja <br>  -DCMAKE_BUILD_TYPE&#x3D;Release <br>  -DCMAKE_INSTALL_PREFIX&#x3D;&#x2F;opt&#x2F;tp <br>  -DCMAKE_PREFIX_PATH&#x3D;&#x2F;opt&#x2F;tp <br>  -DREDIS_PLUS_PLUS_BUILD_TEST&#x3D;OFF</p>
<p>&#x2F;opt&#x2F;tp&#x2F;bin&#x2F;cmake –build . -j”$(nproc)”<br>&#x2F;opt&#x2F;tp&#x2F;bin&#x2F;cmake –install .</p>
]]></content>
      <categories>
        <category>杂七杂八</category>
      </categories>
      <tags>
        <tag>环境配置</tag>
        <tag>构建系统</tag>
        <tag>CMake</tag>
      </tags>
  </entry>
  <entry>
    <title>AI 模型开发进度记录</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E5%BC%80%E5%8F%91%E8%BF%9B%E5%BA%A6%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="AI-模型开发进度记录"><a href="#AI-模型开发进度记录" class="headerlink" title="AI 模型开发进度记录"></a>AI 模型开发进度记录</h1><blockquote>
<p>整理时间：2026-04-20<br>说明：当前文件基于现有笔记和方案文档整理，不额外虚构未发生的开发进展。</p>
</blockquote>
<h2 id="1-项目目标"><a href="#1-项目目标" class="headerlink" title="1. 项目目标"></a>1. 项目目标</h2><p>目标是在本地完成一个可部署、可演示、可维护的科研知识增强生成式 AI 系统，核心能力包括：</p>
<ul>
<li>文档上传与管理</li>
<li>异步解析、切块、索引构建</li>
<li>基于 RAG 的问答</li>
<li>引用返回</li>
<li>多轮会话与上下文管理</li>
<li>流式输出</li>
</ul>
<p>当前确定的主技术路线：</p>
<ul>
<li>C++ 网关：Drogon</li>
<li>Python 服务：FastAPI</li>
<li>异步任务：Celery</li>
<li>数据存储：MySQL</li>
<li>缓存与状态层：Redis</li>
<li>检索能力：Embedding + Chunk + FAISS &#x2F; rerank</li>
</ul>
<p>参考总方案：</p>
<ul>
<li><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/deep-research-report/">deep-research-report.md</a></li>
</ul>
<h2 id="2-当前阶段判断"><a href="#2-当前阶段判断" class="headerlink" title="2. 当前阶段判断"></a>2. 当前阶段判断</h2><p>截至 2026-04-20，从当前目录可见材料判断，项目目前主要处于：</p>
<p><code>方案设计完成 + 技术储备基本成型 + 进入工程落地前/落地初期</code></p>
<p>也就是说，方向和技术路线已经比较清楚，但还缺少连续的实现记录、联调结果和里程碑沉淀。这次整理的重点，就是把已有材料收拢成一份后面能持续更新的主记录。</p>
<h2 id="3-已完成进展"><a href="#3-已完成进展" class="headerlink" title="3. 已完成进展"></a>3. 已完成进展</h2><h3 id="3-1-方案与路线已经明确"><a href="#3-1-方案与路线已经明确" class="headerlink" title="3.1 方案与路线已经明确"></a>3.1 方案与路线已经明确</h3><p>已完成的高价值产出：</p>
<ul>
<li>已产出整体方案文档，明确系统目标、架构分层、8 周开发路线和 MVP 范围</li>
<li>已确定采用 <code>C++ Gateway + Python RAG Service</code> 的双服务方案</li>
<li>已明确 MySQL、Redis、Celery、Embedding、流式输出等关键模块的位置和职责</li>
</ul>
<p>对应文档：</p>
<ul>
<li><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/deep-research-report/">deep-research-report.md</a></li>
</ul>
<h3 id="3-2-后端基础框架学习已完成一轮"><a href="#3-2-后端基础框架学习已完成一轮" class="headerlink" title="3.2 后端基础框架学习已完成一轮"></a>3.2 后端基础框架学习已完成一轮</h3><p>你已经完成一轮和主线高度相关的基础框架学习，且内容覆盖比较完整：</p>
<table>
<thead>
<tr>
<th>模块</th>
<th>状态</th>
<th>说明</th>
<th>对应笔记</th>
</tr>
</thead>
<tbody><tr>
<td>Drogon</td>
<td>已完成基础学习</td>
<td>对 C++ Web 网关、路由、控制器、文件上传、与内部服务协作有了基础认知</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/drogon/">Drogon.md</a></td>
</tr>
<tr>
<td>FastAPI</td>
<td>已完成基础学习</td>
<td>已覆盖 API、参数、请求体、异步、上传、任务入口等内容</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/fasthapi/">FasthAPI.md</a></td>
</tr>
<tr>
<td>Celery</td>
<td>已完成基础学习</td>
<td>已掌握任务队列、Worker、状态、重试、任务查询、与 FastAPI 配合</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/celery/">Celery.md</a></td>
</tr>
<tr>
<td>Redis</td>
<td>已完成基础学习</td>
<td>已覆盖缓存、任务状态、中间结果、计数、TTL 等常见使用场景</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/redis/">Redis.md</a></td>
</tr>
<tr>
<td>MySQL</td>
<td>已完成基础学习</td>
<td>已覆盖连接、建表、字段设计、任务记录和业务数据存储思路</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/mysql/">MySQL.md</a></td>
</tr>
<tr>
<td>Ray</td>
<td>已完成基础学习</td>
<td>已对分布式计算&#x2F;GPU 调度有初步储备，适合作为后续扩展项</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/ray/">Ray.md</a></td>
</tr>
<tr>
<td>Embedding</td>
<td>已完成基础学习</td>
<td>已理解向量表示在语义检索&#x2F;RAG 中的作用</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/embedding/">embedding.md</a></td>
</tr>
</tbody></table>
<h3 id="3-3-检索与模型相关知识已开始进入专项阶段"><a href="#3-3-检索与模型相关知识已开始进入专项阶段" class="headerlink" title="3.3 检索与模型相关知识已开始进入专项阶段"></a>3.3 检索与模型相关知识已开始进入专项阶段</h3><p>2026-04-19 这组笔记说明你的学习已经从“通用后端栈”切到“RAG&#x2F;检索&#x2F;训练专项”：</p>
<table>
<thead>
<tr>
<th>专题</th>
<th>状态</th>
<th>当前意义</th>
<th>对应笔记</th>
</tr>
</thead>
<tbody><tr>
<td>Chunk</td>
<td>已完成入门与策略理解</td>
<td>为文档切分和召回效果打基础</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/chunk%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/">Chunk学习笔记.md</a></td>
</tr>
<tr>
<td>句子嵌入模型</td>
<td>已完成系统性梳理</td>
<td>已开始从业务目标、数据、训练、部署全链路理解 embedding</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/">句子嵌入模型.md</a></td>
</tr>
<tr>
<td>数据处理与评估</td>
<td>已完成一轮整理</td>
<td>已开始关注数据清洗、评估集、badcase 和指标</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/%E6%95%B0%E6%8D%AE%E5%A4%84%E7%90%86%E4%B8%8E%E6%95%B0%E6%8D%AE%E9%9B%86%E8%AF%84%E4%BC%B0/">数据处理与数据集评估.md</a></td>
</tr>
<tr>
<td>LoRA 微调训练</td>
<td>已完成基础梳理</td>
<td>说明你已经开始考虑后续模型训练&#x2F;调优路线</td>
<td><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/lora%E5%BE%AE%E8%B0%83%E8%AE%AD%E7%BB%83/">LoRA微调训练.md</a></td>
</tr>
</tbody></table>
<h3 id="3-4-当前真正“落地完成”的部分"><a href="#3-4-当前真正“落地完成”的部分" class="headerlink" title="3.4 当前真正“落地完成”的部分"></a>3.4 当前真正“落地完成”的部分</h3><p>基于现有文件，可以确认已经完成的是：</p>
<ul>
<li>项目目标与交付路线的规划</li>
<li>主技术栈的选型和学习</li>
<li>检索&#x2F;RAG 关键知识点的初步铺垫</li>
<li>一份适合继续追踪的主进度文档框架</li>
</ul>
<p>目前还不能从现有目录中确认已经完成的内容：</p>
<ul>
<li>实际工程仓库初始化</li>
<li>Docker &#x2F; docker-compose 启动记录</li>
<li>Drogon &#x2F; FastAPI &#x2F; Celery 服务跑通记录</li>
<li>数据库表结构落地</li>
<li>上传、切块、索引、问答联调结果</li>
<li>测试、验收、bug 排查日志</li>
</ul>
<h2 id="4-时间线整理"><a href="#4-时间线整理" class="headerlink" title="4. 时间线整理"></a>4. 时间线整理</h2><p>按现有文件时间整理出的开发脉络如下：</p>
<table>
<thead>
<tr>
<th>日期</th>
<th>进展</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>2026-03-16</td>
<td>完成总体方案研究</td>
<td>产出 <a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/deep-research-report/">deep-research-report.md</a>，明确 8 周路线和总体架构</td>
</tr>
<tr>
<td>2026-03-24</td>
<td>完成 FastAPI &#x2F; Celery &#x2F; Redis 第一轮学习</td>
<td>开始打通 Python 服务、异步任务、状态层相关知识</td>
</tr>
<tr>
<td>2026-03-28</td>
<td>完成 Drogon &#x2F; MySQL 第一轮学习</td>
<td>开始补齐 C++ 网关与关系型数据层</td>
</tr>
<tr>
<td>2026-03-29</td>
<td>完成 Embedding 学习</td>
<td>检索和向量表示方向开始成型</td>
</tr>
<tr>
<td>2026-03-30</td>
<td>完成 Ray 学习</td>
<td>对后续并行计算 &#x2F; GPU 调度有预研储备</td>
</tr>
<tr>
<td>2026-04-19</td>
<td>完成 Chunk &#x2F; 句子嵌入 &#x2F; 数据评估 &#x2F; LoRA 专题整理</td>
<td>学习重点从框架切到检索与模型侧</td>
</tr>
<tr>
<td>2026-04-20</td>
<td>建立正式进度记录</td>
<td>将已有资料整理成统一的开发记录入口</td>
</tr>
</tbody></table>
<h2 id="5-阶段拆分与当前状态"><a href="#5-阶段拆分与当前状态" class="headerlink" title="5. 阶段拆分与当前状态"></a>5. 阶段拆分与当前状态</h2><h3 id="阶段一：方向确认与技术选型"><a href="#阶段一：方向确认与技术选型" class="headerlink" title="阶段一：方向确认与技术选型"></a>阶段一：方向确认与技术选型</h3><p>状态：<code>已完成</code></p>
<p>已完成内容：</p>
<ul>
<li>明确项目目标和 MVP 范围</li>
<li>明确双服务架构</li>
<li>明确数据层、任务层、检索层、流式层的职责分工</li>
</ul>
<h3 id="阶段二：基础知识补齐"><a href="#阶段二：基础知识补齐" class="headerlink" title="阶段二：基础知识补齐"></a>阶段二：基础知识补齐</h3><p>状态：<code>已完成第一轮</code></p>
<p>已完成内容：</p>
<ul>
<li>Web 服务框架学习</li>
<li>存储与缓存学习</li>
<li>异步任务机制学习</li>
<li>向量检索和训练相关基础学习</li>
</ul>
<p>当前缺口：</p>
<ul>
<li>还需要把“概念理解”转成“最小可运行系统”</li>
<li>还缺少工程级联调记录和可验证产物</li>
</ul>
<h3 id="阶段三：最小系统落地"><a href="#阶段三：最小系统落地" class="headerlink" title="阶段三：最小系统落地"></a>阶段三：最小系统落地</h3><p>状态：<code>待开始或未记录</code></p>
<p>建议按下面顺序推进：</p>
<ol>
<li>建立工程目录与 <code>docker-compose</code></li>
<li>跑通 MySQL、Redis</li>
<li>跑通 Drogon <code>health</code> 接口</li>
<li>跑通 FastAPI <code>health</code> 接口</li>
<li>跑通 Celery demo task 和任务状态查询</li>
<li>打通“上传文件 -&gt; 创建任务 -&gt; 查询状态”的最小闭环</li>
</ol>
<h3 id="阶段四：RAG-核心链路"><a href="#阶段四：RAG-核心链路" class="headerlink" title="阶段四：RAG 核心链路"></a>阶段四：RAG 核心链路</h3><p>状态：<code>待开始或未记录</code></p>
<p>核心目标：</p>
<ol>
<li>文档解析与切块</li>
<li>Embedding 生成</li>
<li>向量索引构建</li>
<li>检索与重排</li>
<li>问答生成</li>
<li>引用返回</li>
</ol>
<h3 id="阶段五：工程化与体验优化"><a href="#阶段五：工程化与体验优化" class="headerlink" title="阶段五：工程化与体验优化"></a>阶段五：工程化与体验优化</h3><p>状态：<code>待开始或未记录</code></p>
<p>后续重点：</p>
<ol>
<li>流式输出</li>
<li>会话记忆</li>
<li>配置管理</li>
<li>限流与鉴权</li>
<li>测试与部署</li>
</ol>
<h2 id="6-当前最值得推进的任务"><a href="#6-当前最值得推进的任务" class="headerlink" title="6. 当前最值得推进的任务"></a>6. 当前最值得推进的任务</h2><p>如果你接下来要进入真正开发，我建议优先记录和推进下面这些 P0 事项：</p>
<table>
<thead>
<tr>
<th>优先级</th>
<th>任务</th>
<th>目标产出</th>
</tr>
</thead>
<tbody><tr>
<td>P0</td>
<td>初始化工程骨架</td>
<td>有明确目录结构、启动脚本、配置文件</td>
</tr>
<tr>
<td>P0</td>
<td>跑通 MySQL &#x2F; Redis</td>
<td>基础依赖可稳定启动</td>
</tr>
<tr>
<td>P0</td>
<td>建立 Drogon 最小服务</td>
<td>提供 <code>/health</code>，可启动可访问</td>
</tr>
<tr>
<td>P0</td>
<td>建立 FastAPI + Celery 最小服务</td>
<td>能提交 demo task 并查询状态</td>
</tr>
<tr>
<td>P0</td>
<td>设计数据表</td>
<td>至少有 <code>documents</code>、<code>tasks</code>、<code>sessions</code>、<code>messages</code></td>
</tr>
<tr>
<td>P0</td>
<td>打通上传闭环</td>
<td>上传后生成 <code>doc_id</code> 和 <code>task_id</code></td>
</tr>
<tr>
<td>P0</td>
<td>补充实际开发日志</td>
<td>每次开发记录输入、输出、问题、下一步</td>
</tr>
</tbody></table>
<h2 id="7-当前记录存在的问题"><a href="#7-当前记录存在的问题" class="headerlink" title="7. 当前记录存在的问题"></a>7. 当前记录存在的问题</h2><p>目前你的资料质量其实不低，但“开发记录”层面有几个明显问题：</p>
<ul>
<li>学习笔记很多，但实际开发日志没有集中沉淀</li>
<li>方案很完整，但还没有对应的阶段性验收记录</li>
<li>缺少“今天做了什么、结果如何、卡在哪里、下一步做什么”的连续记录</li>
<li>缺少最小闭环的里程碑标记，后续很容易出现学习很多但难以判断工程进展的情况</li>
</ul>
<h2 id="8-后续建议的记录方式"><a href="#8-后续建议的记录方式" class="headerlink" title="8. 后续建议的记录方式"></a>8. 后续建议的记录方式</h2><p>后续建议把这份文件当成总入口，只保留高价值信息：</p>
<ul>
<li>当前阶段</li>
<li>最近里程碑</li>
<li>已完成事项</li>
<li>当前阻塞点</li>
<li>下一步</li>
</ul>
<p>具体每天或每次开发的细节，可以按下面模板追加。</p>
<h2 id="9-开发日志模板"><a href="#9-开发日志模板" class="headerlink" title="9. 开发日志模板"></a>9. 开发日志模板</h2><figure class="highlight md"><table><tr><td class="code"><pre><span class="line"><span class="section">## YYYY-MM-DD</span></span><br><span class="line"></span><br><span class="line"><span class="section">### 今日目标</span></span><br><span class="line"><span class="bullet">-</span></span><br><span class="line"></span><br><span class="line"><span class="section">### 已完成</span></span><br><span class="line"><span class="bullet">-</span></span><br><span class="line"></span><br><span class="line"><span class="section">### 产出</span></span><br><span class="line"><span class="bullet">-</span> 文档：</span><br><span class="line"><span class="bullet">-</span> 代码：</span><br><span class="line"><span class="bullet">-</span> 接口：</span><br><span class="line"><span class="bullet">-</span> 数据表：</span><br><span class="line"></span><br><span class="line"><span class="section">### 遇到的问题</span></span><br><span class="line"><span class="bullet">-</span></span><br><span class="line"></span><br><span class="line"><span class="section">### 解决方式</span></span><br><span class="line"><span class="bullet">-</span></span><br><span class="line"></span><br><span class="line"><span class="section">### 当前结论</span></span><br><span class="line"><span class="bullet">-</span></span><br><span class="line"></span><br><span class="line"><span class="section">### 下一步</span></span><br><span class="line">-</span><br></pre></td></tr></table></figure>

<h2 id="10-一句话总结当前进度"><a href="#10-一句话总结当前进度" class="headerlink" title="10. 一句话总结当前进度"></a>10. 一句话总结当前进度</h2><p>截至 2026-04-20，你的 AI 模型开发项目已经完成了方案设计和核心技术储备，正在从“学明白”转向“做出来”的临界点；下一阶段的关键不是继续扩大学习范围，而是尽快产出第一个可运行的最小闭环，并持续补充真实开发记录。</p>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
      </categories>
  </entry>
  <entry>
    <title>在 8 周内交付可本地部署的 C++ + Python + MySQL + Redis + RAG 科研知识增强生成式 AI 系统</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/deep-research-report/</url>
    <content><![CDATA[<h1 id="在-8-周内交付可本地部署的-C-Python-MySQL-Redis-RAG-科研知识增强生成式-AI-系统"><a href="#在-8-周内交付可本地部署的-C-Python-MySQL-Redis-RAG-科研知识增强生成式-AI-系统" class="headerlink" title="在 8 周内交付可本地部署的 C++ + Python + MySQL + Redis + RAG 科研知识增强生成式 AI 系统"></a>在 8 周内交付可本地部署的 C++ + Python + MySQL + Redis + RAG 科研知识增强生成式 AI 系统</h1><h2 id="执行摘要"><a href="#执行摘要" class="headerlink" title="执行摘要"></a>执行摘要</h2><p>本报告给出一套可执行的 <strong>8 周单人开发与交付流程</strong>，目标是在本地单机完成一个“科研知识增强生成式 AI 系统”，支持 <strong>文档上传→异步处理→FAISS 检索→多轮问答→引用返回→会话记忆→流式输出（SSE&#x2F;WebSocket）</strong>，并具备可演示、可复现、可维护的工程结构。整体采用 <strong>双服务架构</strong>：C++（Drogon）负责对外 API、会话&#x2F;上下文管理、MySQL 持久化与 Redis 状态层；Python（FastAPI + Celery）负责文档处理与 RAG 推理任务编排。该分层能同时满足“工程化交付”和“你希望把 C++ 融入主线”的目标，同时避免把模型生态强行搬到 C++ 导致工期失控。Drogon 作为高性能 C++ Web 框架具备异步特性与会话支持，适合作为网关与业务层。</p>
<p>关键技术决策如下（每项附理由与替代方案）：</p>
<table>
<thead>
<tr>
<th>决策</th>
<th>选择</th>
<th>理由（简短）</th>
<th>替代方案（简短）</th>
</tr>
</thead>
<tbody><tr>
<td>服务分层</td>
<td>C++ Gateway + Python RAG Service</td>
<td>Python 负责模型链路生态最成熟；C++ 负责高并发 API、状态与上下文治理，兼顾工程价值与完成度</td>
<td>全 Python（更快但 C++ 价值弱）；全 C++（工期和生态风险高）</td>
</tr>
<tr>
<td>异步任务</td>
<td>Celery + Redis（broker&#x2F;backend）</td>
<td>Celery 支持任务状态、进度更新与工作流编排；Redis 可同时作为 broker 与 backend，栈更简单</td>
<td>RQ（更轻，但生态与状态&#x2F;工作流弱）</td>
</tr>
<tr>
<td>向量索引</td>
<td>FAISS（本地文件索引）</td>
<td>单机部署最强性价比；官方推荐 Conda 安装，成熟稳定</td>
<td>Milvus&#x2F;Weaviate（重）；pgvector（DB 压力大）</td>
</tr>
<tr>
<td>流式输出</td>
<td>SSE（对外）+ Redis Streams（跨服务 token 传递）</td>
<td>SSE 是标准化的 server→client 流式协议，适合 AI chat streaming；Redis Streams 提供追加日志式传递（XADD&#x2F;XREADGROUP）可解耦推理与网关</td>
<td>WebSocket（双向更复杂，适合交互）；网关直连 Python 流（Drogon HTTP client 读分块能力存在现实限制与争议）</td>
</tr>
<tr>
<td>嵌入与重排</td>
<td>BGE &#x2F; E5 作为 embedding；BGE reranker &#x2F; Cross-Encoder 作为重排</td>
<td>RAG 范式用“检索+生成”解决知识更新与可追溯引用；Cross-Encoder rerank 能显著提升相关性</td>
<td>仅 embedding 不 rerank（质量下降）；BM25（可加作融合，但需额外工程）</td>
</tr>
</tbody></table>
<h3 id="第一周按天计划（2026-03-16-～-2026-03-22，Asia-Tokyo）"><a href="#第一周按天计划（2026-03-16-～-2026-03-22，Asia-Tokyo）" class="headerlink" title="第一周按天计划（2026-03-16 ～ 2026-03-22，Asia&#x2F;Tokyo）"></a>第一周按天计划（2026-03-16 ～ 2026-03-22，Asia&#x2F;Tokyo）</h3><blockquote>
<p>负责人假设：你（单人）。原则：<strong>每天至少产出“可运行的最小增量”</strong>（可启动、可调用、可观察日志&#x2F;状态）。</p>
</blockquote>
<table>
<thead>
<tr>
<th>日期</th>
<th>目标</th>
<th align="right">任务拆解</th>
<th align="right">预估工时</th>
<th align="right">优先级</th>
<th>当日交付物</th>
</tr>
</thead>
<tbody><tr>
<td>03&#x2F;16（周一）</td>
<td>项目骨架 + 本地依赖一键启动</td>
<td align="right">初始化 mono-repo；写 docker-compose（MySQL&#x2F;Redis）；写 <code>.env.example</code>；定义端口与网络；制定命名规范与目录结构；写最小 README</td>
<td align="right">6h</td>
<td align="right">P0</td>
<td><code>docker compose up -d mysql redis</code> 可用；README v0.1</td>
</tr>
<tr>
<td>03&#x2F;17（周二）</td>
<td>C++ Gateway 最小可运行</td>
<td align="right">Drogon 工程创建；加载 config.json；实现 <code>/health</code>；接 MySQL（DbClient）最小查询；接 Redis（redis-plus-plus）最小 set&#x2F;get</td>
<td align="right">7h</td>
<td align="right">P0</td>
<td>Gateway 容器&#x2F;二进制可启动；健康检查通过</td>
</tr>
<tr>
<td>03&#x2F;18（周三）</td>
<td>Python 服务骨架 + Celery worker 跑通</td>
<td align="right">FastAPI <code>/internal/health</code>；Celery app 初始化（Redis broker&#x2F;backend）；定义 demo task；任务状态查询接口</td>
<td align="right">6h</td>
<td align="right">P0</td>
<td><code>rag_api</code> + <code>celery_worker</code> 启动；任务可异步执行并查询状态</td>
</tr>
<tr>
<td>03&#x2F;19（周四）</td>
<td>文档上传闭环 v1</td>
<td align="right">Gateway：<code>POST /v1/documents</code>（multipart）保存文件；调用 Python <code>/internal/jobs/ingest</code> 提交任务；MySQL 写 documents&#x2F;tasks；Redis 写任务缓存</td>
<td align="right">7h</td>
<td align="right">P0</td>
<td>上传后返回 <code>doc_id + task_id</code>；可查询任务状态</td>
</tr>
<tr>
<td>03&#x2F;20（周五）</td>
<td>文档解析与切片（异步）</td>
<td align="right">Python Celery ingest：解析 txt&#x2F;md；pdf 先用简化策略（后续增强）；切片策略 v1；写 doc_chunks；更新任务进度</td>
<td align="right">7h</td>
<td align="right">P0</td>
<td><code>documents.status</code> 从 <code>processing→ready</code>（文本类）；chunks 入库</td>
</tr>
<tr>
<td>03&#x2F;21（周六）</td>
<td>Embedding + FAISS 索引 v1</td>
<td align="right">Python：embedding（Sentence-Transformers&#x2F;FlagEmbedding）；建立 FAISS IndexFlat*；写 <code>chunk_id↔vector_id</code> 映射；索引文件落盘</td>
<td align="right">6h</td>
<td align="right">P0</td>
<td>可对单文档检索 top-k；索引版本化</td>
</tr>
<tr>
<td>03&#x2F;22（周日）</td>
<td>首个可问答 MVP（非流式或简易流式）</td>
<td align="right">Gateway：创建 session、写 messages；Python：&#x2F;chat job（retrieve→rerank→LLM）；先返回完整 JSON；流式先打通“Redis Streams→SSE”骨架</td>
<td align="right">8h</td>
<td align="right">P0</td>
<td>端到端：上传→索引→提问→返回答案+引用（v0）</td>
</tr>
</tbody></table>
<h3 id="首个可运行-MVP-的快速启动命令示例"><a href="#首个可运行-MVP-的快速启动命令示例" class="headerlink" title="首个可运行 MVP 的快速启动命令示例"></a>首个可运行 MVP 的快速启动命令示例</h3><blockquote>
<p>目标：<strong>本地单机</strong>一键起 MySQL、Redis、Python、Worker、C++ Gateway（以及可选本地 LLM Server）。MySQL 官方镜像支持通过环境变量初始化账号&#x2F;库。<br>Docker Compose 可用 <code>depends_on</code> + <code>healthcheck</code> 控制启动顺序与就绪判断。</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1) 准备环境</span></span><br><span class="line"><span class="built_in">cp</span> .env.example .<span class="built_in">env</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 2) 启动基础依赖（MySQL + Redis）</span></span><br><span class="line">docker compose up -d mysql redis</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3) （可选）启动本地 LLM Server</span></span><br><span class="line"><span class="comment"># 方案 A：llama-cpp-python OpenAI 兼容服务（便于后续切换模型/客户端）</span></span><br><span class="line">docker compose up -d llm_server</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4) 启动 Python RAG API + Celery Worker</span></span><br><span class="line">docker compose up -d --build rag_api celery_worker</span><br><span class="line"></span><br><span class="line"><span class="comment"># 5) 启动 C++ Gateway（Drogon）</span></span><br><span class="line">docker compose up -d --build gateway</span><br><span class="line"></span><br><span class="line"><span class="comment"># 6) 冒烟测试</span></span><br><span class="line">curl -s http://localhost:8080/health</span><br><span class="line">curl -s http://localhost:8000/internal/health</span><br></pre></td></tr></table></figure>

<p>以上示例中 SSE 作为流式输出协议（<code>text/event-stream</code>）能直接服务 AI chat streaming 场景。<br>如你选择 Redis Streams 做跨服务 token 传递，可用 XADD 追加、XREADGROUP（或 XREAD）消费，且命令复杂度与阻塞特性在官方文档可查。</p>
<h2 id="开发计划与里程碑"><a href="#开发计划与里程碑" class="headerlink" title="开发计划与里程碑"></a>开发计划与里程碑</h2><p>下面给出 8 周计划（你单人开发），每周含：目标、任务、预估工时、优先级、里程碑交付物与验收点。建议你严格区分 <strong>P0（必须完成）</strong> 与 <strong>P1（有余力再做）</strong>，避免“功能全做但无闭环”。</p>
<table>
<thead>
<tr>
<th>周期</th>
<th>周目标</th>
<th>任务（浓缩）</th>
<th align="right">预估工时</th>
<th align="right">优先级</th>
<th>里程碑交付物</th>
<th>周末验收标准</th>
</tr>
</thead>
<tbody><tr>
<td>Week 1（03&#x2F;16-03&#x2F;22）</td>
<td>首个端到端 MVP（文档→检索→回答）</td>
<td>见执行摘要日计划；建立骨架；跑通 ingest&#x2F;索引&#x2F;问答</td>
<td align="right">40h</td>
<td align="right">P0</td>
<td><code>v0.1</code> 可用系统</td>
<td>端到端成功 3 次；日志可定位失败原因</td>
</tr>
<tr>
<td>Week 2</td>
<td>引用返回 + MySQL&#x2F;Redis 结构稳固</td>
<td>设计并落库：documents&#x2F;doc_chunks&#x2F;messages&#x2F;tasks&#x2F;citations；实现 citation 生成；完善任务状态</td>
<td align="right">35h</td>
<td align="right">P0</td>
<td>数据层 v1 + 引用 v1</td>
<td>每条回答返回 ≥1 条引用（chunk_id+snippet）</td>
</tr>
<tr>
<td>Week 3</td>
<td>流式输出闭环（SSE）</td>
<td>Redis Streams token 通道；Gateway SSE endpoint；断线重连（Last-Event-ID 最小支持）；取消生成</td>
<td align="right">35h</td>
<td align="right">P0</td>
<td>Streaming v1</td>
<td>前端&#x2F;命令行可看到 token 流；中断可恢复&#x2F;结束</td>
</tr>
<tr>
<td>Week 4</td>
<td>会话记忆（短期&#x2F;长期）与上下文预算</td>
<td>Redis 缓存最近 N 轮；MySQL 保存会话 summary；上下文裁剪；定期摘要任务</td>
<td align="right">35h</td>
<td align="right">P0</td>
<td>Context Manager v1</td>
<td>长会话（≥50 轮）仍能稳定回答且不丢“系统约束”</td>
</tr>
<tr>
<td>Week 5</td>
<td>文档处理增强与质量提升</td>
<td>PDF 正式解析（引入更可靠库）；分块策略优化；加入 rerank（Cross-Encoder &#x2F; bge-reranker）</td>
<td align="right">35h</td>
<td align="right">P0</td>
<td>RAG 质量 v1</td>
<td>10 个问题中命中引用相关 chunk ≥7（主观验收+日志）</td>
</tr>
<tr>
<td>Week 6</td>
<td>工程化：鉴权、限流、缓存、配置</td>
<td>API Key&#x2F;Session；Redis 限流；缓存（embedding&#x2F;query）；配置文件化（Drogon config）</td>
<td align="right">30h</td>
<td align="right">P0</td>
<td>可对外演示版 beta</td>
<td>恶意请求（高频）被限流；配置可通过文件修改</td>
</tr>
<tr>
<td>Week 7</td>
<td>Docker 化交付与测试体系</td>
<td>完整 docker-compose；健康检查；pytest + C++ 单测&#x2F;集成测；E2E 脚本</td>
<td align="right">35h</td>
<td align="right">P0</td>
<td><code>v1.0-rc</code></td>
<td><code>docker compose up --build</code> 15 分钟内完成可用系统</td>
</tr>
<tr>
<td>Week 8</td>
<td>打磨与演示包装</td>
<td>README&#x2F;架构图；Demo 脚本与录屏脚本；性能与风险说明；不足与 roadmap</td>
<td align="right">25h</td>
<td align="right">P1</td>
<td><code>v1.0</code> release</td>
<td>演示 10 分钟无故障；关键卖点可复述</td>
</tr>
</tbody></table>
<p>补充建议：若你只有 6 周窗口，可以合并 Week 6～7（降低测试深度、先做集成测），但不建议牺牲 Week 3～4（流式与会话记忆是你差异化点，也是你对 “AI 上下文丢失” 的正面回应）。</p>
<h2 id="目标、范围与MVP验收标准"><a href="#目标、范围与MVP验收标准" class="headerlink" title="目标、范围与MVP验收标准"></a>目标、范围与MVP验收标准</h2><h3 id="项目目标"><a href="#项目目标" class="headerlink" title="项目目标"></a>项目目标</h3><p>在本地单机部署一个科研知识增强生成式 AI 系统，满足：</p>
<ul>
<li><strong>私有文档知识库</strong>：上传文档→解析切片→向量索引（FAISS）→可检索。</li>
<li><strong>RAG 问答</strong>：基于检索结果生成回答，并返回可追溯引用（provenance）。RAG 的“检索 + 生成”范式被提出用于知识密集任务并强调可追溯与知识更新。</li>
<li><strong>异步处理</strong>：文档 ingest、索引构建、生成任务长耗时，需要任务队列与状态查询；Celery 支持任务状态与进度更新。</li>
<li><strong>多轮会话</strong>：支持会话历史与“会话记忆”（短期+长期摘要），并解决长上下文丢失风险（通过外部记忆与预算管理）。</li>
<li><strong>流式输出</strong>：回答过程可 SSE 流式推送（AI chat streaming 常用）。</li>
<li><strong>工程化交付</strong>：Docker &#x2F; docker-compose 一键拉起，可健康检查、可测试、可演示。</li>
</ul>
<h3 id="MVP-功能清单与验收标准"><a href="#MVP-功能清单与验收标准" class="headerlink" title="MVP 功能清单与验收标准"></a>MVP 功能清单与验收标准</h3><blockquote>
<p>P0：1-2 个月必须完成；P1：做得出来就加分但不能拖主线。</p>
</blockquote>
<table>
<thead>
<tr>
<th>功能</th>
<th>优先级</th>
<th>说明</th>
<th>验收标准（可测试）</th>
</tr>
</thead>
<tbody><tr>
<td>本地部署（docker-compose）</td>
<td>P0</td>
<td>一键启动 MySQL、Redis、Python、Worker、C++ Gateway（可选 LLM）</td>
<td><code>docker compose up --build -d</code> 后：<code>/health</code>、<code>/internal/health</code> 返回 200；依赖可通过 <code>depends_on+healthcheck</code> 正常就绪</td>
</tr>
<tr>
<td>文档上传</td>
<td>P0</td>
<td>支持 PDF&#x2F;MD&#x2F;TXT（起步可先 TXT&#x2F;MD）</td>
<td>上传返回 <code>doc_id</code>；文件落盘；DB 记录 <code>documents.status=uploaded/processing</code></td>
</tr>
<tr>
<td>异步 ingest</td>
<td>P0</td>
<td>解析→切片→写库→embedding→FAISS 索引</td>
<td>返回 <code>task_id</code>；<code>GET /tasks/&#123;id&#125;</code> 可见状态从 PENDING&#x2F;STARTED→SUCCESS&#x2F;FAILURE；支持进度 meta（update_state）</td>
</tr>
<tr>
<td>FAISS 检索</td>
<td>P0</td>
<td>至少 IndexFlat（L2 或 IP）实现 top-k</td>
<td>给定 query 返回 top-k chunk（含 chunk_id 与 score）；索引文件可在本地重载</td>
</tr>
<tr>
<td>Rerank（重排）</td>
<td>P0</td>
<td>Cross-Encoder rerank top-k 提升相关性</td>
<td>对同一 query：启用 rerank 后 top-3 相关性主观提升；日志记录 rerank 前后排名；Cross-Encoder 用于 rerank 是官方推荐范式</td>
</tr>
<tr>
<td>LLM 生成回答</td>
<td>P0</td>
<td>本地开源 LLM，通过统一接口调用（OpenAI-like）</td>
<td>给定检索上下文返回回答文本；若用 llama-cpp-python，可用 OpenAI 兼容 server 快速接入</td>
</tr>
<tr>
<td>引用返回（Provenance）</td>
<td>P0</td>
<td>答案附带引用 chunk（doc、chunk、snippet）</td>
<td>每条 assistant 消息保存 citations；响应 JSON 至少含 <code>citations[]</code>（doc_id、chunk_id、offset&#x2F;snippet）</td>
</tr>
<tr>
<td>多轮会话</td>
<td>P0</td>
<td>session&#x2F;messages 持久化；支持继续对话</td>
<td><code>GET /sessions/&#123;sid&#125;/messages</code> 返回历史；会话可恢复</td>
</tr>
<tr>
<td>会话记忆（短期+长期）</td>
<td>P0</td>
<td>Redis 缓存最近 N 轮；MySQL 保存会话 summary；上下文裁剪</td>
<td>在 ≥50 轮对话中仍能保持系统约束（如“必须引用”）不丢失；日志展示上下文预算分配</td>
</tr>
<tr>
<td>流式输出 SSE</td>
<td>P0</td>
<td>token 流式下发；带完成&#x2F;错误事件</td>
<td>客户端能逐步收到 token；<code>event: done</code> 或最终 <code>data</code> 标记结束；SSE 字段（data&#x2F;event&#x2F;id&#x2F;retry）符合规范</td>
</tr>
<tr>
<td>基础鉴权与限流</td>
<td>P1</td>
<td>API Key 或 session token；Redis 计数器限流</td>
<td>对同一 key 高频请求被 429；Redis TTL&#x2F;EXPIRE 生效</td>
</tr>
<tr>
<td>取消生成</td>
<td>P1</td>
<td>revoke&#x2F;stop 任务或标记取消</td>
<td>调用取消接口后 SSE 返回 <code>event: cancelled</code>；任务状态进入 REVOKED&#x2F;自定义 cancelled</td>
</tr>
</tbody></table>
<h2 id="技术架构与数据流"><a href="#技术架构与数据流" class="headerlink" title="技术架构与数据流"></a>技术架构与数据流</h2><h3 id="总体架构图（C-网关-Python-RAG-Celery-Redis-Streams）"><a href="#总体架构图（C-网关-Python-RAG-Celery-Redis-Streams）" class="headerlink" title="总体架构图（C++ 网关 + Python RAG + Celery + Redis Streams）"></a>总体架构图（C++ 网关 + Python RAG + Celery + Redis Streams）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">flowchart LR</span><br><span class="line">  U[用户/前端&lt;br/&gt;Web/CLI] --&gt;|HTTP JSON / SSE| G[C++ Gateway&lt;br/&gt;Drogon]</span><br><span class="line"></span><br><span class="line">  G --&gt;|ORM/SQL| M[(MySQL&lt;br/&gt;metadata/messages)]</span><br><span class="line">  G --&gt;|cache/limit/session| R[(Redis)]</span><br><span class="line"></span><br><span class="line">  G --&gt;|内部HTTP: 提交任务/查状态| P[Python Control API&lt;br/&gt;FastAPI]</span><br><span class="line">  P --&gt;|enqueue| C[Celery Worker&lt;br/&gt;Redis broker/backend]</span><br><span class="line">  C --&gt;|写入chunks/状态| M</span><br><span class="line">  C --&gt;|写入向量索引| F[(FAISS Index Files)]</span><br><span class="line">  C --&gt;|XADD token流| RS[(Redis Streams&lt;br/&gt;chat:stream:*)]</span><br><span class="line"></span><br><span class="line">  G --&gt;|XREAD(XREADGROUP)| RS</span><br><span class="line">  C --&gt;|调用生成| L[本地LLM服务&lt;br/&gt;llama.cpp/llama-cpp-python]</span><br></pre></td></tr></table></figure>

<p>该设计刻意让“跨服务流式 token”走 Redis Streams（XADD&#x2F;XREADGROUP），避免网关必须从 Python 通过 HTTP 客户端读取分块响应；在 Drogon 生态中，HTTP client 读取分块流的诉求长期存在讨论，工程上用 Redis Streams 解耦可显著降低不确定性。</p>
<h3 id="模块关系图（推荐的代码分层）"><a href="#模块关系图（推荐的代码分层）" class="headerlink" title="模块关系图（推荐的代码分层）"></a>模块关系图（推荐的代码分层）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">flowchart TB</span><br><span class="line">  subgraph Cpp[C++ Gateway / Drogon]</span><br><span class="line">    A1[HTTP API Controllers]</span><br><span class="line">    A2[Auth &amp; RateLimit&lt;br/&gt;Redis]</span><br><span class="line">    A3[Session &amp; Context Manager&lt;br/&gt;Redis+MySQL]</span><br><span class="line">    A4[Storage Repos&lt;br/&gt;MySQL ORM]</span><br><span class="line">    A5[SSE Stream Endpoint&lt;br/&gt;Redis Streams]</span><br><span class="line">    A6[Python Client&lt;br/&gt;HTTP JSON]</span><br><span class="line">    A1 --&gt; A2 --&gt; A3 --&gt; A4</span><br><span class="line">    A1 --&gt; A6</span><br><span class="line">    A1 --&gt; A5</span><br><span class="line">  end</span><br><span class="line"></span><br><span class="line">  subgraph Py[Python RAG Service / FastAPI + Celery]</span><br><span class="line">    B1[FastAPI Internal API]</span><br><span class="line">    B2[Celery Tasks]</span><br><span class="line">    B3[Chunker/Parser]</span><br><span class="line">    B4[Embedding]</span><br><span class="line">    B5[FAISS Indexer]</span><br><span class="line">    B6[Reranker]</span><br><span class="line">    B7[LLM Client]</span><br><span class="line">    B1 --&gt; B2</span><br><span class="line">    B2 --&gt; B3 --&gt; B4 --&gt; B5</span><br><span class="line">    B2 --&gt; B6 --&gt; B7</span><br><span class="line">  end</span><br><span class="line"></span><br><span class="line">  Cpp --&gt; Py</span><br></pre></td></tr></table></figure>

<p>Drogon 支持会话（Session）与配置文件加载，也支持文件上传解析（MultiPartParser&#x2F;HttpFile），适合承载上传入口与会话治理。</p>
<h3 id="异步任务状态流（文档-ingest-与生成）"><a href="#异步任务状态流（文档-ingest-与生成）" class="headerlink" title="异步任务状态流（文档 ingest 与生成）"></a>异步任务状态流（文档 ingest 与生成）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">stateDiagram-v2</span><br><span class="line">  [*] --&gt; PENDING</span><br><span class="line">  PENDING --&gt; STARTED</span><br><span class="line">  STARTED --&gt; PROGRESS</span><br><span class="line">  PROGRESS --&gt; SUCCESS</span><br><span class="line">  PROGRESS --&gt; FAILURE</span><br><span class="line">  STARTED --&gt; FAILURE</span><br><span class="line"></span><br><span class="line">  note right of PROGRESS</span><br><span class="line">    Celery 支持自定义状态与 meta</span><br><span class="line">    update_state(state=&#x27;PROGRESS&#x27;, meta=...)</span><br><span class="line">  end note</span><br></pre></td></tr></table></figure>

<p>Celery 官方示例支持在任务内通过 <code>update_state</code> 上报自定义进度状态与 meta，这使“上传→处理中→就绪”可被 API 查询并展示。</p>
<h2 id="接口协议与API清单"><a href="#接口协议与API清单" class="headerlink" title="接口协议与API清单"></a>接口协议与API清单</h2><h3 id="C-↔-Python-内部接口协议"><a href="#C-↔-Python-内部接口协议" class="headerlink" title="C++ ↔ Python 内部接口协议"></a>C++ ↔ Python 内部接口协议</h3><p><strong>选择：HTTP + JSON（内部私网&#x2F;同机容器网络）</strong><br>理由：实现与调试成本最低（curl 直接测）；与 Celery 解耦（Python 负责 enqueue 与任务状态封装）；避免跨语言复刻 Celery broker 协议。Celery 消息序列化虽默认 JSON，但跨语言直接写入 broker 需要匹配协议细节，性价比不高。<br>替代：gRPC（更强类型&#x2F;性能更好，但 proto 与流式更费时）；直接共享 broker 协议（风险大且难测）。</p>
<h4 id="内部接口：提交文档-ingest-任务"><a href="#内部接口：提交文档-ingest-任务" class="headerlink" title="内部接口：提交文档 ingest 任务"></a>内部接口：提交文档 ingest 任务</h4><ul>
<li><strong>POST</strong> <code>/internal/jobs/ingest</code></li>
<li>Content-Type: <code>application/json</code></li>
</ul>
<p>请求 JSON（示例）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;doc_id&quot;</span><span class="punctuation">:</span> <span class="number">123</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;storage_path&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/data/uploads/2026-03-19/report.pdf&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;mime&quot;</span><span class="punctuation">:</span> <span class="string">&quot;application/pdf&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;options&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;chunk_size&quot;</span><span class="punctuation">:</span> <span class="number">800</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;chunk_overlap&quot;</span><span class="punctuation">:</span> <span class="number">120</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;language&quot;</span><span class="punctuation">:</span> <span class="string">&quot;zh&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>响应 JSON（示例）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;task_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;celery-uuid-xxx&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;status_url&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/internal/tasks/celery-uuid-xxx&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h4 id="内部接口：提交-chat-completion-任务（支持流式）"><a href="#内部接口：提交-chat-completion-任务（支持流式）" class="headerlink" title="内部接口：提交 chat completion 任务（支持流式）"></a>内部接口：提交 chat completion 任务（支持流式）</h4><ul>
<li><strong>POST</strong> <code>/internal/jobs/chat</code></li>
<li>Content-Type: <code>application/json</code></li>
</ul>
<p>请求 JSON（示例）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;session_id&quot;</span><span class="punctuation">:</span> <span class="number">456</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;message_id&quot;</span><span class="punctuation">:</span> <span class="number">789</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;query&quot;</span><span class="punctuation">:</span> <span class="string">&quot;请总结文档的实验设置，并指出可能的误差来源。&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;retrieval&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;top_k&quot;</span><span class="punctuation">:</span> <span class="number">30</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;rerank_top_k&quot;</span><span class="punctuation">:</span> <span class="number">10</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;stream&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;redis_stream_key&quot;</span><span class="punctuation">:</span> <span class="string">&quot;chat:stream:789&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>响应 JSON（示例）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;task_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;celery-uuid-yyy&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;stream_key&quot;</span><span class="punctuation">:</span> <span class="string">&quot;chat:stream:789&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;status_url&quot;</span><span class="punctuation">:</span> <span class="string">&quot;/internal/tasks/celery-uuid-yyy&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h4 id="内部接口：查询任务状态"><a href="#内部接口：查询任务状态" class="headerlink" title="内部接口：查询任务状态"></a>内部接口：查询任务状态</h4><ul>
<li><strong>GET</strong> <code>/internal/tasks/&#123;task_id&#125;</code></li>
</ul>
<p>响应（示例）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;task_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;celery-uuid-yyy&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;state&quot;</span><span class="punctuation">:</span> <span class="string">&quot;PROGRESS&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;meta&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span> <span class="attr">&quot;progress&quot;</span><span class="punctuation">:</span> <span class="number">60</span><span class="punctuation">,</span> <span class="attr">&quot;stage&quot;</span><span class="punctuation">:</span> <span class="string">&quot;embedding&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;updated_at&quot;</span><span class="punctuation">:</span> <span class="string">&quot;2026-03-20T13:31:00+09:00&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>Celery 任务状态与结果查询是其标准能力；内置状态包括 PENDING&#x2F;STARTED&#x2F;RETRY&#x2F;FAILURE&#x2F;SUCCESS，自定义状态可插入在 STARTED 与 SUCCESS&#x2F;FAILURE 之间。</p>
<h3 id="对外-API（C-Gateway）列表"><a href="#对外-API（C-Gateway）列表" class="headerlink" title="对外 API（C++ Gateway）列表"></a>对外 API（C++ Gateway）列表</h3><table>
<thead>
<tr>
<th>类别</th>
<th>Method</th>
<th>Path</th>
<th>说明</th>
<th>鉴权</th>
</tr>
</thead>
<tbody><tr>
<td>健康检查</td>
<td>GET</td>
<td><code>/health</code></td>
<td>网关健康</td>
<td>无</td>
</tr>
<tr>
<td>上传文档</td>
<td>POST</td>
<td><code>/v1/documents</code></td>
<td>multipart 上传，返回 doc_id + ingest_task_id</td>
<td>API Key（P1）</td>
</tr>
<tr>
<td>查询文档</td>
<td>GET</td>
<td><code>/v1/documents/&#123;doc_id&#125;</code></td>
<td>文档元信息与状态</td>
<td>API Key（P1）</td>
</tr>
<tr>
<td>查询任务</td>
<td>GET</td>
<td><code>/v1/tasks/&#123;task_id&#125;</code></td>
<td>透传&#x2F;聚合 Celery 任务状态</td>
<td>API Key（P1）</td>
</tr>
<tr>
<td>创建会话</td>
<td>POST</td>
<td><code>/v1/sessions</code></td>
<td>新建 session</td>
<td>API Key（P1）</td>
</tr>
<tr>
<td>发起问答</td>
<td>POST</td>
<td><code>/v1/sessions/&#123;sid&#125;/messages</code></td>
<td>写入 user 消息 + 触发生成任务</td>
<td>API Key（P1）</td>
</tr>
<tr>
<td>拉取历史</td>
<td>GET</td>
<td><code>/v1/sessions/&#123;sid&#125;/messages</code></td>
<td>返回消息列表</td>
<td>API Key（P1）</td>
</tr>
<tr>
<td>流式输出</td>
<td>GET</td>
<td><code>/v1/sessions/&#123;sid&#125;/messages/&#123;mid&#125;/stream</code></td>
<td>SSE 输出 token 与最终结果</td>
<td>API Key（P1）</td>
</tr>
<tr>
<td>取消生成</td>
<td>POST</td>
<td><code>/v1/sessions/&#123;sid&#125;/messages/&#123;mid&#125;/cancel</code></td>
<td>中止任务&#x2F;标记取消</td>
<td>API Key（P1）</td>
</tr>
</tbody></table>
<h3 id="SSE-事件格式（对外流式输出）"><a href="#SSE-事件格式（对外流式输出）" class="headerlink" title="SSE 事件格式（对外流式输出）"></a>SSE 事件格式（对外流式输出）</h3><p>SSE 是标准的 HTTP 流式文本协议，事件由 <code>data/event/id/retry</code> 等字段组成，适用于 AI chat streaming。</p>
<p>建议你的 SSE 负载统一为 JSON（便于前端解析），定义事件类型：</p>
<ul>
<li><code>event: token</code>：增量 token</li>
<li><code>event: done</code>：最终完成（含 citations、usage、trace）</li>
<li><code>event: error</code>：失败信息（含可重试建议）</li>
</ul>
<p>示例（概念）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">event: token</span><br><span class="line">data: &#123;&quot;delta&quot;:&quot;实验设置包括...&quot;,&quot;message_id&quot;:789&#125;</span><br><span class="line"></span><br><span class="line">event: done</span><br><span class="line">data: &#123;&quot;message_id&quot;:789,&quot;citations&quot;:[&#123;&quot;doc_id&quot;:123,&quot;chunk_id&quot;:55&#125;],&quot;finish_reason&quot;:&quot;stop&quot;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="数据持久化与缓存设计"><a href="#数据持久化与缓存设计" class="headerlink" title="数据持久化与缓存设计"></a>数据持久化与缓存设计</h2><h3 id="MySQL-表设计（ER-表格-约束说明）"><a href="#MySQL-表设计（ER-表格-约束说明）" class="headerlink" title="MySQL 表设计（ER 表格 + 约束说明）"></a>MySQL 表设计（ER 表格 + 约束说明）</h3><p>设计原则：</p>
<ul>
<li>使用 InnoDB（事务与外键约束更适合业务一致性）。MySQL 的外键与约束在 InnoDB 下可用。</li>
<li>每张表定义主键；InnoDB 使用 PRIMARY KEY 作为 clustered index，有利于常见查询与写入性能。</li>
<li>外键引用列需有合适索引；MySQL 文档对外键与索引要求有明确说明。</li>
</ul>
<h4 id="核心表（建议-v1）"><a href="#核心表（建议-v1）" class="headerlink" title="核心表（建议 v1）"></a>核心表（建议 v1）</h4><table>
<thead>
<tr>
<th>表</th>
<th>用途</th>
<th>关键字段（建议）</th>
<th>索引与约束（建议）</th>
</tr>
</thead>
<tbody><tr>
<td><code>users</code></td>
<td>用户</td>
<td><code>id</code>(PK), <code>username</code>(UNIQUE), <code>password_hash</code>, <code>created_at</code></td>
<td><code>UNIQUE(username)</code></td>
</tr>
<tr>
<td><code>sessions</code></td>
<td>会话</td>
<td><code>id</code>(PK), <code>user_id</code>(FK), <code>title</code>, <code>summary</code>, <code>created_at</code>, <code>updated_at</code></td>
<td><code>INDEX(user_id, updated_at)</code>；FK→users.id</td>
</tr>
<tr>
<td><code>messages</code></td>
<td>消息</td>
<td><code>id</code>(PK), <code>session_id</code>(FK), <code>role</code>, <code>content</code>, <code>status</code>, <code>created_at</code></td>
<td><code>INDEX(session_id, created_at)</code>；FK→sessions.id</td>
</tr>
<tr>
<td><code>documents</code></td>
<td>文档元信息</td>
<td><code>id</code>(PK), <code>user_id</code>(FK), <code>filename</code>, <code>mime</code>, <code>sha256</code>, <code>size_bytes</code>, <code>storage_path</code>, <code>status</code>, <code>created_at</code></td>
<td><code>UNIQUE(user_id, sha256)</code>；<code>INDEX(status, created_at)</code></td>
</tr>
<tr>
<td><code>doc_chunks</code></td>
<td>文档切片</td>
<td><code>id</code>(PK), <code>doc_id</code>(FK), <code>chunk_index</code>, <code>text</code>, <code>tokens_est</code>, <code>vector_id</code></td>
<td><code>UNIQUE(doc_id, chunk_index)</code>；<code>INDEX(doc_id)</code></td>
</tr>
<tr>
<td><code>vector_indexes</code></td>
<td>索引版本</td>
<td><code>id</code>(PK), <code>scope</code>(如 user&#x2F;kb), <code>faiss_path</code>, <code>dim</code>, <code>metric</code>, <code>version</code>, <code>updated_at</code></td>
<td><code>UNIQUE(scope, version)</code></td>
</tr>
<tr>
<td><code>tasks</code></td>
<td>异步任务</td>
<td><code>id</code>(PK), <code>celery_task_id</code>(UNIQUE), <code>type</code>, <code>entity_type</code>, <code>entity_id</code>, <code>state</code>, <code>meta_json</code>, <code>error</code>, <code>created_at</code>, <code>updated_at</code></td>
<td><code>INDEX(entity_type, entity_id)</code></td>
</tr>
<tr>
<td><code>citations</code></td>
<td>引用记录</td>
<td><code>id</code>(PK), <code>message_id</code>(FK), <code>doc_id</code>(FK), <code>chunk_id</code>(FK), <code>rank</code>, <code>score</code>, <code>snippet</code></td>
<td><code>INDEX(message_id, rank)</code></td>
</tr>
</tbody></table>
<blockquote>
<p>如果你只做单用户 demo，可暂时弱化 <code>users</code> 与权限，但建议保留字段，为后续扩展铺路。</p>
</blockquote>
<h4 id="ER-关系图（辅助）"><a href="#ER-关系图（辅助）" class="headerlink" title="ER 关系图（辅助）"></a>ER 关系图（辅助）</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">erDiagram</span><br><span class="line">  users ||--o&#123; sessions : owns</span><br><span class="line">  sessions ||--o&#123; messages : contains</span><br><span class="line">  users ||--o&#123; documents : uploads</span><br><span class="line">  documents ||--o&#123; doc_chunks : splits</span><br><span class="line">  messages ||--o&#123; citations : cites</span><br><span class="line">  documents ||--o&#123; citations : referenced_by</span><br><span class="line">  doc_chunks ||--o&#123; citations : referenced_by</span><br></pre></td></tr></table></figure>

<h3 id="Redis-键设计表格（缓存-限流-流式通道）"><a href="#Redis-键设计表格（缓存-限流-流式通道）" class="headerlink" title="Redis 键设计表格（缓存&#x2F;限流&#x2F;流式通道）"></a>Redis 键设计表格（缓存&#x2F;限流&#x2F;流式通道）</h3><p>Redis 设计原则：</p>
<ul>
<li><strong>高频短期状态</strong>放 Redis；需过期的 key 用 EXPIRE&#x2F;SET EX，Redis 官方命令支持设置超时并自动删除。</li>
<li>Streams 用作“追加日志式”的 token 通道，producer 用 XADD，consumer 用 XREAD&#x2F;XREADGROUP（支持阻塞读取）。</li>
</ul>
<table>
<thead>
<tr>
<th>Key Pattern</th>
<th>类型</th>
<th align="right">TTL</th>
<th>写入方</th>
<th>读取方</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td><code>auth:token:&#123;token&#125;</code></td>
<td>string</td>
<td align="right">24h</td>
<td>Gateway</td>
<td>Gateway</td>
<td>API Key &#x2F; session token 映射 user_id（P1）</td>
</tr>
<tr>
<td><code>rl:&#123;user_id&#125;:&#123;route&#125;:&#123;window&#125;</code></td>
<td>string</td>
<td align="right">window 秒</td>
<td>Gateway</td>
<td>Gateway</td>
<td>限流计数器（P1）</td>
</tr>
<tr>
<td><code>sess:recent:&#123;session_id&#125;</code></td>
<td>list</td>
<td align="right">7d（可选）</td>
<td>Gateway</td>
<td>Gateway</td>
<td>最近 N 条消息的 compact JSON（短期记忆）</td>
</tr>
<tr>
<td><code>chat:stream:&#123;message_id&#125;</code></td>
<td>stream</td>
<td align="right">1d（完成后设置）</td>
<td>Worker</td>
<td>Gateway SSE</td>
<td>token 流式通道（XADD&#x2F;XREADGROUP）</td>
</tr>
<tr>
<td><code>task:cache:&#123;task_id&#125;</code></td>
<td>hash</td>
<td align="right">15m</td>
<td>Python API</td>
<td>Gateway</td>
<td>任务状态 hot cache（减少打 Celery backend）</td>
</tr>
<tr>
<td><code>lock:doc:&#123;doc_id&#125;</code></td>
<td>string</td>
<td align="right">60s</td>
<td>Worker</td>
<td>Worker</td>
<td>文档 ingest 幂等锁（防重复消费）</td>
</tr>
<tr>
<td><code>cache:embed:q:&#123;hash&#125;</code></td>
<td>string&#x2F;blob</td>
<td align="right">30m</td>
<td>Worker</td>
<td>Worker</td>
<td>query embedding 缓存（P1）</td>
</tr>
</tbody></table>
<h2 id="实现要点与关键代码示例"><a href="#实现要点与关键代码示例" class="headerlink" title="实现要点与关键代码示例"></a>实现要点与关键代码示例</h2><blockquote>
<p>说明：以下代码片段以“可落地”为目标，强调关键点与接口形状；你需要按项目目录与依赖做适配。引用的框架能力来自官方文档&#x2F;示例。</p>
</blockquote>
<h3 id="C-服务（Drogon）实现要点"><a href="#C-服务（Drogon）实现要点" class="headerlink" title="C++ 服务（Drogon）实现要点"></a>C++ 服务（Drogon）实现要点</h3><p><strong>框架选择：Drogon</strong><br>Drogon 是 C++17&#x2F;20 Web 框架，强调异步与高性能；支持会话、配置文件与文件上传解析。</p>
<h4 id="配置文件与启动（config-json-loadConfigFile）"><a href="#配置文件与启动（config-json-loadConfigFile）" class="headerlink" title="配置文件与启动（config.json + loadConfigFile）"></a>配置文件与启动（config.json + loadConfigFile）</h4><p>Drogon 推荐通过配置文件配置监听端口、日志、数据库等，<code>loadConfigFile()</code> 在 <code>run()</code> 前调用。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// main.cc (示意)</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;drogon/drogon.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  drogon::<span class="built_in">app</span>().<span class="built_in">loadConfigFile</span>(<span class="string">&quot;config.json&quot;</span>);</span><br><span class="line">  drogon::<span class="built_in">app</span>().<span class="built_in">run</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="文件上传（MultiPartParser-HttpFile）"><a href="#文件上传（MultiPartParser-HttpFile）" class="headerlink" title="文件上传（MultiPartParser&#x2F;HttpFile）"></a>文件上传（MultiPartParser&#x2F;HttpFile）</h4><p>Drogon Wiki 给出 <code>MultiPartParser</code> 用于解析 multipart 请求、<code>getFiles()</code> 获取上传文件对象。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// DocumentsController::upload (示意)</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">upload</span><span class="params">(<span class="type">const</span> drogon::HttpRequestPtr&amp; req,</span></span></span><br><span class="line"><span class="params"><span class="function">            std::function&lt;<span class="type">void</span> (<span class="type">const</span> drogon::HttpResponsePtr&amp;)&gt;&amp;&amp; cb)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  drogon::MultiPartParser parser;</span><br><span class="line">  <span class="keyword">if</span> (parser.<span class="built_in">parse</span>(req) != <span class="number">0</span> || parser.<span class="built_in">getFiles</span>().<span class="built_in">empty</span>()) &#123;</span><br><span class="line">    <span class="keyword">auto</span> resp = drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(&#123;&#123;<span class="string">&quot;error&quot;</span>,<span class="string">&quot;no file&quot;</span>&#125;&#125;);</span><br><span class="line">    resp-&gt;<span class="built_in">setStatusCode</span>(drogon::k400BadRequest);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">cb</span>(resp);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">auto</span>&amp; file = parser.<span class="built_in">getFiles</span>()[<span class="number">0</span>];</span><br><span class="line">  <span class="comment">// ⚠️ 风险点：原始文件名可能引发路径穿越（建议自行做 basename/白名单后缀）</span></span><br><span class="line">  <span class="comment">// 有已知上传文件名处理不当导致任意文件写入的安全风险案例，应做防护。</span></span><br><span class="line">  file.<span class="built_in">save</span>(<span class="string">&quot;/data/uploads/...&quot;</span>);</span><br><span class="line">  <span class="comment">// 写 MySQL：documents 记录；调用 Python /internal/jobs/ingest</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Drogon 示例也展示了 <code>MultiPartParser</code> 与 <code>file.save()</code> 的基本用法，可作为你实现参考。</p>
<h4 id="MySQL-访问（DbClient-事务）"><a href="#MySQL-访问（DbClient-事务）" class="headerlink" title="MySQL 访问（DbClient &#x2F; 事务）"></a>MySQL 访问（DbClient &#x2F; 事务）</h4><p>Drogon 的 DbClient 支持同步与异步接口，异步更契合其整体异步模型；每个 DbClient 有 event loop 线程负责数据库 IO。<br>事务对象可由 DbClient 创建，并在析构时自动 commit&#x2F;rollback。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Repo 层伪代码：插入 messages</span></span><br><span class="line"><span class="keyword">auto</span> client = drogon::<span class="built_in">app</span>().<span class="built_in">getDbClient</span>(); <span class="comment">// 需在 config.json 配置 db_clients</span></span><br><span class="line">client-&gt;<span class="built_in">execSqlAsync</span>(</span><br><span class="line">  <span class="string">&quot;INSERT INTO messages(session_id, role, content, status) VALUES(?,?,?,?)&quot;</span>,</span><br><span class="line">  [](<span class="type">const</span> drogon::orm::Result&amp; r)&#123; <span class="comment">/* ok */</span> &#125;,</span><br><span class="line">  [](<span class="type">const</span> drogon::orm::DrogonDbException&amp; e)&#123; <span class="comment">/* log */</span> &#125;,</span><br><span class="line">  sessionId, <span class="string">&quot;user&quot;</span>, content, <span class="string">&quot;created&quot;</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h4 id="Redis-交互（建议-redis-plus-plus）"><a href="#Redis-交互（建议-redis-plus-plus）" class="headerlink" title="Redis 交互（建议 redis-plus-plus）"></a>Redis 交互（建议 redis-plus-plus）</h4><p><code>redis-plus-plus</code> 是常用 C++ Redis 客户端（基于 hiredis），支持连接池等；其 README 说明可用 CMake FetchContent 集成。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sw/redis++/redis++.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">sw::<span class="function">redis::Redis <span class="title">redis</span><span class="params">(<span class="string">&quot;tcp://redis:6379&quot;</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 限流计数器（示意）</span></span><br><span class="line"><span class="keyword">auto</span> key = <span class="string">&quot;rl:uid:route:window&quot;</span>;</span><br><span class="line"><span class="keyword">auto</span> v = redis.<span class="built_in">incr</span>(key);</span><br><span class="line"><span class="keyword">if</span> (v == <span class="number">1</span>) &#123;</span><br><span class="line">  redis.<span class="built_in">expire</span>(key, <span class="number">10</span>); <span class="comment">// 10s 窗口</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Redis 的 EXPIRE&#x2F;SET EX 语义与 TTL 行为可参考官方命令文档。</p>
<h4 id="SSE-流式输出（Gateway-从-Redis-Streams-读取）"><a href="#SSE-流式输出（Gateway-从-Redis-Streams-读取）" class="headerlink" title="SSE 流式输出（Gateway 从 Redis Streams 读取）"></a>SSE 流式输出（Gateway 从 Redis Streams 读取）</h4><p>设计点：Worker 把 token 持续写入 Redis Stream（XADD），Gateway SSE endpoint 用阻塞读取（XREADGROUP 或 XREAD）不断向客户端输出。Redis Streams 的命令语义与复杂度在官方文档给出。</p>
<blockquote>
<p>Drogon 本身有“流式响应”相关能力，但社区也讨论过实现细节（例如示例里用线程发送分块会引入线程开销风险）。因此建议你把“token producer”放在 Python Worker，而 C++ 仅消费 Redis Streams 并向客户端 SSE 输出，结构更稳。</p>
</blockquote>
<p>（示意伪代码：重点是事件格式与循环结构）</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// GET /v1/sessions/&#123;sid&#125;/messages/&#123;mid&#125;/stream</span></span><br><span class="line"><span class="comment">// 伪代码：实现方式可用 chunked response 或框架流式接口封装</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">streamAnswerSSE</span><span class="params">(...)</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 1) 设置响应头 Content-Type: text/event-stream</span></span><br><span class="line">  <span class="comment">// 2) last_id 从 Last-Event-ID 或 query 参数获取</span></span><br><span class="line">  <span class="comment">// 3) 循环 XREADGROUP 读取 chat:stream:&#123;mid&#125;</span></span><br><span class="line">  <span class="comment">// 4) 每条消息输出：</span></span><br><span class="line">  <span class="comment">//    event: token\n</span></span><br><span class="line">  <span class="comment">//    data: &#123;...&#125;\n\n</span></span><br><span class="line">  <span class="comment">// 5) 收到 done/error 后结束并 close</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>SSE 字段与事件结构参考 FastAPI SSE 教程与规范说明；你对外只要遵循同样的事件语义即可。</p>
<h4 id="Drogon-构建与测试指令（建议）"><a href="#Drogon-构建与测试指令（建议）" class="headerlink" title="Drogon 构建与测试指令（建议）"></a>Drogon 构建与测试指令（建议）</h4><ul>
<li>Build：CMake + Ninja（或 Make）</li>
<li>依赖：Drogon、MySQL client、redis-plus-plus（含 hiredis）</li>
<li>最小化：先用 Docker 镜像或 vcpkg&#x2F;conan 简化依赖（可选）。Drogon 官方提供 Docker 镜像，便于在 1-2 个月内快速稳定构建环境。</li>
</ul>
<p>示例（概念）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cmake -S cpp_gateway -B build -DCMAKE_BUILD_TYPE=Release</span><br><span class="line">cmake --build build -j</span><br><span class="line">ctest --test-dir build</span><br></pre></td></tr></table></figure>

<h3 id="Python-服务（FastAPI）实现要点"><a href="#Python-服务（FastAPI）实现要点" class="headerlink" title="Python 服务（FastAPI）实现要点"></a>Python 服务（FastAPI）实现要点</h3><p><strong>框架选择：FastAPI</strong><br>FastAPI 是高性能 Python API 框架，面向生产可用。</p>
<h4 id="文件上传与表单依赖"><a href="#文件上传与表单依赖" class="headerlink" title="文件上传与表单依赖"></a>文件上传与表单依赖</h4><p>FastAPI 接收上传文件使用 <code>UploadFile</code>&#x2F;<code>File</code>；处理 multipart 需要安装 <code>python-multipart</code>。</p>
<h4 id="Celery-配置（Redis-broker-backend）"><a href="#Celery-配置（Redis-broker-backend）" class="headerlink" title="Celery 配置（Redis broker&#x2F;backend）"></a>Celery 配置（Redis broker&#x2F;backend）</h4><p>Celery 文档明确：Redis 可同时作为 broker 与 backend；并提供 Redis backend 相关配置项。</p>
<p>另外，Celery 默认 JSON serializer（v4+），pickle 虽方便但存在安全风险，应避免在不可信场景使用。</p>
<h4 id="任务进度与状态"><a href="#任务进度与状态" class="headerlink" title="任务进度与状态"></a>任务进度与状态</h4><p>Celery 任务可以通过 <code>update_state(state=&#39;PROGRESS&#39;, meta=...)</code> 上报进度；Works well for ingest&#x2F;index 这类长任务。</p>
<h4 id="Embedding-与-Rerank"><a href="#Embedding-与-Rerank" class="headerlink" title="Embedding 与 Rerank"></a>Embedding 与 Rerank</h4><ul>
<li>Sentence-Transformers 提供 embedding 计算与 cross-encoder rerank 能力。</li>
<li>BGE reranker 描述了典型“先 embedding 检索 top-k，再用 reranker 重排 top-k”的平衡方案，并提供 FlagEmbedding 用法。</li>
<li>E5 论文说明其作为通用 embedding 家族在检索任务表现强。</li>
</ul>
<h4 id="FAISS-索引"><a href="#FAISS-索引" class="headerlink" title="FAISS 索引"></a>FAISS 索引</h4><p>FAISS 官方文档推荐用 Conda 安装 <code>faiss-cpu/faiss-gpu</code>，常见索引可在本地加载与检索。</p>
<h4 id="LLM-调用（本地）"><a href="#LLM-调用（本地）" class="headerlink" title="LLM 调用（本地）"></a>LLM 调用（本地）</h4><ul>
<li><code>llama.cpp</code> 提供本地推理能力；其 server 文档说明了轻量 HTTP server 与 REST API。</li>
<li><code>llama-cpp-python</code> 提供 OpenAI API 兼容 server，利于你在系统层面稳定接口（后续也可替换成 Ollama&#x2F;vLLM）。</li>
</ul>
<h3 id="异步任务设计（Celery-Redis）与任务状态流"><a href="#异步任务设计（Celery-Redis）与任务状态流" class="headerlink" title="异步任务设计（Celery + Redis）与任务状态流"></a>异步任务设计（Celery + Redis）与任务状态流</h3><p>建议将任务分为两类（都由 Python 负责执行，C++ 只负责提交与聚合状态）：</p>
<ol>
<li><strong>IngestDocumentTask</strong>：解析→切片→embedding→FAISS 更新→写 DB 状态</li>
<li><strong>ChatCompletionTask</strong>：检索→rerank→构造 prompt→调用 LLM→输出 token 到 Redis Streams→落库最终结果</li>
</ol>
<p>Chat 流式输出的核心是：</p>
<ul>
<li>Worker：对每个 token（或每 N token batch）XADD 写入 <code>chat:stream:&#123;message_id&#125;</code></li>
<li>Gateway：SSE endpoint 阻塞读取（XREADGROUP&#x2F;XREAD）并立即向客户端推送<br>XADD&#x2F;XREADGROUP 的语义与常见使用模式在 Redis 官方 Streams 文档中定义。</li>
</ul>
<h2 id="部署、测试、可观测性与交付物"><a href="#部署、测试、可观测性与交付物" class="headerlink" title="部署、测试、可观测性与交付物"></a>部署、测试、可观测性与交付物</h2><h3 id="Docker-docker-compose-配置示例（骨架）"><a href="#Docker-docker-compose-配置示例（骨架）" class="headerlink" title="Docker &#x2F; docker-compose 配置示例（骨架）"></a>Docker &#x2F; docker-compose 配置示例（骨架）</h3><p>关键点：</p>
<ul>
<li><code>depends_on</code> 只能保证启动顺序，真正“就绪”建议配合 <code>healthcheck</code>。官方 Compose 文档强调启动顺序控制方式。</li>
<li>MySQL 官方镜像通过环境变量初始化 root 密码&#x2F;数据库等（首次启动且数据目录为空时生效）。</li>
<li>Redis 官方给出 Docker 运行方式与文档。</li>
</ul>
<p>（示意 <code>docker-compose.yml</code> 片段）</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">mysql:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">mysql:8.0</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="attr">MYSQL_ROOT_PASSWORD:</span> <span class="string">$&#123;MYSQL_ROOT_PASSWORD&#125;</span></span><br><span class="line">      <span class="attr">MYSQL_DATABASE:</span> <span class="string">$&#123;MYSQL_DATABASE&#125;</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD&quot;</span>, <span class="string">&quot;mysqladmin&quot;</span>, <span class="string">&quot;ping&quot;</span>, <span class="string">&quot;-p$&#123;MYSQL_ROOT_PASSWORD&#125;&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">5s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">3s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">20</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">redis:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">redis:7</span></span><br><span class="line">    <span class="attr">healthcheck:</span></span><br><span class="line">      <span class="attr">test:</span> [<span class="string">&quot;CMD&quot;</span>, <span class="string">&quot;redis-cli&quot;</span>, <span class="string">&quot;ping&quot;</span>]</span><br><span class="line">      <span class="attr">interval:</span> <span class="string">3s</span></span><br><span class="line">      <span class="attr">timeout:</span> <span class="string">2s</span></span><br><span class="line">      <span class="attr">retries:</span> <span class="number">30</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">rag_api:</span></span><br><span class="line">    <span class="attr">build:</span> <span class="string">./python_rag</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="attr">mysql:</span></span><br><span class="line">        <span class="attr">condition:</span> <span class="string">service_healthy</span></span><br><span class="line">      <span class="attr">redis:</span></span><br><span class="line">        <span class="attr">condition:</span> <span class="string">service_healthy</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">celery_worker:</span></span><br><span class="line">    <span class="attr">build:</span> <span class="string">./python_rag</span></span><br><span class="line">    <span class="attr">command:</span> <span class="string">celery</span> <span class="string">-A</span> <span class="string">app.celery_app</span> <span class="string">worker</span> <span class="string">-l</span> <span class="string">INFO</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="attr">rag_api:</span></span><br><span class="line">        <span class="attr">condition:</span> <span class="string">service_started</span></span><br><span class="line"></span><br><span class="line">  <span class="attr">gateway:</span></span><br><span class="line">    <span class="attr">build:</span> <span class="string">./cpp_gateway</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="attr">mysql:</span></span><br><span class="line">        <span class="attr">condition:</span> <span class="string">service_healthy</span></span><br><span class="line">      <span class="attr">redis:</span></span><br><span class="line">        <span class="attr">condition:</span> <span class="string">service_healthy</span></span><br></pre></td></tr></table></figure>

<h3 id="测试计划（单元测试-集成测试-端到端验收）"><a href="#测试计划（单元测试-集成测试-端到端验收）" class="headerlink" title="测试计划（单元测试 &#x2F; 集成测试 &#x2F; 端到端验收）"></a>测试计划（单元测试 &#x2F; 集成测试 &#x2F; 端到端验收）</h3><h4 id="Python-单元测试"><a href="#Python-单元测试" class="headerlink" title="Python 单元测试"></a>Python 单元测试</h4><p>FastAPI 推荐使用 <code>TestClient</code>（基于 httpx）进行测试；文档给出安装与使用方式。</p>
<p>建议用例（示例）：</p>
<table>
<thead>
<tr>
<th>级别</th>
<th>用例</th>
<th>断言</th>
</tr>
</thead>
<tbody><tr>
<td>Unit</td>
<td>chunker 输入长文本</td>
<td>切片数、重叠、最大长度满足规则</td>
</tr>
<tr>
<td>Unit</td>
<td>embedding 输出维度</td>
<td>dim 与索引匹配</td>
</tr>
<tr>
<td>Unit</td>
<td>rerank 排序</td>
<td>score 单调与 top-k 选择</td>
</tr>
<tr>
<td>API</td>
<td><code>/internal/jobs/ingest</code></td>
<td>返回 task_id，状态可查询</td>
</tr>
<tr>
<td>API</td>
<td><code>/internal/tasks/&#123;id&#125;</code></td>
<td>state&#x2F;meta 格式正确</td>
</tr>
</tbody></table>
<h4 id="C-单元-集成测试"><a href="#C-单元-集成测试" class="headerlink" title="C++ 单元&#x2F;集成测试"></a>C++ 单元&#x2F;集成测试</h4><p>建议至少实现“黑盒集成测”（curl&#x2F;脚本即可），并在 Week 7 补充 C++ 单测框架（Catch2&#x2F;GoogleTest 任选）。Drogon 本身有配置文件化与日志能力可帮助观察。</p>
<h4 id="端到端验收测试（E2E）"><a href="#端到端验收测试（E2E）" class="headerlink" title="端到端验收测试（E2E）"></a>端到端验收测试（E2E）</h4><p>定义 3 条“必须通过”的验收脚本（你可以写成 <code>scripts/e2e_demo.sh</code>）：</p>
<ol>
<li>上传 md&#x2F;txt → ingest SUCCESS → 检索可命中</li>
<li>创建 session → 发起问答 → 返回答案 + citations（非空）</li>
<li>同一 session 多轮问答 + SSE 流式输出不断线（至少 30 秒）</li>
</ol>
<h3 id="监控与日志建议"><a href="#监控与日志建议" class="headerlink" title="监控与日志建议"></a>监控与日志建议</h3><ul>
<li>Gateway：请求日志、trace_id（建议 message_id&#x2F;task_id 贯穿）、慢请求统计。Drogon 的线程模型基于 event loop，建议避免在 handler 里做阻塞操作，把重活下沉到 Celery。</li>
<li>Redis：关注 keyspace 增长与 streams 长度；设置过期策略，避免无限增长（EXPIRE&#x2F;TTL）。</li>
<li>Redis 性能：Redis 官方提到网络延迟与 CPU 会直接影响性能，可用 <code>redis-benchmark</code> 或简单 ping 验证链路。</li>
</ul>
<h3 id="风险清单与缓解措施"><a href="#风险清单与缓解措施" class="headerlink" title="风险清单与缓解措施"></a>风险清单与缓解措施</h3><table>
<thead>
<tr>
<th>风险</th>
<th>触发场景</th>
<th>影响</th>
<th>缓解措施</th>
</tr>
</thead>
<tbody><tr>
<td>文档上传安全（路径穿越&#x2F;任意写）</td>
<td>直接用原始文件名保存</td>
<td>高危</td>
<td>只用生成文件名；后缀白名单；禁止 <code>..</code>；严格上传目录；参考已披露风险案例</td>
</tr>
<tr>
<td>Redis 任务&#x2F;流占用无限增长</td>
<td>streams&#x2F;list 不 trim、不 expire</td>
<td>内存爆</td>
<td>完成后 EXPIRE；streams XTRIM（P1）；定期清理任务与 token</td>
</tr>
<tr>
<td>Celery serializer 安全</td>
<td>使用 pickle 且 broker 暴露</td>
<td>高危</td>
<td>强制 JSON serializer；<code>accept_content=[&#39;json&#39;]</code>；Celery 对 pickle 风险有明确警告</td>
</tr>
<tr>
<td>流式输出不稳定</td>
<td>SSE 断线&#x2F;代理缓冲</td>
<td>中</td>
<td>加心跳事件；支持 Last-Event-ID；对外用 SSE 标准字段</td>
</tr>
<tr>
<td>向量索引与 DB 映射错位</td>
<td>vector_id 映射错误</td>
<td>中</td>
<td>映射表不可变；索引版本化；重建流程可重复</td>
</tr>
<tr>
<td>性能不足（CPU 推理慢）</td>
<td>本地 LLM&#x2F; rerank 大模型</td>
<td>中</td>
<td>默认用小模型&#x2F;量化；rerank 限制 top-k；缓存 query embedding；并发限流</td>
</tr>
<tr>
<td>依赖启动顺序</td>
<td>MySQL 未就绪导致服务启动失败</td>
<td>中</td>
<td>Compose healthcheck + depends_on；官方有启动顺序控制说明</td>
</tr>
</tbody></table>
<h3 id="性能估算与优化建议（单机）"><a href="#性能估算与优化建议（单机）" class="headerlink" title="性能估算与优化建议（单机）"></a>性能估算与优化建议（单机）</h3><blockquote>
<p>下述为“工程估算区间”，受 CPU&#x2F;GPU、模型大小、上下文长度影响显著；建议 Week 8 用真实硬件做一次 profiling。</p>
</blockquote>
<ul>
<li><strong>检索链路</strong>：embedding（短 query）+ FAISS top-k 通常明显快于 LLM 生成阶段；优化重点在 batching 与缓存。FAISS 以高效相似度检索为目标，C++ 实现并提供 Python wrapper。</li>
<li><strong>rerank 开销</strong>：Cross-Encoder 需要对（query, doc）成对打分，代价高于 bi-encoder；因此只 rerank embedding 召回的 top-k 是常见做法。</li>
<li><strong>流式输出</strong>：Redis Streams 阻塞读写适合日志&#x2F;队列；XREAD&#x2F;XREADGROUP 的复杂度在官方文档给出，可通过控制 COUNT 与阻塞参数降低开销。</li>
<li><strong>网关高并发</strong>：Drogon 强调异步与低开销；但流式响应实现方式需避免“每连接一线程”这类模型（社区已讨论潜在瓶颈）。</li>
</ul>
<h3 id="最终交付物清单"><a href="#最终交付物清单" class="headerlink" title="最终交付物清单"></a>最终交付物清单</h3><h4 id="代码仓库结构（建议）"><a href="#代码仓库结构（建议）" class="headerlink" title="代码仓库结构（建议）"></a>代码仓库结构（建议）</h4><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">repo/</span><br><span class="line">  cpp_gateway/</span><br><span class="line">    CMakeLists.txt</span><br><span class="line">    config/config.json</span><br><span class="line">    src/</span><br><span class="line">      main.cc</span><br><span class="line">      controllers/</span><br><span class="line">      services/</span><br><span class="line">      repos/</span><br><span class="line">      middleware/</span><br><span class="line">      utils/</span><br><span class="line">    tests/</span><br><span class="line">    Dockerfile</span><br><span class="line">  python_rag/</span><br><span class="line">    app/</span><br><span class="line">      main.py</span><br><span class="line">      api/</span><br><span class="line">      celery_app.py</span><br><span class="line">      tasks/</span><br><span class="line">      rag/</span><br><span class="line">      store/</span><br><span class="line">    tests/</span><br><span class="line">    requirements.txt</span><br><span class="line">    Dockerfile</span><br><span class="line">  db/</span><br><span class="line">    migrations/</span><br><span class="line">    schema.sql</span><br><span class="line">  scripts/</span><br><span class="line">    e2e_demo.sh</span><br><span class="line">    bench.sh</span><br><span class="line">    seed_data.sh</span><br><span class="line">  docker-compose.yml</span><br><span class="line">  .env.example</span><br><span class="line">  README.md</span><br></pre></td></tr></table></figure>

<h4 id="README-模板（必须包含）"><a href="#README-模板（必须包含）" class="headerlink" title="README 模板（必须包含）"></a>README 模板（必须包含）</h4><ul>
<li>项目简介（1 段话）</li>
<li>架构图（mermaid）与模块说明</li>
<li>快速启动（<code>docker compose up --build -d</code>）</li>
<li>Demo 路径（上传→问答→流式）</li>
<li>API 文档（关键 endpoint 与示例）</li>
<li>数据库说明（表、索引、迁移方式）</li>
<li>性能与已知限制</li>
<li>Roadmap（下一步要做的 5 件事）</li>
</ul>
<h4 id="演示脚本（Demo-Script）"><a href="#演示脚本（Demo-Script）" class="headerlink" title="演示脚本（Demo Script）"></a>演示脚本（Demo Script）</h4><p>建议准备一份“10 分钟稳定演示”的脚本，顺序固定：</p>
<ol>
<li>启动服务（输出健康检查）</li>
<li>上传一个短文档（md&#x2F;txt）→展示任务状态（PENDING→PROGRESS→SUCCESS）</li>
<li>创建 session → 提问 → 展示 SSE token 流（终止后返回 citations）</li>
<li>第二轮追问（验证会话记忆&#x2F;上下文保持）</li>
<li>打开 MySQL 查看 messages&#x2F;citations（证明可追溯与可审计）</li>
</ol>
<h4 id="Demo-录制脚本（建议）"><a href="#Demo-录制脚本（建议）" class="headerlink" title="Demo 录制脚本（建议）"></a>Demo 录制脚本（建议）</h4><ul>
<li>终端录制：<code>asciinema rec</code>（或 OBS 全屏录制）</li>
<li>录制前跑 <code>scripts/e2e_demo.sh</code> 确保无报错</li>
<li>录制内容中必须出现：<code>docker compose up</code>、<code>/health</code>、上传、流式回答、引用 JSON、数据库查询结果</li>
</ul>
<h4 id="项目亮点说明（用于简历-答辩）"><a href="#项目亮点说明（用于简历-答辩）" class="headerlink" title="项目亮点说明（用于简历&#x2F;答辩）"></a>项目亮点说明（用于简历&#x2F;答辩）</h4><ul>
<li><strong>C++ 网关治理上下文</strong>：你把“AI 窗口记忆不足”的问题工程化解决（短期缓存 + 长期摘要 + 上下文预算）</li>
<li><strong>Redis Streams 流式桥接</strong>：推理解耦、可恢复、可观察（token 作为事件流）</li>
<li><strong>RAG 可追溯引用</strong>：每条回答都有可审计 provenance（chunk 级引用）——符合 RAG 强调 provenance 的目标</li>
<li><strong>工程化可交付</strong>：docker-compose 一键启动、健康检查、测试用例与 E2E 脚本，具备“可复现交付物”的完整形态</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
      </categories>
  </entry>
  <entry>
    <title>数据库关系</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E6%95%B0%E6%8D%AE%E5%BA%93%E5%85%B3%E7%B3%BB/</url>
    <content><![CDATA[<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">erDiagram</span><br><span class="line">    user_account ||--o&#123; documents : owns</span><br><span class="line">    user_account ||--o&#123; sessions : owns</span><br><span class="line"></span><br><span class="line">    documents ||--o&#123; doc_chunks : splits_into</span><br><span class="line">    documents ||--|| document_indexes : indexed_by</span><br><span class="line">    documents ||--o&#123; citations : referenced_by</span><br><span class="line"></span><br><span class="line">    sessions ||--o&#123; messages : contains</span><br><span class="line">    messages ||--o&#123; citations : has</span><br><span class="line"></span><br><span class="line">    doc_chunks ||--o&#123; citations : cited_from</span><br><span class="line"></span><br><span class="line">    user_account &#123;</span><br><span class="line">        BIGINT id PK</span><br><span class="line">        VARCHAR username</span><br><span class="line">        TIMESTAMP created_at</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    documents &#123;</span><br><span class="line">        BIGINT id PK</span><br><span class="line">        BIGINT user_id FK</span><br><span class="line">        VARCHAR filename</span><br><span class="line">        VARCHAR mime</span><br><span class="line">        CHAR sha256</span><br><span class="line">        BIGINT size_bytes</span><br><span class="line">        VARCHAR storage_path</span><br><span class="line">        VARCHAR status</span><br><span class="line">        TEXT error_message</span><br><span class="line">        TIMESTAMP created_at</span><br><span class="line">        TIMESTAMP updated_at</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    doc_chunks &#123;</span><br><span class="line">        BIGINT id PK</span><br><span class="line">        BIGINT doc_id FK</span><br><span class="line">        INT chunk_index</span><br><span class="line">        LONGTEXT text</span><br><span class="line">        INT tokens_est</span><br><span class="line">        TIMESTAMP created_at</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    document_indexes &#123;</span><br><span class="line">        BIGINT id PK</span><br><span class="line">        BIGINT doc_id FK</span><br><span class="line">        VARCHAR index_type</span><br><span class="line">        VARCHAR embedding_model</span><br><span class="line">        INT dimension</span><br><span class="line">        VARCHAR index_path</span><br><span class="line">        VARCHAR mapping_path</span><br><span class="line">        INT chunk_count</span><br><span class="line">        VARCHAR status</span><br><span class="line">        TIMESTAMP created_at</span><br><span class="line">        TIMESTAMP updated_at</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    sessions &#123;</span><br><span class="line">        BIGINT id PK</span><br><span class="line">        BIGINT user_id FK</span><br><span class="line">        VARCHAR title</span><br><span class="line">        TEXT summary</span><br><span class="line">        TIMESTAMP created_at</span><br><span class="line">        TIMESTAMP updated_at</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    messages &#123;</span><br><span class="line">        BIGINT id PK</span><br><span class="line">        BIGINT session_id FK</span><br><span class="line">        VARCHAR role</span><br><span class="line">        LONGTEXT content</span><br><span class="line">        VARCHAR status</span><br><span class="line">        JSON meta_json</span><br><span class="line">        TIMESTAMP created_at</span><br><span class="line">        TIMESTAMP updated_at</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    citations &#123;</span><br><span class="line">        BIGINT id PK</span><br><span class="line">        BIGINT message_id FK</span><br><span class="line">        BIGINT doc_id FK</span><br><span class="line">        BIGINT chunk_id FK</span><br><span class="line">        INT chunk_index</span><br><span class="line">        DOUBLE score</span><br><span class="line">        TEXT snippet</span><br><span class="line">        TIMESTAMP created_at</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    tasks &#123;</span><br><span class="line">        BIGINT id PK</span><br><span class="line">        VARCHAR celery_task_id</span><br><span class="line">        VARCHAR type</span><br><span class="line">        VARCHAR entity_type</span><br><span class="line">        BIGINT entity_id</span><br><span class="line">        VARCHAR state</span><br><span class="line">        INT progress</span><br><span class="line">        JSON meta_json</span><br><span class="line">        TEXT error</span><br><span class="line">        TIMESTAMP created_at</span><br><span class="line">        TIMESTAMP updated_at</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
      </categories>
  </entry>
  <entry>
    <title>redis 数据结构与对象</title>
    <url>//notes/redis%E8%AE%BE%E8%AE%A1%E4%B8%8E%E5%AE%9E%E7%8E%B0/%E6%95%B0%E6%8D%AE%E7%BB%93%E4%B8%8E%E5%AF%B9%E8%B1%A1/</url>
    <content><![CDATA[<h1 id="redis-数据结构与对象"><a href="#redis-数据结构与对象" class="headerlink" title="redis 数据结构与对象"></a>redis 数据结构与对象</h1><p>time：2026_2_27</p>
<h2 id="SDS-动态字符串"><a href="#SDS-动态字符串" class="headerlink" title="SDS 动态字符串"></a>SDS 动态字符串</h2><p>SDS 和 c 中的string 并不相同</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">sdshdr</span>&#123;</span><br><span class="line">    <span class="type">int</span> len;</span><br><span class="line">    <span class="type">int</span> free;</span><br><span class="line">    <span class="type">char</span> buf[]</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="内存分配"><a href="#内存分配" class="headerlink" title="内存分配"></a>内存分配</h3><ol>
<li>少于1MB 的时候发生扩容的时候，len 和free 长度相同，分配内存为 len+free+1</li>
<li>大于1MB len 为当前大小，free 固定为1MB 内存为 len（buf size）+ free（1MB）+1bt</li>
</ol>
<h3 id="其它特性"><a href="#其它特性" class="headerlink" title="其它特性"></a>其它特性</h3><ol>
<li>SDS具有二进制安全，在记录字符串的时候，能够记录任意位置空格（string 所不具有的），并且通过约定在末尾加入空格的方式能够实现兼容string 实现string的相加等功能</li>
<li>key 为SDS</li>
</ol>
<h2 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">listNode</span>&#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">ListNode</span> * prev;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">ListNode</span> * next;</span><br><span class="line">    <span class="type">void</span> * value;</span><br><span class="line">&#125; listNode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">list</span>&#123;</span><br><span class="line">    listNode *head;<span class="comment">// 头</span></span><br><span class="line">    listNode *tail;<span class="comment">//尾</span></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> <span class="type">long</span> ; <span class="comment">// 长度</span></span><br><span class="line"></span><br><span class="line">    <span class="type">void</span> *(*dup)(<span class="type">void</span> * ptr);<span class="comment">//复制</span></span><br><span class="line">    <span class="type">void</span> *(*free)(<span class="type">void</span> *free);<span class="comment">// 释放</span></span><br><span class="line">    <span class="type">void</span> *(* match)(<span class="type">void</span> *match);<span class="comment">//匹配</span></span><br><span class="line">&#125; list;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h2 id="字典"><a href="#字典" class="headerlink" title="字典"></a>字典</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">dictht</span>&#123;</span><br><span class="line">    dictEntry ** table;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> size;</span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> sizemask;</span><br><span class="line"></span><br><span class="line">    <span class="type">unsigned</span> <span class="type">long</span> used;</span><br><span class="line">&#125;dictht;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">dictEntry</span>&#123;</span><br><span class="line">    <span class="type">void</span> *key;</span><br><span class="line">    <span class="keyword">union</span>&#123;</span><br><span class="line">        <span class="type">void</span> *val;</span><br><span class="line">        uint64_tu64;</span><br><span class="line">        int64_ts64;</span><br><span class="line">    &#125; v;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">dictEntry</span> *next; <span class="comment">// 指向下一个行成表 可以解决相同键值保存的问题</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>![alt text](&#x2F;img&#x2F;notes&#x2F;学习&#x2F;redis设计与实现&#x2F;image&#x2F;截屏2026-02-27 16.38.23.png)</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span> <span class="title class_">dict</span>&#123;</span><br><span class="line">    dicType * type;</span><br><span class="line">    <span class="type">void</span> *provdata;</span><br><span class="line">    dictht ht[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> trehashids;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="数据扩容操作实现"><a href="#数据扩容操作实现" class="headerlink" title="数据扩容操作实现"></a>数据扩容操作实现</h3><h2 id="队列"><a href="#队列" class="headerlink" title="队列"></a>队列</h2><h2 id="跳跃队列"><a href="#跳跃队列" class="headerlink" title="跳跃队列"></a>跳跃队列</h2><h2 id="元素"><a href="#元素" class="headerlink" title="元素"></a>元素</h2>]]></content>
      <categories>
        <category>学习</category>
        <category>redis设计与实现</category>
      </categories>
  </entry>
  <entry>
    <title>Client 实现分析</title>
    <url>//notes/%E5%AE%9E%E6%97%B6%E7%AB%9E%E6%8A%80%E6%B8%B8%E6%88%8F%E5%BC%80%E5%8F%91/client/</url>
    <content><![CDATA[<h1 id="Client-实现分析"><a href="#Client-实现分析" class="headerlink" title="Client 实现分析"></a>Client 实现分析</h1><p>time：2026_1_21</p>
<h2 id="app-依赖包"><a href="#app-依赖包" class="headerlink" title="app 依赖包"></a>app 依赖包</h2><p>ClientRender<br>    可视化</p>
<p>GameConfig<br>    client 基础信息参数 后续可以扩展到server 公用一个确保准确</p>
<p>InputPredicition<br>    预测 x,y 的动作</p>
<h2 id="主体部分"><a href="#主体部分" class="headerlink" title="主体部分"></a>主体部分</h2><p>ClientCtx</p>
<pre><code>lab::net::UdpSocket sock; 发收
lab::net::UdpAddr server&#123;&#125;; server 接受

localHist 本地
remoteHist 预测
stateHist 权威
</code></pre>
<p>BuildCmdVec （localPid localCmd remoteCmds）<br>    通过pid识别是否是本段，保存一个vector<inputcmd>cmds size &#x3D; 2的状态，本段保存输入，对端保存预测<br>    return （输入， 预测）&#x2F;&#x2F; vector<inputcmd>cmds size 2</p>
<p>GetRemoteCmdForTick  ctx pid t</p>
<pre><code>return (预测)
</code></pre>
<p>ApplyAuthoritativeState ctx st</p>
<h2 id="整理"><a href="#整理" class="headerlink" title="整理"></a>整理</h2><p>主要就就两条思路 接受处理，发收处理维护不同的状态</p>
<p>OnUdp<br>Recv：只负责收包 -&gt; 更新 ack&#x2F;state -&gt; 更新对手预测&#x2F;触发回滚</p>
]]></content>
      <categories>
        <category>学习</category>
        <category>实时竞技游戏开发</category>
      </categories>
  </entry>
  <entry>
    <title>模拟</title>
    <url>//notes/%E5%AE%9E%E6%97%B6%E7%AB%9E%E6%8A%80%E6%B8%B8%E6%88%8F%E5%BC%80%E5%8F%91/%E6%A8%A1%E6%8B%9F/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>学习</category>
        <category>实时竞技游戏开发</category>
      </categories>
  </entry>
  <entry>
    <title>服务器分析</title>
    <url>//notes/%E5%AE%9E%E6%97%B6%E7%AB%9E%E6%8A%80%E6%B8%B8%E6%88%8F%E5%BC%80%E5%8F%91/server/</url>
    <content><![CDATA[<h1 id="服务器分析"><a href="#服务器分析" class="headerlink" title="服务器分析"></a>服务器分析</h1><p>time：2026_1_20</p>
<p>ClientConn<br>    历史缓存<br>    tick<br>        最后输入tick<br>        最后权威tick<br>    最后输入包<br>    玩家数量</p>
<p>ServerCtx<br>    基础信息<br>        对局设置<br>        帧同步设置<br>        世界设置<br>    sock 网络<br>    clients 存储客户端状态<br>函数<br>    AssignSlot 分配id 1，2<br>    GetPlayer 从ServerCtx-&gt;clients 取出ClientConn<br>    OnlineCount 在线玩家统计<br>    GetCmdForTick 从ClientConn中获取动作信息，正常接受，如果出现延迟但小于延迟设置，着延续上一次包，但是出现过大延迟直接采用默认信息</p>
<pre><code>OnUdp 收包加入缓存
MaybeStartMatch for pid 轮训发收start包，其中通过利用getplayer 吧serverctx中的clientconn取出，同时检测ctx中的状态已start则跳过

OnTick
    整体事件监听
    推进世界 + 给两边发 State/Ack
</code></pre>
]]></content>
      <categories>
        <category>学习</category>
        <category>实时竞技游戏开发</category>
      </categories>
  </entry>
  <entry>
    <title>渲染</title>
    <url>//notes/%E5%AE%9E%E6%97%B6%E7%AB%9E%E6%8A%80%E6%B8%B8%E6%88%8F%E5%BC%80%E5%8F%91/%E6%B8%B2%E6%9F%93/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>学习</category>
        <category>实时竞技游戏开发</category>
      </categories>
  </entry>
  <entry>
    <title>UDP 通信</title>
    <url>//notes/linux%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B/udp/</url>
    <content><![CDATA[<h1 id="UDP-通信"><a href="#UDP-通信" class="headerlink" title="UDP 通信"></a>UDP 通信</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：报文、无连接、不可靠传输、<code>sendto/recvfrom</code>、MTU、丢包重传、滑动窗口<br>核心目标：先把 UDP 的“简单”理解透，再搞清楚为什么很多实时协议愿意在 UDP 之上自己补可靠性。</p>
</blockquote>
<hr>
<h2 id="1-UDP-是什么"><a href="#1-UDP-是什么" class="headerlink" title="1. UDP 是什么"></a>1. UDP 是什么</h2><p>UDP（User Datagram Protocol）是<strong>面向报文</strong>的传输层协议。</p>
<p>它的特点很直接：</p>
<ul>
<li><strong>无连接</strong>：通信前不需要三次握手</li>
<li><strong>报文边界保留</strong>：发送一次 <code>sendto</code>，接收端看到的是一个完整报文，天然没有 TCP 粘包问题</li>
<li><strong>尽力而为</strong>：协议本身不保证送达、不保证顺序、不保证不重复</li>
<li><strong>开销小、时延低</strong>：头部只有 8 字节，协议栈处理也更轻</li>
</ul>
<p>所以 UDP 很适合：</p>
<ul>
<li>实时音视频</li>
<li>在线游戏</li>
<li>DNS</li>
<li>广播 &#x2F; 组播</li>
<li>能容忍少量丢包，但很在意时延的业务</li>
</ul>
<hr>
<h2 id="2-UDP-和-TCP-的核心差异"><a href="#2-UDP-和-TCP-的核心差异" class="headerlink" title="2. UDP 和 TCP 的核心差异"></a>2. UDP 和 TCP 的核心差异</h2><table>
<thead>
<tr>
<th>维度</th>
<th>UDP</th>
<th>TCP</th>
</tr>
</thead>
<tbody><tr>
<td>连接语义</td>
<td>无连接</td>
<td>面向连接</td>
</tr>
<tr>
<td>数据形式</td>
<td>报文</td>
<td>字节流</td>
</tr>
<tr>
<td>可靠性</td>
<td>不保证</td>
<td>保证送达、按序、去重</td>
</tr>
<tr>
<td>顺序</td>
<td>不保证</td>
<td>保证</td>
</tr>
<tr>
<td>重传</td>
<td>应用层自己做</td>
<td>内核协议栈负责</td>
</tr>
<tr>
<td>流量控制</td>
<td>没有</td>
<td>有</td>
</tr>
<tr>
<td>拥塞控制</td>
<td>没有</td>
<td>有</td>
</tr>
<tr>
<td>时延</td>
<td>更低</td>
<td>较稳定但更重</td>
</tr>
</tbody></table>
<p>最重要的一点：</p>
<blockquote>
<p>UDP 只是“帮你发包”，TCP 则是“帮你把传输这件事做完整”。</p>
</blockquote>
<hr>
<h2 id="3-Linux-下-UDP-的常用接口"><a href="#3-Linux-下-UDP-的常用接口" class="headerlink" title="3. Linux 下 UDP 的常用接口"></a>3. Linux 下 UDP 的常用接口</h2><h3 id="3-1-创建-socket"><a href="#3-1-创建-socket" class="headerlink" title="3.1 创建 socket"></a>3.1 创建 socket</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> fd = socket(AF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p><code>SOCK_DGRAM</code> 表示 UDP 套接字。</p>
<h3 id="3-2-服务端绑定地址"><a href="#3-2-服务端绑定地址" class="headerlink" title="3.2 服务端绑定地址"></a>3.2 服务端绑定地址</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">bind(fd, (<span class="keyword">struct</span> sockaddr*)&amp;addr, <span class="keyword">sizeof</span>(addr));</span><br></pre></td></tr></table></figure>

<p>和 TCP 一样，服务端通常需要 <code>bind</code> 到固定 IP&#x2F;端口。</p>
<h3 id="3-3-接收与发送"><a href="#3-3-接收与发送" class="headerlink" title="3.3 接收与发送"></a>3.3 接收与发送</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">ssize_t</span> n = recvfrom(fd, buf, <span class="keyword">sizeof</span>(buf), <span class="number">0</span>,</span><br><span class="line">                     (<span class="keyword">struct</span> sockaddr*)&amp;peer, &amp;peer_len);</span><br><span class="line"></span><br><span class="line">sendto(fd, data, len, <span class="number">0</span>,</span><br><span class="line">       (<span class="keyword">struct</span> sockaddr*)&amp;peer, peer_len);</span><br></pre></td></tr></table></figure>

<p>这是一对最常见的 UDP I&#x2F;O 接口：</p>
<ul>
<li><code>recvfrom</code>：收到数据时，顺便告诉你包来自谁</li>
<li><code>sendto</code>：每次发送时明确指定目标地址</li>
</ul>
<h3 id="3-4-connect-对-UDP-也有用"><a href="#3-4-connect-对-UDP-也有用" class="headerlink" title="3.4 connect() 对 UDP 也有用"></a>3.4 <code>connect()</code> 对 UDP 也有用</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">connect(fd, (<span class="keyword">struct</span> sockaddr*)&amp;peer, <span class="keyword">sizeof</span>(peer));</span><br></pre></td></tr></table></figure>

<p>UDP 上的 <code>connect()</code> <strong>不是建立连接</strong>，而是：</p>
<ul>
<li>给这个 socket 绑定一个默认对端</li>
<li>之后可以直接用 <code>send/recv</code></li>
<li>内核只接收该对端发来的报文</li>
<li>某些错误能更直接反馈给调用方</li>
</ul>
<p>所以很多客户端 UDP 程序也会先 <code>connect()</code>，这样代码更简洁。</p>
<hr>
<h2 id="4-UDP-通信的基本流程"><a href="#4-UDP-通信的基本流程" class="headerlink" title="4. UDP 通信的基本流程"></a>4. UDP 通信的基本流程</h2><h3 id="4-1-服务端"><a href="#4-1-服务端" class="headerlink" title="4.1 服务端"></a>4.1 服务端</h3><ol>
<li><code>socket(AF_INET, SOCK_DGRAM, 0)</code></li>
<li><code>bind()</code></li>
<li>循环 <code>recvfrom()</code></li>
<li>根据来源地址决定回包对象</li>
<li><code>sendto()</code> 返回结果</li>
</ol>
<h3 id="4-2-客户端"><a href="#4-2-客户端" class="headerlink" title="4.2 客户端"></a>4.2 客户端</h3><ol>
<li><code>socket(AF_INET, SOCK_DGRAM, 0)</code></li>
<li>可选：<code>connect()</code></li>
<li><code>sendto()</code> 或 <code>send()</code></li>
<li><code>recvfrom()</code> 或 <code>recv()</code></li>
</ol>
<p>和 TCP 相比，UDP 没有：</p>
<ul>
<li><code>listen()</code></li>
<li><code>accept()</code></li>
</ul>
<p>因为它没有连接队列这一层。</p>
<hr>
<h2 id="5-UDP-的工程注意点"><a href="#5-UDP-的工程注意点" class="headerlink" title="5. UDP 的工程注意点"></a>5. UDP 的工程注意点</h2><h3 id="5-1-没有粘包，不等于没有协议设计"><a href="#5-1-没有粘包，不等于没有协议设计" class="headerlink" title="5.1 没有粘包，不等于没有协议设计"></a>5.1 没有粘包，不等于没有协议设计</h3><p>UDP 保留报文边界，但应用层仍然要定义：</p>
<ul>
<li>包头</li>
<li>消息类型</li>
<li>序号</li>
<li>校验</li>
<li>重传策略</li>
</ul>
<p>否则出了问题很难排查。</p>
<h3 id="5-2-丢包、乱序、重复都是正常现象"><a href="#5-2-丢包、乱序、重复都是正常现象" class="headerlink" title="5.2 丢包、乱序、重复都是正常现象"></a>5.2 丢包、乱序、重复都是正常现象</h3><p>UDP 编程默认就要接受这些情况：</p>
<ul>
<li>某个包永远收不到</li>
<li>后发的包先到</li>
<li>同一个包被收到两次</li>
</ul>
<p>这不是异常，而是 UDP 的正常工作方式。</p>
<h3 id="5-3-尽量避免-IP-分片"><a href="#5-3-尽量避免-IP-分片" class="headerlink" title="5.3 尽量避免 IP 分片"></a>5.3 尽量避免 IP 分片</h3><p>如果 UDP 报文过大，IP 层可能分片。分片带来的问题：</p>
<ul>
<li>任一分片丢失，整个报文作废</li>
<li>网络设备对分片不友好</li>
<li>性能和稳定性都变差</li>
</ul>
<p>工程上通常建议：</p>
<ul>
<li>单个应用层包尽量控制在 <strong>MTU 以下</strong></li>
<li>以太网常见安全值可以先按 <code>1200</code> 字节左右设计</li>
</ul>
<p>这也是 QUIC 常见的保守做法之一。</p>
<h3 id="5-4-高性能场景通常要配合事件循环"><a href="#5-4-高性能场景通常要配合事件循环" class="headerlink" title="5.4 高性能场景通常要配合事件循环"></a>5.4 高性能场景通常要配合事件循环</h3><p>在 Linux 上，大量 UDP socket 或高频收发通常会配合：</p>
<ul>
<li><code>epoll</code></li>
<li>非阻塞 socket</li>
<li>定时器管理重传</li>
<li>批量收发（如 <code>recvmmsg/sendmmsg</code>）</li>
</ul>
<p>单线程阻塞式 <code>recvfrom()</code> 适合入门，不适合高并发服务。</p>
<hr>
<h2 id="6-一个最小-UDP-回显服务端"><a href="#6-一个最小-UDP-回显服务端" class="headerlink" title="6. 一个最小 UDP 回显服务端"></a>6. 一个最小 UDP 回显服务端</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">int</span> fd = socket(AF_INET, SOCK_DGRAM, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">addr</span>;</span></span><br><span class="line">    <span class="built_in">memset</span>(&amp;addr, <span class="number">0</span>, <span class="keyword">sizeof</span>(addr));</span><br><span class="line">    addr.sin_family = AF_INET;</span><br><span class="line">    addr.sin_addr.s_addr = htonl(INADDR_ANY);</span><br><span class="line">    addr.sin_port = htons(<span class="number">9999</span>);</span><br><span class="line"></span><br><span class="line">    bind(fd, (<span class="keyword">struct</span> sockaddr*)&amp;addr, <span class="keyword">sizeof</span>(addr));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="type">char</span> buf[<span class="number">2048</span>];</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">peer</span>;</span></span><br><span class="line">        <span class="type">socklen_t</span> len = <span class="keyword">sizeof</span>(peer);</span><br><span class="line"></span><br><span class="line">        <span class="type">ssize_t</span> n = recvfrom(fd, buf, <span class="keyword">sizeof</span>(buf), <span class="number">0</span>,</span><br><span class="line">                             (<span class="keyword">struct</span> sockaddr*)&amp;peer, &amp;len);</span><br><span class="line">        <span class="keyword">if</span> (n &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        sendto(fd, buf, (<span class="type">size_t</span>)n, <span class="number">0</span>,</span><br><span class="line">               (<span class="keyword">struct</span> sockaddr*)&amp;peer, len);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个例子足够说明 UDP 服务端的基本模型：</p>
<ul>
<li>一个 socket</li>
<li>不断收报文</li>
<li>每个报文都自带来源地址</li>
<li>回包时显式指定目标</li>
</ul>
<hr>
<h2 id="7-用-UDP-实现-TCP，应该怎么想"><a href="#7-用-UDP-实现-TCP，应该怎么想" class="headerlink" title="7. 用 UDP 实现 TCP，应该怎么想"></a>7. 用 UDP 实现 TCP，应该怎么想</h2><p>这里更准确的说法不是“把 TCP 原样重写一遍”，而是：</p>
<blockquote>
<p>在 UDP 之上补齐 TCP 的核心能力，做出一个“可靠、有序、可控”的传输层。</p>
</blockquote>
<p>这是很多实时网络框架的常见思路，例如：</p>
<ul>
<li><strong>KCP</strong>：在 UDP 上实现可靠传输，强调低延迟</li>
<li><strong>QUIC</strong>：在 UDP 上实现更现代的连接、多路复用、拥塞控制与加密</li>
</ul>
<h3 id="7-1-先分清目标"><a href="#7-1-先分清目标" class="headerlink" title="7.1 先分清目标"></a>7.1 先分清目标</h3><p>如果你只是想让消息“更可靠”，那通常只需要做：</p>
<ul>
<li>序号</li>
<li>ACK</li>
<li>超时重传</li>
<li>简单窗口</li>
</ul>
<p>如果你想完整模拟 TCP，则还要补：</p>
<ul>
<li>连接管理</li>
<li>按序交付</li>
<li>流量控制</li>
<li>拥塞控制</li>
<li>RTT 估计与重传定时器</li>
<li>关闭连接</li>
</ul>
<p>真正困难的部分其实不是“发包”，而是：</p>
<ul>
<li><strong>如何在复杂网络环境里稳定地控制发送节奏</strong></li>
</ul>
<h3 id="7-2-最小协议头可以这样设计"><a href="#7-2-最小协议头可以这样设计" class="headerlink" title="7.2 最小协议头可以这样设计"></a>7.2 最小协议头可以这样设计</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">PacketHeader</span> &#123;</span></span><br><span class="line">    <span class="type">uint32_t</span> conn_id;    <span class="comment">// 连接标识</span></span><br><span class="line">    <span class="type">uint32_t</span> seq;        <span class="comment">// 当前包序号</span></span><br><span class="line">    <span class="type">uint32_t</span> ack;        <span class="comment">// 已确认到的对端序号</span></span><br><span class="line">    <span class="type">uint16_t</span> flags;      <span class="comment">// SYN / ACK / FIN / RST 等</span></span><br><span class="line">    <span class="type">uint16_t</span> wnd;        <span class="comment">// 通告接收窗口</span></span><br><span class="line">    <span class="type">uint32_t</span> ts;         <span class="comment">// 时间戳，用于 RTT 估计</span></span><br><span class="line">    <span class="type">uint16_t</span> len;        <span class="comment">// payload 长度</span></span><br><span class="line">    <span class="type">uint16_t</span> checksum;   <span class="comment">// 包头+包体校验</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如果只是做“可靠消息”而不是“字节流”，这个头已经够搭框架了。</p>
<h3 id="7-3-把-TCP-的能力拆成-6-层功能"><a href="#7-3-把-TCP-的能力拆成-6-层功能" class="headerlink" title="7.3 把 TCP 的能力拆成 6 层功能"></a>7.3 把 TCP 的能力拆成 6 层功能</h3><h4 id="1-连接管理"><a href="#1-连接管理" class="headerlink" title="1. 连接管理"></a>1. 连接管理</h4><p>在 UDP 上自己定义连接状态：</p>
<ul>
<li><code>CLOSED</code></li>
<li><code>SYN_SENT</code></li>
<li><code>SYN_RECV</code></li>
<li><code>ESTABLISHED</code></li>
<li><code>FIN_WAIT</code></li>
</ul>
<p>可以直接借用 TCP 的思路：</p>
<ol>
<li>客户端发 <code>SYN(seq=x)</code></li>
<li>服务端回 <code>SYN|ACK(seq=y, ack=x+1)</code></li>
<li>客户端再发 <code>ACK(ack=y+1)</code></li>
</ol>
<p>这样做的价值是：</p>
<ul>
<li>双方都知道初始序号</li>
<li>可以建立会话状态</li>
<li>便于后续超时、重传和断线清理</li>
</ul>
<h4 id="2-可靠性"><a href="#2-可靠性" class="headerlink" title="2. 可靠性"></a>2. 可靠性</h4><p>发送端维护一个<strong>未确认发送队列</strong>：</p>
<ul>
<li>每发一个包，都放进 <code>unacked_map</code></li>
<li>记录发送时间、重传次数</li>
<li>定时扫描超时包并重发</li>
</ul>
<p>接收端收到包后返回 ACK。</p>
<p>最简单的 ACK 策略可以先做：</p>
<ul>
<li><strong>累计确认</strong>：<code>ack = 下一个期待收到的序号</code></li>
</ul>
<p>再进一步可做：</p>
<ul>
<li><strong>选择确认</strong>（SACK）：显式告诉对端哪些乱序包已经收到</li>
</ul>
<h4 id="3-有序交付"><a href="#3-有序交付" class="headerlink" title="3. 有序交付"></a>3. 有序交付</h4><p>如果包乱序到达：</p>
<ul>
<li>先放入接收缓冲区</li>
<li>只有当 <code>seq == expected_seq</code> 时才向上层提交</li>
<li>提交后继续检查后续缓存是否已连续</li>
</ul>
<p>这就是 TCP “按序交付”的核心。</p>
<h4 id="4-流量控制"><a href="#4-流量控制" class="headerlink" title="4. 流量控制"></a>4. 流量控制</h4><p>需要告诉对方：</p>
<ul>
<li>我这边接收缓冲还有多大</li>
</ul>
<p>也就是在包头里放 <code>wnd</code>，类似 TCP 的接收窗口。</p>
<p>否则发送方可能发得太快，把接收方内存顶爆。</p>
<h4 id="5-拥塞控制"><a href="#5-拥塞控制" class="headerlink" title="5. 拥塞控制"></a>5. 拥塞控制</h4><p>这是最难的一层。</p>
<p>最偷懒的版本可以先不做，只设固定发送速率，但这不是真正的 TCP 级能力。</p>
<p>更接近 TCP 的做法是引入：</p>
<ul>
<li>慢启动</li>
<li>拥塞避免</li>
<li>丢包后乘法减小</li>
</ul>
<p>如果没有拥塞控制，你的协议在局域网看起来正常，到了公网通常就会失控。</p>
<h4 id="6-关闭连接"><a href="#6-关闭连接" class="headerlink" title="6. 关闭连接"></a>6. 关闭连接</h4><p>同样可以模仿 TCP：</p>
<ul>
<li>一端发 <code>FIN</code></li>
<li>对端回 <code>ACK</code></li>
<li>双方完成收尾和资源回收</li>
</ul>
<p>否则 session 容易泄漏。</p>
<hr>
<h2 id="8-一个“UDP-版-TCP”最小实现框架"><a href="#8-一个“UDP-版-TCP”最小实现框架" class="headerlink" title="8. 一个“UDP 版 TCP”最小实现框架"></a>8. 一个“UDP 版 TCP”最小实现框架</h2><p>如果从工程结构上设计，可以拆成下面几层：</p>
<ol>
<li><strong>UdpSocket 层</strong><br>负责 <code>socket/bind/sendto/recvfrom</code> 和 <code>epoll</code></li>
<li><strong>Session 层</strong><br>用 <code>peer_addr + conn_id</code> 管理会话状态</li>
<li><strong>Reliability 层</strong><br>管理 <code>seq/ack</code>、发送窗口、重传队列、乱序缓冲</li>
<li><strong>Timer 层</strong><br>负责 RTO、心跳、超时断线</li>
<li><strong>Congestion &#x2F; Flow Control 层</strong><br>控制能发多少、发多快</li>
<li><strong>Application 层</strong><br>真正的业务协议</li>
</ol>
<p>一个发送端主循环大概是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">应用层提交消息</span><br><span class="line">-&gt; 分配 seq</span><br><span class="line">-&gt; 封包并发送</span><br><span class="line">-&gt; 放入未确认队列</span><br><span class="line">-&gt; 等待 ACK</span><br><span class="line">-&gt; 超时则重传</span><br><span class="line">-&gt; 收到 ACK 后从队列删除</span><br></pre></td></tr></table></figure>

<p>一个接收端主循环大概是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">收到 UDP 报文</span><br><span class="line">-&gt; 校验包头</span><br><span class="line">-&gt; 根据 conn_id 找到 session</span><br><span class="line">-&gt; 判断 seq 是否重复 / 乱序 / 正常</span><br><span class="line">-&gt; 更新接收窗口</span><br><span class="line">-&gt; 发送 ACK</span><br><span class="line">-&gt; 按序把数据交给上层</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="9-真正落地时的几个关键取舍"><a href="#9-真正落地时的几个关键取舍" class="headerlink" title="9. 真正落地时的几个关键取舍"></a>9. 真正落地时的几个关键取舍</h2><h3 id="9-1-先做“可靠消息”，再做“可靠字节流”"><a href="#9-1-先做“可靠消息”，再做“可靠字节流”" class="headerlink" title="9.1 先做“可靠消息”，再做“可靠字节流”"></a>9.1 先做“可靠消息”，再做“可靠字节流”</h3><p>如果目标是游戏状态同步、命令包、RPC：</p>
<ul>
<li>做<strong>可靠消息协议</strong>通常更简单</li>
</ul>
<p>如果非要完全模拟 TCP 的“字节流语义”，复杂度会显著上升，因为你还要处理：</p>
<ul>
<li>流式重组</li>
<li>半关闭</li>
<li>更复杂的缓冲管理</li>
</ul>
<h3 id="9-2-不要一开始就试图复刻内核-TCP"><a href="#9-2-不要一开始就试图复刻内核-TCP" class="headerlink" title="9.2 不要一开始就试图复刻内核 TCP"></a>9.2 不要一开始就试图复刻内核 TCP</h3><p>更现实的路线是：</p>
<ol>
<li>先做连接 + seq + ack + 重传</li>
<li>再做乱序缓存 + 滑动窗口</li>
<li>再补 RTT &#x2F; RTO</li>
<li>最后再考虑拥塞控制</li>
</ol>
<p>否则很容易一上来就把实现写散。</p>
<h3 id="9-3-现代工程更常见的是“在-UDP-上做定制协议”"><a href="#9-3-现代工程更常见的是“在-UDP-上做定制协议”" class="headerlink" title="9.3 现代工程更常见的是“在 UDP 上做定制协议”"></a>9.3 现代工程更常见的是“在 UDP 上做定制协议”</h3><p>很多时候我们并不是要“重新发明 TCP”，而是要：</p>
<ul>
<li>保留 UDP 的低时延和灵活性</li>
<li>只补自己需要的那部分可靠性</li>
</ul>
<p>例如：</p>
<ul>
<li>关键帧可靠重传</li>
<li>状态包不重传，只发送最新值</li>
<li>控制消息必须有 ACK</li>
</ul>
<p>这比完整复刻 TCP 更符合实时系统的实际需求。</p>
<hr>
<h2 id="10-小结"><a href="#10-小结" class="headerlink" title="10. 小结"></a>10. 小结</h2><p>可以把 UDP 记成一句话：</p>
<blockquote>
<p>UDP 提供的是“轻量发报文”的能力，可靠、有序、流控、拥塞控制都要你自己决定要不要补。</p>
</blockquote>
<p>如果要用 UDP 实现类似 TCP 的能力，最关键不是 API，而是这几个机制：</p>
<ul>
<li>序号 <code>seq</code></li>
<li>确认 <code>ack</code></li>
<li>超时重传</li>
<li>滑动窗口</li>
<li>按序交付</li>
<li>流控 &#x2F; 拥塞控制</li>
</ul>
<p>把这些拆开理解之后，“UDP 实现 TCP”就不再神秘，本质上就是：</p>
<ul>
<li><strong>在用户态自己维护传输状态机</strong></li>
</ul>
<hr>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><ul>
<li><a href="https://www.cnblogs.com/yangykaifa/p/19447080">Linux网络基础——传输层协议 UDP 与 TCP</a></li>
<li>RFC 768: UDP</li>
<li>RFC 793: TCP</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>Linux高性能服务器编程</category>
      </categories>
  </entry>
  <entry>
    <title>libevent 笔记</title>
    <url>//notes/linux%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B/1-libevent/</url>
    <content><![CDATA[<h1 id="libevent-笔记"><a href="#libevent-笔记" class="headerlink" title="libevent 笔记"></a>libevent 笔记</h1><h2 id="TCP-evconnlistener"><a href="#TCP-evconnlistener" class="headerlink" title="TCP evconnlistener"></a>TCP evconnlistener</h2><p>evconnlistener机制替代了之前socket bind listen 监听套字的创立</p>
<h3 id="创建evconnlistener-接口"><a href="#创建evconnlistener-接口" class="headerlink" title="创建evconnlistener 接口"></a>创建evconnlistener 接口</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">evconnlistener</span> *<span class="built_in">evconnlistener_new</span>(<span class="keyword">struct</span> event_base *base,</span><br><span class="line">    evconnlistener_cb cb, <span class="type">void</span> *ptr, <span class="type">unsigned</span> flags, <span class="type">int</span> backlog,</span><br><span class="line">    <span class="type">evutil_socket_t</span> fd);</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">evconnlistener</span> *<span class="built_in">evconnlistener_new_bind</span>(<span class="keyword">struct</span> event_base *base,</span><br><span class="line">    evconnlistener_cb cb, <span class="type">void</span> *ptr, <span class="type">unsigned</span> flags, <span class="type">int</span> backlog,</span><br><span class="line">    <span class="type">const</span> <span class="keyword">struct</span> sockaddr *sa, <span class="type">int</span> socklen);</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">evconnlistener_free</span><span class="params">(<span class="keyword">struct</span> evconnlistener *lev)</span></span>;</span><br></pre></td></tr></table></figure>
<p>evconnlistener_new和evconnlistener_new_bind都用于分配和返回一个新的用于监听连接的对象。 每个用于监听的对象都使用event_base来维护一个建立的连接。当有新的TCP 连接发生时，回调函数就会调用.</p>
<p>base： event_base对象。参考event_base。<br>cb： 回调函数， 当有新的TCP连接发生时，会唤醒回调函数。<br>ptr： 传递给回调函数的参数。<br>flags： 一些标志， 后面会进一步介绍。<br>backlog： 监听队列允许容纳的最大连接数。<br>fd： 函数evconnlistener_new假设我们已经绑定了套接字到要监听的端口上，fd参数就是我们已经绑定的socket套接字。<br>sa： evconnlistener_new_bind帮助我们绑定监听地址。sa就是传入的监听地址。<br>socklen： sa的长度。</p>
<p>一般通常情况下flags设置LEV_OPT_CLOSE_ON_FREE，LEV_OPT_REUSEABLE</p>
<h3 id="回调函数"><a href="#回调函数" class="headerlink" title="回调函数"></a>回调函数</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*evconnlistener_cb)</span><span class="params">(<span class="keyword">struct</span> evconnlistener *listener,</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="type">evutil_socket_t</span> sock, <span class="keyword">struct</span> sockaddr *addr, <span class="type">int</span> len, <span class="type">void</span> *ptr)</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="监听开启和关闭"><a href="#监听开启和关闭" class="headerlink" title="监听开启和关闭"></a>监听开启和关闭</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">evconnlistener_disable</span><span class="params">(<span class="keyword">struct</span> evconnlistener *lev)</span></span>;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">evconnlistener_enable</span><span class="params">(<span class="keyword">struct</span> evconnlistener *lev)</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="从enconnlistener-中获取信息"><a href="#从enconnlistener-中获取信息" class="headerlink" title="从enconnlistener 中获取信息"></a>从enconnlistener 中获取信息</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 获得一个evconnlistener对象的套接字</span></span><br><span class="line"><span class="function"><span class="type">evutil_socket_t</span> <span class="title">evconnlistener_get_fd</span><span class="params">(<span class="keyword">struct</span> evconnlistener *lev)</span></span>;</span><br><span class="line"><span class="comment">// 获得一个evconnlistener对象event_base对象</span></span><br><span class="line"><span class="function"><span class="type">evutil_socket_t</span> <span class="title">evconnlistener_get_base</span><span class="params">(<span class="keyword">struct</span> evconnlistener *lev)</span></span>;</span><br></pre></td></tr></table></figure>
<h3 id="完整示范"><a href="#完整示范" class="headerlink" title="完整示范"></a>完整示范</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/listener.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/bufferevent.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/buffer.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;errno.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">void</span></span></span><br><span class="line"><span class="function"><span class="title">echo_read_cb</span><span class="params">(<span class="keyword">struct</span> bufferevent *bev, <span class="type">void</span> *ctx)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">        <span class="comment">/*获取读入读出缓冲区*/</span></span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">evbuffer</span> *input = <span class="built_in">bufferevent_get_input</span>(bev);</span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">evbuffer</span> *output = <span class="built_in">bufferevent_get_output</span>(bev);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 将输入缓冲区的内容添加到输出缓冲区，实现回声功能 */</span></span><br><span class="line">        <span class="built_in">evbuffer_add_buffer</span>(output, input);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">void</span></span></span><br><span class="line"><span class="function"><span class="title">echo_event_cb</span><span class="params">(<span class="keyword">struct</span> bufferevent *bev, <span class="type">short</span> events, <span class="type">void</span> *ctx)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (events &amp; BEV_EVENT_ERROR)</span><br><span class="line">                <span class="built_in">perror</span>(<span class="string">&quot;Error from bufferevent&quot;</span>);</span><br><span class="line">        <span class="keyword">if</span> (events &amp; (BEV_EVENT_EOF | BEV_EVENT_ERROR)) &#123;</span><br><span class="line">                <span class="built_in">bufferevent_free</span>(bev);</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">void</span></span></span><br><span class="line"><span class="function"><span class="title">accept_conn_cb</span><span class="params">(<span class="keyword">struct</span> evconnlistener *listener,</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="type">evutil_socket_t</span> fd, <span class="keyword">struct</span> sockaddr *address, <span class="type">int</span> socklen,</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="type">void</span> *ctx)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">event_base</span> *base = <span class="built_in">evconnlistener_get_base</span>(listener);<span class="comment">//获取事件</span></span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">bufferevent</span> *bev = <span class="built_in">bufferevent_socket_new</span>(</span><br><span class="line">                base, fd, BEV_OPT_CLOSE_ON_FREE);<span class="comment">//建立连接</span></span><br><span class="line"></span><br><span class="line">        <span class="built_in">bufferevent_setcb</span>(bev, echo_read_cb, <span class="literal">NULL</span>, echo_event_cb, <span class="literal">NULL</span>);<span class="comment">//设置read和错误回调函数，传入参数为0</span></span><br><span class="line"></span><br><span class="line">        <span class="built_in">bufferevent_enable</span>(bev, EV_READ|EV_WRITE);<span class="comment">//设置出发条件，EV_READ 读入时触发，EV_WRITE 错误是触发</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">void</span></span></span><br><span class="line"><span class="function"><span class="title">accept_error_cb</span><span class="params">(<span class="keyword">struct</span> evconnlistener *listener, <span class="type">void</span> *ctx)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">event_base</span> *base = <span class="built_in">evconnlistener_get_base</span>(listener);</span><br><span class="line">        <span class="type">int</span> err = <span class="built_in">EVUTIL_SOCKET_ERROR</span>();</span><br><span class="line">        <span class="built_in">fprintf</span>(stderr, <span class="string">&quot;Got an error %d (%s) on the listener. &quot;</span></span><br><span class="line">                <span class="string">&quot;Shutting down.\n&quot;</span>, err, <span class="built_in">evutil_socket_error_to_string</span>(err));</span><br><span class="line"></span><br><span class="line">        <span class="built_in">event_base_loopexit</span>(base, <span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span></span></span><br><span class="line"><span class="function"><span class="title">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">event_base</span> *base;</span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">evconnlistener</span> *listener;</span><br><span class="line">        <span class="keyword">struct</span> <span class="title class_">sockaddr_in</span> sin;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> port = <span class="number">9876</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (argc &gt; <span class="number">1</span>) &#123;</span><br><span class="line">                port = <span class="built_in">atoi</span>(argv[<span class="number">1</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (port&lt;=<span class="number">0</span> || port&gt;<span class="number">65535</span>) &#123;</span><br><span class="line">                <span class="built_in">puts</span>(<span class="string">&quot;Invalid port&quot;</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        base = <span class="built_in">event_base_new</span>();</span><br><span class="line">        <span class="keyword">if</span> (!base) &#123;</span><br><span class="line">                <span class="built_in">puts</span>(<span class="string">&quot;Couldn&#x27;t open event base&quot;</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">memset</span>(&amp;sin, <span class="number">0</span>, <span class="built_in">sizeof</span>(sin));</span><br><span class="line"></span><br><span class="line">        sin.sin_family = AF_INET;</span><br><span class="line">        sin.sin_addr.s_addr = <span class="built_in">htonl</span>(<span class="number">0</span>);</span><br><span class="line">        sin.sin_port = <span class="built_in">htons</span>(port);</span><br><span class="line"></span><br><span class="line">        listener = <span class="built_in">evconnlistener_new_bind</span>(base, accept_conn_cb, <span class="literal">NULL</span>,</span><br><span class="line">            LEV_OPT_CLOSE_ON_FREE|LEV_OPT_REUSEABLE, <span class="number">-1</span>,</span><br><span class="line">            (<span class="keyword">struct</span> sockaddr*)&amp;sin, <span class="built_in">sizeof</span>(sin));</span><br><span class="line">        <span class="keyword">if</span> (!listener) &#123;</span><br><span class="line">                <span class="built_in">perror</span>(<span class="string">&quot;Couldn&#x27;t create listener&quot;</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">evconnlistener_set_error_cb</span>(listener, accept_error_cb);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">event_base_dispatch</span>(base);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="bufferevents"><a href="#bufferevents" class="headerlink" title="bufferevents"></a>bufferevents</h2><p>bufferevent 由一个底层传输系统（比如socket），一个读缓冲区和一个写缓冲区组成</p>
<h3 id="bufferevent和evbuffers"><a href="#bufferevent和evbuffers" class="headerlink" title="bufferevent和evbuffers"></a>bufferevent和evbuffers</h3><p>每一个bufferevent 都有一个输入缓冲区和输出缓冲区， 这些缓冲区（buffer）都是struct evbuffer 类型</p>
<h3 id="基于socket-的bufferevent"><a href="#基于socket-的bufferevent" class="headerlink" title="基于socket 的bufferevent"></a>基于socket 的bufferevent</h3><p>bufferevent_socket_new()<br>struct bufferevent *bufferevent_socket_new(<br>    struct event_base *base,<br>    evutil_socket_t fd,<br>    enum bufferevent_options options);</p>
<p>正常通过ip host进行访问 后续可以使用socket进行构建client客户端连接简化实现<br>int bufferevent_socket_connect(struct bufferevent *bev,<br>    struct sockaddr *address, int addrlen);<br>通过域名进行解析访问<br> int bufferevent_socket_connect_hostname(struct bufferevent *bev,<br>    struct evdns_base *dns_base, int family, const char *hostname,<br>    int port);<br>int bufferevent_socket_get_dns_error(struct bufferevent *bev);</p>
<p>注：<br>bufferevent_socket_connect和evconnlistener_new_bind的区别<br>bufferevent_socket_connect：用于主动建立连接，即作为客户端去连接服务器。<br>evconnlistener_new_bind：用于被动接受连接，即作为服务器监听端口并接受客户端的连接。</p>
<h3 id="回调函数-水位线操作"><a href="#回调函数-水位线操作" class="headerlink" title="回调函数 &amp; 水位线操作"></a>回调函数 &amp; 水位线操作</h3><p>水位线设置<br>void bufferevent_setwatermark(struct bufferevent *bufev, short events,<br>    size_t lowmark, size_t highmark);<br>events参数传入决定设置不同的水危险<br>EV_WRITE、 EV_READ</p>
<p>读、写、报错回调函数调用<br>void bufferevent_setcb(struct bufferevent *bufev,<br>    bufferevent_data_cb readcb, bufferevent_data_cb writecb,<br>    bufferevent_event_cb eventcb, void *cbarg);</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/event.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/bufferevent.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/buffer.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/util.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">info</span> &#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">char</span> *name;</span><br><span class="line">    <span class="type">size_t</span> total_drained;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">read_callback</span><span class="params">(<span class="keyword">struct</span> bufferevent *bev, <span class="type">void</span> *ctx)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">info</span> *inf = ctx;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">evbuffer</span> *input = <span class="built_in">bufferevent_get_input</span>(bev);</span><br><span class="line">    <span class="type">size_t</span> len = <span class="built_in">evbuffer_get_length</span>(input);</span><br><span class="line">    <span class="keyword">if</span> (len) &#123;</span><br><span class="line">        inf-&gt;total_drained += len;</span><br><span class="line">        <span class="built_in">evbuffer_drain</span>(input, len);<span class="comment">// 和evbuffer_remove()操作类似，但不执行copy工作仅删除长度len的缓冲</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Drained %lu bytes from %s\n&quot;</span>,</span><br><span class="line">             (<span class="type">unsigned</span> <span class="type">long</span>) len, inf-&gt;name);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">event_callback</span><span class="params">(<span class="keyword">struct</span> bufferevent *bev, <span class="type">short</span> events, <span class="type">void</span> *ctx)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">info</span> *inf = ctx;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">evbuffer</span> *input = <span class="built_in">bufferevent_get_input</span>(bev);</span><br><span class="line">    <span class="type">int</span> finished = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (events &amp; BEV_EVENT_EOF) &#123;</span><br><span class="line">        <span class="type">size_t</span> len = <span class="built_in">evbuffer_get_length</span>(input);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Got a close from %s.  We drained %lu bytes from it, &quot;</span></span><br><span class="line">            <span class="string">&quot;and have %lu left.\n&quot;</span>, inf-&gt;name,</span><br><span class="line">            (<span class="type">unsigned</span> <span class="type">long</span>)inf-&gt;total_drained, (<span class="type">unsigned</span> <span class="type">long</span>)len);</span><br><span class="line">        finished = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (events &amp; BEV_EVENT_ERROR) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;Got an error from %s: %s\n&quot;</span>,</span><br><span class="line">            inf-&gt;name, <span class="built_in">evutil_socket_error_to_string</span>(<span class="built_in">EVUTIL_SOCKET_ERROR</span>()));</span><br><span class="line">        finished = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (finished) &#123;</span><br><span class="line">        <span class="built_in">free</span>(ctx);</span><br><span class="line">        <span class="built_in">bufferevent_free</span>(bev);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">bufferevent</span> *<span class="built_in">setup_bufferevent</span>(<span class="type">void</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">bufferevent</span> *b1 = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">info</span> *info1;</span><br><span class="line"></span><br><span class="line">    info1 = <span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="keyword">struct</span> info));</span><br><span class="line">    info1-&gt;name = <span class="string">&quot;buffer 1&quot;</span>;</span><br><span class="line">    info1-&gt;total_drained = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* ... Here we should set up the bufferevent and make sure it gets</span></span><br><span class="line"><span class="comment">       connected... */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Trigger the read callback only whenever there is at least 128 bytes</span></span><br><span class="line"><span class="comment">       of data in the buffer. */</span></span><br><span class="line">    <span class="built_in">bufferevent_setwatermark</span>(b1, EV_READ, <span class="number">128</span>, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">bufferevent_setcb</span>(b1, read_callback, <span class="literal">NULL</span>, event_callback, info1);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">bufferevent_enable</span>(b1, EV_READ); <span class="comment">/* Start reading. */</span></span><br><span class="line">    <span class="keyword">return</span> b1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="bufferevent中的数据操作"><a href="#bufferevent中的数据操作" class="headerlink" title="bufferevent中的数据操作"></a>bufferevent中的数据操作</h3><p>获取不同缓冲区的evbuffer描述符<br>struct evbuffer *bufferevent_get_input(struct bufferevent *bufev);<br>struct evbuffer *bufferevent_get_output(struct bufferevent *bufev);</p>
<p>写入读入操作<br>int  bufferevent_write(struct  bufferevent <em>bufev,  const  void</em>data,  size_t  size);<br>int  bufferevent_write_buffer(struct  bufferevent <em>bufev,  struct  evbuffer</em>buf);</p>
<p>size_t bufferevent_read(struct bufferevent *bufev, void *data, size_t size);<br>int bufferevent_read_buffer(struct bufferevent *bufev,<br>    struct evbuffer *buf);</p>
<h3 id="程序自我通信"><a href="#程序自我通信" class="headerlink" title="程序自我通信"></a>程序自我通信</h3><p>int bufferevent_pair_new(struct event_base *base, int options,<br>    struct bufferevent *pair[2]);</p>
<p>pair[0]和pair[1] 设置为一对相互连接的bufferevent</p>
<h2 id="evbuffer缓冲IO的实用功能"><a href="#evbuffer缓冲IO的实用功能" class="headerlink" title="evbuffer缓冲IO的实用功能"></a>evbuffer缓冲IO的实用功能</h2><p>创建和释放<br>struct evbuffer *evbuffer_new(void);<br>void evbuffer_free(struct evbuffer *buf);</p>
]]></content>
      <categories>
        <category>学习</category>
        <category>Linux高性能服务器编程</category>
      </categories>
  </entry>
  <entry>
    <title>网络开发总结</title>
    <url>//notes/%E5%AE%9E%E6%97%B6%E7%AB%9E%E6%8A%80%E6%B8%B8%E6%88%8F%E5%BC%80%E5%8F%91/%E7%BD%91%E7%BB%9C/</url>
    <content><![CDATA[<h1 id="网络开发总结"><a href="#网络开发总结" class="headerlink" title="网络开发总结"></a>网络开发总结</h1><h2 id="模块"><a href="#模块" class="headerlink" title="模块"></a>模块</h2><h3 id="NetCodec"><a href="#NetCodec" class="headerlink" title="NetCodec"></a>NetCodec</h3><ol>
<li><p>Input<br> ID,count, client(本端)最新tick, clientAckServerTick（server确认最新tick） cmd包</p>
</li>
<li><p>Ack<br> 权威tick， 最后tick 以及hash验证</p>
</li>
<li><p>Statae<br> 权威状态包</p>
</li>
<li><p>Start<br> 游戏开始通知</p>
</li>
</ol>
<h3 id="NetStub"><a href="#NetStub" class="headerlink" title="NetStub"></a>NetStub</h3><pre><code>废弃 tick 模拟状态发送和打印监控，状态发送已改为键盘输入
</code></pre>
<h3 id="Packets"><a href="#Packets" class="headerlink" title="Packets"></a>Packets</h3><ol>
<li>PacketType 状态，input ack state start</li>
<li>InputPacket 包 冗余发松多个InputCmd 包以防丢包</li>
<li>start 包<br> 对局状态信息</li>
</ol>
<h3 id="UdpSocket"><a href="#UdpSocket" class="headerlink" title="UdpSocket"></a>UdpSocket</h3><p>udp 连接比tcp简单，持续监听端口</p>
]]></content>
      <categories>
        <category>学习</category>
        <category>实时竞技游戏开发</category>
      </categories>
  </entry>
  <entry>
    <title>Linux 高性能服务器编程</title>
    <url>//notes/linux%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="Linux-高性能服务器编程"><a href="#Linux-高性能服务器编程" class="headerlink" title="Linux 高性能服务器编程"></a>Linux 高性能服务器编程</h1><p>时间：2026&#x2F;04&#x2F;10</p>
<blockquote>
<p>这组笔记当前已经覆盖 UDP、libevent 入门，以及一篇偏工程化的 Linux 服务端进阶专题。<br>阅读时可以先分清“传输语义”“事件驱动模型”“协议与线程分层”这三条线，再把它们串成完整服务端链路。</p>
</blockquote>
<hr>
<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><h3 id="1-UDP-通信"><a href="#1-UDP-通信" class="headerlink" title="1. UDP 通信"></a>1. <a href="/notes/linux%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B/udp/">UDP 通信</a></h3><p>内容重点：</p>
<ul>
<li>UDP 与 TCP 的语义差异</li>
<li>Linux 下的常用 UDP 接口</li>
<li>UDP 服务端的基本工作流</li>
<li>在 UDP 上实现类似 TCP 的可靠传输思路</li>
</ul>
<p>适合先看，因为它能把：</p>
<ul>
<li>报文边界</li>
<li>丢包 &#x2F; 乱序 &#x2F; 重复</li>
<li>可靠性到底由谁负责</li>
</ul>
<p>这些底层问题先讲清楚。</p>
<h3 id="2-1-libevent-md"><a href="#2-1-libevent-md" class="headerlink" title="2. [1]libevent.md"></a>2. <a href="/notes/linux%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B/1-libevent/">[1]libevent.md</a></h3><p>内容重点：</p>
<ul>
<li><code>evconnlistener</code></li>
<li>TCP 监听与连接接入</li>
<li><code>bufferevent</code> 的基础概念</li>
<li>一个 echo server 的最小示例</li>
</ul>
<p>更偏服务端事件驱动入门。</p>
<h3 id="3-2-libevent-md"><a href="#3-2-libevent-md" class="headerlink" title="3. [2]libevent.md"></a>3. <a href="/notes/linux%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B/2-libevent/">[2]libevent.md</a></h3><p>内容重点：</p>
<ul>
<li><code>bufferevent_socket_new</code></li>
<li><code>bufferevent_socket_connect</code></li>
<li>回调、水位、超时</li>
<li>一个最小客户端连接示例</li>
</ul>
<p>更偏“如何基于 libevent 发起连接并管理 I&#x2F;O”。</p>
<h3 id="4-高性能服务器编程笔记一"><a href="#4-高性能服务器编程笔记一" class="headerlink" title="4. 高性能服务器编程笔记一"></a>4. <a href="/notes/linux%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B/%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B%E7%AC%94%E8%AE%B0%E4%B8%80/">高性能服务器编程笔记一</a></h3><p>内容重点：</p>
<ul>
<li><code>epoll</code> 的 LT &#x2F; ET 与 Reactor</li>
<li>非阻塞 socket 的常见错误处理</li>
<li><code>timerfd</code> &#x2F; <code>eventfd</code> &#x2F; <code>signalfd</code></li>
<li>线程池与 I&#x2F;O 线程分层</li>
<li>HTTP 解析与应用层协议设计</li>
<li>Linux 网络调优参数与排障方法</li>
</ul>
<p>更偏“把一个事件驱动服务端补成可落地工程模型”。</p>
<hr>
<h2 id="建议阅读顺序"><a href="#建议阅读顺序" class="headerlink" title="建议阅读顺序"></a>建议阅读顺序</h2><ol>
<li><code>UDP.md</code></li>
<li><code>[1]libevent.md</code></li>
<li><code>[2]libevent.md</code></li>
<li><code>高性能服务器编程笔记一.md</code></li>
</ol>
<p>原因很简单：</p>
<ul>
<li>先理解传输层语义</li>
<li>再看事件驱动服务端如何接连接</li>
<li>最后看 <code>bufferevent</code> 客户端和缓冲管理</li>
<li>再补 <code>epoll</code>、线程模型、协议与调优这些工程问题</li>
</ul>
<hr>
<h2 id="已补充的进阶主题"><a href="#已补充的进阶主题" class="headerlink" title="已补充的进阶主题"></a>已补充的进阶主题</h2><ul>
<li><code>epoll</code> 的 LT &#x2F; ET 模式与 Reactor</li>
<li>非阻塞 socket 的错误处理</li>
<li><code>timerfd</code> &#x2F; <code>eventfd</code> &#x2F; <code>signalfd</code></li>
<li>线程池与 I&#x2F;O 线程分层</li>
<li>HTTP 解析与应用层协议设计</li>
<li>Linux 网络调优参数与排障方法</li>
</ul>
<h2 id="后续仍可补的主题"><a href="#后续仍可补的主题" class="headerlink" title="后续仍可补的主题"></a>后续仍可补的主题</h2><ul>
<li><code>sendfile</code> &#x2F; <code>splice</code> &#x2F; <code>mmap</code> 与零拷贝</li>
<li><code>SO_REUSEPORT</code>、RSS &#x2F; RPS &#x2F; XPS 与多队列</li>
<li><code>io_uring</code> 与传统 Reactor 的差异</li>
<li>数据库 &#x2F; 缓存 &#x2F; RPC 接入后的背压治理</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>Linux高性能服务器编程</category>
      </categories>
  </entry>
  <entry>
    <title>高性能服务器编程笔记一</title>
    <url>//notes/linux%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B/%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B%E7%AC%94%E8%AE%B0%E4%B8%80/</url>
    <content><![CDATA[<h1 id="高性能服务器编程笔记一"><a href="#高性能服务器编程笔记一" class="headerlink" title="高性能服务器编程笔记一"></a>高性能服务器编程笔记一</h1><p>时间：2026&#x2F;04&#x2F;10</p>
<blockquote>
<p>关键词：<code>epoll</code>、LT &#x2F; ET、Reactor、非阻塞 socket、<code>timerfd</code>、<code>eventfd</code>、<code>signalfd</code>、线程池、HTTP、网络调优<br>核心目标：把 Linux 服务端里最常见的一条工程主线串起来，即“事件循环 + 非阻塞 I&#x2F;O + 协议解析 + 任务分发 + 排障调优”。</p>
</blockquote>
<hr>
<h2 id="1-epoll-的-LT-ET-模式与-Reactor"><a href="#1-epoll-的-LT-ET-模式与-Reactor" class="headerlink" title="1. epoll 的 LT &#x2F; ET 模式与 Reactor"></a>1. <code>epoll</code> 的 LT &#x2F; ET 模式与 Reactor</h2><h3 id="1-1-Reactor-到底在做什么"><a href="#1-1-Reactor-到底在做什么" class="headerlink" title="1.1 Reactor 到底在做什么"></a>1.1 Reactor 到底在做什么</h3><p>Reactor 的核心不是“某个库”，而是一种组织方式：</p>
<ol>
<li>用 <code>epoll_wait()</code> 等待一批就绪事件</li>
<li>根据 fd 和事件类型找到对应处理器</li>
<li>执行 <code>accept/read/write/timer/signal</code> 等处理逻辑</li>
<li>业务计算如果比较重，就把它移交给工作线程</li>
<li>结果回到 I&#x2F;O 线程后，再负责发包</li>
</ol>
<p>可以把它理解成：</p>
<blockquote>
<p><code>epoll</code> 负责告诉你“谁准备好了”，Reactor 负责定义“准备好之后该怎么处理”。</p>
</blockquote>
<p>一个典型 TCP Reactor 流程：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">listen fd -&gt; accept -&gt; conn fd</span><br><span class="line">conn fd readable -&gt; read into buffer</span><br><span class="line">buffer -&gt; parse protocol -&gt; dispatch business task</span><br><span class="line">business result -&gt; enqueue response</span><br><span class="line">conn fd writable -&gt; flush output buffer</span><br></pre></td></tr></table></figure>

<h3 id="1-2-LT-和-ET-的区别"><a href="#1-2-LT-和-ET-的区别" class="headerlink" title="1.2 LT 和 ET 的区别"></a>1.2 LT 和 ET 的区别</h3><table>
<thead>
<tr>
<th>模式</th>
<th>含义</th>
<th>行为特点</th>
<th>优点</th>
<th>风险</th>
</tr>
</thead>
<tbody><tr>
<td>LT</td>
<td>Level Triggered，水平触发</td>
<td>只要 fd 仍可读&#x2F;可写，就会反复通知</td>
<td>语义直观，代码更稳</td>
<td>重复通知较多</td>
</tr>
<tr>
<td>ET</td>
<td>Edge Triggered，边沿触发</td>
<td>只在状态从“不可读”变“可读”时通知一次</td>
<td>通知更少，适合高并发</td>
<td>容易因为没读干净而丢事件</td>
</tr>
</tbody></table>
<p>LT 是默认模式，更像“提醒式”：</p>
<ul>
<li>套接字里还有数据没读完，下一轮 <code>epoll_wait()</code> 还会继续通知</li>
<li>对初学和排查问题更友好</li>
</ul>
<p>ET 更像“状态变化通知”：</p>
<ul>
<li>一次从无到有的变化，只提醒一次</li>
<li>如果你这次没把数据读到 <code>EAGAIN</code>，内核通常不会再额外提醒你</li>
</ul>
<p>所以 ET 的两个前提几乎是硬规则：</p>
<ul>
<li>fd 必须设成非阻塞</li>
<li><code>read/write/accept</code> 都要循环到 <code>EAGAIN</code> 或 <code>EWOULDBLOCK</code></li>
</ul>
<h3 id="1-3-ET-下为什么必须“读空-写尽”"><a href="#1-3-ET-下为什么必须“读空-写尽”" class="headerlink" title="1.3 ET 下为什么必须“读空 &#x2F; 写尽”"></a>1.3 ET 下为什么必须“读空 &#x2F; 写尽”</h3><p>ET 的常见错误写法是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 错误示意：只读一次</span></span><br><span class="line"><span class="type">ssize_t</span> n = recv(fd, buf, <span class="keyword">sizeof</span>(buf), <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>如果内核缓冲区里其实还有数据没读完，那么这批“剩余数据”不会再触发新边沿，连接就可能卡住。</p>
<p>正确思路是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    <span class="type">ssize_t</span> n = recv(fd, buf, <span class="keyword">sizeof</span>(buf), <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 处理数据</span></span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (n == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 对端关闭</span></span><br><span class="line">        close(fd);</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (errno == EINTR) &#123;</span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (errno == EAGAIN || errno == EWOULDBLOCK) &#123;</span><br><span class="line">        <span class="comment">// 真的读空了</span></span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 其他错误</span></span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>accept()</code> 也是同样逻辑。监听 fd 用 ET 时，应该把已完成连接队列里的连接一次性 <code>accept</code> 干净。</p>
<h3 id="1-4-epoll-中常见的事件组合"><a href="#1-4-epoll-中常见的事件组合" class="headerlink" title="1.4 epoll 中常见的事件组合"></a>1.4 <code>epoll</code> 中常见的事件组合</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">ev.events = EPOLLIN | EPOLLRDHUP | EPOLLERR;</span><br></pre></td></tr></table></figure>

<p>常见关注点：</p>
<ul>
<li><code>EPOLLIN</code>：可读</li>
<li><code>EPOLLOUT</code>：可写</li>
<li><code>EPOLLRDHUP</code>：对端半关闭，常用于尽早发现连接关闭</li>
<li><code>EPOLLERR</code>：错误事件</li>
<li><code>EPOLLHUP</code>：挂起</li>
<li><code>EPOLLET</code>：启用 ET</li>
<li><code>EPOLLONESHOT</code>：触发一次后自动失活，适合多线程下避免同一连接被并发处理</li>
</ul>
<p>工程上常见搭配：</p>
<ul>
<li>单线程 Reactor：<code>EPOLLIN | EPOLLRDHUP</code></li>
<li>ET Reactor：<code>EPOLLIN | EPOLLRDHUP | EPOLLET</code></li>
<li>多线程连接处理：<code>EPOLLIN | EPOLLRDHUP | EPOLLONESHOT</code></li>
</ul>
<p>还有一个常见实践：</p>
<ul>
<li><code>EPOLLOUT</code> 往往只在“发送缓冲里还有没发完的数据”时才临时关注</li>
<li>如果平时一直常驻监听 <code>EPOLLOUT</code>，很多 socket 会长期表现为“可写”，导致无意义唤醒</li>
</ul>
<h3 id="1-5-LT-ET-怎么选"><a href="#1-5-LT-ET-怎么选" class="headerlink" title="1.5 LT &#x2F; ET 怎么选"></a>1.5 LT &#x2F; ET 怎么选</h3><p>建议顺序很明确：</p>
<ol>
<li>先用 LT 把协议、状态机、错误处理做对</li>
<li>确认瓶颈真的在事件通知频率，再考虑 ET</li>
<li>如果用了 ET，先检查“是否所有 I&#x2F;O 路径都循环到 <code>EAGAIN</code>”</li>
</ol>
<p>不要把 ET 当成“默认更高级”的模式。很多线上服务最后依然选择 LT，因为：</p>
<ul>
<li>业务瓶颈常常不在 <code>epoll</code></li>
<li>LT 更容易做正确</li>
<li>排障成本明显更低</li>
</ul>
<hr>
<h2 id="2-非阻塞-socket-的错误处理"><a href="#2-非阻塞-socket-的错误处理" class="headerlink" title="2. 非阻塞 socket 的错误处理"></a>2. 非阻塞 socket 的错误处理</h2><h3 id="2-1-非阻塞不是“没有阻塞”，而是“阻塞变成返回码”"><a href="#2-1-非阻塞不是“没有阻塞”，而是“阻塞变成返回码”" class="headerlink" title="2.1 非阻塞不是“没有阻塞”，而是“阻塞变成返回码”"></a>2.1 非阻塞不是“没有阻塞”，而是“阻塞变成返回码”</h3><p>设置非阻塞通常是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> flags = fcntl(fd, F_GETFL, <span class="number">0</span>);</span><br><span class="line">fcntl(fd, F_SETFL, flags | O_NONBLOCK);</span><br></pre></td></tr></table></figure>

<p>设置后，很多原本会阻塞的操作不再睡眠，而是直接返回：</p>
<ul>
<li><code>EAGAIN</code></li>
<li><code>EWOULDBLOCK</code></li>
<li><code>EINPROGRESS</code></li>
</ul>
<p>这意味着：</p>
<blockquote>
<p>非阻塞编程的关键不是“调用成功”，而是“正确解释暂时失败”。</p>
</blockquote>
<h3 id="2-2-connect-的处理"><a href="#2-2-connect-的处理" class="headerlink" title="2.2 connect() 的处理"></a>2.2 <code>connect()</code> 的处理</h3><p>非阻塞 <code>connect()</code> 最常见的返回是：</p>
<ul>
<li><code>0</code>：立即连接成功</li>
<li><code>-1 + errno == EINPROGRESS</code>：连接正在进行中，这是正常路径</li>
<li><code>-1 + errno == EINTR</code>：被信号打断，通常继续按“连接中”处理或重试检查</li>
<li><code>-1 + ECONNREFUSED / ENETUNREACH / ETIMEDOUT</code>：明确失败</li>
</ul>
<p><code>EINPROGRESS</code> 之后，不能仅凭“fd 可写”就认定连接成功。正确做法是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> err = <span class="number">0</span>;</span><br><span class="line"><span class="type">socklen_t</span> len = <span class="keyword">sizeof</span>(err);</span><br><span class="line">getsockopt(fd, SOL_SOCKET, SO_ERROR, &amp;err, &amp;len);</span><br><span class="line"><span class="keyword">if</span> (err == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// connect 成功</span></span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// connect 失败，err 才是真正错误码</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-3-accept-的处理"><a href="#2-3-accept-的处理" class="headerlink" title="2.3 accept() 的处理"></a>2.3 <code>accept()</code> 的处理</h3><p>非阻塞监听 fd 常见返回：</p>
<ul>
<li><code>EAGAIN / EWOULDBLOCK</code>：当前已没有新连接，退出循环</li>
<li><code>EINTR</code>：被信号打断，继续</li>
<li><code>ECONNABORTED</code>：连接在 <code>accept</code> 前就中止了，记录后继续接下一个</li>
<li><code>EMFILE / ENFILE</code>：进程或系统 fd 用尽，这是高优先级故障</li>
</ul>
<p><code>EMFILE</code> 的工程应对通常包括：</p>
<ul>
<li>提前把 <code>ulimit -n</code> 和系统文件句柄上限调够</li>
<li>预留一个“空闲 fd”作为应急位</li>
<li>日志里把错误打出来，不要静默丢掉</li>
</ul>
<h3 id="2-4-recv-read-的处理"><a href="#2-4-recv-read-的处理" class="headerlink" title="2.4 recv() &#x2F; read() 的处理"></a>2.4 <code>recv()</code> &#x2F; <code>read()</code> 的处理</h3><table>
<thead>
<tr>
<th>返回值 &#x2F; 错误码</th>
<th>含义</th>
<th>处理建议</th>
</tr>
</thead>
<tbody><tr>
<td><code>n &gt; 0</code></td>
<td>收到数据</td>
<td>进入协议解析</td>
</tr>
<tr>
<td><code>n == 0</code></td>
<td>对端正常关闭</td>
<td>回收连接</td>
</tr>
<tr>
<td><code>EAGAIN/EWOULDBLOCK</code></td>
<td>当前读空了</td>
<td>等下次读事件</td>
</tr>
<tr>
<td><code>EINTR</code></td>
<td>被信号打断</td>
<td>继续读</td>
</tr>
<tr>
<td><code>ECONNRESET</code></td>
<td>对端复位</td>
<td>记录并关闭</td>
</tr>
<tr>
<td>其他错误</td>
<td>异常</td>
<td>记录并关闭</td>
</tr>
</tbody></table>
<p>注意：</p>
<ul>
<li><code>n == 0</code> 不是“没数据”，而是 EOF</li>
<li><code>EAGAIN</code> 不是错误，而是“暂时读不到”</li>
</ul>
<h3 id="2-5-send-write-的处理"><a href="#2-5-send-write-的处理" class="headerlink" title="2.5 send() &#x2F; write() 的处理"></a>2.5 <code>send()</code> &#x2F; <code>write()</code> 的处理</h3><p>非阻塞写最容易踩的坑有两个：</p>
<ol>
<li>只写了一部分</li>
<li>对已关闭连接写，触发 <code>EPIPE</code> &#x2F; <code>SIGPIPE</code></li>
</ol>
<p>正确思路：</p>
<ul>
<li>维护用户态发送缓冲区</li>
<li><code>send()</code> 返回部分字节时，把未发送部分保留到下次继续</li>
<li><code>EAGAIN</code> 时注册 <code>EPOLLOUT</code>，等可写后继续刷</li>
<li>用 <code>MSG_NOSIGNAL</code> 或忽略 <code>SIGPIPE</code></li>
</ul>
<p>一个典型发送循环：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (;;) &#123;</span><br><span class="line">    <span class="type">ssize_t</span> n = send(fd, out + sent, len - sent, MSG_NOSIGNAL);</span><br><span class="line">    <span class="keyword">if</span> (n &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        sent += (<span class="type">size_t</span>)n;</span><br><span class="line">        <span class="keyword">if</span> (sent == len) &#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (n &lt; <span class="number">0</span> &amp;&amp; errno == EINTR) &#123;</span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (n &lt; <span class="number">0</span> &amp;&amp; (errno == EAGAIN || errno == EWOULDBLOCK)) &#123;</span><br><span class="line">        <span class="comment">// 还没发完，等 EPOLLOUT</span></span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// EPIPE / ECONNRESET / 其他错误</span></span><br><span class="line">    close(fd);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-6-一些工程上必须记住的点"><a href="#2-6-一些工程上必须记住的点" class="headerlink" title="2.6 一些工程上必须记住的点"></a>2.6 一些工程上必须记住的点</h3><ul>
<li><code>EINTR</code> 通常不是失败，而是“重来一次”</li>
<li><code>EAGAIN</code> 通常不是失败，而是“现在先停一下”</li>
<li><code>EPOLLERR</code> 到来时，要用 <code>getsockopt(... SO_ERROR ...)</code> 取真实错误</li>
<li>半关闭连接要配合 <code>EPOLLRDHUP</code>、协议状态和发送缓冲综合判断</li>
<li>非阻塞只是避免线程睡死，不会替你处理半包、乱序状态和应用层背压</li>
</ul>
<hr>
<h2 id="3-timerfd-eventfd-signalfd"><a href="#3-timerfd-eventfd-signalfd" class="headerlink" title="3. timerfd &#x2F; eventfd &#x2F; signalfd"></a>3. <code>timerfd</code> &#x2F; <code>eventfd</code> &#x2F; <code>signalfd</code></h2><h3 id="3-1-为什么这三个-fd-很重要"><a href="#3-1-为什么这三个-fd-很重要" class="headerlink" title="3.1 为什么这三个 fd 很重要"></a>3.1 为什么这三个 fd 很重要</h3><p>它们的共同价值是：</p>
<blockquote>
<p>把“时间、线程通知、信号”也统一成 fd，交给 <code>epoll</code> 一起处理。</p>
</blockquote>
<p>这样事件循环里就不需要：</p>
<ul>
<li>到处插异步回调</li>
<li>自己维护复杂的管道唤醒逻辑</li>
<li>在信号处理函数里做太多不安全操作</li>
</ul>
<h3 id="3-2-timerfd"><a href="#3-2-timerfd" class="headerlink" title="3.2 timerfd"></a>3.2 <code>timerfd</code></h3><p><code>timerfd</code> 用于把定时器变成可读 fd。</p>
<p>常见用途：</p>
<ul>
<li>心跳检测</li>
<li>超时连接清理</li>
<li>定时重传</li>
<li>周期性统计采样</li>
</ul>
<p>基本用法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> tfd = timerfd_create(CLOCK_MONOTONIC, TFD_NONBLOCK | TFD_CLOEXEC);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">itimerspec</span> <span class="title">its</span>;</span></span><br><span class="line"><span class="built_in">memset</span>(&amp;its, <span class="number">0</span>, <span class="keyword">sizeof</span>(its));</span><br><span class="line">its.it_value.tv_sec = <span class="number">1</span>;      <span class="comment">// 1 秒后首次触发</span></span><br><span class="line">its.it_interval.tv_sec = <span class="number">1</span>;   <span class="comment">// 之后每秒触发一次</span></span><br><span class="line">timerfd_settime(tfd, <span class="number">0</span>, &amp;its, <span class="literal">NULL</span>);</span><br></pre></td></tr></table></figure>

<p>事件到来后要 <code>read()</code> 一个 <code>uint64_t</code>：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">uint64_t</span> expirations = <span class="number">0</span>;</span><br><span class="line">read(tfd, &amp;expirations, <span class="keyword">sizeof</span>(expirations));</span><br></pre></td></tr></table></figure>

<p>这个值表示自上次读取以来一共超时了多少次，避免丢 tick。</p>
<h3 id="3-3-eventfd"><a href="#3-3-eventfd" class="headerlink" title="3.3 eventfd"></a>3.3 <code>eventfd</code></h3><p><code>eventfd</code> 本质是一个 64 位计数器，特别适合做线程间唤醒。</p>
<p>常见用途：</p>
<ul>
<li>工作线程把结果投递给 I&#x2F;O 线程后，唤醒它</li>
<li>多生产者向事件循环提交异步任务</li>
<li>替代“自建 pipe + 一个字节通知”的老办法</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> efd = eventfd(<span class="number">0</span>, EFD_NONBLOCK | EFD_CLOEXEC);</span><br></pre></td></tr></table></figure>

<p>写端：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">uint64_t</span> one = <span class="number">1</span>;</span><br><span class="line">write(efd, &amp;one, <span class="keyword">sizeof</span>(one));</span><br></pre></td></tr></table></figure>

<p>读端：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">uint64_t</span> cnt = <span class="number">0</span>;</span><br><span class="line">read(efd, &amp;cnt, <span class="keyword">sizeof</span>(cnt));</span><br></pre></td></tr></table></figure>

<p>读出来的是累计值，所以常见模式是：</p>
<ul>
<li>读一次把计数清掉</li>
<li>顺手把任务队列里的任务批量取完</li>
</ul>
<h3 id="3-4-signalfd"><a href="#3-4-signalfd" class="headerlink" title="3.4 signalfd"></a>3.4 <code>signalfd</code></h3><p><code>signalfd</code> 用来把信号也纳入事件循环。</p>
<p>典型场景：</p>
<ul>
<li>收到 <code>SIGTERM</code> 时优雅退出</li>
<li>收到 <code>SIGHUP</code> 时重载配置</li>
<li>不想把复杂逻辑塞进传统信号处理函数</li>
</ul>
<p>用法关键点：</p>
<ol>
<li>先用 <code>sigprocmask()</code> 阻塞这些信号</li>
<li>再创建 <code>signalfd</code></li>
<li>在 <code>epoll</code> 里监听它</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">sigset_t</span> mask;</span><br><span class="line">sigemptyset(&amp;mask);</span><br><span class="line">sigaddset(&amp;mask, SIGINT);</span><br><span class="line">sigaddset(&amp;mask, SIGTERM);</span><br><span class="line">sigprocmask(SIG_BLOCK, &amp;mask, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> sfd = signalfd(<span class="number">-1</span>, &amp;mask, SFD_NONBLOCK | SFD_CLOEXEC);</span><br></pre></td></tr></table></figure>

<p>读取后可得到：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">signalfd_siginfo</span> <span class="title">si</span>;</span></span><br><span class="line">read(sfd, &amp;si, <span class="keyword">sizeof</span>(si));</span><br></pre></td></tr></table></figure>

<p>然后根据 <code>si.ssi_signo</code> 做统一处理。</p>
<h3 id="3-5-三者如何和-Reactor-配合"><a href="#3-5-三者如何和-Reactor-配合" class="headerlink" title="3.5 三者如何和 Reactor 配合"></a>3.5 三者如何和 Reactor 配合</h3><p>一个典型事件循环会把它们全放进 <code>epoll</code>：</p>
<ul>
<li>连接 fd：读写事件</li>
<li><code>timerfd</code>：超时检查、定时任务</li>
<li><code>eventfd</code>：跨线程任务投递</li>
<li><code>signalfd</code>：退出 &#x2F; 重载 &#x2F; 信号治理</li>
</ul>
<p>这样主循环就更像一个统一调度器，而不是“网络逻辑 + 一堆额外异步机制拼起来的东西”。</p>
<hr>
<h2 id="4-线程池与-I-O-线程分层"><a href="#4-线程池与-I-O-线程分层" class="headerlink" title="4. 线程池与 I&#x2F;O 线程分层"></a>4. 线程池与 I&#x2F;O 线程分层</h2><h3 id="4-1-一个更稳的分层模型"><a href="#4-1-一个更稳的分层模型" class="headerlink" title="4.1 一个更稳的分层模型"></a>4.1 一个更稳的分层模型</h3><p>高性能服务器里，一个常见的稳定结构是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">主线程/Acceptor</span><br><span class="line">    -&gt; 接连接并分发到某个 I/O 线程</span><br><span class="line"></span><br><span class="line">I/O 线程</span><br><span class="line">    -&gt; 跑 event loop</span><br><span class="line">    -&gt; 管 socket、收发包、协议解析、连接状态机</span><br><span class="line"></span><br><span class="line">Worker 线程池</span><br><span class="line">    -&gt; 跑 CPU 密集或潜在阻塞的业务逻辑</span><br><span class="line"></span><br><span class="line">回到 I/O 线程</span><br><span class="line">    -&gt; 序列化响应并发送</span><br></pre></td></tr></table></figure>

<p>核心原则是：</p>
<blockquote>
<p>socket 的生命周期和输出顺序，最好由固定 I&#x2F;O 线程拥有。</p>
</blockquote>
<h3 id="4-2-为什么不让工作线程直接操作-socket"><a href="#4-2-为什么不让工作线程直接操作-socket" class="headerlink" title="4.2 为什么不让工作线程直接操作 socket"></a>4.2 为什么不让工作线程直接操作 socket</h3><p>因为这样很容易引入：</p>
<ul>
<li>一个连接被多个线程同时读写</li>
<li>发送顺序被打乱</li>
<li>锁粒度变大，缓存局部性变差</li>
<li>排障时很难定位“这个连接到底归谁管”</li>
</ul>
<p>更稳的做法是：</p>
<ul>
<li>I&#x2F;O 线程负责网络层和协议层</li>
<li>Worker 线程只处理业务输入，生成业务结果</li>
<li>结果通过无锁队列或锁队列回投给归属 I&#x2F;O 线程</li>
<li>用 <code>eventfd</code> 唤醒 I&#x2F;O 线程继续发送</li>
</ul>
<h3 id="4-3-一个常见的任务流"><a href="#4-3-一个常见的任务流" class="headerlink" title="4.3 一个常见的任务流"></a>4.3 一个常见的任务流</h3><ol>
<li>I&#x2F;O 线程收到请求，完成协议解析</li>
<li>组装一个“业务任务”对象，投递到线程池</li>
<li>Worker 线程执行业务逻辑，得到响应对象</li>
<li>把响应对象投递回对应 I&#x2F;O 线程的待发送队列</li>
<li>向该 I&#x2F;O 线程的 <code>eventfd</code> 写入计数</li>
<li>I&#x2F;O 线程被唤醒，刷新待发送队列并 <code>send</code></li>
</ol>
<p>这个结构有两个直接好处：</p>
<ul>
<li>连接状态不跨线程乱跑</li>
<li>网络收发和业务执行都能独立扩缩</li>
</ul>
<h3 id="4-4-线程数怎么定"><a href="#4-4-线程数怎么定" class="headerlink" title="4.4 线程数怎么定"></a>4.4 线程数怎么定</h3><p>经验上可以先这样起步：</p>
<ul>
<li>I&#x2F;O 线程数：先从 <code>1 ~ CPU 核数</code> 之间选，小步压测</li>
<li>纯 CPU 业务线程池：通常接近 CPU 核数</li>
<li>会阻塞的业务：单独拆出阻塞线程池，不要和 CPU 任务混用</li>
</ul>
<p>不要一开始就把线程开得很多。线程越多，不一定吞吐越高，反而可能带来：</p>
<ul>
<li>调度开销</li>
<li>锁竞争</li>
<li>cache miss</li>
<li>尾延迟抖动</li>
</ul>
<h3 id="4-5-线程模型里的常见坑"><a href="#4-5-线程模型里的常见坑" class="headerlink" title="4.5 线程模型里的常见坑"></a>4.5 线程模型里的常见坑</h3><ul>
<li>把数据库、磁盘、RPC 阻塞调用放进 I&#x2F;O 线程</li>
<li>一个大线程池同时跑 CPU 任务和慢 I&#x2F;O 任务</li>
<li>没有限制任务队列长度，导致内存膨胀</li>
<li>没做背压，读得太快、处理太慢、写缓冲越积越大</li>
<li>让 worker 直接 <code>close(fd)</code> 或直接修改连接对象状态</li>
</ul>
<p>更稳的工程实践通常包括：</p>
<ul>
<li>每个连接有明确 owner I&#x2F;O 线程</li>
<li>回投结果时只传消息，不直接抢 socket</li>
<li>对线程池队列、连接输入缓冲、输出缓冲都设上限</li>
<li>把“超时、丢弃、降级”当成正式设计，而不是补丁</li>
</ul>
<hr>
<h2 id="5-HTTP-解析与应用层协议设计"><a href="#5-HTTP-解析与应用层协议设计" class="headerlink" title="5. HTTP 解析与应用层协议设计"></a>5. HTTP 解析与应用层协议设计</h2><h3 id="5-1-HTTP-解析不要靠“字符串碰运气”"><a href="#5-1-HTTP-解析不要靠“字符串碰运气”" class="headerlink" title="5.1 HTTP 解析不要靠“字符串碰运气”"></a>5.1 HTTP 解析不要靠“字符串碰运气”</h3><p>HTTP&#x2F;1.1 在服务端最常见的解析路径是增量状态机：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">REQUEST_LINE -&gt; HEADERS -&gt; BODY -&gt; DONE</span><br></pre></td></tr></table></figure>

<p>原因很简单，TCP 是字节流，所以：</p>
<ul>
<li>一次 <code>recv()</code> 可能只收到半个请求行</li>
<li>也可能一次收到“一个半请求”</li>
<li>keep-alive 连接上还可能连续收到多个请求</li>
</ul>
<p>所以解析器一般要面对的是“缓冲区里的不完整字节流”，而不是“一次 <code>recv()</code> 就是一条完整 HTTP 请求”。</p>
<h3 id="5-2-一个更合理的-HTTP-处理流程"><a href="#5-2-一个更合理的-HTTP-处理流程" class="headerlink" title="5.2 一个更合理的 HTTP 处理流程"></a>5.2 一个更合理的 HTTP 处理流程</h3><ol>
<li>I&#x2F;O 线程把字节流追加到连接输入缓冲</li>
<li>解析器尝试从当前缓冲推进状态机</li>
<li>如果请求还不完整，就停下来等更多数据</li>
<li>如果请求完整，就生成请求对象</li>
<li>业务层处理后，再序列化成 HTTP 响应</li>
</ol>
<p>最少要覆盖这些点：</p>
<ul>
<li>请求行解析：方法、URL、版本</li>
<li>头部解析：按 <code>\r\n</code> 切分</li>
<li>空行判定：头部结束</li>
<li>body 长度：<code>Content-Length</code></li>
<li>分块传输：<code>Transfer-Encoding: chunked</code></li>
<li>keep-alive：一条连接上连续请求</li>
<li>大包限制：头部长度、body 长度都要有限制</li>
</ul>
<h3 id="5-3-HTTP-工程上常见坑"><a href="#5-3-HTTP-工程上常见坑" class="headerlink" title="5.3 HTTP 工程上常见坑"></a>5.3 HTTP 工程上常见坑</h3><ul>
<li>只支持 <code>Content-Length</code>，完全忽略 chunked</li>
<li>没有限制 header 大小，容易被慢请求或畸形请求拖死</li>
<li>把一整个大 body 一次性读入内存，导致峰值过高</li>
<li>没处理 pipeline&#x2F;连续请求，缓冲区里残留数据被误丢</li>
<li>业务处理过慢但连接持续可读，导致输入缓冲无限增长</li>
</ul>
<p>如果是自己练习写 HTTP server，建议至少加上：</p>
<ul>
<li>header 大小上限</li>
<li>body 大小上限</li>
<li>header 读取超时</li>
<li>空闲连接超时</li>
<li>非法请求快速返回 <code>400</code></li>
</ul>
<h3 id="5-4-应用层协议设计，比“能传输”更重要"><a href="#5-4-应用层协议设计，比“能传输”更重要" class="headerlink" title="5.4 应用层协议设计，比“能传输”更重要"></a>5.4 应用层协议设计，比“能传输”更重要</h3><p>如果不是做 HTTP，而是做自定义 RPC &#x2F; 游戏 &#x2F; 推送协议，最重要的是先把“帧边界”定义清楚。</p>
<p>最常见的做法是长度字段协议：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">| magic | version | type | request_id | body_len | body... |</span><br></pre></td></tr></table></figure>

<p>这样解析时可以先读固定头，再按 <code>body_len</code> 判断包体是否完整。</p>
<p>比起纯文本协议，它的优势是：</p>
<ul>
<li>包边界明确</li>
<li>解析开销更稳定</li>
<li>扩展字段更容易版本化</li>
</ul>
<h3 id="5-5-自定义协议建议至少包含这些元素"><a href="#5-5-自定义协议建议至少包含这些元素" class="headerlink" title="5.5 自定义协议建议至少包含这些元素"></a>5.5 自定义协议建议至少包含这些元素</h3><ul>
<li><code>magic</code>：快速识别协议，防止串流或脏数据</li>
<li><code>version</code>：协议演进时兼容旧客户端</li>
<li><code>type</code>：请求类型 &#x2F; 响应类型 &#x2F; 心跳 &#x2F; 控制消息</li>
<li><code>request_id</code>：做请求响应关联，便于并发与排障</li>
<li><code>body_len</code>：确定包体边界</li>
<li>可选 <code>flags</code>：压缩、序列化格式、错误码等</li>
</ul>
<p>还要配套以下约束：</p>
<ul>
<li>最大包长限制</li>
<li>超时与重试语义</li>
<li>幂等语义</li>
<li>错误响应格式</li>
<li>压缩&#x2F;加密是否协商</li>
</ul>
<h3 id="5-6-协议设计和线程模型其实是连在一起的"><a href="#5-6-协议设计和线程模型其实是连在一起的" class="headerlink" title="5.6 协议设计和线程模型其实是连在一起的"></a>5.6 协议设计和线程模型其实是连在一起的</h3><p>如果协议天然支持：</p>
<ul>
<li>明确包边界</li>
<li>快速校验合法性</li>
<li>带 <code>request_id</code></li>
<li>支持分片&#x2F;流式 body</li>
</ul>
<p>那么线程池和 I&#x2F;O 线程的协作会简单很多。</p>
<p>反过来，如果协议定义含糊，后面就容易出现：</p>
<ul>
<li>半包难处理</li>
<li>多请求复用难关联</li>
<li>排障日志定位困难</li>
<li>一旦扩协议版本就全链路改动</li>
</ul>
<hr>
<h2 id="6-Linux-网络调优参数与排障方法"><a href="#6-Linux-网络调优参数与排障方法" class="headerlink" title="6. Linux 网络调优参数与排障方法"></a>6. Linux 网络调优参数与排障方法</h2><h3 id="6-1-先记住一个原则：先观察，再调参"><a href="#6-1-先记住一个原则：先观察，再调参" class="headerlink" title="6.1 先记住一个原则：先观察，再调参"></a>6.1 先记住一个原则：先观察，再调参</h3><p>网络调优里最常见的误区是：</p>
<ul>
<li>一看到超时就改 <code>sysctl</code></li>
<li>一看到连接多就盲目加线程</li>
<li>一看到吞吐不高就切 ET</li>
</ul>
<p>更稳的方法是先回答三个问题：</p>
<ol>
<li>瓶颈在应用、内核、网卡，还是对端？</li>
<li>问题是吞吐不够、延迟过高，还是抖动严重？</li>
<li>是连接建立阶段有问题，还是已建立连接的收发有问题？</li>
</ol>
<h3 id="6-2-常见调优项"><a href="#6-2-常见调优项" class="headerlink" title="6.2 常见调优项"></a>6.2 常见调优项</h3><p>下面这些参数最常见，但不应该脱离场景硬改。</p>
<table>
<thead>
<tr>
<th>方向</th>
<th>参数&#x2F;项</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>fd 上限</td>
<td><code>ulimit -n</code>、<code>fs.file-max</code></td>
<td>控制可打开文件句柄数量</td>
</tr>
<tr>
<td>监听队列</td>
<td><code>net.core.somaxconn</code></td>
<td>限制 listen backlog 上限</td>
</tr>
<tr>
<td>SYN 队列</td>
<td><code>net.ipv4.tcp_max_syn_backlog</code></td>
<td>半连接队列容量</td>
</tr>
<tr>
<td>网卡收包积压</td>
<td><code>net.core.netdev_max_backlog</code></td>
<td>网卡到协议栈的积压队列</td>
</tr>
<tr>
<td>Socket 缓冲</td>
<td><code>net.core.rmem_max</code>、<code>net.core.wmem_max</code></td>
<td>接收&#x2F;发送缓冲上限</td>
</tr>
<tr>
<td>TCP 缓冲</td>
<td><code>net.ipv4.tcp_rmem</code>、<code>net.ipv4.tcp_wmem</code></td>
<td>TCP 自适应缓冲范围</td>
</tr>
<tr>
<td>端口范围</td>
<td><code>net.ipv4.ip_local_port_range</code></td>
<td>客户端侧临时端口范围</td>
</tr>
<tr>
<td>TIME_WAIT</td>
<td><code>net.ipv4.tcp_tw_reuse</code></td>
<td>特定场景下重用 TIME_WAIT，需谨慎</td>
</tr>
<tr>
<td>SYN 防护</td>
<td><code>net.ipv4.tcp_syncookies</code></td>
<td>SYN flood 防护</td>
</tr>
</tbody></table>
<p>还要配合关注：</p>
<ul>
<li><code>SO_REUSEADDR</code></li>
<li><code>SO_REUSEPORT</code></li>
<li><code>TCP_NODELAY</code></li>
<li><code>TCP_QUICKACK</code></li>
<li>进程实际打开 fd 数量</li>
<li>网卡多队列、RSS、RPS&#x2F;XPS 是否匹配</li>
</ul>
<h3 id="6-3-几类典型现象怎么查"><a href="#6-3-几类典型现象怎么查" class="headerlink" title="6.3 几类典型现象怎么查"></a>6.3 几类典型现象怎么查</h3><h4 id="1-新连接进不来"><a href="#1-新连接进不来" class="headerlink" title="1. 新连接进不来"></a>1. 新连接进不来</h4><p>先看：</p>
<ul>
<li><code>ss -lnt</code></li>
<li><code>ss -s</code></li>
<li><code>cat /proc/sys/net/core/somaxconn</code></li>
<li><code>cat /proc/sys/net/ipv4/tcp_max_syn_backlog</code></li>
</ul>
<p>再判断是：</p>
<ul>
<li>应用根本没及时 <code>accept</code></li>
<li>accept 队列太小</li>
<li>SYN 队列顶满</li>
<li>进程 fd 已耗尽</li>
</ul>
<h4 id="2-连接很多，但吞吐很低"><a href="#2-连接很多，但吞吐很低" class="headerlink" title="2. 连接很多，但吞吐很低"></a>2. 连接很多，但吞吐很低</h4><p>优先排查：</p>
<ul>
<li>I&#x2F;O 线程是否被业务阻塞</li>
<li>发送缓冲是否长期积压</li>
<li>是否出现大量重传</li>
<li>对端是否慢读</li>
</ul>
<p>常用命令：</p>
<ul>
<li><code>ss -tin</code></li>
<li><code>sar -n DEV 1</code></li>
<li><code>ethtool -S eth0</code></li>
<li><code>ip -s link</code></li>
<li><code>perf top -p &lt;pid&gt;</code></li>
</ul>
<h4 id="3-延迟抖动大、尾延迟高"><a href="#3-延迟抖动大、尾延迟高" class="headerlink" title="3. 延迟抖动大、尾延迟高"></a>3. 延迟抖动大、尾延迟高</h4><p>常见方向：</p>
<ul>
<li>线程太多导致调度抖动</li>
<li>GC &#x2F; 内存分配 &#x2F; 大对象复制</li>
<li>某些慢任务卡住 worker</li>
<li>日志同步刷盘</li>
<li>I&#x2F;O 线程偶发做了阻塞操作</li>
</ul>
<p>这时经常要结合：</p>
<ul>
<li><code>pidstat -t 1 -p &lt;pid&gt;</code></li>
<li><code>perf record/report</code></li>
<li><code>strace -tt -p &lt;pid&gt;</code></li>
</ul>
<h4 id="4-连接莫名被断开"><a href="#4-连接莫名被断开" class="headerlink" title="4. 连接莫名被断开"></a>4. 连接莫名被断开</h4><p>需要区分：</p>
<ul>
<li>对端主动关闭</li>
<li>中间网络设备超时清理</li>
<li>写关闭触发 <code>EPIPE</code></li>
<li><code>ECONNRESET</code></li>
<li>自己应用层超时策略太激进</li>
</ul>
<p>这时 <code>tcpdump</code> 很有价值：</p>
<ul>
<li><code>tcpdump -nn -i any tcp port 8080</code></li>
</ul>
<p>抓包后重点看：</p>
<ul>
<li>谁先发 <code>FIN</code></li>
<li>是否有 <code>RST</code></li>
<li>是否反复重传</li>
<li>三次握手是否完整</li>
</ul>
<h3 id="6-4-排障时建议按这条线走"><a href="#6-4-排障时建议按这条线走" class="headerlink" title="6.4 排障时建议按这条线走"></a>6.4 排障时建议按这条线走</h3><ol>
<li>先看应用日志和连接状态</li>
<li>再看 <code>ss</code>、<code>sar</code>、<code>pidstat</code>、<code>perf</code></li>
<li>再看 <code>/proc</code>、<code>sysctl</code>、网卡统计</li>
<li>最后再决定是否抓包</li>
</ol>
<p>不要一上来就抓包，也不要一上来就调内核参数。</p>
<h3 id="6-5-一些很实用的排障清单"><a href="#6-5-一些很实用的排障清单" class="headerlink" title="6.5 一些很实用的排障清单"></a>6.5 一些很实用的排障清单</h3><p>看到“连接接不进来”时，先检查：</p>
<ul>
<li>进程是否在监听正确地址和端口</li>
<li>backlog 是否足够</li>
<li>fd 是否打满</li>
<li>是否被防火墙或安全组拦截</li>
</ul>
<p>看到“CPU 很高”时，先检查：</p>
<ul>
<li>热点在业务代码、锁、内存分配，还是系统调用</li>
<li><code>epoll_wait</code> 是否其实不忙，真正忙的是工作线程</li>
<li>是否有大量空转 wakeup</li>
</ul>
<p>看到“内存一直涨”时，先检查：</p>
<ul>
<li>输入缓冲 &#x2F; 输出缓冲是否无上限</li>
<li>请求队列是否积压</li>
<li>是否慢连接太多</li>
<li>是否大包被整包缓存</li>
</ul>
<hr>
<h2 id="7-小结"><a href="#7-小结" class="headerlink" title="7. 小结"></a>7. 小结</h2><p>把这些主题串起来，其实就是一条完整的服务端工程路径：</p>
<ol>
<li>用 <code>epoll</code> 搭 Reactor，先 LT 做对，再考虑 ET</li>
<li>用非阻塞 socket 正确处理 <code>EINPROGRESS</code>、<code>EAGAIN</code>、部分读写和连接关闭</li>
<li>用 <code>timerfd</code>、<code>eventfd</code>、<code>signalfd</code> 把“时间 &#x2F; 线程通知 &#x2F; 信号”并入统一事件循环</li>
<li>让 I&#x2F;O 线程管连接，让线程池管业务，结果再回到 I&#x2F;O 线程发出</li>
<li>用状态机解析 HTTP 或自定义协议，把包边界、版本和错误语义设计清楚</li>
<li>出问题时先观察现象和链路，再做内核调优</li>
</ol>
<p>如果把这些基础打稳，后面再看：</p>
<ul>
<li><code>sendfile/splice</code> 零拷贝</li>
<li><code>SO_REUSEPORT</code> + 多队列</li>
<li><code>io_uring</code></li>
<li>更复杂的 RPC 框架</li>
</ul>
<p>就会轻松很多。</p>
]]></content>
      <categories>
        <category>学习</category>
        <category>Linux高性能服务器编程</category>
      </categories>
  </entry>
  <entry>
    <title>bufferevent</title>
    <url>//notes/linux%E9%AB%98%E6%80%A7%E8%83%BD%E6%9C%8D%E5%8A%A1%E5%99%A8%E7%BC%96%E7%A8%8B/2-libevent/</url>
    <content><![CDATA[<h1 id="bufferevent"><a href="#bufferevent" class="headerlink" title="bufferevent"></a>bufferevent</h1><h2 id="1-基本概念"><a href="#1-基本概念" class="headerlink" title="1. 基本概念"></a>1. 基本概念</h2><ul>
<li><strong>bufferevent</strong>：在套接字之上封装了<strong>输入&#x2F;输出缓冲</strong>与<strong>回调</strong>机制的 I&#x2F;O 对象，避免手写 <code>read/write</code> 和边沿触发细节。</li>
<li><strong>事件循环</strong>：<code>event_base</code> 驱动，所有 I&#x2F;O、超时、信号都在此 loop 内调度。</li>
</ul>
<hr>
<h2 id="2-关键-API"><a href="#2-关键-API" class="headerlink" title="2. 关键 API"></a>2. 关键 API</h2><h3 id="buferevent-socket-new"><a href="#buferevent-socket-new" class="headerlink" title="buferevent_socket_new()"></a>buferevent_socket_new()</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> bufferevent* <span class="title function_">bufferevent_socket_new</span><span class="params">(</span></span><br><span class="line"><span class="params">    <span class="keyword">struct</span> event_base *base,</span></span><br><span class="line"><span class="params">    <span class="type">evutil_socket_t</span> fd,           <span class="comment">// 通常传 -1，让 libevent 自己创建 socket</span></span></span><br><span class="line"><span class="params">    <span class="type">int</span> options                   <span class="comment">// 常用：BEV_OPT_CLOSE_ON_FREE | BEV_OPT_DEFER_CALLBACKS</span></span></span><br><span class="line"><span class="params">)</span>;</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong></p>
<ul>
<li><code>fd = -1</code>：由 libevent 负责创建 socket，更简洁。</li>
<li><code>BEV_OPT_CLOSE_ON_FREE</code>：在 <code>bufferevent_free()</code> 时自动关闭 socket。</li>
<li><code>BEV_OPT_DEFER_CALLBACKS</code>：把回调延迟到事件循环安全点执行，减少重入问题。</li>
</ul>
<h3 id="buferevent-socket-connect"><a href="#buferevent-socket-connect" class="headerlink" title="buferevent_socket_connect()"></a>buferevent_socket_connect()</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">bufferevent_socket_connect</span><span class="params">(</span></span><br><span class="line"><span class="params">    <span class="keyword">struct</span> bufferevent *bev,</span></span><br><span class="line"><span class="params">    <span class="keyword">struct</span> sockaddr *addr,</span></span><br><span class="line"><span class="params">    <span class="type">int</span> addrlen</span></span><br><span class="line"><span class="params">)</span>;</span><br></pre></td></tr></table></figure>

<p><strong>说明</strong></p>
<ul>
<li>非阻塞发起连接；成功返回 0，失败返回 -1（同时触发 event 回调中的 <code>BEV_EVENT_ERROR</code>）。</li>
<li>连接成功后会触发 event 回调中的 <code>BEV_EVENT_CONNECTED</code>。</li>
</ul>
<blockquote>
<p>如果要用主机名而非 IP，推荐 <code>bufferevent_socket_connect_hostname()</code>（需配合 <code>evdns_base</code> 进行异步解析）。</p>
</blockquote>
<hr>
<h2 id="3-最小可用示例（IPv4）"><a href="#3-最小可用示例（IPv4）" class="headerlink" title="3. 最小可用示例（IPv4）"></a>3. 最小可用示例（IPv4）</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/event.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/bufferevent.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/buffer.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/util.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">read_cb</span><span class="params">(<span class="keyword">struct</span> bufferevent* bev, <span class="type">void</span>* ctx)</span> &#123;</span><br><span class="line">    <span class="type">char</span> buf[<span class="number">4096</span>];</span><br><span class="line">    <span class="type">size_t</span> n;</span><br><span class="line">    <span class="keyword">while</span> ((n = bufferevent_read(bev, buf, <span class="keyword">sizeof</span>(buf))) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        fwrite(buf, <span class="number">1</span>, n, <span class="built_in">stdout</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">static</span> <span class="type">void</span> <span class="title function_">event_cb</span><span class="params">(<span class="keyword">struct</span> bufferevent* bev, <span class="type">short</span> what, <span class="type">void</span>* ctx)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (what &amp; BEV_EVENT_CONNECTED) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;[OK] connected\n&quot;</span>);</span><br><span class="line">        <span class="type">const</span> <span class="type">char</span>* msg = <span class="string">&quot;GET / HTTP/1.1\r\nHost: 127.0.0.1\r\n\r\n&quot;</span>;</span><br><span class="line">        bufferevent_write(bev, msg, <span class="built_in">strlen</span>(msg));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (what &amp; BEV_EVENT_EOF) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;[*] server closed\n&quot;</span>);</span><br><span class="line">        bufferevent_free(bev);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (what &amp; BEV_EVENT_ERROR) &#123;</span><br><span class="line">        <span class="type">int</span> err = EVUTIL_SOCKET_ERROR();</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;[ERR] %s\n&quot;</span>, evutil_socket_error_to_string(err));</span><br><span class="line">        bufferevent_free(bev);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (what &amp; BEV_EVENT_TIMEOUT) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;[!] timeout\n&quot;</span>);</span><br><span class="line">        bufferevent_free(bev);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">event_base</span>* <span class="title">base</span> =</span> event_base_new();</span><br><span class="line"></span><br><span class="line">    <span class="comment">//创建套字 socket</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">bufferevent</span>* <span class="title">bev</span> =</span> bufferevent_socket_new(</span><br><span class="line">        base, <span class="number">-1</span>, BEV_OPT_CLOSE_ON_FREE | BEV_OPT_DEFER_CALLBACKS);</span><br><span class="line">    <span class="comment">// 将read_cb event_cb 回调函数先绑定防止漏听</span></span><br><span class="line">    bufferevent_setcb(bev, read_cb, <span class="literal">NULL</span>, event_cb, <span class="literal">NULL</span>);</span><br><span class="line">    bufferevent_enable(bev, EV_READ | EV_WRITE);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 可选：设置读/写超时（连接阶段通常走写超时）</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> <span class="title">rto</span> =</span> &#123;<span class="number">10</span>, <span class="number">0</span>&#125;, wto = &#123;<span class="number">10</span>, <span class="number">0</span>&#125;;</span><br><span class="line">    bufferevent_set_timeouts(bev, &amp;rto, &amp;wto);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 目标地址</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">sockaddr_in</span> <span class="title">sin</span>;</span></span><br><span class="line">    <span class="built_in">memset</span>(&amp;<span class="built_in">sin</span>, <span class="number">0</span>, <span class="keyword">sizeof</span>(<span class="built_in">sin</span>));</span><br><span class="line">    <span class="built_in">sin</span>.sin_family = AF_INET;</span><br><span class="line">    <span class="built_in">sin</span>.sin_port   = htons(<span class="number">8080</span>);</span><br><span class="line">    evutil_inet_pton(AF_INET, <span class="string">&quot;127.0.0.1&quot;</span>, &amp;<span class="built_in">sin</span>.sin_addr);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (bufferevent_socket_connect(bev, (<span class="keyword">struct</span> sockaddr*)&amp;<span class="built_in">sin</span>, <span class="keyword">sizeof</span>(<span class="built_in">sin</span>)) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        perror(<span class="string">&quot;connect&quot;</span>);</span><br><span class="line">        bufferevent_free(bev);</span><br><span class="line">        event_base_free(base);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    event_base_dispatch(base);</span><br><span class="line"></span><br><span class="line">    event_base_free(base);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="4-使用要点与常见坑"><a href="#4-使用要点与常见坑" class="headerlink" title="4. 使用要点与常见坑"></a>4. 使用要点与常见坑</h2><ol>
<li><p><strong>回调必须先设再 connect</strong><br>先 <code>bufferevent_setcb()</code> + <code>bufferevent_enable()</code>，再调用 <code>bufferevent_socket_connect()</code>，避免早到事件丢失。</p>
</li>
<li><p><strong>超时设置</strong></p>
<ul>
<li><code>bufferevent_set_timeouts()</code> 同时设置读&#x2F;写超时。</li>
<li>连接阶段通常依赖<strong>写超时</strong>触发（因为 connect 过程中会等待可写）。</li>
</ul>
</li>
<li><p><strong>水位（可选）</strong></p>
<ul>
<li>读水位：<code>bufferevent_setwatermark(bev, EV_READ, low, high)</code><br>当输入缓冲超过 <code>high</code> 时会<strong>暂时禁用读事件</strong>，防止内存暴涨。</li>
<li>写水位：同理控制输出缓冲积压。</li>
</ul>
</li>
<li><p><strong>线程安全</strong></p>
<ul>
<li>libevent 可多线程，但一个 <code>event_base</code> 通常<strong>只在一个线程</strong>内跑；跨线程提交任务请用 <code>event_base_once()</code> 或管道&#x2F;通知机制。</li>
</ul>
</li>
<li><p><strong>DNS 支持（推荐）</strong></p>
<ul>
<li>异步解析：<code>evdns_base_new(base, 1)</code> + <code>bufferevent_socket_connect_hostname(bev, dns, AF_UNSPEC, &quot;example.com&quot;, 80)</code></li>
<li>避免阻塞式 <code>getaddrinfo()</code>。</li>
</ul>
</li>
<li><p><strong>清理顺序</strong></p>
<ul>
<li>一般在 <code>event_cb</code> 收到 <code>EOF/ERROR/TIMEOUT</code> 后 <code>bufferevent_free()</code>；loop 退出后 <code>event_base_free()</code>。</li>
</ul>
</li>
<li><p><strong>错误定位</strong></p>
<ul>
<li><code>BEV_EVENT_ERROR</code> 时使用 <code>EVUTIL_SOCKET_ERROR()</code> + <code>evutil_socket_error_to_string()</code> 打印 errno。</li>
<li>常见：<code>ECONNREFUSED</code>（服务未起）、<code>ETIMEDOUT</code>（网络&#x2F;防火墙&#x2F;超时）、<code>EHOSTUNREACH</code>。</li>
</ul>
</li>
<li><p><strong>选项建议</strong></p>
<ul>
<li><code>BEV_OPT_CLOSE_ON_FREE</code>：免漏句柄</li>
<li><code>BEV_OPT_DEFER_CALLBACKS</code>：减少重入与回调嵌套</li>
<li>如需<strong>逐线程</strong>回调可考虑 <code>BEV_OPT_THREADSAFE</code>（需要启用 <code>evthread_use_pthreads()</code> 等）。</li>
</ul>
</li>
</ol>
<hr>
<h2 id="5-主机名连接（带-DNS）的示例（简版）"><a href="#5-主机名连接（带-DNS）的示例（简版）" class="headerlink" title="5. 主机名连接（带 DNS）的示例（简版）"></a>5. 主机名连接（带 DNS）的示例（简版）</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;event2/dns.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">evdns_base</span>* <span class="title">dns</span> =</span> evdns_base_new(base, <span class="number">1</span>); <span class="comment">// 1=初始化内置解析器</span></span><br><span class="line"><span class="type">int</span> rc = bufferevent_socket_connect_hostname(</span><br><span class="line">    bev, dns, AF_UNSPEC, <span class="string">&quot;example.com&quot;</span>, <span class="number">80</span>);</span><br><span class="line"><span class="keyword">if</span> (rc &lt; <span class="number">0</span>) &#123; <span class="comment">/* handle error */</span> &#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="6-小结（流程速记）"><a href="#6-小结（流程速记）" class="headerlink" title="6. 小结（流程速记）"></a>6. 小结（流程速记）</h2><ol>
<li><code>event_base_new()</code></li>
<li><code>bufferevent_socket_new(base, -1, BEV_OPT_CLOSE_ON_FREE | BEV_OPT_DEFER_CALLBACKS)</code></li>
<li><code>bufferevent_setcb()</code> → <code>bufferevent_enable(EV_READ|EV_WRITE)</code> → <code>bufferevent_set_timeouts()</code></li>
<li><code>bufferevent_socket_connect()</code>（或 <code>_connect_hostname()</code>）</li>
<li>在 <code>event_cb</code> 处理 <code>BEV_EVENT_CONNECTED / EOF / ERROR / TIMEOUT</code></li>
<li>退出时 <code>bufferevent_free()</code>、<code>event_base_free()</code></li>
</ol>
<hr>
<h2 id="7-进阶建议"><a href="#7-进阶建议" class="headerlink" title="7. 进阶建议"></a>7. 进阶建议</h2><ul>
<li><strong>TLS</strong>：用 <code>bufferevent_openssl_socket_new()</code> 包装 SSL 会话，接口与普通 <code>bev</code> 基本一致。</li>
<li><strong>背压控制</strong>：配合水位、分块写入，避免写缓冲过大。</li>
<li><strong>协议层</strong>：在 <code>read_cb</code> 内从 <code>evbuffer</code> 取数据，做<strong>半包&#x2F;粘包</strong>处理（如定长头 + 可变体）。</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>Linux高性能服务器编程</category>
      </categories>
  </entry>
  <entry>
    <title>现代 C++ 实践导读</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/00-%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5%E5%AF%BC%E8%AF%BB/</url>
    <content><![CDATA[<h1 id="现代-C-实践导读"><a href="#现代-C-实践导读" class="headerlink" title="现代 C++ 实践导读"></a>现代 C++ 实践导读</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<p>这组笔记按“对象语义 -&gt; 内存与所有权 -&gt; 并发 -&gt; 设计模式 -&gt; 网络与协程 -&gt; 常用工具”的顺序整理。<br>不建议按最早写作顺序读，建议优先按下面的编号顺序走。</p>
<hr>
<h2 id="1-推荐阅读顺序"><a href="#1-推荐阅读顺序" class="headerlink" title="1. 推荐阅读顺序"></a>1. 推荐阅读顺序</h2><ol>
<li>对象生命周期、特殊成员函数与移动语义</li>
<li>智能指针与所有权</li>
<li>allocator、自定义内存分配与 pmr</li>
<li>生产者-消费者模式与阻塞队列</li>
<li>线程同步消息队列与线程池</li>
<li>工厂模式、多态与接口设计</li>
<li>游戏常见设计模式</li>
<li>对象布局、栈堆与未定义行为</li>
<li>网络服务基础：TCP 粘包、线程模型与 HTTP(S)</li>
<li>C++20 协程入门与实践</li>
<li>现代 C++ 常用工具类型</li>
<li>ranges 与 views</li>
<li>错误处理与 <code>expected</code>、异常设计</li>
<li>内存泄漏检测与管理</li>
</ol>
<hr>
<h2 id="2-这次整理做了什么"><a href="#2-这次整理做了什么" class="headerlink" title="2. 这次整理做了什么"></a>2. 这次整理做了什么</h2><p>这次整理不只是改编号，还做了四件事：</p>
<ol>
<li>把错名、重复和草稿笔记重构成清晰主题</li>
<li>把空白或过短内容补成可复习版本</li>
<li>新增一篇缺失但非常常用的工具类型笔记</li>
<li>补上了 <code>ranges/views</code>、错误处理设计，以及内存泄漏检测与管理这三块现代 C++ 高频主题</li>
</ol>
<hr>
<h2 id="3-如果时间有限"><a href="#3-如果时间有限" class="headerlink" title="3. 如果时间有限"></a>3. 如果时间有限</h2><p>优先看这 5 篇：</p>
<ol>
<li>生命周期、特殊成员函数与移动语义</li>
<li>智能指针与所有权</li>
<li>生产者-消费者模式与阻塞队列</li>
<li>线程池与消息队列</li>
<li>协程入门与实践</li>
</ol>
<p>这几篇最直接影响现代 C++ 的工程写法。</p>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>对象生命周期、特殊成员函数与移动语义</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/01-%E5%AF%B9%E8%B1%A1%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F-%E7%89%B9%E6%AE%8A%E6%88%90%E5%91%98%E5%87%BD%E6%95%B0%E4%B8%8E%E7%A7%BB%E5%8A%A8%E8%AF%AD%E4%B9%89/</url>
    <content><![CDATA[<h1 id="对象生命周期、特殊成员函数与移动语义"><a href="#对象生命周期、特殊成员函数与移动语义" class="headerlink" title="对象生命周期、特殊成员函数与移动语义"></a>对象生命周期、特殊成员函数与移动语义</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：生命周期、RAII、拷贝构造、拷贝赋值、移动构造、移动赋值、Rule of Zero&#x2F;Five、<code>std::move</code><br>核心目标：搞清楚一个对象从创建到销毁会经历什么，以及类该如何正确管理资源。</p>
</blockquote>
<hr>
<h2 id="1-对象生命周期是什么"><a href="#1-对象生命周期是什么" class="headerlink" title="1. 对象生命周期是什么"></a>1. 对象生命周期是什么</h2><p>一个对象通常会经历：</p>
<ol>
<li>构造</li>
<li>使用</li>
<li>析构</li>
</ol>
<p>最重要的实践原则是：</p>
<ul>
<li>对象一旦构造完成，就应该处于“可用且满足类不变量”的状态</li>
<li>对象一旦析构完成，就不该再被访问</li>
</ul>
<p>局部对象在离开作用域时析构：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::string s = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line">&#125; <span class="comment">// 这里自动析构</span></span><br></pre></td></tr></table></figure>

<p>动态对象则由拥有者负责释放：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> p = std::<span class="built_in">make_unique</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2-RAII-是生命周期管理的核心"><a href="#2-RAII-是生命周期管理的核心" class="headerlink" title="2. RAII 是生命周期管理的核心"></a>2. RAII 是生命周期管理的核心</h2><p>RAII 的意思是：</p>
<ul>
<li>构造时获取资源</li>
<li>析构时释放资源</li>
</ul>
<p>典型资源包括：</p>
<ul>
<li>动态内存</li>
<li>文件句柄</li>
<li>socket</li>
<li>锁</li>
<li>线程句柄</li>
</ul>
<p>RAII 的价值不是“语法优雅”，而是：</p>
<ul>
<li>不容易忘记释放</li>
<li>异常发生时也能自动清理</li>
</ul>
<hr>
<h2 id="3-六个特殊成员函数"><a href="#3-六个特殊成员函数" class="headerlink" title="3. 六个特殊成员函数"></a>3. 六个特殊成员函数</h2><p>一个类最重要的 6 个函数是：</p>
<ol>
<li>默认构造函数</li>
<li>析构函数</li>
<li>拷贝构造函数</li>
<li>拷贝赋值运算符</li>
<li>移动构造函数</li>
<li>移动赋值运算符</li>
</ol>
<p>它们决定了对象如何：</p>
<ul>
<li>创建</li>
<li>复制</li>
<li>转移资源</li>
<li>销毁</li>
</ul>
<hr>
<h2 id="4-拷贝构造-vs-拷贝赋值"><a href="#4-拷贝构造-vs-拷贝赋值" class="headerlink" title="4. 拷贝构造 vs 拷贝赋值"></a>4. 拷贝构造 vs 拷贝赋值</h2><h3 id="4-1-拷贝构造"><a href="#4-1-拷贝构造" class="headerlink" title="4.1 拷贝构造"></a>4.1 拷贝构造</h3><p>用一个对象去初始化另一个“新对象”：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">T <span class="title">b</span><span class="params">(a)</span></span>;</span><br><span class="line">T c = a;</span><br></pre></td></tr></table></figure>

<h3 id="4-2-拷贝赋值"><a href="#4-2-拷贝赋值" class="headerlink" title="4.2 拷贝赋值"></a>4.2 拷贝赋值</h3><p>把一个已经存在的对象覆盖成另一个对象的状态：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">T b;</span><br><span class="line">b = a;</span><br></pre></td></tr></table></figure>

<p>核心差异：</p>
<ul>
<li>拷贝构造是“从无到有”</li>
<li>拷贝赋值是“已有对象被覆盖”</li>
</ul>
<p>后者通常还要考虑：</p>
<ul>
<li>旧资源释放</li>
<li>自赋值</li>
<li>异常安全</li>
</ul>
<hr>
<h2 id="5-为什么移动语义很重要"><a href="#5-为什么移动语义很重要" class="headerlink" title="5. 为什么移动语义很重要"></a>5. 为什么移动语义很重要</h2><p>如果一个类持有资源，单纯拷贝代价可能很大。</p>
<p>例如：</p>
<ul>
<li>动态数组</li>
<li>大字符串</li>
<li>文件句柄包装对象</li>
</ul>
<p>移动语义的核心思想是：</p>
<ul>
<li>不复制资源内容</li>
<li>直接转移所有权或资源句柄</li>
</ul>
<p>这就需要：</p>
<ul>
<li>移动构造</li>
<li>移动赋值</li>
</ul>
<hr>
<h2 id="6-std-move-到底在做什么"><a href="#6-std-move-到底在做什么" class="headerlink" title="6. std::move 到底在做什么"></a>6. <code>std::move</code> 到底在做什么</h2><p><code>std::move</code> 本身不移动资源。<br>它只是把一个表达式转换成右值形式，让后续重载决议优先匹配移动版本。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::string s = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line">std::string t = std::<span class="built_in">move</span>(s);</span><br></pre></td></tr></table></figure>

<p>真正移动的是：</p>
<ul>
<li><code>std::string</code> 的移动构造函数</li>
</ul>
<p>不是 <code>std::move</code> 本身。</p>
<hr>
<h2 id="7-一个最小资源类示例"><a href="#7-一个最小资源类示例" class="headerlink" title="7. 一个最小资源类示例"></a>7. 一个最小资源类示例</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Buffer</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Buffer</span>() = <span class="keyword">default</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">Buffer</span><span class="params">(std::<span class="type">size_t</span> n)</span></span></span><br><span class="line"><span class="function">        : size_(n), data_(n ? new int[n] : nullptr) &#123;</span>&#125;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">Buffer</span>() &#123;</span><br><span class="line">        <span class="keyword">delete</span>[] data_;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">Buffer</span>(<span class="type">const</span> Buffer&amp; other)</span><br><span class="line">        : <span class="built_in">size_</span>(other.size_), <span class="built_in">data_</span>(other.size_ ? <span class="keyword">new</span> <span class="type">int</span>[other.size_] : <span class="literal">nullptr</span>) &#123;</span><br><span class="line">        std::<span class="built_in">copy</span>(other.data_, other.data_ + size_, data_);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Buffer&amp; <span class="keyword">operator</span>=(<span class="type">const</span> Buffer&amp; other) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span> == &amp;other) <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">        <span class="function">Buffer <span class="title">tmp</span><span class="params">(other)</span></span>;</span><br><span class="line">        <span class="built_in">swap</span>(tmp);</span><br><span class="line">        <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">Buffer</span>(Buffer&amp;&amp; other) <span class="keyword">noexcept</span></span><br><span class="line">        : <span class="built_in">size_</span>(other.size_), <span class="built_in">data_</span>(other.data_) &#123;</span><br><span class="line">        other.size_ = <span class="number">0</span>;</span><br><span class="line">        other.data_ = <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    Buffer&amp; <span class="keyword">operator</span>=(Buffer&amp;&amp; other) <span class="keyword">noexcept</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span> == &amp;other) <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">        <span class="keyword">delete</span>[] data_;</span><br><span class="line">        size_ = other.size_;</span><br><span class="line">        data_ = other.data_;</span><br><span class="line">        other.size_ = <span class="number">0</span>;</span><br><span class="line">        other.data_ = <span class="literal">nullptr</span>;</span><br><span class="line">        <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">swap</span><span class="params">(Buffer&amp; other)</span> <span class="keyword">noexcept</span> </span>&#123;</span><br><span class="line">        std::<span class="built_in">swap</span>(size_, other.size_);</span><br><span class="line">        std::<span class="built_in">swap</span>(data_, other.data_);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::<span class="type">size_t</span> size_ = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span>* data_ = <span class="literal">nullptr</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这个例子体现了：</p>
<ul>
<li>深拷贝</li>
<li>资源转移</li>
<li>移动后源对象置空</li>
</ul>
<hr>
<h2 id="8-Rule-of-Three-Five-Zero"><a href="#8-Rule-of-Three-Five-Zero" class="headerlink" title="8. Rule of Three &#x2F; Five &#x2F; Zero"></a>8. Rule of Three &#x2F; Five &#x2F; Zero</h2><h3 id="8-1-Rule-of-Three"><a href="#8-1-Rule-of-Three" class="headerlink" title="8.1 Rule of Three"></a>8.1 Rule of Three</h3><p>如果类手写了下面之一，通常就要认真考虑另外两个：</p>
<ul>
<li>析构</li>
<li>拷贝构造</li>
<li>拷贝赋值</li>
</ul>
<p>因为这通常意味着类在管理资源。</p>
<h3 id="8-2-Rule-of-Five"><a href="#8-2-Rule-of-Five" class="headerlink" title="8.2 Rule of Five"></a>8.2 Rule of Five</h3><p>C++11 以后再加上：</p>
<ul>
<li>移动构造</li>
<li>移动赋值</li>
</ul>
<p>如果类显式管理资源，通常要一起考虑这五个。</p>
<h3 id="8-3-Rule-of-Zero"><a href="#8-3-Rule-of-Zero" class="headerlink" title="8.3 Rule of Zero"></a>8.3 Rule of Zero</h3><p>现代 C++ 更推荐：</p>
<ul>
<li>尽量不要自己手写资源管理</li>
<li>把资源交给现成 RAII 类型</li>
</ul>
<p>例如：</p>
<ul>
<li><code>std::vector</code></li>
<li><code>std::string</code></li>
<li><code>std::unique_ptr</code></li>
</ul>
<p>这样很多特殊成员函数甚至可以完全默认生成。</p>
<hr>
<h2 id="9-默认生成和-default-delete"><a href="#9-默认生成和-default-delete" class="headerlink" title="9. 默认生成和 = default &#x2F; = delete"></a>9. 默认生成和 <code>= default</code> &#x2F; <code>= delete</code></h2><h3 id="9-1-default"><a href="#9-1-default" class="headerlink" title="9.1 = default"></a>9.1 <code>= default</code></h3><p>显式告诉编译器：</p>
<ul>
<li>用默认生成版本</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">MyType</span>() = <span class="keyword">default</span>;</span><br></pre></td></tr></table></figure>

<h3 id="9-2-delete"><a href="#9-2-delete" class="headerlink" title="9.2 = delete"></a>9.2 <code>= delete</code></h3><p>显式禁止某种操作：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">MyType</span>(<span class="type">const</span> MyType&amp;) = <span class="keyword">delete</span>;</span><br><span class="line">MyType&amp; <span class="keyword">operator</span>=(<span class="type">const</span> MyType&amp;) = <span class="keyword">delete</span>;</span><br></pre></td></tr></table></figure>

<p>这在：</p>
<ul>
<li>独占资源类型</li>
<li>锁对象</li>
<li>文件句柄包装类</li>
</ul>
<p>里很常见。</p>
<hr>
<h2 id="10-拷贝省略与返回值优化"><a href="#10-拷贝省略与返回值优化" class="headerlink" title="10. 拷贝省略与返回值优化"></a>10. 拷贝省略与返回值优化</h2><p>现代 C++ 里：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">Buffer <span class="title">make_buffer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">Buffer <span class="title">b</span><span class="params">(<span class="number">1024</span>)</span></span>;</span><br><span class="line">    <span class="keyword">return</span> b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>很多情况下不会真的发生拷贝，甚至连移动都可能被省掉。<br>这就是：</p>
<ul>
<li>RVO</li>
<li>NRVO</li>
<li>guaranteed copy elision</li>
</ul>
<p>所以写代码时不要过度手工干预，先让编译器优化。</p>
<hr>
<h2 id="11-常见坑"><a href="#11-常见坑" class="headerlink" title="11. 常见坑"></a>11. 常见坑</h2><h3 id="11-1-移动后还把源对象当原值使用"><a href="#11-1-移动后还把源对象当原值使用" class="headerlink" title="11.1 移动后还把源对象当原值使用"></a>11.1 移动后还把源对象当原值使用</h3><p>移动后的对象通常只保证：</p>
<ul>
<li>仍然有效</li>
<li>可以析构或重新赋值</li>
</ul>
<p>但不保证保留原内容。</p>
<h3 id="11-2-手写资源类却只写析构，不写拷贝-移动"><a href="#11-2-手写资源类却只写析构，不写拷贝-移动" class="headerlink" title="11.2 手写资源类却只写析构，不写拷贝&#x2F;移动"></a>11.2 手写资源类却只写析构，不写拷贝&#x2F;移动</h3><p>这很容易造成：</p>
<ul>
<li>双重释放</li>
<li>浅拷贝问题</li>
</ul>
<h3 id="11-3-拷贝赋值没处理自赋值和异常安全"><a href="#11-3-拷贝赋值没处理自赋值和异常安全" class="headerlink" title="11.3 拷贝赋值没处理自赋值和异常安全"></a>11.3 拷贝赋值没处理自赋值和异常安全</h3><p>尤其是手动 <code>delete</code> 再 <code>new</code> 的写法，容易把对象弄到半残状态。</p>
<h3 id="11-4-本来可以-Rule-of-Zero，却硬写五个函数"><a href="#11-4-本来可以-Rule-of-Zero，却硬写五个函数" class="headerlink" title="11.4 本来可以 Rule of Zero，却硬写五个函数"></a>11.4 本来可以 Rule of Zero，却硬写五个函数</h3><p>不必要的手写资源管理会增加 bug 面积。</p>
<hr>
<h2 id="12-一页总结"><a href="#12-一页总结" class="headerlink" title="12. 一页总结"></a>12. 一页总结</h2><p>这篇最重要的是记住三件事：</p>
<ol>
<li>生命周期就是“构造到析构”的可控过程</li>
<li>管资源的类必须认真处理拷贝和移动</li>
<li>最好的实践通常不是手写五件套，而是尽量 Rule of Zero</li>
</ol>
<p>如果只记一个工程结论：</p>
<blockquote>
<p>能把资源交给现成 RAII 类型，就不要自己手写裸资源生命周期。</p>
</blockquote>
<hr>
<h2 id="13-建议继续补充的相关主题"><a href="#13-建议继续补充的相关主题" class="headerlink" title="13. 建议继续补充的相关主题"></a>13. 建议继续补充的相关主题</h2><ol>
<li>智能指针与所有权</li>
<li>完美转发与引用折叠</li>
<li>异常安全保证</li>
<li><code>noexcept move</code> 与容器优化</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>allocator、自定义内存分配与 pmr 入门</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/03-allocator-%E8%87%AA%E5%AE%9A%E4%B9%89%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%E4%B8%8Epmr%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="allocator、自定义内存分配与-pmr-入门"><a href="#allocator、自定义内存分配与-pmr-入门" class="headerlink" title="allocator、自定义内存分配与 pmr 入门"></a>allocator、自定义内存分配与 pmr 入门</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：<code>std::allocator</code>、<code>allocator_traits</code>、分配与构造分离、状态型分配器、<code>std::pmr</code><br>核心目标：理解 STL 容器如何管理内存，以及什么时候值得定制分配器。</p>
</blockquote>
<hr>
<h2 id="1-为什么-allocator-存在"><a href="#1-为什么-allocator-存在" class="headerlink" title="1. 为什么 allocator 存在"></a>1. 为什么 allocator 存在</h2><p>容器不仅要“放元素”，还要处理：</p>
<ul>
<li>申请原始内存</li>
<li>在内存上构造对象</li>
<li>销毁对象</li>
<li>释放内存</li>
</ul>
<p>标准库把这层职责抽象成 allocator。</p>
<hr>
<h2 id="2-std-allocator-的核心职责"><a href="#2-std-allocator-的核心职责" class="headerlink" title="2. std::allocator 的核心职责"></a>2. <code>std::allocator</code> 的核心职责</h2><p>可以粗略理解成四步：</p>
<ol>
<li><code>allocate(n)</code>：分配能容纳 <code>n</code> 个对象的原始内存</li>
<li><code>construct(...)</code>：在指定位置构造对象</li>
<li><code>destroy(...)</code>：调用析构</li>
<li><code>deallocate(...)</code>：释放内存</li>
</ol>
<p>现代实现中更常通过：</p>
<ul>
<li><code>std::allocator_traits</code></li>
</ul>
<p>来统一调度这些接口。</p>
<hr>
<h2 id="3-为什么“分配”和“构造”是两件事"><a href="#3-为什么“分配”和“构造”是两件事" class="headerlink" title="3. 为什么“分配”和“构造”是两件事"></a>3. 为什么“分配”和“构造”是两件事</h2><p>因为拿到一块内存，不等于对象已经存在。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">T* p = alloc.<span class="built_in">allocate</span>(<span class="number">4</span>); <span class="comment">// 只有内存</span></span><br><span class="line">std::<span class="built_in">construct_at</span>(p, value); <span class="comment">// 这里对象才真正构造</span></span><br></pre></td></tr></table></figure>

<p>这也是容器能高效管理未初始化存储区的基础。</p>
<hr>
<h2 id="4-一个最小自定义分配器骨架"><a href="#4-一个最小自定义分配器骨架" class="headerlink" title="4. 一个最小自定义分配器骨架"></a>4. 一个最小自定义分配器骨架</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">MyAllocator</span> &#123;</span><br><span class="line">    <span class="keyword">using</span> value_type = T;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">MyAllocator</span>() = <span class="keyword">default</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">U</span>&gt;</span><br><span class="line">    <span class="built_in">MyAllocator</span>(<span class="type">const</span> MyAllocator&lt;U&gt;&amp;) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">T* <span class="title">allocate</span><span class="params">(std::<span class="type">size_t</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">static_cast</span>&lt;T*&gt;(::<span class="keyword">operator</span> <span class="built_in">new</span>(n * <span class="built_in">sizeof</span>(T)));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">deallocate</span><span class="params">(T* p, std::<span class="type">size_t</span>)</span> </span>&#123;</span><br><span class="line">        ::<span class="function"><span class="keyword">operator</span> <span class="title">delete</span><span class="params">(p)</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>配合容器使用：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>, MyAllocator&lt;<span class="type">int</span>&gt;&gt; v;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="5-什么时候值得自定义-allocator"><a href="#5-什么时候值得自定义-allocator" class="headerlink" title="5. 什么时候值得自定义 allocator"></a>5. 什么时候值得自定义 allocator</h2><p>不是所有项目都需要。</p>
<p>更常见的适用场景：</p>
<ul>
<li>频繁小对象分配</li>
<li>想用内存池减少碎片</li>
<li>想把对象放到特定区域</li>
<li>需要统计分配行为</li>
<li>游戏&#x2F;服务端有帧级或 arena 分配需求</li>
</ul>
<p>如果只是普通业务代码，标准分配器通常已经够用。</p>
<hr>
<h2 id="6-状态型分配器"><a href="#6-状态型分配器" class="headerlink" title="6. 状态型分配器"></a>6. 状态型分配器</h2><p>有些分配器不仅有类型，还有内部状态，例如：</p>
<ul>
<li>指向某个内存池</li>
<li>指向某个 arena</li>
</ul>
<p>这类分配器要特别注意：</p>
<ul>
<li>拷贝行为</li>
<li>容器复制&#x2F;移动时状态如何传播</li>
</ul>
<p>这也是 <code>allocator_traits</code> 很重要的原因之一。</p>
<hr>
<h2 id="7-std-pmr：现代-C-更实用的内存资源抽象"><a href="#7-std-pmr：现代-C-更实用的内存资源抽象" class="headerlink" title="7. std::pmr：现代 C++ 更实用的内存资源抽象"></a>7. <code>std::pmr</code>：现代 C++ 更实用的内存资源抽象</h2><p>C++17 提供了 <code>std::pmr</code>：</p>
<ul>
<li>polymorphic memory resource</li>
</ul>
<p>它把“分配策略”从模板参数层搬到了运行时资源对象层。</p>
<p>最常见组件：</p>
<ul>
<li><code>std::pmr::memory_resource</code></li>
<li><code>std::pmr::polymorphic_allocator</code></li>
<li><code>std::pmr::vector</code></li>
<li><code>std::pmr::monotonic_buffer_resource</code></li>
</ul>
<p>这通常比手写 allocator 模板更实用。</p>
<hr>
<h2 id="8-monotonic-buffer-resource-的直觉"><a href="#8-monotonic-buffer-resource-的直觉" class="headerlink" title="8. monotonic_buffer_resource 的直觉"></a>8. <code>monotonic_buffer_resource</code> 的直觉</h2><p>这种资源很适合：</p>
<ul>
<li>批量分配</li>
<li>很少单独释放</li>
<li>整体回收</li>
</ul>
<p>例如一次请求、一次帧更新、一次解析过程。</p>
<p>好处：</p>
<ul>
<li>分配快</li>
<li>局部性好</li>
</ul>
<p>代价：</p>
<ul>
<li>单个对象通常不能灵活归还给池</li>
</ul>
<hr>
<h2 id="9-和容器性能的关系"><a href="#9-和容器性能的关系" class="headerlink" title="9. 和容器性能的关系"></a>9. 和容器性能的关系</h2><p>allocator 影响的通常不是接口语义，而是：</p>
<ul>
<li>分配次数</li>
<li>分配成本</li>
<li>碎片</li>
<li>局部性</li>
</ul>
<p>但要注意：</p>
<ul>
<li>allocator 优化通常排在算法和数据布局之后</li>
<li>不要在没有证据前，把 allocator 当成首要瓶颈</li>
</ul>
<hr>
<h2 id="10-常见误区"><a href="#10-常见误区" class="headerlink" title="10. 常见误区"></a>10. 常见误区</h2><h3 id="10-1-把-allocator-当成“所有性能问题的解药”"><a href="#10-1-把-allocator-当成“所有性能问题的解药”" class="headerlink" title="10.1 把 allocator 当成“所有性能问题的解药”"></a>10.1 把 allocator 当成“所有性能问题的解药”</h3><p>很多性能问题其实更可能出在：</p>
<ul>
<li>数据布局</li>
<li>扩容策略</li>
<li>锁争用</li>
<li>随机访问</li>
</ul>
<h3 id="10-2-只会写-allocate-deallocate，却不理解对象构造时机"><a href="#10-2-只会写-allocate-deallocate，却不理解对象构造时机" class="headerlink" title="10.2 只会写 allocate/deallocate，却不理解对象构造时机"></a>10.2 只会写 <code>allocate/deallocate</code>，却不理解对象构造时机</h3><p>allocator 真正重要的是：</p>
<ul>
<li>内存和对象是分开的</li>
</ul>
<h3 id="10-3-在没有统一资源模型时滥用自定义-allocator"><a href="#10-3-在没有统一资源模型时滥用自定义-allocator" class="headerlink" title="10.3 在没有统一资源模型时滥用自定义 allocator"></a>10.3 在没有统一资源模型时滥用自定义 allocator</h3><p>结果容易让代码复杂度大幅上升。</p>
<hr>
<h2 id="11-一页总结"><a href="#11-一页总结" class="headerlink" title="11. 一页总结"></a>11. 一页总结</h2><p>allocator 这篇最值得记住的是：</p>
<ol>
<li>容器管理的是“原始内存 + 对象构造&#x2F;销毁”</li>
<li>allocator 负责内存来源</li>
<li><code>allocator_traits</code> 是现代实现的核心适配层</li>
<li>真正工程里，<code>std::pmr</code> 往往比手写 allocator 更实用</li>
</ol>
<p>如果只记一句：</p>
<blockquote>
<p>allocator 优化通常是进阶优化，前提是你已经把算法、数据布局和容器选择做对了。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>智能指针与所有权</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/02-%E6%99%BA%E8%83%BD%E6%8C%87%E9%92%88%E4%B8%8E%E6%89%80%E6%9C%89%E6%9D%83/</url>
    <content><![CDATA[<h1 id="智能指针与所有权"><a href="#智能指针与所有权" class="headerlink" title="智能指针与所有权"></a>智能指针与所有权</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：所有权、观察者、<code>unique_ptr</code>、<code>shared_ptr</code>、<code>weak_ptr</code>、自定义删除器<br>核心目标：把“谁负责释放资源”这件事表达清楚，而不是靠约定和记忆。</p>
</blockquote>
<hr>
<h2 id="1-为什么现代-C-强调所有权"><a href="#1-为什么现代-C-强调所有权" class="headerlink" title="1. 为什么现代 C++ 强调所有权"></a>1. 为什么现代 C++ 强调所有权</h2><p>裸指针只能表达：</p>
<ul>
<li>“这里有个地址”</li>
</ul>
<p>但它不能天然表达：</p>
<ul>
<li>谁拥有这个对象</li>
<li>谁负责释放</li>
<li>是否允许共享</li>
</ul>
<p>现代 C++ 实践里，第一件要说清的就是所有权。</p>
<hr>
<h2 id="2-三种常见关系"><a href="#2-三种常见关系" class="headerlink" title="2. 三种常见关系"></a>2. 三种常见关系</h2><h3 id="2-1-拥有（owning）"><a href="#2-1-拥有（owning）" class="headerlink" title="2.1 拥有（owning）"></a>2.1 拥有（owning）</h3><p>对象负责管理资源生命周期。</p>
<h3 id="2-2-观察（non-owning）"><a href="#2-2-观察（non-owning）" class="headerlink" title="2.2 观察（non-owning）"></a>2.2 观察（non-owning）</h3><p>对象只访问资源，不负责释放。</p>
<h3 id="2-3-共享拥有（shared-owning）"><a href="#2-3-共享拥有（shared-owning）" class="headerlink" title="2.3 共享拥有（shared owning）"></a>2.3 共享拥有（shared owning）</h3><p>多个对象共同延长同一资源生命周期。</p>
<hr>
<h2 id="3-unique-ptr：默认首选"><a href="#3-unique-ptr：默认首选" class="headerlink" title="3. unique_ptr：默认首选"></a>3. <code>unique_ptr</code>：默认首选</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> p = std::<span class="built_in">make_unique</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li>独占所有权</li>
<li>不可拷贝</li>
<li>可移动</li>
<li>开销低</li>
</ul>
<p>经验上：</p>
<ul>
<li>只要不是明确需要共享，优先用 <code>unique_ptr</code></li>
</ul>
<hr>
<h2 id="4-shared-ptr：共享拥有"><a href="#4-shared-ptr：共享拥有" class="headerlink" title="4. shared_ptr：共享拥有"></a>4. <code>shared_ptr</code>：共享拥有</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> p1 = std::<span class="built_in">make_shared</span>&lt;std::string&gt;(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line"><span class="keyword">auto</span> p2 = p1;</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li>引用计数</li>
<li>多个拥有者</li>
<li>生命周期更灵活</li>
</ul>
<p>代价：</p>
<ul>
<li>控制块</li>
<li>原子计数开销</li>
<li>更复杂的所有权关系</li>
</ul>
<p>所以不要把它当默认选项。</p>
<hr>
<h2 id="5-weak-ptr：打破循环"><a href="#5-weak-ptr：打破循环" class="headerlink" title="5. weak_ptr：打破循环"></a>5. <code>weak_ptr</code>：打破循环</h2><p><code>weak_ptr</code> 不拥有对象，只是观察。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::weak_ptr&lt;Foo&gt; weak = shared;</span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">auto</span> sp = weak.<span class="built_in">lock</span>()) &#123;</span><br><span class="line">    <span class="comment">// 对象还活着</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>它最重要的作用是：</p>
<ul>
<li>避免两个 <code>shared_ptr</code> 互相引用导致循环泄漏</li>
</ul>
<hr>
<h2 id="6-原则：拥有和观察要分开"><a href="#6-原则：拥有和观察要分开" class="headerlink" title="6. 原则：拥有和观察要分开"></a>6. 原则：拥有和观察要分开</h2><p>推荐的接口风格通常是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">take</span><span class="params">(std::unique_ptr&lt;Foo&gt; p)</span></span>; <span class="comment">// 接管所有权</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">use</span><span class="params">(Foo&amp; x)</span></span>;                  <span class="comment">// 一定存在，只观察</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">maybe</span><span class="params">(Foo* p)</span></span>;                <span class="comment">// 可为空观察</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">share</span><span class="params">(std::shared_ptr&lt;Foo&gt; p)</span></span>;<span class="comment">// 共享拥有</span></span><br></pre></td></tr></table></figure>

<p>这比“什么都传裸指针”更清楚。</p>
<hr>
<h2 id="7-自定义删除器"><a href="#7-自定义删除器" class="headerlink" title="7. 自定义删除器"></a>7. 自定义删除器</h2><p>有些资源不是 <code>delete</code> 释放，例如：</p>
<ul>
<li><code>FILE*</code> 要 <code>fclose</code></li>
<li><code>malloc</code> 对应 <code>free</code></li>
</ul>
<p>可以这样包装：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdio&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> FilePtr = std::unique_ptr&lt;FILE, <span class="built_in">int</span>(*)(FILE*)&gt;;</span><br><span class="line"></span><br><span class="line"><span class="function">FilePtr <span class="title">open_file</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* path)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">FilePtr</span>(std::<span class="built_in">fopen</span>(path, <span class="string">&quot;r&quot;</span>), std::fclose);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="8-常见误区"><a href="#8-常见误区" class="headerlink" title="8. 常见误区"></a>8. 常见误区</h2><h3 id="8-1-裸指针默认表示拥有"><a href="#8-1-裸指针默认表示拥有" class="headerlink" title="8.1 裸指针默认表示拥有"></a>8.1 裸指针默认表示拥有</h3><p>不推荐。<br>裸指针更适合表达观察关系。</p>
<h3 id="8-2-到处用-shared-ptr"><a href="#8-2-到处用-shared-ptr" class="headerlink" title="8.2 到处用 shared_ptr"></a>8.2 到处用 <code>shared_ptr</code></h3><p>这会让生命周期图变得混乱，还会带来额外开销。</p>
<h3 id="8-3-从-unique-ptr-的-get-拿到裸指针后乱删"><a href="#8-3-从-unique-ptr-的-get-拿到裸指针后乱删" class="headerlink" title="8.3 从 unique_ptr 的 get() 拿到裸指针后乱删"></a>8.3 从 <code>unique_ptr</code> 的 <code>get()</code> 拿到裸指针后乱删</h3><p><code>get()</code> 只是观察，不转移所有权。</p>
<hr>
<h2 id="9-一页总结"><a href="#9-一页总结" class="headerlink" title="9. 一页总结"></a>9. 一页总结</h2><p>最值得记住的顺序是：</p>
<ol>
<li>默认值语义</li>
<li>必须动态分配时优先 <code>unique_ptr</code></li>
<li>确实共享拥有时才用 <code>shared_ptr</code></li>
<li>观察关系用引用、裸指针或 <code>weak_ptr</code></li>
</ol>
<p>如果只记一句：</p>
<blockquote>
<p>智能指针不是为了“更高级”，而是为了把所有权表达清楚。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>生产者-消费者模式与阻塞队列</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/04-%E7%94%9F%E4%BA%A7%E8%80%85-%E6%B6%88%E8%B4%B9%E8%80%85%E6%A8%A1%E5%BC%8F%E4%B8%8E%E9%98%BB%E5%A1%9E%E9%98%9F%E5%88%97/</url>
    <content><![CDATA[<h1 id="生产者-消费者模式与阻塞队列"><a href="#生产者-消费者模式与阻塞队列" class="headerlink" title="生产者-消费者模式与阻塞队列"></a>生产者-消费者模式与阻塞队列</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：<code>mutex</code>、<code>condition_variable</code>、阻塞队列、bounded queue、shutdown、spurious wakeup<br>核心目标：写出一个正确、可复用的生产者-消费者队列，而不是“能跑但容易死锁或卡住”的版本。</p>
</blockquote>
<hr>
<h2 id="1-这个模式在解决什么问题"><a href="#1-这个模式在解决什么问题" class="headerlink" title="1. 这个模式在解决什么问题"></a>1. 这个模式在解决什么问题</h2><p>生产者-消费者模式适用于：</p>
<ul>
<li>生产方产生任务或消息</li>
<li>消费方异步处理</li>
<li>双方速度不一致</li>
</ul>
<p>典型场景：</p>
<ul>
<li>日志队列</li>
<li>任务队列</li>
<li>网络消息分发</li>
<li>线程池任务提交</li>
</ul>
<p>它的核心不只是“一个队列”，而是三件事：</p>
<ul>
<li>互斥</li>
<li>条件通知</li>
<li>生命周期关闭</li>
</ul>
<hr>
<h2 id="2-最小阻塞队列骨架"><a href="#2-最小阻塞队列骨架" class="headerlink" title="2. 最小阻塞队列骨架"></a>2. 最小阻塞队列骨架</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;condition_variable&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;queue&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BlockingQueue</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">push</span><span class="params">(T value)</span> </span>&#123;</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">            queue_.<span class="built_in">push</span>(std::<span class="built_in">move</span>(value));</span><br><span class="line">        &#125;</span><br><span class="line">        cv_.<span class="built_in">notify_one</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">T <span class="title">pop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="function">std::unique_lock&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">        cv_.<span class="built_in">wait</span>(lk, [&amp;] &#123; <span class="keyword">return</span> !queue_.<span class="built_in">empty</span>(); &#125;);</span><br><span class="line">        T value = std::<span class="built_in">move</span>(queue_.<span class="built_in">front</span>());</span><br><span class="line">        queue_.<span class="built_in">pop</span>();</span><br><span class="line">        <span class="keyword">return</span> value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::mutex mutex_;</span><br><span class="line">    std::condition_variable cv_;</span><br><span class="line">    std::queue&lt;T&gt; queue_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-为什么一定要用谓词版-wait"><a href="#3-为什么一定要用谓词版-wait" class="headerlink" title="3. 为什么一定要用谓词版 wait"></a>3. 为什么一定要用谓词版 <code>wait</code></h2><p>错误直觉是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">cv.<span class="built_in">wait</span>(lock);</span><br></pre></td></tr></table></figure>

<p>然后醒来就认为一定有数据。<br>这是不安全的，因为存在：</p>
<ul>
<li>虚假唤醒</li>
<li>多个线程竞争同一个条件</li>
</ul>
<p>正确写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">cv.<span class="built_in">wait</span>(lock, [&amp;] &#123; <span class="keyword">return</span> !queue_.<span class="built_in">empty</span>(); &#125;);</span><br></pre></td></tr></table></figure>

<p>也就是：</p>
<ul>
<li>醒来后重新检查条件</li>
</ul>
<hr>
<h2 id="4-bounded-queue：为什么需要容量上限"><a href="#4-bounded-queue：为什么需要容量上限" class="headerlink" title="4. bounded queue：为什么需要容量上限"></a>4. bounded queue：为什么需要容量上限</h2><p>如果生产速度远大于消费速度，无界队列会不断膨胀。<br>这时常需要有界队列：</p>
<ul>
<li>队列满时，生产者阻塞或失败</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (queue_.<span class="built_in">size</span>() &gt;= capacity_) &#123;</span><br><span class="line">    not_full_.<span class="built_in">wait</span>(lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样可以建立：</p>
<ul>
<li>背压</li>
<li>内存上限</li>
</ul>
<hr>
<h2 id="5-一个更完整的阻塞队列设计"><a href="#5-一个更完整的阻塞队列设计" class="headerlink" title="5. 一个更完整的阻塞队列设计"></a>5. 一个更完整的阻塞队列设计</h2><p>更工程化的队列通常需要这些接口：</p>
<ul>
<li><code>push</code></li>
<li><code>try_push</code></li>
<li><code>pop</code></li>
<li><code>try_pop</code></li>
<li><code>close</code></li>
</ul>
<p><code>close()</code> 很关键，因为消费者可能永远在等：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (closed_ &amp;&amp; queue_.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">    <span class="keyword">return</span> std::<span class="literal">nullopt</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>否则程序退出时很容易卡死在线程等待上。</p>
<hr>
<h2 id="6-推荐的关闭语义"><a href="#6-推荐的关闭语义" class="headerlink" title="6. 推荐的关闭语义"></a>6. 推荐的关闭语义</h2><p>常见设计是：</p>
<ul>
<li>关闭后不允许再 <code>push</code></li>
<li>还能把队列里剩余任务消费完</li>
<li>队列空且关闭时，<code>pop</code> 返回“结束”</li>
</ul>
<p>示意：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::optional&lt;T&gt; <span class="title">pop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">std::unique_lock&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">    cv_.<span class="built_in">wait</span>(lk, [&amp;] &#123; <span class="keyword">return</span> closed_ || !queue_.<span class="built_in">empty</span>(); &#125;);</span><br><span class="line">    <span class="keyword">if</span> (queue_.<span class="built_in">empty</span>()) <span class="keyword">return</span> std::<span class="literal">nullopt</span>;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这比靠塞一个 <code>&quot;EXIT&quot;</code> 哨兵值更通用。</p>
<hr>
<h2 id="7-什么时候用-notify-one，什么时候用-notify-all"><a href="#7-什么时候用-notify-one，什么时候用-notify-all" class="headerlink" title="7. 什么时候用 notify_one，什么时候用 notify_all"></a>7. 什么时候用 <code>notify_one</code>，什么时候用 <code>notify_all</code></h2><p>经验上：</p>
<ul>
<li>普通入队，通常 <code>notify_one</code></li>
<li>全局状态变化，比如 <code>close()</code>，通常 <code>notify_all</code></li>
</ul>
<p>因为关闭时可能有多个线程都在等待，需要全部唤醒重新判断。</p>
<hr>
<h2 id="8-一个更稳妥的示例"><a href="#8-一个更稳妥的示例" class="headerlink" title="8. 一个更稳妥的示例"></a>8. 一个更稳妥的示例</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;condition_variable&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;optional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;queue&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BlockingQueue</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">BlockingQueue</span><span class="params">(std::<span class="type">size_t</span> capacity)</span> : capacity_(capacity) &#123;</span>&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">push</span><span class="params">(T value)</span> </span>&#123;</span><br><span class="line">        <span class="function">std::unique_lock&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">        not_full_.<span class="built_in">wait</span>(lk, [&amp;] &#123; <span class="keyword">return</span> closed_ || queue_.<span class="built_in">size</span>() &lt; capacity_; &#125;);</span><br><span class="line">        <span class="keyword">if</span> (closed_) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        queue_.<span class="built_in">push</span>(std::<span class="built_in">move</span>(value));</span><br><span class="line">        lk.<span class="built_in">unlock</span>();</span><br><span class="line">        not_empty_.<span class="built_in">notify_one</span>();</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::optional&lt;T&gt; <span class="title">pop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="function">std::unique_lock&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">        not_empty_.<span class="built_in">wait</span>(lk, [&amp;] &#123; <span class="keyword">return</span> closed_ || !queue_.<span class="built_in">empty</span>(); &#125;);</span><br><span class="line">        <span class="keyword">if</span> (queue_.<span class="built_in">empty</span>()) <span class="keyword">return</span> std::<span class="literal">nullopt</span>;</span><br><span class="line">        T value = std::<span class="built_in">move</span>(queue_.<span class="built_in">front</span>());</span><br><span class="line">        queue_.<span class="built_in">pop</span>();</span><br><span class="line">        lk.<span class="built_in">unlock</span>();</span><br><span class="line">        not_full_.<span class="built_in">notify_one</span>();</span><br><span class="line">        <span class="keyword">return</span> value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">close</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">        closed_ = <span class="literal">true</span>;</span><br><span class="line">        not_empty_.<span class="built_in">notify_all</span>();</span><br><span class="line">        not_full_.<span class="built_in">notify_all</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::<span class="type">size_t</span> capacity_;</span><br><span class="line">    std::queue&lt;T&gt; queue_;</span><br><span class="line">    <span class="type">bool</span> closed_ = <span class="literal">false</span>;</span><br><span class="line">    std::mutex mutex_;</span><br><span class="line">    std::condition_variable not_empty_;</span><br><span class="line">    std::condition_variable not_full_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="9-常见坑"><a href="#9-常见坑" class="headerlink" title="9. 常见坑"></a>9. 常见坑</h2><h3 id="9-1-if-代替-while-谓词"><a href="#9-1-if-代替-while-谓词" class="headerlink" title="9.1 if 代替 while&#x2F;谓词"></a>9.1 <code>if</code> 代替 <code>while</code>&#x2F;谓词</h3><p>这是最常见的条件变量错误。</p>
<h3 id="9-2-持锁太久"><a href="#9-2-持锁太久" class="headerlink" title="9.2 持锁太久"></a>9.2 持锁太久</h3><p>如果拿着锁做重计算或 I&#x2F;O，会严重拖慢并发吞吐。</p>
<h3 id="9-3-没有关闭语义"><a href="#9-3-没有关闭语义" class="headerlink" title="9.3 没有关闭语义"></a>9.3 没有关闭语义</h3><p>线程可能永远阻塞退出不了。</p>
<h3 id="9-4-用哨兵值替代通用关闭协议"><a href="#9-4-用哨兵值替代通用关闭协议" class="headerlink" title="9.4 用哨兵值替代通用关闭协议"></a>9.4 用哨兵值替代通用关闭协议</h3><p>对简单 demo 可以，但扩展性差。</p>
<hr>
<h2 id="10-一页总结"><a href="#10-一页总结" class="headerlink" title="10. 一页总结"></a>10. 一页总结</h2><p>生产者-消费者模式的核心不是“有个队列”，而是：</p>
<ol>
<li>用互斥保护共享队列</li>
<li>用条件变量等待状态变化</li>
<li>用关闭协议管理线程退出</li>
<li>必要时用容量上限建立背压</li>
</ol>
<p>如果只记一句：</p>
<blockquote>
<p>条件变量永远和“共享状态 + 谓词检查”一起使用，不能只靠通知本身。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>线程同步消息队列与线程池</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/05-%E7%BA%BF%E7%A8%8B%E5%90%8C%E6%AD%A5%E6%B6%88%E6%81%AF%E9%98%9F%E5%88%97%E4%B8%8E%E7%BA%BF%E7%A8%8B%E6%B1%A0/</url>
    <content><![CDATA[<h1 id="线程同步消息队列与线程池"><a href="#线程同步消息队列与线程池" class="headerlink" title="线程同步消息队列与线程池"></a>线程同步消息队列与线程池</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：任务队列、worker thread、<code>future</code>、停止协议、背压、线程池<br>核心目标：理解线程池为什么几乎总是“队列 + 工作线程 + 生命周期管理”的组合。</p>
</blockquote>
<hr>
<h2 id="1-为什么线程池比“每个任务一个线程”更常见"><a href="#1-为什么线程池比“每个任务一个线程”更常见" class="headerlink" title="1. 为什么线程池比“每个任务一个线程”更常见"></a>1. 为什么线程池比“每个任务一个线程”更常见</h2><p>直接为每个任务创建线程的问题在于：</p>
<ul>
<li>创建销毁开销高</li>
<li>线程数不可控</li>
<li>容易把系统调度器压爆</li>
</ul>
<p>线程池的思路是：</p>
<ul>
<li>预先创建固定数量 worker</li>
<li>任务进入共享队列</li>
<li>worker 从队列取任务执行</li>
</ul>
<hr>
<h2 id="2-线程池最小结构"><a href="#2-线程池最小结构" class="headerlink" title="2. 线程池最小结构"></a>2. 线程池最小结构</h2><p>一个线程池通常包含：</p>
<ul>
<li>任务队列</li>
<li>多个工作线程</li>
<li>停止标志</li>
<li>提交接口</li>
</ul>
<p>示意：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">producer -&gt; task queue -&gt; workers</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-推荐的任务表示"><a href="#3-推荐的任务表示" class="headerlink" title="3. 推荐的任务表示"></a>3. 推荐的任务表示</h2><p>最常见的是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::function&lt;<span class="type">void</span>()&gt;</span><br></pre></td></tr></table></figure>

<p>这样线程池不关心任务具体类型，只负责执行。</p>
<p>如果要返回值，可以把真实任务包装进：</p>
<ul>
<li><code>std::packaged_task</code></li>
<li><code>std::promise</code></li>
<li><code>std::future</code></li>
</ul>
<hr>
<h2 id="4-一个最小线程池骨架"><a href="#4-一个最小线程池骨架" class="headerlink" title="4. 一个最小线程池骨架"></a>4. 一个最小线程池骨架</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;condition_variable&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ThreadPool</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">ThreadPool</span><span class="params">(std::<span class="type">size_t</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (std::<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">            workers_.<span class="built_in">emplace_back</span>([<span class="keyword">this</span>] &#123; <span class="built_in">worker_loop</span>(); &#125;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">ThreadPool</span>() &#123;</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">            stop_ = <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        cv_.<span class="built_in">notify_all</span>();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; t : workers_) &#123;</span><br><span class="line">            <span class="keyword">if</span> (t.<span class="built_in">joinable</span>()) t.<span class="built_in">join</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">submit</span><span class="params">(std::function&lt;<span class="type">void</span>()&gt; task)</span> </span>&#123;</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">            tasks_.<span class="built_in">push</span>(std::<span class="built_in">move</span>(task));</span><br><span class="line">        &#125;</span><br><span class="line">        cv_.<span class="built_in">notify_one</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">worker_loop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">            std::function&lt;<span class="type">void</span>()&gt; task;</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="function">std::unique_lock&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mutex_)</span></span>;</span><br><span class="line">                cv_.<span class="built_in">wait</span>(lk, [&amp;] &#123; <span class="keyword">return</span> stop_ || !tasks_.<span class="built_in">empty</span>(); &#125;);</span><br><span class="line">                <span class="keyword">if</span> (stop_ &amp;&amp; tasks_.<span class="built_in">empty</span>()) <span class="keyword">return</span>;</span><br><span class="line">                task = std::<span class="built_in">move</span>(tasks_.<span class="built_in">front</span>());</span><br><span class="line">                tasks_.<span class="built_in">pop</span>();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">task</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">bool</span> stop_ = <span class="literal">false</span>;</span><br><span class="line">    std::mutex mutex_;</span><br><span class="line">    std::condition_variable cv_;</span><br><span class="line">    std::queue&lt;std::function&lt;<span class="type">void</span>()&gt;&gt; tasks_;</span><br><span class="line">    std::vector&lt;std::thread&gt; workers_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="5-为什么停止协议很重要"><a href="#5-为什么停止协议很重要" class="headerlink" title="5. 为什么停止协议很重要"></a>5. 为什么停止协议很重要</h2><p>如果没有明确的停止逻辑，线程池很容易在析构时：</p>
<ul>
<li>worker 永远等在 <code>wait</code></li>
<li>主线程 join 不回来</li>
</ul>
<p>正确退出条件通常是：</p>
<ul>
<li><code>stop_ == true</code></li>
<li>并且队列已空</li>
</ul>
<hr>
<h2 id="6-返回值怎么做"><a href="#6-返回值怎么做" class="headerlink" title="6. 返回值怎么做"></a>6. 返回值怎么做</h2><p>常见写法是：</p>
<ul>
<li>把用户任务包装成 <code>packaged_task</code></li>
<li>返回对应 <code>future</code></li>
</ul>
<p>这样提交方既能异步执行，也能之后 <code>get()</code> 结果。</p>
<p>线程池的接口常见长这样：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">F</span>, <span class="keyword">class</span>... Args&gt;</span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">enqueue</span><span class="params">(F&amp;&amp; f, Args&amp;&amp;... args)</span> -&gt; std::future&lt;...&gt;</span>;</span><br></pre></td></tr></table></figure>

<p>这也是完美转发的高频实战场景。</p>
<hr>
<h2 id="7-有界任务队列与背压"><a href="#7-有界任务队列与背压" class="headerlink" title="7. 有界任务队列与背压"></a>7. 有界任务队列与背压</h2><p>如果任务生产速度远大于消费速度，线程池也可能把内存吃爆。<br>所以工程上经常要考虑：</p>
<ul>
<li>队列容量上限</li>
<li>超限后阻塞</li>
<li>超限后丢弃</li>
<li>超限后降级</li>
</ul>
<p>这其实就是背压策略。</p>
<hr>
<h2 id="8-线程池不是越多线程越好"><a href="#8-线程池不是越多线程越好" class="headerlink" title="8. 线程池不是越多线程越好"></a>8. 线程池不是越多线程越好</h2><p>线程数通常取决于：</p>
<ul>
<li>CPU 核心数</li>
<li>任务是否 CPU 密集</li>
<li>任务是否经常阻塞 I&#x2F;O</li>
</ul>
<p>经验上：</p>
<ul>
<li>CPU 密集型：线程数通常接近核心数</li>
<li>I&#x2F;O 密集型：线程数可适当更大</li>
</ul>
<hr>
<h2 id="9-消息队列-vs-线程池"><a href="#9-消息队列-vs-线程池" class="headerlink" title="9. 消息队列 vs 线程池"></a>9. 消息队列 vs 线程池</h2><p>这两个概念经常一起出现，但不完全一样。</p>
<ul>
<li>消息队列：强调数据传递与同步</li>
<li>线程池：强调任务执行与线程复用</li>
</ul>
<p>线程池内部几乎总会用到任务队列，但消息队列本身不一定等于线程池。</p>
<hr>
<h2 id="10-常见坑"><a href="#10-常见坑" class="headerlink" title="10. 常见坑"></a>10. 常见坑</h2><h3 id="10-1-任务里抛异常没人管"><a href="#10-1-任务里抛异常没人管" class="headerlink" title="10.1 任务里抛异常没人管"></a>10.1 任务里抛异常没人管</h3><p>如果没有 <code>future</code> 或显式捕获，异常可能直接导致线程终止。</p>
<h3 id="10-2-析构时仍允许提交任务"><a href="#10-2-析构时仍允许提交任务" class="headerlink" title="10.2 析构时仍允许提交任务"></a>10.2 析构时仍允许提交任务</h3><p>这会让生命周期变得混乱。</p>
<h3 id="10-3-持锁执行任务"><a href="#10-3-持锁执行任务" class="headerlink" title="10.3 持锁执行任务"></a>10.3 持锁执行任务</h3><p>这是严重错误。<br>正确做法是：</p>
<ul>
<li>取出任务后释放锁</li>
<li>再执行任务</li>
</ul>
<h3 id="10-4-线程池里再无限提交内部任务"><a href="#10-4-线程池里再无限提交内部任务" class="headerlink" title="10.4 线程池里再无限提交内部任务"></a>10.4 线程池里再无限提交内部任务</h3><p>这可能制造级联膨胀和死锁风险。</p>
<hr>
<h2 id="11-一页总结"><a href="#11-一页总结" class="headerlink" title="11. 一页总结"></a>11. 一页总结</h2><p>线程池最关键的不是模板技巧，而是三个工程点：</p>
<ol>
<li>任务队列</li>
<li>worker 生命周期</li>
<li>明确的停止与背压策略</li>
</ol>
<p>如果只记一句：</p>
<blockquote>
<p>线程池本质上是“用受控线程数去消费一个受控任务流”。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>游戏常见设计模式</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/07-%E6%B8%B8%E6%88%8F%E5%B8%B8%E8%A7%81%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="游戏常见设计模式"><a href="#游戏常见设计模式" class="headerlink" title="游戏常见设计模式"></a>游戏常见设计模式</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：单例、状态模式、命令模式、观察者、组件化、对象池<br>核心目标：从游戏开发常见场景出发，理解哪些模式真的有用，以及哪些模式容易被滥用。</p>
</blockquote>
<hr>
<h2 id="1-游戏代码为什么特别容易模式化"><a href="#1-游戏代码为什么特别容易模式化" class="headerlink" title="1. 游戏代码为什么特别容易模式化"></a>1. 游戏代码为什么特别容易模式化</h2><p>游戏逻辑常见特点：</p>
<ul>
<li>实体多</li>
<li>状态多</li>
<li>事件多</li>
<li>生命周期复杂</li>
<li>性能敏感</li>
</ul>
<p>因此很多经典模式在游戏里非常常见，但也特别容易被滥用。</p>
<hr>
<h2 id="2-单例模式：能用，但要克制"><a href="#2-单例模式：能用，但要克制" class="headerlink" title="2. 单例模式：能用，但要克制"></a>2. 单例模式：能用，但要克制</h2><p>单例通常用于：</p>
<ul>
<li>配置中心</li>
<li>日志系统</li>
<li>全局资源管理器</li>
</ul>
<p>最简单安全的写法通常是局部静态：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">GameConfig</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">static</span> GameConfig&amp; <span class="title">instance</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="type">static</span> GameConfig cfg;</span><br><span class="line">        <span class="keyword">return</span> cfg;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">GameConfig</span>() = <span class="keyword">default</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>优点：</p>
<ul>
<li>简单</li>
<li>线程安全初始化</li>
</ul>
<p>缺点：</p>
<ul>
<li>全局依赖隐蔽</li>
<li>测试困难</li>
<li>生命周期难拆</li>
</ul>
<p>所以经验上：</p>
<ul>
<li>单例适合少量基础设施，不适合把一切都做成全局对象</li>
</ul>
<hr>
<h2 id="3-状态模式：替代大-switch"><a href="#3-状态模式：替代大-switch" class="headerlink" title="3. 状态模式：替代大 switch"></a>3. 状态模式：替代大 <code>switch</code></h2><p>当一个角色会在多种状态之间切换，例如：</p>
<ul>
<li>Idle</li>
<li>Chase</li>
<li>Attack</li>
<li>Dead</li>
</ul>
<p>如果全写在一个大 <code>switch</code> 里，代码会越来越乱。<br>状态模式的思路是：</p>
<ul>
<li>每个状态自己负责更新逻辑和转移条件</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Monster</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">State</span> &#123;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">State</span>() = <span class="keyword">default</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">update</span><span class="params">(Monster&amp; m)</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这样“状态行为”会比“状态枚举 + 大分支”更容易扩展。</p>
<hr>
<h2 id="4-命令模式：把输入和行为解耦"><a href="#4-命令模式：把输入和行为解耦" class="headerlink" title="4. 命令模式：把输入和行为解耦"></a>4. 命令模式：把输入和行为解耦</h2><p>适用场景：</p>
<ul>
<li>输入映射</li>
<li>回放系统</li>
<li>AI 行为排队</li>
<li>网络同步操作记录</li>
</ul>
<p>基本思路：</p>
<ul>
<li>把“做什么”封装成命令对象</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Command</span> &#123;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Command</span>() = <span class="keyword">default</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">execute</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这样可以做到：</p>
<ul>
<li>排队执行</li>
<li>延迟执行</li>
<li>撤销&#x2F;重放</li>
</ul>
<hr>
<h2 id="5-观察者-事件模式"><a href="#5-观察者-事件模式" class="headerlink" title="5. 观察者 &#x2F; 事件模式"></a>5. 观察者 &#x2F; 事件模式</h2><p>适合：</p>
<ul>
<li>UI 更新</li>
<li>成就系统</li>
<li>音效触发</li>
<li>状态广播</li>
</ul>
<p>思路是：</p>
<ul>
<li>某个系统发事件</li>
<li>多个订阅者响应</li>
</ul>
<p>优点：</p>
<ul>
<li>降低模块直接耦合</li>
</ul>
<p>风险：</p>
<ul>
<li>调用链变隐蔽</li>
<li>调试困难</li>
</ul>
<p>所以事件系统要控制好：</p>
<ul>
<li>事件粒度</li>
<li>生命周期</li>
<li>订阅关系</li>
</ul>
<hr>
<h2 id="6-组件化-ECS-思路"><a href="#6-组件化-ECS-思路" class="headerlink" title="6. 组件化 &#x2F; ECS 思路"></a>6. 组件化 &#x2F; ECS 思路</h2><p>传统继承层级：</p>
<ul>
<li><code>Monster -&gt; BossMonster -&gt; FlyingBossMonster -&gt; ...</code></li>
</ul>
<p>很容易爆炸。<br>组件化更偏向：</p>
<ul>
<li>Position</li>
<li>Render</li>
<li>Physics</li>
<li>Health</li>
</ul>
<p>通过组合形成实体能力。</p>
<p>这样更灵活，也更适合：</p>
<ul>
<li>数据驱动</li>
<li>批量更新</li>
<li>SoA &#x2F; cache 友好设计</li>
</ul>
<hr>
<h2 id="7-对象池：减少频繁分配"><a href="#7-对象池：减少频繁分配" class="headerlink" title="7. 对象池：减少频繁分配"></a>7. 对象池：减少频繁分配</h2><p>游戏里这些对象往往高频创建销毁：</p>
<ul>
<li>子弹</li>
<li>粒子</li>
<li>临时特效</li>
</ul>
<p>如果每次都 <code>new/delete</code>，可能带来：</p>
<ul>
<li>分配开销</li>
<li>碎片</li>
<li>抖动</li>
</ul>
<p>对象池的思路是：</p>
<ul>
<li>提前分配一批对象</li>
<li>使用时取出</li>
<li>用完后归还</li>
</ul>
<p>但要注意：</p>
<ul>
<li>池化会增加状态管理复杂度</li>
<li>不是所有对象都值得池化</li>
</ul>
<hr>
<h2 id="8-模板方法模式"><a href="#8-模板方法模式" class="headerlink" title="8. 模板方法模式"></a>8. 模板方法模式</h2><p>适用于：</p>
<ul>
<li>主流程固定</li>
<li>个别步骤由派生类决定</li>
</ul>
<p>例如角色更新：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Character</span> &#123;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Character</span>() = <span class="keyword">default</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">think</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">move</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">draw</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">update</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="built_in">think</span>();</span><br><span class="line">        <span class="built_in">move</span>();</span><br><span class="line">        <span class="built_in">draw</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这种模式的优点是流程稳定，但要避免基类职责过重。</p>
<hr>
<h2 id="9-游戏开发里最常见的误区"><a href="#9-游戏开发里最常见的误区" class="headerlink" title="9. 游戏开发里最常见的误区"></a>9. 游戏开发里最常见的误区</h2><h3 id="9-1-什么都做成单例"><a href="#9-1-什么都做成单例" class="headerlink" title="9.1 什么都做成单例"></a>9.1 什么都做成单例</h3><p>最后会形成巨型全局依赖网。</p>
<h3 id="9-2-继承层级过深"><a href="#9-2-继承层级过深" class="headerlink" title="9.2 继承层级过深"></a>9.2 继承层级过深</h3><p>很多时候组合比继承更稳。</p>
<h3 id="9-3-事件系统滥用"><a href="#9-3-事件系统滥用" class="headerlink" title="9.3 事件系统滥用"></a>9.3 事件系统滥用</h3><p>会让控制流难以追踪。</p>
<h3 id="9-4-为了模式而模式"><a href="#9-4-为了模式而模式" class="headerlink" title="9.4 为了模式而模式"></a>9.4 为了模式而模式</h3><p>很多小项目只需要清晰结构，不需要把所有经典模式全搬进来。</p>
<hr>
<h2 id="10-一页总结"><a href="#10-一页总结" class="headerlink" title="10. 一页总结"></a>10. 一页总结</h2><p>游戏里真正常用、且值得优先掌握的几个模式是：</p>
<ol>
<li>状态模式</li>
<li>命令模式</li>
<li>观察者 &#x2F; 事件模式</li>
<li>组件化 &#x2F; ECS</li>
<li>对象池</li>
</ol>
<p>单例不是不能用，但一定要克制。</p>
<p>如果只记一句：</p>
<blockquote>
<p>游戏模式的价值不在“名词多高级”，而在于它能不能降低复杂状态和高频对象管理的混乱度。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>工厂模式、多态与接口设计</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/06-%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F-%E5%A4%9A%E6%80%81%E4%B8%8E%E6%8E%A5%E5%8F%A3%E8%AE%BE%E8%AE%A1/</url>
    <content><![CDATA[<h1 id="工厂模式、多态与接口设计"><a href="#工厂模式、多态与接口设计" class="headerlink" title="工厂模式、多态与接口设计"></a>工厂模式、多态与接口设计</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：抽象接口、虚函数、工厂函数、依赖倒置、<code>override</code>、虚析构、<code>unique_ptr</code><br>核心目标：理解什么时候该把“创建对象”和“使用对象”分开。</p>
</blockquote>
<hr>
<h2 id="1-为什么需要工厂模式"><a href="#1-为什么需要工厂模式" class="headerlink" title="1. 为什么需要工厂模式"></a>1. 为什么需要工厂模式</h2><p>很多代码的问题不是“不会 new”，而是：</p>
<ul>
<li>调用方知道太多具体类型</li>
<li>构造逻辑散落各处</li>
<li>后续替换实现很痛苦</li>
</ul>
<p>工厂模式的核心价值是：</p>
<ul>
<li>把对象创建逻辑集中起来</li>
<li>让调用方依赖抽象接口，而不是具体实现</li>
</ul>
<hr>
<h2 id="2-多态接口的基础"><a href="#2-多态接口的基础" class="headerlink" title="2. 多态接口的基础"></a>2. 多态接口的基础</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Pet</span> &#123;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Pet</span>() = <span class="keyword">default</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">speak</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Cat</span> : Pet &#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">speak</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123; std::<span class="built_in">puts</span>(<span class="string">&quot;meow&quot;</span>); &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Dog</span> : Pet &#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">speak</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123; std::<span class="built_in">puts</span>(<span class="string">&quot;woof&quot;</span>); &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这里要点有两个：</p>
<ul>
<li>基类析构函数要么虚，要么不允许多态删除</li>
<li>派生类重写时用 <code>override</code></li>
</ul>
<hr>
<h2 id="3-一个最简单的工厂函数"><a href="#3-一个最简单的工厂函数" class="headerlink" title="3. 一个最简单的工厂函数"></a>3. 一个最简单的工厂函数</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function">std::unique_ptr&lt;Pet&gt; <span class="title">make_pet</span><span class="params">(<span class="type">const</span> std::string&amp; kind)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (kind == <span class="string">&quot;cat&quot;</span>) <span class="keyword">return</span> std::<span class="built_in">make_unique</span>&lt;Cat&gt;();</span><br><span class="line">    <span class="keyword">if</span> (kind == <span class="string">&quot;dog&quot;</span>) <span class="keyword">return</span> std::<span class="built_in">make_unique</span>&lt;Dog&gt;();</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>调用方只关心：</p>
<ul>
<li>我要一个 <code>Pet</code></li>
</ul>
<p>而不关心：</p>
<ul>
<li>具体怎么构造 <code>Cat</code> &#x2F; <code>Dog</code></li>
</ul>
<hr>
<h2 id="4-为什么返回-unique-ptr"><a href="#4-为什么返回-unique-ptr" class="headerlink" title="4. 为什么返回 unique_ptr"></a>4. 为什么返回 <code>unique_ptr</code></h2><p>返回裸指针会引入一个问题：</p>
<ul>
<li>谁负责 <code>delete</code></li>
</ul>
<p>用 <code>std::unique_ptr</code> 更清晰：</p>
<ul>
<li>工厂负责创建</li>
<li>调用方接管独占所有权</li>
</ul>
<p>这是现代 C++ 工厂接口最常见的实践。</p>
<hr>
<h2 id="5-简单工厂-vs-工厂方法"><a href="#5-简单工厂-vs-工厂方法" class="headerlink" title="5. 简单工厂 vs 工厂方法"></a>5. 简单工厂 vs 工厂方法</h2><h3 id="5-1-简单工厂"><a href="#5-1-简单工厂" class="headerlink" title="5.1 简单工厂"></a>5.1 简单工厂</h3><p>一个集中函数，根据参数分支创建对象。</p>
<p>优点：</p>
<ul>
<li>简单直接</li>
</ul>
<p>缺点：</p>
<ul>
<li>新增类型时可能要改原工厂</li>
</ul>
<h3 id="5-2-工厂方法"><a href="#5-2-工厂方法" class="headerlink" title="5.2 工厂方法"></a>5.2 工厂方法</h3><p>把“创建哪种对象”交给子类。</p>
<p>适合：</p>
<ul>
<li>框架式扩展</li>
<li>产品族较复杂</li>
</ul>
<p>但对小项目来说，简单工厂已经够用。</p>
<hr>
<h2 id="6-接口设计比模式名更重要"><a href="#6-接口设计比模式名更重要" class="headerlink" title="6. 接口设计比模式名更重要"></a>6. 接口设计比模式名更重要</h2><p>真正工程里，更该关注这些问题：</p>
<ul>
<li>基类是不是表达了稳定抽象</li>
<li>调用方是否真的不需要知道具体类型</li>
<li>返回所有权是否清晰</li>
<li>是否需要注册表或插件化</li>
</ul>
<p>很多时候模式本身不复杂，难的是接口边界。</p>
<hr>
<h2 id="7-一个更贴近工程的例子"><a href="#7-一个更贴近工程的例子" class="headerlink" title="7. 一个更贴近工程的例子"></a>7. 一个更贴近工程的例子</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Reducer</span> &#123;</span><br><span class="line">    <span class="keyword">virtual</span> ~<span class="built_in">Reducer</span>() = <span class="keyword">default</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">int</span> <span class="title">init</span><span class="params">()</span> <span class="type">const</span> </span>= <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">int</span> <span class="title">combine</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> <span class="type">const</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">SumReducer</span> : Reducer &#123;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">init</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">override</span> </span>&#123; <span class="keyword">return</span> <span class="number">0</span>; &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">combine</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> <span class="type">const</span> <span class="keyword">override</span> </span>&#123; <span class="keyword">return</span> a + b; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">MulReducer</span> : Reducer &#123;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">init</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">override</span> </span>&#123; <span class="keyword">return</span> <span class="number">1</span>; &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">combine</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> <span class="type">const</span> <span class="keyword">override</span> </span>&#123; <span class="keyword">return</span> a * b; &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这里“算法骨架”不变，“聚合策略”可替换。<br>这类设计常和工厂、策略模式一起出现。</p>
<hr>
<h2 id="8-工厂模式的常见扩展"><a href="#8-工厂模式的常见扩展" class="headerlink" title="8. 工厂模式的常见扩展"></a>8. 工厂模式的常见扩展</h2><h3 id="8-1-注册表工厂"><a href="#8-1-注册表工厂" class="headerlink" title="8.1 注册表工厂"></a>8.1 注册表工厂</h3><p>把字符串或类型 id 映射到创建函数：</p>
<ul>
<li>更适合插件化</li>
<li>更适合可扩展系统</li>
</ul>
<h3 id="8-2-抽象工厂"><a href="#8-2-抽象工厂" class="headerlink" title="8.2 抽象工厂"></a>8.2 抽象工厂</h3><p>如果你需要创建一整组关联对象，就可能进入抽象工厂场景。</p>
<p>例如：</p>
<ul>
<li>UI 皮肤</li>
<li>跨平台组件族</li>
</ul>
<hr>
<h2 id="9-常见坑"><a href="#9-常见坑" class="headerlink" title="9. 常见坑"></a>9. 常见坑</h2><h3 id="9-1-基类没有虚析构"><a href="#9-1-基类没有虚析构" class="headerlink" title="9.1 基类没有虚析构"></a>9.1 基类没有虚析构</h3><p>多态删除会出问题。</p>
<h3 id="9-2-工厂返回裸指针"><a href="#9-2-工厂返回裸指针" class="headerlink" title="9.2 工厂返回裸指针"></a>9.2 工厂返回裸指针</h3><p>所有权不清晰。</p>
<h3 id="9-3-为了“用模式而用模式”"><a href="#9-3-为了“用模式而用模式”" class="headerlink" title="9.3 为了“用模式而用模式”"></a>9.3 为了“用模式而用模式”</h3><p>小项目里过度抽象只会增加复杂度。</p>
<h3 id="9-4-基类接口设计得太宽"><a href="#9-4-基类接口设计得太宽" class="headerlink" title="9.4 基类接口设计得太宽"></a>9.4 基类接口设计得太宽</h3><p>会让派生类被迫实现很多并不需要的东西。</p>
<hr>
<h2 id="10-一页总结"><a href="#10-一页总结" class="headerlink" title="10. 一页总结"></a>10. 一页总结</h2><p>工厂模式最核心的收益不是“设计模式名词”，而是：</p>
<ol>
<li>创建逻辑集中</li>
<li>调用方依赖抽象</li>
<li>所有权表达清晰</li>
</ol>
<p>如果只记一句：</p>
<blockquote>
<p>当“对象怎么创建”开始影响“对象怎么使用”时，就该考虑把创建逻辑抽出来。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>对象布局、栈堆与未定义行为</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/08-%E5%AF%B9%E8%B1%A1%E5%B8%83%E5%B1%80-%E6%A0%88%E5%A0%86%E4%B8%8E%E6%9C%AA%E5%AE%9A%E4%B9%89%E8%A1%8C%E4%B8%BA/</url>
    <content><![CDATA[<h1 id="对象布局、栈堆与未定义行为"><a href="#对象布局、栈堆与未定义行为" class="headerlink" title="对象布局、栈堆与未定义行为"></a>对象布局、栈堆与未定义行为</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：栈、堆、静态区、对齐、padding、悬空指针、越界、strict aliasing<br>核心目标：建立“对象怎么放在内存里”的正确直觉，避免把现代 C++ 写成偶发崩溃的未定义行为集合。</p>
</blockquote>
<hr>
<h2 id="1-栈和堆最容易被误解的点"><a href="#1-栈和堆最容易被误解的点" class="headerlink" title="1. 栈和堆最容易被误解的点"></a>1. 栈和堆最容易被误解的点</h2><p>简单说：</p>
<ul>
<li>栈：作用域驱动的自动存储</li>
<li>堆：动态分配、手动或 RAII 管理</li>
</ul>
<p>但更准确的重点不是“栈连续还是不连续”，而是：</p>
<ul>
<li>对象生命周期</li>
<li>所有权</li>
<li>是否发生悬空和越界</li>
</ul>
<hr>
<h2 id="2-常见内存区域"><a href="#2-常见内存区域" class="headerlink" title="2. 常见内存区域"></a>2. 常见内存区域</h2><p>粗略理解：</p>
<ul>
<li>代码区</li>
<li>全局&#x2F;静态区</li>
<li>栈</li>
<li>堆</li>
</ul>
<p>局部变量通常在栈上：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p>动态分配通常在堆上：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> p = std::<span class="built_in">make_unique</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-栈对象的最大优点"><a href="#3-栈对象的最大优点" class="headerlink" title="3. 栈对象的最大优点"></a>3. 栈对象的最大优点</h2><p>栈对象最大优点不是“快”这一个字，而是：</p>
<ul>
<li>生命周期清晰</li>
<li>自动析构</li>
<li>适合 RAII</li>
</ul>
<p>所以现代 C++ 的默认倾向是：</p>
<ul>
<li>能值语义就值语义</li>
<li>能局部对象就局部对象</li>
</ul>
<hr>
<h2 id="4-栈对象最大的风险"><a href="#4-栈对象最大的风险" class="headerlink" title="4. 栈对象最大的风险"></a>4. 栈对象最大的风险</h2><p>不是“栈内存不连续”，而是：</p>
<ul>
<li>返回局部变量地址</li>
<li>返回局部引用</li>
<li>离开作用域后继续访问</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span>* <span class="title">bad</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> x = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> &amp;x; <span class="comment">// 错</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="5-堆对象的价值和代价"><a href="#5-堆对象的价值和代价" class="headerlink" title="5. 堆对象的价值和代价"></a>5. 堆对象的价值和代价</h2><p>堆对象适合：</p>
<ul>
<li>跨作用域存活</li>
<li>运行期决定大小</li>
<li>多态对象</li>
</ul>
<p>代价是：</p>
<ul>
<li>需要明确所有权</li>
<li>分配释放有开销</li>
<li>更容易泄漏或悬空</li>
</ul>
<p>所以：</p>
<ul>
<li>堆不是默认选项</li>
<li>只有确实需要时才动态分配</li>
</ul>
<hr>
<h2 id="6-对齐与-padding"><a href="#6-对齐与-padding" class="headerlink" title="6. 对齐与 padding"></a>6. 对齐与 padding</h2><p>对象内存布局受类型对齐影响。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">A</span> &#123;</span><br><span class="line">    <span class="type">char</span> c;</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><code>sizeof(A)</code> 往往不只是 5，而可能是 8。<br>原因是：</p>
<ul>
<li>编译器会插入 padding 满足对齐要求</li>
</ul>
<p>这会影响：</p>
<ul>
<li>内存占用</li>
<li>缓存利用率</li>
<li>二进制布局</li>
</ul>
<hr>
<h2 id="7-未定义行为最常见的几类"><a href="#7-未定义行为最常见的几类" class="headerlink" title="7. 未定义行为最常见的几类"></a>7. 未定义行为最常见的几类</h2><h3 id="7-1-越界访问"><a href="#7-1-越界访问" class="headerlink" title="7.1 越界访问"></a>7.1 越界访问</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">4</span>];</span><br><span class="line"><span class="type">int</span> x = a[<span class="number">10</span>];</span><br></pre></td></tr></table></figure>

<h3 id="7-2-悬空指针"><a href="#7-2-悬空指针" class="headerlink" title="7.2 悬空指针"></a>7.2 悬空指针</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* p = <span class="keyword">new</span> <span class="built_in">int</span>(<span class="number">1</span>);</span><br><span class="line"><span class="keyword">delete</span> p;</span><br><span class="line">*p = <span class="number">2</span>; <span class="comment">// 错</span></span><br></pre></td></tr></table></figure>

<h3 id="7-3-错误类型解释"><a href="#7-3-错误类型解释" class="headerlink" title="7.3 错误类型解释"></a>7.3 错误类型解释</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">double</span> d = <span class="number">3.14</span>;</span><br><span class="line"><span class="type">int</span>* p = <span class="built_in">reinterpret_cast</span>&lt;<span class="type">int</span>*&gt;(&amp;d); <span class="comment">// 极危险</span></span><br></pre></td></tr></table></figure>

<h3 id="7-4-严格别名相关问题"><a href="#7-4-严格别名相关问题" class="headerlink" title="7.4 严格别名相关问题"></a>7.4 严格别名相关问题</h3><p>某些类型转换会让编译器优化假设失效。</p>
<hr>
<h2 id="8-为什么现代-C-强调封装"><a href="#8-为什么现代-C-强调封装" class="headerlink" title="8. 为什么现代 C++ 强调封装"></a>8. 为什么现代 C++ 强调封装</h2><p>很多底层问题不是“不允许碰”，而是：</p>
<ul>
<li>一旦你直接操作裸内存，就必须承担全部正确性责任</li>
</ul>
<p>所以现代实践更推荐：</p>
<ul>
<li><code>std::vector</code></li>
<li><code>std::string</code></li>
<li><code>std::array</code></li>
<li><code>std::span</code></li>
<li>智能指针</li>
</ul>
<p>来包住底层细节。</p>
<hr>
<h2 id="9-一页总结"><a href="#9-一页总结" class="headerlink" title="9. 一页总结"></a>9. 一页总结</h2><p>最重要的三条：</p>
<ol>
<li>栈对象优先，因为生命周期最清晰</li>
<li>堆对象只有在确实需要动态生命周期时才用</li>
<li>越界、悬空、错误类型解释都不是“小问题”，而是未定义行为</li>
</ol>
<p>如果只记一句：</p>
<blockquote>
<p>现代 C++ 不是不让你碰底层，而是要求你在碰底层时明确对象生命周期和内存语义。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>C++20 协程入门与实践</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/10-c-20%E5%8D%8F%E7%A8%8B%E5%85%A5%E9%97%A8%E4%B8%8E%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<h1 id="C-20-协程入门与实践"><a href="#C-20-协程入门与实践" class="headerlink" title="C++20 协程入门与实践"></a>C++20 协程入门与实践</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：<code>co_await</code>、<code>co_return</code>、<code>co_yield</code>、<code>promise_type</code>、挂起点、异步流程、generator<br>核心目标：理解协程为什么不是“更轻的线程”，而是一种把异步控制流写成顺序代码的语言机制。</p>
</blockquote>
<hr>
<h2 id="1-协程到底解决什么问题"><a href="#1-协程到底解决什么问题" class="headerlink" title="1. 协程到底解决什么问题"></a>1. 协程到底解决什么问题</h2><p>传统异步代码常见两个问题：</p>
<ul>
<li>回调层层嵌套</li>
<li>状态机代码难写难读</li>
</ul>
<p>协程的核心价值是：</p>
<ul>
<li>把“会暂停、稍后恢复”的流程写成看起来接近顺序代码的形式</li>
</ul>
<p>所以它更像：</p>
<ul>
<li>语言级状态机生成器</li>
</ul>
<p>而不是线程替代品。</p>
<hr>
<h2 id="2-协程不是线程"><a href="#2-协程不是线程" class="headerlink" title="2. 协程不是线程"></a>2. 协程不是线程</h2><p>这点最重要。</p>
<p>协程：</p>
<ul>
<li>默认不并行</li>
<li>默认不自动切线程</li>
<li>只是能挂起和恢复</li>
</ul>
<p>线程：</p>
<ul>
<li>是操作系统调度实体</li>
<li>真正涉及并行执行</li>
</ul>
<p>所以：</p>
<ul>
<li>协程解决的是“控制流组织”</li>
<li>线程解决的是“执行资源”</li>
</ul>
<hr>
<h2 id="3-三个关键字"><a href="#3-三个关键字" class="headerlink" title="3. 三个关键字"></a>3. 三个关键字</h2><h3 id="3-1-co-await"><a href="#3-1-co-await" class="headerlink" title="3.1 co_await"></a>3.1 <code>co_await</code></h3><p>等待某个可等待对象，并可能挂起当前协程。</p>
<h3 id="3-2-co-return"><a href="#3-2-co-return" class="headerlink" title="3.2 co_return"></a>3.2 <code>co_return</code></h3><p>从协程返回结果。</p>
<h3 id="3-3-co-yield"><a href="#3-3-co-yield" class="headerlink" title="3.3 co_yield"></a>3.3 <code>co_yield</code></h3><p>常用于生成器场景，逐个产出值。</p>
<hr>
<h2 id="4-编译器视角下协程发生了什么"><a href="#4-编译器视角下协程发生了什么" class="headerlink" title="4. 编译器视角下协程发生了什么"></a>4. 编译器视角下协程发生了什么</h2><p>当函数里出现协程关键字后，它会被编译器改写成：</p>
<ul>
<li>一个协程状态对象</li>
<li>一个 <code>promise_type</code></li>
<li>若干挂起点</li>
<li>一个恢复入口</li>
</ul>
<p>也就是说，协程本质上是：</p>
<ul>
<li>编译器自动帮你拆出来的状态机</li>
</ul>
<hr>
<h2 id="5-promise-type-是什么"><a href="#5-promise-type-是什么" class="headerlink" title="5. promise_type 是什么"></a>5. <code>promise_type</code> 是什么</h2><p>如果一个返回类型想作为协程返回对象，就需要配套定义：</p>
<ul>
<li><code>promise_type</code></li>
</ul>
<p>它负责描述：</p>
<ul>
<li>协程如何创建</li>
<li>如何返回值</li>
<li>初始&#x2F;最终是否挂起</li>
<li>异常怎么处理</li>
</ul>
<p>这是 C++ 协程里最“底层”的部分。</p>
<hr>
<h2 id="6-一个最小协程返回类型骨架"><a href="#6-一个最小协程返回类型骨架" class="headerlink" title="6. 一个最小协程返回类型骨架"></a>6. 一个最小协程返回类型骨架</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;coroutine&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Task</span> &#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">promise_type</span> &#123;</span><br><span class="line">        <span class="function">Task <span class="title">get_return_object</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> &#123;&#125;; &#125;</span><br><span class="line">        <span class="function">std::suspend_never <span class="title">initial_suspend</span><span class="params">()</span> <span class="keyword">noexcept</span> </span>&#123; <span class="keyword">return</span> &#123;&#125;; &#125;</span><br><span class="line">        <span class="function">std::suspend_never <span class="title">final_suspend</span><span class="params">()</span> <span class="keyword">noexcept</span> </span>&#123; <span class="keyword">return</span> &#123;&#125;; &#125;</span><br><span class="line">        <span class="function"><span class="type">void</span> <span class="title">return_void</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">        <span class="function"><span class="type">void</span> <span class="title">unhandled_exception</span><span class="params">()</span> </span>&#123; std::<span class="built_in">terminate</span>(); &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这样一个返回 <code>Task</code> 的函数就可以写成协程。</p>
<hr>
<h2 id="7-co-await-的直觉"><a href="#7-co-await-的直觉" class="headerlink" title="7. co_await 的直觉"></a>7. <code>co_await</code> 的直觉</h2><p>写：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">co_await</span> something;</span><br></pre></td></tr></table></figure>

<p>编译器会尝试把 <code>something</code> 变成一个 awaitable，并调用：</p>
<ul>
<li><code>await_ready()</code></li>
<li><code>await_suspend()</code></li>
<li><code>await_resume()</code></li>
</ul>
<p>可以粗略理解为：</p>
<ol>
<li>先问要不要挂起</li>
<li>如果挂起，如何安排恢复</li>
<li>恢复后返回什么结果</li>
</ol>
<hr>
<h2 id="8-generator-场景为什么适合协程"><a href="#8-generator-场景为什么适合协程" class="headerlink" title="8. generator 场景为什么适合协程"></a>8. <code>generator</code> 场景为什么适合协程</h2><p>例如想逐个生成值：</p>
<ul>
<li>传统写法要手工保存状态</li>
<li>协程可以自然写成“产出一个，暂停，再继续”</li>
</ul>
<p>这正是 <code>co_yield</code> 最直观的用法。</p>
<p>所以协程特别适合：</p>
<ul>
<li>lazy sequence</li>
<li>parser</li>
<li>pipeline</li>
<li>异步流</li>
</ul>
<hr>
<h2 id="9-协程最常见的工程用途"><a href="#9-协程最常见的工程用途" class="headerlink" title="9. 协程最常见的工程用途"></a>9. 协程最常见的工程用途</h2><h3 id="9-1-异步-I-O"><a href="#9-1-异步-I-O" class="headerlink" title="9.1 异步 I&#x2F;O"></a>9.1 异步 I&#x2F;O</h3><p>把：</p>
<ul>
<li>发请求</li>
<li>等待回包</li>
<li>继续处理</li>
</ul>
<p>写成线性流程。</p>
<h3 id="9-2-生成器"><a href="#9-2-生成器" class="headerlink" title="9.2 生成器"></a>9.2 生成器</h3><p>按需逐个产出数据。</p>
<h3 id="9-3-Actor-任务系统"><a href="#9-3-Actor-任务系统" class="headerlink" title="9.3 Actor &#x2F; 任务系统"></a>9.3 Actor &#x2F; 任务系统</h3><p>用协程表达暂停与恢复点。</p>
<hr>
<h2 id="10-协程为什么很容易“看起来简单，实际上不简单”"><a href="#10-协程为什么很容易“看起来简单，实际上不简单”" class="headerlink" title="10. 协程为什么很容易“看起来简单，实际上不简单”"></a>10. 协程为什么很容易“看起来简单，实际上不简单”</h2><p>因为源码很线性，但真实问题仍然存在：</p>
<ul>
<li>生命周期谁管</li>
<li>在哪条线程恢复</li>
<li>恢复时机谁触发</li>
<li>异常怎么传播</li>
<li>取消怎么处理</li>
</ul>
<p>也就是说：</p>
<ul>
<li>协程简化的是控制流表达</li>
<li>没有消灭异步系统的本质复杂度</li>
</ul>
<hr>
<h2 id="11-一个最重要的工程问题：恢复在哪发生"><a href="#11-一个最重要的工程问题：恢复在哪发生" class="headerlink" title="11. 一个最重要的工程问题：恢复在哪发生"></a>11. 一个最重要的工程问题：恢复在哪发生</h2><p>协程本身不决定线程。<br>真正决定“恢复在哪个执行器&#x2F;事件循环&#x2F;线程池”的，是 awaitable 或运行时框架。</p>
<p>所以写协程框架时一定要搞清楚：</p>
<ul>
<li>谁调 <code>resume</code></li>
<li>什么时候调</li>
<li>在哪调</li>
</ul>
<hr>
<h2 id="12-常见坑"><a href="#12-常见坑" class="headerlink" title="12. 常见坑"></a>12. 常见坑</h2><h3 id="12-1-把协程当轻量线程"><a href="#12-1-把协程当轻量线程" class="headerlink" title="12.1 把协程当轻量线程"></a>12.1 把协程当轻量线程</h3><p>这是最常见误解。</p>
<h3 id="12-2-忽略返回对象和-promise-生命周期"><a href="#12-2-忽略返回对象和-promise-生命周期" class="headerlink" title="12.2 忽略返回对象和 promise 生命周期"></a>12.2 忽略返回对象和 promise 生命周期</h3><p>协程帧如果没人管理，容易泄漏或悬空。</p>
<h3 id="12-3-只学语法，不理解恢复机制"><a href="#12-3-只学语法，不理解恢复机制" class="headerlink" title="12.3 只学语法，不理解恢复机制"></a>12.3 只学语法，不理解恢复机制</h3><p>这样很快就会在真实异步项目里迷路。</p>
<h3 id="12-4-在协程里捕获悬空引用"><a href="#12-4-在协程里捕获悬空引用" class="headerlink" title="12.4 在协程里捕获悬空引用"></a>12.4 在协程里捕获悬空引用</h3><p>因为协程可能挂起很久，引用生命周期尤其要小心。</p>
<hr>
<h2 id="13-一页总结"><a href="#13-一页总结" class="headerlink" title="13. 一页总结"></a>13. 一页总结</h2><p>协程最重要的理解链是：</p>
<ol>
<li>协程不是线程，而是可挂起函数</li>
<li>编译器会把协程改写成状态机</li>
<li><code>co_await</code> 的核心是等待、挂起和恢复</li>
<li>真正工程难点在生命周期、调度器和恢复时机</li>
</ol>
<p>如果只记一句：</p>
<blockquote>
<p>协程的价值是把异步流程写直，而不是把并发问题自动解决掉。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>现代 C++ 常用工具类型</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/11-%E7%8E%B0%E4%BB%A3c-%E5%B8%B8%E7%94%A8%E5%B7%A5%E5%85%B7%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<h1 id="现代-C-常用工具类型"><a href="#现代-C-常用工具类型" class="headerlink" title="现代 C++ 常用工具类型"></a>现代 C++ 常用工具类型</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：<code>optional</code>、<code>variant</code>、<code>any</code>、<code>string_view</code>、<code>span</code>、<code>expected</code><br>核心目标：掌握几个现代 C++ 里非常高频、能直接改善接口表达和代码质量的标准库类型。</p>
</blockquote>
<hr>
<h2 id="1-为什么这些类型重要"><a href="#1-为什么这些类型重要" class="headerlink" title="1. 为什么这些类型重要"></a>1. 为什么这些类型重要</h2><p>现代 C++ 的很多进步，不只在语法，还在于：</p>
<ul>
<li>用更明确的类型表达意图</li>
</ul>
<p>比如：</p>
<ul>
<li>“可能没有值”</li>
<li>“可能是多种类型之一”</li>
<li>“只读字符串视图”</li>
<li>“一段连续内存视图”</li>
</ul>
<p>这些都不该继续靠：</p>
<ul>
<li><code>nullptr</code></li>
<li>魔法值</li>
<li><code>void*</code></li>
<li>裸指针 + 长度</li>
</ul>
<p>来硬撑。</p>
<hr>
<h2 id="2-std-optional：可能有，也可能没有"><a href="#2-std-optional：可能有，也可能没有" class="headerlink" title="2. std::optional&lt;T&gt;：可能有，也可能没有"></a>2. <code>std::optional&lt;T&gt;</code>：可能有，也可能没有</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;optional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function">std::optional&lt;<span class="type">int</span>&gt; <span class="title">find_id</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>

<p>它适合表达：</p>
<ul>
<li>成功返回一个值</li>
<li>失败时没有值</li>
</ul>
<p>比“返回 <code>-1</code> 表示失败”更清晰。</p>
<p>常用接口：</p>
<ul>
<li><code>has_value()</code></li>
<li><code>value()</code></li>
<li><code>value_or(default)</code></li>
</ul>
<hr>
<h2 id="3-std-variant：类型安全的联合体"><a href="#3-std-variant：类型安全的联合体" class="headerlink" title="3. std::variant：类型安全的联合体"></a>3. <code>std::variant</code>：类型安全的联合体</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;variant&gt;</span></span></span><br><span class="line"></span><br><span class="line">std::variant&lt;<span class="type">int</span>, std::string&gt; v;</span><br></pre></td></tr></table></figure>

<p>它表示：</p>
<ul>
<li>值一定是若干候选类型中的一个</li>
</ul>
<p>相比传统 <code>union</code>，它：</p>
<ul>
<li>类型安全</li>
<li>自动管理对象生命周期</li>
</ul>
<p>配合 <code>std::visit</code> 很常见。</p>
<hr>
<h2 id="4-std-any：完全动态类型"><a href="#4-std-any：完全动态类型" class="headerlink" title="4. std::any：完全动态类型"></a>4. <code>std::any</code>：完全动态类型</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;any&gt;</span></span></span><br><span class="line"></span><br><span class="line">std::any x = <span class="number">42</span>;</span><br><span class="line">x = std::<span class="built_in">string</span>(<span class="string">&quot;hello&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>它适合：</p>
<ul>
<li>真正不知道运行期会是什么类型</li>
</ul>
<p>但代价是：</p>
<ul>
<li>类型信息晚到运行期</li>
<li>可读性和性能都不如 <code>variant</code></li>
</ul>
<p>经验上：</p>
<ul>
<li>能用 <code>variant</code> 就不要先上 <code>any</code></li>
</ul>
<hr>
<h2 id="5-std-string-view：无拷贝字符串视图"><a href="#5-std-string-view：无拷贝字符串视图" class="headerlink" title="5. std::string_view：无拷贝字符串视图"></a>5. <code>std::string_view</code>：无拷贝字符串视图</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string_view&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">(std::string_view s)</span></span>;</span><br></pre></td></tr></table></figure>

<p>优点：</p>
<ul>
<li>不拥有字符串</li>
<li>不分配内存</li>
<li>可接 <code>std::string</code>、字面量、子串视图</li>
</ul>
<p>风险：</p>
<ul>
<li>它不延长底层字符串生命周期</li>
</ul>
<p>所以不能把它保存得比源字符串活得更久。</p>
<hr>
<h2 id="6-std-span：无拷贝连续内存视图"><a href="#6-std-span：无拷贝连续内存视图" class="headerlink" title="6. std::span&lt;T&gt;：无拷贝连续内存视图"></a>6. <code>std::span&lt;T&gt;</code>：无拷贝连续内存视图</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;span&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">process</span><span class="params">(std::span&lt;<span class="type">const</span> <span class="type">int</span>&gt; xs)</span></span>;</span><br></pre></td></tr></table></figure>

<p>适合：</p>
<ul>
<li>数组</li>
<li><code>std::vector</code></li>
<li><code>std::array</code></li>
</ul>
<p>相比传：</p>
<ul>
<li>裸指针 + 长度</li>
</ul>
<p>更清晰，也更安全。</p>
<hr>
<h2 id="7-std-expected：值或错误"><a href="#7-std-expected：值或错误" class="headerlink" title="7. std::expected：值或错误"></a>7. <code>std::expected</code>：值或错误</h2><p>如果你的环境支持 C++23，可以关注：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::expected&lt;Value, Error&gt;</span><br></pre></td></tr></table></figure>

<p>它适合表达：</p>
<ul>
<li>成功时返回值</li>
<li>失败时返回明确错误信息</li>
</ul>
<p>相比 <code>optional</code>，它多了：</p>
<ul>
<li>为什么失败</li>
</ul>
<hr>
<h2 id="8-这些类型最核心的接口收益"><a href="#8-这些类型最核心的接口收益" class="headerlink" title="8. 这些类型最核心的接口收益"></a>8. 这些类型最核心的接口收益</h2><h3 id="8-1-optional"><a href="#8-1-optional" class="headerlink" title="8.1 optional"></a>8.1 <code>optional</code></h3><p>不再靠魔法值表示“没有结果”。</p>
<h3 id="8-2-variant"><a href="#8-2-variant" class="headerlink" title="8.2 variant"></a>8.2 <code>variant</code></h3><p>不再靠手写 tag + union。</p>
<h3 id="8-3-string-view"><a href="#8-3-string-view" class="headerlink" title="8.3 string_view"></a>8.3 <code>string_view</code></h3><p>函数参数更轻、更泛化。</p>
<h3 id="8-4-span"><a href="#8-4-span" class="headerlink" title="8.4 span"></a>8.4 <code>span</code></h3><p>数组接口更现代。</p>
<hr>
<h2 id="9-常见坑"><a href="#9-常见坑" class="headerlink" title="9. 常见坑"></a>9. 常见坑</h2><h3 id="9-1-string-view-span-生命周期错误"><a href="#9-1-string-view-span-生命周期错误" class="headerlink" title="9.1 string_view &#x2F; span 生命周期错误"></a>9.1 <code>string_view</code> &#x2F; <code>span</code> 生命周期错误</h3><p>它们都只是视图，不拥有数据。</p>
<h3 id="9-2-用-any-代替清晰设计"><a href="#9-2-用-any-代替清晰设计" class="headerlink" title="9.2 用 any 代替清晰设计"></a>9.2 用 <code>any</code> 代替清晰设计</h3><p>很多时候 <code>any</code> 只是把类型问题往后拖。</p>
<h3 id="9-3-optional-里塞重对象却频繁拷贝"><a href="#9-3-optional-里塞重对象却频繁拷贝" class="headerlink" title="9.3 optional 里塞重对象却频繁拷贝"></a>9.3 <code>optional</code> 里塞重对象却频繁拷贝</h3><p>虽然语义清晰，但也要注意值类别和性能。</p>
<hr>
<h2 id="10-一页总结"><a href="#10-一页总结" class="headerlink" title="10. 一页总结"></a>10. 一页总结</h2><p>这几个类型的价值可以压缩成一句话：</p>
<blockquote>
<p>用更准确的标准库类型表达接口语义，减少魔法值、裸指针和不透明约定。</p>
</blockquote>
<p>最值得优先掌握的顺序通常是：</p>
<ol>
<li><code>optional</code></li>
<li><code>string_view</code></li>
<li><code>span</code></li>
<li><code>variant</code></li>
<li><code>any</code></li>
<li><code>expected</code></li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>网络服务基础：TCP 粘包、线程模型与 HTTP(S)</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/09-%E7%BD%91%E7%BB%9C%E6%9C%8D%E5%8A%A1%E5%9F%BA%E7%A1%80-tcp%E7%B2%98%E5%8C%85-%E7%BA%BF%E7%A8%8B%E6%A8%A1%E5%9E%8B%E4%B8%8Ehttp-s/</url>
    <content><![CDATA[<h1 id="网络服务基础：TCP-粘包、线程模型与-HTTP-S"><a href="#网络服务基础：TCP-粘包、线程模型与-HTTP-S" class="headerlink" title="网络服务基础：TCP 粘包、线程模型与 HTTP(S)"></a>网络服务基础：TCP 粘包、线程模型与 HTTP(S)</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：TCP stream、粘包拆包、长度前缀、分隔符、线程池、epoll、TLS、wrk<br>核心目标：建立服务端编程的基本工程直觉，先把“消息边界、并发模型、协议分层”分清楚。</p>
</blockquote>
<hr>
<h2 id="1-TCP-为什么会有“粘包”问题"><a href="#1-TCP-为什么会有“粘包”问题" class="headerlink" title="1. TCP 为什么会有“粘包”问题"></a>1. TCP 为什么会有“粘包”问题</h2><p>因为 TCP 是<strong>字节流</strong>，不是消息流。</p>
<p>这意味着：</p>
<ul>
<li>发送端发了两次 <code>send</code></li>
<li>接收端不一定就对应收到两次 <code>recv</code></li>
</ul>
<p>接收端看到的只是连续字节流，所以必须自己定义消息边界。</p>
<hr>
<h2 id="2-两种最常见的拆包方案"><a href="#2-两种最常见的拆包方案" class="headerlink" title="2. 两种最常见的拆包方案"></a>2. 两种最常见的拆包方案</h2><h3 id="2-1-长度前缀"><a href="#2-1-长度前缀" class="headerlink" title="2.1 长度前缀"></a>2.1 长度前缀</h3><p>格式示例：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[4字节长度][payload]</span><br></pre></td></tr></table></figure>

<p>接收端流程：</p>
<ol>
<li>先读够头部</li>
<li>解析长度</li>
<li>再继续读够 payload</li>
</ol>
<p>这通常是最通用、最可靠的做法。</p>
<h3 id="2-2-分隔符协议"><a href="#2-2-分隔符协议" class="headerlink" title="2.2 分隔符协议"></a>2.2 分隔符协议</h3><p>例如按 <code>\n</code> 分隔：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">hello\nworld\n</span><br></pre></td></tr></table></figure>

<p>优点：</p>
<ul>
<li>简单直观</li>
</ul>
<p>缺点：</p>
<ul>
<li>payload 中若可能出现分隔符，需要转义或编码</li>
</ul>
<hr>
<h2 id="3-长度前缀接收缓冲的核心思路"><a href="#3-长度前缀接收缓冲的核心思路" class="headerlink" title="3. 长度前缀接收缓冲的核心思路"></a>3. 长度前缀接收缓冲的核心思路</h2><p>接收端通常需要一个累积缓冲区：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;std::<span class="type">uint8_t</span>&gt; inbuf;</span><br></pre></td></tr></table></figure>

<p>每次 <code>recv</code> 到数据后：</p>
<ul>
<li>先追加到 <code>inbuf</code></li>
<li>再循环解析完整包</li>
</ul>
<p>关键不是“每次只解析一个包”，而是：</p>
<ul>
<li>一次 <code>recv</code> 可能带来 0.5 个包、1 个包、或者多个包</li>
</ul>
<hr>
<h2 id="4-为什么不能假设一次-recv-就是一条完整消息"><a href="#4-为什么不能假设一次-recv-就是一条完整消息" class="headerlink" title="4. 为什么不能假设一次 recv 就是一条完整消息"></a>4. 为什么不能假设一次 <code>recv</code> 就是一条完整消息</h2><p>因为可能出现：</p>
<ul>
<li>半包</li>
<li>多包合并</li>
<li>多次拆散</li>
</ul>
<p>所以网络编程第一条纪律就是：</p>
<blockquote>
<p>任何协议都必须先定义并实现消息 framing。</p>
</blockquote>
<hr>
<h2 id="5-线程模型的几种常见选择"><a href="#5-线程模型的几种常见选择" class="headerlink" title="5. 线程模型的几种常见选择"></a>5. 线程模型的几种常见选择</h2><h3 id="5-1-每连接一个线程"><a href="#5-1-每连接一个线程" class="headerlink" title="5.1 每连接一个线程"></a>5.1 每连接一个线程</h3><p>优点：</p>
<ul>
<li>思维简单</li>
</ul>
<p>缺点：</p>
<ul>
<li>连接数一大就撑不住</li>
</ul>
<h3 id="5-2-单线程事件循环"><a href="#5-2-单线程事件循环" class="headerlink" title="5.2 单线程事件循环"></a>5.2 单线程事件循环</h3><p>优点：</p>
<ul>
<li>资源利用率高</li>
</ul>
<p>缺点：</p>
<ul>
<li>业务阻塞会拖住整个 loop</li>
</ul>
<h3 id="5-3-Reactor-线程池"><a href="#5-3-Reactor-线程池" class="headerlink" title="5.3 Reactor + 线程池"></a>5.3 Reactor + 线程池</h3><p>常见工程做法：</p>
<ul>
<li>I&#x2F;O 线程负责收发和事件分发</li>
<li>工作线程池处理业务</li>
</ul>
<p>这样可以同时兼顾：</p>
<ul>
<li>网络扩展性</li>
<li>业务并发</li>
</ul>
<hr>
<h2 id="6-epoll-kqueue-IOCP-在解决什么问题"><a href="#6-epoll-kqueue-IOCP-在解决什么问题" class="headerlink" title="6. epoll &#x2F; kqueue &#x2F; IOCP&#96; 在解决什么问题"></a>6. <code>epoll</code> &#x2F; <code>kqueue</code> &#x2F; IOCP&#96; 在解决什么问题</h2><p>这些机制本质上都在解决：</p>
<ul>
<li>如何高效等待大量连接上的 I&#x2F;O 事件</li>
</ul>
<p>Linux 上最常见的是：</p>
<ul>
<li><code>epoll</code></li>
</ul>
<p>它不是协议，也不是线程池，而是：</p>
<ul>
<li>一个事件通知机制</li>
</ul>
<hr>
<h2 id="7-HTTP-和-HTTPS-不要混成一层"><a href="#7-HTTP-和-HTTPS-不要混成一层" class="headerlink" title="7. HTTP 和 HTTPS 不要混成一层"></a>7. HTTP 和 HTTPS 不要混成一层</h2><h3 id="7-1-HTTP"><a href="#7-1-HTTP" class="headerlink" title="7.1 HTTP"></a>7.1 HTTP</h3><p>应用层协议，定义：</p>
<ul>
<li>请求行</li>
<li>头部</li>
<li>body</li>
</ul>
<h3 id="7-2-HTTPS"><a href="#7-2-HTTPS" class="headerlink" title="7.2 HTTPS"></a>7.2 HTTPS</h3><p>可以粗略理解成：</p>
<ul>
<li>HTTP over TLS</li>
</ul>
<p>也就是：</p>
<ul>
<li>先做 TLS 加密通道</li>
<li>再在其上跑 HTTP</li>
</ul>
<p>所以 HTTPS 的复杂度不仅来自 HTTP，还来自：</p>
<ul>
<li>握手</li>
<li>证书</li>
<li>加解密</li>
</ul>
<hr>
<h2 id="8-一个服务端最基础的工程分层"><a href="#8-一个服务端最基础的工程分层" class="headerlink" title="8. 一个服务端最基础的工程分层"></a>8. 一个服务端最基础的工程分层</h2><p>可以先按这几层理解：</p>
<ol>
<li>socket 层</li>
<li>缓冲区与 framing 层</li>
<li>协议解析层</li>
<li>业务处理层</li>
<li>线程模型 &#x2F; 调度层</li>
</ol>
<p>很多初学者的问题是把所有逻辑都揉进一次 <code>recv</code> 回调里，后期几乎无法维护。</p>
<hr>
<h2 id="9-线程池在网络服务里的角色"><a href="#9-线程池在网络服务里的角色" class="headerlink" title="9. 线程池在网络服务里的角色"></a>9. 线程池在网络服务里的角色</h2><p>网络服务中，线程池通常不应该负责：</p>
<ul>
<li>直接阻塞式读写所有 socket</li>
</ul>
<p>更常见的是负责：</p>
<ul>
<li>处理业务任务</li>
<li>数据库访问</li>
<li>CPU 密集计算</li>
<li>日志&#x2F;异步处理</li>
</ul>
<p>也就是说：</p>
<ul>
<li>I&#x2F;O 线程负责“把事情收进来”</li>
<li>线程池负责“把事情做完”</li>
</ul>
<hr>
<h2 id="10-TLS-HTTPS-的最小认知"><a href="#10-TLS-HTTPS-的最小认知" class="headerlink" title="10. TLS &#x2F; HTTPS 的最小认知"></a>10. TLS &#x2F; HTTPS 的最小认知</h2><p>只要记住这几点就够做入门框架理解：</p>
<ul>
<li>TLS 负责加密通道</li>
<li>证书用于身份认证</li>
<li>HTTPS 不是“新的 HTTP 消息格式”，而是多了一层安全传输</li>
</ul>
<p>工程上常见做法是：</p>
<ul>
<li>直接用成熟库处理 TLS</li>
<li>不自己从零实现加密协议</li>
</ul>
<hr>
<h2 id="11-压测为什么重要"><a href="#11-压测为什么重要" class="headerlink" title="11. 压测为什么重要"></a>11. 压测为什么重要</h2><p>服务端“能跑”不等于“能扛”。<br>至少要关注：</p>
<ul>
<li>QPS</li>
<li>延迟</li>
<li>P99</li>
<li>错误率</li>
<li>CPU &#x2F; 内存占用</li>
</ul>
<p><code>wrk</code> 是一个常用 HTTP 压测工具，适合快速做吞吐和延迟观察。</p>
<hr>
<h2 id="12-常见坑"><a href="#12-常见坑" class="headerlink" title="12. 常见坑"></a>12. 常见坑</h2><h3 id="12-1-把-TCP-当消息协议"><a href="#12-1-把-TCP-当消息协议" class="headerlink" title="12.1 把 TCP 当消息协议"></a>12.1 把 TCP 当消息协议</h3><p>这会直接导致粘包拆包错误。</p>
<h3 id="12-2-收到数据就立刻假设包完整"><a href="#12-2-收到数据就立刻假设包完整" class="headerlink" title="12.2 收到数据就立刻假设包完整"></a>12.2 收到数据就立刻假设包完整</h3><p>半包是常态，不是例外。</p>
<h3 id="12-3-网络线程直接做重业务"><a href="#12-3-网络线程直接做重业务" class="headerlink" title="12.3 网络线程直接做重业务"></a>12.3 网络线程直接做重业务</h3><p>会拖慢整个事件循环。</p>
<h3 id="12-4-自己手写-TLS-协议栈"><a href="#12-4-自己手写-TLS-协议栈" class="headerlink" title="12.4 自己手写 TLS 协议栈"></a>12.4 自己手写 TLS 协议栈</h3><p>不现实，也没必要。</p>
<hr>
<h2 id="13-一页总结"><a href="#13-一页总结" class="headerlink" title="13. 一页总结"></a>13. 一页总结</h2><p>服务端编程最重要的理解链是：</p>
<ol>
<li>TCP 只有字节流，没有消息边界</li>
<li>所以必须先做 framing</li>
<li>高并发服务通常要把 I&#x2F;O 与业务处理分层</li>
<li>HTTPS &#x3D; HTTP + TLS，不是单纯“更安全的 socket”</li>
</ol>
<p>如果只记一句：</p>
<blockquote>
<p>网络服务首先是“协议边界和并发模型”问题，其次才是 API 调用问题。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>ranges 与 views</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/12-ranges%E4%B8%8Eviews/</url>
    <content><![CDATA[<h1 id="ranges-与-views"><a href="#ranges-与-views" class="headerlink" title="ranges 与 views"></a>ranges 与 views</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：<code>std::ranges</code>、<code>std::views</code>、惰性求值、管道风格、projection、view、dangling<br>核心目标：理解 ranges 为什么不是“语法糖”，以及 views 在工程里到底解决了什么问题。</p>
</blockquote>
<hr>
<h2 id="1-为什么会有-ranges"><a href="#1-为什么会有-ranges" class="headerlink" title="1. 为什么会有 ranges"></a>1. 为什么会有 ranges</h2><p>传统 STL 算法常见写法是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::<span class="built_in">sort</span>(v.<span class="built_in">begin</span>(), v.<span class="built_in">end</span>());</span><br><span class="line"><span class="keyword">auto</span> it = std::<span class="built_in">find_if</span>(v.<span class="built_in">begin</span>(), v.<span class="built_in">end</span>(), pred);</span><br></pre></td></tr></table></figure>

<p>它的问题不是不能用，而是：</p>
<ul>
<li><code>begin/end</code> 很机械</li>
<li>容器、区间、子区间表达不统一</li>
<li>组合多步处理时可读性一般</li>
</ul>
<p><code>std::ranges</code> 的目标是：</p>
<ul>
<li>直接面向“区间”编程</li>
<li>让算法和数据视图更自然地组合</li>
</ul>
<hr>
<h2 id="2-什么是-range"><a href="#2-什么是-range" class="headerlink" title="2. 什么是 range"></a>2. 什么是 range</h2><p>可以先粗略理解成：</p>
<blockquote>
<p>一个可以拿到 <code>begin</code> 和 <code>end</code> 的可遍历对象。</p>
</blockquote>
<p>例如：</p>
<ul>
<li><code>std::vector</code></li>
<li><code>std::array</code></li>
<li><code>std::string</code></li>
<li>某些 view</li>
</ul>
<p>所以 <code>ranges</code> 的核心不是新容器，而是：</p>
<ul>
<li>一套更统一的区间抽象</li>
</ul>
<hr>
<h2 id="3-ranges-算法和传统算法的区别"><a href="#3-ranges-算法和传统算法的区别" class="headerlink" title="3. ranges 算法和传统算法的区别"></a>3. ranges 算法和传统算法的区别</h2><p>传统写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::<span class="built_in">sort</span>(v.<span class="built_in">begin</span>(), v.<span class="built_in">end</span>());</span><br></pre></td></tr></table></figure>

<p>ranges 写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::ranges::<span class="built_in">sort</span>(v);</span><br></pre></td></tr></table></figure>

<p>优点：</p>
<ul>
<li>少写重复样板</li>
<li>更容易配合子区间和 view</li>
<li>接口更贴近“处理一段范围”这件事</li>
</ul>
<hr>
<h2 id="4-views-是什么"><a href="#4-views-是什么" class="headerlink" title="4. views 是什么"></a>4. views 是什么</h2><p><code>view</code> 可以先理解成：</p>
<blockquote>
<p>一个轻量、通常不拥有数据、按需计算的区间视图。</p>
</blockquote>
<p>它最重要的特性通常是：</p>
<ul>
<li>不拷贝底层数据</li>
<li>惰性求值</li>
<li>可组合</li>
</ul>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> even = v | std::views::<span class="built_in">filter</span>([](<span class="type">int</span> x) &#123; <span class="keyword">return</span> x % <span class="number">2</span> == <span class="number">0</span>; &#125;);</span><br></pre></td></tr></table></figure>

<p>这里并没有立刻生成一个新容器。</p>
<hr>
<h2 id="5-为什么-views-很有价值"><a href="#5-为什么-views-很有价值" class="headerlink" title="5. 为什么 views 很有价值"></a>5. 为什么 views 很有价值</h2><p>如果没有 views，很多处理中间会写成：</p>
<ul>
<li>先过滤到一个新 vector</li>
<li>再 transform 到另一个新 vector</li>
<li>再截取前几个元素</li>
</ul>
<p>这样的问题是：</p>
<ul>
<li>中间容器多</li>
<li>拷贝和分配多</li>
<li>代码意图被“存中间结果”打断</li>
</ul>
<p>views 的思路是：</p>
<ul>
<li>先把处理流程串起来</li>
<li>真正遍历时再逐步应用</li>
</ul>
<hr>
<h2 id="6-最常见的-view-适配器"><a href="#6-最常见的-view-适配器" class="headerlink" title="6. 最常见的 view 适配器"></a>6. 最常见的 view 适配器</h2><h3 id="6-1-filter"><a href="#6-1-filter" class="headerlink" title="6.1 filter"></a>6.1 <code>filter</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> even = v | std::views::<span class="built_in">filter</span>([](<span class="type">int</span> x) &#123; <span class="keyword">return</span> x % <span class="number">2</span> == <span class="number">0</span>; &#125;);</span><br></pre></td></tr></table></figure>

<h3 id="6-2-transform"><a href="#6-2-transform" class="headerlink" title="6.2 transform"></a>6.2 <code>transform</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> sq = v | std::views::<span class="built_in">transform</span>([](<span class="type">int</span> x) &#123; <span class="keyword">return</span> x * x; &#125;);</span><br></pre></td></tr></table></figure>

<h3 id="6-3-take"><a href="#6-3-take" class="headerlink" title="6.3 take"></a>6.3 <code>take</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> first3 = v | std::views::<span class="built_in">take</span>(<span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<h3 id="6-4-drop"><a href="#6-4-drop" class="headerlink" title="6.4 drop"></a>6.4 <code>drop</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> tail = v | std::views::<span class="built_in">drop</span>(<span class="number">5</span>);</span><br></pre></td></tr></table></figure>

<h3 id="6-5-keys-values"><a href="#6-5-keys-values" class="headerlink" title="6.5 keys &#x2F; values"></a>6.5 <code>keys</code> &#x2F; <code>values</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> ks = mp | std::views::keys;</span><br><span class="line"><span class="keyword">auto</span> vs = mp | std::views::values;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="7-管道风格最直观的例子"><a href="#7-管道风格最直观的例子" class="headerlink" title="7. 管道风格最直观的例子"></a>7. 管道风格最直观的例子</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;ranges&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::vector&lt;<span class="type">int</span>&gt; v = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> result = v</span><br><span class="line">        | std::views::<span class="built_in">filter</span>([](<span class="type">int</span> x) &#123; <span class="keyword">return</span> x % <span class="number">2</span> == <span class="number">0</span>; &#125;)</span><br><span class="line">        | std::views::<span class="built_in">transform</span>([](<span class="type">int</span> x) &#123; <span class="keyword">return</span> x * x; &#125;)</span><br><span class="line">        | std::views::<span class="built_in">take</span>(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> x : result) &#123;</span><br><span class="line">        std::cout &lt;&lt; x &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="8-views-的一个关键点：惰性"><a href="#8-views-的一个关键点：惰性" class="headerlink" title="8. views 的一个关键点：惰性"></a>8. views 的一个关键点：惰性</h2><p>下面这句：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> result = v | std::views::<span class="built_in">filter</span>(pred);</span><br></pre></td></tr></table></figure>

<p>通常不会立刻把所有元素筛一遍。<br>真正发生计算，往往是在你：</p>
<ul>
<li>遍历它</li>
<li>构造新容器</li>
<li>调用需要实际消费元素的算法</li>
</ul>
<p>所以 <code>view</code> 更像“处理规则的组合”，不是立即产出的结果集。</p>
<hr>
<h2 id="9-view-和容器的区别"><a href="#9-view-和容器的区别" class="headerlink" title="9. view 和容器的区别"></a>9. view 和容器的区别</h2><p>容器更像：</p>
<ul>
<li>真正拥有数据</li>
<li>独立存储结果</li>
</ul>
<p>view 更像：</p>
<ul>
<li>一层观察或变换</li>
<li>常常依赖底层对象继续存在</li>
</ul>
<p>这一点直接影响生命周期安全。</p>
<hr>
<h2 id="10-什么时候需要把-view-落地成容器"><a href="#10-什么时候需要把-view-落地成容器" class="headerlink" title="10. 什么时候需要把 view 落地成容器"></a>10. 什么时候需要把 view 落地成容器</h2><p>如果你需要：</p>
<ul>
<li>持久保存结果</li>
<li>随机访问结果</li>
<li>与不支持 ranges 的旧接口交互</li>
</ul>
<p>就需要把 view materialize 成容器。</p>
<p>常见方式：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::vector&lt;<span class="type">int</span>&gt; <span class="title">out</span><span class="params">(std::ranges::begin(view), std::ranges::end(view))</span></span>;</span><br></pre></td></tr></table></figure>

<p>如果是 C++23，还常见：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> out = view | std::ranges::<span class="built_in">to</span>&lt;std::vector&gt;();</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="11-projection：ranges-里非常实用但经常被忽略的点"><a href="#11-projection：ranges-里非常实用但经常被忽略的点" class="headerlink" title="11. projection：ranges 里非常实用但经常被忽略的点"></a>11. <code>projection</code>：ranges 里非常实用但经常被忽略的点</h2><p>很多 ranges 算法支持 projection。<br>意思是：</p>
<ul>
<li>比较或匹配前，先对元素投影出某个字段</li>
</ul>
<p>例如按成员排序：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">User</span> &#123;</span><br><span class="line">    <span class="type">int</span> id;</span><br><span class="line">    std::string name;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">std::ranges::<span class="built_in">sort</span>(users, &#123;&#125;, &amp;User::id);</span><br></pre></td></tr></table></figure>

<p>这在工程里很实用。</p>
<hr>
<h2 id="12-常见算法示例"><a href="#12-常见算法示例" class="headerlink" title="12. 常见算法示例"></a>12. 常见算法示例</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> it = std::ranges::<span class="built_in">find</span>(v, <span class="number">42</span>);</span><br><span class="line">std::ranges::<span class="built_in">sort</span>(v);</span><br><span class="line"><span class="type">bool</span> ok = std::ranges::<span class="built_in">all_of</span>(v, pred);</span><br><span class="line">std::ranges::<span class="built_in">copy</span>(v, std::<span class="built_in">back_inserter</span>(out));</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="13-生命周期问题：views-最大的坑之一"><a href="#13-生命周期问题：views-最大的坑之一" class="headerlink" title="13. 生命周期问题：views 最大的坑之一"></a>13. 生命周期问题：views 最大的坑之一</h2><p>因为很多 view 不拥有数据，所以要小心底层对象生命周期。</p>
<p>危险例子：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">make_view</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::vector&lt;<span class="type">int</span>&gt; v = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;</span><br><span class="line">    <span class="keyword">return</span> v | std::views::<span class="built_in">filter</span>([](<span class="type">int</span> x) &#123; <span class="keyword">return</span> x &gt; <span class="number">1</span>; &#125;); <span class="comment">// 危险</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>所以要记住：</p>
<ul>
<li>view 很轻，但通常不负责延长底层容器生命周期</li>
</ul>
<hr>
<h2 id="14-常见坑"><a href="#14-常见坑" class="headerlink" title="14. 常见坑"></a>14. 常见坑</h2><h3 id="14-1-把-view-当拥有结果的容器"><a href="#14-1-把-view-当拥有结果的容器" class="headerlink" title="14.1 把 view 当拥有结果的容器"></a>14.1 把 view 当拥有结果的容器</h3><p>它通常不是。</p>
<h3 id="14-2-底层容器变了，view-却还在继续用"><a href="#14-2-底层容器变了，view-却还在继续用" class="headerlink" title="14.2 底层容器变了，view 却还在继续用"></a>14.2 底层容器变了，view 却还在继续用</h3><p>例如容器被销毁、扩容、失效。</p>
<h3 id="14-3-pipeline-过长且带副作用"><a href="#14-3-pipeline-过长且带副作用" class="headerlink" title="14.3 pipeline 过长且带副作用"></a>14.3 pipeline 过长且带副作用</h3><p>会让调试和推理变难。</p>
<h3 id="14-4-误以为-ranges-一定更快"><a href="#14-4-误以为-ranges-一定更快" class="headerlink" title="14.4 误以为 ranges 一定更快"></a>14.4 误以为 ranges 一定更快</h3><p>语义更清晰不代表每个场景都自动最优。</p>
<hr>
<h2 id="15-一页总结"><a href="#15-一页总结" class="headerlink" title="15. 一页总结"></a>15. 一页总结</h2><p>ranges 与 views 最重要的理解链是：</p>
<ol>
<li>ranges 让算法直接面向区间</li>
<li>views 提供不拥有数据、可组合、惰性的处理视图</li>
<li>它们最擅长表达“数据处理流水线”</li>
<li>真正要注意的是生命周期、materialize 时机和可读性边界</li>
</ol>
<p>如果只记一句：</p>
<blockquote>
<p>view 更像“处理规则”，容器才是“真正结果”。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>错误处理与 `expected`、异常设计</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/13-%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86%E4%B8%8Eexpected-%E5%BC%82%E5%B8%B8%E8%AE%BE%E8%AE%A1/</url>
    <content><![CDATA[<h1 id="错误处理与-expected、异常设计"><a href="#错误处理与-expected、异常设计" class="headerlink" title="错误处理与 expected、异常设计"></a>错误处理与 <code>expected</code>、异常设计</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：异常、<code>noexcept</code>、<code>std::expected</code>、<code>optional</code>、错误传播、恢复性错误、编程错误<br>核心目标：建立一套工程上可执行的判断标准，知道什么时候该抛异常，什么时候该返回错误值或 <code>expected</code>。</p>
</blockquote>
<hr>
<h2 id="1-错误处理不是“选一个-API”那么简单"><a href="#1-错误处理不是“选一个-API”那么简单" class="headerlink" title="1. 错误处理不是“选一个 API”那么简单"></a>1. 错误处理不是“选一个 API”那么简单</h2><p>错误处理真正要先回答的是：</p>
<ul>
<li>这是不是预期内会发生的失败</li>
<li>调用方是否应该恢复</li>
<li>失败信息需要多详细</li>
<li>代码库是否接受异常</li>
</ul>
<p>所以讨论异常和 <code>expected</code> 时，重点不是站队，而是：</p>
<ul>
<li>哪种语义更适合这一层接口</li>
</ul>
<hr>
<h2 id="2-先把失败分类型"><a href="#2-先把失败分类型" class="headerlink" title="2. 先把失败分类型"></a>2. 先把失败分类型</h2><p>最有用的分类通常是：</p>
<h3 id="2-1-编程错误-违反前置条件"><a href="#2-1-编程错误-违反前置条件" class="headerlink" title="2.1 编程错误 &#x2F; 违反前置条件"></a>2.1 编程错误 &#x2F; 违反前置条件</h3><p>例如：</p>
<ul>
<li>越界</li>
<li>非法状态</li>
<li>不满足接口约束</li>
</ul>
<p>这类错误通常不属于“正常业务失败”。</p>
<h3 id="2-2-可恢复的业务失败"><a href="#2-2-可恢复的业务失败" class="headerlink" title="2.2 可恢复的业务失败"></a>2.2 可恢复的业务失败</h3><p>例如：</p>
<ul>
<li>解析失败</li>
<li>文件不存在</li>
<li>权限不足</li>
<li>网络超时</li>
</ul>
<p>调用方通常有机会决定下一步怎么做。</p>
<h3 id="2-3-致命错误"><a href="#2-3-致命错误" class="headerlink" title="2.3 致命错误"></a>2.3 致命错误</h3><p>例如：</p>
<ul>
<li>系统资源耗尽</li>
<li>程序已进入不一致状态</li>
</ul>
<hr>
<h2 id="3-异常适合什么场景"><a href="#3-异常适合什么场景" class="headerlink" title="3. 异常适合什么场景"></a>3. 异常适合什么场景</h2><p>异常最适合：</p>
<ul>
<li>错误很少发生</li>
<li>一旦发生，需要沿调用栈自动展开</li>
<li>局部函数不适合层层手动返回错误码</li>
</ul>
<p>典型例子：</p>
<ul>
<li>构造函数失败</li>
<li>资源获取失败</li>
<li>深层调用链中的异常退出</li>
</ul>
<hr>
<h2 id="4-expected-适合什么场景"><a href="#4-expected-适合什么场景" class="headerlink" title="4. expected 适合什么场景"></a>4. <code>expected</code> 适合什么场景</h2><p><code>std::expected&lt;T, E&gt;</code> 更适合：</p>
<ul>
<li>失败是正常、可预期分支</li>
<li>调用方需要显式处理失败</li>
<li>你希望错误成为接口类型的一部分</li>
</ul>
<p>例如：</p>
<ul>
<li>配置解析</li>
<li>用户输入校验</li>
<li>业务规则检查</li>
<li>网络协议解析</li>
</ul>
<hr>
<h2 id="5-optional-和-expected-的区别"><a href="#5-optional-和-expected-的区别" class="headerlink" title="5. optional 和 expected 的区别"></a>5. <code>optional</code> 和 <code>expected</code> 的区别</h2><p><code>optional&lt;T&gt;</code> 表示：</p>
<ul>
<li>可能有值，也可能没值</li>
</ul>
<p>但它不告诉你：</p>
<ul>
<li>为什么没值</li>
</ul>
<p><code>expected&lt;T, E&gt;</code> 表示：</p>
<ul>
<li>要么有 <code>T</code></li>
<li>要么有错误 <code>E</code></li>
</ul>
<p>所以经验上：</p>
<ul>
<li>只有“有没有结果”时，用 <code>optional</code></li>
<li>需要表达“为什么失败”时，用 <code>expected</code></li>
</ul>
<hr>
<h2 id="6-一个最直接的例子"><a href="#6-一个最直接的例子" class="headerlink" title="6. 一个最直接的例子"></a>6. 一个最直接的例子</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::optional&lt;User&gt; <span class="title">find_user</span><span class="params">(<span class="type">int</span> id)</span></span>;</span><br><span class="line"><span class="function">std::expected&lt;User, ParseError&gt; <span class="title">parse_user</span><span class="params">(std::string_view text)</span></span>;</span><br></pre></td></tr></table></figure>

<p>前者表达“可能没有”，后者表达“失败且要知道原因”。</p>
<hr>
<h2 id="7-什么时候不适合抛异常"><a href="#7-什么时候不适合抛异常" class="headerlink" title="7. 什么时候不适合抛异常"></a>7. 什么时候不适合抛异常</h2><h3 id="7-1-失败是高频正常分支"><a href="#7-1-失败是高频正常分支" class="headerlink" title="7.1 失败是高频正常分支"></a>7.1 失败是高频正常分支</h3><h3 id="7-2-热路径里非常在意开销和可预测性"><a href="#7-2-热路径里非常在意开销和可预测性" class="headerlink" title="7.2 热路径里非常在意开销和可预测性"></a>7.2 热路径里非常在意开销和可预测性</h3><h3 id="7-3-跨-ABI-跨模块边界不方便统一异常策略"><a href="#7-3-跨-ABI-跨模块边界不方便统一异常策略" class="headerlink" title="7.3 跨 ABI &#x2F; 跨模块边界不方便统一异常策略"></a>7.3 跨 ABI &#x2F; 跨模块边界不方便统一异常策略</h3><h3 id="7-4-团队整体约束就是禁用异常"><a href="#7-4-团队整体约束就是禁用异常" class="headerlink" title="7.4 团队整体约束就是禁用异常"></a>7.4 团队整体约束就是禁用异常</h3><p>这时更适合：</p>
<ul>
<li><code>expected</code></li>
<li>error code</li>
<li>status object</li>
</ul>
<hr>
<h2 id="8-什么时候异常特别合理"><a href="#8-什么时候异常特别合理" class="headerlink" title="8. 什么时候异常特别合理"></a>8. 什么时候异常特别合理</h2><h3 id="8-1-构造函数失败"><a href="#8-1-构造函数失败" class="headerlink" title="8.1 构造函数失败"></a>8.1 构造函数失败</h3><h3 id="8-2-无法在每层都写样板检查"><a href="#8-2-无法在每层都写样板检查" class="headerlink" title="8.2 无法在每层都写样板检查"></a>8.2 无法在每层都写样板检查</h3><h3 id="8-3-资源清理由-RAII-承担"><a href="#8-3-资源清理由-RAII-承担" class="headerlink" title="8.3 资源清理由 RAII 承担"></a>8.3 资源清理由 RAII 承担</h3><p>异常和 RAII 配合得最好：</p>
<ul>
<li>抛出异常</li>
<li>栈展开</li>
<li>局部资源自动释放</li>
</ul>
<hr>
<h2 id="9-一个-expected-风格的例子"><a href="#9-一个-expected-风格的例子" class="headerlink" title="9. 一个 expected 风格的例子"></a>9. 一个 <code>expected</code> 风格的例子</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;expected&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string_view&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">enum class</span> <span class="title class_">ParseError</span> &#123;</span><br><span class="line">    Empty,</span><br><span class="line">    InvalidNumber</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">std::expected&lt;<span class="type">int</span>, ParseError&gt; <span class="title">parse_int</span><span class="params">(std::string_view s)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (s.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">        <span class="keyword">return</span> std::<span class="built_in">unexpected</span>(ParseError::Empty);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> value = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">char</span> ch : s) &#123;</span><br><span class="line">        <span class="keyword">if</span> (ch &lt; <span class="string">&#x27;0&#x27;</span> || ch &gt; <span class="string">&#x27;9&#x27;</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> std::<span class="built_in">unexpected</span>(ParseError::InvalidNumber);</span><br><span class="line">        &#125;</span><br><span class="line">        value = value * <span class="number">10</span> + (ch - <span class="string">&#x27;0&#x27;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> value;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="10-异常风格的例子"><a href="#10-异常风格的例子" class="headerlink" title="10. 异常风格的例子"></a>10. 异常风格的例子</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">parse_int_or_throw</span><span class="params">(std::string_view s)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (s.<span class="built_in">empty</span>()) <span class="keyword">throw</span> std::<span class="built_in">invalid_argument</span>(<span class="string">&quot;empty&quot;</span>);</span><br><span class="line">    <span class="type">int</span> value = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">char</span> ch : s) &#123;</span><br><span class="line">        <span class="keyword">if</span> (ch &lt; <span class="string">&#x27;0&#x27;</span> || ch &gt; <span class="string">&#x27;9&#x27;</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> std::<span class="built_in">invalid_argument</span>(<span class="string">&quot;invalid number&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        value = value * <span class="number">10</span> + (ch - <span class="string">&#x27;0&#x27;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> value;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="11-一层系统里最好统一风格"><a href="#11-一层系统里最好统一风格" class="headerlink" title="11. 一层系统里最好统一风格"></a>11. 一层系统里最好统一风格</h2><p>最容易出问题的是混乱：</p>
<ul>
<li>一半函数抛异常</li>
<li>一半函数返回错误码</li>
<li>一半函数返回空值</li>
</ul>
<p>更稳妥的做法是：</p>
<ul>
<li>每一层接口尽量有统一错误处理约定</li>
</ul>
<hr>
<h2 id="12-noexcept-的意义"><a href="#12-noexcept-的意义" class="headerlink" title="12. noexcept 的意义"></a>12. <code>noexcept</code> 的意义</h2><p><code>noexcept</code> 表示：</p>
<ul>
<li>这个函数承诺不抛异常</li>
</ul>
<p>它既是语义约束，也会影响某些容器对移动操作的选择。</p>
<hr>
<h2 id="13-析构函数为什么通常不能抛"><a href="#13-析构函数为什么通常不能抛" class="headerlink" title="13. 析构函数为什么通常不能抛"></a>13. 析构函数为什么通常不能抛</h2><p>析构阶段如果异常继续外逃，尤其在栈展开过程中，会很危险。<br>工程上通常遵循：</p>
<ul>
<li>析构函数不要让异常逃出</li>
</ul>
<hr>
<h2 id="14-一个很实用的决策表"><a href="#14-一个很实用的决策表" class="headerlink" title="14. 一个很实用的决策表"></a>14. 一个很实用的决策表</h2><h3 id="14-1-用异常"><a href="#14-1-用异常" class="headerlink" title="14.1 用异常"></a>14.1 用异常</h3><p>当失败：</p>
<ul>
<li>不常发生</li>
<li>不适合做常规分支</li>
<li>需要栈展开自动清理</li>
</ul>
<h3 id="14-2-用-expected"><a href="#14-2-用-expected" class="headerlink" title="14.2 用 expected"></a>14.2 用 <code>expected</code></h3><p>当失败：</p>
<ul>
<li>很常见</li>
<li>需要显式处理</li>
<li>希望错误成为接口类型的一部分</li>
</ul>
<h3 id="14-3-用-optional"><a href="#14-3-用-optional" class="headerlink" title="14.3 用 optional"></a>14.3 用 <code>optional</code></h3><p>当失败：</p>
<ul>
<li>只是“没有结果”</li>
<li>不需要详细错误原因</li>
</ul>
<hr>
<h2 id="15-常见坑"><a href="#15-常见坑" class="headerlink" title="15. 常见坑"></a>15. 常见坑</h2><h3 id="15-1-用异常做普通循环分支"><a href="#15-1-用异常做普通循环分支" class="headerlink" title="15.1 用异常做普通循环分支"></a>15.1 用异常做普通循环分支</h3><h3 id="15-2-用-optional-隐藏真实错误信息"><a href="#15-2-用-optional-隐藏真实错误信息" class="headerlink" title="15.2 用 optional 隐藏真实错误信息"></a>15.2 用 <code>optional</code> 隐藏真实错误信息</h3><h3 id="15-3-一个模块里混用三四种错误风格"><a href="#15-3-一个模块里混用三四种错误风格" class="headerlink" title="15.3 一个模块里混用三四种错误风格"></a>15.3 一个模块里混用三四种错误风格</h3><h3 id="15-4-给所有函数乱加-noexcept"><a href="#15-4-给所有函数乱加-noexcept" class="headerlink" title="15.4 给所有函数乱加 noexcept"></a>15.4 给所有函数乱加 <code>noexcept</code></h3><hr>
<h2 id="16-一页总结"><a href="#16-一页总结" class="headerlink" title="16. 一页总结"></a>16. 一页总结</h2><p>错误处理最重要的不是“异常 vs <code>expected</code> 谁更先进”，而是：</p>
<ol>
<li>先判断失败是不是正常可恢复分支</li>
<li>再决定错误是否应该进入类型系统</li>
<li>再决定是否需要异常自动展开调用栈</li>
</ol>
<p>可以直接记这条经验：</p>
<ul>
<li>不常发生、跨层传播、依赖 RAII 清理：优先考虑异常</li>
<li>常见失败、需要显式处理、想把错误写进接口：优先考虑 <code>expected</code></li>
</ul>
<p>如果只记一句：</p>
<blockquote>
<p>错误处理风格最怕的不是选错，而是同一层接口没有一致性。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>内存泄漏检测与管理</title>
    <url>//notes/%E7%8E%B0%E4%BB%A3c-%E5%AE%9E%E8%B7%B5/14-%E5%86%85%E5%AD%98%E6%B3%84%E6%BC%8F%E6%A3%80%E6%B5%8B%E4%B8%8E%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h1 id="内存泄漏检测与管理"><a href="#内存泄漏检测与管理" class="headerlink" title="内存泄漏检测与管理"></a>内存泄漏检测与管理</h1><p>时间：2026&#x2F;04&#x2F;16</p>
<blockquote>
<p>关键词：RAII、<code>unique_ptr</code>、<code>shared_ptr</code> 循环引用、Sanitizer、Valgrind、资源封装<br>核心目标：把“谁释放、何时释放、怎么定位泄漏”变成工程上可检查的规则。</p>
</blockquote>
<hr>
<h2 id="1-什么才叫内存泄漏"><a href="#1-什么才叫内存泄漏" class="headerlink" title="1. 什么才叫内存泄漏"></a>1. 什么才叫内存泄漏</h2><p>最典型的内存泄漏是：</p>
<ul>
<li>一块堆内存已经没有任何有效路径再访问它</li>
<li>但它也永远不会被释放</li>
</ul>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">bad</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span>* p = <span class="keyword">new</span> <span class="built_in">int</span>(<span class="number">42</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>函数结束后，<code>p</code> 没了，这块内存也没人能再 <code>delete</code>。<br>这就是最标准的泄漏。</p>
<p>但工程里还要区分另一类问题：</p>
<ul>
<li>对象严格来说还“可达”</li>
<li>但长期不释放，内存占用持续上涨</li>
</ul>
<p>这未必是严格意义上的 leak，但同样会把服务拖垮。</p>
<hr>
<h2 id="2-最常见的泄漏来源"><a href="#2-最常见的泄漏来源" class="headerlink" title="2. 最常见的泄漏来源"></a>2. 最常见的泄漏来源</h2><h3 id="2-1-裸-new-delete-配对失败"><a href="#2-1-裸-new-delete-配对失败" class="headerlink" title="2.1 裸 new &#x2F; delete 配对失败"></a>2.1 裸 <code>new</code> &#x2F; <code>delete</code> 配对失败</h3><p>最常见的问题不是“不会 <code>delete</code>”，而是：</p>
<ul>
<li>提前 <code>return</code></li>
<li>中途 <code>throw</code></li>
<li>多分支路径漏掉释放</li>
</ul>
<h3 id="2-2-容器里放-owning-raw-pointer"><a href="#2-2-容器里放-owning-raw-pointer" class="headerlink" title="2.2 容器里放 owning raw pointer"></a>2.2 容器里放 owning raw pointer</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;Foo*&gt; items;</span><br><span class="line">items.<span class="built_in">push_back</span>(<span class="keyword">new</span> <span class="built_in">Foo</span>());</span><br></pre></td></tr></table></figure>

<p>这种写法会把“谁来删”变成记忆题。</p>
<h3 id="2-3-shared-ptr-循环引用"><a href="#2-3-shared-ptr-循环引用" class="headerlink" title="2.3 shared_ptr 循环引用"></a>2.3 <code>shared_ptr</code> 循环引用</h3><p>两个对象互相持有 <code>shared_ptr</code>，引用计数永远不会归零。</p>
<h3 id="2-4-C-风格资源没及时封装"><a href="#2-4-C-风格资源没及时封装" class="headerlink" title="2.4 C 风格资源没及时封装"></a>2.4 C 风格资源没及时封装</h3><p>比如：</p>
<ul>
<li><code>FILE*</code></li>
<li><code>malloc/free</code></li>
<li>socket &#x2F; fd</li>
<li>第三方库句柄</li>
</ul>
<p>如果它们在业务代码里裸奔，后面很容易漏掉释放。</p>
<hr>
<h2 id="3-第一原则：先别写出会泄漏的代码"><a href="#3-第一原则：先别写出会泄漏的代码" class="headerlink" title="3. 第一原则：先别写出会泄漏的代码"></a>3. 第一原则：先别写出会泄漏的代码</h2><p>现代 C++ 管理泄漏，重点不是“人工记得回收”，而是默认采用不容易泄漏的结构。</p>
<p>优先顺序通常是：</p>
<ol>
<li>能值语义就值语义</li>
<li>能栈对象就栈对象</li>
<li>必须动态分配时优先 <code>std::unique_ptr</code></li>
<li>确实需要共享拥有时才用 <code>std::shared_ptr</code></li>
<li>裸指针和引用默认只表达观察，不表达拥有</li>
</ol>
<p>这背后的核心思想就是 RAII：</p>
<ul>
<li>对象析构时自动释放资源</li>
</ul>
<p>只要生命周期跟对象绑在一起，泄漏风险会大幅下降。</p>
<hr>
<h2 id="4-一个典型泄漏例子"><a href="#4-一个典型泄漏例子" class="headerlink" title="4. 一个典型泄漏例子"></a>4. 一个典型泄漏例子</h2><p>错误写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Widget</span> &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">Widget* <span class="title">create_widget</span><span class="params">(<span class="type">bool</span> failed)</span> </span>&#123;</span><br><span class="line">    Widget* p = <span class="keyword">new</span> <span class="built_in">Widget</span>();</span><br><span class="line">    <span class="keyword">if</span> (failed) <span class="keyword">return</span> <span class="literal">nullptr</span>; <span class="comment">// 泄漏</span></span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>问题不在 <code>new</code> 本身，而在：</p>
<ul>
<li>释放依赖调用路径是否完整</li>
</ul>
<p>更稳妥的写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Widget</span> &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">std::unique_ptr&lt;Widget&gt; <span class="title">create_widget</span><span class="params">(<span class="type">bool</span> failed)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> p = std::<span class="built_in">make_unique</span>&lt;Widget&gt;();</span><br><span class="line">    <span class="keyword">if</span> (failed) <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样即使中途提前返回，局部对象也会自动清理。</p>
<hr>
<h2 id="5-智能指针也不是绝对安全"><a href="#5-智能指针也不是绝对安全" class="headerlink" title="5. 智能指针也不是绝对安全"></a>5. 智能指针也不是绝对安全</h2><p><code>unique_ptr</code> 很少造成泄漏，真正容易出问题的是 <code>shared_ptr</code>。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Node</span> &#123;</span><br><span class="line">    std::shared_ptr&lt;Node&gt; next;</span><br><span class="line">    std::shared_ptr&lt;Node&gt; prev; <span class="comment">// 错：可能形成环</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如果两个节点互相持有，引用计数就会卡住。</p>
<p>更常见的修正方式是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Node</span> &#123;</span><br><span class="line">    std::shared_ptr&lt;Node&gt; next;</span><br><span class="line">    std::weak_ptr&lt;Node&gt; prev; <span class="comment">// 观察，不拥有</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>经验上：</p>
<ul>
<li>形成树状、图状、双向关系时，先主动检查是否存在环</li>
<li>“回指”通常更适合 <code>weak_ptr</code></li>
</ul>
<hr>
<h2 id="6-泄漏和“内存一直涨”不是一回事"><a href="#6-泄漏和“内存一直涨”不是一回事" class="headerlink" title="6. 泄漏和“内存一直涨”不是一回事"></a>6. 泄漏和“内存一直涨”不是一回事</h2><p>下面这些情况不一定是严格意义上的 leak：</p>
<ul>
<li>全局缓存只增不减</li>
<li><code>std::vector</code> &#x2F; <code>std::string</code> 容量长期不回收</li>
<li>任务队列积压</li>
<li>对象池尺寸只扩不缩</li>
</ul>
<p>它们的问题是：</p>
<ul>
<li>生命周期策略不合理</li>
<li>上限控制缺失</li>
</ul>
<p>所以排查内存问题时，要先分清两类：</p>
<ol>
<li>对象已经不可达，但没释放</li>
<li>对象还可达，但系统把它留得太久</li>
</ol>
<p>前者更像 bug，后者更像管理问题，但两者都要处理。</p>
<hr>
<h2 id="7-怎么检测泄漏"><a href="#7-怎么检测泄漏" class="headerlink" title="7. 怎么检测泄漏"></a>7. 怎么检测泄漏</h2><h3 id="7-1-先上-Sanitizer"><a href="#7-1-先上-Sanitizer" class="headerlink" title="7.1 先上 Sanitizer"></a>7.1 先上 Sanitizer</h3><p>本地开发最常用的办法通常是编译时打开 Sanitizer：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">clang++ -std=c++20 -g -O1 -fno-omit-frame-pointer -fsanitize=address main.cpp -o app</span><br><span class="line">ASAN_OPTIONS=detect_leaks=1 ./app</span><br></pre></td></tr></table></figure>

<p>如果你的工具链把 leak 检测拆开，也可以按需使用：</p>
<ul>
<li><code>-fsanitize=leak</code></li>
</ul>
<p>它的优点是：</p>
<ul>
<li>定位快</li>
<li>栈回溯清楚</li>
<li>很适合集成到测试里</li>
</ul>
<h3 id="7-2-再用-Valgrind-看存量问题"><a href="#7-2-再用-Valgrind-看存量问题" class="headerlink" title="7.2 再用 Valgrind 看存量问题"></a>7.2 再用 Valgrind 看存量问题</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">valgrind --leak-check=full --show-leak-kinds=all ./app</span><br></pre></td></tr></table></figure>

<p>它更慢，但对一些历史代码排查仍然很有价值。</p>
<h3 id="7-3-别只测“正常退出”"><a href="#7-3-别只测“正常退出”" class="headerlink" title="7.3 别只测“正常退出”"></a>7.3 别只测“正常退出”</h3><p>很多泄漏只在这些场景出现：</p>
<ul>
<li>异常路径</li>
<li>超时取消</li>
<li>重试逻辑</li>
<li>长时间运行</li>
<li>高并发压力</li>
</ul>
<p>所以测试不能只跑正常路径。</p>
<hr>
<h2 id="8-工程里的泄漏管理清单"><a href="#8-工程里的泄漏管理清单" class="headerlink" title="8. 工程里的泄漏管理清单"></a>8. 工程里的泄漏管理清单</h2><p>真正有效的治理，通常不是靠某一个工具，而是靠几条长期规则：</p>
<ul>
<li>新代码默认不写“拥有语义的裸指针”</li>
<li>业务代码里尽量不直接出现 <code>new</code> &#x2F; <code>delete</code></li>
<li>工厂接口优先返回 <code>std::unique_ptr</code></li>
<li>容器里优先放对象或智能指针，不放“需要人工回收”的裸指针</li>
<li>第三方资源在进入系统边界时立即封装成 RAII 类型</li>
<li><code>shared_ptr</code> 关系图需要专门检查循环引用</li>
<li>测试或 CI 中固定开启 Sanitizer 版本</li>
<li>对缓存、对象池、队列设置上限，而不是默认无限增长</li>
</ul>
<p>这才叫“管理”，不是出了问题再临时抓日志。</p>
<hr>
<h2 id="9-一页总结"><a href="#9-一页总结" class="headerlink" title="9. 一页总结"></a>9. 一页总结</h2><p>最值得记住的五条：</p>
<ol>
<li>泄漏治理的核心不是“记得释放”，而是明确所有权</li>
<li>默认优先值语义、栈对象和 RAII</li>
<li>动态分配优先 <code>unique_ptr</code>，不是裸指针</li>
<li><code>shared_ptr</code> 最大的风险是循环引用</li>
<li>用 Sanitizer 和 Valgrind 查问题，不要靠肉眼猜</li>
</ol>
<p>如果只记一句：</p>
<blockquote>
<p>预防内存泄漏最有效的方法，不是手写更多 <code>delete</code>，而是让代码结构默认不需要手写 <code>delete</code>。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>现代C++实践</category>
      </categories>
  </entry>
  <entry>
    <title>高性能 C++ 并行编程导读</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/00-%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B%E5%AF%BC%E8%AF%BB/</url>
    <content><![CDATA[<h1 id="高性能-C-并行编程导读"><a href="#高性能-C-并行编程导读" class="headerlink" title="高性能 C++ 并行编程导读"></a>高性能 C++ 并行编程导读</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<p>这组笔记现在按“从语言与内存基础，到 CPU 并行，再到 GPU 与实战”的顺序整理。<br>阅读时不建议完全按时间写作顺序看，而建议按下面这条主线走。</p>
<hr>
<h2 id="1-推荐学习顺序"><a href="#1-推荐学习顺序" class="headerlink" title="1. 推荐学习顺序"></a>1. 推荐学习顺序</h2><ol>
<li><code>C 指针与内存模型</code></li>
<li><code>RAII 与智能指针</code></li>
<li><code>左右值、完美转发与引用折叠</code></li>
<li><code>模板与元编程</code></li>
<li><code>vector 容器优化</code></li>
<li><code>编译器优化与汇编视角</code></li>
<li><code>访存优化</code></li>
<li><code>C++11 多线程编程</code></li>
<li><code>原子操作、内存序与无锁基础</code></li>
<li><code>TBB 并行编程</code></li>
<li><code>CUDA 开启的 GPU 编程</code></li>
<li><code>流体仿真实战</code></li>
</ol>
<hr>
<h2 id="2-每篇笔记解决什么问题"><a href="#2-每篇笔记解决什么问题" class="headerlink" title="2. 每篇笔记解决什么问题"></a>2. 每篇笔记解决什么问题</h2><table>
<thead>
<tr>
<th>编号</th>
<th>主题</th>
<th>核心问题</th>
</tr>
</thead>
<tbody><tr>
<td><code>[01]</code></td>
<td>C 指针与内存模型</td>
<td>地址、生命周期、数组退化、别名与缓存直觉</td>
</tr>
<tr>
<td><code>[02]</code></td>
<td>RAII 与智能指针</td>
<td>资源如何自动释放，所有权如何表达</td>
</tr>
<tr>
<td><code>[03]</code></td>
<td>左右值、完美转发与引用折叠</td>
<td><code>std::move</code> &#x2F; <code>std::forward</code> 到底在做什么</td>
</tr>
<tr>
<td><code>[04]</code></td>
<td>模板与元编程</td>
<td>泛型、类型推导、<code>if constexpr</code>、traits、concepts</td>
</tr>
<tr>
<td><code>[05]</code></td>
<td>vector 容器优化</td>
<td>连续内存、扩容、失效规则、并发写入模式</td>
</tr>
<tr>
<td><code>[06]</code></td>
<td>编译器优化与汇编视角</td>
<td>编译器如何重写 C++，哪些写法更利于优化</td>
</tr>
<tr>
<td><code>[07]</code></td>
<td>访存优化</td>
<td>cache、AoS&#x2F;SoA、stride、blocking、false sharing</td>
</tr>
<tr>
<td><code>[08]</code></td>
<td>C++11 多线程编程</td>
<td><code>thread/mutex/condition_variable/future/thread pool</code></td>
</tr>
<tr>
<td><code>[09]</code></td>
<td>原子操作、内存序与无锁基础</td>
<td><code>atomic</code>、CAS、acquire&#x2F;release、<code>seq_cst</code></td>
</tr>
<tr>
<td><code>[10]</code></td>
<td>TBB 并行编程之旅</td>
<td>task、并行循环、归约、scan、arena、pipeline</td>
</tr>
<tr>
<td><code>[11]</code></td>
<td>CUDA 开启的 GPU 编程</td>
<td>kernel、内存、shared memory、stream、Thrust</td>
</tr>
<tr>
<td><code>[12]</code></td>
<td>流体仿真实战</td>
<td>把 CUDA、访存、数值迭代放到一个完整案例里</td>
</tr>
</tbody></table>
<hr>
<h2 id="3-这次整理做了什么"><a href="#3-这次整理做了什么" class="headerlink" title="3. 这次整理做了什么"></a>3. 这次整理做了什么</h2><p>这次不是只改文件名，而是做了三类整理：</p>
<ol>
<li>把空白或草稿笔记补成正式版：<br><code>C 指针</code>、<code>RAII</code>、<code>编译器优化</code>、<code>访存优化</code>、<code>流体仿真</code></li>
<li>新增了一篇缺失但关键的笔记：<br><code>原子操作、内存序与无锁基础</code></li>
<li>统一为按内容排序的 <code>[NN]xxx.md</code> 命名</li>
</ol>
<hr>
<h2 id="4-如果只想抓主干"><a href="#4-如果只想抓主干" class="headerlink" title="4. 如果只想抓主干"></a>4. 如果只想抓主干</h2><p>时间有限时，可以先看这 6 篇：</p>
<ol>
<li><code>C 指针与内存模型</code></li>
<li><code>RAII 与智能指针</code></li>
<li><code>左右值、完美转发与引用折叠</code></li>
<li><code>vector 容器优化</code></li>
<li><code>访存优化</code></li>
<li><code>C++11 多线程编程</code></li>
</ol>
<p>这几篇能先把 C++ 高性能和并行里最常见的基础坑位补齐。</p>
<hr>
<h2 id="5-接下来的自然扩展"><a href="#5-接下来的自然扩展" class="headerlink" title="5. 接下来的自然扩展"></a>5. 接下来的自然扩展</h2><p>如果后面继续补这套笔记，最值得优先新增的主题是：</p>
<ol>
<li>SIMD 与自动向量化</li>
<li>Benchmark 与性能分析方法</li>
<li>NUMA 与多路 CPU 内存访问</li>
<li><code>std::pmr</code> 与内存池</li>
<li>C++20&#x2F;23 并发工具：<code>jthread / latch / barrier / semaphore</code></li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>从计算机原理探讨 C 指针</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/01-c%E6%8C%87%E9%92%88%E4%B8%8E%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8B/</url>
    <content><![CDATA[<h1 id="从计算机原理探讨-C-指针"><a href="#从计算机原理探讨-C-指针" class="headerlink" title="从计算机原理探讨 C 指针"></a>从计算机原理探讨 C 指针</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：地址、虚拟内存、对象模型、指针运算、别名、对齐、悬空指针、缓存局部性<br>核心目标：不把指针只当“语法”，而是把它看成“对内存地址与对象布局的直接操作”。</p>
</blockquote>
<hr>
<h2 id="1-为什么-C-C-里指针如此重要"><a href="#1-为什么-C-C-里指针如此重要" class="headerlink" title="1. 为什么 C&#x2F;C++ 里指针如此重要"></a>1. 为什么 C&#x2F;C++ 里指针如此重要</h2><p>指针本质上是在保存一个地址。<br>而 C&#x2F;C++ 强大的地方就在于：程序员可以相对直接地操作内存、对象布局和资源生命周期。</p>
<p>所以指针不仅是“会不会写 <code>*p</code> 和 <code>&amp;x</code>”的问题，更关系到：</p>
<ul>
<li>数据结构如何组织</li>
<li>代码能否避免多余拷贝</li>
<li>CPU 缓存能否高效利用</li>
<li>是否会出现悬空引用、越界、未定义行为</li>
</ul>
<p>在高性能 C++ 里，指针既是能力，也是风险。</p>
<hr>
<h2 id="2-先从内存模型看地址"><a href="#2-先从内存模型看地址" class="headerlink" title="2. 先从内存模型看地址"></a>2. 先从内存模型看地址</h2><h3 id="2-1-程序眼里的内存不是“真实物理地址”"><a href="#2-1-程序眼里的内存不是“真实物理地址”" class="headerlink" title="2.1 程序眼里的内存不是“真实物理地址”"></a>2.1 程序眼里的内存不是“真实物理地址”</h3><p>现代操作系统下，程序通常工作在<strong>虚拟地址空间</strong>中。</p>
<p>可以先粗略理解成：</p>
<ul>
<li>程序拿到的是虚拟地址</li>
<li>CPU 的 MMU 会把虚拟地址映射到物理内存</li>
<li>不同进程彼此地址空间隔离</li>
</ul>
<p>这意味着：</p>
<ul>
<li>同样一个数值形式的地址，在不同进程中意义不同</li>
<li>指针能否访问成功，不只取决于值，还取决于映射和权限</li>
</ul>
<h3 id="2-2-常见内存区域"><a href="#2-2-常见内存区域" class="headerlink" title="2.2 常见内存区域"></a>2.2 常见内存区域</h3><p>可以先用最常见的划分理解：</p>
<ul>
<li><strong>代码区</strong>：机器指令</li>
<li><strong>全局&#x2F;静态区</strong>：全局变量、静态变量</li>
<li><strong>栈</strong>：函数局部变量、返回地址、调用帧</li>
<li><strong>堆</strong>：动态分配内存</li>
</ul>
<p>示意：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">高地址</span><br><span class="line">┌────────────────────┐</span><br><span class="line">│      栈 stack      │</span><br><span class="line">├────────────────────┤</span><br><span class="line">│        ...         │</span><br><span class="line">├────────────────────┤</span><br><span class="line">│      堆 heap       │</span><br><span class="line">├────────────────────┤</span><br><span class="line">│ 全局/静态数据区     │</span><br><span class="line">├────────────────────┤</span><br><span class="line">│      代码区         │</span><br><span class="line">└────────────────────┘</span><br><span class="line">低地址</span><br></pre></td></tr></table></figure>

<p>指针只是“指向某个地址”，它本身并不关心这个地址来自栈、堆还是全局区，但程序员必须关心对象生命周期。</p>
<hr>
<h2 id="3-指针是什么"><a href="#3-指针是什么" class="headerlink" title="3. 指针是什么"></a>3. 指针是什么</h2><h3 id="3-1-最基本定义"><a href="#3-1-最基本定义" class="headerlink" title="3.1 最基本定义"></a>3.1 最基本定义</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">42</span>;</span><br><span class="line"><span class="type">int</span>* p = &amp;x;</span><br></pre></td></tr></table></figure>

<p>这里：</p>
<ul>
<li><code>x</code> 是一个 <code>int</code> 对象</li>
<li><code>&amp;x</code> 取出 <code>x</code> 的地址</li>
<li><code>p</code> 保存这个地址</li>
</ul>
<p>解引用：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">*p = <span class="number">100</span>;</span><br></pre></td></tr></table></figure>

<p>表示“沿着地址找到对象并访问它”。</p>
<h3 id="3-2-指针的类型非常重要"><a href="#3-2-指针的类型非常重要" class="headerlink" title="3.2 指针的类型非常重要"></a>3.2 指针的类型非常重要</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* p1;</span><br><span class="line"><span class="type">double</span>* p2;</span><br><span class="line"><span class="type">char</span>* p3;</span><br></pre></td></tr></table></figure>

<p>类型决定了很多事情：</p>
<ul>
<li>解引用后按什么类型解释内存</li>
<li>指针运算步长是多少</li>
<li>编译器如何做别名分析和优化</li>
</ul>
<p>所以指针不是“纯粹整数”，它是“带类型的地址”。</p>
<hr>
<h2 id="4-指针运算为什么会“自动跳格子”"><a href="#4-指针运算为什么会“自动跳格子”" class="headerlink" title="4. 指针运算为什么会“自动跳格子”"></a>4. 指针运算为什么会“自动跳格子”</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">4</span>] = &#123;<span class="number">10</span>, <span class="number">20</span>, <span class="number">30</span>, <span class="number">40</span>&#125;;</span><br><span class="line"><span class="type">int</span>* p = a;</span><br></pre></td></tr></table></figure>

<p>此时：</p>
<ul>
<li><code>p</code> 指向 <code>a[0]</code></li>
<li><code>p + 1</code> 指向 <code>a[1]</code></li>
</ul>
<p>原因不是编译器在“魔法加一”，而是：</p>
<ul>
<li><code>int*</code> 的步长是 <code>sizeof(int)</code></li>
</ul>
<p>所以：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">p + n</span><br></pre></td></tr></table></figure>

<p>实际含义是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">原地址 + n * sizeof(int)</span><br></pre></td></tr></table></figure>

<p>这也是为什么不同类型的指针运算结果不同。</p>
<hr>
<h2 id="5-数组与指针的关系"><a href="#5-数组与指针的关系" class="headerlink" title="5. 数组与指针的关系"></a>5. 数组与指针的关系</h2><h3 id="5-1-数组名在很多场景下会退化成指针"><a href="#5-1-数组名在很多场景下会退化成指针" class="headerlink" title="5.1 数组名在很多场景下会退化成指针"></a>5.1 数组名在很多场景下会退化成指针</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">4</span>] = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>&#125;;</span><br><span class="line"><span class="type">int</span>* p = a;</span><br></pre></td></tr></table></figure>

<p>这里 <code>a</code> 在表达式里通常会退化为指向首元素的指针，也就是 <code>&amp;a[0]</code>。</p>
<h3 id="5-2-但数组不等于指针"><a href="#5-2-但数组不等于指针" class="headerlink" title="5.2 但数组不等于指针"></a>5.2 但数组不等于指针</h3><p>这点很重要：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">4</span>];</span><br><span class="line"><span class="type">int</span>* p = a;</span><br></pre></td></tr></table></figure>

<p>虽然很多场景里 <code>a</code> 能转成 <code>int*</code>，但它们不是同一种东西：</p>
<ul>
<li><code>a</code> 的类型是 <code>int[4]</code></li>
<li><code>p</code> 的类型是 <code>int*</code></li>
</ul>
<p>区别举例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sizeof</span>(a) <span class="comment">// 4 * sizeof(int)</span></span><br><span class="line"><span class="built_in">sizeof</span>(p) <span class="comment">// 指针本身大小，通常是 8 或 4</span></span><br></pre></td></tr></table></figure>

<h3 id="5-3-a-与-a-也不同"><a href="#5-3-a-与-a-也不同" class="headerlink" title="5.3 a 与 &amp;a 也不同"></a>5.3 <code>a</code> 与 <code>&amp;a</code> 也不同</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">4</span>];</span><br></pre></td></tr></table></figure>

<ul>
<li><code>a</code> 退化后接近 <code>int*</code></li>
<li><code>&amp;a</code> 的类型是 <code>int (*)[4]</code>，即“指向整个数组的指针”</li>
</ul>
<p>这在多维数组里尤其重要。</p>
<hr>
<h2 id="6-指针与对象生命周期"><a href="#6-指针与对象生命周期" class="headerlink" title="6. 指针与对象生命周期"></a>6. 指针与对象生命周期</h2><p>指针能不能安全使用，不取决于“地址像不像对的”，而取决于它指向的对象是否还活着。</p>
<h3 id="6-1-悬空指针"><a href="#6-1-悬空指针" class="headerlink" title="6.1 悬空指针"></a>6.1 悬空指针</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span>* <span class="title">bad</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> x = <span class="number">42</span>;</span><br><span class="line">    <span class="keyword">return</span> &amp;x; <span class="comment">// 错</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>函数返回后，<code>x</code> 生命周期结束，返回的地址失效，这就是悬空指针。</p>
<h3 id="6-2-delete-后继续使用"><a href="#6-2-delete-后继续使用" class="headerlink" title="6.2 delete 后继续使用"></a>6.2 <code>delete</code> 后继续使用</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* p = <span class="keyword">new</span> <span class="built_in">int</span>(<span class="number">10</span>);</span><br><span class="line"><span class="keyword">delete</span> p;</span><br><span class="line"><span class="comment">// *p = 20; // 未定义行为</span></span><br></pre></td></tr></table></figure>

<p><code>delete</code> 只表示这块动态内存已经归还，<code>p</code> 变量本身不会自动变成安全状态。</p>
<p>一个常见但有限的自保动作是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">p = <span class="literal">nullptr</span>;</span><br></pre></td></tr></table></figure>

<h3 id="6-3-指针失效比“值错误”更危险"><a href="#6-3-指针失效比“值错误”更危险" class="headerlink" title="6.3 指针失效比“值错误”更危险"></a>6.3 指针失效比“值错误”更危险</h3><p>因为很多时候程序不会立刻崩溃，而是：</p>
<ul>
<li>偶发错误</li>
<li>数据被静默污染</li>
<li>只在某个编译选项或某台机器上复现</li>
</ul>
<p>这也是 C++ 为什么强调 RAII 和智能指针。</p>
<hr>
<h2 id="7-const-和指针"><a href="#7-const-和指针" class="headerlink" title="7. const 和指针"></a>7. <code>const</code> 和指针</h2><p>这是高频混淆点。</p>
<h3 id="7-1-指向常量的指针"><a href="#7-1-指向常量的指针" class="headerlink" title="7.1 指向常量的指针"></a>7.1 指向常量的指针</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span>* p;</span><br></pre></td></tr></table></figure>

<p>或者：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="type">const</span>* p;</span><br></pre></td></tr></table></figure>

<p>含义：</p>
<ul>
<li>不能通过 <code>p</code> 修改它指向的值</li>
<li>但 <code>p</code> 自己可以改指向</li>
</ul>
<h3 id="7-2-常量指针"><a href="#7-2-常量指针" class="headerlink" title="7.2 常量指针"></a>7.2 常量指针</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* <span class="type">const</span> p = &amp;x;</span><br></pre></td></tr></table></figure>

<p>含义：</p>
<ul>
<li><code>p</code> 自己不能改指向</li>
<li>但能通过 <code>p</code> 修改对象</li>
</ul>
<h3 id="7-3-两者都常量"><a href="#7-3-两者都常量" class="headerlink" title="7.3 两者都常量"></a>7.3 两者都常量</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span>* <span class="type">const</span> p = &amp;x;</span><br></pre></td></tr></table></figure>

<p>记忆口诀：</p>
<ul>
<li><code>const</code> 修饰 <code>*</code> 左边内容，表示“指向的对象只读”</li>
<li><code>const</code> 修饰变量本身，表示“指针本身不可改”</li>
</ul>
<hr>
<h2 id="8-空指针、野指针、悬空指针"><a href="#8-空指针、野指针、悬空指针" class="headerlink" title="8. 空指针、野指针、悬空指针"></a>8. 空指针、野指针、悬空指针</h2><p>这三个概念经常混在一起，但不一样。</p>
<h3 id="8-1-空指针"><a href="#8-1-空指针" class="headerlink" title="8.1 空指针"></a>8.1 空指针</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* p = <span class="literal">nullptr</span>;</span><br></pre></td></tr></table></figure>

<p>它明确表示“不指向任何对象”。<br>空指针本身是安全的状态，但<strong>不能解引用</strong>。</p>
<h3 id="8-2-野指针"><a href="#8-2-野指针" class="headerlink" title="8.2 野指针"></a>8.2 野指针</h3><p>通常指未初始化、值不可信的指针：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* p; <span class="comment">// 未初始化</span></span><br></pre></td></tr></table></figure>

<p>这时 <code>p</code> 中是什么值不确定。</p>
<h3 id="8-3-悬空指针"><a href="#8-3-悬空指针" class="headerlink" title="8.3 悬空指针"></a>8.3 悬空指针</h3><p>原来指向过合法对象，但对象已经没了：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* p = <span class="keyword">new</span> <span class="built_in">int</span>(<span class="number">1</span>);</span><br><span class="line"><span class="keyword">delete</span> p; <span class="comment">// p 变成悬空指针</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="9-引用和指针的区别"><a href="#9-引用和指针的区别" class="headerlink" title="9. 引用和指针的区别"></a>9. 引用和指针的区别</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">10</span>;</span><br><span class="line"><span class="type">int</span>* p = &amp;x;</span><br><span class="line"><span class="type">int</span>&amp; r = x;</span><br></pre></td></tr></table></figure>

<p>可以这样理解：</p>
<ul>
<li>指针更像“显式保存地址”</li>
<li>引用更像“对象别名”</li>
</ul>
<p>常见区别：</p>
<table>
<thead>
<tr>
<th>项目</th>
<th>指针</th>
<th>引用</th>
</tr>
</thead>
<tbody><tr>
<td>是否可为空</td>
<td>可以</td>
<td>不可以直接为空</td>
</tr>
<tr>
<td>是否可改指向</td>
<td>可以</td>
<td>不可以重新绑定</td>
</tr>
<tr>
<td>是否需要显式解引用</td>
<td>需要 <code>*p</code></td>
<td>不需要</td>
</tr>
<tr>
<td>是否更接近底层内存模型</td>
<td>是</td>
<td>否</td>
</tr>
</tbody></table>
<p>高性能代码里两者都常见：</p>
<ul>
<li>接口表达“可选对象”时，指针更自然</li>
<li>表达“必须存在的别名”时，引用更自然</li>
</ul>
<hr>
<h2 id="10-void-与类型擦除"><a href="#10-void-与类型擦除" class="headerlink" title="10. void* 与类型擦除"></a>10. <code>void*</code> 与类型擦除</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span>* p = <span class="built_in">malloc</span>(<span class="number">128</span>);</span><br></pre></td></tr></table></figure>

<p><code>void*</code> 可以表示“未知具体类型的地址”，但它失去了类型信息：</p>
<ul>
<li>不能直接解引用</li>
<li>不能直接做带步长的指针运算</li>
</ul>
<p>必须先转回具体类型：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* q = <span class="built_in">static_cast</span>&lt;<span class="type">int</span>*&gt;(p);</span><br></pre></td></tr></table></figure>

<p>在 C++ 中，<code>void*</code> 主要用于：</p>
<ul>
<li>和 C API 交互</li>
<li>极底层内存管理</li>
<li>通用资源句柄封装</li>
</ul>
<p>现代 C++ 常更倾向于：</p>
<ul>
<li>模板</li>
<li><code>std::byte</code></li>
<li><code>std::span</code></li>
<li>类型安全封装</li>
</ul>
<hr>
<h2 id="11-对齐（alignment）为什么重要"><a href="#11-对齐（alignment）为什么重要" class="headerlink" title="11. 对齐（alignment）为什么重要"></a>11. 对齐（alignment）为什么重要</h2><p>每种类型通常都有自己的对齐要求。</p>
<p>例如：</p>
<ul>
<li><code>char</code> 常见对齐为 1</li>
<li><code>int</code> 常见对齐为 4</li>
<li><code>double</code> 常见对齐为 8</li>
</ul>
<p>原因是：</p>
<ul>
<li>CPU 按特定粒度取数更高效</li>
<li>某些平台上未对齐访问代价更高，甚至会触发异常</li>
</ul>
<h3 id="11-1-结构体中的填充"><a href="#11-1-结构体中的填充" class="headerlink" title="11.1 结构体中的填充"></a>11.1 结构体中的填充</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">A</span> &#123;</span><br><span class="line">    <span class="type">char</span> c;</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><code>sizeof(A)</code> 往往不是 <code>1 + 4 = 5</code>，而是可能变成 <code>8</code>，因为编译器会插入 padding 以满足对齐。</p>
<p>这直接影响：</p>
<ul>
<li>内存占用</li>
<li>缓存利用率</li>
<li>二进制布局</li>
</ul>
<hr>
<h2 id="12-指针、缓存与性能"><a href="#12-指针、缓存与性能" class="headerlink" title="12. 指针、缓存与性能"></a>12. 指针、缓存与性能</h2><h3 id="12-1-指针追逐为什么慢"><a href="#12-1-指针追逐为什么慢" class="headerlink" title="12.1 指针追逐为什么慢"></a>12.1 指针追逐为什么慢</h3><p>链表、树、图这类结构经常依赖指针跳转：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">node -&gt; next -&gt; next -&gt; next</span><br></pre></td></tr></table></figure>

<p>问题在于：</p>
<ul>
<li>节点可能分散在内存各处</li>
<li>CPU 很难提前预取</li>
<li>每一步都可能触发 cache miss</li>
</ul>
<p>这就是为什么很多高性能场景中：</p>
<ul>
<li><code>std::vector</code></li>
<li>扁平数组</li>
<li>SoA（Struct of Arrays）</li>
</ul>
<p>会比“满地指针的节点结构”更快。</p>
<h3 id="12-2-顺序访问更友好"><a href="#12-2-顺序访问更友好" class="headerlink" title="12.2 顺序访问更友好"></a>12.2 顺序访问更友好</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    sum += a[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种模式容易利用：</p>
<ul>
<li>空间局部性</li>
<li>硬件预取</li>
<li>向量化</li>
</ul>
<h3 id="12-3-指针不只是“能访问”，还影响优化"><a href="#12-3-指针不只是“能访问”，还影响优化" class="headerlink" title="12.3 指针不只是“能访问”，还影响优化"></a>12.3 指针不只是“能访问”，还影响优化</h3><p>编译器如果怀疑两个指针可能指向同一块内存，就会更保守。<br>这叫<strong>别名分析</strong>问题。</p>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">saxpy</span><span class="params">(<span class="type">float</span>* x, <span class="type">float</span>* y, <span class="type">float</span> a, <span class="type">size_t</span> n)</span></span>;</span><br></pre></td></tr></table></figure>

<p>如果编译器无法确定 <code>x</code> 和 <code>y</code> 是否重叠，某些优化就可能受限。</p>
<hr>
<h2 id="13-别名（aliasing）与优化"><a href="#13-别名（aliasing）与优化" class="headerlink" title="13. 别名（aliasing）与优化"></a>13. 别名（aliasing）与优化</h2><h3 id="13-1-什么是别名"><a href="#13-1-什么是别名" class="headerlink" title="13.1 什么是别名"></a>13.1 什么是别名</h3><p>如果两个指针可能指向同一对象，就说它们可能别名。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">0</span>;</span><br><span class="line"><span class="type">int</span>* p = &amp;x;</span><br><span class="line"><span class="type">int</span>* q = &amp;x;</span><br></pre></td></tr></table></figure>

<p>这里 <code>p</code> 和 <code>q</code> 明显别名。</p>
<h3 id="13-2-为什么编译器怕别名"><a href="#13-2-为什么编译器怕别名" class="headerlink" title="13.2 为什么编译器怕别名"></a>13.2 为什么编译器怕别名</h3><p>看这种代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">*p = <span class="number">1</span>;</span><br><span class="line"><span class="type">int</span> t = *q;</span><br></pre></td></tr></table></figure>

<p>如果 <code>p</code> 和 <code>q</code> 指向同一对象，那么 <code>t</code> 就是 1；<br>如果不是，结果又不同。</p>
<p>为了不出错，编译器只能保守。</p>
<h3 id="13-3-高性能代码的一个常见思路"><a href="#13-3-高性能代码的一个常见思路" class="headerlink" title="13.3 高性能代码的一个常见思路"></a>13.3 高性能代码的一个常见思路</h3><p>尽量让数据布局和接口更清晰，让编译器更容易推断：</p>
<ul>
<li>输入输出分离</li>
<li>连续数组而不是复杂指针结构</li>
<li>避免不必要的类型转换和 <code>reinterpret_cast</code></li>
</ul>
<hr>
<h2 id="14-多级指针与二维数组"><a href="#14-多级指针与二维数组" class="headerlink" title="14. 多级指针与二维数组"></a>14. 多级指针与二维数组</h2><h3 id="14-1-指针的指针"><a href="#14-1-指针的指针" class="headerlink" title="14.1 指针的指针"></a>14.1 指针的指针</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">1</span>;</span><br><span class="line"><span class="type">int</span>* p = &amp;x;</span><br><span class="line"><span class="type">int</span>** pp = &amp;p;</span><br></pre></td></tr></table></figure>

<p>这在“修改调用者持有的指针”时很常见。</p>
<h3 id="14-2-二维数组不等于-T"><a href="#14-2-二维数组不等于-T" class="headerlink" title="14.2 二维数组不等于 T**"></a>14.2 二维数组不等于 <code>T**</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">3</span>][<span class="number">4</span>];</span><br></pre></td></tr></table></figure>

<p>它的类型是“3 个 <code>int[4]</code> 组成的数组”，不是普通的 <code>int**</code>。</p>
<p>所以：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>** p = a; <span class="comment">// 错</span></span><br></pre></td></tr></table></figure>

<p>正确理解更接近：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">int</span> (*p)[<span class="number">4</span>] = a;</span><br></pre></td></tr></table></figure>

<p>这类问题在图像处理、矩阵运算、CUDA host 端数据布局里很常见。</p>
<hr>
<h2 id="15-什么时候该少写裸指针"><a href="#15-什么时候该少写裸指针" class="headerlink" title="15. 什么时候该少写裸指针"></a>15. 什么时候该少写裸指针</h2><p>裸指针不是原罪，但它不应该默认承担“所有权管理”。</p>
<p>更推荐的分工是：</p>
<ul>
<li><strong>所有权</strong>：交给 <code>std::unique_ptr</code>、<code>std::shared_ptr</code>、容器</li>
<li><strong>观察&#x2F;访问</strong>：用裸指针、引用、<code>std::span</code></li>
</ul>
<p>换句话说：</p>
<ul>
<li>“谁负责释放资源”这件事不要靠口头约定</li>
<li>裸指针更适合表达“我只是访问，不拥有”</li>
</ul>
<hr>
<h2 id="16-指针相关的高频错误"><a href="#16-指针相关的高频错误" class="headerlink" title="16. 指针相关的高频错误"></a>16. 指针相关的高频错误</h2><h3 id="16-1-返回局部变量地址"><a href="#16-1-返回局部变量地址" class="headerlink" title="16.1 返回局部变量地址"></a>16.1 返回局部变量地址</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span>* <span class="title">bad</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> x = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> &amp;x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="16-2-越界访问"><a href="#16-2-越界访问" class="headerlink" title="16.2 越界访问"></a>16.2 越界访问</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">4</span>];</span><br><span class="line"><span class="type">int</span>* p = a;</span><br><span class="line"><span class="type">int</span> x = p[<span class="number">10</span>]; <span class="comment">// 未定义行为</span></span><br></pre></td></tr></table></figure>

<h3 id="16-3-类型解释错误"><a href="#16-3-类型解释错误" class="headerlink" title="16.3 类型解释错误"></a>16.3 类型解释错误</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">double</span> d = <span class="number">3.14</span>;</span><br><span class="line"><span class="type">int</span>* p = <span class="built_in">reinterpret_cast</span>&lt;<span class="type">int</span>*&gt;(&amp;d); <span class="comment">// 极危险</span></span><br></pre></td></tr></table></figure>

<h3 id="16-4-忘记对象生命周期"><a href="#16-4-忘记对象生命周期" class="headerlink" title="16.4 忘记对象生命周期"></a>16.4 忘记对象生命周期</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; v = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;</span><br><span class="line"><span class="type">int</span>* p = v.<span class="built_in">data</span>();</span><br><span class="line">v.<span class="built_in">push_back</span>(<span class="number">4</span>); <span class="comment">// 可能扩容</span></span><br><span class="line"><span class="comment">// p 可能失效</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="17-一页总结"><a href="#17-一页总结" class="headerlink" title="17. 一页总结"></a>17. 一页总结</h2><p>指针最本质的理解是：</p>
<ul>
<li>它保存的是地址</li>
<li>但这个地址必须和“对象类型、生命周期、对齐、布局”一起理解</li>
</ul>
<p>高性能 C++ 里，指针相关最重要的不是炫技，而是这几条：</p>
<ol>
<li>清楚对象生命周期，避免悬空</li>
<li>理解指针运算基于类型步长</li>
<li>不把数组和指针混为一谈</li>
<li>重视缓存局部性，少做指针追逐</li>
<li>让数据布局更连续、更可预测</li>
<li>用智能指针和容器管理所有权，少让裸指针负责释放</li>
</ol>
<hr>
<h2 id="18-建议继续补充的相关主题"><a href="#18-建议继续补充的相关主题" class="headerlink" title="18. 建议继续补充的相关主题"></a>18. 建议继续补充的相关主题</h2><p>和本篇衔接最紧密的内容：</p>
<ol>
<li><code>RAII</code> 与智能指针</li>
<li><code>std::span</code>、无拷贝视图与边界表达</li>
<li>严格别名规则（strict aliasing）</li>
<li>对齐、padding 与 SIMD</li>
<li><code>AoS</code> &#x2F; <code>SoA</code> 的访存差异</li>
</ol>
<hr>
<h2 id="19-参考资料"><a href="#19-参考资料" class="headerlink" title="19. 参考资料"></a>19. 参考资料</h2><ol>
<li><p>cppreference: pointers<br><a href="https://en.cppreference.com/w/cpp/language/pointer">https://en.cppreference.com/w/cpp/language/pointer</a></p>
</li>
<li><p>cppreference: object lifetime<br><a href="https://en.cppreference.com/w/cpp/language/lifetime">https://en.cppreference.com/w/cpp/language/lifetime</a></p>
</li>
<li><p>cppreference: alignment<br><a href="https://en.cppreference.com/w/cpp/language/object#Alignment">https://en.cppreference.com/w/cpp/language/object#Alignment</a></p>
</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>RAII 与智能指针笔记</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/02-raii%E4%B8%8E%E6%99%BA%E8%83%BD%E6%8C%87%E9%92%88/</url>
    <content><![CDATA[<h1 id="RAII-与智能指针笔记"><a href="#RAII-与智能指针笔记" class="headerlink" title="RAII 与智能指针笔记"></a>RAII 与智能指针笔记</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：RAII、资源生命周期、异常安全、<code>unique_ptr</code>、<code>shared_ptr</code>、<code>weak_ptr</code>、自定义 deleter、锁管理<br>核心目标：把“资源一定会被释放”这件事交给对象生命周期，而不是交给人脑记忆。</p>
</blockquote>
<hr>
<h2 id="1-RAII-是什么"><a href="#1-RAII-是什么" class="headerlink" title="1. RAII 是什么"></a>1. RAII 是什么</h2><p>RAII 全称：</p>
<blockquote>
<p>Resource Acquisition Is Initialization<br>资源获取即初始化</p>
</blockquote>
<p>核心思想：</p>
<ul>
<li>在对象构造时获取资源</li>
<li>在对象析构时释放资源</li>
</ul>
<p>这样做的价值非常直接：</p>
<ul>
<li>不容易忘记释放</li>
<li>遇到异常时也能自动回收</li>
<li>生命周期更清晰</li>
<li>接口更容易组合</li>
</ul>
<p>这里的“资源”不只指内存，还包括：</p>
<ul>
<li>动态内存</li>
<li>文件句柄</li>
<li>socket</li>
<li>数据库连接</li>
<li>互斥锁</li>
<li>线程句柄</li>
<li>GPU&#x2F;系统资源</li>
</ul>
<hr>
<h2 id="2-为什么-RAII-在-C-里特别重要"><a href="#2-为什么-RAII-在-C-里特别重要" class="headerlink" title="2. 为什么 RAII 在 C++ 里特别重要"></a>2. 为什么 RAII 在 C++ 里特别重要</h2><p>C++ 有确定性析构：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    MyObj obj;</span><br><span class="line">    <span class="comment">// 离开作用域时 obj 一定析构</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这意味着“资源释放”可以天然绑定到作用域结束。</p>
<p>相比手工写：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">open</span>();</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"><span class="built_in">close</span>();</span><br></pre></td></tr></table></figure>

<p>RAII 更稳，因为它不依赖你记得在每一条返回路径都调用 <code>close()</code>。</p>
<hr>
<h2 id="3-没有-RAII-时最容易出什么问题"><a href="#3-没有-RAII-时最容易出什么问题" class="headerlink" title="3. 没有 RAII 时最容易出什么问题"></a>3. 没有 RAII 时最容易出什么问题</h2><h3 id="3-1-多返回路径漏释放"><a href="#3-1-多返回路径漏释放" class="headerlink" title="3.1 多返回路径漏释放"></a>3.1 多返回路径漏释放</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">FILE* fp = <span class="built_in">fopen</span>(<span class="string">&quot;data.txt&quot;</span>, <span class="string">&quot;r&quot;</span>);</span><br><span class="line"><span class="keyword">if</span> (!fp) <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (<span class="built_in">bad_input</span>()) &#123;</span><br><span class="line">    <span class="keyword">return</span>; <span class="comment">// 忘了 fclose(fp)</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">fclose</span>(fp);</span><br></pre></td></tr></table></figure>

<h3 id="3-2-异常打断控制流"><a href="#3-2-异常打断控制流" class="headerlink" title="3.2 异常打断控制流"></a>3.2 异常打断控制流</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span>* p = <span class="keyword">new</span> <span class="type">int</span>[<span class="number">100</span>];</span><br><span class="line">    <span class="built_in">g</span>(); <span class="comment">// 这里如果抛异常，delete[] 可能永远执行不到</span></span><br><span class="line">    <span class="keyword">delete</span>[] p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-3-锁没有释放"><a href="#3-3-锁没有释放" class="headerlink" title="3.3 锁没有释放"></a>3.3 锁没有释放</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">mtx.<span class="built_in">lock</span>();</span><br><span class="line"><span class="built_in">do_work</span>(); <span class="comment">// 中途 return/throw 都可能导致锁没解开</span></span><br><span class="line">mtx.<span class="built_in">unlock</span>();</span><br></pre></td></tr></table></figure>

<p>这些问题都可以通过 RAII 化简。</p>
<hr>
<h2 id="4-最小-RAII-示例"><a href="#4-最小-RAII-示例" class="headerlink" title="4. 最小 RAII 示例"></a>4. 最小 RAII 示例</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdio&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">FileGuard</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">FileGuard</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* path)</span></span></span><br><span class="line"><span class="function">        : fp_(std::fopen(path, <span class="string">&quot;r&quot;</span>)) &#123;</span>&#125;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">FileGuard</span>() &#123;</span><br><span class="line">        <span class="keyword">if</span> (fp_) std::<span class="built_in">fclose</span>(fp_);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">FILE* <span class="title">get</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> fp_; &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">FileGuard</span>(<span class="type">const</span> FileGuard&amp;) = <span class="keyword">delete</span>;</span><br><span class="line">    FileGuard&amp; <span class="keyword">operator</span>=(<span class="type">const</span> FileGuard&amp;) = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    FILE* fp_ = <span class="literal">nullptr</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>要点：</p>
<ul>
<li>构造函数负责获取资源</li>
<li>析构函数负责释放资源</li>
<li>禁止拷贝，避免多个对象同时认为自己拥有同一资源</li>
</ul>
<hr>
<h2 id="5-智能指针就是-RAII-的标准化实践"><a href="#5-智能指针就是-RAII-的标准化实践" class="headerlink" title="5. 智能指针就是 RAII 的标准化实践"></a>5. 智能指针就是 RAII 的标准化实践</h2><p>最常见的智能指针有三类：</p>
<ul>
<li><code>std::unique_ptr</code></li>
<li><code>std::shared_ptr</code></li>
<li><code>std::weak_ptr</code></li>
</ul>
<p>它们的本质都是：</p>
<ul>
<li>用对象包装裸指针</li>
<li>把释放逻辑放进析构函数</li>
</ul>
<hr>
<h2 id="6-std-unique-ptr：独占所有权"><a href="#6-std-unique-ptr：独占所有权" class="headerlink" title="6. std::unique_ptr：独占所有权"></a>6. <code>std::unique_ptr</code>：独占所有权</h2><h3 id="6-1-基本语义"><a href="#6-1-基本语义" class="headerlink" title="6.1 基本语义"></a>6.1 基本语义</h3><p>一个资源在任意时刻只能有一个拥有者。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> p = std::<span class="built_in">make_unique</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li>不可拷贝</li>
<li>可以移动</li>
<li>开销小</li>
<li>语义最清晰</li>
</ul>
<h3 id="6-2-为什么优先推荐-make-unique"><a href="#6-2-为什么优先推荐-make-unique" class="headerlink" title="6.2 为什么优先推荐 make_unique"></a>6.2 为什么优先推荐 <code>make_unique</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> p = std::<span class="built_in">make_unique</span>&lt;std::string&gt;(<span class="string">&quot;hello&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>相比手写：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;std::string&gt; <span class="title">p</span><span class="params">(<span class="keyword">new</span> std::string(<span class="string">&quot;hello&quot;</span>))</span></span>;</span><br></pre></td></tr></table></figure>

<p>更推荐 <code>make_unique</code>，因为：</p>
<ul>
<li>更简洁</li>
<li>更不容易写出异常安全漏洞</li>
<li>类型推导更清楚</li>
</ul>
<h3 id="6-3-移动所有权"><a href="#6-3-移动所有权" class="headerlink" title="6.3 移动所有权"></a>6.3 移动所有权</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::unique_ptr&lt;<span class="type">int</span>&gt; p1 = std::<span class="built_in">make_unique</span>&lt;<span class="type">int</span>&gt;(<span class="number">7</span>);</span><br><span class="line">std::unique_ptr&lt;<span class="type">int</span>&gt; p2 = std::<span class="built_in">move</span>(p1);</span><br></pre></td></tr></table></figure>

<p>移动后：</p>
<ul>
<li><code>p2</code> 接管资源</li>
<li><code>p1</code> 变为空</li>
</ul>
<h3 id="6-4-作为参数怎么传"><a href="#6-4-作为参数怎么传" class="headerlink" title="6.4 作为参数怎么传"></a>6.4 作为参数怎么传</h3><p>常见几种风格：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">use</span><span class="params">(<span class="type">const</span> std::unique_ptr&lt;Foo&gt;&amp; p)</span></span>; <span class="comment">// 只观察，不接管</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">take</span><span class="params">(std::unique_ptr&lt;Foo&gt; p)</span></span>;       <span class="comment">// 接管所有权</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">view</span><span class="params">(Foo* p)</span></span>;                       <span class="comment">// 只观察底层对象</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">ref</span><span class="params">(Foo&amp; x)</span></span>;                        <span class="comment">// 保证对象存在</span></span><br></pre></td></tr></table></figure>

<p>经验上：</p>
<ul>
<li>要表达“接管所有权”，就按值接收 <code>unique_ptr</code></li>
<li>只观察对象，不一定非要把参数写成智能指针类型</li>
</ul>
<hr>
<h2 id="7-std-shared-ptr：共享所有权"><a href="#7-std-shared-ptr：共享所有权" class="headerlink" title="7. std::shared_ptr：共享所有权"></a>7. <code>std::shared_ptr</code>：共享所有权</h2><h3 id="7-1-基本语义"><a href="#7-1-基本语义" class="headerlink" title="7.1 基本语义"></a>7.1 基本语义</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> p1 = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line"><span class="keyword">auto</span> p2 = p1;</span><br></pre></td></tr></table></figure>

<p>此时：</p>
<ul>
<li><code>p1</code> 和 <code>p2</code> 共同拥有同一个对象</li>
<li>内部通过引用计数决定何时释放</li>
</ul>
<h3 id="7-2-什么时候用-shared-ptr"><a href="#7-2-什么时候用-shared-ptr" class="headerlink" title="7.2 什么时候用 shared_ptr"></a>7.2 什么时候用 <code>shared_ptr</code></h3><p>只有在“确实存在共享拥有关系”时才使用。</p>
<p>典型场景：</p>
<ul>
<li>对象要被多个异步任务共同持有</li>
<li>图结构&#x2F;对象系统中需要共享生命周期</li>
<li>回调系统中对象可能跨作用域存活</li>
</ul>
<h3 id="7-3-成本是什么"><a href="#7-3-成本是什么" class="headerlink" title="7.3 成本是什么"></a>7.3 成本是什么</h3><p><code>shared_ptr</code> 比 <code>unique_ptr</code> 重得多，因为它通常涉及：</p>
<ul>
<li>控制块</li>
<li>原子引用计数</li>
<li>更复杂的生命周期管理</li>
</ul>
<p>所以不要把它当默认选择。</p>
<hr>
<h2 id="8-std-weak-ptr：打破循环引用"><a href="#8-std-weak-ptr：打破循环引用" class="headerlink" title="8. std::weak_ptr：打破循环引用"></a>8. <code>std::weak_ptr</code>：打破循环引用</h2><h3 id="8-1-为什么需要它"><a href="#8-1-为什么需要它" class="headerlink" title="8.1 为什么需要它"></a>8.1 为什么需要它</h3><p>如果两个 <code>shared_ptr</code> 互相持有，就会形成循环，导致引用计数永远不归零。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">B</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">A</span> &#123;</span><br><span class="line">    std::shared_ptr&lt;B&gt; b;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">B</span> &#123;</span><br><span class="line">    std::shared_ptr&lt;A&gt; a;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这会泄漏。</p>
<h3 id="8-2-正确思路"><a href="#8-2-正确思路" class="headerlink" title="8.2 正确思路"></a>8.2 正确思路</h3><p>把“非拥有关系”的那一侧改成 <code>weak_ptr</code>：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">B</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">A</span> &#123;</span><br><span class="line">    std::shared_ptr&lt;B&gt; b;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">B</span> &#123;</span><br><span class="line">    std::weak_ptr&lt;A&gt; a;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="8-3-使用方式"><a href="#8-3-使用方式" class="headerlink" title="8.3 使用方式"></a>8.3 使用方式</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="keyword">auto</span> sp = weak.<span class="built_in">lock</span>()) &#123;</span><br><span class="line">    <span class="comment">// 对象还活着</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>weak_ptr</code> 的语义是：</p>
<ul>
<li>我知道这个对象可能存在</li>
<li>但我不参与拥有</li>
</ul>
<hr>
<h2 id="9-自定义-deleter"><a href="#9-自定义-deleter" class="headerlink" title="9. 自定义 deleter"></a>9. 自定义 deleter</h2><p>有些资源不是 <code>delete</code> 释放的，例如：</p>
<ul>
<li><code>FILE*</code> 要 <code>fclose</code></li>
<li><code>malloc</code> 对应 <code>free</code></li>
<li>socket &#x2F; GPU handle &#x2F; 句柄各有自己的释放函数</li>
</ul>
<p>这时可以给智能指针指定自定义删除器。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdio&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> FilePtr = std::unique_ptr&lt;FILE, <span class="built_in">int</span>(*)(FILE*)&gt;;</span><br><span class="line"></span><br><span class="line"><span class="function">FilePtr <span class="title">open_file</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* path)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">FilePtr</span>(std::<span class="built_in">fopen</span>(path, <span class="string">&quot;r&quot;</span>), std::fclose);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样离开作用域时就会自动 <code>fclose</code>。</p>
<hr>
<h2 id="10-make-shared-与-shared-ptr-new-T"><a href="#10-make-shared-与-shared-ptr-new-T" class="headerlink" title="10. make_shared 与 shared_ptr(new T)"></a>10. <code>make_shared</code> 与 <code>shared_ptr(new T)</code></h2><h3 id="10-1-为什么通常优先-make-shared"><a href="#10-1-为什么通常优先-make-shared" class="headerlink" title="10.1 为什么通常优先 make_shared"></a>10.1 为什么通常优先 <code>make_shared</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> p = std::<span class="built_in">make_shared</span>&lt;MyType&gt;(args...);</span><br></pre></td></tr></table></figure>

<p>优点通常包括：</p>
<ul>
<li>代码更简洁</li>
<li>常见实现里对象和控制块可一次分配</li>
<li>异常安全更直接</li>
</ul>
<h3 id="10-2-什么时候不能用-make-shared"><a href="#10-2-什么时候不能用-make-shared" class="headerlink" title="10.2 什么时候不能用 make_shared"></a>10.2 什么时候不能用 <code>make_shared</code></h3><p>比如你需要：</p>
<ul>
<li>自定义 deleter</li>
<li>控制对象与控制块分离</li>
<li>与某些特殊分配策略配合</li>
</ul>
<p>这时才考虑直接构造 <code>shared_ptr</code>。</p>
<hr>
<h2 id="11-RAII-不只用于内存"><a href="#11-RAII-不只用于内存" class="headerlink" title="11. RAII 不只用于内存"></a>11. RAII 不只用于内存</h2><h3 id="11-1-互斥锁"><a href="#11-1-互斥锁" class="headerlink" title="11.1 互斥锁"></a>11.1 互斥锁</h3><p>错误写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">mtx.<span class="built_in">lock</span>();</span><br><span class="line"><span class="built_in">work</span>();</span><br><span class="line">mtx.<span class="built_in">unlock</span>();</span><br></pre></td></tr></table></figure>

<p>推荐写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mtx)</span></span>;</span><br><span class="line"><span class="built_in">work</span>();</span><br></pre></td></tr></table></figure>

<p><code>lock_guard</code> 就是典型 RAII：</p>
<ul>
<li>构造时加锁</li>
<li>析构时解锁</li>
</ul>
<h3 id="11-2-更灵活的-unique-lock"><a href="#11-2-更灵活的-unique-lock" class="headerlink" title="11.2 更灵活的 unique_lock"></a>11.2 更灵活的 <code>unique_lock</code></h3><p>当你需要：</p>
<ul>
<li>延迟加锁</li>
<li>手动 <code>unlock</code></li>
<li>配合条件变量</li>
</ul>
<p>就用 <code>std::unique_lock</code>。</p>
<h3 id="11-3-多锁场景"><a href="#11-3-多锁场景" class="headerlink" title="11.3 多锁场景"></a>11.3 多锁场景</h3><p>可以使用：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::scoped_lock <span class="title">lk</span><span class="params">(m1, m2)</span></span>;</span><br></pre></td></tr></table></figure>

<p>它也是 RAII，且能帮你规避一部分死锁风险。</p>
<hr>
<h2 id="12-线程也应该-RAII-化"><a href="#12-线程也应该-RAII-化" class="headerlink" title="12. 线程也应该 RAII 化"></a>12. 线程也应该 RAII 化</h2><h3 id="12-1-std-thread-的问题"><a href="#12-1-std-thread-的问题" class="headerlink" title="12.1 std::thread 的问题"></a>12.1 <code>std::thread</code> 的问题</h3><p><code>std::thread</code> 如果在析构前既没有 <code>join()</code> 也没有 <code>detach()</code>，程序会 <code>std::terminate</code>。</p>
<p>所以线程句柄本身也需要生命周期管理。</p>
<h3 id="12-2-一个简单的-join-guard"><a href="#12-2-一个简单的-join-guard" class="headerlink" title="12.2 一个简单的 join guard"></a>12.2 一个简单的 join guard</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">JoiningThread</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">JoiningThread</span><span class="params">(std::thread t)</span> : t_(std::move(t)) &#123;</span>&#125;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">JoiningThread</span>() &#123;</span><br><span class="line">        <span class="keyword">if</span> (t_.<span class="built_in">joinable</span>()) t_.<span class="built_in">join</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">JoiningThread</span>(<span class="type">const</span> JoiningThread&amp;) = <span class="keyword">delete</span>;</span><br><span class="line">    JoiningThread&amp; <span class="keyword">operator</span>=(<span class="type">const</span> JoiningThread&amp;) = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::thread t_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这也是典型 RAII：</p>
<ul>
<li>构造时接管线程对象</li>
<li>析构时确保 <code>join</code></li>
</ul>
<p>如果是 C++20，可以直接优先考虑 <code>std::jthread</code>。</p>
<hr>
<h2 id="13-异常安全为什么和-RAII-绑定得这么紧"><a href="#13-异常安全为什么和-RAII-绑定得这么紧" class="headerlink" title="13. 异常安全为什么和 RAII 绑定得这么紧"></a>13. 异常安全为什么和 RAII 绑定得这么紧</h2><p>RAII 最强的地方不是“少写一行 <code>delete</code>”，而是它天然支持异常安全。</p>
<p>看这段代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> p = std::<span class="built_in">make_unique</span>&lt;BigObject&gt;();</span><br><span class="line">    <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(mtx)</span></span>;</span><br><span class="line">    <span class="built_in">g</span>(); <span class="comment">// 这里如果抛异常</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>即使 <code>g()</code> 抛异常：</p>
<ul>
<li><code>lk</code> 会析构并解锁</li>
<li><code>p</code> 会析构并释放对象</li>
</ul>
<p>这就是现代 C++ 推荐“资源对象化”的原因。</p>
<hr>
<h2 id="14-所有权设计比选哪种智能指针更重要"><a href="#14-所有权设计比选哪种智能指针更重要" class="headerlink" title="14. 所有权设计比选哪种智能指针更重要"></a>14. 所有权设计比选哪种智能指针更重要</h2><p>高质量 C++ 代码首先要明确：</p>
<ul>
<li>谁拥有对象</li>
<li>谁只观察对象</li>
<li>谁负责释放对象</li>
</ul>
<p>一个常见的经验顺序是：</p>
<ol>
<li>默认用对象值语义</li>
<li>必须动态分配时，优先 <code>unique_ptr</code></li>
<li>只有确实共享拥有时才用 <code>shared_ptr</code></li>
<li>非拥有关系用裸指针、引用、<code>weak_ptr</code>、<code>std::span</code></li>
</ol>
<p>也就是说，智能指针不是“越多越高级”，而是要表达清晰语义。</p>
<hr>
<h2 id="15-常见误区"><a href="#15-常见误区" class="headerlink" title="15. 常见误区"></a>15. 常见误区</h2><h3 id="15-1-裸指针不等于拥有权"><a href="#15-1-裸指针不等于拥有权" class="headerlink" title="15.1 裸指针不等于拥有权"></a>15.1 裸指针不等于拥有权</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">Foo* p = <span class="built_in">get</span>();</span><br></pre></td></tr></table></figure>

<p>单看这一行，没人知道：</p>
<ul>
<li>你是不是要 <code>delete p</code></li>
<li>这个对象是不是别人管理的</li>
</ul>
<p>所以裸指针更适合表达“观察”，不适合表达“拥有”。</p>
<h3 id="15-2-到处滥用-shared-ptr"><a href="#15-2-到处滥用-shared-ptr" class="headerlink" title="15.2 到处滥用 shared_ptr"></a>15.2 到处滥用 <code>shared_ptr</code></h3><p>很多代码用 <code>shared_ptr</code> 只是为了“省心”，结果带来：</p>
<ul>
<li>生命周期更复杂</li>
<li>隐式共享关系</li>
<li>性能开销</li>
<li>循环引用问题</li>
</ul>
<h3 id="15-3-从-unique-ptr-里随手-get-再乱传"><a href="#15-3-从-unique-ptr-里随手-get-再乱传" class="headerlink" title="15.3 从 unique_ptr 里随手 get() 再乱传"></a>15.3 从 <code>unique_ptr</code> 里随手 <code>get()</code> 再乱传</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> p = std::<span class="built_in">make_unique</span>&lt;Foo&gt;();</span><br><span class="line"><span class="built_in">take_raw</span>(p.<span class="built_in">get</span>());</span><br></pre></td></tr></table></figure>

<p>这本身不一定错，但要非常清楚：</p>
<ul>
<li><code>get()</code> 只拿观察指针</li>
<li>不会转移所有权</li>
<li>不能让对方 <code>delete</code></li>
</ul>
<h3 id="15-4-shared-from-this-误用"><a href="#15-4-shared-from-this-误用" class="headerlink" title="15.4 shared_from_this() 误用"></a>15.4 <code>shared_from_this()</code> 误用</h3><p>如果对象不是由 <code>shared_ptr</code> 管理，直接 <code>shared_from_this()</code> 会出问题。<br>这类场景要配合 <code>std::enable_shared_from_this</code> 正确使用。</p>
<h3 id="15-5-智能指针不是性能万能解"><a href="#15-5-智能指针不是性能万能解" class="headerlink" title="15.5 智能指针不是性能万能解"></a>15.5 智能指针不是性能万能解</h3><p>它们解决的是：</p>
<ul>
<li>资源释放正确性</li>
<li>生命周期表达</li>
</ul>
<p>而不是自动让程序变快。<br>在极热路径里，真正重要的还是：</p>
<ul>
<li>数据布局</li>
<li>分配次数</li>
<li>缓存局部性</li>
</ul>
<hr>
<h2 id="16-和并发编程的关系"><a href="#16-和并发编程的关系" class="headerlink" title="16. 和并发编程的关系"></a>16. 和并发编程的关系</h2><p>RAII 在并发里尤其重要，因为并发代码最怕“中途退出时资源没收干净”。</p>
<p>最常见的 RAII 对象：</p>
<ul>
<li><code>std::lock_guard</code></li>
<li><code>std::unique_lock</code></li>
<li><code>std::scoped_lock</code></li>
<li>线程 join guard</li>
</ul>
<p>并发里的基本原则可以记成一句：</p>
<blockquote>
<p>锁、线程、句柄这类资源，不要靠手动成对调用管理，优先对象化。</p>
</blockquote>
<hr>
<h2 id="17-一页总结"><a href="#17-一页总结" class="headerlink" title="17. 一页总结"></a>17. 一页总结</h2><p>RAII 的本质不是某个库技巧，而是一种设计原则：</p>
<ul>
<li>资源由对象接管</li>
<li>生命周期由作用域决定</li>
<li>释放在析构中自动完成</li>
</ul>
<p>智能指针则是这条原则在内存管理上的标准化实现：</p>
<ul>
<li><code>unique_ptr</code>：独占所有权，默认首选</li>
<li><code>shared_ptr</code>：共享所有权，按需使用</li>
<li><code>weak_ptr</code>：非拥有观察，打破循环</li>
</ul>
<p>如果只记三条经验：</p>
<ol>
<li>能不用动态分配就不用</li>
<li>动态分配优先 <code>unique_ptr</code></li>
<li>共享拥有关系才用 <code>shared_ptr</code></li>
</ol>
<hr>
<h2 id="18-建议继续补充的相关主题"><a href="#18-建议继续补充的相关主题" class="headerlink" title="18. 建议继续补充的相关主题"></a>18. 建议继续补充的相关主题</h2><p>和本篇衔接最紧密的内容：</p>
<ol>
<li>左右值、移动语义与 <code>std::move</code></li>
<li>自定义 allocator 与内存池</li>
<li><code>enable_shared_from_this</code></li>
<li><code>scope_exit</code> &#x2F; defer 风格工具</li>
<li>Rule of Five 与异常安全保证</li>
</ol>
<hr>
<h2 id="19-参考资料"><a href="#19-参考资料" class="headerlink" title="19. 参考资料"></a>19. 参考资料</h2><ol>
<li><p>cppreference: RAII<br><a href="https://en.cppreference.com/w/cpp/language/raii">https://en.cppreference.com/w/cpp/language/raii</a></p>
</li>
<li><p>cppreference: <code>std::unique_ptr</code><br><a href="https://en.cppreference.com/w/cpp/memory/unique_ptr">https://en.cppreference.com/w/cpp/memory/unique_ptr</a></p>
</li>
<li><p>cppreference: <code>std::shared_ptr</code><br><a href="https://en.cppreference.com/w/cpp/memory/shared_ptr">https://en.cppreference.com/w/cpp/memory/shared_ptr</a></p>
</li>
<li><p>cppreference: <code>std::weak_ptr</code><br><a href="https://en.cppreference.com/w/cpp/memory/weak_ptr">https://en.cppreference.com/w/cpp/memory/weak_ptr</a></p>
</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>左值、右值、`std::forward` 与完美转发笔记</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/03-%E5%B7%A6%E5%8F%B3%E5%80%BC-%E5%AE%8C%E7%BE%8E%E8%BD%AC%E5%8F%91%E4%B8%8E%E5%BC%95%E7%94%A8%E6%8A%98%E5%8F%A0/</url>
    <content><![CDATA[<h1 id="左值、右值、std-forward-与完美转发笔记"><a href="#左值、右值、std-forward-与完美转发笔记" class="headerlink" title="左值、右值、std::forward 与完美转发笔记"></a>左值、右值、<code>std::forward</code> 与完美转发笔记</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：左值、右值、移动语义、<code>std::move</code>、转发引用、引用折叠、<code>std::forward</code>、完美转发<br>核心目标：搞清楚“一个参数到底该拷贝、移动，还是原样转发”。</p>
</blockquote>
<hr>
<h2 id="1-这篇笔记解决什么问题"><a href="#1-这篇笔记解决什么问题" class="headerlink" title="1. 这篇笔记解决什么问题"></a>1. 这篇笔记解决什么问题</h2><p>C++ 模板和现代泛型代码里，最容易混乱的一组概念就是：</p>
<ul>
<li>左值 &#x2F; 右值</li>
<li>左值引用 &#x2F; 右值引用</li>
<li><code>std::move</code></li>
<li><code>std::forward</code></li>
<li>完美转发</li>
<li>引用折叠</li>
</ul>
<p>如果这几个点没有打通，就很容易出现这些问题：</p>
<ul>
<li>以为 <code>T&amp;&amp;</code> 一定是右值引用</li>
<li>以为 <code>std::move</code> 真的会“立即移动对象”</li>
<li>写了包装函数，却把右值错误地当成左值传下去了</li>
<li>用错 <code>std::forward</code>，导致重复移动或行为退化</li>
</ul>
<p>这篇笔记的主线是：</p>
<ol>
<li>先分清值类别</li>
<li>再理解引用类型</li>
<li>再理解移动语义</li>
<li>最后看 <code>std::forward</code> 和完美转发为什么成立</li>
</ol>
<hr>
<h2 id="2-什么是左值和右值"><a href="#2-什么是左值和右值" class="headerlink" title="2. 什么是左值和右值"></a>2. 什么是左值和右值</h2><h3 id="2-1-先用直觉理解"><a href="#2-1-先用直觉理解" class="headerlink" title="2.1 先用直觉理解"></a>2.1 先用直觉理解</h3><p>可以先粗略理解为：</p>
<ul>
<li><strong>左值</strong>：有身份、可定位、可被多次使用的对象</li>
<li><strong>右值</strong>：通常是临时结果，生命周期短，更适合被移动</li>
</ul>
<p>例子：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="type">int</span> b = a;      <span class="comment">// a 是左值</span></span><br><span class="line"><span class="type">int</span> c = a + <span class="number">1</span>;  <span class="comment">// a + 1 是右值</span></span><br></pre></td></tr></table></figure>

<p>这里：</p>
<ul>
<li><code>a</code> 有名字、有地址，通常是左值</li>
<li><code>a + 1</code> 是表达式求值结果，通常是右值</li>
</ul>
<h3 id="2-2-常见例子"><a href="#2-2-常见例子" class="headerlink" title="2.2 常见例子"></a>2.2 常见例子</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">x           <span class="comment">// 左值</span></span><br><span class="line">(x)         <span class="comment">// 左值</span></span><br><span class="line">std::<span class="built_in">move</span>(x) <span class="comment">// 右值中的 xvalue</span></span><br><span class="line"><span class="number">1</span>           <span class="comment">// 右值</span></span><br><span class="line">x + <span class="number">1</span>       <span class="comment">// 右值</span></span><br><span class="line"><span class="string">&quot;hello&quot;</span>     <span class="comment">// 字符串字面量是左值</span></span><br></pre></td></tr></table></figure>

<p>注意：很多入门资料喜欢说“能放在赋值号左边的是左值”，这个说法只能帮助建立最初直觉，<strong>不够严格</strong>。</p>
<h3 id="2-3-更准确的现代分类"><a href="#2-3-更准确的现代分类" class="headerlink" title="2.3 更准确的现代分类"></a>2.3 更准确的现代分类</h3><p>C++11 以后，值类别更完整地分成：</p>
<ul>
<li><code>lvalue</code></li>
<li><code>xvalue</code></li>
<li><code>prvalue</code></li>
</ul>
<p>再往上组合成：</p>
<ul>
<li><code>glvalue = lvalue + xvalue</code></li>
<li><code>rvalue = prvalue + xvalue</code></li>
</ul>
<p>复习时可以先抓住最重要的：</p>
<ul>
<li>普通有名字对象，大多是 <code>lvalue</code></li>
<li>临时值大多是 <code>prvalue</code></li>
<li><code>std::move(x)</code> 产生的是 <code>xvalue</code></li>
</ul>
<hr>
<h2 id="3-左值引用与右值引用"><a href="#3-左值引用与右值引用" class="headerlink" title="3. 左值引用与右值引用"></a>3. 左值引用与右值引用</h2><h3 id="3-1-左值引用-T"><a href="#3-1-左值引用-T" class="headerlink" title="3.1 左值引用 T&amp;"></a>3.1 左值引用 <code>T&amp;</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">10</span>;</span><br><span class="line"><span class="type">int</span>&amp; ref = x;</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li>必须绑定到左值</li>
<li>绑定后就是原对象的别名</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>&amp; a = x;   <span class="comment">// 对</span></span><br><span class="line"><span class="type">int</span>&amp; b = <span class="number">10</span>;  <span class="comment">// 错，普通左值引用不能绑定右值</span></span><br></pre></td></tr></table></figure>

<h3 id="3-2-const-T-很特殊"><a href="#3-2-const-T-很特殊" class="headerlink" title="3.2 const T&amp; 很特殊"></a>3.2 <code>const T&amp;</code> 很特殊</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span>&amp; a = <span class="number">10</span>; <span class="comment">// 对</span></span><br></pre></td></tr></table></figure>

<p><code>const T&amp;</code> 可以绑定：</p>
<ul>
<li>左值</li>
<li>右值</li>
<li>临时对象</li>
</ul>
<p>这也是它经常被用来做“只读传参”的原因。</p>
<h3 id="3-3-右值引用-T"><a href="#3-3-右值引用-T" class="headerlink" title="3.3 右值引用 T&amp;&amp;"></a>3.3 右值引用 <code>T&amp;&amp;</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>&amp;&amp; r = <span class="number">10</span>; <span class="comment">// 对</span></span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li>通常绑定右值</li>
<li>常用于移动语义</li>
<li>常用于表达“这个对象可以被偷资源”</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">10</span>;</span><br><span class="line"><span class="type">int</span>&amp;&amp; a = <span class="number">20</span>;          <span class="comment">// 对</span></span><br><span class="line"><span class="type">int</span>&amp;&amp; b = std::<span class="built_in">move</span>(x); <span class="comment">// 对</span></span><br><span class="line"><span class="type">int</span>&amp;&amp; c = x;           <span class="comment">// 错，x 是左值</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="4-一个容易忽略但极其重要的事实"><a href="#4-一个容易忽略但极其重要的事实" class="headerlink" title="4. 一个容易忽略但极其重要的事实"></a>4. 一个容易忽略但极其重要的事实</h2><blockquote>
<p>任何“有名字的变量”，即使它的类型是 <code>T&amp;&amp;</code>，表达式本身依然是左值。</p>
</blockquote>
<p>例子：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">(<span class="type">int</span>&amp;&amp; x)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// x 的类型是 int&amp;&amp;</span></span><br><span class="line">    <span class="comment">// 但表达式 x 是左值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这句话是理解 <code>std::forward</code> 和 <code>std::move</code> 的关键前提。<br>因为只要参数进入函数体，它就有名字了，于是直接写 <code>x</code> 时，它就是左值表达式。</p>
<hr>
<h2 id="5-为什么需要移动语义"><a href="#5-为什么需要移动语义" class="headerlink" title="5. 为什么需要移动语义"></a>5. 为什么需要移动语义</h2><p>在 C++11 之前，临时对象也只能拷贝，很多场景成本很高。</p>
<p>例如一个大对象：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::vector&lt;<span class="type">int</span>&gt; <span class="title">make_big_vector</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">int</span>&gt; <span class="title">v</span><span class="params">(<span class="number">1&#x27;000&#x27;000</span>, <span class="number">42</span>)</span></span>;</span><br><span class="line">    <span class="keyword">return</span> v;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果只能拷贝，那么返回 <code>v</code> 的代价很大。<br>移动语义的核心思想是：</p>
<ul>
<li>对于即将销毁的对象，不一定要深拷贝</li>
<li>可以“偷走”它内部持有的资源</li>
</ul>
<p>这就引出了移动构造和移动赋值。</p>
<hr>
<h2 id="6-std-move-到底做了什么"><a href="#6-std-move-到底做了什么" class="headerlink" title="6. std::move 到底做了什么"></a>6. <code>std::move</code> 到底做了什么</h2><h3 id="6-1-std-move-不是“移动”"><a href="#6-1-std-move-不是“移动”" class="headerlink" title="6.1 std::move 不是“移动”"></a>6.1 <code>std::move</code> 不是“移动”</h3><p>它本质上只是一个<strong>类型转换</strong>，把表达式转换成右值形式，告诉编译器：</p>
<blockquote>
<p>这个对象后面我不打算保留原语义了，可以把它当成可移动对象处理。</p>
</blockquote>
<p>例子：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line">std::string s = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line">std::string t = std::<span class="built_in">move</span>(s);</span><br></pre></td></tr></table></figure>

<p>这里真正发生移动的是：</p>
<ul>
<li><code>std::string</code> 的移动构造函数</li>
</ul>
<p>而不是 <code>std::move</code> 本身。</p>
<h3 id="6-2-可以把它理解成"><a href="#6-2-可以把它理解成" class="headerlink" title="6.2 可以把它理解成"></a>6.2 可以把它理解成</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::<span class="built_in">move</span>(x)</span><br></pre></td></tr></table></figure>

<p>约等于：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static_cast</span>&lt;std::<span class="type">remove_reference_t</span>&lt;<span class="keyword">decltype</span>(x)&gt;&amp;&amp;&gt;(x)</span><br></pre></td></tr></table></figure>

<p>也就是把 <code>x</code> 强制转成右值。</p>
<h3 id="6-3-被移动后的对象"><a href="#6-3-被移动后的对象" class="headerlink" title="6.3 被移动后的对象"></a>6.3 被移动后的对象</h3><p>被移动后的对象通常满足：</p>
<ul>
<li>仍然处于<strong>有效状态</strong></li>
<li>但值是<strong>未指定的</strong></li>
</ul>
<p>这意味着：</p>
<ul>
<li>可以析构</li>
<li>可以重新赋值</li>
<li>不应该假设它仍然保持原值</li>
</ul>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::string s = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line">std::string t = std::<span class="built_in">move</span>(s);</span><br><span class="line"></span><br><span class="line"><span class="comment">// s 依然有效，但内容不要依赖</span></span><br><span class="line">s = <span class="string">&quot;new value&quot;</span>; <span class="comment">// 可以</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="7-std-move-和拷贝-移动构造的关系"><a href="#7-std-move-和拷贝-移动构造的关系" class="headerlink" title="7. std::move 和拷贝 &#x2F; 移动构造的关系"></a>7. <code>std::move</code> 和拷贝 &#x2F; 移动构造的关系</h2><p>看一个最典型的重载：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">consume</span><span class="params">(<span class="type">const</span> std::string&amp; s)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;copy-style view: &quot;</span> &lt;&lt; s &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">consume</span><span class="params">(std::string&amp;&amp; s)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;move-style view: &quot;</span> &lt;&lt; s &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::string s = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">consume</span>(s);            <span class="comment">// 传左值</span></span><br><span class="line">    <span class="built_in">consume</span>(std::<span class="built_in">move</span>(s)); <span class="comment">// 传右值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里：</p>
<ul>
<li><code>consume(s)</code> 选中左值版本</li>
<li><code>consume(std::move(s))</code> 选中右值版本</li>
</ul>
<p>所以 <code>std::move</code> 更准确的作用是：</p>
<ul>
<li><strong>改变重载决议和构造&#x2F;赋值选择</strong></li>
</ul>
<hr>
<h2 id="8-转发引用是什么"><a href="#8-转发引用是什么" class="headerlink" title="8. 转发引用是什么"></a>8. 转发引用是什么</h2><h3 id="8-1-最经典定义"><a href="#8-1-最经典定义" class="headerlink" title="8.1 最经典定义"></a>8.1 最经典定义</h3><p>当且仅当满足下面条件时，<code>T&amp;&amp;</code> 才是<strong>转发引用</strong>：</p>
<ul>
<li>形式是 <code>T&amp;&amp;</code></li>
<li><code>T</code> 需要通过模板参数推导得到</li>
</ul>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">wrapper</span><span class="params">(T&amp;&amp; x)</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里的 <code>x</code> 是转发引用。</p>
<h3 id="8-2-为什么它特殊"><a href="#8-2-为什么它特殊" class="headerlink" title="8.2 为什么它特殊"></a>8.2 为什么它特殊</h3><p>因为它既能接左值，也能接右值：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="built_in">wrapper</span>(a);   <span class="comment">// T 推导为 int&amp;</span></span><br><span class="line"><span class="built_in">wrapper</span>(<span class="number">20</span>);  <span class="comment">// T 推导为 int</span></span><br></pre></td></tr></table></figure>

<p>于是：</p>
<ul>
<li>传左值时，<code>T = int&amp;</code></li>
<li>传右值时，<code>T = int</code></li>
</ul>
<p>这正是完美转发的前提。</p>
<h3 id="8-3-不是所有-都是转发引用"><a href="#8-3-不是所有-都是转发引用" class="headerlink" title="8.3 不是所有 &amp;&amp; 都是转发引用"></a>8.3 不是所有 <code>&amp;&amp;</code> 都是转发引用</h3><p>下面这些都<strong>不是</strong>转发引用：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">(<span class="type">int</span>&amp;&amp; x)</span></span>;          <span class="comment">// 不是，T 没有推导</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">g</span><span class="params">(std::vector&lt;T&gt;&amp;&amp; x)</span></span>; <span class="comment">// 这里参数整体不是 T&amp;&amp;，不是转发引用</span></span><br></pre></td></tr></table></figure>

<p>类模板中也常见这个坑：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Box</span> &#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">set</span><span class="params">(T&amp;&amp; x)</span></span>; <span class="comment">// 这里通常不是转发引用</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>因为这里的 <code>T</code> 是类模板参数，在成员函数处并不是靠这次调用来推导。</p>
<h3 id="8-4-auto-也常常是转发引用"><a href="#8-4-auto-也常常是转发引用" class="headerlink" title="8.4 auto&amp;&amp; 也常常是转发引用"></a>8.4 <code>auto&amp;&amp;</code> 也常常是转发引用</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span>&amp;&amp; x = expr;</span><br></pre></td></tr></table></figure>

<p>当 <code>auto</code> 发生推导时，<code>auto&amp;&amp;</code> 也会表现出类似转发引用的特性。</p>
<hr>
<h2 id="9-引用折叠"><a href="#9-引用折叠" class="headerlink" title="9. 引用折叠"></a>9. 引用折叠</h2><p>引用折叠是解释“为什么 <code>T&amp;&amp;</code> 能同时接左值和右值”的关键规则。</p>
<h3 id="9-1-四条规则"><a href="#9-1-四条规则" class="headerlink" title="9.1 四条规则"></a>9.1 四条规则</h3><table>
<thead>
<tr>
<th>写出来的形式</th>
<th>折叠结果</th>
</tr>
</thead>
<tbody><tr>
<td><code>T&amp; &amp;</code></td>
<td><code>T&amp;</code></td>
</tr>
<tr>
<td><code>T&amp; &amp;&amp;</code></td>
<td><code>T&amp;</code></td>
</tr>
<tr>
<td><code>T&amp;&amp; &amp;</code></td>
<td><code>T&amp;</code></td>
</tr>
<tr>
<td><code>T&amp;&amp; &amp;&amp;</code></td>
<td><code>T&amp;&amp;</code></td>
</tr>
</tbody></table>
<p>可以直接记成一句：</p>
<blockquote>
<p>只要有左值引用参与折叠，结果就是左值引用；只有全是右值引用时，结果才是右值引用。</p>
</blockquote>
<h3 id="9-2-为什么-wrapper-a-能成立"><a href="#9-2-为什么-wrapper-a-能成立" class="headerlink" title="9.2 为什么 wrapper(a) 能成立"></a>9.2 为什么 <code>wrapper(a)</code> 能成立</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">wrapper</span><span class="params">(T&amp;&amp; x)</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="built_in">wrapper</span>(a);</span><br></pre></td></tr></table></figure>

<p>当传入左值 <code>a</code> 时：</p>
<ul>
<li><code>T</code> 推导为 <code>int&amp;</code></li>
<li>于是参数类型变成 <code>int&amp; &amp;&amp;</code></li>
<li>根据引用折叠规则，最终变成 <code>int&amp;</code></li>
</ul>
<p>所以左值能匹配成功。</p>
<h3 id="9-3-传右值时"><a href="#9-3-传右值时" class="headerlink" title="9.3 传右值时"></a>9.3 传右值时</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">wrapper</span>(<span class="number">10</span>);</span><br></pre></td></tr></table></figure>

<p>此时：</p>
<ul>
<li><code>T</code> 推导为 <code>int</code></li>
<li>参数类型是 <code>int&amp;&amp;</code></li>
</ul>
<p>于是右值版本也能成立。</p>
<hr>
<h2 id="10-为什么需要-std-forward"><a href="#10-为什么需要-std-forward" class="headerlink" title="10. 为什么需要 std::forward"></a>10. 为什么需要 <code>std::forward</code></h2><p>看一个最典型的包装函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">process</span><span class="params">(<span class="type">int</span>&amp; x)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;lvalue\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">process</span><span class="params">(<span class="type">int</span>&amp;&amp; x)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;rvalue\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">wrapper</span><span class="params">(T&amp;&amp; arg)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">process</span>(arg); <span class="comment">// 有问题</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>调用：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="built_in">wrapper</span>(a);</span><br><span class="line"><span class="built_in">wrapper</span>(<span class="number">2</span>);</span><br></pre></td></tr></table></figure>

<p>结果会是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">lvalue</span><br><span class="line">lvalue</span><br></pre></td></tr></table></figure>

<p>原因不是模板坏了，而是：</p>
<ul>
<li><code>arg</code> 在函数体里有名字</li>
<li>所以表达式 <code>arg</code> 永远是左值</li>
</ul>
<p>这就导致即使最初传进来的是右值，传给 <code>process</code> 时也退化成了左值。</p>
<hr>
<h2 id="11-std-forward-如何保留值类别"><a href="#11-std-forward-如何保留值类别" class="headerlink" title="11. std::forward 如何保留值类别"></a>11. <code>std::forward</code> 如何保留值类别</h2><p>正确写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">process</span><span class="params">(<span class="type">int</span>&amp; x)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;lvalue\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">process</span><span class="params">(<span class="type">int</span>&amp;&amp; x)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;rvalue\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">wrapper</span><span class="params">(T&amp;&amp; arg)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">process</span>(std::forward&lt;T&gt;(arg));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">wrapper</span>(a);  <span class="comment">// lvalue</span></span><br><span class="line">    <span class="built_in">wrapper</span>(<span class="number">2</span>);  <span class="comment">// rvalue</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里：</p>
<ul>
<li>如果 <code>T = int&amp;</code>，那么 <code>std::forward&lt;T&gt;(arg)</code> 最终是左值</li>
<li>如果 <code>T = int</code>，那么 <code>std::forward&lt;T&gt;(arg)</code> 最终是右值</li>
</ul>
<p>所以 <code>std::forward</code> 的本质是：</p>
<ul>
<li><strong>按模板参数 <code>T</code> 原样恢复调用点的值类别</strong></li>
</ul>
<p>这就是“转发”的含义。</p>
<hr>
<h2 id="12-完美转发是什么"><a href="#12-完美转发是什么" class="headerlink" title="12. 完美转发是什么"></a>12. 完美转发是什么</h2><p>所谓完美转发，就是：</p>
<blockquote>
<p>在包装层不改变实参的左值&#x2F;右值属性、不额外引入拷贝或错误移动，把参数继续传给下游函数。</p>
</blockquote>
<p>标准写法几乎总是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">wrapper</span><span class="params">(T&amp;&amp; arg)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">target</span>(std::forward&lt;T&gt;(arg));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>多个参数时：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span>... Ts&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">wrapper</span><span class="params">(Ts&amp;&amp;... args)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">target</span>(std::forward&lt;Ts&gt;(args)...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这就是最经典的完美转发模板。</p>
<hr>
<h2 id="13-std-forward-和-std-move-的区别"><a href="#13-std-forward-和-std-move-的区别" class="headerlink" title="13. std::forward 和 std::move 的区别"></a>13. <code>std::forward</code> 和 <code>std::move</code> 的区别</h2><table>
<thead>
<tr>
<th>工具</th>
<th>作用</th>
<th>典型用途</th>
</tr>
</thead>
<tbody><tr>
<td><code>std::move(x)</code></td>
<td>无条件把 <code>x</code> 转成右值</td>
<td>明确表示“可以移动”</td>
</tr>
<tr>
<td><code>std::forward&lt;T&gt;(x)</code></td>
<td>按 <code>T</code> 的推导结果恢复原值类别</td>
<td>转发引用场景下继续传参</td>
</tr>
</tbody></table>
<p>一句话记忆：</p>
<ul>
<li><code>move</code> 是“我决定把它当右值”</li>
<li><code>forward</code> 是“我保持它原本是什么就还是什么”</li>
</ul>
<h3 id="13-1-什么时候用-move"><a href="#13-1-什么时候用-move" class="headerlink" title="13.1 什么时候用 move"></a>13.1 什么时候用 <code>move</code></h3><p>当你明确知道：</p>
<ul>
<li>这个对象后面不再按原语义使用</li>
<li>想把资源交出去</li>
</ul>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::string s = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line">vec.<span class="built_in">push_back</span>(std::<span class="built_in">move</span>(s));</span><br></pre></td></tr></table></figure>

<h3 id="13-2-什么时候用-forward"><a href="#13-2-什么时候用-forward" class="headerlink" title="13.2 什么时候用 forward"></a>13.2 什么时候用 <code>forward</code></h3><p>当你写的是包装层、工厂函数、泛型接口，需要把参数继续传下去，并保留调用者传入时的左值&#x2F;右值属性。</p>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">relay</span><span class="params">(T&amp;&amp; x)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">consume</span>(std::forward&lt;T&gt;(x));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="14-一个最重要的工厂函数例子"><a href="#14-一个最重要的工厂函数例子" class="headerlink" title="14. 一个最重要的工厂函数例子"></a>14. 一个最重要的工厂函数例子</h2><h3 id="14-1-错误或退化的版本"><a href="#14-1-错误或退化的版本" class="headerlink" title="14.1 错误或退化的版本"></a>14.1 错误或退化的版本</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">Arg</span>&gt;</span><br><span class="line"><span class="function">std::unique_ptr&lt;T&gt; <span class="title">make_obj_bad</span><span class="params">(Arg arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> std::<span class="built_in">make_unique</span>&lt;T&gt;(arg);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>问题：</p>
<ul>
<li>参数被按值接收，已经发生了一次拷贝或移动</li>
<li>无法保留调用点的值类别</li>
</ul>
<h3 id="14-2-正确的完美转发版本"><a href="#14-2-正确的完美转发版本" class="headerlink" title="14.2 正确的完美转发版本"></a>14.2 正确的完美转发版本</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span>... Args&gt;</span><br><span class="line"><span class="function">std::unique_ptr&lt;T&gt; <span class="title">make_obj</span><span class="params">(Args&amp;&amp;... args)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> std::<span class="built_in">make_unique</span>&lt;T&gt;(std::forward&lt;Args&gt;(args)...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这类写法在标准库里非常常见，例如：</p>
<ul>
<li><code>std::make_unique</code></li>
<li><code>std::make_shared</code></li>
<li>各种 <code>emplace</code></li>
</ul>
<hr>
<h2 id="15-emplace-back-为什么离不开完美转发"><a href="#15-emplace-back-为什么离不开完美转发" class="headerlink" title="15. emplace_back 为什么离不开完美转发"></a>15. <code>emplace_back</code> 为什么离不开完美转发</h2><p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;std::string&gt; v;</span><br><span class="line">v.<span class="built_in">emplace_back</span>(<span class="string">&quot;hello&quot;</span>);</span><br></pre></td></tr></table></figure>

<p><code>emplace_back</code> 的核心价值不是“语法更短”，而是：</p>
<ul>
<li>直接在容器内部构造对象</li>
<li>参数通过完美转发传给元素构造函数</li>
</ul>
<p>可以把它粗略理解成：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span>... Args&gt;</span><br><span class="line"><span class="function">reference <span class="title">emplace_back</span><span class="params">(Args&amp;&amp;... args)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 在尾部原地构造 T(std::forward&lt;Args&gt;(args)...)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>也就是说：</p>
<ul>
<li>左值实参按左值传进去</li>
<li>右值实参按右值传进去</li>
</ul>
<hr>
<h2 id="16-std-forward-的直觉实现"><a href="#16-std-forward-的直觉实现" class="headerlink" title="16. std::forward 的直觉实现"></a>16. <code>std::forward</code> 的直觉实现</h2><p>可以先用一种“帮助理解”的方式记它：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function">T&amp;&amp; <span class="title">forward</span><span class="params">(std::<span class="type">remove_reference_t</span>&lt;T&gt;&amp; arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">static_cast</span>&lt;T&amp;&amp;&gt;(arg);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>标准库实现会更严谨，通常还会提供多个重载并处理误用问题，但核心思想就是：</p>
<ul>
<li>先拿到一个引用</li>
<li>再按 <code>T&amp;&amp;</code> 转回去</li>
<li>借助引用折叠恢复正确类别</li>
</ul>
<p>所以 <code>forward</code> 能工作，不是因为它“会智能判断”，而是因为：</p>
<ul>
<li>模板推导已经把信息保存在 <code>T</code> 里了</li>
</ul>
<hr>
<h2 id="17-为什么说“完美转发”离不开引用折叠"><a href="#17-为什么说“完美转发”离不开引用折叠" class="headerlink" title="17. 为什么说“完美转发”离不开引用折叠"></a>17. 为什么说“完美转发”离不开引用折叠</h2><p>还是看：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">wrapper</span><span class="params">(T&amp;&amp; arg)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">target</span>(std::forward&lt;T&gt;(arg));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果传左值：</p>
<ul>
<li><code>T = U&amp;</code></li>
<li><code>T&amp;&amp; = U&amp; &amp;&amp;</code></li>
<li>折叠后为 <code>U&amp;</code></li>
<li><code>forward&lt;T&gt;(arg)</code> 返回左值</li>
</ul>
<p>如果传右值：</p>
<ul>
<li><code>T = U</code></li>
<li><code>T&amp;&amp; = U&amp;&amp;</code></li>
<li><code>forward&lt;T&gt;(arg)</code> 返回右值</li>
</ul>
<p>所以：</p>
<ul>
<li><strong>转发引用负责“接住任何值类别”</strong></li>
<li><strong>引用折叠负责“把最终类型算对”</strong></li>
<li><strong><code>std::forward</code> 负责“把原值类别恢复出来”</strong></li>
</ul>
<p>这三者必须连起来理解。</p>
<hr>
<h2 id="18-常见坑"><a href="#18-常见坑" class="headerlink" title="18. 常见坑"></a>18. 常见坑</h2><h3 id="18-1-不是所有-T-都要-std-forward"><a href="#18-1-不是所有-T-都要-std-forward" class="headerlink" title="18.1 不是所有 T&amp;&amp; 都要 std::forward"></a>18.1 不是所有 <code>T&amp;&amp;</code> 都要 <code>std::forward</code></h3><p>只有当它是<strong>转发引用</strong>，且你要把它继续传递给下游时，才需要 <code>std::forward&lt;T&gt;(x)</code>。</p>
<p>如果你是普通右值引用参数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">(std::string&amp;&amp; s)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">use</span>(std::<span class="built_in">move</span>(s)); <span class="comment">// 通常更合理</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里没有模板推导得到的 <code>T</code>，所以谈不上“恢复原值类别”，更常见的是显式 <code>move</code>。</p>
<h3 id="18-2-不要对同一个对象重复-forward-move"><a href="#18-2-不要对同一个对象重复-forward-move" class="headerlink" title="18.2 不要对同一个对象重复 forward &#x2F; move"></a>18.2 不要对同一个对象重复 <code>forward</code> &#x2F; <code>move</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">target</span>(std::forward&lt;T&gt;(arg));</span><br><span class="line"><span class="built_in">target</span>(std::forward&lt;T&gt;(arg)); <span class="comment">// 危险</span></span><br></pre></td></tr></table></figure>

<p>如果第一次已经把它作为右值传出，第二次再用通常就不合理了。</p>
<h3 id="18-3-const-会限制移动"><a href="#18-3-const-会限制移动" class="headerlink" title="18.3 const 会限制移动"></a>18.3 <code>const</code> 会限制移动</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> std::string s = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line"><span class="keyword">auto</span> t = std::<span class="built_in">move</span>(s); <span class="comment">// 通常会退化成拷贝</span></span><br></pre></td></tr></table></figure>

<p>因为移动通常需要修改源对象内部状态，而 <code>const</code> 对象不允许。</p>
<p>所以经验上：</p>
<ul>
<li>想要可移动，就不要随手把对象声明成 <code>const</code></li>
</ul>
<h3 id="18-4-const-T-很少用"><a href="#18-4-const-T-很少用" class="headerlink" title="18.4 const T&amp;&amp; 很少用"></a>18.4 <code>const T&amp;&amp;</code> 很少用</h3><p>它既不像 <code>const T&amp;</code> 那样通用，也不适合作为可移动对象接口。<br>绝大多数场景里，它都不是你想要的东西。</p>
<h3 id="18-5-命名后的右值引用变量仍然是左值"><a href="#18-5-命名后的右值引用变量仍然是左值" class="headerlink" title="18.5 命名后的右值引用变量仍然是左值"></a>18.5 命名后的右值引用变量仍然是左值</h3><p>这个坑值得再强调一次：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">(std::string&amp;&amp; s)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">g</span>(s);            <span class="comment">// s 是左值</span></span><br><span class="line">    <span class="built_in">g</span>(std::<span class="built_in">move</span>(s)); <span class="comment">// 才是右值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="18-6-完美转发不是“性能银弹”"><a href="#18-6-完美转发不是“性能银弹”" class="headerlink" title="18.6 完美转发不是“性能银弹”"></a>18.6 完美转发不是“性能银弹”</h3><p>它的价值是：</p>
<ul>
<li>保留值类别</li>
<li>避免不必要拷贝</li>
</ul>
<p>但如果接口设计本身不合理，或者对象很小、移动和拷贝差不多，那么完美转发不一定有明显收益。</p>
<hr>
<h2 id="19-面试和复习高频问题"><a href="#19-面试和复习高频问题" class="headerlink" title="19. 面试和复习高频问题"></a>19. 面试和复习高频问题</h2><h3 id="Q1：std-move-会真的移动对象吗？"><a href="#Q1：std-move-会真的移动对象吗？" class="headerlink" title="Q1：std::move 会真的移动对象吗？"></a>Q1：<code>std::move</code> 会真的移动对象吗？</h3><p>不会。<br>它只是把对象转换成右值形式，真正是否发生移动取决于后续是否匹配到移动构造 &#x2F; 移动赋值 &#x2F; 右值重载。</p>
<h3 id="Q2：为什么-T-能同时接左值和右值？"><a href="#Q2：为什么-T-能同时接左值和右值？" class="headerlink" title="Q2：为什么 T&amp;&amp; 能同时接左值和右值？"></a>Q2：为什么 <code>T&amp;&amp;</code> 能同时接左值和右值？</h3><p>因为当 <code>T</code> 通过模板推导得到时，它是转发引用；传左值时 <code>T</code> 会推导成引用类型，再通过引用折叠得到左值引用。</p>
<h3 id="Q3：std-forward-和-std-move-最大区别是什么？"><a href="#Q3：std-forward-和-std-move-最大区别是什么？" class="headerlink" title="Q3：std::forward 和 std::move 最大区别是什么？"></a>Q3：<code>std::forward</code> 和 <code>std::move</code> 最大区别是什么？</h3><ul>
<li><code>move</code>：无条件转右值</li>
<li><code>forward</code>：按模板参数恢复原值类别</li>
</ul>
<h3 id="Q4：为什么包装函数里直接-target-arg-不行？"><a href="#Q4：为什么包装函数里直接-target-arg-不行？" class="headerlink" title="Q4：为什么包装函数里直接 target(arg) 不行？"></a>Q4：为什么包装函数里直接 <code>target(arg)</code> 不行？</h3><p>因为 <code>arg</code> 有名字，所以它是左值表达式，右值信息丢了。</p>
<h3 id="Q5：为什么-emplace-back-常和完美转发一起出现？"><a href="#Q5：为什么-emplace-back-常和完美转发一起出现？" class="headerlink" title="Q5：为什么 emplace_back 常和完美转发一起出现？"></a>Q5：为什么 <code>emplace_back</code> 常和完美转发一起出现？</h3><p>因为它需要把用户传入的构造参数原样传给元素构造函数，同时避免不必要的中间对象。</p>
<hr>
<h2 id="20-一页总结"><a href="#20-一页总结" class="headerlink" title="20. 一页总结"></a>20. 一页总结</h2><p>把这篇笔记压成最短记忆链，可以记成下面几句：</p>
<ol>
<li>左值有身份，右值更像临时结果</li>
<li><code>T&amp;</code> 绑左值，<code>const T&amp;</code> 几乎都能绑，<code>T&amp;&amp;</code> 通常绑右值</li>
<li>命名后的 <code>T&amp;&amp;</code> 变量，表达式本身仍然是左值</li>
<li><code>std::move</code> 是把对象强制转成右值，不等于真的移动</li>
<li>当 <code>T&amp;&amp;</code> 中的 <code>T</code> 需要推导时，它是转发引用</li>
<li>完美转发的标准写法是 <code>std::forward&lt;T&gt;(arg)</code></li>
<li>引用折叠的记忆口诀：只要有左值引用参与，结果就是左值引用</li>
</ol>
<p>如果只背一个代码模板，就背这个：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span>... Args&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">wrapper</span><span class="params">(Args&amp;&amp;... args)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">target</span>(std::forward&lt;Args&gt;(args)...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="21-建议继续补充的相关主题"><a href="#21-建议继续补充的相关主题" class="headerlink" title="21. 建议继续补充的相关主题"></a>21. 建议继续补充的相关主题</h2><p>和这篇衔接最紧密的内容：</p>
<ol>
<li>移动构造、移动赋值、Rule of Five</li>
<li><code>noexcept move</code> 为什么影响容器扩容策略</li>
<li><code>decltype(auto)</code>、返回值类别与转发返回</li>
<li><code>std::invoke</code>、<code>std::apply</code> 与泛型调用包装</li>
<li>C++23 的 <code>std::forward_like</code></li>
</ol>
<hr>
<h2 id="22-参考资料"><a href="#22-参考资料" class="headerlink" title="22. 参考资料"></a>22. 参考资料</h2><ol>
<li><p>cppreference: value categories<br><a href="https://en.cppreference.com/w/cpp/language/value_category">https://en.cppreference.com/w/cpp/language/value_category</a></p>
</li>
<li><p>cppreference: reference<br><a href="https://en.cppreference.com/w/cpp/language/reference">https://en.cppreference.com/w/cpp/language/reference</a></p>
</li>
<li><p>cppreference: <code>std::move</code><br><a href="https://en.cppreference.com/w/cpp/utility/move">https://en.cppreference.com/w/cpp/utility/move</a></p>
</li>
<li><p>cppreference: <code>std::forward</code><br><a href="https://en.cppreference.com/w/cpp/utility/forward">https://en.cppreference.com/w/cpp/utility/forward</a></p>
</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>C++ 进阶编程：模板与元编程笔记</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/04-%E6%A8%A1%E6%9D%BF%E4%B8%8E%E5%85%83%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="C-进阶编程：模板与元编程笔记"><a href="#C-进阶编程：模板与元编程笔记" class="headerlink" title="C++ 进阶编程：模板与元编程笔记"></a>C++ 进阶编程：模板与元编程笔记</h1><p>时间：2025&#x2F;12&#x2F;16</p>
<blockquote>
<p>关键词：泛型编程、编译期计算、零成本抽象、类型推导、约束、类型萃取<br>核心目标：把“同一套逻辑支持多种类型”这件事交给编译器完成。</p>
</blockquote>
<hr>
<h2 id="1-模板到底在解决什么问题"><a href="#1-模板到底在解决什么问题" class="headerlink" title="1. 模板到底在解决什么问题"></a>1. 模板到底在解决什么问题</h2><p>如果没有模板，很多函数只能为不同类型重复写多份：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">twice</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123; <span class="keyword">return</span> x * <span class="number">2</span>; &#125;</span><br><span class="line"><span class="function"><span class="type">float</span> <span class="title">twice</span><span class="params">(<span class="type">float</span> x)</span> </span>&#123; <span class="keyword">return</span> x * <span class="number">2</span>; &#125;</span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">twice</span><span class="params">(<span class="type">double</span> x)</span> </span>&#123; <span class="keyword">return</span> x * <span class="number">2</span>; &#125;</span><br></pre></td></tr></table></figure>

<p>模板的作用就是把“变化的类型”抽象成参数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function">T <span class="title">twice</span><span class="params">(T x)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x * <span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样带来的好处：</p>
<ul>
<li>一份代码可适配多种类型</li>
<li>类型检查发生在编译期</li>
<li>通常没有运行时额外开销</li>
<li>很适合高性能场景中的“零成本抽象”</li>
</ul>
<hr>
<h2 id="2-函数模板"><a href="#2-函数模板" class="headerlink" title="2. 函数模板"></a>2. 函数模板</h2><h3 id="2-1-最基本写法"><a href="#2-1-最基本写法" class="headerlink" title="2.1 最基本写法"></a>2.1 最基本写法</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function">T <span class="title">twice</span><span class="params">(T x)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x * <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">twice</span>(<span class="number">2</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;      <span class="comment">// T 推导为 int</span></span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">twice</span>(<span class="number">3.5</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;    <span class="comment">// T 推导为 double</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>class</code> 和 <code>typename</code> 在模板类型参数里通常等价：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function">T <span class="title">twice</span><span class="params">(T x)</span> </span>&#123; <span class="keyword">return</span> x * <span class="number">2</span>; &#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-2-显式指定模板参数"><a href="#2-2-显式指定模板参数" class="headerlink" title="2.2 显式指定模板参数"></a>2.2 显式指定模板参数</h3><p>有时可以手动指定：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::cout &lt;&lt; <span class="built_in">twice</span>&lt;<span class="type">int</span>&gt;(<span class="number">2</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br></pre></td></tr></table></figure>

<p>但绝大多数情况下，函数模板都可以依靠参数自动推导。</p>
<h3 id="2-3-模板不是“自动重载一切”"><a href="#2-3-模板不是“自动重载一切”" class="headerlink" title="2.3 模板不是“自动重载一切”"></a>2.3 模板不是“自动重载一切”</h3><p>模板函数会参与重载决议，但它不是“万能匹配”。<br>如果你还写了一个普通函数，编译器会按重载规则选择更合适的版本：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function">T <span class="title">twice</span><span class="params">(T x)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x * <span class="number">2</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">std::string <span class="title">twice</span><span class="params">(<span class="type">const</span> std::string&amp; s)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> s + s;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">twice</span>(<span class="number">10</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">twice</span>(std::<span class="built_in">string</span>(<span class="string">&quot;hi&quot;</span>)) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里字符串版本不是“模板自动生成”的，而是我们手动提供了一个更合适的重载。</p>
<hr>
<h2 id="3-类模板"><a href="#3-类模板" class="headerlink" title="3. 类模板"></a>3. 类模板</h2><p>函数可以模板化，类也可以：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Box</span> &#123;</span><br><span class="line">    T value;</span><br><span class="line"></span><br><span class="line">    <span class="function">T <span class="title">get</span><span class="params">()</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> value;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Box&lt;<span class="type">int</span>&gt; a&#123;<span class="number">42</span>&#125;;</span><br><span class="line">    Box&lt;<span class="type">double</span>&gt; b&#123;<span class="number">3.14</span>&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>类模板常见用途：</p>
<ul>
<li>容器：<code>std::vector&lt;T&gt;</code></li>
<li>智能指针：<code>std::unique_ptr&lt;T&gt;</code></li>
<li>泛型工具类：<code>std::optional&lt;T&gt;</code>、<code>std::function&lt;T&gt;</code></li>
</ul>
<hr>
<h2 id="4-非类型模板参数"><a href="#4-非类型模板参数" class="headerlink" title="4. 非类型模板参数"></a>4. 非类型模板参数</h2><p>模板参数不一定是类型，也可以是编译期常量。</p>
<h3 id="4-1-经典写法"><a href="#4-1-经典写法" class="headerlink" title="4.1 经典写法"></a>4.1 经典写法</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="type">int</span> N&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">show_times</span><span class="params">(<span class="type">const</span> std::string&amp; msg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; ++i) &#123;</span><br><span class="line">        std::cout &lt;&lt; msg &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">show_times</span>&lt;<span class="number">3</span>&gt;(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里的 <code>N</code> 在编译期就已经确定。</p>
<h3 id="4-2-用编译期布尔值控制分支"><a href="#4-2-用编译期布尔值控制分支" class="headerlink" title="4.2 用编译期布尔值控制分支"></a>4.2 用编译期布尔值控制分支</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="type">bool</span> Debug&gt;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">sum_to</span><span class="params">(<span class="type">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> res = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; ++i) &#123;</span><br><span class="line">        res += i;</span><br><span class="line">        <span class="function"><span class="keyword">if</span> <span class="title">constexpr</span> <span class="params">(Debug)</span> </span>&#123;</span><br><span class="line">            std::cout &lt;&lt; <span class="string">&quot;i=&quot;</span> &lt;&lt; i &lt;&lt; <span class="string">&quot;, res=&quot;</span> &lt;&lt; res &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">sum_to</span>&lt;<span class="literal">false</span>&gt;(<span class="number">10</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>if constexpr</code> 的含义是：</p>
<ul>
<li>条件在编译期判断</li>
<li>不满足条件的分支会被丢弃</li>
<li>很适合模板里的静态分发</li>
</ul>
<h3 id="4-3-现代-C-的扩展"><a href="#4-3-现代-C-的扩展" class="headerlink" title="4.3 现代 C++ 的扩展"></a>4.3 现代 C++ 的扩展</h3><p>C++17 起可以写：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">auto</span> N&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">ConstValue</span> &#123;&#125;;</span><br></pre></td></tr></table></figure>

<p>也就是让非类型模板参数的类型由编译器推导。</p>
<hr>
<h2 id="5-模板参数推导与-auto"><a href="#5-模板参数推导与-auto" class="headerlink" title="5. 模板参数推导与 auto"></a>5. 模板参数推导与 <code>auto</code></h2><p>模板学习里最容易混乱的其实是“类型怎么被推导出来”。</p>
<h3 id="5-1-值传递"><a href="#5-1-值传递" class="headerlink" title="5.1 值传递"></a>5.1 值传递</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">(T x)</span> </span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<p>如果传入：</p>
<ul>
<li><code>int</code>，则 <code>T = int</code></li>
<li><code>const int</code>，顶层 <code>const</code> 会被忽略，通常还是 <code>T = int</code></li>
<li>引用也会退化为值</li>
</ul>
<h3 id="5-2-左值引用"><a href="#5-2-左值引用" class="headerlink" title="5.2 左值引用"></a>5.2 左值引用</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">(T&amp; x)</span> </span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<p>此时：</p>
<ul>
<li>传左值可以</li>
<li><code>const</code> 属性会被保留</li>
</ul>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="built_in">f</span>(a); <span class="comment">// T = const int</span></span><br></pre></td></tr></table></figure>

<h3 id="5-3-万能引用-转发引用"><a href="#5-3-万能引用-转发引用" class="headerlink" title="5.3 万能引用 &#x2F; 转发引用"></a>5.3 万能引用 &#x2F; 转发引用</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">(T&amp;&amp; x)</span> </span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<p>当 <code>T</code> 需要推导且参数形式为 <code>T&amp;&amp;</code> 时，它是<strong>转发引用</strong>：</p>
<ul>
<li>传右值时，<code>T</code> 推导为普通类型</li>
<li>传左值时，<code>T</code> 推导为左值引用</li>
</ul>
<p>这是完美转发的基础。</p>
<h3 id="5-4-auto-的推导规则和模板很像"><a href="#5-4-auto-的推导规则和模板很像" class="headerlink" title="5.4 auto 的推导规则和模板很像"></a>5.4 <code>auto</code> 的推导规则和模板很像</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> x = <span class="number">1</span>;        <span class="comment">// int</span></span><br><span class="line"><span class="type">const</span> <span class="keyword">auto</span> y = x;  <span class="comment">// const int</span></span><br><span class="line"><span class="keyword">auto</span>&amp; z = y;       <span class="comment">// const int&amp;</span></span><br></pre></td></tr></table></figure>

<p>可以粗略理解为：</p>
<ul>
<li><code>auto</code> 很像“让编译器帮你写模板参数推导”</li>
<li><code>decltype(expr)</code> 则是“精确获取表达式类型”</li>
</ul>
<hr>
<h2 id="6-函数对象与-lambda"><a href="#6-函数对象与-lambda" class="headerlink" title="6. 函数对象与 lambda"></a>6. 函数对象与 lambda</h2><p>模板经常和“可调用对象”配合使用。</p>
<h3 id="6-1-函数对象"><a href="#6-1-函数对象" class="headerlink" title="6.1 函数对象"></a>6.1 函数对象</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Printer</span> &#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span> x)</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; x &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Func</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">call_twice</span><span class="params">(Func func)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">func</span>(<span class="number">0</span>);</span><br><span class="line">    <span class="built_in">func</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">call_twice</span>(Printer&#123;&#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>Func</code> 可以是：</p>
<ul>
<li>普通函数指针</li>
<li>仿函数对象</li>
<li>lambda</li>
</ul>
<h3 id="6-2-lambda-本质上也是对象"><a href="#6-2-lambda-本质上也是对象" class="headerlink" title="6.2 lambda 本质上也是对象"></a>6.2 lambda 本质上也是对象</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Func</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">call_twice</span><span class="params">(<span class="type">const</span> Func&amp; func)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func</span>(<span class="number">1</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func</span>(<span class="number">2</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">make_times</span><span class="params">(<span class="type">int</span> factor)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> [=](<span class="type">int</span> n) &#123;</span><br><span class="line">        <span class="keyword">return</span> n * factor;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> twice = <span class="built_in">make_times</span>(<span class="number">2</span>);</span><br><span class="line">    <span class="built_in">call_twice</span>(twice);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-3-捕获方式"><a href="#6-3-捕获方式" class="headerlink" title="6.3 捕获方式"></a>6.3 捕获方式</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">[&amp;]  <span class="comment">// 按引用捕获</span></span><br><span class="line">[=]  <span class="comment">// 按值捕获</span></span><br><span class="line">[x]  <span class="comment">// 只捕获 x</span></span><br><span class="line">[&amp;x] <span class="comment">// 按引用捕获 x</span></span><br></pre></td></tr></table></figure>

<p>注意：</p>
<ul>
<li>按引用捕获要注意生命周期</li>
<li>闭包对象会保存按值捕获的副本</li>
</ul>
<h3 id="6-4-泛型-lambda"><a href="#6-4-泛型-lambda" class="headerlink" title="6.4 泛型 lambda"></a>6.4 泛型 lambda</h3><p>C++14 起，lambda 参数可以写 <code>auto</code>：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> print = [](<span class="type">const</span> <span class="keyword">auto</span>&amp; x) &#123;</span><br><span class="line">    std::cout &lt;&lt; x &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这相当于编译器帮我们生成了一个带模板 <code>operator()</code> 的闭包类型。</p>
<hr>
<h2 id="7-if-constexpr：模板时代码分支的关键工具"><a href="#7-if-constexpr：模板时代码分支的关键工具" class="headerlink" title="7. if constexpr：模板时代码分支的关键工具"></a>7. <code>if constexpr</code>：模板时代码分支的关键工具</h2><p>模板中经常需要根据类型走不同逻辑。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print_info</span><span class="params">(<span class="type">const</span> T&amp; x)</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">if</span> <span class="title">constexpr</span> <span class="params">(std::is_integral_v&lt;T&gt;)</span> </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;integral: &quot;</span> &lt;&lt; x &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> <span class="built_in">constexpr</span> (std::is_floating_point_v&lt;T&gt;) &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;floating: &quot;</span> &lt;&lt; x &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;other type\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>相比普通 <code>if</code>，<code>if constexpr</code> 在模板里更重要，因为它可以彻底丢弃无效分支，避免编译错误。</p>
<hr>
<h2 id="8-可变参数模板"><a href="#8-可变参数模板" class="headerlink" title="8. 可变参数模板"></a>8. 可变参数模板</h2><p>可变参数模板可以接收任意个模板参数或函数参数。</p>
<h3 id="8-1-基本写法"><a href="#8-1-基本写法" class="headerlink" title="8.1 基本写法"></a>8.1 基本写法</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span>... Ts&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(Ts... args)</span> </span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<p>这里：</p>
<ul>
<li><code>Ts...</code> 是模板参数包</li>
<li><code>args...</code> 是函数参数包</li>
</ul>
<h3 id="8-2-递归展开"><a href="#8-2-递归展开" class="headerlink" title="8.2 递归展开"></a>8.2 递归展开</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print_all</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span>... Ts&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print_all</span><span class="params">(<span class="type">const</span> T&amp; first, <span class="type">const</span> Ts&amp;... rest)</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; first &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    <span class="built_in">print_all</span>(rest...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="8-3-折叠表达式"><a href="#8-3-折叠表达式" class="headerlink" title="8.3 折叠表达式"></a>8.3 折叠表达式</h3><p>C++17 起，更推荐用 fold expression：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span>... Ts&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print_all</span><span class="params">(<span class="type">const</span> Ts&amp;... xs)</span> </span>&#123;</span><br><span class="line">    ((std::cout &lt;&lt; xs &lt;&lt; <span class="string">&#x27;\n&#x27;</span>), ...);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span>... Ts&gt;</span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">sum</span><span class="params">(<span class="type">const</span> Ts&amp;... xs)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (xs + ...);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这比手写递归更直观。</p>
<hr>
<h2 id="9-模板特化"><a href="#9-模板特化" class="headerlink" title="9. 模板特化"></a>9. 模板特化</h2><p>当某些类型需要特殊处理时，可以使用特化。</p>
<h3 id="9-1-全特化"><a href="#9-1-全特化" class="headerlink" title="9.1 全特化"></a>9.1 全特化</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">TypeName</span> &#123;</span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="keyword">constexpr</span> <span class="type">const</span> <span class="type">char</span>* <span class="title">get</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="string">&quot;unknown&quot;</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">TypeName</span>&lt;<span class="type">int</span>&gt; &#123;</span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="keyword">constexpr</span> <span class="type">const</span> <span class="type">char</span>* <span class="title">get</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> <span class="string">&quot;int&quot;</span>; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; TypeName&lt;<span class="type">int</span>&gt;::<span class="built_in">get</span>() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="9-2-偏特化"><a href="#9-2-偏特化" class="headerlink" title="9.2 偏特化"></a>9.2 偏特化</h3><p>类模板支持偏特化，函数模板不支持偏特化。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">IsPointer</span> &#123;</span><br><span class="line">    <span class="type">static</span> <span class="keyword">constexpr</span> <span class="type">bool</span> value = <span class="literal">false</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">IsPointer</span>&lt;T*&gt; &#123;</span><br><span class="line">    <span class="type">static</span> <span class="keyword">constexpr</span> <span class="type">bool</span> value = <span class="literal">true</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这里 <code>T*</code> 就是“对一类类型进行特化”。</p>
<hr>
<h2 id="10-类型萃取-type-traits"><a href="#10-类型萃取-type-traits" class="headerlink" title="10. 类型萃取 type traits"></a>10. 类型萃取 <code>type traits</code></h2><p><code>type traits</code> 是模板元编程里非常实用的一组工具，用来在编译期判断、转换、组合类型信息。</p>
<p>最常见的几个：</p>
<ul>
<li><code>std::is_same_v&lt;A, B&gt;</code></li>
<li><code>std::is_integral_v&lt;T&gt;</code></li>
<li><code>std::is_floating_point_v&lt;T&gt;</code></li>
<li><code>std::is_pointer_v&lt;T&gt;</code></li>
<li><code>std::remove_reference_t&lt;T&gt;</code></li>
<li><code>std::decay_t&lt;T&gt;</code></li>
</ul>
<p>例子：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">static_assert</span>(std::is_same_v&lt;<span class="type">int</span>, <span class="type">int</span>&gt;);</span><br><span class="line"><span class="built_in">static_assert</span>(std::is_integral_v&lt;<span class="type">int</span>&gt;);</span><br><span class="line"><span class="built_in">static_assert</span>(std::is_pointer_v&lt;<span class="type">int</span>*&gt;);</span><br></pre></td></tr></table></figure>

<h3 id="10-1-一个实用例子"><a href="#10-1-一个实用例子" class="headerlink" title="10.1 一个实用例子"></a>10.1 一个实用例子</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Func</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">fetch_data</span><span class="params">(<span class="type">const</span> Func&amp; func)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">4</span>; ++i) &#123;</span><br><span class="line">        <span class="built_in">func</span>(i);</span><br><span class="line">        <span class="built_in">func</span>(i + <span class="number">0.5f</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::vector&lt;<span class="type">int</span>&gt; res_i;</span><br><span class="line">    std::vector&lt;<span class="type">float</span>&gt; res_f;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">fetch_data</span>([&amp;](<span class="type">const</span> <span class="keyword">auto</span>&amp; x) &#123;</span><br><span class="line">        <span class="keyword">using</span> T = std::<span class="type">decay_t</span>&lt;<span class="keyword">decltype</span>(x)&gt;;</span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">constexpr</span> (std::is_same_v&lt;T, <span class="type">int</span>&gt;) &#123;</span><br><span class="line">            res_i.<span class="built_in">push_back</span>(x);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> <span class="built_in">constexpr</span> (std::is_same_v&lt;T, <span class="type">float</span>&gt;) &#123;</span><br><span class="line">            res_f.<span class="built_in">push_back</span>(x);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个模式的核心是：</p>
<ul>
<li><code>decltype(x)</code> 取表达式类型</li>
<li><code>std::decay_t</code> 做常见退化</li>
<li><code>if constexpr</code> 做编译期分支</li>
</ul>
<hr>
<h2 id="11-tuple：把多个异构值打包"><a href="#11-tuple：把多个异构值打包" class="headerlink" title="11. tuple：把多个异构值打包"></a>11. <code>tuple</code>：把多个异构值打包</h2><p><code>tuple</code> 可以装不同类型的数据。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tuple&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line">std::tuple&lt;<span class="type">int</span>, <span class="type">double</span>, std::string&gt; info&#123;<span class="number">1</span>, <span class="number">3.14</span>, <span class="string">&quot;cpp&quot;</span>&#125;;</span><br></pre></td></tr></table></figure>

<p>常见操作：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> x = std::<span class="built_in">get</span>&lt;<span class="number">0</span>&gt;(info);</span><br><span class="line"><span class="keyword">auto</span> y = std::<span class="built_in">get</span>&lt;<span class="number">1</span>&gt;(info);</span><br></pre></td></tr></table></figure>

<p>也可以结构化绑定：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> [id, score, name] = info;</span><br></pre></td></tr></table></figure>

<p><code>tuple</code> 在模板里很重要，因为它经常和参数包、泛型封装、返回多个值一起使用。</p>
<h3 id="11-1-配合-std-apply"><a href="#11-1-配合-std-apply" class="headerlink" title="11.1 配合 std::apply"></a>11.1 配合 <code>std::apply</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tuple&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b, <span class="type">int</span> c)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b + c;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> t = std::<span class="built_in">make_tuple</span>(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line">    std::cout &lt;&lt; std::<span class="built_in">apply</span>(add, t) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="12-编译期计算与元编程"><a href="#12-编译期计算与元编程" class="headerlink" title="12. 编译期计算与元编程"></a>12. 编译期计算与元编程</h2><p>模板元编程最早常见的写法是“模板递归”。</p>
<h3 id="12-1-经典例子：编译期阶乘"><a href="#12-1-经典例子：编译期阶乘" class="headerlink" title="12.1 经典例子：编译期阶乘"></a>12.1 经典例子：编译期阶乘</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="type">int</span> N&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Factorial</span> &#123;</span><br><span class="line">    <span class="type">static</span> <span class="keyword">constexpr</span> <span class="type">int</span> value = N * Factorial&lt;N - <span class="number">1</span>&gt;::value;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Factorial</span>&lt;<span class="number">0</span>&gt; &#123;</span><br><span class="line">    <span class="type">static</span> <span class="keyword">constexpr</span> <span class="type">int</span> value = <span class="number">1</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">static_assert</span>(Factorial&lt;<span class="number">5</span>&gt;::value == <span class="number">120</span>);</span><br></pre></td></tr></table></figure>

<p>这就是典型的模板元编程：</p>
<ul>
<li>编译期递归展开</li>
<li>编译期得到结果</li>
</ul>
<p>但现代 C++ 中，很多时候更推荐 <code>constexpr</code> 函数，因为更自然、可读性更好。</p>
<h3 id="12-2-更现代的写法：constexpr"><a href="#12-2-更现代的写法：constexpr" class="headerlink" title="12.2 更现代的写法：constexpr"></a>12.2 更现代的写法：<code>constexpr</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">constexpr</span> <span class="type">int</span> <span class="title">factorial</span><span class="params">(<span class="type">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> n &lt;= <span class="number">1</span> ? <span class="number">1</span> : n * <span class="built_in">factorial</span>(n - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">static_assert</span>(<span class="built_in">factorial</span>(<span class="number">5</span>) == <span class="number">120</span>);</span><br></pre></td></tr></table></figure>

<p>经验上可以这么记：</p>
<ul>
<li>需要泛型类型操作时，用模板</li>
<li>需要编译期值计算时，优先考虑 <code>constexpr</code></li>
</ul>
<hr>
<h2 id="13-SFINAE-与-Concepts"><a href="#13-SFINAE-与-Concepts" class="headerlink" title="13. SFINAE 与 Concepts"></a>13. SFINAE 与 Concepts</h2><p>这部分属于模板进阶重点。</p>
<h3 id="13-1-SFINAE-是什么"><a href="#13-1-SFINAE-是什么" class="headerlink" title="13.1 SFINAE 是什么"></a>13.1 SFINAE 是什么</h3><p>SFINAE 全称：</p>
<blockquote>
<p>Substitution Failure Is Not An Error</p>
</blockquote>
<p>含义是：</p>
<ul>
<li>模板替换失败时，不一定报错</li>
<li>编译器会把这个候选模板从重载集合里移除</li>
</ul>
<p>过去常用 <code>std::enable_if</code> 来写约束：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>,</span><br><span class="line">          <span class="keyword">class</span> = std::<span class="type">enable_if_t</span>&lt;std::is_integral_v&lt;T&gt;&gt;&gt;</span><br><span class="line">T <span class="built_in">add_one</span>(T x) &#123;</span><br><span class="line">    <span class="keyword">return</span> x + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="13-2-现代写法：Concepts"><a href="#13-2-现代写法：Concepts" class="headerlink" title="13.2 现代写法：Concepts"></a>13.2 现代写法：Concepts</h3><p>C++20 更推荐直接写约束：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;concepts&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;std::integral T&gt;</span><br><span class="line"><span class="function">T <span class="title">add_one</span><span class="params">(T x)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>或者：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="keyword">requires</span> std::integral&lt;T&gt;</span></span><br><span class="line"><span class="function">T <span class="title">add_one</span><span class="params">(T x)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>优势：</p>
<ul>
<li>代码更直观</li>
<li>编译错误信息更友好</li>
<li>语义上更接近“声明接口要求”</li>
</ul>
<hr>
<h2 id="14-模板与高性能的关系"><a href="#14-模板与高性能的关系" class="headerlink" title="14. 模板与高性能的关系"></a>14. 模板与高性能的关系</h2><p>模板在高性能 C++ 里不是“语法炫技”，而是重要工具。</p>
<p>它常被用来实现：</p>
<ul>
<li>泛型容器与算法</li>
<li>编译期分发，避免运行时 <code>if/switch</code></li>
<li>针对不同类型或策略做静态优化</li>
<li>内联与零成本抽象</li>
</ul>
<p>例如一个“策略模板”：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Policy</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">process</span><span class="params">(<span class="type">const</span> Policy&amp; policy)</span> </span>&#123;</span><br><span class="line">    policy.<span class="built_in">run</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果策略类型在编译期确定，编译器往往可以做更激进的内联和优化。</p>
<hr>
<h2 id="15-常见坑"><a href="#15-常见坑" class="headerlink" title="15. 常见坑"></a>15. 常见坑</h2><h3 id="15-1-模板报错通常很长，先看“第一处真正失败的地方”"><a href="#15-1-模板报错通常很长，先看“第一处真正失败的地方”" class="headerlink" title="15.1 模板报错通常很长，先看“第一处真正失败的地方”"></a>15.1 模板报错通常很长，先看“第一处真正失败的地方”</h3><p>不要一上来被几百行错误吓住。<br>模板错误栈很深时，先找：</p>
<ul>
<li>第一个用户代码位置</li>
<li>哪个类型替换失败</li>
<li>哪个约束没满足</li>
</ul>
<h3 id="15-2-函数模板不能偏特化"><a href="#15-2-函数模板不能偏特化" class="headerlink" title="15.2 函数模板不能偏特化"></a>15.2 函数模板不能偏特化</h3><p>类模板可以偏特化，函数模板不能。<br>函数模板通常靠：</p>
<ul>
<li>重载</li>
<li><code>if constexpr</code></li>
<li><code>enable_if</code></li>
<li>concepts</li>
</ul>
<p>来做选择。</p>
<h3 id="15-3-模板定义通常要放头文件"><a href="#15-3-模板定义通常要放头文件" class="headerlink" title="15.3 模板定义通常要放头文件"></a>15.3 模板定义通常要放头文件</h3><p>因为模板需要在使用点可见，编译器才能实例化。<br>这也是很多模板库几乎全写在头文件里的原因。</p>
<h3 id="15-4-typename-的两个常见位置"><a href="#15-4-typename-的两个常见位置" class="headerlink" title="15.4 typename 的两个常见位置"></a>15.4 <code>typename</code> 的两个常见位置</h3><p>第一种：声明模板类型参数</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br></pre></td></tr></table></figure>

<p>第二种：告诉编译器“这是一个类型”</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">typename</span> T::value_type x&#123;&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因为 <code>T::value_type</code> 在模板阶段不一定能立刻判断它是“类型”还是“静态成员”。</p>
<hr>
<h2 id="16-运行期常量-vs-编译期常量"><a href="#16-运行期常量-vs-编译期常量" class="headerlink" title="16. 运行期常量 vs 编译期常量"></a>16. 运行期常量 vs 编译期常量</h2><p>运行期常量：</p>
<ul>
<li>程序运行时才确定</li>
<li>例如函数参数 <code>int n</code></li>
</ul>
<p>编译期常量：</p>
<ul>
<li>编译阶段就确定</li>
<li>例如模板参数、<code>constexpr</code> 结果、<code>static_assert</code> 条件</li>
</ul>
<p>区别的意义在于：</p>
<ul>
<li>编译期常量能参与模板实例化</li>
<li>编译器可以据此裁剪分支、展开逻辑、做更多优化</li>
</ul>
<hr>
<h2 id="17-一页总结"><a href="#17-一页总结" class="headerlink" title="17. 一页总结"></a>17. 一页总结</h2><p>模板学习的主线可以概括成：</p>
<ol>
<li>用模板把“类型差异”参数化</li>
<li>用推导、特化、类型萃取处理不同类型</li>
<li>用参数包和 <code>tuple</code> 处理变长、异构数据</li>
<li>用 <code>if constexpr</code>、SFINAE、concepts 实现静态约束和分发</li>
<li>用 <code>constexpr</code> 与模板配合，把部分逻辑提前到编译期</li>
</ol>
<p>如果只记几个最高频关键词：</p>
<ul>
<li>函数模板 &#x2F; 类模板</li>
<li>非类型模板参数</li>
<li><code>auto</code> &#x2F; <code>decltype</code> &#x2F; 引用折叠</li>
<li><code>if constexpr</code></li>
<li><code>type traits</code></li>
<li>参数包与 fold expression</li>
<li>特化</li>
<li><code>constexpr</code></li>
<li>concepts</li>
</ul>
<hr>
<h2 id="18-建议继续补充的相关主题"><a href="#18-建议继续补充的相关主题" class="headerlink" title="18. 建议继续补充的相关主题"></a>18. 建议继续补充的相关主题</h2><p>如果后面继续整理，这几个主题和本文衔接最好：</p>
<ol>
<li><code>std::forward</code>、完美转发与引用折叠</li>
<li><code>std::move</code>、移动语义和模板中的值类别</li>
<li><code>CRTP</code>、策略类、静态多态</li>
<li><code>std::invoke</code>、<code>std::apply</code>、通用调用封装</li>
<li>ranges 与 concepts 在现代泛型编程里的用法</li>
</ol>
<hr>
<h2 id="19-参考资料"><a href="#19-参考资料" class="headerlink" title="19. 参考资料"></a>19. 参考资料</h2><ol>
<li><p>cppreference: templates<br><a href="https://en.cppreference.com/w/cpp/language/templates">https://en.cppreference.com/w/cpp/language/templates</a></p>
</li>
<li><p>cppreference: type traits<br><a href="https://en.cppreference.com/w/cpp/types">https://en.cppreference.com/w/cpp/types</a></p>
</li>
<li><p>cppreference: fold expressions<br><a href="https://en.cppreference.com/w/cpp/language/fold">https://en.cppreference.com/w/cpp/language/fold</a></p>
</li>
<li><p>cppreference: concepts<br><a href="https://en.cppreference.com/w/cpp/concepts">https://en.cppreference.com/w/cpp/concepts</a></p>
</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>深入浅出访存优化</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/07-%E8%AE%BF%E5%AD%98%E4%BC%98%E5%8C%96/</url>
    <content><![CDATA[<h1 id="深入浅出访存优化"><a href="#深入浅出访存优化" class="headerlink" title="深入浅出访存优化"></a>深入浅出访存优化</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：缓存、cache line、局部性、AoS&#x2F;SoA、stride、blocking、false sharing、带宽、延迟<br>核心目标：理解“为什么同样是 <code>O(n)</code>，不同内存访问方式会有非常大的性能差异”。</p>
</blockquote>
<hr>
<h2 id="1-为什么访存优化这么重要"><a href="#1-为什么访存优化这么重要" class="headerlink" title="1. 为什么访存优化这么重要"></a>1. 为什么访存优化这么重要</h2><p>很多程序并不是算得慢，而是<strong>等内存等得慢</strong>。</p>
<p>现代 CPU 的特点是：</p>
<ul>
<li>算术单元非常快</li>
<li>主存访问相对很慢</li>
<li>一旦 cache miss，CPU 可能要空等很多周期</li>
</ul>
<p>所以高性能编程里经常会出现这种现象：</p>
<ul>
<li>算法复杂度没变</li>
<li>指令数也没怎么变</li>
<li>只是换了数据布局和访问顺序</li>
<li>性能却提升一大截</li>
</ul>
<p>这背后的核心就是访存模式。</p>
<hr>
<h2 id="2-CPU-不是直接从内存一格一格取数据"><a href="#2-CPU-不是直接从内存一格一格取数据" class="headerlink" title="2. CPU 不是直接从内存一格一格取数据"></a>2. CPU 不是直接从内存一格一格取数据</h2><h3 id="2-1-缓存层级"><a href="#2-1-缓存层级" class="headerlink" title="2.1 缓存层级"></a>2.1 缓存层级</h3><p>可以先粗略理解成：</p>
<ul>
<li><code>L1</code>：最小、最快</li>
<li><code>L2</code>：更大、稍慢</li>
<li><code>L3</code>：更大、共享、再慢一些</li>
<li><code>DRAM</code>：远慢于缓存</li>
</ul>
<p>因此访问一个数据时：</p>
<ul>
<li>如果在缓存里，代价较低</li>
<li>如果不在，就会发生 cache miss，从更慢层级取</li>
</ul>
<h3 id="2-2-cache-line"><a href="#2-2-cache-line" class="headerlink" title="2.2 cache line"></a>2.2 cache line</h3><p>CPU 取数据通常不是只取一个字节或一个 <code>int</code>，而是按<strong>缓存行</strong>成块搬运。</p>
<p>常见 cache line 大小是：</p>
<ul>
<li>64 字节</li>
</ul>
<p>这意味着：</p>
<ul>
<li>如果你访问了 <code>a[i]</code></li>
<li>那么 <code>a[i]</code> 周围的一片数据往往也会一起进缓存</li>
</ul>
<p>所以连续访问会特别受益。</p>
<hr>
<h2 id="3-两种最重要的局部性"><a href="#3-两种最重要的局部性" class="headerlink" title="3. 两种最重要的局部性"></a>3. 两种最重要的局部性</h2><h3 id="3-1-空间局部性"><a href="#3-1-空间局部性" class="headerlink" title="3.1 空间局部性"></a>3.1 空间局部性</h3><p>如果你访问了某个地址，附近地址也很可能马上会被访问。<br>连续数组遍历就是最典型例子。</p>
<h3 id="3-2-时间局部性"><a href="#3-2-时间局部性" class="headerlink" title="3.2 时间局部性"></a>3.2 时间局部性</h3><p>如果你刚访问过某个数据，很可能短时间内还会再访问它。</p>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    sum += a[i];</span><br><span class="line">    sum += a[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第二次访问 <code>a[i]</code> 很可能仍在缓存里。</p>
<hr>
<h2 id="4-为什么顺序访问通常比随机访问快"><a href="#4-为什么顺序访问通常比随机访问快" class="headerlink" title="4. 为什么顺序访问通常比随机访问快"></a>4. 为什么顺序访问通常比随机访问快</h2><p>看两个模式：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    sum += a[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>和：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    sum += a[idx[i]];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第二种如果 <code>idx</code> 很随机，就容易出现：</p>
<ul>
<li>cache line 利用率低</li>
<li>硬件预取器很难提前猜中</li>
<li>CPU 长时间等待内存</li>
</ul>
<p>这就是为什么：</p>
<ul>
<li>链表遍历</li>
<li>稀疏随机访问</li>
<li>指针追逐</li>
</ul>
<p>常常会显著慢于顺序数组遍历。</p>
<hr>
<h2 id="5-AoS-与-SoA"><a href="#5-AoS-与-SoA" class="headerlink" title="5. AoS 与 SoA"></a>5. AoS 与 SoA</h2><p>这是访存优化里最经典的话题之一。</p>
<h3 id="5-1-AoS：Array-of-Struct"><a href="#5-1-AoS：Array-of-Struct" class="headerlink" title="5.1 AoS：Array of Struct"></a>5.1 AoS：Array of Struct</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Particle</span> &#123;</span><br><span class="line">    <span class="type">float</span> x, y, z;</span><br><span class="line">    <span class="type">float</span> vx, vy, vz;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">std::vector&lt;Particle&gt; a;</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li>单个对象的属性紧挨着存</li>
<li>适合“经常整对象一起处理”的场景</li>
</ul>
<h3 id="5-2-SoA：Struct-of-Arrays"><a href="#5-2-SoA：Struct-of-Arrays" class="headerlink" title="5.2 SoA：Struct of Arrays"></a>5.2 SoA：Struct of Arrays</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Particles</span> &#123;</span><br><span class="line">    std::vector&lt;<span class="type">float</span>&gt; x, y, z;</span><br><span class="line">    std::vector&lt;<span class="type">float</span>&gt; vx, vy, vz;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li>同一属性的数据连续存储</li>
<li>更适合批量处理同一字段</li>
<li>常更利于 SIMD &#x2F; GPU &#x2F; cache</li>
</ul>
<h3 id="5-3-什么时候-SoA-更有优势"><a href="#5-3-什么时候-SoA-更有优势" class="headerlink" title="5.3 什么时候 SoA 更有优势"></a>5.3 什么时候 SoA 更有优势</h3><p>如果你的计算主要是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (...) &#123;</span><br><span class="line">    x[i] += vx[i] * dt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>那么 SoA 往往更好，因为：</p>
<ul>
<li>访问字段连续</li>
<li>每个 cache line 里装的都是“当前真正要用的数据”</li>
</ul>
<h3 id="5-4-什么时候-AoS-更自然"><a href="#5-4-什么时候-AoS-更自然" class="headerlink" title="5.4 什么时候 AoS 更自然"></a>5.4 什么时候 AoS 更自然</h3><p>如果逻辑经常要完整处理单个对象，例如：</p>
<ul>
<li>读一个粒子的全部属性</li>
<li>面向对象接口较多</li>
</ul>
<p>AoS 可能更直观。</p>
<p>经验上：</p>
<ul>
<li>计算密集且按字段批量处理时，优先考虑 SoA</li>
<li>业务逻辑围绕单对象时，AoS 更自然</li>
</ul>
<hr>
<h2 id="6-stride（步长）为什么会直接影响性能"><a href="#6-stride（步长）为什么会直接影响性能" class="headerlink" title="6. stride（步长）为什么会直接影响性能"></a>6. stride（步长）为什么会直接影响性能</h2><h3 id="6-1-连续步长访问"><a href="#6-1-连续步长访问" class="headerlink" title="6.1 连续步长访问"></a>6.1 连续步长访问</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    sum += a[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这通常最好。</p>
<h3 id="6-2-大步长访问"><a href="#6-2-大步长访问" class="headerlink" title="6.2 大步长访问"></a>6.2 大步长访问</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; i += <span class="number">16</span>) &#123;</span><br><span class="line">    sum += a[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>问题在于：</p>
<ul>
<li>每次只用到一个 cache line 里的很少一部分</li>
<li>带宽利用率低</li>
</ul>
<h3 id="6-3-二维数组里的列访问"><a href="#6-3-二维数组里的列访问" class="headerlink" title="6.3 二维数组里的列访问"></a>6.3 二维数组里的列访问</h3><p>假设按行连续存储：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> j = <span class="number">0</span>; j &lt; m; ++j) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">        sum += a[i][j];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果内层循环跨行跳，会比按行访问差很多。</p>
<p>因此多维数据常见经验是：</p>
<ul>
<li><strong>内层循环尽量走连续维</strong></li>
</ul>
<hr>
<h2 id="7-blocking-tiling：为什么分块常常更快"><a href="#7-blocking-tiling：为什么分块常常更快" class="headerlink" title="7. blocking &#x2F; tiling：为什么分块常常更快"></a>7. blocking &#x2F; tiling：为什么分块常常更快</h2><p>当工作集太大，无法完全放进缓存时，可以按块处理。</p>
<h3 id="7-1-直觉"><a href="#7-1-直觉" class="headerlink" title="7.1 直觉"></a>7.1 直觉</h3><p>不要一次把全量数据扫完再回来重扫，而是：</p>
<ul>
<li>先拿一小块</li>
<li>在这小块上做尽量多的计算</li>
<li>让这小块在缓存中被充分复用</li>
</ul>
<h3 id="7-2-矩阵乘法是最典型场景"><a href="#7-2-矩阵乘法是最典型场景" class="headerlink" title="7.2 矩阵乘法是最典型场景"></a>7.2 矩阵乘法是最典型场景</h3><p>朴素矩阵乘法经常很慢，不是乘法本身慢，而是访存复用差。<br>分块后的核心收益是：</p>
<ul>
<li>提高缓存命中</li>
<li>减少大跨度反复读</li>
</ul>
<h3 id="7-3-图像处理也一样"><a href="#7-3-图像处理也一样" class="headerlink" title="7.3 图像处理也一样"></a>7.3 图像处理也一样</h3><p>例如卷积、滤波、stencil 计算，都常通过 tile&#x2F;block 改善访存。</p>
<hr>
<h2 id="8-预取与硬件预取器"><a href="#8-预取与硬件预取器" class="headerlink" title="8. 预取与硬件预取器"></a>8. 预取与硬件预取器</h2><p>现代 CPU 会尝试根据访问规律提前把数据搬进缓存，这就是硬件预取。</p>
<p>它更擅长：</p>
<ul>
<li>连续访问</li>
<li>稳定步长访问</li>
</ul>
<p>它不擅长：</p>
<ul>
<li>随机访问</li>
<li>指针跳转</li>
<li>强数据依赖链</li>
</ul>
<p>所以很多“看起来只是更整齐的循环”之所以更快，本质上是在帮硬件预取器工作。</p>
<hr>
<h2 id="9-false-sharing：并发里最隐蔽的访存杀手之一"><a href="#9-false-sharing：并发里最隐蔽的访存杀手之一" class="headerlink" title="9. false sharing：并发里最隐蔽的访存杀手之一"></a>9. false sharing：并发里最隐蔽的访存杀手之一</h2><h3 id="9-1-它是什么"><a href="#9-1-它是什么" class="headerlink" title="9.1 它是什么"></a>9.1 它是什么</h3><p>两个线程虽然更新的是<strong>不同变量</strong>，但这两个变量落在同一个 cache line 里。<br>结果两个核心会频繁抢这个 cache line 的所有权，导致性能急剧下降。</p>
<h3 id="9-2-例子"><a href="#9-2-例子" class="headerlink" title="9.2 例子"></a>9.2 例子</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">CounterPair</span> &#123;</span><br><span class="line">    std::atomic&lt;<span class="type">int</span>&gt; a;</span><br><span class="line">    std::atomic&lt;<span class="type">int</span>&gt; b;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如果线程 1 只写 <code>a</code>，线程 2 只写 <code>b</code>，理论上彼此独立；<br>但如果 <code>a</code> 和 <code>b</code> 在同一 cache line，就可能 false sharing。</p>
<h3 id="9-3-典型缓解方式"><a href="#9-3-典型缓解方式" class="headerlink" title="9.3 典型缓解方式"></a>9.3 典型缓解方式</h3><ul>
<li>padding</li>
<li><code>alignas(64)</code></li>
<li>每线程局部累加，最后汇总</li>
</ul>
<p>这在并行归约、线程本地统计里特别常见。</p>
<hr>
<h2 id="10-对齐、padding-与缓存布局"><a href="#10-对齐、padding-与缓存布局" class="headerlink" title="10. 对齐、padding 与缓存布局"></a>10. 对齐、padding 与缓存布局</h2><h3 id="10-1-对齐影响加载效率"><a href="#10-1-对齐影响加载效率" class="headerlink" title="10.1 对齐影响加载效率"></a>10.1 对齐影响加载效率</h3><p>对齐良好的数据更利于：</p>
<ul>
<li>高效加载</li>
<li>SIMD 指令</li>
<li>cache line 对齐访问</li>
</ul>
<h3 id="10-2-结构体布局会影响缓存利用率"><a href="#10-2-结构体布局会影响缓存利用率" class="headerlink" title="10.2 结构体布局会影响缓存利用率"></a>10.2 结构体布局会影响缓存利用率</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">A</span> &#123;</span><br><span class="line">    <span class="type">char</span> flag;</span><br><span class="line">    <span class="type">double</span> value;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>编译器可能为了对齐插入 padding。<br>如果一个结构体很大，而你每次只用其中一两个字段，就会带来缓存浪费。</p>
<p>这也是为什么“对象设计”会直接变成“性能设计”。</p>
<hr>
<h2 id="11-分支与访存也常常绑定在一起"><a href="#11-分支与访存也常常绑定在一起" class="headerlink" title="11. 分支与访存也常常绑定在一起"></a>11. 分支与访存也常常绑定在一起</h2><p>下面这类代码既影响分支预测，也影响访存效率：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (a[i] &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    sum += heavy_table[a[i]];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果：</p>
<ul>
<li><code>a[i] &gt; 0</code> 高度不可预测</li>
<li><code>heavy_table</code> 访问又很随机</li>
</ul>
<p>性能通常会明显变差。</p>
<p>所以优化时别把“计算”和“访存”割裂看，它们常是一起出问题的。</p>
<hr>
<h2 id="12-为什么-vector-通常比-list-更快"><a href="#12-为什么-vector-通常比-list-更快" class="headerlink" title="12. 为什么 vector 通常比 list 更快"></a>12. 为什么 <code>vector</code> 通常比 <code>list</code> 更快</h2><p>很多人第一次接触会觉得：</p>
<ul>
<li><code>list</code> 插入删除是 <code>O(1)</code></li>
<li><code>vector</code> 中间插删是 <code>O(n)</code></li>
</ul>
<p>于是误以为 <code>list</code> 应该经常更快。<br>但现实里，很多遍历型或批量处理场景中，<code>vector</code> 常常更快，原因就在于：</p>
<ul>
<li>连续内存</li>
<li>cache 友好</li>
<li>更利于预取与向量化</li>
</ul>
<p>所以算法复杂度只是第一层，访存模式是第二层。</p>
<hr>
<h2 id="13-多线程写入时为什么要分区"><a href="#13-多线程写入时为什么要分区" class="headerlink" title="13. 多线程写入时为什么要分区"></a>13. 多线程写入时为什么要分区</h2><p>如果多个线程写同一个大数组，比较好的模式通常是：</p>
<ul>
<li>每个线程负责一个连续区间</li>
<li>尽量避免多个线程反复改同一个 cache line</li>
</ul>
<p>不好的模式则常见于：</p>
<ul>
<li>交错写入</li>
<li>所有线程抢同一个计数器</li>
<li>共享小对象频繁原子更新</li>
</ul>
<p>这会带来：</p>
<ul>
<li>cache line 争用</li>
<li>false sharing</li>
<li>带宽浪费</li>
</ul>
<hr>
<h2 id="14-GPU-视角下为什么更重视-SoA"><a href="#14-GPU-视角下为什么更重视-SoA" class="headerlink" title="14. GPU 视角下为什么更重视 SoA"></a>14. GPU 视角下为什么更重视 SoA</h2><p>GPU 更强调大规模并行和合并访问（coalescing）。<br>因此当一批线程访问同一字段的连续元素时，SoA 往往比 AoS 更有优势。</p>
<p>所以很多 CPU&#x2F;GPU 共享的高性能数据结构设计原则其实是相通的：</p>
<ul>
<li>扁平化</li>
<li>连续化</li>
<li>尽量批处理同类字段</li>
</ul>
<hr>
<h2 id="15-访存优化的实战原则"><a href="#15-访存优化的实战原则" class="headerlink" title="15. 访存优化的实战原则"></a>15. 访存优化的实战原则</h2><p>可以直接记这几条：</p>
<h3 id="15-1-尽量连续访问"><a href="#15-1-尽量连续访问" class="headerlink" title="15.1 尽量连续访问"></a>15.1 尽量连续访问</h3><p>先改访问顺序，再谈别的。</p>
<h3 id="15-2-尽量缩小热数据集"><a href="#15-2-尽量缩小热数据集" class="headerlink" title="15.2 尽量缩小热数据集"></a>15.2 尽量缩小热数据集</h3><p>把频繁访问的数据做得更紧凑。</p>
<h3 id="15-3-尽量增加复用"><a href="#15-3-尽量增加复用" class="headerlink" title="15.3 尽量增加复用"></a>15.3 尽量增加复用</h3><p>让已经进缓存的数据多用几次。</p>
<h3 id="15-4-尽量避免无意义共享"><a href="#15-4-尽量避免无意义共享" class="headerlink" title="15.4 尽量避免无意义共享"></a>15.4 尽量避免无意义共享</h3><p>特别是在并行代码里。</p>
<h3 id="15-5-用数据布局服务算法"><a href="#15-5-用数据布局服务算法" class="headerlink" title="15.5 用数据布局服务算法"></a>15.5 用数据布局服务算法</h3><p>不是“先写一个漂亮对象模型，再看性能”，而是：</p>
<ul>
<li>哪种布局更适合核心计算，就优先哪种</li>
</ul>
<hr>
<h2 id="16-常见误区"><a href="#16-常见误区" class="headerlink" title="16. 常见误区"></a>16. 常见误区</h2><h3 id="16-1-只盯算法复杂度，不看访存"><a href="#16-1-只盯算法复杂度，不看访存" class="headerlink" title="16.1 只盯算法复杂度，不看访存"></a>16.1 只盯算法复杂度，不看访存</h3><p>两个同为 <code>O(n)</code> 的实现，真实速度可能差很多倍。</p>
<h3 id="16-2-误以为-cache-是“编译器问题”"><a href="#16-2-误以为-cache-是“编译器问题”" class="headerlink" title="16.2 误以为 cache 是“编译器问题”"></a>16.2 误以为 cache 是“编译器问题”</h3><p>缓存问题本质上是：</p>
<ul>
<li>数据布局问题</li>
<li>访问顺序问题</li>
<li>并发共享问题</li>
</ul>
<h3 id="16-3-把所有对象都做成“大而全”"><a href="#16-3-把所有对象都做成“大而全”" class="headerlink" title="16.3 把所有对象都做成“大而全”"></a>16.3 把所有对象都做成“大而全”</h3><p>热路径只用两个字段时，大结构体会造成大量无效搬运。</p>
<h3 id="16-4-并发场景只关注锁，不关注-cache-line"><a href="#16-4-并发场景只关注锁，不关注-cache-line" class="headerlink" title="16.4 并发场景只关注锁，不关注 cache line"></a>16.4 并发场景只关注锁，不关注 cache line</h3><p>很多“看起来没锁争用”的程序，其实卡在 false sharing。</p>
<hr>
<h2 id="17-一页总结"><a href="#17-一页总结" class="headerlink" title="17. 一页总结"></a>17. 一页总结</h2><p>访存优化可以压缩成一句话：</p>
<blockquote>
<p>让 CPU&#x2F;GPU 更容易在更少次、更连续、更可预测的内存访问中拿到真正需要的数据。</p>
</blockquote>
<p>最关键的实践点有：</p>
<ol>
<li>顺序访问优于随机访问</li>
<li>连续布局优于分散指针结构</li>
<li>SoA 在按字段批量处理时常优于 AoS</li>
<li>分块能提升缓存复用</li>
<li>并发时要警惕 false sharing</li>
</ol>
<hr>
<h2 id="18-建议继续补充的相关主题"><a href="#18-建议继续补充的相关主题" class="headerlink" title="18. 建议继续补充的相关主题"></a>18. 建议继续补充的相关主题</h2><p>和本篇衔接最紧密的内容：</p>
<ol>
<li><code>vector</code> 容器优化</li>
<li>自动向量化与 SIMD</li>
<li>NUMA 与多路 CPU 访存</li>
<li>GPU 合并访存与 shared memory</li>
<li>矩阵分块与 stencil 优化</li>
</ol>
<hr>
<h2 id="19-参考资料"><a href="#19-参考资料" class="headerlink" title="19. 参考资料"></a>19. 参考资料</h2><ol>
<li><p>Intel Optimization Manual<br><a href="https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html">https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html</a></p>
</li>
<li><p>cppreference: <code>alignas</code><br><a href="https://en.cppreference.com/w/cpp/language/alignas">https://en.cppreference.com/w/cpp/language/alignas</a></p>
</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>从汇编角度看编译器优化</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/06-%E7%BC%96%E8%AF%91%E5%99%A8%E4%BC%98%E5%8C%96%E4%B8%8E%E6%B1%87%E7%BC%96%E8%A7%86%E8%A7%92/</url>
    <content><![CDATA[<h1 id="从汇编角度看编译器优化"><a href="#从汇编角度看编译器优化" class="headerlink" title="从汇编角度看编译器优化"></a>从汇编角度看编译器优化</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：<code>-O0/-O2/-O3</code>、内联、常量传播、循环优化、自动向量化、别名分析、未定义行为、反汇编<br>核心目标：理解“编译器为什么会把同一段 C++ 变成完全不同的机器代码”，以及哪些写法更利于优化。</p>
</blockquote>
<hr>
<h2 id="1-为什么要从汇编角度看优化"><a href="#1-为什么要从汇编角度看优化" class="headerlink" title="1. 为什么要从汇编角度看优化"></a>1. 为什么要从汇编角度看优化</h2><p>高性能 C++ 里，很多性能差异并不是算法层面，而是来自：</p>
<ul>
<li>编译器有没有内联</li>
<li>有没有消除无用分支</li>
<li>有没有做向量化</li>
<li>有没有把循环搬成更高效的机器指令</li>
</ul>
<p>如果只停留在源代码表面，就很容易误判：</p>
<ul>
<li>“写法 A 更短，所以一定更快”</li>
<li>“手写展开一定比编译器强”</li>
<li>“我明明只改了一点语法，为什么性能差了很多”</li>
</ul>
<p>看汇编的意义不是要手写汇编，而是为了：</p>
<ul>
<li>验证编译器到底做没做优化</li>
<li>理解某种写法为什么更快</li>
<li>避免用错误假设指导优化</li>
</ul>
<hr>
<h2 id="2-编译器优化到底在做什么"><a href="#2-编译器优化到底在做什么" class="headerlink" title="2. 编译器优化到底在做什么"></a>2. 编译器优化到底在做什么</h2><p>可以把优化粗略理解成两类：</p>
<h3 id="2-1-前端-中间层优化"><a href="#2-1-前端-中间层优化" class="headerlink" title="2.1 前端 &#x2F; 中间层优化"></a>2.1 前端 &#x2F; 中间层优化</h3><p>偏“语义层”的变换，例如：</p>
<ul>
<li>常量传播</li>
<li>死代码消除</li>
<li>公共子表达式消除</li>
<li>循环不变量外提</li>
<li>内联</li>
<li>自动向量化</li>
</ul>
<h3 id="2-2-后端-指令层优化"><a href="#2-2-后端-指令层优化" class="headerlink" title="2.2 后端 &#x2F; 指令层优化"></a>2.2 后端 &#x2F; 指令层优化</h3><p>偏“机器码层”的变换，例如：</p>
<ul>
<li>指令选择</li>
<li>寄存器分配</li>
<li>指令调度</li>
<li>分支布局</li>
<li>调用约定处理</li>
</ul>
<p>所以“优化”不是一招，而是一整条流水线。</p>
<hr>
<h2 id="3-常见优化等级"><a href="#3-常见优化等级" class="headerlink" title="3. 常见优化等级"></a>3. 常见优化等级</h2><p>最常见的是：</p>
<ul>
<li><code>-O0</code>：几乎不优化，便于调试</li>
<li><code>-O1</code></li>
<li><code>-O2</code>：多数项目的主力发布选项</li>
<li><code>-O3</code>：更激进，常包含更多循环和向量化优化</li>
<li><code>-Ofast</code>：更激进，但可能放宽标准语义约束</li>
</ul>
<h3 id="3-1-一个关键认知"><a href="#3-1-一个关键认知" class="headerlink" title="3.1 一个关键认知"></a>3.1 一个关键认知</h3><p><code>Debug</code> 和 <code>Release</code> 的性能差异，很多时候不是代码逻辑差异，而是：</p>
<ul>
<li>编译器有没有替你把高层写法压成高效机器码</li>
</ul>
<p>所以性能分析时一定要先确认：</p>
<ul>
<li>是不是在 <code>Release</code></li>
<li>是否真的开启了预期优化选项</li>
</ul>
<hr>
<h2 id="4-看汇编时重点看什么"><a href="#4-看汇编时重点看什么" class="headerlink" title="4. 看汇编时重点看什么"></a>4. 看汇编时重点看什么</h2><p>不要一上来盯每条指令。<br>高性能视角下，更该看这些问题：</p>
<h3 id="4-1-函数有没有被内联"><a href="#4-1-函数有没有被内联" class="headerlink" title="4.1 函数有没有被内联"></a>4.1 函数有没有被内联</h3><p>如果内联成功，调用边界消失，后续优化空间会更大。</p>
<h3 id="4-2-循环里有没有多余加载-存储"><a href="#4-2-循环里有没有多余加载-存储" class="headerlink" title="4.2 循环里有没有多余加载 &#x2F; 存储"></a>4.2 循环里有没有多余加载 &#x2F; 存储</h3><p>比如一个值本来可以放寄存器里，却被反复回写内存。</p>
<h3 id="4-3-有没有真正产生-SIMD-指令"><a href="#4-3-有没有真正产生-SIMD-指令" class="headerlink" title="4.3 有没有真正产生 SIMD 指令"></a>4.3 有没有真正产生 SIMD 指令</h3><p>例如：</p>
<ul>
<li><code>xmm/ymm/zmm</code></li>
<li><code>addps/mulps</code></li>
<li><code>vmovups/vfmadd...</code></li>
</ul>
<p>如果你以为代码被向量化了，但汇编里只有标量指令，那优化可能并没有发生。</p>
<h3 id="4-4-分支多不多，是否容易预测"><a href="#4-4-分支多不多，是否容易预测" class="headerlink" title="4.4 分支多不多，是否容易预测"></a>4.4 分支多不多，是否容易预测</h3><p>高度不可预测的分支会影响流水线和吞吐。</p>
<h3 id="4-5-是否有不必要的函数调用和栈操作"><a href="#4-5-是否有不必要的函数调用和栈操作" class="headerlink" title="4.5 是否有不必要的函数调用和栈操作"></a>4.5 是否有不必要的函数调用和栈操作</h3><p>热路径里频繁 <code>call</code> &#x2F; <code>ret</code>、大量栈搬运，通常意味着优化空间还大。</p>
<hr>
<h2 id="5-几种最常见的编译器优化"><a href="#5-几种最常见的编译器优化" class="headerlink" title="5. 几种最常见的编译器优化"></a>5. 几种最常见的编译器优化</h2><h3 id="5-1-常量传播与常量折叠"><a href="#5-1-常量传播与常量折叠" class="headerlink" title="5.1 常量传播与常量折叠"></a>5.1 常量传播与常量折叠</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">3</span> * <span class="number">7</span> + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>优化后编译器通常直接返回常量结果，而不会真的乘法加法都执行一遍。</p>
<h3 id="5-2-死代码消除"><a href="#5-2-死代码消除" class="headerlink" title="5.2 死代码消除"></a>5.2 死代码消除</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">f</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> y = x * <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">return</span> x + <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果 <code>y</code> 从未被使用，它就会被删掉。</p>
<h3 id="5-3-循环不变量外提"><a href="#5-3-循环不变量外提" class="headerlink" title="5.3 循环不变量外提"></a>5.3 循环不变量外提</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    sum += a[i] * scale;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果 <code>scale</code> 在循环里不变，编译器会尽量避免重复计算。</p>
<h3 id="5-4-内联"><a href="#5-4-内联" class="headerlink" title="5.4 内联"></a>5.4 内联</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="type">int</span> <span class="title">add1</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123; <span class="keyword">return</span> x + <span class="number">1</span>; &#125;</span><br></pre></td></tr></table></figure>

<p>内联后，调用点可能直接展开为 <code>x + 1</code>。<br>不过要注意：</p>
<ul>
<li><code>inline</code> 只是建议，不是强制命令</li>
<li>真正决定是否内联的，还是编译器</li>
</ul>
<h3 id="5-5-强度削弱（strength-reduction）"><a href="#5-5-强度削弱（strength-reduction）" class="headerlink" title="5.5 强度削弱（strength reduction）"></a>5.5 强度削弱（strength reduction）</h3><p>例如：</p>
<ul>
<li>乘法变移位</li>
<li>某些复杂索引计算被简化</li>
</ul>
<h3 id="5-6-自动向量化"><a href="#5-6-自动向量化" class="headerlink" title="5.6 自动向量化"></a>5.6 自动向量化</h3><p>对于连续数组、无数据相关、无复杂分支的循环，编译器可能把标量循环变成 SIMD 指令批量处理。</p>
<hr>
<h2 id="6-一个最常见的误区：源代码“看起来简洁”不等于更快"><a href="#6-一个最常见的误区：源代码“看起来简洁”不等于更快" class="headerlink" title="6. 一个最常见的误区：源代码“看起来简洁”不等于更快"></a>6. 一个最常见的误区：源代码“看起来简洁”不等于更快</h2><p>例如这两种写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">sum += a[i] * b[i];</span><br></pre></td></tr></table></figure>

<p>和：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> x = a[i];</span><br><span class="line"><span class="keyword">auto</span> y = b[i];</span><br><span class="line">sum += x * y;</span><br></pre></td></tr></table></figure>

<p>在优化级别足够高时，编译器可能生成几乎一样的机器码。</p>
<p>所以高性能优化的基本纪律是：</p>
<blockquote>
<p>不凭感觉下结论，要么看汇编，要么做基准测试。</p>
</blockquote>
<hr>
<h2 id="7-为什么编译器有时不敢优化"><a href="#7-为什么编译器有时不敢优化" class="headerlink" title="7. 为什么编译器有时不敢优化"></a>7. 为什么编译器有时不敢优化</h2><h3 id="7-1-别名分析不明确"><a href="#7-1-别名分析不明确" class="headerlink" title="7.1 别名分析不明确"></a>7.1 别名分析不明确</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">saxpy</span><span class="params">(<span class="type">float</span>* x, <span class="type">float</span>* y, <span class="type">float</span> a, <span class="type">size_t</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">        y[i] = a * x[i] + y[i];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果编译器不确定 <code>x</code> 和 <code>y</code> 是否重叠，它可能更保守。</p>
<h3 id="7-2-函数调用边界太多"><a href="#7-2-函数调用边界太多" class="headerlink" title="7.2 函数调用边界太多"></a>7.2 函数调用边界太多</h3><p>尤其是：</p>
<ul>
<li>小函数没有内联</li>
<li>虚函数调用</li>
<li>间接调用</li>
</ul>
<p>都可能阻碍进一步优化。</p>
<h3 id="7-3-分支过于复杂"><a href="#7-3-分支过于复杂" class="headerlink" title="7.3 分支过于复杂"></a>7.3 分支过于复杂</h3><p>循环体里复杂控制流会影响：</p>
<ul>
<li>向量化</li>
<li>指令调度</li>
<li>分支预测</li>
</ul>
<h3 id="7-4-访问模式不规则"><a href="#7-4-访问模式不规则" class="headerlink" title="7.4 访问模式不规则"></a>7.4 访问模式不规则</h3><p>例如链表遍历、随机跳表、指针追逐，很难像连续数组那样优化得漂亮。</p>
<hr>
<h2 id="8-自动向量化为什么有时失败"><a href="#8-自动向量化为什么有时失败" class="headerlink" title="8. 自动向量化为什么有时失败"></a>8. 自动向量化为什么有时失败</h2><p>向量化通常喜欢这种代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    c[i] = a[i] + b[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因为它具备：</p>
<ul>
<li>连续内存</li>
<li>简单循环</li>
<li>没有复杂依赖</li>
<li>没有难以证明的别名问题</li>
</ul>
<p>而下面这些情况容易阻碍向量化：</p>
<ul>
<li>循环体里有函数调用</li>
<li>分支复杂</li>
<li>访问模式不连续</li>
<li>编译器怀疑存在数据依赖</li>
<li>对齐信息不明确</li>
</ul>
<p>所以写高性能代码时，经常要主动让循环“更像机器喜欢的样子”。</p>
<hr>
<h2 id="9-constexpr、const-与编译期优化"><a href="#9-constexpr、const-与编译期优化" class="headerlink" title="9. constexpr、const 与编译期优化"></a>9. <code>constexpr</code>、<code>const</code> 与编译期优化</h2><h3 id="9-1-const-不等于编译期常量"><a href="#9-1-const-不等于编译期常量" class="headerlink" title="9.1 const 不等于编译期常量"></a>9.1 <code>const</code> 不等于编译期常量</h3><p><code>const</code> 主要表达“值不可改”，但它不一定保证编译期求值。</p>
<h3 id="9-2-constexpr-更明确"><a href="#9-2-constexpr-更明确" class="headerlink" title="9.2 constexpr 更明确"></a>9.2 <code>constexpr</code> 更明确</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">constexpr</span> <span class="type">int</span> <span class="title">square</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x * x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果上下文允许，编译器可以在编译期直接求值。</p>
<p>这对优化的意义是：</p>
<ul>
<li>更多常量传播机会</li>
<li>更少运行期开销</li>
<li>更强的静态约束</li>
</ul>
<hr>
<h2 id="10-未定义行为为什么会影响优化"><a href="#10-未定义行为为什么会影响优化" class="headerlink" title="10. 未定义行为为什么会影响优化"></a>10. 未定义行为为什么会影响优化</h2><p>很多人以为 UB 只是“程序可能崩”，其实它还会直接改变优化结果。</p>
<p>例如：</p>
<h3 id="10-1-有符号整数溢出"><a href="#10-1-有符号整数溢出" class="headerlink" title="10.1 有符号整数溢出"></a>10.1 有符号整数溢出</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = ...;</span><br><span class="line"><span class="type">int</span> y = x + <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p>在标准语义里，有符号溢出是未定义行为。<br>这意味着编译器可以据此做非常激进的推理。</p>
<h3 id="10-2-越界访问"><a href="#10-2-越界访问" class="headerlink" title="10.2 越界访问"></a>10.2 越界访问</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">4</span>];</span><br><span class="line"><span class="keyword">return</span> a[<span class="number">10</span>];</span><br></pre></td></tr></table></figure>

<p>这不是“读到一个垃圾值”这么简单，而是整个程序行为都不再受标准保障。</p>
<h3 id="10-3-空指针解引用"><a href="#10-3-空指针解引用" class="headerlink" title="10.3 空指针解引用"></a>10.3 空指针解引用</h3><p>一旦代码包含这类 UB，编译器的很多重排与删改都会变得“看起来很反直觉”。</p>
<p>所以要记住：</p>
<blockquote>
<p>UB 不只是 bug，它还会让你对性能和代码路径的直觉失效。</p>
</blockquote>
<hr>
<h2 id="11-size-t、符号位与索引"><a href="#11-size-t、符号位与索引" class="headerlink" title="11. size_t、符号位与索引"></a>11. <code>size_t</code>、符号位与索引</h2><p>数组和容器索引通常更适合用：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">size_t</span></span><br></pre></td></tr></table></figure>

<p>原因包括：</p>
<ul>
<li>表达“非负大小&#x2F;索引”更自然</li>
<li>与很多标准库接口匹配</li>
</ul>
<p>但也别机械套用：</p>
<ul>
<li>如果逻辑上需要表示 <code>-1</code> 这类哨兵，盲目用 <code>size_t</code> 反而会引入隐式转换坑</li>
</ul>
<p>更准确的原则是：</p>
<ul>
<li>让类型表达真实语义</li>
<li>避免无意识的 signed&#x2F;unsigned 混算</li>
</ul>
<hr>
<h2 id="12-内联、模板与零成本抽象"><a href="#12-内联、模板与零成本抽象" class="headerlink" title="12. 内联、模板与零成本抽象"></a>12. 内联、模板与零成本抽象</h2><p>现代 C++ 很多高性能抽象之所以能成立，靠的是：</p>
<ul>
<li>模板实例化</li>
<li>静态分发</li>
<li>内联</li>
</ul>
<p>例如一个模板函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function">T <span class="title">add</span><span class="params">(T a, T b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在优化后，往往不会留下“模板层”的运行时开销。<br>这就是所谓零成本抽象的核心基础之一。</p>
<hr>
<h2 id="13-为什么“看汇编”时要结合上下文"><a href="#13-为什么“看汇编”时要结合上下文" class="headerlink" title="13. 为什么“看汇编”时要结合上下文"></a>13. 为什么“看汇编”时要结合上下文</h2><p>只看一小段反汇编，很容易误判。<br>真正要结合这些条件：</p>
<ul>
<li>编译器版本</li>
<li>编译选项</li>
<li>目标架构</li>
<li>是否开启 LTO</li>
<li>是否开启异常 &#x2F; RTTI</li>
<li>热点输入数据形态</li>
</ul>
<p>同一份 C++ 源码，在不同平台上生成的机器码可能差异很大。</p>
<p>所以讨论优化时最好带上：</p>
<ul>
<li>编译器</li>
<li>版本</li>
<li><code>-O</code> 等级</li>
<li>CPU 架构</li>
</ul>
<hr>
<h2 id="14-实战里如何正确使用“汇编视角”"><a href="#14-实战里如何正确使用“汇编视角”" class="headerlink" title="14. 实战里如何正确使用“汇编视角”"></a>14. 实战里如何正确使用“汇编视角”</h2><h3 id="14-1-先确认算法和数据布局"><a href="#14-1-先确认算法和数据布局" class="headerlink" title="14.1 先确认算法和数据布局"></a>14.1 先确认算法和数据布局</h3><p>大头问题通常不在一条指令，而在：</p>
<ul>
<li>算法复杂度</li>
<li>访存模式</li>
<li>是否频繁分配</li>
<li>是否有锁争用</li>
</ul>
<h3 id="14-2-再确认编译器有没有帮你做该做的事"><a href="#14-2-再确认编译器有没有帮你做该做的事" class="headerlink" title="14.2 再确认编译器有没有帮你做该做的事"></a>14.2 再确认编译器有没有帮你做该做的事</h3><p>例如：</p>
<ul>
<li>是否内联</li>
<li>是否向量化</li>
<li>是否消掉多余拷贝</li>
</ul>
<h3 id="14-3-最后才考虑手写微优化"><a href="#14-3-最后才考虑手写微优化" class="headerlink" title="14.3 最后才考虑手写微优化"></a>14.3 最后才考虑手写微优化</h3><p>很多手写“聪明写法”在现代编译器前未必更好，还可能降低可读性和可维护性。</p>
<hr>
<h2 id="15-常见工具"><a href="#15-常见工具" class="headerlink" title="15. 常见工具"></a>15. 常见工具</h2><h3 id="15-1-Compiler-Explorer"><a href="#15-1-Compiler-Explorer" class="headerlink" title="15.1 Compiler Explorer"></a>15.1 Compiler Explorer</h3><p>最适合快速看：</p>
<ul>
<li>同一段代码在不同编译器下的汇编</li>
<li><code>-O0/-O2/-O3</code> 差异</li>
<li>向量化与内联效果</li>
</ul>
<h3 id="15-2-本地查看汇编"><a href="#15-2-本地查看汇编" class="headerlink" title="15.2 本地查看汇编"></a>15.2 本地查看汇编</h3><p>常见方式：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g++ -O3 -S main.cpp</span><br><span class="line">clang++ -O3 -S main.cpp</span><br><span class="line">objdump -d a.out</span><br></pre></td></tr></table></figure>

<h3 id="15-3-配合性能分析工具"><a href="#15-3-配合性能分析工具" class="headerlink" title="15.3 配合性能分析工具"></a>15.3 配合性能分析工具</h3><p>只看汇编还不够，最好配合：</p>
<ul>
<li><code>perf</code></li>
<li>Instruments</li>
<li>VTune</li>
<li>Nsight</li>
</ul>
<p>一起确认瓶颈在哪里。</p>
<hr>
<h2 id="16-高频误区"><a href="#16-高频误区" class="headerlink" title="16. 高频误区"></a>16. 高频误区</h2><h3 id="16-1-inline-不是强制内联"><a href="#16-1-inline-不是强制内联" class="headerlink" title="16.1 inline 不是强制内联"></a>16.1 <code>inline</code> 不是强制内联</h3><p>它更多还是语言层和 ODR 相关语义，真正是否内联由编译器决定。</p>
<h3 id="16-2-volatile-不是性能优化工具"><a href="#16-2-volatile-不是性能优化工具" class="headerlink" title="16.2 volatile 不是性能优化工具"></a>16.2 <code>volatile</code> 不是性能优化工具</h3><p><code>volatile</code> 主要用于表达“这个对象可能被外部环境改变”，并不是线程同步手段，也不该拿来替代 <code>std::atomic</code>。</p>
<h3 id="16-3-O3-不一定总比-O2-快"><a href="#16-3-O3-不一定总比-O2-快" class="headerlink" title="16.3 -O3 不一定总比 -O2 快"></a>16.3 <code>-O3</code> 不一定总比 <code>-O2</code> 快</h3><p>更激进优化可能：</p>
<ul>
<li>增加代码体积</li>
<li>降低指令缓存命中</li>
<li>引入其他副作用</li>
</ul>
<h3 id="16-4-手写汇编式代码不一定更快"><a href="#16-4-手写汇编式代码不一定更快" class="headerlink" title="16.4 手写汇编式代码不一定更快"></a>16.4 手写汇编式代码不一定更快</h3><p>现代编译器很强，很多“看起来更底层”的写法可能反而破坏优化空间。</p>
<hr>
<h2 id="17-一页总结"><a href="#17-一页总结" class="headerlink" title="17. 一页总结"></a>17. 一页总结</h2><p>从汇编角度看编译器优化，真正要建立的是这套思维：</p>
<ol>
<li>高层 C++ 写法最后都要落成机器码</li>
<li>编译器会在语义允许范围内做大量重写</li>
<li>能否优化好，取决于代码是否给了编译器足够清晰的信息</li>
<li>别名、分支、访存模式、未定义行为都会显著影响优化结果</li>
<li>优化结论应靠汇编和基准测试验证，而不是靠直觉</li>
</ol>
<hr>
<h2 id="18-建议继续补充的相关主题"><a href="#18-建议继续补充的相关主题" class="headerlink" title="18. 建议继续补充的相关主题"></a>18. 建议继续补充的相关主题</h2><p>和本篇衔接最紧密的内容：</p>
<ol>
<li>自动向量化与 SIMD</li>
<li>严格别名规则（strict aliasing）</li>
<li><code>restrict</code> &#x2F; noalias 风格约束</li>
<li>LTO &#x2F; PGO</li>
<li>基准测试与性能分析方法论</li>
</ol>
<hr>
<h2 id="19-参考资料"><a href="#19-参考资料" class="headerlink" title="19. 参考资料"></a>19. 参考资料</h2><ol>
<li><p>Compiler Explorer<br><a href="https://godbolt.org/">https://godbolt.org/</a></p>
</li>
<li><p>GCC Optimize Options<br><a href="https://gcc.gnu.org/onlinedocs/gcc/Optimize-Options.html">https://gcc.gnu.org/onlinedocs/gcc/Optimize-Options.html</a></p>
</li>
<li><p>Clang Optimization Remarks<br><a href="https://clang.llvm.org/docs/UsersManual.html">https://clang.llvm.org/docs/UsersManual.html</a></p>
</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>原子操作、内存序与无锁基础</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/09-%E5%8E%9F%E5%AD%90%E6%93%8D%E4%BD%9C-%E5%86%85%E5%AD%98%E5%BA%8F%E4%B8%8E%E6%97%A0%E9%94%81%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h1 id="原子操作、内存序与无锁基础"><a href="#原子操作、内存序与无锁基础" class="headerlink" title="原子操作、内存序与无锁基础"></a>原子操作、内存序与无锁基础</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：<code>std::atomic</code>、CAS、memory order、acquire&#x2F;release、<code>seq_cst</code>、false sharing、lock-free<br>核心目标：搞清楚“原子变量为什么不仅是线程安全的普通变量”，以及不同内存序到底在约束什么。</p>
</blockquote>
<hr>
<h2 id="1-为什么需要原子操作"><a href="#1-为什么需要原子操作" class="headerlink" title="1. 为什么需要原子操作"></a>1. 为什么需要原子操作</h2><p>并发程序里最核心的问题是：</p>
<ul>
<li>多个线程会同时访问共享数据</li>
<li>如果至少一个线程写，且没有同步，就会产生数据竞争</li>
</ul>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> counter = <span class="number">0</span>;</span><br><span class="line"><span class="comment">// 多线程同时 ++counter;</span></span><br></pre></td></tr></table></figure>

<p>这不是“结果偶尔不准”，而是未定义行为。</p>
<p>原子操作的价值就在于：</p>
<ul>
<li>某些共享读写可以不加互斥锁</li>
<li>但仍然具备明确同步语义</li>
</ul>
<hr>
<h2 id="2-std-atomic-是什么"><a href="#2-std-atomic-是什么" class="headerlink" title="2. std::atomic 是什么"></a>2. <code>std::atomic</code> 是什么</h2><p>最基本的用法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;atomic&gt;</span></span></span><br><span class="line"></span><br><span class="line">std::atomic&lt;<span class="type">int</span>&gt; counter&#123;<span class="number">0</span>&#125;;</span><br></pre></td></tr></table></figure>

<p>它提供的不是“更快的 int”，而是：</p>
<ul>
<li>不会被撕裂的原子读写</li>
<li>受内存模型约束的同步语义</li>
</ul>
<p>常见操作：</p>
<ul>
<li><code>load</code></li>
<li><code>store</code></li>
<li><code>fetch_add</code></li>
<li><code>fetch_sub</code></li>
<li><code>exchange</code></li>
<li><code>compare_exchange_weak</code></li>
<li><code>compare_exchange_strong</code></li>
</ul>
<hr>
<h2 id="3-原子不等于万能替代锁"><a href="#3-原子不等于万能替代锁" class="headerlink" title="3. 原子不等于万能替代锁"></a>3. 原子不等于万能替代锁</h2><p>原子适合的场景通常是：</p>
<ul>
<li>计数器</li>
<li>标志位</li>
<li>状态发布</li>
<li>无锁数据结构中的基本原语</li>
</ul>
<p>不适合的场景通常是：</p>
<ul>
<li>需要保护一整段复合逻辑</li>
<li>需要同时维护多个共享变量的不变式</li>
<li>业务逻辑复杂，容易写错同步关系</li>
</ul>
<p>一句话：</p>
<ul>
<li>原子擅长保护“一个共享状态”</li>
<li>锁擅长保护“一个临界区”</li>
</ul>
<hr>
<h2 id="4-最基本的原子操作"><a href="#4-最基本的原子操作" class="headerlink" title="4. 最基本的原子操作"></a>4. 最基本的原子操作</h2><h3 id="4-1-load-store"><a href="#4-1-load-store" class="headerlink" title="4.1 load &#x2F; store"></a>4.1 <code>load</code> &#x2F; <code>store</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::atomic&lt;<span class="type">int</span>&gt; x&#123;<span class="number">0</span>&#125;;</span><br><span class="line">x.<span class="built_in">store</span>(<span class="number">10</span>);</span><br><span class="line"><span class="type">int</span> v = x.<span class="built_in">load</span>();</span><br></pre></td></tr></table></figure>

<h3 id="4-2-fetch-add"><a href="#4-2-fetch-add" class="headerlink" title="4.2 fetch_add"></a>4.2 <code>fetch_add</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::atomic&lt;<span class="type">int</span>&gt; cnt&#123;<span class="number">0</span>&#125;;</span><br><span class="line">cnt.<span class="built_in">fetch_add</span>(<span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>这比 <code>cnt = cnt + 1</code> 更重要，因为它是一个不可分割的原子读改写。</p>
<h3 id="4-3-exchange"><a href="#4-3-exchange" class="headerlink" title="4.3 exchange"></a>4.3 <code>exchange</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> old = flag.<span class="built_in">exchange</span>(<span class="literal">true</span>);</span><br></pre></td></tr></table></figure>

<p>含义：</p>
<ul>
<li>把 <code>flag</code> 设为 <code>true</code></li>
<li>返回旧值</li>
</ul>
<hr>
<h2 id="5-CAS：无锁算法的核心原语"><a href="#5-CAS：无锁算法的核心原语" class="headerlink" title="5. CAS：无锁算法的核心原语"></a>5. CAS：无锁算法的核心原语</h2><p>CAS 指 Compare-And-Swap（或 Compare-And-Exchange）。</p>
<h3 id="5-1-直觉理解"><a href="#5-1-直觉理解" class="headerlink" title="5.1 直觉理解"></a>5.1 直觉理解</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">如果当前值仍然等于 expected，</span><br><span class="line">就把它改成 desired；</span><br><span class="line">否则不改，并告诉我失败了。</span><br></pre></td></tr></table></figure>

<h3 id="5-2-C-里的写法"><a href="#5-2-C-里的写法" class="headerlink" title="5.2 C++ 里的写法"></a>5.2 C++ 里的写法</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::atomic&lt;<span class="type">int</span>&gt; x&#123;<span class="number">0</span>&#125;;</span><br><span class="line"><span class="type">int</span> expected = <span class="number">0</span>;</span><br><span class="line"><span class="type">bool</span> ok = x.<span class="built_in">compare_exchange_strong</span>(expected, <span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>如果成功：</p>
<ul>
<li><code>x</code> 变成 1</li>
<li>返回 <code>true</code></li>
</ul>
<p>如果失败：</p>
<ul>
<li><code>x</code> 保持原值</li>
<li><code>expected</code> 会被写成当前实际值</li>
</ul>
<h3 id="5-3-weak-和-strong"><a href="#5-3-weak-和-strong" class="headerlink" title="5.3 weak 和 strong"></a>5.3 <code>weak</code> 和 <code>strong</code></h3><ul>
<li><code>compare_exchange_weak</code>：允许伪失败，适合循环重试</li>
<li><code>compare_exchange_strong</code>：不允许伪失败，语义更直接</li>
</ul>
<p>常见模式：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> expected = old;</span><br><span class="line"><span class="keyword">while</span> (!x.<span class="built_in">compare_exchange_weak</span>(expected, new_value)) &#123;</span><br><span class="line">    <span class="comment">// expected 已被更新为当前值，继续重试</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="6-“原子”到底保证了什么"><a href="#6-“原子”到底保证了什么" class="headerlink" title="6. “原子”到底保证了什么"></a>6. “原子”到底保证了什么</h2><p>原子操作通常有两层含义：</p>
<h3 id="6-1-操作本身不可分割"><a href="#6-1-操作本身不可分割" class="headerlink" title="6.1 操作本身不可分割"></a>6.1 操作本身不可分割</h3><p>例如 <code>fetch_add</code> 不会被拆成“先读、再改、再写”被别的线程插进来。</p>
<h3 id="6-2-它还可能携带同步顺序语义"><a href="#6-2-它还可能携带同步顺序语义" class="headerlink" title="6.2 它还可能携带同步顺序语义"></a>6.2 它还可能携带同步顺序语义</h3><p>这就进入<strong>内存序</strong>问题。</p>
<p>如果只知道“原子不会撕裂”，还远远不够。</p>
<hr>
<h2 id="7-为什么会有内存序"><a href="#7-为什么会有内存序" class="headerlink" title="7. 为什么会有内存序"></a>7. 为什么会有内存序</h2><p>现代 CPU 和编译器都会重排指令，只要不改变单线程可观察结果就行。<br>但并发下，如果没有同步约束，另一个线程看到的顺序可能和源码顺序不一样。</p>
<p>所以原子操作不只是“安全读写”，还承担：</p>
<ul>
<li>约束编译器重排</li>
<li>约束 CPU 可见性顺序</li>
</ul>
<p>这就是 <code>memory_order</code> 的意义。</p>
<hr>
<h2 id="8-六种常见内存序"><a href="#8-六种常见内存序" class="headerlink" title="8. 六种常见内存序"></a>8. 六种常见内存序</h2><p>C++ 里最常见的有：</p>
<ul>
<li><code>memory_order_relaxed</code></li>
<li><code>memory_order_consume</code></li>
<li><code>memory_order_acquire</code></li>
<li><code>memory_order_release</code></li>
<li><code>memory_order_acq_rel</code></li>
<li><code>memory_order_seq_cst</code></li>
</ul>
<p>实际工程里最常用、最值得掌握的是：</p>
<ul>
<li><code>relaxed</code></li>
<li><code>acquire</code></li>
<li><code>release</code></li>
<li><code>acq_rel</code></li>
<li><code>seq_cst</code></li>
</ul>
<p><code>consume</code> 在现代工程里很少主动使用。</p>
<hr>
<h2 id="9-relaxed：只保证原子性，不保证顺序"><a href="#9-relaxed：只保证原子性，不保证顺序" class="headerlink" title="9. relaxed：只保证原子性，不保证顺序"></a>9. <code>relaxed</code>：只保证原子性，不保证顺序</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">counter.<span class="built_in">fetch_add</span>(<span class="number">1</span>, std::memory_order_relaxed);</span><br></pre></td></tr></table></figure>

<p>适合：</p>
<ul>
<li>纯计数</li>
<li>统计量</li>
<li>不需要借此发布其他数据</li>
</ul>
<p>不适合：</p>
<ul>
<li>“先写数据，再置位通知别人读取”</li>
</ul>
<p>因为 <code>relaxed</code> 不保证其他普通内存写入的可见顺序。</p>
<hr>
<h2 id="10-release-acquire：发布与获取"><a href="#10-release-acquire：发布与获取" class="headerlink" title="10. release &#x2F; acquire：发布与获取"></a>10. <code>release</code> &#x2F; <code>acquire</code>：发布与获取</h2><p>这是最重要的一组。</p>
<h3 id="10-1-发布方：release"><a href="#10-1-发布方：release" class="headerlink" title="10.1 发布方：release"></a>10.1 发布方：<code>release</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">data = <span class="number">42</span>;</span><br><span class="line">ready.<span class="built_in">store</span>(<span class="literal">true</span>, std::memory_order_release);</span><br></pre></td></tr></table></figure>

<p>含义可以粗略理解为：</p>
<ul>
<li>在这之前的写入，不能被重排到这个 <code>store</code> 之后</li>
</ul>
<h3 id="10-2-获取方：acquire"><a href="#10-2-获取方：acquire" class="headerlink" title="10.2 获取方：acquire"></a>10.2 获取方：<code>acquire</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (ready.<span class="built_in">load</span>(std::memory_order_acquire)) &#123;</span><br><span class="line">    <span class="built_in">use</span>(data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果这个 <code>acquire load</code> 读到了对应的 <code>release store</code> 写入的值，那么发布前的写入也对当前线程可见。</p>
<h3 id="10-3-最经典的发布-订阅模式"><a href="#10-3-最经典的发布-订阅模式" class="headerlink" title="10.3 最经典的发布-订阅模式"></a>10.3 最经典的发布-订阅模式</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;atomic&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> data = <span class="number">0</span>;</span><br><span class="line">std::atomic&lt;<span class="type">bool</span>&gt; ready&#123;<span class="literal">false</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">producer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    data = <span class="number">42</span>;</span><br><span class="line">    ready.<span class="built_in">store</span>(<span class="literal">true</span>, std::memory_order_release);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">consumer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (!ready.<span class="built_in">load</span>(std::memory_order_acquire)) &#123;&#125;</span><br><span class="line">    <span class="comment">// 这里可以安全看到 data = 42</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这就是 acquire&#x2F;release 最核心的使用场景。</p>
<hr>
<h2 id="11-acq-rel：读改写操作常用"><a href="#11-acq-rel：读改写操作常用" class="headerlink" title="11. acq_rel：读改写操作常用"></a>11. <code>acq_rel</code>：读改写操作常用</h2><p>对于像 <code>fetch_add</code>、<code>exchange</code>、CAS 这样的读改写操作，经常用：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::memory_order_acq_rel</span><br></pre></td></tr></table></figure>

<p>它表示：</p>
<ul>
<li>对之前的写有 release 效果</li>
<li>对之后的读有 acquire 效果</li>
</ul>
<p>适合：</p>
<ul>
<li>需要同时承担“发布 + 获取”角色的 RMW 操作</li>
</ul>
<hr>
<h2 id="12-seq-cst：最强也最直观"><a href="#12-seq-cst：最强也最直观" class="headerlink" title="12. seq_cst：最强也最直观"></a>12. <code>seq_cst</code>：最强也最直观</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">x.<span class="built_in">store</span>(<span class="number">1</span>, std::memory_order_seq_cst);</span><br></pre></td></tr></table></figure>

<p>它提供最强、最容易理解的全局顺序语义。<br>可以先粗略理解为：</p>
<ul>
<li>所有 <code>seq_cst</code> 原子操作在所有线程看来像排成了一条总顺序</li>
</ul>
<p>优点：</p>
<ul>
<li>最不容易想错</li>
</ul>
<p>缺点：</p>
<ul>
<li>可能比更弱顺序更保守</li>
</ul>
<p>经验上：</p>
<ul>
<li>不确定时可以先用 <code>seq_cst</code></li>
<li>真有性能证据，再考虑是否降到 acquire&#x2F;release 或 relaxed</li>
</ul>
<hr>
<h2 id="13-一个常见误区：原子变量保护不了“旁边的普通变量”，除非顺序写对"><a href="#13-一个常见误区：原子变量保护不了“旁边的普通变量”，除非顺序写对" class="headerlink" title="13. 一个常见误区：原子变量保护不了“旁边的普通变量”，除非顺序写对"></a>13. 一个常见误区：原子变量保护不了“旁边的普通变量”，除非顺序写对</h2><p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> data = <span class="number">0</span>;</span><br><span class="line">std::atomic&lt;<span class="type">bool</span>&gt; ready&#123;<span class="literal">false</span>&#125;;</span><br></pre></td></tr></table></figure>

<p>如果你这样写：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">data = <span class="number">42</span>;</span><br><span class="line">ready.<span class="built_in">store</span>(<span class="literal">true</span>, std::memory_order_relaxed);</span><br></pre></td></tr></table></figure>

<p>另一个线程即便读到 <code>ready == true</code>，也不一定能可靠看见 <code>data == 42</code>。<br>因为这里只保证 <code>ready</code> 本身原子，不保证普通变量 <code>data</code> 的可见顺序。</p>
<p>所以：</p>
<ul>
<li>“用一个原子标志通知别人去读普通数据”<br>这个模式必须认真选内存序</li>
</ul>
<hr>
<h2 id="14-自旋等待与忙等"><a href="#14-自旋等待与忙等" class="headerlink" title="14. 自旋等待与忙等"></a>14. 自旋等待与忙等</h2><p>原子标志很容易写出自旋：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (!ready.<span class="built_in">load</span>(std::memory_order_acquire)) &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这在短等待场景可能可接受，但有风险：</p>
<ul>
<li>浪费 CPU</li>
<li>争抢资源</li>
<li>等待时间一长非常低效</li>
</ul>
<p>所以如果等待时间不可控，通常更适合：</p>
<ul>
<li>条件变量</li>
<li>futex &#x2F; event</li>
<li>更高层并发原语</li>
</ul>
<hr>
<h2 id="15-atomic-flag-与自旋锁"><a href="#15-atomic-flag-与自旋锁" class="headerlink" title="15. atomic_flag 与自旋锁"></a>15. <code>atomic_flag</code> 与自旋锁</h2><p>最简单的原子标志是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::atomic_flag lock = ATOMIC_FLAG_INIT;</span><br></pre></td></tr></table></figure>

<p>可实现一个最简单自旋锁：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (lock.<span class="built_in">test_and_set</span>(std::memory_order_acquire)) &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// critical section</span></span><br><span class="line"></span><br><span class="line">lock.<span class="built_in">clear</span>(std::memory_order_release);</span><br></pre></td></tr></table></figure>

<p>但要明确：</p>
<ul>
<li>这只是帮助理解 acquire&#x2F;release 的经典例子</li>
<li>真实工程里自旋锁并不一定是好选择</li>
</ul>
<p>原因包括：</p>
<ul>
<li>高竞争下性能糟糕</li>
<li>容易烧 CPU</li>
<li>可能不公平</li>
</ul>
<hr>
<h2 id="16-lock-free-不等于更快"><a href="#16-lock-free-不等于更快" class="headerlink" title="16. lock-free 不等于更快"></a>16. lock-free 不等于更快</h2><p>这是并发里最常见的误判之一。</p>
<p>“无锁”通常只表示：</p>
<ul>
<li>线程推进不依赖传统互斥锁</li>
</ul>
<p>它不自动意味着：</p>
<ul>
<li>更低延迟</li>
<li>更高吞吐</li>
<li>更少 cache traffic</li>
</ul>
<p>实际上，原子和 CAS 往往会带来：</p>
<ul>
<li>cache line 抖动</li>
<li>重试开销</li>
<li>更难维护的代码</li>
</ul>
<p>所以工程经验是：</p>
<blockquote>
<p>先用正确、清晰的同步方案，再证明锁真的成了瓶颈，才考虑无锁化。</p>
</blockquote>
<hr>
<h2 id="17-false-sharing-在原子场景里更常见"><a href="#17-false-sharing-在原子场景里更常见" class="headerlink" title="17. false sharing 在原子场景里更常见"></a>17. false sharing 在原子场景里更常见</h2><p>多个线程频繁更新不同的原子变量，如果这些变量落在同一个 cache line，也会很慢。</p>
<p>例如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Counters</span> &#123;</span><br><span class="line">    std::atomic&lt;<span class="type">long</span> <span class="type">long</span>&gt; a;</span><br><span class="line">    std::atomic&lt;<span class="type">long</span> <span class="type">long</span>&gt; b;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如果两个线程分别只改 <code>a</code> 和 <code>b</code>，仍可能严重互相干扰。</p>
<p>常见缓解方式：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">alignas</span>(<span class="number">64</span>) Counter &#123;</span><br><span class="line">    std::atomic&lt;<span class="type">long</span> <span class="type">long</span>&gt; value&#123;<span class="number">0</span>&#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>或者：</p>
<ul>
<li>每线程本地累加</li>
<li>最后统一合并</li>
</ul>
<hr>
<h2 id="18-原子引用计数与-shared-ptr"><a href="#18-原子引用计数与-shared-ptr" class="headerlink" title="18. 原子引用计数与 shared_ptr"></a>18. 原子引用计数与 <code>shared_ptr</code></h2><p><code>shared_ptr</code> 之所以比 <code>unique_ptr</code> 重，很大一部分原因就在于：</p>
<ul>
<li>它内部常涉及线程安全的引用计数更新</li>
</ul>
<p>也就是说，很多“我只是图省事用了 <code>shared_ptr</code>”的代码，实际上已经把原子开销带进来了。</p>
<p>所以在性能敏感路径里：</p>
<ul>
<li>不要把共享所有权当默认方案</li>
</ul>
<hr>
<h2 id="19-一个高频模式：单生产者发布结果"><a href="#19-一个高频模式：单生产者发布结果" class="headerlink" title="19. 一个高频模式：单生产者发布结果"></a>19. 一个高频模式：单生产者发布结果</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Result</span> &#123;</span><br><span class="line">    <span class="type">int</span> value;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">Result result;</span><br><span class="line">std::atomic&lt;<span class="type">bool</span>&gt; done&#123;<span class="literal">false</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">producer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    result.value = <span class="number">123</span>;</span><br><span class="line">    done.<span class="built_in">store</span>(<span class="literal">true</span>, std::memory_order_release);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">consumer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (!done.<span class="built_in">load</span>(std::memory_order_acquire)) &#123;&#125;</span><br><span class="line">    <span class="comment">// 这里读取 result.value</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这类模式是 acquire&#x2F;release 最值得优先熟练掌握的用法。</p>
<hr>
<h2 id="20-一个高频模式：纯统计计数"><a href="#20-一个高频模式：纯统计计数" class="headerlink" title="20. 一个高频模式：纯统计计数"></a>20. 一个高频模式：纯统计计数</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::atomic&lt;<span class="type">long</span> <span class="type">long</span>&gt; counter&#123;<span class="number">0</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">worker</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100000</span>; ++i) &#123;</span><br><span class="line">        counter.<span class="built_in">fetch_add</span>(<span class="number">1</span>, std::memory_order_relaxed);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里若只是最后读总数，<code>relaxed</code> 往往就足够，因为你并不依赖这个计数去同步其他数据。</p>
<hr>
<h2 id="21-常见误区"><a href="#21-常见误区" class="headerlink" title="21. 常见误区"></a>21. 常见误区</h2><h3 id="21-1-“原子-不需要思考同步”"><a href="#21-1-“原子-不需要思考同步”" class="headerlink" title="21.1 “原子 &#x3D; 不需要思考同步”"></a>21.1 “原子 &#x3D; 不需要思考同步”</h3><p>错。<br>原子只解决了一部分问题，真正难的是：</p>
<ul>
<li>你想建立什么可见性关系</li>
<li>你要不要顺序保证</li>
</ul>
<h3 id="21-2-“我用了原子，就能保护一大片普通变量”"><a href="#21-2-“我用了原子，就能保护一大片普通变量”" class="headerlink" title="21.2 “我用了原子，就能保护一大片普通变量”"></a>21.2 “我用了原子，就能保护一大片普通变量”</h3><p>只有当发布&#x2F;获取关系写对时才成立。</p>
<h3 id="21-3-“lock-free-一定更先进”"><a href="#21-3-“lock-free-一定更先进”" class="headerlink" title="21.3 “lock-free 一定更先进”"></a>21.3 “lock-free 一定更先进”</h3><p>并发工程里先进不先进不重要，重要的是：</p>
<ul>
<li>正确</li>
<li>可维护</li>
<li>有实际性能收益</li>
</ul>
<h3 id="21-4-“volatile-可以替代-atomic”"><a href="#21-4-“volatile-可以替代-atomic”" class="headerlink" title="21.4 “volatile 可以替代 atomic”"></a>21.4 “<code>volatile</code> 可以替代 <code>atomic</code>”</h3><p>不能。<br><code>volatile</code> 不是线程同步原语。</p>
<hr>
<h2 id="22-一页总结"><a href="#22-一页总结" class="headerlink" title="22. 一页总结"></a>22. 一页总结</h2><p>原子操作最重要的不是 API 数量，而是建立这条理解链：</p>
<ol>
<li>原子保证操作本身不会被数据竞争破坏</li>
<li>内存序决定不同线程看到这些操作及相关普通内存的顺序关系</li>
<li><code>relaxed</code> 只保原子性，不保发布顺序</li>
<li><code>release/acquire</code> 是最常用的发布-获取模型</li>
<li><code>seq_cst</code> 最直观，适合先写对</li>
<li>无锁不是性能银弹，false sharing 和 CAS 重试都可能很贵</li>
</ol>
<p>如果只记两个最高频结论：</p>
<ul>
<li>纯计数：优先考虑 <code>relaxed</code></li>
<li>发布数据给别的线程：优先考虑 <code>release/acquire</code></li>
</ul>
<hr>
<h2 id="23-建议继续补充的相关主题"><a href="#23-建议继续补充的相关主题" class="headerlink" title="23. 建议继续补充的相关主题"></a>23. 建议继续补充的相关主题</h2><p>和本篇衔接最紧密的内容：</p>
<ol>
<li>C++ 内存模型正式定义</li>
<li>ABA 问题</li>
<li>无锁队列 &#x2F; Michael-Scott Queue</li>
<li>hazard pointers &#x2F; epoch reclamation</li>
<li><code>atomic_ref</code> 与 <code>std::barrier</code></li>
</ol>
<hr>
<h2 id="24-参考资料"><a href="#24-参考资料" class="headerlink" title="24. 参考资料"></a>24. 参考资料</h2><ol>
<li><p>cppreference: <code>std::atomic</code><br><a href="https://en.cppreference.com/w/cpp/atomic/atomic">https://en.cppreference.com/w/cpp/atomic/atomic</a></p>
</li>
<li><p>cppreference: memory order<br><a href="https://en.cppreference.com/w/cpp/atomic/memory_order">https://en.cppreference.com/w/cpp/atomic/memory_order</a></p>
</li>
<li><p>C++ Core Guidelines, Concurrency<br><a href="https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines">https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines</a></p>
</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>vector 容器优化编程笔记</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/05-vector%E5%AE%B9%E5%99%A8%E4%BC%98%E5%8C%96%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="vector-容器优化编程笔记"><a href="#vector-容器优化编程笔记" class="headerlink" title="vector 容器优化编程笔记"></a>vector 容器优化编程笔记</h1><p>时间：2025&#x2F;11&#x2F;22<br>课程地址：Bilibili - BV1qF411T7sd<br>课件仓库：<code>parallel101/course</code></p>
<hr>
<h2 id="1-先建立正确认知"><a href="#1-先建立正确认知" class="headerlink" title="1. 先建立正确认知"></a>1. 先建立正确认知</h2><p><code>std::vector&lt;T&gt;</code> 本质上是一个<strong>连续内存上的动态数组</strong>：</p>
<ul>
<li>支持 <code>O(1)</code> 随机访问</li>
<li>尾部追加通常是摊还 <code>O(1)</code></li>
<li>中间插入、删除通常是 <code>O(n)</code></li>
<li>元素存储连续，因此<strong>缓存友好</strong>，这也是它在高性能 C++ 里极其常见的原因</li>
</ul>
<p>可以把一个 <code>vector</code> 理解成 3 个核心状态：</p>
<ul>
<li><code>data</code>：底层连续内存首地址</li>
<li><code>size</code>：当前已构造的元素个数</li>
<li><code>capacity</code>：当前已分配、但不一定都已使用的容量</li>
</ul>
<p>示意：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">栈上（若 vec 是局部变量）</span><br><span class="line">    vec 对象本身</span><br><span class="line">    ├── data -----&gt; 堆上连续内存</span><br><span class="line">    ├── size</span><br><span class="line">    └── capacity</span><br><span class="line"></span><br><span class="line">堆上</span><br><span class="line">    [elem0][elem1][elem2]...[elemN-1]</span><br></pre></td></tr></table></figure>

<p>注意：</p>
<ul>
<li><code>vector</code> 对象本身未必一定在栈上，它也可以位于堆上、全局区或作为成员存在</li>
<li>但它管理的<strong>元素存储区</strong>通常在动态分配的连续内存中</li>
</ul>
<hr>
<h2 id="2-底层发生了什么"><a href="#2-底层发生了什么" class="headerlink" title="2. 底层发生了什么"></a>2. 底层发生了什么</h2><h3 id="2-1-分配器-allocator"><a href="#2-1-分配器-allocator" class="headerlink" title="2.1 分配器 allocator"></a>2.1 分配器 allocator</h3><p><code>vector</code> 并不是直接手写 <code>malloc/free</code>，而是通过分配器管理内存。标准库实现里更准确地说，通常是通过 <code>std::allocator_traits&lt;Alloc&gt;</code> 来调用：</p>
<table>
<thead>
<tr>
<th>操作</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><code>allocate(n)</code></td>
<td>分配可容纳 <code>n</code> 个 <code>T</code> 的原始内存</td>
</tr>
<tr>
<td><code>deallocate(p, n)</code></td>
<td>释放之前分配的内存</td>
</tr>
<tr>
<td><code>construct(p, args...)</code></td>
<td>在指定地址构造对象</td>
</tr>
<tr>
<td><code>destroy(p)</code></td>
<td>调用对象析构</td>
</tr>
</tbody></table>
<h3 id="2-2-扩容时的流程"><a href="#2-2-扩容时的流程" class="headerlink" title="2.2 扩容时的流程"></a>2.2 扩容时的流程</h3><p>当 <code>size == capacity</code> 且继续插入时，<code>vector</code> 通常会：</p>
<ol>
<li>申请一块更大的新内存</li>
<li>将旧元素搬迁到新内存</li>
<li>销毁旧内存中的对象</li>
<li>释放旧内存</li>
<li>更新 <code>data/capacity</code></li>
</ol>
<p>这里的关键代价不是“申请内存”本身，而是<strong>元素搬迁</strong>。</p>
<p>如果 <code>T</code>：</p>
<ul>
<li>支持廉价移动，并且移动构造 <code>noexcept</code>，扩容代价通常较小</li>
<li>只能复制，或者复制&#x2F;移动代价大，扩容成本就会明显上升</li>
</ul>
<p>对平凡类型（如 <code>int/float/POD</code>）来说，搬迁通常更便宜。</p>
<h3 id="2-3-为什么-vector-性能常常很好"><a href="#2-3-为什么-vector-性能常常很好" class="headerlink" title="2.3 为什么 vector 性能常常很好"></a>2.3 为什么 <code>vector</code> 性能常常很好</h3><p>核心不是“接口简单”，而是它的存储模型很适合现代 CPU：</p>
<ul>
<li>连续内存，缓存命中率高</li>
<li>顺序遍历容易被硬件预取</li>
<li>适合 SIMD &#x2F; 批量处理</li>
<li>与 C 接口天然兼容，可直接用 <code>data()</code></li>
</ul>
<p>所以很多场景中，哪怕算法复杂度一样，<code>vector</code> 也会比 <code>list/map</code> 这类节点式容器更快。</p>
<hr>
<h2 id="3-迭代器、指针与连续内存"><a href="#3-迭代器、指针与连续内存" class="headerlink" title="3. 迭代器、指针与连续内存"></a>3. 迭代器、指针与连续内存</h2><p><code>vector</code> 的迭代器是<strong>随机访问迭代器</strong>，很多实现中本质上接近普通指针。</p>
<p>常见容器对比：</p>
<table>
<thead>
<tr>
<th>容器</th>
<th>迭代器类型</th>
<th>连续内存</th>
</tr>
</thead>
<tbody><tr>
<td><code>vector</code></td>
<td>随机访问</td>
<td>是</td>
</tr>
<tr>
<td><code>array</code></td>
<td>随机访问</td>
<td>是</td>
</tr>
<tr>
<td><code>deque</code></td>
<td>随机访问</td>
<td>否</td>
</tr>
<tr>
<td><code>list</code></td>
<td>双向迭代器</td>
<td>否</td>
</tr>
<tr>
<td><code>set/map</code></td>
<td>双向迭代器</td>
<td>否</td>
</tr>
</tbody></table>
<p>这意味着：</p>
<ul>
<li><code>vector</code> 支持 <code>it + n</code>、<code>it[n]</code></li>
<li>可高效配合排序、二分、批量算法</li>
<li>可用 <code>data()</code> 直接获得底层首地址</li>
</ul>
<hr>
<h2 id="4-常用接口整理"><a href="#4-常用接口整理" class="headerlink" title="4. 常用接口整理"></a>4. 常用接口整理</h2><h3 id="4-1-创建"><a href="#4-1-创建" class="headerlink" title="4.1 创建"></a>4.1 创建</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; a;          <span class="comment">// 空 vector</span></span><br><span class="line"><span class="function">std::vector&lt;<span class="type">int</span>&gt; <span class="title">b</span><span class="params">(<span class="number">4</span>)</span></span>;       <span class="comment">// 4 个元素，值初始化为 0</span></span><br><span class="line"><span class="function">std::vector&lt;<span class="type">int</span>&gt; <span class="title">c</span><span class="params">(<span class="number">4</span>, <span class="number">7</span>)</span></span>;    <span class="comment">// 4 个元素，均为 7</span></span><br></pre></td></tr></table></figure>

<h3 id="4-2-读写与访问"><a href="#4-2-读写与访问" class="headerlink" title="4.2 读写与访问"></a>4.2 读写与访问</h3><table>
<thead>
<tr>
<th>接口</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>a[i]</code></td>
<td><code>O(1)</code> 访问，不做边界检查</td>
</tr>
<tr>
<td><code>a.at(i)</code></td>
<td>带边界检查，越界会抛异常</td>
</tr>
<tr>
<td><code>a.front()</code></td>
<td>首元素，空容器上调用是未定义行为</td>
</tr>
<tr>
<td><code>a.back()</code></td>
<td>尾元素，空容器上调用是未定义行为</td>
</tr>
<tr>
<td><code>a.data()</code></td>
<td>返回底层连续内存首地址；空容器时不可解引用</td>
</tr>
<tr>
<td><code>a.size()</code></td>
<td>当前元素个数</td>
</tr>
<tr>
<td><code>a.capacity()</code></td>
<td>当前容量</td>
</tr>
<tr>
<td><code>a.empty()</code></td>
<td>是否为空</td>
</tr>
</tbody></table>
<h3 id="4-3-修改"><a href="#4-3-修改" class="headerlink" title="4.3 修改"></a>4.3 修改</h3><table>
<thead>
<tr>
<th>接口</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>push_back(x)</code></td>
<td>尾部追加一个元素</td>
</tr>
<tr>
<td><code>emplace_back(args...)</code></td>
<td>尾部原位构造对象</td>
</tr>
<tr>
<td><code>pop_back()</code></td>
<td>删除尾元素，不返回值</td>
</tr>
<tr>
<td><code>clear()</code></td>
<td>清空元素，通常不释放 capacity</td>
</tr>
<tr>
<td><code>resize(n)</code></td>
<td>改变 size；变大时会构造新元素</td>
</tr>
<tr>
<td><code>resize(n, val)</code></td>
<td>扩大时用 <code>val</code> 填充</td>
</tr>
<tr>
<td><code>reserve(n)</code></td>
<td>预留至少 <code>n</code> 个容量，不改变 size</td>
</tr>
<tr>
<td><code>shrink_to_fit()</code></td>
<td>请求回收多余容量，但不保证一定执行</td>
</tr>
<tr>
<td><code>insert(pos, x)</code></td>
<td>在任意位置插入，通常需要搬移后续元素</td>
</tr>
<tr>
<td><code>erase(pos)</code></td>
<td>删除一个元素，后续元素前移</td>
</tr>
<tr>
<td><code>erase(first, last)</code></td>
<td>删除一个区间</td>
</tr>
<tr>
<td><code>assign(...)</code></td>
<td>覆盖原有内容</td>
</tr>
<tr>
<td><code>swap(other)</code></td>
<td>常数级交换内部资源</td>
</tr>
</tbody></table>
<h3 id="4-4-reserve-和-resize-的区别"><a href="#4-4-reserve-和-resize-的区别" class="headerlink" title="4.4 reserve 和 resize 的区别"></a>4.4 <code>reserve</code> 和 <code>resize</code> 的区别</h3><p>这是最容易混淆，也最影响性能的一组接口。</p>
<table>
<thead>
<tr>
<th>接口</th>
<th>改 size</th>
<th>改 capacity</th>
<th>是否构造元素</th>
</tr>
</thead>
<tbody><tr>
<td><code>reserve(n)</code></td>
<td>否</td>
<td>是，至少到 <code>n</code></td>
<td>否</td>
</tr>
<tr>
<td><code>resize(n)</code></td>
<td>是</td>
<td>可能会变大</td>
<td>是</td>
</tr>
</tbody></table>
<p>例子：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line">v.<span class="built_in">reserve</span>(<span class="number">100</span>); <span class="comment">// 只预留空间，还没有元素</span></span><br><span class="line">v.<span class="built_in">resize</span>(<span class="number">100</span>);  <span class="comment">// 现在真的有 100 个元素了</span></span><br></pre></td></tr></table></figure>

<p>如果你只是“后面准备持续 <code>push_back</code> 数据”，通常应该优先考虑 <code>reserve</code>，而不是 <code>resize</code>。</p>
<hr>
<h2 id="5-复杂度与性能直觉"><a href="#5-复杂度与性能直觉" class="headerlink" title="5. 复杂度与性能直觉"></a>5. 复杂度与性能直觉</h2><table>
<thead>
<tr>
<th>操作</th>
<th>复杂度</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>随机访问 <code>a[i]</code></td>
<td><code>O(1)</code></td>
<td>动态数组优势</td>
</tr>
<tr>
<td><code>push_back</code></td>
<td>摊还 <code>O(1)</code></td>
<td>扩容那次会更贵</td>
</tr>
<tr>
<td><code>pop_back</code></td>
<td><code>O(1)</code></td>
<td>尾删很便宜</td>
</tr>
<tr>
<td><code>insert</code> 到尾部</td>
<td>摊还 <code>O(1)</code></td>
<td>本质接近 <code>push_back</code></td>
</tr>
<tr>
<td><code>insert/erase</code> 中间位置</td>
<td><code>O(n)</code></td>
<td>后续元素要搬移</td>
</tr>
<tr>
<td>顺序遍历</td>
<td><code>O(n)</code></td>
<td>且通常缓存友好</td>
</tr>
</tbody></table>
<p>一句话总结：</p>
<ul>
<li><strong>尾部加删很适合 vector</strong></li>
<li><strong>头部&#x2F;中间频繁插删不适合 vector</strong></li>
</ul>
<hr>
<h2 id="6-迭代器、引用、指针何时失效"><a href="#6-迭代器、引用、指针何时失效" class="headerlink" title="6. 迭代器、引用、指针何时失效"></a>6. 迭代器、引用、指针何时失效</h2><p>这是 <code>vector</code> 最重要的易错点之一。</p>
<h3 id="6-1-会导致整体失效的典型情况"><a href="#6-1-会导致整体失效的典型情况" class="headerlink" title="6.1 会导致整体失效的典型情况"></a>6.1 会导致整体失效的典型情况</h3><p>一旦发生<strong>扩容</strong>，原来的：</p>
<ul>
<li>迭代器</li>
<li>指针</li>
<li>引用</li>
</ul>
<p>都可能全部失效，因为底层内存地址变了。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line">v.<span class="built_in">reserve</span>(<span class="number">1</span>);</span><br><span class="line">v.<span class="built_in">push_back</span>(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line"><span class="type">int</span>* p = &amp;v[<span class="number">0</span>];</span><br><span class="line">v.<span class="built_in">push_back</span>(<span class="number">20</span>); <span class="comment">// 可能扩容</span></span><br><span class="line"><span class="comment">// p 此时可能已经悬空，不能继续使用</span></span><br></pre></td></tr></table></figure>

<h3 id="6-2-局部失效的情况"><a href="#6-2-局部失效的情况" class="headerlink" title="6.2 局部失效的情况"></a>6.2 局部失效的情况</h3><p>即使没有扩容，<code>insert/erase</code> 也会让<strong>插入点&#x2F;删除点及其之后</strong>的迭代器、引用、指针失效，因为元素位置被搬动了。</p>
<h3 id="6-3-记忆方式"><a href="#6-3-记忆方式" class="headerlink" title="6.3 记忆方式"></a>6.3 记忆方式</h3><p>可以直接记成一句：</p>
<blockquote>
<p>只要 <code>vector</code> 的元素位置可能发生变化，相关迭代器&#x2F;引用&#x2F;指针就要重新获取。</p>
</blockquote>
<hr>
<h2 id="7-高性能写法与优化要点"><a href="#7-高性能写法与优化要点" class="headerlink" title="7. 高性能写法与优化要点"></a>7. 高性能写法与优化要点</h2><h3 id="7-1-已知大概元素个数时，先-reserve"><a href="#7-1-已知大概元素个数时，先-reserve" class="headerlink" title="7.1 已知大概元素个数时，先 reserve"></a>7.1 已知大概元素个数时，先 <code>reserve</code></h3><p>这是最实用、收益也最稳定的优化。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line">v.<span class="built_in">reserve</span>(n);</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    v.<span class="built_in">push_back</span>(i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>好处：</p>
<ul>
<li>避免多次扩容</li>
<li>减少元素重复搬迁</li>
<li>降低 allocator 调用次数</li>
</ul>
<h3 id="7-2-“稍后赋值”时，不要误用-resize"><a href="#7-2-“稍后赋值”时，不要误用-resize" class="headerlink" title="7.2 “稍后赋值”时，不要误用 resize"></a>7.2 “稍后赋值”时，不要误用 <code>resize</code></h3><p>如果你只是想“预留空间，后面再逐个写入”，错误写法往往是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line">v.<span class="built_in">resize</span>(n); <span class="comment">// 先构造 n 个元素</span></span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    v.<span class="built_in">push_back</span>(i); <span class="comment">// 最终 size 变成 2n，逻辑也错了</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>正确思路通常是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line">v.<span class="built_in">reserve</span>(n);</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    v.<span class="built_in">push_back</span>(i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果你确实要“先开好 n 个元素，再按下标写”，那才用 <code>resize(n)</code>：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line">v.<span class="built_in">resize</span>(n);</span><br><span class="line"><span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">    v[i] = i;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="7-3-对复杂对象，优先考虑-emplace-back"><a href="#7-3-对复杂对象，优先考虑-emplace-back" class="headerlink" title="7.3 对复杂对象，优先考虑 emplace_back"></a>7.3 对复杂对象，优先考虑 <code>emplace_back</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    std::string name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="built_in">Person</span>(std::string n, <span class="type">int</span> a) : <span class="built_in">name</span>(std::<span class="built_in">move</span>(n)), <span class="built_in">age</span>(a) &#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">std::vector&lt;Person&gt; v;</span><br><span class="line">v.<span class="built_in">emplace_back</span>(<span class="string">&quot;alice&quot;</span>, <span class="number">18</span>);</span><br></pre></td></tr></table></figure>

<p><code>emplace_back</code> 的主要收益是：</p>
<ul>
<li>直接在尾部构造对象</li>
<li>减少临时对象</li>
</ul>
<p>但对 <code>int/double</code> 这类简单类型，<code>emplace_back</code> 和 <code>push_back</code> 通常差别不大。</p>
<h3 id="7-4-批量删除时使用-erase-remove-惯用法"><a href="#7-4-批量删除时使用-erase-remove-惯用法" class="headerlink" title="7.4 批量删除时使用 erase-remove 惯用法"></a>7.4 批量删除时使用 erase-remove 惯用法</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; v = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>&#125;;</span><br><span class="line">v.<span class="built_in">erase</span>(std::<span class="built_in">remove_if</span>(v.<span class="built_in">begin</span>(), v.<span class="built_in">end</span>(),</span><br><span class="line">                       [](<span class="type">int</span> x) &#123; <span class="keyword">return</span> x % <span class="number">2</span> == <span class="number">0</span>; &#125;),</span><br><span class="line">        v.<span class="built_in">end</span>());</span><br></pre></td></tr></table></figure>

<p><code>remove_if</code> 不是真的删元素，它只是把“保留元素”搬到前面，真正缩短容器还要靠 <code>erase</code>。</p>
<h3 id="7-5-尽量顺序访问"><a href="#7-5-尽量顺序访问" class="headerlink" title="7.5 尽量顺序访问"></a>7.5 尽量顺序访问</h3><p>相比随机跳跃式访问，顺序遍历更容易利用：</p>
<ul>
<li>cache line</li>
<li>CPU 预取</li>
<li>向量化优化</li>
</ul>
<p>这就是为什么很多数值计算、图像处理、游戏 ECS、批量数据处理都偏好 <code>vector</code>。</p>
<h3 id="7-6-减少中间插入和删除"><a href="#7-6-减少中间插入和删除" class="headerlink" title="7.6 减少中间插入和删除"></a>7.6 减少中间插入和删除</h3><p>如果你的操作模式是：</p>
<ul>
<li>频繁在头部插入</li>
<li>频繁在中间删除</li>
<li>大量元素反复搬移</li>
</ul>
<p>那么 <code>vector</code> 往往不是好选择。<br>可以重新审视算法，或者考虑：</p>
<ul>
<li><code>deque</code></li>
<li>分块数组</li>
<li>索引表 + 延迟删除</li>
<li>SoA 数据布局</li>
</ul>
<hr>
<h2 id="8-容量增长与摊还复杂度"><a href="#8-容量增长与摊还复杂度" class="headerlink" title="8. 容量增长与摊还复杂度"></a>8. 容量增长与摊还复杂度</h2><p>为什么 <code>push_back</code> 明明偶尔会很贵，却仍然说是摊还 <code>O(1)</code>？</p>
<p>因为 <code>vector</code> 不会每次只多分配 1 个元素。实现通常会按一定倍率增长容量，例如：</p>
<ul>
<li>1.5 倍</li>
<li>2 倍</li>
</ul>
<p>具体倍率由标准库实现决定，<strong>标准没有强制规定</strong>。</p>
<p>正因为每次扩容都“多留一些余量”，所以虽然少数插入很贵，但均摊到大量 <code>push_back</code> 上，单次成本仍可视为常数级。</p>
<hr>
<h2 id="9-并发场景下的注意点"><a href="#9-并发场景下的注意点" class="headerlink" title="9. 并发场景下的注意点"></a>9. 并发场景下的注意点</h2><p>这部分在“并行编程”里很重要。</p>
<h3 id="9-1-vector-本身不是线程安全容器"><a href="#9-1-vector-本身不是线程安全容器" class="headerlink" title="9.1 vector 本身不是线程安全容器"></a>9.1 <code>vector</code> 本身不是线程安全容器</h3><p>如果多个线程同时：</p>
<ul>
<li>读同一个 <code>vector</code>，且没有线程写，一般没问题</li>
<li>只要有线程写，就必须同步</li>
</ul>
<p>尤其是以下操作风险极高：</p>
<ul>
<li><code>push_back</code></li>
<li><code>emplace_back</code></li>
<li><code>insert</code></li>
<li><code>erase</code></li>
<li><code>resize</code></li>
<li><code>reserve</code></li>
</ul>
<p>因为它们可能改变底层内存布局。</p>
<h3 id="9-2-并行读通常友好，并行写要分区"><a href="#9-2-并行读通常友好，并行写要分区" class="headerlink" title="9.2 并行读通常友好，并行写要分区"></a>9.2 并行读通常友好，并行写要分区</h3><p>比较典型的安全模式是：</p>
<ul>
<li>先 <code>resize(n)</code></li>
<li>再让不同线程写入互不重叠的下标区间</li>
</ul>
<p>例如每个线程只负责 <code>[L, R)</code> 一段：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">float</span>&gt; v;</span><br><span class="line">v.<span class="built_in">resize</span>(n); <span class="comment">// 先把元素都构造好</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 不同线程各自写不同区间 v[L..R)</span></span><br></pre></td></tr></table></figure>

<p>这种模式比多个线程同时 <code>push_back</code> 更容易获得正确性和性能。</p>
<h3 id="9-3-并行场景下的一个常见优化思路"><a href="#9-3-并行场景下的一个常见优化思路" class="headerlink" title="9.3 并行场景下的一个常见优化思路"></a>9.3 并行场景下的一个常见优化思路</h3><p>不要让所有线程争抢同一个全局 <code>vector</code>：</p>
<ul>
<li>每个线程先写自己的局部 <code>vector</code></li>
<li>局部阶段用 <code>reserve</code></li>
<li>最后统一汇总到总容器</li>
</ul>
<p>这样通常比共享一个 <code>vector</code> + 加锁 <code>push_back</code> 更高效。</p>
<hr>
<h2 id="10-容易忽略但很重要的点"><a href="#10-容易忽略但很重要的点" class="headerlink" title="10. 容易忽略但很重要的点"></a>10. 容易忽略但很重要的点</h2><h3 id="10-1-data-在空容器上可取，但不可解引用"><a href="#10-1-data-在空容器上可取，但不可解引用" class="headerlink" title="10.1 data() 在空容器上可取，但不可解引用"></a>10.1 <code>data()</code> 在空容器上可取，但不可解引用</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">std::vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line"><span class="keyword">auto</span> p = v.<span class="built_in">data</span>(); <span class="comment">// 可以拿到</span></span><br><span class="line"><span class="comment">// *p; // 错，空容器上不能解引用</span></span><br></pre></td></tr></table></figure>

<p>所以和 C 接口交互时，要同时传 <code>data()</code> 和 <code>size()</code>，不能只传裸指针。</p>
<h3 id="10-2-clear-不等于释放内存"><a href="#10-2-clear-不等于释放内存" class="headerlink" title="10.2 clear() 不等于释放内存"></a>10.2 <code>clear()</code> 不等于释放内存</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">v.<span class="built_in">clear</span>();</span><br></pre></td></tr></table></figure>

<p>这通常只会把 <code>size</code> 变成 0，<code>capacity</code> 往往还在。<br>如果后面还会继续填充数据，这反而是好事，因为可以复用之前申请过的内存。</p>
<h3 id="10-3-shrink-to-fit-只是“请求”"><a href="#10-3-shrink-to-fit-只是“请求”" class="headerlink" title="10.3 shrink_to_fit() 只是“请求”"></a>10.3 <code>shrink_to_fit()</code> 只是“请求”</h3><p>它不是强制命令，标准并不保证一定回收成功。</p>
<h3 id="10-4-vector-是特化版，行为不像普通-vector"><a href="#10-4-vector-是特化版，行为不像普通-vector" class="headerlink" title="10.4 vector&lt;bool&gt; 是特化版，行为不像普通 vector&lt;T&gt;"></a>10.4 <code>vector&lt;bool&gt;</code> 是特化版，行为不像普通 <code>vector&lt;T&gt;</code></h3><p><code>vector&lt;bool&gt;</code> 为了节省空间，会按位压缩存储，它返回的很多东西不是普通 <code>bool&amp;</code>。<br>所以在泛型代码、并发位操作、取地址等场景里常常带来意外问题。</p>
<p>如果你需要稳定、直观的布尔数组语义，常见替代方案是：</p>
<ul>
<li><code>std::vector&lt;char&gt;</code></li>
<li><code>std::vector&lt;uint8_t&gt;</code></li>
<li><code>std::bitset</code></li>
<li>专用 bitset&#x2F;bitmap 容器</li>
</ul>
<h3 id="10-5-不要长期持有元素地址并假设它永远稳定"><a href="#10-5-不要长期持有元素地址并假设它永远稳定" class="headerlink" title="10.5 不要长期持有元素地址并假设它永远稳定"></a>10.5 不要长期持有元素地址并假设它永远稳定</h3><p>这类代码很危险：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span>* p = &amp;v[<span class="number">0</span>];</span><br><span class="line"><span class="comment">// ... 中间若发生扩容/插入/删除</span></span><br><span class="line"><span class="built_in">use</span>(*p); <span class="comment">// 可能悬空</span></span><br></pre></td></tr></table></figure>

<p>更稳妥的做法通常是：</p>
<ul>
<li>只在短时间内使用指针&#x2F;引用</li>
<li>改动容器后重新获取</li>
<li>或保存索引，而不是保存地址</li>
</ul>
<hr>
<h2 id="11-什么时候优先选-vector"><a href="#11-什么时候优先选-vector" class="headerlink" title="11. 什么时候优先选 vector"></a>11. 什么时候优先选 <code>vector</code></h2><p>优先考虑 <code>vector</code> 的典型场景：</p>
<ul>
<li>元素个数大，且以遍历为主</li>
<li>需要频繁随机访问</li>
<li>主要在尾部追加</li>
<li>希望更好的缓存局部性</li>
<li>需要和 C API &#x2F; GPU &#x2F; 数值库交互</li>
</ul>
<p>不太适合的场景：</p>
<ul>
<li>频繁头插头删</li>
<li>中间位置大量插删</li>
<li>强依赖“节点地址永不变化”</li>
</ul>
<hr>
<h2 id="12-高频面试-复习题"><a href="#12-高频面试-复习题" class="headerlink" title="12. 高频面试 &#x2F; 复习题"></a>12. 高频面试 &#x2F; 复习题</h2><h3 id="Q1：reserve-和-resize-的区别？"><a href="#Q1：reserve-和-resize-的区别？" class="headerlink" title="Q1：reserve 和 resize 的区别？"></a>Q1：<code>reserve</code> 和 <code>resize</code> 的区别？</h3><ul>
<li><code>reserve</code> 调整容量，不创建元素</li>
<li><code>resize</code> 调整元素个数，必要时构造或销毁元素</li>
</ul>
<h3 id="Q2：为什么-push-back-是摊还-O-1-？"><a href="#Q2：为什么-push-back-是摊还-O-1-？" class="headerlink" title="Q2：为什么 push_back 是摊还 O(1)？"></a>Q2：为什么 <code>push_back</code> 是摊还 <code>O(1)</code>？</h3><ul>
<li>因为容量按倍率增长</li>
<li>少数扩容代价被大量普通插入均摊</li>
</ul>
<h3 id="Q3：什么时候迭代器会失效？"><a href="#Q3：什么时候迭代器会失效？" class="headerlink" title="Q3：什么时候迭代器会失效？"></a>Q3：什么时候迭代器会失效？</h3><ul>
<li>扩容后，通常全部失效</li>
<li><code>insert/erase</code> 后，受影响位置及其之后通常失效</li>
</ul>
<h3 id="Q4：为什么高性能代码偏爱-vector？"><a href="#Q4：为什么高性能代码偏爱-vector？" class="headerlink" title="Q4：为什么高性能代码偏爱 vector？"></a>Q4：为什么高性能代码偏爱 <code>vector</code>？</h3><ul>
<li>连续内存</li>
<li>缓存友好</li>
<li>顺序遍历快</li>
<li>接近底层数组，抽象成本低</li>
</ul>
<h3 id="Q5：为什么很多并行写入场景不建议多个线程同时-push-back？"><a href="#Q5：为什么很多并行写入场景不建议多个线程同时-push-back？" class="headerlink" title="Q5：为什么很多并行写入场景不建议多个线程同时 push_back？"></a>Q5：为什么很多并行写入场景不建议多个线程同时 <code>push_back</code>？</h3><ul>
<li>存在数据竞争</li>
<li>扩容会改写底层地址</li>
<li>即便加锁，争用也会明显拖慢性能</li>
</ul>
<hr>
<h2 id="13-一页总结"><a href="#13-一页总结" class="headerlink" title="13. 一页总结"></a>13. 一页总结</h2><p><code>vector</code> 最重要的不是“会用 API”，而是要掌握它的性能模型：</p>
<ul>
<li>它是连续内存动态数组</li>
<li>尾插强，中间插删弱</li>
<li><code>reserve</code> 是最常用的性能优化手段</li>
<li>扩容会导致迭代器&#x2F;引用&#x2F;指针失效</li>
<li>在并发环境里，推荐“先分配、再分区写入”</li>
</ul>
<p>如果只记一条实战原则，那就是：</p>
<blockquote>
<p>能预估元素个数时，优先 <code>reserve</code>；能顺序访问时，尽量顺序访问。</p>
</blockquote>
<hr>
<h2 id="14-可继续补充的相关主题"><a href="#14-可继续补充的相关主题" class="headerlink" title="14. 可继续补充的相关主题"></a>14. 可继续补充的相关主题</h2><p>和本节关系最紧密、值得继续整理的内容：</p>
<ol>
<li><code>AoS</code> 与 <code>SoA</code> 的访存差异</li>
<li><code>std::span</code> 与 <code>data()/size()</code> 的无拷贝视图</li>
<li><code>std::pmr::vector</code> 与内存池优化</li>
<li>平凡类型、移动语义、<code>noexcept move</code> 对扩容成本的影响</li>
<li>并行归约、分块写入、避免伪共享的 <code>vector</code> 用法</li>
</ol>
<hr>
<h2 id="15-参考资料"><a href="#15-参考资料" class="headerlink" title="15. 参考资料"></a>15. 参考资料</h2><ol>
<li><p>cppreference: <code>std::vector</code><br><a href="https://en.cppreference.com/w/cpp/container/vector">https://en.cppreference.com/w/cpp/container/vector</a></p>
</li>
<li><p>cppreference: iterator invalidation<br><a href="https://en.cppreference.com/w/cpp/container#Iterator_invalidation">https://en.cppreference.com/w/cpp/container#Iterator_invalidation</a></p>
</li>
<li><p>parallel101&#x2F;course<br><a href="https://github.com/parallel101/course">https://github.com/parallel101/course</a></p>
</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>CUDA 开启的 GPU 编程</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/11-cuda%E5%BC%80%E5%90%AF%E7%9A%84gpu%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="CUDA-开启的-GPU-编程"><a href="#CUDA-开启的-GPU-编程" class="headerlink" title="CUDA 开启的 GPU 编程"></a>CUDA 开启的 GPU 编程</h1><p>time：2025_12_26</p>
<hr>
<h2 id="1-工程与编译（CMake-nvcc）"><a href="#1-工程与编译（CMake-nvcc）" class="headerlink" title="1. 工程与编译（CMake &#x2F; nvcc）"></a>1. 工程与编译（CMake &#x2F; nvcc）</h2><h3 id="1-1-最小-CMake-工程"><a href="#1-1-最小-CMake-工程" class="headerlink" title="1.1 最小 CMake 工程"></a>1.1 最小 CMake 工程</h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">cmake_minimum_required</span>(VERSION <span class="number">3.18</span>)</span><br><span class="line"><span class="keyword">project</span>(hellocuda LANGUAGES CXX CUDA)</span><br><span class="line"></span><br><span class="line"><span class="keyword">add_executable</span>(main main.cu)</span><br><span class="line"></span><br><span class="line"><span class="keyword">set_target_properties</span>(main PROPERTIES</span><br><span class="line">  CUDA_STANDARD <span class="number">17</span></span><br><span class="line">  CUDA_STANDARD_REQUIRED <span class="keyword">ON</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 需要跨 .cu 调用 device 函数/做 device link 时开启</span></span><br><span class="line"><span class="keyword">set_target_properties</span>(main PROPERTIES CUDA_SEPARABLE_COMPILATION <span class="keyword">ON</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 常用 CUDA 编译选项（按需启用）</span></span><br><span class="line"><span class="keyword">target_compile_options</span>(main PUBLIC</span><br><span class="line">  $&lt;$&lt;COMPILE_LANGUAGE:CUDA&gt;:--expt-relaxed-constexpr&gt;</span><br><span class="line">  $&lt;$&lt;COMPILE_LANGUAGE:CUDA&gt;:--expt-extended-lambda&gt;</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<h3 id="1-2-推荐的基础错误检查宏"><a href="#1-2-推荐的基础错误检查宏" class="headerlink" title="1.2 推荐的基础错误检查宏"></a>1.2 推荐的基础错误检查宏</h3><p>避免依赖 sample 的 <code>helper_cuda.h</code>，直接自带一个最小版：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdio&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdlib&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cuda_runtime.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> CUDA_CHECK(call) do &#123;                                  \</span></span><br><span class="line"><span class="meta">  cudaError_t err = (call);                                    \</span></span><br><span class="line"><span class="meta">  <span class="keyword">if</span> (err != cudaSuccess) &#123;                                    \</span></span><br><span class="line"><span class="meta">    std::fprintf(stderr, <span class="string">&quot;CUDA error %s:%d: %s\n&quot;</span>,             \</span></span><br><span class="line"><span class="meta">      __FILE__, __LINE__, cudaGetErrorString(err));            \</span></span><br><span class="line"><span class="meta">    std::exit(1);                                              \</span></span><br><span class="line"><span class="meta">  &#125;                                                            \</span></span><br><span class="line"><span class="meta">&#125; while(0)</span></span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2-CUDA-基础：函数修饰符与执行位置"><a href="#2-CUDA-基础：函数修饰符与执行位置" class="headerlink" title="2. CUDA 基础：函数修饰符与执行位置"></a>2. CUDA 基础：函数修饰符与执行位置</h2><h3 id="2-1-global-device-host"><a href="#2-1-global-device-host" class="headerlink" title="2.1 __global__ / __device__ / __host__"></a>2.1 <code>__global__ / __device__ / __host__</code></h3><ul>
<li><p><code>__global__</code>：核函数（kernel）</p>
<ul>
<li>在 GPU 上并行执行</li>
<li>由主机端（CPU）发起 <code>&lt;&lt;&lt;...&gt;&gt;&gt;</code></li>
<li>返回类型必须为 <code>void</code>（通常通过指针写回结果）</li>
</ul>
</li>
<li><p><code>__device__</code>：设备函数</p>
<ul>
<li>在 GPU 上执行</li>
<li>只能从 device&#x2F;global 调用</li>
</ul>
</li>
<li><p><code>__host__</code>：主机函数</p>
<ul>
<li>在 CPU 上执行</li>
<li>未标注的普通函数默认就是 host</li>
</ul>
</li>
</ul>
<p>组合：</p>
<ul>
<li><code>__host__ __device__</code>：同一函数在 CPU&#x2F;GPU 两侧都可用（注意 device 侧不支持完整的 C++ 标准库能力）</li>
</ul>
<h3 id="2-2-CUDA-ARCH-（区分-device-host-编译路径）"><a href="#2-2-CUDA-ARCH-（区分-device-host-编译路径）" class="headerlink" title="2.2 __CUDA_ARCH__（区分 device&#x2F;host 编译路径）"></a>2.2 <code>__CUDA_ARCH__</code>（区分 device&#x2F;host 编译路径）</h3><ul>
<li><code>__CUDA_ARCH__</code> 只在 device 编译路径中定义，值为计算能力架构号（例如 750&#x2F;800 等）</li>
<li>常用于同一个函数在 host&#x2F;device 的条件编译</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__host__ __device__ inline int where_am_i() &#123;</span><br><span class="line">#ifdef __CUDA_ARCH__</span><br><span class="line">    return __CUDA_ARCH__;   // device：架构号</span><br><span class="line">#else</span><br><span class="line">    return -1;              // host：标记</span><br><span class="line">#endif</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-3-constexpr-与-device-代码"><a href="#2-3-constexpr-与-device-代码" class="headerlink" title="2.3 constexpr 与 device 代码"></a>2.3 <code>constexpr</code> 与 device 代码</h3><ul>
<li>如需更宽松的 <code>constexpr</code> 在 device 侧工作，常用 <code>--expt-relaxed-constexpr</code></li>
<li>device 侧 lambda 扩展常用 <code>--expt-extended-lambda</code></li>
</ul>
<hr>
<h2 id="3-Kernel-启动、线程块模型与索引"><a href="#3-Kernel-启动、线程块模型与索引" class="headerlink" title="3. Kernel 启动、线程块模型与索引"></a>3. Kernel 启动、线程块模型与索引</h2><h3 id="3-1-Kernel-启动语法"><a href="#3-1-Kernel-启动语法" class="headerlink" title="3.1 Kernel 启动语法"></a>3.1 Kernel 启动语法</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">kernel&lt;&lt;&lt;grid, block, shared_bytes, stream&gt;&gt;&gt;(args...);</span><br></pre></td></tr></table></figure>

<ul>
<li>常用：<code>&lt;&lt;&lt;grid, block&gt;&gt;&gt;</code></li>
<li><code>shared_bytes</code>：动态共享内存字节数（默认 0）</li>
<li><code>stream</code>：CUDA 流（默认 0）</li>
</ul>
<h3 id="3-2-线程-块索引"><a href="#3-2-线程-块索引" class="headerlink" title="3.2 线程&#x2F;块索引"></a>3.2 线程&#x2F;块索引</h3><ul>
<li><code>threadIdx.&#123;x,y,z&#125;</code>：线程在块内索引</li>
<li><code>blockIdx.&#123;x,y,z&#125;</code>：块在网格内索引</li>
<li><code>blockDim.&#123;x,y,z&#125;</code>：每块线程数维度</li>
<li><code>gridDim.&#123;x,y,z&#125;</code>：网格块数维度</li>
</ul>
<h3 id="3-3-典型打印示例（便于理解执行模型）"><a href="#3-3-典型打印示例（便于理解执行模型）" class="headerlink" title="3.3 典型打印示例（便于理解执行模型）"></a>3.3 典型打印示例（便于理解执行模型）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;cstdio&gt;</span><br><span class="line">#include &lt;cuda_runtime.h&gt;</span><br><span class="line"></span><br><span class="line">__global__ void kernel() &#123;</span><br><span class="line">    printf(&quot;block %d/%d, thread %d/%d\n&quot;,</span><br><span class="line">           blockIdx.x, gridDim.x,</span><br><span class="line">           threadIdx.x, blockDim.x);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    kernel&lt;&lt;&lt;2, 3&gt;&gt;&gt;();</span><br><span class="line">    cudaDeviceSynchronize();</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-4-Grid-Stride-Loop（通用遍历范式）"><a href="#3-4-Grid-Stride-Loop（通用遍历范式）" class="headerlink" title="3.4 Grid-Stride Loop（通用遍历范式）"></a>3.4 Grid-Stride Loop（通用遍历范式）</h3><p>适用于任意大小数据与任意 grid&#x2F;block 配置：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__global__ void work(int* a, int n) &#123;</span><br><span class="line">    for (int i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">         i &lt; n;</span><br><span class="line">         i += blockDim.x * gridDim.x) &#123;</span><br><span class="line">        a[i] = i;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="4-同步与错误处理"><a href="#4-同步与错误处理" class="headerlink" title="4. 同步与错误处理"></a>4. 同步与错误处理</h2><h3 id="4-1-CPU-GPU-默认异步"><a href="#4-1-CPU-GPU-默认异步" class="headerlink" title="4.1 CPU&#x2F;GPU 默认异步"></a>4.1 CPU&#x2F;GPU 默认异步</h3><ul>
<li><p>kernel launch 对 host 来说通常是异步的</p>
</li>
<li><p>常用同步：</p>
<ul>
<li><code>cudaDeviceSynchronize()</code>：等待当前设备上所有已提交工作完成</li>
<li><code>cudaStreamSynchronize(stream)</code>：等待某个流完成</li>
</ul>
</li>
</ul>
<h3 id="4-2-推荐的-launch-后检查模板"><a href="#4-2-推荐的-launch-后检查模板" class="headerlink" title="4.2 推荐的 launch 后检查模板"></a>4.2 推荐的 launch 后检查模板</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">kernel&lt;&lt;&lt;grid, block&gt;&gt;&gt;(...);</span><br><span class="line"><span class="built_in">CUDA_CHECK</span>(<span class="built_in">cudaGetLastError</span>());</span><br><span class="line"><span class="built_in">CUDA_CHECK</span>(<span class="built_in">cudaDeviceSynchronize</span>());</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="5-内存管理（Host-Device-Unified）"><a href="#5-内存管理（Host-Device-Unified）" class="headerlink" title="5. 内存管理（Host&#x2F;Device&#x2F;Unified）"></a>5. 内存管理（Host&#x2F;Device&#x2F;Unified）</h2><h3 id="5-1-经典模式：cudaMalloc-cudaMemcpy"><a href="#5-1-经典模式：cudaMalloc-cudaMemcpy" class="headerlink" title="5.1 经典模式：cudaMalloc + cudaMemcpy"></a>5.1 经典模式：<code>cudaMalloc + cudaMemcpy</code></h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;cstdio&gt;</span><br><span class="line">#include &lt;cuda_runtime.h&gt;</span><br><span class="line"></span><br><span class="line">#define CUDA_CHECK(call) do &#123; \</span><br><span class="line">  cudaError_t err = (call); \</span><br><span class="line">  if (err != cudaSuccess) &#123; \</span><br><span class="line">    std::fprintf(stderr, &quot;CUDA error: %s\n&quot;, cudaGetErrorString(err)); \</span><br><span class="line">    std::exit(1); \</span><br><span class="line">  &#125; \</span><br><span class="line">&#125; while(0)</span><br><span class="line"></span><br><span class="line">__global__ void kernel(int* out) &#123; *out = 42; &#125;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    int* d_out = nullptr;</span><br><span class="line">    CUDA_CHECK(cudaMalloc(&amp;d_out, sizeof(int)));</span><br><span class="line"></span><br><span class="line">    kernel&lt;&lt;&lt;1,1&gt;&gt;&gt;(d_out);</span><br><span class="line">    CUDA_CHECK(cudaGetLastError());</span><br><span class="line">    CUDA_CHECK(cudaDeviceSynchronize());</span><br><span class="line"></span><br><span class="line">    int h_out = 0;</span><br><span class="line">    CUDA_CHECK(cudaMemcpy(&amp;h_out, d_out, sizeof(int), cudaMemcpyDeviceToHost));</span><br><span class="line"></span><br><span class="line">    std::printf(&quot;ret=%d\n&quot;, h_out);</span><br><span class="line"></span><br><span class="line">    CUDA_CHECK(cudaFree(d_out));</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-2-统一内存（Unified-Memory）：cudaMallocManaged"><a href="#5-2-统一内存（Unified-Memory）：cudaMallocManaged" class="headerlink" title="5.2 统一内存（Unified Memory）：cudaMallocManaged"></a>5.2 统一内存（Unified Memory）：<code>cudaMallocManaged</code></h3><ul>
<li>一份指针同时可被 CPU&#x2F;GPU 访问</li>
<li>常配合同步；性能敏感时可用预取提升稳定性</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;cstdio&gt;</span><br><span class="line">#include &lt;cuda_runtime.h&gt;</span><br><span class="line"></span><br><span class="line">__global__ void fill(int* a, int n) &#123;</span><br><span class="line">    for (int i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">         i &lt; n; i += blockDim.x * gridDim.x) &#123;</span><br><span class="line">        a[i] = i;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    int n = 32;</span><br><span class="line">    int* a = nullptr;</span><br><span class="line">    cudaMallocManaged(&amp;a, sizeof(int) * n);</span><br><span class="line"></span><br><span class="line">    fill&lt;&lt;&lt;1, 128&gt;&gt;&gt;(a, n);</span><br><span class="line">    cudaDeviceSynchronize();</span><br><span class="line"></span><br><span class="line">    for (int i = 0; i &lt; n; ++i) std::printf(&quot;a[%d]=%d\n&quot;, i, a[i]);</span><br><span class="line">    cudaFree(a);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-3-预取（Prefetch）与驻留优化（进阶但常用）"><a href="#5-3-预取（Prefetch）与驻留优化（进阶但常用）" class="headerlink" title="5.3 预取（Prefetch）与驻留优化（进阶但常用）"></a>5.3 预取（Prefetch）与驻留优化（进阶但常用）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> dev = <span class="number">0</span>;</span><br><span class="line"><span class="built_in">cudaGetDevice</span>(&amp;dev);</span><br><span class="line"><span class="built_in">cudaMemPrefetchAsync</span>(a, <span class="built_in">sizeof</span>(<span class="type">int</span>)*n, dev, <span class="number">0</span>);     <span class="comment">// 预取到 GPU</span></span><br><span class="line"><span class="comment">// kernel ...</span></span><br><span class="line"><span class="built_in">cudaMemPrefetchAsync</span>(a, <span class="built_in">sizeof</span>(<span class="type">int</span>)*n, cudaCpuDeviceId, <span class="number">0</span>); <span class="comment">// 预取回 CPU</span></span><br><span class="line"><span class="built_in">cudaDeviceSynchronize</span>();</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="6-C-封装：RAII-与可复用接口"><a href="#6-C-封装：RAII-与可复用接口" class="headerlink" title="6. C++ 封装：RAII 与可复用接口"></a>6. C++ 封装：RAII 与可复用接口</h2><h3 id="6-1-RAII-管理-Unified-Memory-指针（简单、可靠、适合入门）"><a href="#6-1-RAII-管理-Unified-Memory-指针（简单、可靠、适合入门）" class="headerlink" title="6.1 RAII 管理 Unified Memory 指针（简单、可靠、适合入门）"></a>6.1 RAII 管理 Unified Memory 指针（简单、可靠、适合入门）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cuda_runtime.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">ManagedArray</span> &#123;</span><br><span class="line">    T* p&#123;<span class="literal">nullptr</span>&#125;;</span><br><span class="line">    <span class="type">size_t</span> n&#123;<span class="number">0</span>&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">ManagedArray</span><span class="params">(<span class="type">size_t</span> n_)</span> : n(n_) &#123;</span></span><br><span class="line">        <span class="built_in">cudaMallocManaged</span>(&amp;p, <span class="built_in">sizeof</span>(T) * n);</span><br><span class="line">    &#125;</span><br><span class="line">    ~<span class="built_in">ManagedArray</span>() &#123;</span><br><span class="line">        <span class="keyword">if</span> (p) <span class="built_in">cudaFree</span>(p);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">T* <span class="title">data</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> p; &#125;</span><br><span class="line">    <span class="function"><span class="type">const</span> T* <span class="title">data</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> p; &#125;</span><br><span class="line">    T&amp; <span class="keyword">operator</span>[](<span class="type">size_t</span> i) &#123; <span class="keyword">return</span> p[i]; &#125;</span><br><span class="line">    <span class="type">const</span> T&amp; <span class="keyword">operator</span>[](<span class="type">size_t</span> i) <span class="type">const</span> &#123; <span class="keyword">return</span> p[i]; &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="6-2-结合-kernel-使用"><a href="#6-2-结合-kernel-使用" class="headerlink" title="6.2 结合 kernel 使用"></a>6.2 结合 kernel 使用</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__global__ void init(int* a, int n) &#123;</span><br><span class="line">    for (int i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">         i &lt; n; i += blockDim.x * gridDim.x) &#123;</span><br><span class="line">        a[i] = i;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    int n = 1000;</span><br><span class="line">    ManagedArray&lt;int&gt; a(n);</span><br><span class="line">    init&lt;&lt;&lt;32, 128&gt;&gt;&gt;(a.data(), n);</span><br><span class="line">    cudaDeviceSynchronize();</span><br><span class="line">    // CPU 侧直接读</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>allocator 方式也可把 unified memory 接入 <code>std::vector</code>，但 allocator 细节较多；建议先把 RAII 指针与 <code>.data()</code> 传参掌握牢。</p>
</blockquote>
<hr>
<h2 id="7-Thrust-库：容器与算法（高层-CUDA）"><a href="#7-Thrust-库：容器与算法（高层-CUDA）" class="headerlink" title="7. Thrust 库：容器与算法（高层 CUDA）"></a>7. Thrust 库：容器与算法（高层 CUDA）</h2><h3 id="7-1-常用容器"><a href="#7-1-常用容器" class="headerlink" title="7.1 常用容器"></a>7.1 常用容器</h3><ul>
<li><code>thrust::host_vector&lt;T&gt;</code>：主机端 vector</li>
<li><code>thrust::device_vector&lt;T&gt;</code>：设备端 vector</li>
<li>通过赋值可触发 H2D &#x2F; D2H 拷贝（更准确地说：构造&#x2F;赋值会在 host&#x2F;device 容器之间进行数据迁移）</li>
</ul>
<h3 id="7-2-AXPY-示例（device-vector-自写-kernel）"><a href="#7-2-AXPY-示例（device-vector-自写-kernel）" class="headerlink" title="7.2 AXPY 示例（device_vector + 自写 kernel）"></a>7.2 AXPY 示例（device_vector + 自写 kernel）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;thrust/host_vector.h&gt;</span><br><span class="line">#include &lt;thrust/device_vector.h&gt;</span><br><span class="line">#include &lt;cuda_runtime.h&gt;</span><br><span class="line">#include &lt;cstdio&gt;</span><br><span class="line"></span><br><span class="line">__global__ void axpy(float* x, const float* y, float a, int n) &#123;</span><br><span class="line">    for (int i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">         i &lt; n; i += blockDim.x * gridDim.x) &#123;</span><br><span class="line">        x[i] = a * x[i] + y[i];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    int n = 1 &lt;&lt; 20;</span><br><span class="line">    float a = 3.14f;</span><br><span class="line"></span><br><span class="line">    thrust::host_vector&lt;float&gt; hx(n), hy(n);</span><br><span class="line">    for (int i = 0; i &lt; n; ++i) &#123; hx[i] = i * 0.001f; hy[i] = 1.0f; &#125;</span><br><span class="line"></span><br><span class="line">    thrust::device_vector&lt;float&gt; dx = hx;</span><br><span class="line">    thrust::device_vector&lt;float&gt; dy = hy;</span><br><span class="line"></span><br><span class="line">    axpy&lt;&lt;&lt;256, 256&gt;&gt;&gt;(thrust::raw_pointer_cast(dx.data()),</span><br><span class="line">                       thrust::raw_pointer_cast(dy.data()),</span><br><span class="line">                       a, n);</span><br><span class="line">    cudaDeviceSynchronize();</span><br><span class="line"></span><br><span class="line">    hx = dx;</span><br><span class="line">    std::printf(&quot;hx[0]=%f, hx[n-1]=%f\n&quot;, hx[0], hx[n-1]);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="8-原子操作（Atomic）"><a href="#8-原子操作（Atomic）" class="headerlink" title="8. 原子操作（Atomic）"></a>8. 原子操作（Atomic）</h2><h3 id="8-1-常用原子"><a href="#8-1-常用原子" class="headerlink" title="8.1 常用原子"></a>8.1 常用原子</h3><ul>
<li><code>atomicAdd / atomicSub</code></li>
<li><code>atomicAnd / atomicOr / atomicXor</code></li>
<li><code>atomicMin / atomicMax</code></li>
<li><code>atomicCAS</code>：Compare-And-Swap，可用于构造自定义原子操作</li>
</ul>
<h3 id="8-2-用-CAS-实现自定义原子加"><a href="#8-2-用-CAS-实现自定义原子加" class="headerlink" title="8.2 用 CAS 实现自定义原子加"></a>8.2 用 CAS 实现自定义原子加</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__device__ __forceinline__ int my_atomic_add(int* dst, int val) &#123;</span><br><span class="line">    int old = *dst;</span><br><span class="line">    int assumed;</span><br><span class="line">    do &#123;</span><br><span class="line">        assumed = old;</span><br><span class="line">        old = atomicCAS(dst, assumed, assumed + val);</span><br><span class="line">    &#125; while (assumed != old);</span><br><span class="line">    return old;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="8-3-朴素并行求和（全局原子累加）"><a href="#8-3-朴素并行求和（全局原子累加）" class="headerlink" title="8.3 朴素并行求和（全局原子累加）"></a>8.3 朴素并行求和（全局原子累加）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__global__ void parallel_sum(int* sum, const int* arr, int n) &#123;</span><br><span class="line">    for (int i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">         i &lt; n; i += blockDim.x * gridDim.x) &#123;</span><br><span class="line">        atomicAdd(sum, arr[i]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="9-线程块与共享内存（Shared-Memory）"><a href="#9-线程块与共享内存（Shared-Memory）" class="headerlink" title="9. 线程块与共享内存（Shared Memory）"></a>9. 线程块与共享内存（Shared Memory）</h2><h3 id="9-1-核心概念"><a href="#9-1-核心概念" class="headerlink" title="9.1 核心概念"></a>9.1 核心概念</h3><ul>
<li><code>__shared__</code>：块内共享内存（一个 block 内所有线程可见）</li>
<li><code>__syncthreads()</code>：块内同步屏障（必须保证同一 block 的线程都能到达）</li>
</ul>
<p>共享内存常用于：</p>
<ul>
<li>块内复用数据（减少 global memory 访问）</li>
<li>块内归约（reduce）</li>
<li>tile-based 计算（矩阵乘、卷积、图像算子）</li>
</ul>
<h3 id="9-2-块内归约：每块只做一次全局原子"><a href="#9-2-块内归约：每块只做一次全局原子" class="headerlink" title="9.2 块内归约：每块只做一次全局原子"></a>9.2 块内归约：每块只做一次全局原子</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;cuda_runtime.h&gt;</span><br><span class="line"></span><br><span class="line">__global__ void reduce_sum(const int* arr, int n, int* out) &#123;</span><br><span class="line">    extern __shared__ int sdata[]; // 动态共享内存</span><br><span class="line">    int tid = threadIdx.x;</span><br><span class="line">    int i = blockIdx.x * blockDim.x + tid;</span><br><span class="line"></span><br><span class="line">    sdata[tid] = (i &lt; n) ? arr[i] : 0;</span><br><span class="line">    __syncthreads();</span><br><span class="line"></span><br><span class="line">    for (int s = blockDim.x / 2; s &gt; 0; s &gt;&gt;= 1) &#123;</span><br><span class="line">        if (tid &lt; s) sdata[tid] += sdata[tid + s];</span><br><span class="line">        __syncthreads();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    if (tid == 0) atomicAdd(out, sdata[0]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>启动方式（动态共享内存大小）：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> threads = <span class="number">256</span>;</span><br><span class="line"><span class="type">int</span> blocks = (n + threads - <span class="number">1</span>) / threads;</span><br><span class="line">reduce_sum&lt;&lt;&lt;blocks, threads, <span class="function">threads * <span class="title">sizeof</span><span class="params">(<span class="type">int</span>)</span>&gt;&gt;&gt;<span class="params">(arr, n, out)</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="9-3-Tile-示例：2D-图像-3x3-均值滤波（共享内存加速范式）"><a href="#9-3-Tile-示例：2D-图像-3x3-均值滤波（共享内存加速范式）" class="headerlink" title="9.3 Tile 示例：2D 图像 3x3 均值滤波（共享内存加速范式）"></a>9.3 Tile 示例：2D 图像 3x3 均值滤波（共享内存加速范式）</h3><p>适用于图像&#x2F;矩阵类任务（tile + halo）：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;cuda_runtime.h&gt;</span><br><span class="line"></span><br><span class="line">__global__ void mean3x3(const float* in, float* out, int H, int W) &#123;</span><br><span class="line">    // blockDim = (Bx, By)</span><br><span class="line">    const int x = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">    const int y = blockIdx.y * blockDim.y + threadIdx.y;</span><br><span class="line"></span><br><span class="line">    // tile 尺寸：块大小 + halo(上下左右各1)</span><br><span class="line">    const int Bx = blockDim.x;</span><br><span class="line">    const int By = blockDim.y;</span><br><span class="line">    extern __shared__ float tile[];</span><br><span class="line"></span><br><span class="line">    // tile 索引函数</span><br><span class="line">    auto t = [&amp;](int ty, int tx) -&gt; float&amp; &#123;</span><br><span class="line">        return tile[ty * (Bx + 2) + tx];</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    // 对应 tile 坐标（+1 是为了留 halo）</span><br><span class="line">    const int tx = threadIdx.x + 1;</span><br><span class="line">    const int ty = threadIdx.y + 1;</span><br><span class="line"></span><br><span class="line">    // 读主区域</span><br><span class="line">    float v = 0.f;</span><br><span class="line">    if (x &lt; W &amp;&amp; y &lt; H) v = in[y * W + x];</span><br><span class="line">    t(ty, tx) = v;</span><br><span class="line"></span><br><span class="line">    // 读 halo（边界处做 clamp 或置零，这里用置零策略）</span><br><span class="line">    if (threadIdx.x == 0) &#123;</span><br><span class="line">        float lv = (x &gt; 0 &amp;&amp; y &lt; H) ? in[y * W + (x - 1)] : 0.f;</span><br><span class="line">        t(ty, 0) = lv;</span><br><span class="line">    &#125;</span><br><span class="line">    if (threadIdx.x == Bx - 1) &#123;</span><br><span class="line">        float rv = (x + 1 &lt; W &amp;&amp; y &lt; H) ? in[y * W + (x + 1)] : 0.f;</span><br><span class="line">        t(ty, Bx + 1) = rv;</span><br><span class="line">    &#125;</span><br><span class="line">    if (threadIdx.y == 0) &#123;</span><br><span class="line">        float uv = (y &gt; 0 &amp;&amp; x &lt; W) ? in[(y - 1) * W + x] : 0.f;</span><br><span class="line">        t(0, tx) = uv;</span><br><span class="line">    &#125;</span><br><span class="line">    if (threadIdx.y == By - 1) &#123;</span><br><span class="line">        float dv = (y + 1 &lt; H &amp;&amp; x &lt; W) ? in[(y + 1) * W + x] : 0.f;</span><br><span class="line">        t(By + 1, tx) = dv;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // 角落 halo（四个角）</span><br><span class="line">    if (threadIdx.x == 0 &amp;&amp; threadIdx.y == 0) &#123;</span><br><span class="line">        t(0,0) = (x&gt;0 &amp;&amp; y&gt;0) ? in[(y-1)*W + (x-1)] : 0.f;</span><br><span class="line">    &#125;</span><br><span class="line">    if (threadIdx.x == Bx-1 &amp;&amp; threadIdx.y == 0) &#123;</span><br><span class="line">        t(0,Bx+1) = (x+1&lt;W &amp;&amp; y&gt;0) ? in[(y-1)*W + (x+1)] : 0.f;</span><br><span class="line">    &#125;</span><br><span class="line">    if (threadIdx.x == 0 &amp;&amp; threadIdx.y == By-1) &#123;</span><br><span class="line">        t(By+1,0) = (x&gt;0 &amp;&amp; y+1&lt;H) ? in[(y+1)*W + (x-1)] : 0.f;</span><br><span class="line">    &#125;</span><br><span class="line">    if (threadIdx.x == Bx-1 &amp;&amp; threadIdx.y == By-1) &#123;</span><br><span class="line">        t(By+1,Bx+1) = (x+1&lt;W &amp;&amp; y+1&lt;H) ? in[(y+1)*W + (x+1)] : 0.f;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    __syncthreads();</span><br><span class="line"></span><br><span class="line">    if (x &lt; W &amp;&amp; y &lt; H) &#123;</span><br><span class="line">        float sum = 0.f;</span><br><span class="line">        sum += t(ty-1, tx-1); sum += t(ty-1, tx); sum += t(ty-1, tx+1);</span><br><span class="line">        sum += t(ty,   tx-1); sum += t(ty,   tx); sum += t(ty,   tx+1);</span><br><span class="line">        sum += t(ty+1, tx-1); sum += t(ty+1, tx); sum += t(ty+1, tx+1);</span><br><span class="line">        out[y * W + x] = sum / 9.f;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>共享内存大小（字节）：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">dim3 <span class="title">block</span><span class="params">(<span class="number">16</span>, <span class="number">16</span>)</span></span>;</span><br><span class="line"><span class="function">dim3 <span class="title">grid</span><span class="params">((W + block.x - <span class="number">1</span>) / block.x, (H + block.y - <span class="number">1</span>) / block.y)</span></span>;</span><br><span class="line"><span class="type">size_t</span> shared_bytes = (block.x + <span class="number">2</span>) * (block.y + <span class="number">2</span>) * <span class="built_in">sizeof</span>(<span class="type">float</span>);</span><br><span class="line">mean3x3&lt;&lt;&lt;grid, block, shared_bytes&gt;&gt;&gt;(in, out, H, W);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="10-CUDA-Streams-与异步拷贝"><a href="#10-CUDA-Streams-与异步拷贝" class="headerlink" title="10. CUDA Streams 与异步拷贝"></a>10. CUDA Streams 与异步拷贝</h2><h3 id="10-1-为什么需要-streams"><a href="#10-1-为什么需要-streams" class="headerlink" title="10.1 为什么需要 streams"></a>10.1 为什么需要 streams</h3><ul>
<li><p>默认 stream（stream 0）会形成较强的串行依赖</p>
</li>
<li><p>多 stream 可以实现：</p>
<ul>
<li>H2D 拷贝与 kernel 重叠</li>
<li>多批次流水线（pipeline）</li>
<li>与 <code>cudaMemcpyAsync</code> 配合提升吞吐</li>
</ul>
</li>
</ul>
<h3 id="10-2-pinned（页锁定）主机内存：提升异步拷贝效率"><a href="#10-2-pinned（页锁定）主机内存：提升异步拷贝效率" class="headerlink" title="10.2 pinned（页锁定）主机内存：提升异步拷贝效率"></a>10.2 pinned（页锁定）主机内存：提升异步拷贝效率</h3><ul>
<li><code>cudaMallocHost</code> &#x2F; <code>cudaFreeHost</code></li>
<li>pinned 内存更利于 DMA，<code>cudaMemcpyAsync</code> 才更有意义</li>
</ul>
<h3 id="10-3-基本模板：两条-stream-流水搬运"><a href="#10-3-基本模板：两条-stream-流水搬运" class="headerlink" title="10.3 基本模板：两条 stream 流水搬运"></a>10.3 基本模板：两条 stream 流水搬运</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;cstdio&gt;</span><br><span class="line">#include &lt;cuda_runtime.h&gt;</span><br><span class="line"></span><br><span class="line">#define CUDA_CHECK(call) do &#123;                                  \</span><br><span class="line">  cudaError_t err = (call);                                    \</span><br><span class="line">  if (err != cudaSuccess) &#123;                                    \</span><br><span class="line">    std::fprintf(stderr, &quot;CUDA error %s:%d: %s\n&quot;,             \</span><br><span class="line">      __FILE__, __LINE__, cudaGetErrorString(err));            \</span><br><span class="line">    std::exit(1);                                              \</span><br><span class="line">  &#125;                                                            \</span><br><span class="line">&#125; while(0)</span><br><span class="line"></span><br><span class="line">__global__ void scale(float* x, int n, float a) &#123;</span><br><span class="line">    for (int i = blockIdx.x * blockDim.x + threadIdx.x;</span><br><span class="line">         i &lt; n; i += blockDim.x * gridDim.x) &#123;</span><br><span class="line">        x[i] *= a;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    const int n = 1 &lt;&lt; 20;</span><br><span class="line">    const size_t bytes = n * sizeof(float);</span><br><span class="line"></span><br><span class="line">    // pinned host memory</span><br><span class="line">    float* h = nullptr;</span><br><span class="line">    CUDA_CHECK(cudaMallocHost(&amp;h, bytes));</span><br><span class="line"></span><br><span class="line">    // device memory</span><br><span class="line">    float* d = nullptr;</span><br><span class="line">    CUDA_CHECK(cudaMalloc(&amp;d, bytes));</span><br><span class="line"></span><br><span class="line">    // init host</span><br><span class="line">    for (int i = 0; i &lt; n; ++i) h[i] = 1.0f;</span><br><span class="line"></span><br><span class="line">    cudaStream_t s;</span><br><span class="line">    CUDA_CHECK(cudaStreamCreate(&amp;s));</span><br><span class="line"></span><br><span class="line">    // async H2D</span><br><span class="line">    CUDA_CHECK(cudaMemcpyAsync(d, h, bytes, cudaMemcpyHostToDevice, s));</span><br><span class="line"></span><br><span class="line">    // kernel in same stream (will wait for H2D in this stream)</span><br><span class="line">    scale&lt;&lt;&lt;256, 256, 0, s&gt;&gt;&gt;(d, n, 2.0f);</span><br><span class="line">    CUDA_CHECK(cudaGetLastError());</span><br><span class="line"></span><br><span class="line">    // async D2H</span><br><span class="line">    CUDA_CHECK(cudaMemcpyAsync(h, d, bytes, cudaMemcpyDeviceToHost, s));</span><br><span class="line"></span><br><span class="line">    // wait stream done</span><br><span class="line">    CUDA_CHECK(cudaStreamSynchronize(s));</span><br><span class="line"></span><br><span class="line">    std::printf(&quot;h[0]=%f, h[n-1]=%f\n&quot;, h[0], h[n-1]);</span><br><span class="line"></span><br><span class="line">    CUDA_CHECK(cudaStreamDestroy(s));</span><br><span class="line">    CUDA_CHECK(cudaFree(d));</span><br><span class="line">    CUDA_CHECK(cudaFreeHost(h));</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="10-4-事件计时（event-timing）"><a href="#10-4-事件计时（event-timing）" class="headerlink" title="10.4 事件计时（event timing）"></a>10.4 事件计时（event timing）</h3><p>用于测量 GPU 端耗时：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">cudaEvent_t st, ed;</span><br><span class="line"><span class="built_in">cudaEventCreate</span>(&amp;st); <span class="built_in">cudaEventCreate</span>(&amp;ed);</span><br><span class="line"></span><br><span class="line"><span class="built_in">cudaEventRecord</span>(st, stream);</span><br><span class="line"><span class="comment">// kernel / memcpyAsync ...</span></span><br><span class="line"><span class="built_in">cudaEventRecord</span>(ed, stream);</span><br><span class="line"><span class="built_in">cudaEventSynchronize</span>(ed);</span><br><span class="line"></span><br><span class="line"><span class="type">float</span> ms = <span class="number">0.f</span>;</span><br><span class="line"><span class="built_in">cudaEventElapsedTime</span>(&amp;ms, st, ed);</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>TBB 开启的并行编程之旅</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/10-tbb%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B%E4%B9%8B%E6%97%85/</url>
    <content><![CDATA[<h1 id="TBB-开启的并行编程之旅"><a href="#TBB-开启的并行编程之旅" class="headerlink" title="TBB 开启的并行编程之旅"></a>TBB 开启的并行编程之旅</h1><p>time: 2025-12-23</p>
<h2 id="1-并发与并行：概念与-TBB-的定位"><a href="#1-并发与并行：概念与-TBB-的定位" class="headerlink" title="1. 并发与并行：概念与 TBB 的定位"></a>1. 并发与并行：概念与 TBB 的定位</h2><ul>
<li><strong>并发（Concurrency）</strong>：关注“任务结构”。多个任务在同一时间段内推进即可（单核也能并发）。</li>
<li><strong>并行（Parallelism）</strong>：关注“同时执行”。多个任务在同一时刻运行（通常依赖多核）。</li>
</ul>
<h2 id="2-并发任务：task-group-与-parallel-invoke"><a href="#2-并发任务：task-group-与-parallel-invoke" class="headerlink" title="2. 并发任务：task_group 与 parallel_invoke"></a>2. 并发任务：<code>task_group</code> 与 <code>parallel_invoke</code></h2><h3 id="2-1-task-group：提交多个独立任务"><a href="#2-1-task-group：提交多个独立任务" class="headerlink" title="2.1 task_group：提交多个独立任务"></a>2.1 <code>task_group</code>：提交多个独立任务</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/task_group.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;chrono&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">download</span><span class="params">(<span class="type">const</span> std::string&amp; file)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) &#123;</span><br><span class="line">        std::this_thread::<span class="built_in">sleep_for</span>(std::chrono::<span class="built_in">milliseconds</span>(<span class="number">400</span>));</span><br><span class="line">    &#125;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Downloaded: &quot;</span> &lt;&lt; file &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">interact</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::string name;</span><br><span class="line">    std::cin &gt;&gt; name;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Hello, &quot;</span> &lt;&lt; name &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    tbb::task_group tg;</span><br><span class="line">    tg.<span class="built_in">run</span>([&amp;]&#123; <span class="built_in">download</span>(<span class="string">&quot;hello.zip&quot;</span>); &#125;);</span><br><span class="line">    tg.<span class="built_in">run</span>([&amp;]&#123; <span class="built_in">interact</span>(); &#125;);</span><br><span class="line">    tg.<span class="built_in">wait</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-2-parallel-invoke：更简洁的并发调用"><a href="#2-2-parallel-invoke：更简洁的并发调用" class="headerlink" title="2.2 parallel_invoke：更简洁的并发调用"></a>2.2 <code>parallel_invoke</code>：更简洁的并发调用</h3><p>当只是“并行执行几个函数”，优先用 <code>parallel_invoke</code>：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_invoke.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">tbb::<span class="built_in">parallel_invoke</span>(</span><br><span class="line">    [&amp;]&#123; <span class="built_in">download</span>(<span class="string">&quot;hello.zip&quot;</span>); &#125;,</span><br><span class="line">    [&amp;]&#123; <span class="built_in">interact</span>(); &#125;</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-数据并行：parallel-for-parallel-for-each"><a href="#3-数据并行：parallel-for-parallel-for-each" class="headerlink" title="3. 数据并行：parallel_for &#x2F; parallel_for_each"></a>3. 数据并行：<code>parallel_for</code> &#x2F; <code>parallel_for_each</code></h2><h3 id="3-1-手动分块（task-group）→-推荐替换为-parallel-for"><a href="#3-1-手动分块（task-group）→-推荐替换为-parallel-for" class="headerlink" title="3.1 手动分块（task_group）→ 推荐替换为 parallel_for"></a>3.1 手动分块（<code>task_group</code>）→ 推荐替换为 <code>parallel_for</code></h3><p>手动分块能跑，但属于“自己实现调度”。TBB 风格是把“范围”交给调度器切分。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_for.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/blocked_range.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">1u</span> &lt;&lt; <span class="number">16</span>;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">float</span>&gt; <span class="title">dp</span><span class="params">(n)</span></span>;</span><br><span class="line"></span><br><span class="line">    tbb::<span class="built_in">parallel_for</span>(</span><br><span class="line">        tbb::<span class="built_in">blocked_range</span>&lt;<span class="type">size_t</span>&gt;(<span class="number">0</span>, n),</span><br><span class="line">        [&amp;](<span class="type">const</span> tbb::blocked_range&lt;<span class="type">size_t</span>&gt;&amp; r) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">size_t</span> i = r.<span class="built_in">begin</span>(); i != r.<span class="built_in">end</span>(); ++i) &#123;</span><br><span class="line">                dp[i] = std::<span class="built_in">sinf</span>(<span class="built_in">static_cast</span>&lt;<span class="type">float</span>&gt;(i));</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-2-parallel-for-begin-end-body-：最短常用写法"><a href="#3-2-parallel-for-begin-end-body-：最短常用写法" class="headerlink" title="3.2 parallel_for(begin,end,body)：最短常用写法"></a>3.2 <code>parallel_for(begin,end,body)</code>：最短常用写法</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_for.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cmath&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">1u</span> &lt;&lt; <span class="number">16</span>;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">float</span>&gt; <span class="title">dp</span><span class="params">(n)</span></span>;</span><br><span class="line"></span><br><span class="line">    tbb::<span class="built_in">parallel_for</span>((<span class="type">size_t</span>)<span class="number">0</span>, n, [&amp;](<span class="type">size_t</span> i)&#123;</span><br><span class="line">        dp[i] = std::<span class="built_in">sinf</span>((<span class="type">float</span>)i);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-3-parallel-for-each：并行遍历容器元素"><a href="#3-3-parallel-for-each：并行遍历容器元素" class="headerlink" title="3.3 parallel_for_each：并行遍历容器元素"></a>3.3 <code>parallel_for_each</code>：并行遍历容器元素</h3><p>语义：对 <code>[first,last)</code> 每个元素并行调用一次 <code>func(element)</code>；要求元素之间无数据冲突。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_for_each.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cmath&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">float</span>&gt; <span class="title">a</span><span class="params">(<span class="number">1</span>&lt;&lt;<span class="number">16</span>, <span class="number">1.0f</span>)</span></span>;</span><br><span class="line">    tbb::<span class="built_in">parallel_for_each</span>(a.<span class="built_in">begin</span>(), a.<span class="built_in">end</span>(), [&amp;](<span class="type">float</span>&amp; f)&#123;</span><br><span class="line">        f = std::<span class="built_in">sinf</span>(f);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="4-多维并行：blocked-range2d-3d"><a href="#4-多维并行：blocked-range2d-3d" class="headerlink" title="4. 多维并行：blocked_range2d / 3d"></a>4. 多维并行：<code>blocked_range2d / 3d</code></h2><h3 id="4-1-二维：blocked-range2d（rows-cols）"><a href="#4-1-二维：blocked-range2d（rows-cols）" class="headerlink" title="4.1 二维：blocked_range2d（rows&#x2F;cols）"></a>4.1 二维：<code>blocked_range2d</code>（rows&#x2F;cols）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_for.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/blocked_range2d.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">1024</span>;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">float</span>&gt; <span class="title">a</span><span class="params">(n * n)</span></span>;</span><br><span class="line"></span><br><span class="line">    tbb::<span class="built_in">parallel_for</span>(</span><br><span class="line">        tbb::<span class="built_in">blocked_range2d</span>&lt;<span class="type">size_t</span>&gt;(<span class="number">0</span>, n, <span class="number">0</span>, n),</span><br><span class="line">        [&amp;](<span class="type">const</span> tbb::blocked_range2d&lt;<span class="type">size_t</span>&gt;&amp; r) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">size_t</span> i = r.<span class="built_in">rows</span>().<span class="built_in">begin</span>(); i != r.<span class="built_in">rows</span>().<span class="built_in">end</span>(); ++i) &#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">size_t</span> j = r.<span class="built_in">cols</span>().<span class="built_in">begin</span>(); j != r.<span class="built_in">cols</span>().<span class="built_in">end</span>(); ++j) &#123;</span><br><span class="line">                    a[i*n + j] = std::<span class="built_in">sinf</span>((<span class="type">float</span>)(i*n + j));</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    );</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-2-维度记忆"><a href="#4-2-维度记忆" class="headerlink" title="4.2 维度记忆"></a>4.2 维度记忆</h3><ul>
<li>1D：<code>tbb::blocked_range&lt;T&gt;</code></li>
<li>2D：<code>tbb::blocked_range2d&lt;T&gt;</code>：<code>rows()</code>、<code>cols()</code></li>
<li>3D：<code>tbb::blocked_range3d&lt;T&gt;</code>：<code>pages()</code>、<code>rows()</code>、<code>cols()</code></li>
</ul>
<hr>
<h2 id="5-缩并（Reduce）：从“共享变量”到-parallel-reduce"><a href="#5-缩并（Reduce）：从“共享变量”到-parallel-reduce" class="headerlink" title="5. 缩并（Reduce）：从“共享变量”到 parallel_reduce"></a>5. 缩并（Reduce）：从“共享变量”到 <code>parallel_reduce</code></h2><p>当任务存在“跨迭代依赖”（比如求和、统计），不要用共享变量直接累加，会数据竞争或锁开销巨大。<br>标准范式：<strong>局部累加 + 合并</strong>。</p>
<h3 id="5-1-parallel-reduce（lambda-版：最常用）"><a href="#5-1-parallel-reduce（lambda-版：最常用）" class="headerlink" title="5.1 parallel_reduce（lambda 版：最常用）"></a>5.1 <code>parallel_reduce</code>（lambda 版：最常用）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_reduce.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/blocked_range.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">1u</span> &lt;&lt; <span class="number">26</span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> sum = tbb::<span class="built_in">parallel_reduce</span>(</span><br><span class="line">        tbb::<span class="built_in">blocked_range</span>&lt;<span class="type">size_t</span>&gt;(<span class="number">0</span>, n),</span><br><span class="line">        <span class="number">0.0f</span>, <span class="comment">// identity</span></span><br><span class="line">        [&amp;](<span class="type">const</span> tbb::blocked_range&lt;<span class="type">size_t</span>&gt;&amp; r, <span class="type">float</span> local) -&gt; <span class="type">float</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">size_t</span> i = r.<span class="built_in">begin</span>(); i != r.<span class="built_in">end</span>(); ++i) &#123;</span><br><span class="line">                local += std::<span class="built_in">sinf</span>((<span class="type">float</span>)i);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> local;</span><br><span class="line">        &#125;,</span><br><span class="line">        [](<span class="type">float</span> a, <span class="type">float</span> b) -&gt; <span class="type">float</span> &#123; <span class="keyword">return</span> a + b; &#125; <span class="comment">// combine</span></span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    (<span class="type">void</span>)sum;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-2-parallel-deterministic-reduce"><a href="#5-2-parallel-deterministic-reduce" class="headerlink" title="5.2 parallel_deterministic_reduce"></a>5.2 <code>parallel_deterministic_reduce</code></h3><p>浮点加法不满足结合律：合并顺序不同，末位可能不同。<br>若需要“每次运行更一致的合并顺序”，可考虑 deterministic 版本（可能略慢）。</p>
<hr>
<h2 id="6-缩并（工程写法）：Reducer-结构体"><a href="#6-缩并（工程写法）：Reducer-结构体" class="headerlink" title="6. 缩并（工程写法）：Reducer 结构体"></a>6. 缩并（工程写法）：Reducer 结构体</h2><p>当归约逻辑变复杂（多个字段、多统计量、希望复用），用结构体 reducer 更稳、更清晰。</p>
<h3 id="6-1-结构体-reducer：并行-sum-sin-i"><a href="#6-1-结构体-reducer：并行-sum-sin-i" class="headerlink" title="6.1 结构体 reducer：并行 sum sin(i)"></a>6.1 结构体 reducer：并行 <code>sum sin(i)</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_reduce.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/blocked_range.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">SinSumReducer</span> &#123;</span><br><span class="line">    <span class="type">float</span> sum;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">SinSumReducer</span>() : <span class="built_in">sum</span>(<span class="number">0.0f</span>) &#123;&#125;                       <span class="comment">// identity</span></span><br><span class="line">    <span class="built_in">SinSumReducer</span>(SinSumReducer&amp;, tbb::split) : <span class="built_in">sum</span>(<span class="number">0.0f</span>) &#123;&#125; <span class="comment">// split ctor</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">const</span> tbb::blocked_range&lt;<span class="type">size_t</span>&gt;&amp; r)</span> </span>&#123;</span><br><span class="line">        <span class="type">float</span> local = sum;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">size_t</span> i = r.<span class="built_in">begin</span>(); i != r.<span class="built_in">end</span>(); ++i) &#123;</span><br><span class="line">            local += std::<span class="built_in">sinf</span>((<span class="type">float</span>)i);</span><br><span class="line">        &#125;</span><br><span class="line">        sum = local;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">join</span><span class="params">(<span class="type">const</span> SinSumReducer&amp; rhs)</span> </span>&#123;</span><br><span class="line">        sum += rhs.sum;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">1u</span> &lt;&lt; <span class="number">26</span>;</span><br><span class="line">    SinSumReducer body;</span><br><span class="line">    tbb::<span class="built_in">parallel_reduce</span>(tbb::<span class="built_in">blocked_range</span>&lt;<span class="type">size_t</span>&gt;(<span class="number">0</span>, n), body);</span><br><span class="line">    <span class="type">float</span> result = body.sum;</span><br><span class="line">    (<span class="type">void</span>)result;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-2-多字段统计：sum-min-max-count"><a href="#6-2-多字段统计：sum-min-max-count" class="headerlink" title="6.2 多字段统计：sum &#x2F; min &#x2F; max &#x2F; count"></a>6.2 多字段统计：sum &#x2F; min &#x2F; max &#x2F; count</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_reduce.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/blocked_range.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;limits&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">StatsReducer</span> &#123;</span><br><span class="line">    <span class="type">double</span> sum;</span><br><span class="line">    <span class="type">float</span>  mn;</span><br><span class="line">    <span class="type">float</span>  mx;</span><br><span class="line">    <span class="type">size_t</span> cnt;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">StatsReducer</span>()</span><br><span class="line">        : <span class="built_in">sum</span>(<span class="number">0.0</span>),</span><br><span class="line">          <span class="built_in">mn</span>(std::numeric_limits&lt;<span class="type">float</span>&gt;::<span class="built_in">infinity</span>()),</span><br><span class="line">          <span class="built_in">mx</span>(-std::numeric_limits&lt;<span class="type">float</span>&gt;::<span class="built_in">infinity</span>()),</span><br><span class="line">          <span class="built_in">cnt</span>(<span class="number">0</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">StatsReducer</span>(StatsReducer&amp;, tbb::split)</span><br><span class="line">        : <span class="built_in">sum</span>(<span class="number">0.0</span>),</span><br><span class="line">          <span class="built_in">mn</span>(std::numeric_limits&lt;<span class="type">float</span>&gt;::<span class="built_in">infinity</span>()),</span><br><span class="line">          <span class="built_in">mx</span>(-std::numeric_limits&lt;<span class="type">float</span>&gt;::<span class="built_in">infinity</span>()),</span><br><span class="line">          <span class="built_in">cnt</span>(<span class="number">0</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">const</span> std::vector&lt;<span class="type">float</span>&gt;* a = <span class="literal">nullptr</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">const</span> tbb::blocked_range&lt;<span class="type">size_t</span>&gt;&amp; r)</span> </span>&#123;</span><br><span class="line">        <span class="type">double</span> s = sum;</span><br><span class="line">        <span class="type">float</span>  lo = mn, hi = mx;</span><br><span class="line">        <span class="type">size_t</span> c = cnt;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">size_t</span> i = r.<span class="built_in">begin</span>(); i != r.<span class="built_in">end</span>(); ++i) &#123;</span><br><span class="line">            <span class="type">float</span> v = (*a)[i];</span><br><span class="line">            s += v;</span><br><span class="line">            <span class="keyword">if</span> (v &lt; lo) lo = v;</span><br><span class="line">            <span class="keyword">if</span> (v &gt; hi) hi = v;</span><br><span class="line">            ++c;</span><br><span class="line">        &#125;</span><br><span class="line">        sum = s; mn = lo; mx = hi; cnt = c;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">join</span><span class="params">(<span class="type">const</span> StatsReducer&amp; rhs)</span> </span>&#123;</span><br><span class="line">        sum += rhs.sum;</span><br><span class="line">        <span class="keyword">if</span> (rhs.mn &lt; mn) mn = rhs.mn;</span><br><span class="line">        <span class="keyword">if</span> (rhs.mx &gt; mx) mx = rhs.mx;</span><br><span class="line">        cnt += rhs.cnt;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">float</span>&gt; <span class="title">a</span><span class="params">(<span class="number">1u</span>&lt;&lt;<span class="number">20</span>, <span class="number">1.0f</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    StatsReducer body;</span><br><span class="line">    body.a = &amp;a;</span><br><span class="line">    tbb::<span class="built_in">parallel_reduce</span>(tbb::<span class="built_in">blocked_range</span>&lt;<span class="type">size_t</span>&gt;(<span class="number">0</span>, a.<span class="built_in">size</span>()), body);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// body.sum/body.mn/body.mx/body.cnt</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="7-线程本地累加器：combinable-enumerable-thread-specific"><a href="#7-线程本地累加器：combinable-enumerable-thread-specific" class="headerlink" title="7. 线程本地累加器：combinable &#x2F; enumerable_thread_specific"></a>7. 线程本地累加器：<code>combinable</code> &#x2F; <code>enumerable_thread_specific</code></h2><p>当模式是“每线程一份局部值，最后合并”，这两者非常实用。</p>
<h3 id="7-1-tbb-combinable（标量-小对象）"><a href="#7-1-tbb-combinable（标量-小对象）" class="headerlink" title="7.1 tbb::combinable&lt;T&gt;（标量&#x2F;小对象）"></a>7.1 <code>tbb::combinable&lt;T&gt;</code>（标量&#x2F;小对象）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_for.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/combinable.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">1u</span> &lt;&lt; <span class="number">26</span>;</span><br><span class="line">    <span class="function">tbb::combinable&lt;<span class="type">double</span>&gt; <span class="title">tls_sum</span><span class="params">([]&#123; <span class="keyword">return</span> <span class="number">0.0</span>; &#125;)</span></span>;</span><br><span class="line"></span><br><span class="line">    tbb::<span class="built_in">parallel_for</span>((<span class="type">size_t</span>)<span class="number">0</span>, n, [&amp;](<span class="type">size_t</span> i)&#123;</span><br><span class="line">        tls_sum.<span class="built_in">local</span>() += std::<span class="built_in">sin</span>((<span class="type">double</span>)i);</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="type">double</span> sum = tls_sum.<span class="built_in">combine</span>([](<span class="type">double</span> a, <span class="type">double</span> b)&#123; <span class="keyword">return</span> a + b; &#125;);</span><br><span class="line">    (<span class="type">void</span>)sum;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="7-2-tbb-enumerable-thread-specific"><a href="#7-2-tbb-enumerable-thread-specific" class="headerlink" title="7.2 tbb::enumerable_thread_specific&lt;T&gt;"></a>7.2 <code>tbb::enumerable_thread_specific&lt;T&gt;</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_for.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/enumerable_thread_specific.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">1u</span> &lt;&lt; <span class="number">20</span>;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">int</span>&gt; <span class="title">data</span><span class="params">(n, <span class="number">0</span>)</span></span>; <span class="comment">// 假设值域 [0,255]</span></span><br><span class="line"></span><br><span class="line">    tbb::enumerable_thread_specific&lt;std::vector&lt;<span class="type">size_t</span>&gt;&gt; <span class="built_in">tls_hist</span>(</span><br><span class="line">        []&#123; <span class="keyword">return</span> std::<span class="built_in">vector</span>&lt;<span class="type">size_t</span>&gt;(<span class="number">256</span>, <span class="number">0</span>); &#125;</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    tbb::<span class="built_in">parallel_for</span>((<span class="type">size_t</span>)<span class="number">0</span>, n, [&amp;](<span class="type">size_t</span> i)&#123;</span><br><span class="line">        tls_hist.<span class="built_in">local</span>()[(<span class="type">unsigned</span>)data[i]] += <span class="number">1</span>;</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">size_t</span>&gt; <span class="title">hist</span><span class="params">(<span class="number">256</span>, <span class="number">0</span>)</span></span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; h : tls_hist)</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> b = <span class="number">0</span>; b &lt; <span class="number">256</span>; ++b) hist[b] += h[b];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="8-扫描（Scan）：parallel-scan（前缀和-累计输出）"><a href="#8-扫描（Scan）：parallel-scan（前缀和-累计输出）" class="headerlink" title="8. 扫描（Scan）：parallel_scan（前缀和&#x2F;累计输出）"></a>8. 扫描（Scan）：<code>parallel_scan</code>（前缀和&#x2F;累计输出）</h2><p><code>parallel_scan</code> 常用于：前缀和、累计概率、积分图等。<br>关键机制：两阶段（pre-scan &#x2F; final-scan），用 <code>is_final</code> 控制是否写输出。</p>
<h3 id="8-1-parallel-scan（lambda-版）"><a href="#8-1-parallel-scan（lambda-版）" class="headerlink" title="8.1 parallel_scan（lambda 版）"></a>8.1 <code>parallel_scan</code>（lambda 版）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_scan.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/blocked_range.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">1u</span> &lt;&lt; <span class="number">20</span>;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">float</span>&gt; <span class="title">prefix</span><span class="params">(n)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="type">float</span> total = tbb::<span class="built_in">parallel_scan</span>(</span><br><span class="line">        tbb::<span class="built_in">blocked_range</span>&lt;<span class="type">size_t</span>&gt;(<span class="number">0</span>, n),</span><br><span class="line">        <span class="number">0.0f</span>,</span><br><span class="line">        [&amp;](<span class="type">const</span> tbb::blocked_range&lt;<span class="type">size_t</span>&gt;&amp; r, <span class="type">float</span> running, <span class="type">bool</span> is_final) -&gt; <span class="type">float</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="type">size_t</span> i = r.<span class="built_in">begin</span>(); i != r.<span class="built_in">end</span>(); ++i) &#123;</span><br><span class="line">                running += std::<span class="built_in">sinf</span>((<span class="type">float</span>)i);</span><br><span class="line">                <span class="keyword">if</span> (is_final) prefix[i] = running;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> running;</span><br><span class="line">        &#125;,</span><br><span class="line">        [](<span class="type">float</span> a, <span class="type">float</span> b) -&gt; <span class="type">float</span> &#123; <span class="keyword">return</span> a + b; &#125;</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; prefix[n/<span class="number">2</span>] &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">    std::cout &lt;&lt; total &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="8-2-parallel-scan（结构体版：工程范式）"><a href="#8-2-parallel-scan（结构体版：工程范式）" class="headerlink" title="8.2 parallel_scan（结构体版：工程范式）"></a>8.2 <code>parallel_scan</code>（结构体版：工程范式）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_scan.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/blocked_range.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstddef&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">PrefixScanBody</span> &#123;</span><br><span class="line">    <span class="type">const</span> std::vector&lt;<span class="type">float</span>&gt;&amp; in;</span><br><span class="line">    std::vector&lt;<span class="type">float</span>&gt;&amp; out;</span><br><span class="line">    <span class="type">float</span> sum;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">PrefixScanBody</span>(<span class="type">const</span> std::vector&lt;<span class="type">float</span>&gt;&amp; in_, std::vector&lt;<span class="type">float</span>&gt;&amp; out_)</span><br><span class="line">        : <span class="built_in">in</span>(in_), <span class="built_in">out</span>(out_), <span class="built_in">sum</span>(<span class="number">0.0f</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">PrefixScanBody</span>(PrefixScanBody&amp; b, tbb::split)</span><br><span class="line">        : <span class="built_in">in</span>(b.in), <span class="built_in">out</span>(b.out), <span class="built_in">sum</span>(<span class="number">0.0f</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">template</span> &lt;<span class="keyword">typename</span> Tag&gt;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">const</span> tbb::blocked_range&lt;<span class="type">size_t</span>&gt;&amp; r, Tag)</span> </span>&#123;</span><br><span class="line">        <span class="type">float</span> temp = sum;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">size_t</span> i = r.<span class="built_in">begin</span>(); i != r.<span class="built_in">end</span>(); ++i) &#123;</span><br><span class="line">            temp += in[i];</span><br><span class="line">            <span class="function"><span class="keyword">if</span> <span class="title">constexpr</span> <span class="params">(std::is_same_v&lt;Tag, tbb::final_scan_tag&gt;)</span> </span>&#123;</span><br><span class="line">                out[i] = temp;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        sum = temp;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">reverse_join</span><span class="params">(PrefixScanBody&amp; rhs)</span> </span>&#123; sum += rhs.sum; &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">assign</span><span class="params">(PrefixScanBody&amp; rhs)</span>       </span>&#123; sum  = rhs.sum; &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">1u</span> &lt;&lt; <span class="number">20</span>;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">float</span>&gt; <span class="title">in</span><span class="params">(n, <span class="number">1.0f</span>)</span></span>;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">float</span>&gt; <span class="title">out</span><span class="params">(n, <span class="number">0.0f</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">PrefixScanBody <span class="title">body</span><span class="params">(in, out)</span></span>;</span><br><span class="line">    tbb::<span class="built_in">parallel_scan</span>(tbb::<span class="built_in">blocked_range</span>&lt;<span class="type">size_t</span>&gt;(<span class="number">0</span>, n), body);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="9-任务域与嵌套：task-arena-isolate"><a href="#9-任务域与嵌套：task-arena-isolate" class="headerlink" title="9. 任务域与嵌套：task_arena &#x2F; isolate"></a>9. 任务域与嵌套：<code>task_arena</code> &#x2F; <code>isolate</code></h2><h3 id="9-1-task-arena：限制并行度-隔离并行区域"><a href="#9-1-task-arena：限制并行度-隔离并行区域" class="headerlink" title="9.1 task_arena：限制并行度 &#x2F; 隔离并行区域"></a>9.1 <code>task_arena</code>：限制并行度 &#x2F; 隔离并行区域</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/task_arena.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_for.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cmath&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">1u</span> &lt;&lt; <span class="number">20</span>;</span><br><span class="line">    <span class="function">std::vector&lt;<span class="type">float</span>&gt; <span class="title">a</span><span class="params">(n)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">tbb::task_arena <span class="title">arena</span><span class="params">(<span class="number">4</span>)</span></span>; <span class="comment">// 该区域最多 4 个线程参与</span></span><br><span class="line">    arena.<span class="built_in">execute</span>([&amp;]&#123;</span><br><span class="line">        tbb::<span class="built_in">parallel_for</span>((<span class="type">size_t</span>)<span class="number">0</span>, n, [&amp;](<span class="type">size_t</span> i)&#123;</span><br><span class="line">            a[i] = std::<span class="built_in">sinf</span>((<span class="type">float</span>)i);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="9-2-this-task-arena-isolate：禁止内部任务被窃取（隔离干扰）"><a href="#9-2-this-task-arena-isolate：禁止内部任务被窃取（隔离干扰）" class="headerlink" title="9.2 this_task_arena::isolate：禁止内部任务被窃取（隔离干扰）"></a>9.2 <code>this_task_arena::isolate</code>：禁止内部任务被窃取（隔离干扰）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/this_task_arena.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">tbb::this_task_arena::<span class="built_in">isolate</span>([&amp;]&#123;</span><br><span class="line">    <span class="comment">// 这里 spawn 的任务更隔离，不易跨域被 steal</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>实务提醒：嵌套并行时不要依赖“线程固定&#x2F;执行路径固定”。更推荐减少共享状态、用 reduce&#x2F;tls 合并，或用 arena&#x2F;isolate 控制并行边界。</p>
</blockquote>
<hr>
<h2 id="10-分块策略（Partitioner）：性能与可预测性"><a href="#10-分块策略（Partitioner）：性能与可预测性" class="headerlink" title="10. 分块策略（Partitioner）：性能与可预测性"></a>10. 分块策略（Partitioner）：性能与可预测性</h2><p>TBB 切分范围时可以指定 partitioner：</p>
<ul>
<li><code>tbb::static_partitioner</code>：划分更固定、可预测</li>
<li><code>tbb::affinity_partitioner</code>：记录历史映射，提高缓存命中（适合重复执行的相似循环）</li>
<li><code>tbb::simple_partitioner</code>：简单切分策略</li>
</ul>
<p>示例：观察每个线程拿到的块</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_for.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/blocked_range.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/task_arena.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/this_task_arena.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">const</span> <span class="type">size_t</span> n = <span class="number">32</span>;</span><br><span class="line">    <span class="function">tbb::task_arena <span class="title">arena</span><span class="params">(<span class="number">4</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    arena.<span class="built_in">execute</span>([&amp;]&#123;</span><br><span class="line">        tbb::<span class="built_in">parallel_for</span>(</span><br><span class="line">            tbb::<span class="built_in">blocked_range</span>&lt;<span class="type">size_t</span>&gt;(<span class="number">0</span>, n),</span><br><span class="line">            [&amp;](<span class="type">const</span> tbb::blocked_range&lt;<span class="type">size_t</span>&gt;&amp; r)&#123;</span><br><span class="line">                std::cout</span><br><span class="line">                    &lt;&lt; <span class="string">&quot;tid=&quot;</span> &lt;&lt; tbb::this_task_arena::<span class="built_in">current_thread_index</span>()</span><br><span class="line">                    &lt;&lt; <span class="string">&quot; range=[&quot;</span> &lt;&lt; r.<span class="built_in">begin</span>() &lt;&lt; <span class="string">&quot;,&quot;</span> &lt;&lt; r.<span class="built_in">end</span>() &lt;&lt; <span class="string">&quot;)&quot;</span></span><br><span class="line">                    &lt;&lt; <span class="string">&quot; size=&quot;</span> &lt;&lt; r.<span class="built_in">size</span>() &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">            &#125;,</span><br><span class="line">            tbb::static_partitioner&#123;&#125;</span><br><span class="line">        );</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="11-全局并行度控制：global-control（工程常用）"><a href="#11-全局并行度控制：global-control（工程常用）" class="headerlink" title="11. 全局并行度控制：global_control（工程常用）"></a>11. 全局并行度控制：<code>global_control</code>（工程常用）</h2><p>当你不希望 TBB “吃满所有核”，可全局限制：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/global_control.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">tbb::global_control <span class="title">gc</span><span class="params">(tbb::global_control::max_allowed_parallelism, <span class="number">8</span>)</span></span>;</span><br><span class="line">    <span class="comment">// 后续 TBB 并行算法最多使用 8 个线程</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="12-并发容器：concurrent-vector"><a href="#12-并发容器：concurrent-vector" class="headerlink" title="12. 并发容器：concurrent_vector"></a>12. 并发容器：<code>concurrent_vector</code></h2><p>特点：并发 push 更友好，但实现上可能是分段存储，不等同于 <code>std::vector</code> 的严格连续内存语义。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/concurrent_vector.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_for.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    tbb::concurrent_vector&lt;std::string&gt; out;</span><br><span class="line"></span><br><span class="line">    tbb::<span class="built_in">parallel_for</span>(<span class="number">0</span>, <span class="number">1000</span>, [&amp;](<span class="type">int</span> i)&#123;</span><br><span class="line">        out.<span class="built_in">push_back</span>(<span class="string">&quot;item_&quot;</span> + std::<span class="built_in">to_string</span>(i));</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="13-流水线并行：parallel-pipeline（I-O-compute-的标准解法）"><a href="#13-流水线并行：parallel-pipeline（I-O-compute-的标准解法）" class="headerlink" title="13. 流水线并行：parallel_pipeline（I&#x2F;O + compute 的标准解法）"></a>13. 流水线并行：<code>parallel_pipeline</code>（I&#x2F;O + compute 的标准解法）</h2><p>适用：读入→解析→计算→写出，多阶段、不同并行度需求。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/parallel_pipeline.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;tbb/flow_control.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> x = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    tbb::<span class="built_in">parallel_pipeline</span>(</span><br><span class="line">        <span class="number">4</span>, <span class="comment">// max live tokens</span></span><br><span class="line"></span><br><span class="line">        tbb::<span class="built_in">make_filter</span>&lt;<span class="type">void</span>, <span class="type">int</span>&gt;(</span><br><span class="line">            tbb::filter_mode::serial_in_order,</span><br><span class="line">            [&amp;](tbb::flow_control&amp; fc) -&gt; <span class="type">int</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (x &gt;= <span class="number">20</span>) &#123; fc.<span class="built_in">stop</span>(); <span class="keyword">return</span> <span class="number">0</span>; &#125;</span><br><span class="line">                <span class="keyword">return</span> x++;</span><br><span class="line">            &#125;</span><br><span class="line">        )</span><br><span class="line">        &amp;</span><br><span class="line">        tbb::<span class="built_in">make_filter</span>&lt;<span class="type">int</span>, <span class="type">int</span>&gt;(</span><br><span class="line">            tbb::filter_mode::parallel,</span><br><span class="line">            [&amp;](<span class="type">int</span> v) -&gt; <span class="type">int</span> &#123;</span><br><span class="line">                <span class="keyword">return</span> v * v; <span class="comment">// heavy compute</span></span><br><span class="line">            &#125;</span><br><span class="line">        )</span><br><span class="line">        &amp;</span><br><span class="line">        tbb::<span class="built_in">make_filter</span>&lt;<span class="type">int</span>, <span class="type">void</span>&gt;(</span><br><span class="line">            tbb::filter_mode::serial_in_order,</span><br><span class="line">            [&amp;](<span class="type">int</span> y) &#123;</span><br><span class="line">                std::cout &lt;&lt; y &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        )</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h2><p><a href="https://en.cppreference.com/w/cpp/language/access.html?utm_source=chatgpt.com">https://en.cppreference.com/w/cpp/language/access.html?utm_source=chatgpt.com</a><br><a href="https://en.cppreference.com/w/cpp/language/operators.html?utm_source=chatgpt.com">https://en.cppreference.com/w/cpp/language/operators.html?utm_source=chatgpt.com</a><br><a href="https://mooshak.dcc.fc.up.pt/~oni-judge/doc/cppreference/reference/en/cpp/language/constructor.html?utm_source=chatgpt.com">https://mooshak.dcc.fc.up.pt/~oni-judge/doc/cppreference/reference/en/cpp/language/constructor.html?utm_source=chatgpt.com</a></p>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>Fighting Netcode 项目知识笔记</title>
    <url>//notes/%E9%A1%B9%E7%9B%AE%E5%88%86%E6%9E%90/fighting-netcode-%E9%A1%B9%E7%9B%AE%E7%9F%A5%E8%AF%86%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="Fighting-Netcode-项目知识笔记"><a href="#Fighting-Netcode-项目知识笔记" class="headerlink" title="Fighting Netcode 项目知识笔记"></a>Fighting Netcode 项目知识笔记</h1><h2 id="1-这个项目是什么"><a href="#1-这个项目是什么" class="headerlink" title="1. 这个项目是什么"></a>1. 这个项目是什么</h2><p><code>Fighting</code> 是一个用 C++ 写的联机动作游戏同步 Demo。</p>
<p>项目地址：</p>
<p><code>/Users/chutian/Desktop/Fighting</code></p>
<p>它不是 AI 模型训练项目，而是一个很适合学习“实时系统工程”的项目。它实现的是：</p>
<ul>
<li>固定帧推进</li>
<li>UDP 通信</li>
<li>服务端权威状态</li>
<li>客户端本地预测</li>
<li>输入冗余</li>
<li>回滚重放</li>
<li>状态快照</li>
<li>状态哈希对账</li>
<li>压力测试</li>
</ul>
<p>可以把它理解成一个“小型实时分布式系统”。</p>
<p>如果放到 AI 模型开发学习体系里，它最值得参考的不是模型算法，而是工程能力：</p>
<ul>
<li>如何让多个端的状态保持一致</li>
<li>如何处理延迟、丢包和乱序</li>
<li>如何用快照、日志和 hash 定位状态分叉</li>
<li>如何用固定 tick 管理实时流程</li>
<li>如何把核心逻辑拆成可测试的模块</li>
</ul>
<p>一句话概括：</p>
<blockquote>
<p>这个项目是一个 60Hz 回滚同步网络游戏 Demo，但它背后的工程思想可以迁移到 AI 推理服务、Agent 执行系统、实时任务调度和分布式状态一致性设计里。</p>
</blockquote>
<hr>
<h2 id="2-项目整体目标"><a href="#2-项目整体目标" class="headerlink" title="2. 项目整体目标"></a>2. 项目整体目标</h2><p>这个项目不是为了做一个完整游戏，而是为了跑通联机动作游戏最难的几件事。</p>
<p>核心目标是：</p>
<ul>
<li>玩家输入能快速反馈</li>
<li>服务端保持最终权威</li>
<li>网络延迟不会让本地操作卡顿</li>
<li>客户端预测错了以后可以回滚修正</li>
<li>服务端和客户端可以通过 hash 判断状态是否分叉</li>
</ul>
<p>联机动作游戏里，一个常见问题是：</p>
<blockquote>
<p>如果每次按键都等服务端确认，游戏会非常卡；如果完全相信客户端，又容易作弊和状态不一致。</p>
</blockquote>
<p>这个项目采用的方案是：</p>
<ul>
<li>客户端先预测，保证操作手感</li>
<li>服务端做权威推进，保证最终正确</li>
<li>客户端收到权威状态后回滚重放，修正预测误差</li>
</ul>
<p>这就是 Rollback Netcode 的核心思想。</p>
<hr>
<h2 id="3-项目目录结构"><a href="#3-项目目录结构" class="headerlink" title="3. 项目目录结构"></a>3. 项目目录结构</h2><p>项目主要目录如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">apps/</span><br><span class="line">  client_main.cpp</span><br><span class="line">  server_main.cpp</span><br><span class="line"></span><br><span class="line">include/lab/</span><br><span class="line">  app/</span><br><span class="line">  core/</span><br><span class="line">  io/</span><br><span class="line">  net/</span><br><span class="line">  sim/</span><br><span class="line">  time/</span><br><span class="line">  util/</span><br><span class="line"></span><br><span class="line">src/</span><br><span class="line">  app/</span><br><span class="line">  core/</span><br><span class="line">  io/</span><br><span class="line">  net/</span><br><span class="line">  sim/</span><br><span class="line">  time/</span><br><span class="line"></span><br><span class="line">tests/</span><br><span class="line">  core_tests.cpp</span><br><span class="line">  stress_tests.cpp</span><br><span class="line"></span><br><span class="line">docs/</span><br><span class="line">  ARCHITECTURE.md</span><br><span class="line"></span><br><span class="line">README.md</span><br><span class="line">日志.md</span><br><span class="line">CMakeLists.txt</span><br></pre></td></tr></table></figure>

<p>可以按三层理解：</p>
<table>
<thead>
<tr>
<th>层级</th>
<th>目录</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>模拟层</td>
<td><code>include/lab/sim</code>、<code>src/sim</code></td>
<td>世界状态、输入、快照、回滚、hash</td>
</tr>
<tr>
<td>网络层</td>
<td><code>include/lab/net</code>、<code>src/net</code></td>
<td>UDP、协议包、二进制编解码</td>
</tr>
<tr>
<td>应用层</td>
<td><code>apps</code>、<code>include/lab/app</code>、<code>src/app</code></td>
<td>服务端循环、客户端预测、渲染、输入采样</td>
</tr>
</tbody></table>
<p>这个分层比较清晰：</p>
<ul>
<li>模拟层不关心网络和窗口</li>
<li>网络层不关心游戏规则</li>
<li>应用层负责把模拟、网络、渲染拼起来</li>
</ul>
<p>这也是很多 AI 工程项目应该学习的拆法：</p>
<ul>
<li>模型推理核心逻辑</li>
<li>API &#x2F; 网络协议层</li>
<li>应用编排层</li>
<li>测试和压测层</li>
</ul>
<hr>
<h2 id="4-快速运行方式"><a href="#4-快速运行方式" class="headerlink" title="4. 快速运行方式"></a>4. 快速运行方式</h2><p>项目依赖：</p>
<ul>
<li>CMake 3.20+</li>
<li>C++20</li>
<li>libevent</li>
<li>SDL2</li>
<li>SDL2_ttf</li>
<li>nlohmann_json</li>
</ul>
<p>构建：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cmake -S . -B build</span><br><span class="line">cmake --build build</span><br></pre></td></tr></table></figure>

<p>启动服务端：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./build/lab_server</span><br></pre></td></tr></table></figure>

<p>启动两个客户端：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./build/lab_client</span><br><span class="line">./build/lab_client</span><br></pre></td></tr></table></figure>

<p>运行测试：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ctest --test-dir build --output-on-failure</span><br></pre></td></tr></table></figure>

<p>运行压力测试：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./build/lab_stress</span><br></pre></td></tr></table></figure>

<p>更长时间压力测试：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./build/lab_stress --ticks 200000 --<span class="built_in">history</span> 8192 --state-delay 12</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="5-核心概念总览"><a href="#5-核心概念总览" class="headerlink" title="5. 核心概念总览"></a>5. 核心概念总览</h2><p>这个项目最重要的概念有 8 个。</p>
<h3 id="5-1-Tick"><a href="#5-1-Tick" class="headerlink" title="5.1 Tick"></a>5.1 Tick</h3><p><code>Tick</code> 是逻辑帧编号。</p>
<p>代码里定义：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> Tick = <span class="type">uint32_t</span>;</span><br></pre></td></tr></table></figure>

<p>游戏不是直接按真实时间推进，而是按离散帧推进。</p>
<p>例如 60Hz 表示：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1 秒 = 60 个 tick</span><br><span class="line">1 tick = 1 / 60 秒</span><br></pre></td></tr></table></figure>

<p>这样做的好处是：</p>
<ul>
<li>逻辑顺序明确</li>
<li>输入可以绑定到某个 tick</li>
<li>状态可以按 tick 保存</li>
<li>回滚时可以从某个 tick 重新模拟</li>
<li>网络包可以明确说明自己属于哪一帧</li>
</ul>
<p>AI 工程迁移理解：</p>
<blockquote>
<p>Tick 类似任务系统里的 step id、事件序号、offset、version。只要系统存在异步、重试、回放，就需要这种明确的逻辑编号。</p>
</blockquote>
<h3 id="5-2-InputCmd"><a href="#5-2-InputCmd" class="headerlink" title="5.2 InputCmd"></a>5.2 InputCmd</h3><p><code>InputCmd</code> 是每一帧的最小输入单位。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">InputCmd</span> &#123;</span><br><span class="line">    Tick tick = <span class="number">0</span>;</span><br><span class="line">    <span class="type">uint16_t</span> buttons = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int8_t</span> moveX = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int8_t</span> moveY = <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>它包含：</p>
<ul>
<li>属于哪个 tick</li>
<li>按键位图</li>
<li>水平方向</li>
<li>垂直方向</li>
</ul>
<p>这个结构很小，适合频繁发送。</p>
<p>设计重点：</p>
<ul>
<li>输入要带 tick</li>
<li>输入结构要尽量小</li>
<li>输入可以重复发送，服务端按 tick 去重覆盖</li>
</ul>
<h3 id="5-3-WorldSnapshot"><a href="#5-3-WorldSnapshot" class="headerlink" title="5.3 WorldSnapshot"></a>5.3 WorldSnapshot</h3><p><code>WorldSnapshot</code> 是世界状态快照。</p>
<p>它包含：</p>
<ul>
<li>当前 tick</li>
<li>玩家状态</li>
<li>子弹状态</li>
<li>迷宫 seed</li>
<li>迷宫网格</li>
</ul>
<p>它的作用是：</p>
<ul>
<li>网络状态同步</li>
<li>回滚恢复</li>
<li>hash 对账</li>
<li>测试验证</li>
</ul>
<p>这说明项目把“状态数据”和“推进逻辑”分开了。</p>
<p>这种设计很重要：</p>
<blockquote>
<p>只要系统支持回滚、回放、审计、调试，就应该有明确的 Snapshot 结构。</p>
</blockquote>
<h3 id="5-4-World-Step"><a href="#5-4-World-Step" class="headerlink" title="5.4 World::Step"></a>5.4 World::Step</h3><p><code>World::Step(cmds, dt)</code> 是世界推进入口。</p>
<p>它接收所有玩家在同一个 tick 的输入，然后推进一帧。</p>
<p>关键要求：</p>
<ul>
<li><code>cmds.size()</code> 必须等于玩家数量</li>
<li>所有 <code>cmd.tick</code> 必须一致</li>
<li>同样输入和同样初始状态，应该得到同样输出</li>
</ul>
<p>这就是确定性模拟的基础。</p>
<h3 id="5-5-InputBuffer"><a href="#5-5-InputBuffer" class="headerlink" title="5.5 InputBuffer"></a>5.5 InputBuffer</h3><p><code>InputBuffer</code> 是按 tick 存输入的环形缓冲。</p>
<p>核心逻辑：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">idx = tick % capacity</span><br></pre></td></tr></table></figure>

<p>读取时不只看槽位，还要校验 tick：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (ring_[idx].cmd.tick != tick) <span class="keyword">return</span> std::<span class="literal">nullopt</span>;</span><br></pre></td></tr></table></figure>

<p>这个校验非常关键。</p>
<p>因为环形缓冲会覆盖旧数据，如果只看下标，不看 tick，就可能把旧帧误当成当前帧。</p>
<h3 id="5-6-StateHistory"><a href="#5-6-StateHistory" class="headerlink" title="5.6 StateHistory"></a>5.6 StateHistory</h3><p><code>StateHistory</code> 是按 tick 存世界快照的环形缓冲。</p>
<p>它和 <code>InputBuffer</code> 思路一样：</p>
<ul>
<li>写入时按 <code>tick % cap</code></li>
<li>读取时校验真实 tick</li>
</ul>
<p>它用于：</p>
<ul>
<li>客户端保存预测状态</li>
<li>收到权威状态后比较误差</li>
<li>从某个权威 tick 恢复并重放</li>
<li>hash 对账</li>
</ul>
<h3 id="5-7-State-Hash"><a href="#5-7-State-Hash" class="headerlink" title="5.7 State Hash"></a>5.7 State Hash</h3><p><code>Hasher</code> 对 <code>WorldSnapshot</code> 做 hash。</p>
<p>它不是直接 hash float 的内存，而是先做毫米级量化：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int32_t</span> <span class="title">QuantizeMm</span><span class="params">(<span class="type">float</span> v)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> std::<span class="built_in">lround</span>(v * <span class="number">1000.0f</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样做是为了避免浮点误差造成假 mismatch。</p>
<p>hash 覆盖内容包括：</p>
<ul>
<li>tick</li>
<li>玩家位置和速度</li>
<li>玩家 HP</li>
<li>action</li>
<li>cooldown</li>
<li>aim 方向</li>
<li>子弹状态</li>
<li>迷宫 seed 和网格</li>
</ul>
<p>如果新增一个会影响模拟结果的字段，但忘了加入快照、网络包或 hash，就容易出现服务端和客户端状态分叉。</p>
<h3 id="5-8-Rollback-Replay"><a href="#5-8-Rollback-Replay" class="headerlink" title="5.8 Rollback Replay"></a>5.8 Rollback Replay</h3><p>回滚重放是客户端收到权威状态后的修正流程。</p>
<p>基本公式：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Restore(authoritativeSnapshot)</span><br><span class="line">for t in authoritativeTick + 1 .. localTick - 1:</span><br><span class="line">  取本地输入</span><br><span class="line">  取远端预测输入</span><br><span class="line">  World::Step(cmds, dt)</span><br><span class="line">  保存快照</span><br></pre></td></tr></table></figure>

<p>它解决的问题是：</p>
<ul>
<li>客户端先预测，保证响应快</li>
<li>权威状态来了以后，以权威状态为准</li>
<li>重新模拟未来帧，让当前画面追上本地时间</li>
</ul>
<hr>
<h2 id="6-服务端流程"><a href="#6-服务端流程" class="headerlink" title="6. 服务端流程"></a>6. 服务端流程</h2><p>服务端是权威端。</p>
<p>核心文件：</p>
<p><code>apps/server_main.cpp</code></p>
<p>服务端主要做 6 件事：</p>
<ol>
<li>启动 UDP socket</li>
<li>接收客户端 hello &#x2F; input</li>
<li>按地址分配玩家 slot</li>
<li>收齐玩家后发送 <code>Start</code></li>
<li>每个 tick 收集输入并推进权威世界</li>
<li>下发 <code>Ack</code> 和 <code>State</code></li>
</ol>
<h3 id="6-1-玩家分配"><a href="#6-1-玩家分配" class="headerlink" title="6.1 玩家分配"></a>6.1 玩家分配</h3><p>服务端用客户端地址作为 key。</p>
<p>每个客户端连接后分配：</p>
<ul>
<li>player1</li>
<li>player2</li>
</ul>
<p>如果人数满了，后续客户端会被拒绝。</p>
<h3 id="6-2-开局同步"><a href="#6-2-开局同步" class="headerlink" title="6.2 开局同步"></a>6.2 开局同步</h3><p>服务端收齐 <code>kRequiredPlayers = 2</code> 后，不是立刻从当前 tick 开始，而是设置：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">startTick = tick + kStartDelayTicks;</span><br></pre></td></tr></table></figure>

<p>这个设计用于给客户端一点时间接收开局包，减少起步不同步。</p>
<h3 id="6-3-缺输入处理"><a href="#6-3-缺输入处理" class="headerlink" title="6.3 缺输入处理"></a>6.3 缺输入处理</h3><p>服务端每个 tick 都要给每个玩家拿到输入。</p>
<p>如果该 tick 有输入：</p>
<ul>
<li>直接使用</li>
</ul>
<p>如果没有输入，但上次输入还在 hold 窗口内：</p>
<ul>
<li>复用上一帧输入</li>
<li>把 tick 改成当前 tick</li>
</ul>
<p>如果超出 hold 窗口：</p>
<ul>
<li>使用默认空输入</li>
</ul>
<p>这段逻辑本质上是在处理 UDP 丢包和延迟。</p>
<h3 id="6-4-权威推进"><a href="#6-4-权威推进" class="headerlink" title="6.4 权威推进"></a>6.4 权威推进</h3><p>服务端每帧执行：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">cmds = 每个玩家当前 tick 的输入</span><br><span class="line">world.Step(cmds, dt)</span><br><span class="line">snapshot = world.Snapshot()</span><br><span class="line">hash = Hasher::Hash(snapshot)</span><br></pre></td></tr></table></figure>

<p>服务端的状态是最终可信状态。</p>
<p>客户端的预测状态只是为了体验。</p>
<h3 id="6-5-状态下发"><a href="#6-5-状态下发" class="headerlink" title="6.5 状态下发"></a>6.5 状态下发</h3><p>服务端每帧发 <code>Ack</code>，每隔若干帧发完整 <code>State</code>。</p>
<p>当前配置：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">kStateEvery = <span class="number">2</span></span><br></pre></td></tr></table></figure>

<p>也就是每 2 tick 下发一次完整状态。</p>
<p>这种设计平衡了：</p>
<ul>
<li>状态同步频率</li>
<li>带宽消耗</li>
<li>回滚修正速度</li>
</ul>
<hr>
<h2 id="7-客户端流程"><a href="#7-客户端流程" class="headerlink" title="7. 客户端流程"></a>7. 客户端流程</h2><p>客户端负责“先动起来，然后等权威校正”。</p>
<p>核心文件：</p>
<p><code>apps/client_main.cpp</code></p>
<p>客户端主要做 8 件事：</p>
<ol>
<li>未开局时定期发送 hello</li>
<li>收到 <code>Start</code> 后对齐 <code>startTick</code></li>
<li>每 tick 采样键盘输入</li>
<li>本地保存输入历史</li>
<li>预测远端玩家输入</li>
<li>本地推进预测世界</li>
<li>发送带冗余的输入包</li>
<li>收到权威 <code>State</code> 后恢复并重放</li>
</ol>
<h3 id="7-1-本地预测"><a href="#7-1-本地预测" class="headerlink" title="7.1 本地预测"></a>7.1 本地预测</h3><p>本地玩家的输入来自键盘。</p>
<p>远端玩家的输入无法立即知道，只能预测。</p>
<p>当前预测逻辑比较简单：</p>
<ul>
<li>根据远端玩家速度推测移动方向</li>
<li>如果远端处于 Hitstun，则预测不动</li>
<li>如果速度接近 0，则预测停止</li>
</ul>
<p>相关文件：</p>
<p><code>src/app/InputPrediction.cpp</code></p>
<p>这不是高级 AI，而是一个经验规则。</p>
<h3 id="7-2-输入冗余发送"><a href="#7-2-输入冗余发送" class="headerlink" title="7.2 输入冗余发送"></a>7.2 输入冗余发送</h3><p>客户端每个 tick 不只发送当前输入，而是发送最近 K 帧输入。</p>
<p>配置：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">kInputRedundancy = <span class="number">4</span></span><br></pre></td></tr></table></figure>

<p>这样即使 UDP 丢了某个包，后续包也可能补上之前几帧输入。</p>
<p>输入包里包含：</p>
<ul>
<li>playerId</li>
<li>seq</li>
<li>newestTick</li>
<li>clientAckServerTick</li>
<li>最近 K 个 <code>InputCmd</code></li>
</ul>
<h3 id="7-3-权威状态应用"><a href="#7-3-权威状态应用" class="headerlink" title="7.3 权威状态应用"></a>7.3 权威状态应用</h3><p>客户端收到 <code>State</code> 后，会把网络包还原成 <code>WorldSnapshot</code>。</p>
<p>然后做几件事：</p>
<ul>
<li>校验 <code>stateHash</code></li>
<li>判断本地玩家是否偏离权威状态</li>
<li>把权威快照写入 <code>StateHistory</code></li>
<li>从权威快照恢复</li>
<li>重放后续输入到当前 tick</li>
</ul>
<p>注意：</p>
<p>项目只用本地玩家差异来判断是否计入 rollback。</p>
<p>原因是：</p>
<blockquote>
<p>远端玩家本来就是预测的，如果把远端误差也算进回滚触发条件，客户端会频繁回滚。</p>
</blockquote>
<h3 id="7-4-为什么即使没有计入-rollback-也要-rebase"><a href="#7-4-为什么即使没有计入-rollback-也要-rebase" class="headerlink" title="7.4 为什么即使没有计入 rollback 也要 rebase"></a>7.4 为什么即使没有计入 rollback 也要 rebase</h3><p>代码里有一个重要设计：</p>
<p>即使本地玩家误差没有超过阈值，客户端仍然会从权威快照 rebase，然后 replay。</p>
<p>这样做的原因是：</p>
<ul>
<li>远端玩家状态应该尽快采用权威值</li>
<li>子弹状态也应该采用权威值</li>
<li>不然画面可能长期偏离真实世界</li>
</ul>
<p>也就是说：</p>
<ul>
<li>rollbackCount 只是统计“本地玩家明显错误”的次数</li>
<li>rebase + replay 是状态校正流程本身</li>
</ul>
<hr>
<h2 id="8-网络协议设计"><a href="#8-网络协议设计" class="headerlink" title="8. 网络协议设计"></a>8. 网络协议设计</h2><p>核心文件：</p>
<ul>
<li><code>include/lab/net/Packets.h</code></li>
<li><code>src/net/NetCode.cpp</code></li>
</ul>
<p>当前协议版本：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">kVersion = <span class="number">3</span></span><br></pre></td></tr></table></figure>

<p>协议包类型：</p>
<table>
<thead>
<tr>
<th>包</th>
<th>方向</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><code>Input</code></td>
<td>Client -&gt; Server</td>
<td>上传玩家输入</td>
</tr>
<tr>
<td><code>Start</code></td>
<td>Server -&gt; Client</td>
<td>分配 playerId，告知 startTick</td>
</tr>
<tr>
<td><code>Ack</code></td>
<td>Server -&gt; Client</td>
<td>告知服务端处理进度和 hash</td>
</tr>
<tr>
<td><code>State</code></td>
<td>Server -&gt; Client</td>
<td>下发权威状态</td>
</tr>
</tbody></table>
<h3 id="8-1-二进制编解码"><a href="#8-1-二进制编解码" class="headerlink" title="8.1 二进制编解码"></a>8.1 二进制编解码</h3><p>项目手写了二进制协议。</p>
<p>特点：</p>
<ul>
<li>使用固定 magic</li>
<li>使用协议 version</li>
<li>使用 packet type</li>
<li>整数统一转网络字节序</li>
<li>64 位整数拆成两个 32 位写入</li>
<li>float 不直接传，而是转毫米整数</li>
</ul>
<p>例如状态里的位置：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">ps.x_mm = std::<span class="built_in">lround</span>(wp.x * <span class="number">1000.0f</span>);</span><br></pre></td></tr></table></figure>

<p>接收端还原：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> x = x_mm / <span class="number">1000.0f</span>;</span><br></pre></td></tr></table></figure>

<p>这比直接传 float 更稳定，也更容易做 hash 对账。</p>
<h3 id="8-2-为什么要有-magic-和-version"><a href="#8-2-为什么要有-magic-和-version" class="headerlink" title="8.2 为什么要有 magic 和 version"></a>8.2 为什么要有 magic 和 version</h3><p><code>magic</code> 用来识别是不是本协议的数据包。</p>
<p><code>version</code> 用来防止不同协议版本之间误解码。</p>
<p>这在 AI 服务协议里也很重要。</p>
<p>例如模型推理请求如果格式变了，最好也有：</p>
<ul>
<li>api version</li>
<li>schema version</li>
<li>feature version</li>
<li>model version</li>
</ul>
<p>否则新旧客户端混用时很难排查问题。</p>
<h3 id="8-3-Input-包为什么带冗余"><a href="#8-3-Input-包为什么带冗余" class="headerlink" title="8.3 Input 包为什么带冗余"></a>8.3 Input 包为什么带冗余</h3><p>UDP 不保证可靠。</p>
<p>如果每个输入只发一次：</p>
<ul>
<li>丢包就会丢输入</li>
<li>服务端只能默认空输入</li>
<li>玩家表现会突然停顿</li>
</ul>
<p>所以 Input 包会带最近 K 帧输入。</p>
<p>这是一种简单可靠的抗丢包策略。</p>
<p>AI 工程里类似的思想是：</p>
<ul>
<li>请求重试带幂等 id</li>
<li>消息队列消费保留 offset</li>
<li>事件流消费允许重复事件</li>
<li>服务端按版本号或序号去重</li>
</ul>
<hr>
<h2 id="9-世界模拟逻辑"><a href="#9-世界模拟逻辑" class="headerlink" title="9. 世界模拟逻辑"></a>9. 世界模拟逻辑</h2><p>核心文件：</p>
<p><code>src/sim/World.cpp</code></p>
<p>当前世界是一个顶视角小型“迷宫坦克”。</p>
<p>包含：</p>
<ul>
<li>迷宫地图</li>
<li>玩家移动</li>
<li>墙体碰撞</li>
<li>玩家之间 pushbox 分离</li>
<li>子弹发射</li>
<li>子弹碰撞</li>
<li>HP 扣减</li>
<li>hitstun</li>
<li>cooldown</li>
</ul>
<h3 id="9-1-迷宫生成"><a href="#9-1-迷宫生成" class="headerlink" title="9.1 迷宫生成"></a>9.1 迷宫生成</h3><p>迷宫由固定 seed 生成。</p>
<p>服务端通过 <code>mazeSeed</code> 下发给客户端。</p>
<p>这样客户端不需要每次接收完整地图，只要用同样 seed 就可以生成同样迷宫。</p>
<p>但项目的 <code>WorldSnapshot</code> 里也保留了迷宫网格。</p>
<p>这样更稳：</p>
<ul>
<li>seed 用于重建</li>
<li>maze 数据用于快照、hash 和回滚</li>
</ul>
<h3 id="9-2-玩家移动"><a href="#9-2-玩家移动" class="headerlink" title="9.2 玩家移动"></a>9.2 玩家移动</h3><p>玩家速度不是瞬间切换，而是用摩擦插值靠近目标速度：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">v += (targetV - v) * alpha</span><br></pre></td></tr></table></figure>

<p>这样移动更平滑。</p>
<p>但它依然是确定性的，因为每一帧都只由：</p>
<ul>
<li>当前状态</li>
<li>当前输入</li>
<li>固定 dt</li>
</ul>
<p>共同决定。</p>
<h3 id="9-3-子弹状态"><a href="#9-3-子弹状态" class="headerlink" title="9.3 子弹状态"></a>9.3 子弹状态</h3><p>子弹包含：</p>
<ul>
<li>位置</li>
<li>速度</li>
<li>owner</li>
<li>life</li>
</ul>
<p>子弹撞墙或命中玩家后消失。</p>
<p>命中后：</p>
<ul>
<li>被击中玩家 HP 减少</li>
<li>进入 Hitstun</li>
<li>产生轻微击退</li>
</ul>
<p>子弹也进入快照、网络包和 hash。</p>
<p>这是必须的。</p>
<p>因为子弹会影响未来游戏状态。</p>
<h3 id="9-4-aimX-aimY-的意义"><a href="#9-4-aimX-aimY-的意义" class="headerlink" title="9.4 aimX &#x2F; aimY 的意义"></a>9.4 aimX &#x2F; aimY 的意义</h3><p>玩家状态里有：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int8_t</span> aimX;</span><br><span class="line"><span class="type">int8_t</span> aimY;</span><br></pre></td></tr></table></figure>

<p>它保存最近的瞄准方向。</p>
<p>这个字段很关键。</p>
<p>如果玩家停下来以后再开火，当前 moveX &#x2F; moveY 可能是 0。</p>
<p>这时开火方向就要依赖上一次方向。</p>
<p>如果回滚恢复时没有保存 aim 方向，重放结果就可能和服务端不同。</p>
<p>这个例子说明：</p>
<blockquote>
<p>只要某个字段会影响未来模拟结果，它就必须进入 Snapshot、网络包和 hash。</p>
</blockquote>
<hr>
<h2 id="10-固定帧推进"><a href="#10-固定帧推进" class="headerlink" title="10. 固定帧推进"></a>10. 固定帧推进</h2><p>服务端和客户端都使用 accumulator 控制固定时间步。</p>
<p>基本结构：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">frame = now - prev</span><br><span class="line">acc += min(frame, maxFrame)</span><br><span class="line"></span><br><span class="line">while acc &gt;= dt:</span><br><span class="line">  step one tick</span><br><span class="line">  acc -= dt</span><br></pre></td></tr></table></figure>

<p>项目里：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">dt = <span class="number">1.0</span> / <span class="number">60.0</span></span><br><span class="line">maxFrame = <span class="number">0.25</span></span><br></pre></td></tr></table></figure>

<p>为什么不用真实 frameTime 直接推进？</p>
<p>因为真实时间不稳定：</p>
<ul>
<li>系统调度会抖动</li>
<li>渲染耗时会变化</li>
<li>网络回调时间不可控</li>
</ul>
<p>固定 dt 的好处是：</p>
<ul>
<li>模拟稳定</li>
<li>回滚可重放</li>
<li>hash 更容易一致</li>
<li>测试更容易复现</li>
</ul>
<p>AI 工程迁移理解：</p>
<p>在 Agent、多轮任务执行、流式处理、训练调度里，也应该尽量区分：</p>
<ul>
<li>真实时间</li>
<li>逻辑步数</li>
<li>状态版本</li>
</ul>
<p>系统内部最好基于逻辑 step 推进，而不是让 wall clock 到处影响业务逻辑。</p>
<hr>
<h2 id="11-回滚同步完整链路"><a href="#11-回滚同步完整链路" class="headerlink" title="11. 回滚同步完整链路"></a>11. 回滚同步完整链路</h2><p>完整链路可以这样理解。</p>
<h3 id="11-1-正常预测阶段"><a href="#11-1-正常预测阶段" class="headerlink" title="11.1 正常预测阶段"></a>11.1 正常预测阶段</h3><p>每个客户端每 tick 做：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">采样本地输入</span><br><span class="line">保存 localHist[tick]</span><br><span class="line">预测远端输入</span><br><span class="line">World::Step(allCmds, dt)</span><br><span class="line">保存 stateHist[tick]</span><br><span class="line">发送最近 K 帧输入给服务端</span><br><span class="line">tick++</span><br></pre></td></tr></table></figure>

<h3 id="11-2-服务端权威阶段"><a href="#11-2-服务端权威阶段" class="headerlink" title="11.2 服务端权威阶段"></a>11.2 服务端权威阶段</h3><p>服务端每 tick 做：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">读取每个玩家输入</span><br><span class="line">缺输入则 hold/default</span><br><span class="line">World::Step(allCmds, dt)</span><br><span class="line">生成权威 snapshot</span><br><span class="line">计算 hash</span><br><span class="line">发送 Ack</span><br><span class="line">按频率发送 State</span><br><span class="line">tick++</span><br></pre></td></tr></table></figure>

<h3 id="11-3-客户端校正阶段"><a href="#11-3-客户端校正阶段" class="headerlink" title="11.3 客户端校正阶段"></a>11.3 客户端校正阶段</h3><p>客户端收到权威 State：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">解码 State</span><br><span class="line">还原 auth snapshot</span><br><span class="line">校验 stateHash</span><br><span class="line">比较本地玩家误差</span><br><span class="line">写入权威快照</span><br><span class="line">Restore(auth)</span><br><span class="line">Replay auth.tick + 1 到当前 tick</span><br></pre></td></tr></table></figure>

<p>这个流程的本质是：</p>
<blockquote>
<p>客户端永远允许自己先猜，但最终必须回到服务端认可的历史上。</p>
</blockquote>
<hr>
<h2 id="12-测试设计"><a href="#12-测试设计" class="headerlink" title="12. 测试设计"></a>12. 测试设计</h2><p>项目测试分两类。</p>
<h3 id="12-1-core-tests"><a href="#12-1-core-tests" class="headerlink" title="12.1 core_tests"></a>12.1 core_tests</h3><p>文件：</p>
<p><code>tests/core_tests.cpp</code></p>
<p>主要验证：</p>
<ul>
<li><code>InputBuffer</code> 零容量时会被修正为 1</li>
<li><code>StateHistory</code> 零容量时会被修正为 1</li>
<li><code>InputPacket</code> 编解码正确</li>
<li><code>StatePacket</code> 编解码正确</li>
<li><code>Hasher</code> 使用毫米精度</li>
<li><code>Hasher</code> 覆盖 shotCooldown</li>
<li><code>World(0)</code> 会至少创建 1 个玩家</li>
</ul>
<p>这些是小而关键的回归测试。</p>
<h3 id="12-2-stress-tests"><a href="#12-2-stress-tests" class="headerlink" title="12.2 stress_tests"></a>12.2 stress_tests</h3><p>文件：</p>
<p><code>tests/stress_tests.cpp</code></p>
<p>压力测试更重要。</p>
<p>它在单进程里模拟：</p>
<ul>
<li>权威世界</li>
<li>客户端预测世界</li>
<li>输入历史</li>
<li>网络包编解码</li>
<li>State 延迟</li>
<li>State 抖动</li>
<li>回滚重放</li>
<li>hash 校验</li>
</ul>
<p>测试重点不是窗口和真实 socket，而是核心逻辑链路。</p>
<p>它验证：</p>
<ul>
<li>从原始权威快照恢复并重放，能追上权威历史</li>
<li>从网络量化后的 State 恢复并重放，hash 仍然一致</li>
<li>输入冗余包可以正常编解码</li>
<li>长时间 tick 推进不会破坏状态一致性</li>
</ul>
<p>这类测试对 AI 工程也有参考意义：</p>
<blockquote>
<p>真正可靠的系统，不只测单个函数，还要测“数据经过网络、延迟、恢复、重放之后是否仍然正确”。</p>
</blockquote>
<hr>
<h2 id="13-这个项目值得学习的重点知识点"><a href="#13-这个项目值得学习的重点知识点" class="headerlink" title="13. 这个项目值得学习的重点知识点"></a>13. 这个项目值得学习的重点知识点</h2><h3 id="13-1-分层设计"><a href="#13-1-分层设计" class="headerlink" title="13.1 分层设计"></a>13.1 分层设计</h3><p>项目没有把所有逻辑写在一个 main 里，而是拆成：</p>
<ul>
<li>sim</li>
<li>net</li>
<li>app</li>
<li>tests</li>
</ul>
<p>这种拆分让核心模拟可以脱离网络和渲染测试。</p>
<p>AI 工程对应：</p>
<ul>
<li>模型调用逻辑不要和 HTTP handler 混在一起</li>
<li>数据处理逻辑不要和 UI 混在一起</li>
<li>推理服务核心最好能被单元测试和压测直接调用</li>
</ul>
<h3 id="13-2-状态快照"><a href="#13-2-状态快照" class="headerlink" title="13.2 状态快照"></a>13.2 状态快照</h3><p><code>WorldSnapshot</code> 是这个项目的核心中间结构。</p>
<p>它让系统可以：</p>
<ul>
<li>保存状态</li>
<li>传输状态</li>
<li>恢复状态</li>
<li>比较状态</li>
<li>hash 状态</li>
</ul>
<p>AI 工程对应：</p>
<ul>
<li>Agent 运行状态</li>
<li>RAG 查询状态</li>
<li>会话上下文快照</li>
<li>任务执行 checkpoint</li>
<li>分布式训练 checkpoint</li>
</ul>
<h3 id="13-3-输入日志"><a href="#13-3-输入日志" class="headerlink" title="13.3 输入日志"></a>13.3 输入日志</h3><p>项目不是只保存当前输入，而是保存输入历史。</p>
<p>这让系统可以从任意历史点重新计算。</p>
<p>AI 工程对应：</p>
<ul>
<li>prompt history</li>
<li>tool call history</li>
<li>message queue event log</li>
<li>workflow step log</li>
<li>训练样本处理日志</li>
</ul>
<h3 id="13-4-确定性推进"><a href="#13-4-确定性推进" class="headerlink" title="13.4 确定性推进"></a>13.4 确定性推进</h3><p>同样状态 + 同样输入 + 同样 dt，应该得到同样结果。</p>
<p>这对回滚极其重要。</p>
<p>AI 工程里虽然模型推理本身可能有随机性，但工程层也可以尽量确定：</p>
<ul>
<li>固定 prompt 模板版本</li>
<li>固定模型版本</li>
<li>固定采样参数</li>
<li>固定工具调用输入</li>
<li>固定数据预处理逻辑</li>
</ul>
<p>否则线上问题很难复现。</p>
<h3 id="13-5-网络协议版本化"><a href="#13-5-网络协议版本化" class="headerlink" title="13.5 网络协议版本化"></a>13.5 网络协议版本化</h3><p>项目协议有：</p>
<ul>
<li>magic</li>
<li>version</li>
<li>packet type</li>
</ul>
<p>这能避免错误解码。</p>
<p>AI 服务接口也应该重视：</p>
<ul>
<li>API version</li>
<li>request schema version</li>
<li>model version</li>
<li>embedding version</li>
<li>prompt version</li>
</ul>
<h3 id="13-6-冗余和幂等"><a href="#13-6-冗余和幂等" class="headerlink" title="13.6 冗余和幂等"></a>13.6 冗余和幂等</h3><p>Input 包重复携带最近 K 帧输入。</p>
<p>这是一种用冗余换可靠性的设计。</p>
<p>AI 工程对应：</p>
<ul>
<li>任务重试要有 task id</li>
<li>消息重复消费要能去重</li>
<li>请求超时后重发不能重复扣费或重复写库</li>
<li>流式输出恢复要能从 offset 继续</li>
</ul>
<h3 id="13-7-Hash-对账"><a href="#13-7-Hash-对账" class="headerlink" title="13.7 Hash 对账"></a>13.7 Hash 对账</h3><p>项目用 hash 判断状态是否一致。</p>
<p>AI 工程也可以用类似思想：</p>
<ul>
<li>对输入文档算 hash</li>
<li>对 chunk 结果算 hash</li>
<li>对 embedding 输入算 hash</li>
<li>对模型配置算 hash</li>
<li>对 prompt 模板算 hash</li>
<li>对 workflow 状态算 hash</li>
</ul>
<p>这能帮助定位：</p>
<ul>
<li>是输入变了</li>
<li>是模型变了</li>
<li>是参数变了</li>
<li>是代码逻辑变了</li>
<li>是缓存污染了</li>
</ul>
<h3 id="13-8-压力测试"><a href="#13-8-压力测试" class="headerlink" title="13.8 压力测试"></a>13.8 压力测试</h3><p><code>lab_stress</code> 很值得参考。</p>
<p>它没有依赖窗口和真实 socket，而是直接压核心逻辑。</p>
<p>AI 工程里也可以这样做：</p>
<ul>
<li>不一定先压真实 API</li>
<li>可以先压核心 pipeline</li>
<li>模拟延迟、乱序、失败和重试</li>
<li>验证恢复后结果是否一致</li>
</ul>
<hr>
<h2 id="14-和-AI-模型开发的关联"><a href="#14-和-AI-模型开发的关联" class="headerlink" title="14. 和 AI 模型开发的关联"></a>14. 和 AI 模型开发的关联</h2><p>这个项目虽然不是 AI 项目，但可以帮助理解 AI 工程里的几个难点。</p>
<h3 id="14-1-和推理服务的关系"><a href="#14-1-和推理服务的关系" class="headerlink" title="14.1 和推理服务的关系"></a>14.1 和推理服务的关系</h3><p>推理服务也有类似问题：</p>
<ul>
<li>请求并发</li>
<li>网络延迟</li>
<li>状态追踪</li>
<li>版本一致性</li>
<li>超时重试</li>
<li>结果校验</li>
</ul>
<p>可以借鉴：</p>
<ul>
<li>每个请求带 request id</li>
<li>每个模型有 model version</li>
<li>每次调用记录输入 hash</li>
<li>缓存 key 包含模型和参数版本</li>
<li>异步任务保存 checkpoint</li>
</ul>
<h3 id="14-2-和-Agent-系统的关系"><a href="#14-2-和-Agent-系统的关系" class="headerlink" title="14.2 和 Agent 系统的关系"></a>14.2 和 Agent 系统的关系</h3><p>Agent 执行多步任务时，很像 tick 推进。</p>
<p>每一步可以看成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">step_id</span><br><span class="line">observation</span><br><span class="line">decision</span><br><span class="line">tool_call</span><br><span class="line">tool_result</span><br><span class="line">state_update</span><br></pre></td></tr></table></figure>

<p>如果中途失败，就需要：</p>
<ul>
<li>从某个 step 恢复</li>
<li>重放历史</li>
<li>判断状态是否一致</li>
<li>避免重复执行副作用工具</li>
</ul>
<p>这和回滚同步的思路很接近。</p>
<h3 id="14-3-和-RAG-系统的关系"><a href="#14-3-和-RAG-系统的关系" class="headerlink" title="14.3 和 RAG 系统的关系"></a>14.3 和 RAG 系统的关系</h3><p>RAG 里也需要状态和版本管理。</p>
<p>例如：</p>
<ul>
<li>文档版本</li>
<li>chunk 版本</li>
<li>embedding 模型版本</li>
<li>向量库索引版本</li>
<li>查询改写版本</li>
<li>reranker 版本</li>
</ul>
<p>如果没有这些版本，线上效果变化时很难排查。</p>
<p>可以参考这个项目的 hash 思想，对关键中间结果做记录。</p>
<h3 id="14-4-和分布式训练的关系"><a href="#14-4-和分布式训练的关系" class="headerlink" title="14.4 和分布式训练的关系"></a>14.4 和分布式训练的关系</h3><p>分布式训练也重视：</p>
<ul>
<li>step</li>
<li>checkpoint</li>
<li>deterministic replay</li>
<li>状态恢复</li>
<li>参数同步</li>
<li>异常恢复</li>
</ul>
<p><code>WorldSnapshot</code> 类似训练 checkpoint。</p>
<p><code>InputCmd</code> 类似每一步的 batch &#x2F; event。</p>
<p><code>StateHistory</code> 类似最近 checkpoint 历史。</p>
<p><code>Hasher</code> 类似一致性校验。</p>
<hr>
<h2 id="15-如果继续完善这个项目"><a href="#15-如果继续完善这个项目" class="headerlink" title="15. 如果继续完善这个项目"></a>15. 如果继续完善这个项目</h2><p>这个项目已经把核心链路跑通了，但如果要继续做，可以考虑这些方向。</p>
<h3 id="15-1-网络层增强"><a href="#15-1-网络层增强" class="headerlink" title="15.1 网络层增强"></a>15.1 网络层增强</h3><p>可以增加：</p>
<ul>
<li>延迟统计</li>
<li>丢包率统计</li>
<li>RTT 估计</li>
<li>抖动缓冲</li>
<li>包乱序处理</li>
<li>客户端 ping &#x2F; pong</li>
<li>输入确认窗口</li>
</ul>
<h3 id="15-2-协议层增强"><a href="#15-2-协议层增强" class="headerlink" title="15.2 协议层增强"></a>15.2 协议层增强</h3><p>可以增加：</p>
<ul>
<li>协议 schema 文档</li>
<li>包大小统计</li>
<li>包字段版本兼容</li>
<li>State 增量同步</li>
<li>压缩</li>
<li>加密或签名</li>
</ul>
<h3 id="15-3-回滚体验增强"><a href="#15-3-回滚体验增强" class="headerlink" title="15.3 回滚体验增强"></a>15.3 回滚体验增强</h3><p>可以增加：</p>
<ul>
<li>插值平滑</li>
<li>远端玩家视觉修正</li>
<li>回滚次数可视化</li>
<li>回滚帧数统计</li>
<li>输入延迟动态调整</li>
</ul>
<h3 id="15-4-测试增强"><a href="#15-4-测试增强" class="headerlink" title="15.4 测试增强"></a>15.4 测试增强</h3><p>可以增加：</p>
<ul>
<li>真实 UDP bot 压测</li>
<li>随机丢包模拟</li>
<li>随机乱序模拟</li>
<li>多平台 hash 对比</li>
<li>fuzz 解码测试</li>
<li>长时间 soak test</li>
</ul>
<h3 id="15-5-工程结构增强"><a href="#15-5-工程结构增强" class="headerlink" title="15.5 工程结构增强"></a>15.5 工程结构增强</h3><p>可以增加：</p>
<ul>
<li>更明确的协议文档</li>
<li>更统一的配置来源</li>
<li>CI 自动测试</li>
<li>clang-format</li>
<li>sanitizers</li>
<li>性能 profiling</li>
</ul>
<hr>
<h2 id="16-学习路线建议"><a href="#16-学习路线建议" class="headerlink" title="16. 学习路线建议"></a>16. 学习路线建议</h2><p>如果你要用这个项目作为学习材料，可以按这个顺序看。</p>
<h3 id="16-1-第一遍：先看整体"><a href="#16-1-第一遍：先看整体" class="headerlink" title="16.1 第一遍：先看整体"></a>16.1 第一遍：先看整体</h3><p>先读：</p>
<ul>
<li><code>README.md</code></li>
<li><code>docs/ARCHITECTURE.md</code></li>
<li><code>CMakeLists.txt</code></li>
</ul>
<p>目标是知道：</p>
<ul>
<li>项目怎么构建</li>
<li>有哪些模块</li>
<li>服务端和客户端怎么交互</li>
</ul>
<h3 id="16-2-第二遍：看数据结构"><a href="#16-2-第二遍：看数据结构" class="headerlink" title="16.2 第二遍：看数据结构"></a>16.2 第二遍：看数据结构</h3><p>重点看：</p>
<ul>
<li><code>InputCmd.h</code></li>
<li><code>StateSnapshot.h</code></li>
<li><code>InputBuffer.h</code></li>
<li><code>StateHistory.h</code></li>
<li><code>Packets.h</code></li>
</ul>
<p>目标是理解：</p>
<ul>
<li>输入怎么表示</li>
<li>状态怎么表示</li>
<li>网络包怎么表示</li>
<li>历史怎么保存</li>
</ul>
<h3 id="16-3-第三遍：看核心模拟"><a href="#16-3-第三遍：看核心模拟" class="headerlink" title="16.3 第三遍：看核心模拟"></a>16.3 第三遍：看核心模拟</h3><p>重点看：</p>
<ul>
<li><code>World.h</code></li>
<li><code>World.cpp</code></li>
<li><code>Rules.h</code></li>
<li><code>Hasher.cpp</code></li>
</ul>
<p>目标是理解：</p>
<ul>
<li>一帧怎么推进</li>
<li>哪些字段影响确定性</li>
<li>为什么 hash 要覆盖这些字段</li>
</ul>
<h3 id="16-4-第四遍：看服务端"><a href="#16-4-第四遍：看服务端" class="headerlink" title="16.4 第四遍：看服务端"></a>16.4 第四遍：看服务端</h3><p>重点看：</p>
<ul>
<li><code>server_main.cpp</code></li>
</ul>
<p>目标是理解：</p>
<ul>
<li>玩家怎么分配</li>
<li>输入怎么接收</li>
<li>缺输入怎么处理</li>
<li>权威状态怎么广播</li>
</ul>
<h3 id="16-5-第五遍：看客户端"><a href="#16-5-第五遍：看客户端" class="headerlink" title="16.5 第五遍：看客户端"></a>16.5 第五遍：看客户端</h3><p>重点看：</p>
<ul>
<li><code>client_main.cpp</code></li>
<li><code>InputPrediction.cpp</code></li>
</ul>
<p>目标是理解：</p>
<ul>
<li>本地预测怎么做</li>
<li>远端输入怎么猜</li>
<li>收到权威 State 后怎么回滚</li>
<li>hash mismatch 怎么统计</li>
</ul>
<h3 id="16-6-第六遍：看测试"><a href="#16-6-第六遍：看测试" class="headerlink" title="16.6 第六遍：看测试"></a>16.6 第六遍：看测试</h3><p>重点看：</p>
<ul>
<li><code>core_tests.cpp</code></li>
<li><code>stress_tests.cpp</code></li>
</ul>
<p>目标是理解：</p>
<ul>
<li>这个项目如何验证状态一致性</li>
<li>压测为什么不依赖真实窗口和 socket</li>
<li>如何模拟延迟和重放</li>
</ul>
<hr>
<h2 id="17-重点代码阅读清单"><a href="#17-重点代码阅读清单" class="headerlink" title="17. 重点代码阅读清单"></a>17. 重点代码阅读清单</h2><p>建议重点阅读这些文件：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">include/lab/sim/InputCmd.h</span><br><span class="line">include/lab/sim/StateSnapshot.h</span><br><span class="line">include/lab/sim/InputBuffer.h</span><br><span class="line">include/lab/sim/StateHistory.h</span><br><span class="line">include/lab/net/Packets.h</span><br><span class="line">src/sim/World.cpp</span><br><span class="line">src/sim/Hasher.cpp</span><br><span class="line">src/net/NetCode.cpp</span><br><span class="line">apps/server_main.cpp</span><br><span class="line">apps/client_main.cpp</span><br><span class="line">tests/stress_tests.cpp</span><br></pre></td></tr></table></figure>

<p>阅读时重点问自己几个问题：</p>
<ul>
<li>每个 tick 的输入从哪里来？</li>
<li>服务端和客户端的 tick 如何对齐？</li>
<li>客户端什么时候预测？</li>
<li>权威状态什么时候覆盖预测状态？</li>
<li>什么字段进入 Snapshot？</li>
<li>什么字段进入网络包？</li>
<li>什么字段进入 hash？</li>
<li>如果新增一个状态字段，需要改哪些地方？</li>
<li>如果 UDP 丢包，会发生什么？</li>
<li>如果 State 延迟到达，客户端如何追上当前 tick？</li>
</ul>
<hr>
<h2 id="18-常见易错点"><a href="#18-常见易错点" class="headerlink" title="18. 常见易错点"></a>18. 常见易错点</h2><h3 id="18-1-新增字段只改了-World，没有改-Snapshot"><a href="#18-1-新增字段只改了-World，没有改-Snapshot" class="headerlink" title="18.1 新增字段只改了 World，没有改 Snapshot"></a>18.1 新增字段只改了 World，没有改 Snapshot</h3><p>如果新增字段会影响模拟，但没有进入 Snapshot，回滚恢复后就会丢状态。</p>
<h3 id="18-2-新增字段只改了-Snapshot，没有改网络包"><a href="#18-2-新增字段只改了-Snapshot，没有改网络包" class="headerlink" title="18.2 新增字段只改了 Snapshot，没有改网络包"></a>18.2 新增字段只改了 Snapshot，没有改网络包</h3><p>服务端有这个状态，但客户端收不到，预测和权威会分叉。</p>
<h3 id="18-3-新增字段没有进入-hash"><a href="#18-3-新增字段没有进入-hash" class="headerlink" title="18.3 新增字段没有进入 hash"></a>18.3 新增字段没有进入 hash</h3><p>状态已经不同，但 hash 检查不出来。</p>
<h3 id="18-4-直接-hash-float-内存"><a href="#18-4-直接-hash-float-内存" class="headerlink" title="18.4 直接 hash float 内存"></a>18.4 直接 hash float 内存</h3><p>不同平台或编解码后可能有微小浮点差异，导致假 mismatch。</p>
<p>项目用毫米量化规避了这个问题。</p>
<h3 id="18-5-环形缓冲读取时不校验-tick"><a href="#18-5-环形缓冲读取时不校验-tick" class="headerlink" title="18.5 环形缓冲读取时不校验 tick"></a>18.5 环形缓冲读取时不校验 tick</h3><p>只用 <code>tick % cap</code> 会读到被覆盖的旧数据。</p>
<p>项目里读取时会校验真实 tick，这是正确做法。</p>
<h3 id="18-6-把远端预测误差也算作-rollback-条件"><a href="#18-6-把远端预测误差也算作-rollback-条件" class="headerlink" title="18.6 把远端预测误差也算作 rollback 条件"></a>18.6 把远端预测误差也算作 rollback 条件</h3><p>远端玩家本来就是猜的。</p>
<p>如果把远端差异也作为回滚计数依据，会导致 rollback 统计爆炸。</p>
<p>项目只比较本地玩家，比较合理。</p>
<hr>
<h2 id="19-可以写成博客的版本"><a href="#19-可以写成博客的版本" class="headerlink" title="19. 可以写成博客的版本"></a>19. 可以写成博客的版本</h2><h3 id="标题"><a href="#标题" class="headerlink" title="标题"></a>标题</h3><p>从一个 C++ 格斗游戏 Demo 学实时系统：固定帧、预测、回滚与状态一致性</p>
<h3 id="开头"><a href="#开头" class="headerlink" title="开头"></a>开头</h3><p>很多人以为游戏网络同步只是“把坐标发给别人”。</p>
<p>但动作游戏真正困难的地方在于：</p>
<ul>
<li>玩家按键必须立刻有反馈</li>
<li>网络包可能延迟或丢失</li>
<li>服务端必须保持权威</li>
<li>客户端预测错了还要能修回来</li>
</ul>
<p>这个项目用一个小型 60Hz 顶视角对战 Demo，把这些问题完整串了起来。</p>
<h3 id="第一部分：为什么需要固定帧"><a href="#第一部分：为什么需要固定帧" class="headerlink" title="第一部分：为什么需要固定帧"></a>第一部分：为什么需要固定帧</h3><p>实时系统最怕逻辑跟真实时间强绑定。</p>
<p>如果每一帧都直接用真实耗时推进，状态会受到系统调度、渲染耗时和网络回调影响。</p>
<p>所以项目采用固定 tick：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">1 秒 60 帧</span><br><span class="line">每帧 dt = 1 / 60</span><br><span class="line">所有输入和状态都绑定 tick</span><br></pre></td></tr></table></figure>

<p>这让回放、回滚和调试都变得可控。</p>
<h3 id="第二部分：客户端为什么要预测"><a href="#第二部分：客户端为什么要预测" class="headerlink" title="第二部分：客户端为什么要预测"></a>第二部分：客户端为什么要预测</h3><p>如果玩家按键后必须等服务端确认，游戏会非常卡。</p>
<p>所以客户端先假设自己的输入有效，立即推进本地世界。</p>
<p>这样玩家看到的是即时反馈。</p>
<p>但客户端不是最终权威。</p>
<p>服务端稍后会下发真实状态，客户端再根据权威状态修正。</p>
<h3 id="第三部分：回滚的本质"><a href="#第三部分：回滚的本质" class="headerlink" title="第三部分：回滚的本质"></a>第三部分：回滚的本质</h3><p>回滚不是简单地把当前位置拉回服务端位置。</p>
<p>真正的流程是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">恢复到服务端给的历史快照</span><br><span class="line">重新执行这之后的输入</span><br><span class="line">追上当前本地 tick</span><br></pre></td></tr></table></figure>

<p>这要求系统必须保存：</p>
<ul>
<li>历史输入</li>
<li>历史状态</li>
<li>可恢复的快照</li>
<li>确定性的 Step 函数</li>
</ul>
<h3 id="第四部分：为什么-hash-很重要"><a href="#第四部分：为什么-hash-很重要" class="headerlink" title="第四部分：为什么 hash 很重要"></a>第四部分：为什么 hash 很重要</h3><p>服务端和客户端状态一旦分叉，肉眼很难定位原因。</p>
<p>项目用 <code>Hasher</code> 对关键状态做 hash。</p>
<p>只要 hash 不一致，就说明某个状态字段不同。</p>
<p>更重要的是，hash 不是直接混合 float，而是和网络包一样做毫米量化。</p>
<p>这样可以避免浮点误差造成误报。</p>
<h3 id="第五部分：这个项目对-AI-工程的启发"><a href="#第五部分：这个项目对-AI-工程的启发" class="headerlink" title="第五部分：这个项目对 AI 工程的启发"></a>第五部分：这个项目对 AI 工程的启发</h3><p>虽然这是游戏项目，但它的工程思想非常适合迁移到 AI 系统。</p>
<p>AI Agent 也有 step。</p>
<p>RAG 也有中间状态。</p>
<p>推理服务也有请求版本、模型版本和缓存一致性。</p>
<p>分布式训练也有 checkpoint 和恢复。</p>
<p>如果一个 AI 系统需要稳定运行，就不能只关心模型输出，还要关心：</p>
<ul>
<li>输入是否可追踪</li>
<li>中间状态是否可恢复</li>
<li>版本是否明确</li>
<li>错误是否可复现</li>
<li>结果是否可校验</li>
</ul>
<h3 id="结尾"><a href="#结尾" class="headerlink" title="结尾"></a>结尾</h3><p>这个 Fighting 项目真正值得学习的，不只是 Rollback Netcode，而是它展示了一套实时系统的基本工程方法：</p>
<ul>
<li>用 tick 管理时间</li>
<li>用输入日志支撑回放</li>
<li>用快照支撑恢复</li>
<li>用 hash 支撑一致性检查</li>
<li>用压力测试验证长链路正确性</li>
</ul>
<p>这些能力放到 AI 模型开发中，同样非常重要。</p>
<hr>
<h2 id="20-可扩展博客选题"><a href="#20-可扩展博客选题" class="headerlink" title="20. 可扩展博客选题"></a>20. 可扩展博客选题</h2><p>后续可以基于这份笔记拆成几篇博客。</p>
<h3 id="20-1-选题一：Rollback-Netcode-入门"><a href="#20-1-选题一：Rollback-Netcode-入门" class="headerlink" title="20.1 选题一：Rollback Netcode 入门"></a>20.1 选题一：Rollback Netcode 入门</h3><p>重点讲：</p>
<ul>
<li>为什么动作游戏不能只靠服务端确认</li>
<li>本地预测是什么</li>
<li>回滚重放是什么</li>
<li>输入历史和状态快照怎么设计</li>
</ul>
<h3 id="20-2-选题二：用-C-实现一个确定性模拟核心"><a href="#20-2-选题二：用-C-实现一个确定性模拟核心" class="headerlink" title="20.2 选题二：用 C++ 实现一个确定性模拟核心"></a>20.2 选题二：用 C++ 实现一个确定性模拟核心</h3><p>重点讲：</p>
<ul>
<li><code>World::Step</code></li>
<li>固定 dt</li>
<li>状态字段管理</li>
<li>碰撞和子弹逻辑</li>
<li>如何避免不确定性</li>
</ul>
<h3 id="20-3-选题三：UDP-协议如何对抗丢包"><a href="#20-3-选题三：UDP-协议如何对抗丢包" class="headerlink" title="20.3 选题三：UDP 协议如何对抗丢包"></a>20.3 选题三：UDP 协议如何对抗丢包</h3><p>重点讲：</p>
<ul>
<li>为什么用 UDP</li>
<li>Input 包为什么带冗余</li>
<li>服务端如何 hold last input</li>
<li>Ack 和 State 分别解决什么问题</li>
</ul>
<h3 id="20-4-选题四：状态-hash-如何定位分布式系统分叉"><a href="#20-4-选题四：状态-hash-如何定位分布式系统分叉" class="headerlink" title="20.4 选题四：状态 hash 如何定位分布式系统分叉"></a>20.4 选题四：状态 hash 如何定位分布式系统分叉</h3><p>重点讲：</p>
<ul>
<li>为什么需要 hash</li>
<li>为什么不能直接 hash float</li>
<li>哪些字段必须进入 hash</li>
<li>hash mismatch 如何排查</li>
</ul>
<h3 id="20-5-选题五：从游戏同步看-AI-Agent-状态恢复"><a href="#20-5-选题五：从游戏同步看-AI-Agent-状态恢复" class="headerlink" title="20.5 选题五：从游戏同步看 AI Agent 状态恢复"></a>20.5 选题五：从游戏同步看 AI Agent 状态恢复</h3><p>重点讲：</p>
<ul>
<li>tick 和 Agent step 的类比</li>
<li>InputCmd 和 tool call 的类比</li>
<li>Snapshot 和 checkpoint 的类比</li>
<li>Replay 和失败恢复的类比</li>
</ul>
<hr>
<h2 id="21-总结"><a href="#21-总结" class="headerlink" title="21. 总结"></a>21. 总结</h2><p>这个项目可以用一句话总结：</p>
<blockquote>
<p>用 C++ 实现了一个小型实时联机同步系统，通过固定帧、输入冗余、本地预测、服务端权威、回滚重放和 hash 对账来解决网络延迟下的状态一致性问题。</p>
</blockquote>
<p>最应该记住的重点：</p>
<ul>
<li><code>Tick</code> 是逻辑时间基础</li>
<li><code>InputCmd</code> 是最小输入事件</li>
<li><code>WorldSnapshot</code> 是恢复和同步的核心</li>
<li><code>World::Step</code> 必须尽量确定性</li>
<li><code>InputBuffer</code> 和 <code>StateHistory</code> 用环形缓冲保存历史</li>
<li>客户端预测是为了体验，服务端权威是为了正确</li>
<li>回滚不是瞬移，而是恢复历史状态后重放输入</li>
<li>hash 对账要和网络量化精度一致</li>
<li>压力测试应该覆盖完整链路，而不是只测单个函数</li>
</ul>
<p>放到 AI 模型开发学习里，这个项目最有价值的地方是：</p>
<blockquote>
<p>它训练的是工程化思维：状态、版本、恢复、重放、对账、压测。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>项目分析</category>
      </categories>
  </entry>
  <entry>
    <title>动态规划</title>
    <url>//notes/%E4%BB%A3%E7%A0%81%E7%BB%83%E4%B9%A0/%E5%8A%9B%E6%89%A3%E6%89%93%E5%8D%A1%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h1 id="动态规划"><a href="#动态规划" class="headerlink" title="动态规划"></a>动态规划</h1><h2 id="入门"><a href="#入门" class="headerlink" title="入门"></a>入门</h2><ol start="70">
<li>爬楼梯</li>
<li>使用最小花费爬楼梯</li>
<li>爬楼梯 II<br>需要注意数组初始化问题</li>
<li>组合总和 Ⅳ<br>翻译成爬楼梯的问题<br>这个题可以在想想扩展一下</li>
<li>统计构造好字符串的方案数<br>在dp数组不确定初始化的情况下<br>采用dfs的方法通过规定边界实现初始化简化</li>
<li>统计打字方案数【1】<br>好好在思考一下</li>
</ol>
<h2 id="打家劫舍"><a href="#打家劫舍" class="headerlink" title="打家劫舍"></a>打家劫舍</h2><ol start="198">
<li>打家劫舍<br>lambda 在递归中应用需要补一下</li>
<li>打家劫舍 II</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>代码练习</category>
      </categories>
  </entry>
  <entry>
    <title>C++11 起的多线程编程笔记</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/08-c-11%E5%A4%9A%E7%BA%BF%E7%A8%8B%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="C-11-起的多线程编程笔记"><a href="#C-11-起的多线程编程笔记" class="headerlink" title="C++11 起的多线程编程笔记"></a>C++11 起的多线程编程笔记</h1><blockquote>
<p>C++11 标准库并发组件：<code>&lt;thread&gt; / &lt;mutex&gt; / &lt;condition_variable&gt; / &lt;future&gt; / &lt;atomic&gt; / &lt;chrono&gt;</code><br>目标：并发执行（性能） + 正确同步（安全） + 生命周期可控（可维护）。</p>
</blockquote>
<h2 id="1-时间与计时-std-chrono"><a href="#1-时间与计时-std-chrono" class="headerlink" title="1. 时间与计时 std::chrono"></a>1. 时间与计时 <code>std::chrono</code></h2><h3 id="1-1-三个核心类型"><a href="#1-1-三个核心类型" class="headerlink" title="1.1 三个核心类型"></a>1.1 三个核心类型</h3><ul>
<li><code>clock</code>：时钟（如 <code>steady_clock</code>、<code>system_clock</code>）</li>
<li><code>time_point</code>：时间点（<code>clock::time_point</code>）</li>
<li><code>duration</code>：时长（<code>seconds/milliseconds/...</code>）</li>
</ul>
<h3 id="1-2-计时：推荐-steady-clock（不会被系统调时影响）"><a href="#1-2-计时：推荐-steady-clock（不会被系统调时影响）" class="headerlink" title="1.2 计时：推荐 steady_clock（不会被系统调时影响）"></a>1.2 计时：推荐 <code>steady_clock</code>（不会被系统调时影响）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;chrono&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cstdint&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> t0 = std::chrono::steady_clock::<span class="built_in">now</span>();</span><br><span class="line">    <span class="keyword">auto</span> t1 = t0 + std::chrono::<span class="built_in">seconds</span>(<span class="number">30</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> dt = t1 - t0; <span class="comment">// duration</span></span><br><span class="line">    std::<span class="type">int64_t</span> sec = std::chrono::<span class="built_in">duration_cast</span>&lt;std::chrono::seconds&gt;(dt).<span class="built_in">count</span>();</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;dt = &quot;</span> &lt;&lt; sec &lt;&lt; <span class="string">&quot; s\n&quot;</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-3-常见时间单位"><a href="#1-3-常见时间单位" class="headerlink" title="1.3 常见时间单位"></a>1.3 常见时间单位</h3><ul>
<li><code>std::chrono::seconds(x)</code></li>
<li><code>std::chrono::milliseconds(x)</code></li>
<li><code>std::chrono::microseconds(x)</code></li>
<li><code>std::chrono::nanoseconds(x)</code></li>
</ul>
<h3 id="1-4-测耗时并转-double-毫秒"><a href="#1-4-测耗时并转-double-毫秒" class="headerlink" title="1.4 测耗时并转 double 毫秒"></a>1.4 测耗时并转 double 毫秒</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;chrono&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> t0 = std::chrono::steady_clock::<span class="built_in">now</span>();</span><br><span class="line"></span><br><span class="line">    std::<span class="type">uint64_t</span> acc = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">1&#x27;000&#x27;000</span>; ++i) acc += i;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> t1 = std::chrono::steady_clock::<span class="built_in">now</span>();</span><br><span class="line">    <span class="keyword">using</span> double_ms = std::chrono::duration&lt;<span class="type">double</span>, std::milli&gt;;</span><br><span class="line">    <span class="type">double</span> ms = std::chrono::<span class="built_in">duration_cast</span>&lt;double_ms&gt;(t1 - t0).<span class="built_in">count</span>();</span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;cost = &quot;</span> &lt;&lt; ms &lt;&lt; <span class="string">&quot; ms, acc=&quot;</span> &lt;&lt; acc &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2-线程休眠：避免忙等"><a href="#2-线程休眠：避免忙等" class="headerlink" title="2. 线程休眠：避免忙等"></a>2. 线程休眠：避免忙等</h2><h3 id="2-1-sleep-for"><a href="#2-1-sleep-for" class="headerlink" title="2.1 sleep_for"></a>2.1 <code>sleep_for</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;chrono&gt;</span></span></span><br><span class="line"></span><br><span class="line">std::this_thread::<span class="built_in">sleep_for</span>(std::chrono::<span class="built_in">milliseconds</span>(<span class="number">400</span>));</span><br></pre></td></tr></table></figure>

<h3 id="2-2-sleep-until（对齐节拍更合适）"><a href="#2-2-sleep-until（对齐节拍更合适）" class="headerlink" title="2.2 sleep_until（对齐节拍更合适）"></a>2.2 <code>sleep_until</code>（对齐节拍更合适）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;chrono&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> t = std::chrono::steady_clock::<span class="built_in">now</span>() + std::chrono::<span class="built_in">milliseconds</span>(<span class="number">400</span>);</span><br><span class="line">std::this_thread::<span class="built_in">sleep_until</span>(t);</span><br></pre></td></tr></table></figure>

<h3 id="2-3-实例：固定频率循环（推荐写法）"><a href="#2-3-实例：固定频率循环（推荐写法）" class="headerlink" title="2.3 实例：固定频率循环（推荐写法）"></a>2.3 实例：固定频率循环（推荐写法）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;chrono&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">using</span> clock = std::chrono::steady_clock;</span><br><span class="line">    <span class="keyword">auto</span> next = clock::<span class="built_in">now</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; ++i) &#123;</span><br><span class="line">        next += std::chrono::<span class="built_in">milliseconds</span>(<span class="number">100</span>);</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;tick &quot;</span> &lt;&lt; i &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">        std::this_thread::<span class="built_in">sleep_until</span>(next);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="3-基础线程-std-thread"><a href="#3-基础线程-std-thread" class="headerlink" title="3. 基础线程 std::thread"></a>3. 基础线程 <code>std::thread</code></h2><h3 id="3-1-启动线程与-join（必须）"><a href="#3-1-启动线程与-join（必须）" class="headerlink" title="3.1 启动线程与 join（必须）"></a>3.1 启动线程与 join（必须）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">work</span><span class="params">(<span class="type">int</span> id)</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;worker &quot;</span> &lt;&lt; id &lt;&lt; <span class="string">&quot;\n&quot;</span>; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">std::thread <span class="title">t</span><span class="params">(work, <span class="number">1</span>)</span></span>;</span><br><span class="line">    t.<span class="built_in">join</span>(); <span class="comment">// 必须 join 或 detach，否则析构会 std::terminate</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>join()</code>：等待线程结束（最常用）</li>
<li><code>detach()</code>：放飞线程（风险高：对象生命周期&#x2F;退出时机难控）</li>
</ul>
<hr>
<h2 id="4-异步与返回值：std-async-std-future"><a href="#4-异步与返回值：std-async-std-future" class="headerlink" title="4. 异步与返回值：std::async &#x2F; std::future"></a>4. 异步与返回值：<code>std::async</code> &#x2F; <code>std::future</code></h2><h3 id="4-1-基本用法（带返回值）"><a href="#4-1-基本用法（带返回值）" class="headerlink" title="4.1 基本用法（带返回值）"></a>4.1 基本用法（带返回值）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;future&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;chrono&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">download</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::this_thread::<span class="built_in">sleep_for</span>(std::chrono::<span class="built_in">milliseconds</span>(<span class="number">200</span>));</span><br><span class="line">    <span class="keyword">return</span> <span class="number">404</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> fut = std::<span class="built_in">async</span>(std::launch::async, [] &#123; <span class="keyword">return</span> <span class="built_in">download</span>(); &#125;);</span><br><span class="line">    <span class="type">int</span> ret = fut.<span class="built_in">get</span>(); <span class="comment">// get 只能调用一次</span></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;ret=&quot;</span> &lt;&lt; ret &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-2-启动策略"><a href="#4-2-启动策略" class="headerlink" title="4.2 启动策略"></a>4.2 启动策略</h3><ul>
<li><code>std::launch::async</code>：倾向于并行执行</li>
<li><code>std::launch::deferred</code>：延迟到 <code>get()/wait()</code> 才在当前线程执行</li>
</ul>
<h3 id="4-3-wait-wait-for-wait-until"><a href="#4-3-wait-wait-for-wait-until" class="headerlink" title="4.3 wait &#x2F; wait_for &#x2F; wait_until"></a>4.3 wait &#x2F; wait_for &#x2F; wait_until</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> fut = std::<span class="built_in">async</span>(std::launch::async, []&#123;</span><br><span class="line">    std::this_thread::<span class="built_in">sleep_for</span>(std::chrono::<span class="built_in">seconds</span>(<span class="number">2</span>));</span><br><span class="line">    <span class="keyword">return</span> <span class="number">123</span>;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">fut.<span class="built_in">wait</span>(); <span class="comment">// 等到完成</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> st = fut.<span class="built_in">wait_for</span>(std::chrono::<span class="built_in">milliseconds</span>(<span class="number">100</span>));</span><br><span class="line"><span class="keyword">if</span> (st == std::future_status::timeout) &#123;</span><br><span class="line">    <span class="comment">// 还没完成</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-4-异常会在-get-处重抛（非常实用）"><a href="#4-4-异常会在-get-处重抛（非常实用）" class="headerlink" title="4.4 异常会在 get() 处重抛（非常实用）"></a>4.4 异常会在 get() 处重抛（非常实用）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> fut = std::<span class="built_in">async</span>(std::launch::async, []() -&gt; <span class="type">int</span> &#123;</span><br><span class="line">    <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;boom&quot;</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span> &#123; fut.<span class="built_in">get</span>(); &#125;</span><br><span class="line"><span class="built_in">catch</span> (<span class="type">const</span> std::exception&amp; e) &#123; std::cout &lt;&lt; e.<span class="built_in">what</span>() &lt;&lt; <span class="string">&quot;\n&quot;</span>; &#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="5-互斥锁-std-mutex：保护共享数据"><a href="#5-互斥锁-std-mutex：保护共享数据" class="headerlink" title="5. 互斥锁 std::mutex：保护共享数据"></a>5. 互斥锁 <code>std::mutex</code>：保护共享数据</h2><h3 id="5-1-lock-guard（RAII-自动解锁）"><a href="#5-1-lock-guard（RAII-自动解锁）" class="headerlink" title="5.1 lock_guard（RAII 自动解锁）"></a>5.1 <code>lock_guard</code>（RAII 自动解锁）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::mutex m;</span><br><span class="line">    <span class="type">int</span> counter = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> inc = [&amp;] &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100000</span>; ++i) &#123;</span><br><span class="line">            std::lock_guard&lt;std::mutex&gt; <span class="built_in">lk</span>(m);</span><br><span class="line">            ++counter;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::thread <span class="title">t1</span><span class="params">(inc)</span>, <span class="title">t2</span><span class="params">(inc)</span></span>;</span><br><span class="line">    t<span class="number">1.</span><span class="built_in">join</span>(); t<span class="number">2.</span><span class="built_in">join</span>();</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;counter=&quot;</span> &lt;&lt; counter &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-2-死锁规避（两个锁）"><a href="#5-2-死锁规避（两个锁）" class="headerlink" title="5.2 死锁规避（两个锁）"></a>5.2 死锁规避（两个锁）</h3><p>C++11 可用 <code>std::lock</code> + <code>std::adopt_lock</code>：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"></span><br><span class="line">std::mutex m1, m2;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">f</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::<span class="built_in">lock</span>(m1, m2);</span><br><span class="line">    <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lk1</span><span class="params">(m1, std::adopt_lock)</span></span>;</span><br><span class="line">    <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lk2</span><span class="params">(m2, std::adopt_lock)</span></span>;</span><br><span class="line">    <span class="comment">// 临界区</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="6-条件变量-std-condition-variable：生产者-消费者（比-sleep-正确）"><a href="#6-条件变量-std-condition-variable：生产者-消费者（比-sleep-正确）" class="headerlink" title="6. 条件变量 std::condition_variable：生产者-消费者（比 sleep 正确）"></a>6. 条件变量 <code>std::condition_variable</code>：生产者-消费者（比 sleep 正确）</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;condition_variable&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::mutex m;</span><br><span class="line">    std::condition_variable cv;</span><br><span class="line">    std::queue&lt;<span class="type">int</span>&gt; q;</span><br><span class="line">    <span class="type">bool</span> done = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::thread <span class="title">producer</span><span class="params">([&amp;]&#123;</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; ++i) &#123;</span></span></span><br><span class="line"><span class="params"><span class="function">            &#123; std::lock_guard&lt;std::mutex&gt; lk(m); q.push(i); &#125;</span></span></span><br><span class="line"><span class="params"><span class="function">            cv.notify_one();</span></span></span><br><span class="line"><span class="params"><span class="function">        &#125;</span></span></span><br><span class="line"><span class="params"><span class="function">        &#123; std::lock_guard&lt;std::mutex&gt; lk(m); done = <span class="literal">true</span>; &#125;</span></span></span><br><span class="line"><span class="params"><span class="function">        cv.notify_one();</span></span></span><br><span class="line"><span class="params"><span class="function">    &#125;)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::thread <span class="title">consumer</span><span class="params">([&amp;]&#123;</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span></span></span><br><span class="line"><span class="params"><span class="function">            std::unique_lock&lt;std::mutex&gt; lk(m);</span></span></span><br><span class="line"><span class="params"><span class="function">            cv.wait(lk, [&amp;]&#123; <span class="keyword">return</span> done || !q.empty(); &#125;); <span class="comment">// 带谓词防虚假唤醒</span></span></span></span><br><span class="line"><span class="params"><span class="function">            <span class="keyword">while</span> (!q.empty()) &#123;</span></span></span><br><span class="line"><span class="params"><span class="function">                std::cout &lt;&lt; <span class="string">&quot;consume &quot;</span> &lt;&lt; q.front() &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span></span></span><br><span class="line"><span class="params"><span class="function">                q.pop();</span></span></span><br><span class="line"><span class="params"><span class="function">            &#125;</span></span></span><br><span class="line"><span class="params"><span class="function">            <span class="keyword">if</span> (done) <span class="keyword">break</span>;</span></span></span><br><span class="line"><span class="params"><span class="function">        &#125;</span></span></span><br><span class="line"><span class="params"><span class="function">    &#125;)</span></span>;</span><br><span class="line"></span><br><span class="line">    producer.<span class="built_in">join</span>();</span><br><span class="line">    consumer.<span class="built_in">join</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="7-原子-std-atomic：轻量同步（计数-标志位）"><a href="#7-原子-std-atomic：轻量同步（计数-标志位）" class="headerlink" title="7. 原子 std::atomic：轻量同步（计数&#x2F;标志位）"></a>7. 原子 <code>std::atomic</code>：轻量同步（计数&#x2F;标志位）</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;atomic&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::atomic&lt;<span class="type">int</span>&gt; counter&#123;<span class="number">0</span>&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> inc = [&amp;] &#123; <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100000</span>; ++i) ++counter; &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="function">std::thread <span class="title">t1</span><span class="params">(inc)</span>, <span class="title">t2</span><span class="params">(inc)</span></span>;</span><br><span class="line">    t<span class="number">1.</span><span class="built_in">join</span>(); t<span class="number">2.</span><span class="built_in">join</span>();</span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;counter=&quot;</span> &lt;&lt; counter.<span class="built_in">load</span>() &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="8-线程池-任务队列（C-11-可直接用）"><a href="#8-线程池-任务队列（C-11-可直接用）" class="headerlink" title="8. 线程池&#x2F;任务队列（C++11 可直接用）"></a>8. 线程池&#x2F;任务队列（C++11 可直接用）</h2><blockquote>
<p>线程池 &#x3D; 固定 worker 线程 + 任务队列（阻塞队列）<br>典型价值：避免频繁创建&#x2F;销毁线程，提高吞吐；统一任务提交接口，支持 <code>future</code> 返回值与异常回传。</p>
</blockquote>
<h3 id="8-1-任务队列：BlockingQueue（阻塞队列）"><a href="#8-1-任务队列：BlockingQueue（阻塞队列）" class="headerlink" title="8.1 任务队列：BlockingQueue（阻塞队列）"></a>8.1 任务队列：BlockingQueue（阻塞队列）</h3><p>特性：</p>
<ul>
<li><code>push()</code> 入队并唤醒消费者</li>
<li><code>pop()</code> 阻塞等待任务；队列关闭且为空时返回 <code>false</code></li>
<li><code>close()</code> 关闭队列并唤醒全部等待线程</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">pragma</span> once</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;condition_variable&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BlockingQueue</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">BlockingQueue</span>() : <span class="built_in">closed_</span>(<span class="literal">false</span>) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">BlockingQueue</span>(<span class="type">const</span> BlockingQueue&amp;) = <span class="keyword">delete</span>;</span><br><span class="line">    BlockingQueue&amp; <span class="keyword">operator</span>=(<span class="type">const</span> BlockingQueue&amp;) = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">push</span><span class="params">(T value)</span> </span>&#123;</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(m_)</span></span>;</span><br><span class="line">            <span class="keyword">if</span> (closed_) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">            q_.<span class="built_in">push</span>(std::<span class="built_in">move</span>(value));</span><br><span class="line">        &#125;</span><br><span class="line">        cv_.<span class="built_in">notify_one</span>();</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">pop</span><span class="params">(T&amp; out)</span> </span>&#123;</span><br><span class="line">        <span class="function">std::unique_lock&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(m_)</span></span>;</span><br><span class="line">        cv_.<span class="built_in">wait</span>(lk, [&amp;]&#123; <span class="keyword">return</span> closed_ || !q_.<span class="built_in">empty</span>(); &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (q_.<span class="built_in">empty</span>()) <span class="keyword">return</span> <span class="literal">false</span>; <span class="comment">// closed_ &amp;&amp; empty</span></span><br><span class="line">        out = std::<span class="built_in">move</span>(q_.<span class="built_in">front</span>());</span><br><span class="line">        q_.<span class="built_in">pop</span>();</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">close</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lk</span><span class="params">(m_)</span></span>;</span><br><span class="line">            closed_ = <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        cv_.<span class="built_in">notify_all</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::mutex m_;</span><br><span class="line">    std::condition_variable cv_;</span><br><span class="line">    std::queue&lt;T&gt; q_;</span><br><span class="line">    <span class="type">bool</span> closed_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="8-2-线程池：ThreadPool（enqueue-返回-future）"><a href="#8-2-线程池：ThreadPool（enqueue-返回-future）" class="headerlink" title="8.2 线程池：ThreadPool（enqueue 返回 future）"></a>8.2 线程池：ThreadPool（enqueue 返回 future）</h3><p>特性：</p>
<ul>
<li>固定线程数 worker</li>
<li><code>enqueue(f, args...)</code> 返回 <code>std::future&lt;R&gt;</code></li>
<li>任务抛异常 → 在 <code>future.get()</code> 时重抛</li>
<li>析构&#x2F;<code>shutdown()</code>：停止接收新任务 + 关闭队列 + <code>join</code> 所有 worker</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">pragma</span> once</span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;future&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdexcept&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;atomic&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;utility&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;blocking_queue.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ThreadPool</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">ThreadPool</span><span class="params">(<span class="type">size_t</span> nthreads)</span> : accept_(true) &#123;</span></span><br><span class="line">        <span class="keyword">if</span> (nthreads == <span class="number">0</span>) nthreads = <span class="number">1</span>;</span><br><span class="line">        workers_.<span class="built_in">reserve</span>(nthreads);</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">size_t</span> i = <span class="number">0</span>; i &lt; nthreads; ++i) &#123;</span><br><span class="line">            workers_.<span class="built_in">push_back</span>(std::<span class="built_in">thread</span>([<span class="keyword">this</span>]&#123; <span class="built_in">worker_loop</span>(); &#125;));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">ThreadPool</span>(<span class="type">const</span> ThreadPool&amp;) = <span class="keyword">delete</span>;</span><br><span class="line">    ThreadPool&amp; <span class="keyword">operator</span>=(<span class="type">const</span> ThreadPool&amp;) = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">ThreadPool</span>() &#123; <span class="built_in">shutdown</span>(); &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">F</span>, <span class="keyword">class</span>... Args&gt;</span><br><span class="line">    <span class="function"><span class="keyword">auto</span> <span class="title">enqueue</span><span class="params">(F&amp;&amp; f, Args&amp;&amp;... args)</span></span></span><br><span class="line"><span class="function">        -&gt; std::future&lt;<span class="keyword">typename</span> std::result_of&lt;<span class="title">F</span><span class="params">(Args...)</span>&gt;::type&gt;</span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">typedef</span> <span class="keyword">typename</span> std::result_of&lt;<span class="built_in">F</span>(Args...)&gt;::type R;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!accept_.<span class="built_in">load</span>()) &#123;</span><br><span class="line">            <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;ThreadPool is not accepting new tasks.&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">auto</span> task_ptr = std::make_shared&lt;std::packaged_task&lt;<span class="built_in">R</span>()&gt;&gt;(</span><br><span class="line">            std::<span class="built_in">bind</span>(std::forward&lt;F&gt;(f), std::forward&lt;Args&gt;(args)...)</span><br><span class="line">        );</span><br><span class="line">        std::future&lt;R&gt; fut = task_ptr-&gt;<span class="built_in">get_future</span>();</span><br><span class="line"></span><br><span class="line">        <span class="type">bool</span> ok = tasks_.<span class="built_in">push</span>([task_ptr]&#123; (*task_ptr)(); &#125;);</span><br><span class="line">        <span class="keyword">if</span> (!ok) <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;Task queue is closed.&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> fut;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">shutdown</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="type">bool</span> expected = <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">if</span> (accept_.<span class="built_in">compare_exchange_strong</span>(expected, <span class="literal">false</span>)) &#123;</span><br><span class="line">            tasks_.<span class="built_in">close</span>();</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; t : workers_) &#123;</span><br><span class="line">                <span class="keyword">if</span> (t.<span class="built_in">joinable</span>()) t.<span class="built_in">join</span>();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">worker_loop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        std::function&lt;<span class="type">void</span>()&gt; task;</span><br><span class="line">        <span class="keyword">while</span> (tasks_.<span class="built_in">pop</span>(task)) &#123;</span><br><span class="line">            <span class="built_in">task</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::vector&lt;std::thread&gt; workers_;</span><br><span class="line">    BlockingQueue&lt;std::function&lt;<span class="type">void</span>()&gt;&gt; tasks_;</span><br><span class="line">    std::atomic&lt;<span class="type">bool</span>&gt; accept_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="8-3-线程池使用示例（返回值-异常回传）"><a href="#8-3-线程池使用示例（返回值-异常回传）" class="headerlink" title="8.3 线程池使用示例（返回值 + 异常回传）"></a>8.3 线程池使用示例（返回值 + 异常回传）</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;chrono&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;thread_pool.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">slow_add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">    std::this_thread::<span class="built_in">sleep_for</span>(std::chrono::<span class="built_in">milliseconds</span>(<span class="number">200</span>));</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="function">ThreadPool <span class="title">pool</span><span class="params">(<span class="number">4</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> f1 = pool.<span class="built_in">enqueue</span>(slow_add, <span class="number">1</span>, <span class="number">2</span>);</span><br><span class="line">    <span class="keyword">auto</span> f2 = pool.<span class="built_in">enqueue</span>([](<span class="type">int</span> x)&#123; <span class="keyword">return</span> x * x; &#125;, <span class="number">12</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> f3 = pool.<span class="built_in">enqueue</span>([]() -&gt; <span class="type">int</span> &#123;</span><br><span class="line">        <span class="keyword">throw</span> std::<span class="built_in">runtime_error</span>(<span class="string">&quot;boom&quot;</span>);</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;f1=&quot;</span> &lt;&lt; f<span class="number">1.</span><span class="built_in">get</span>() &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;f2=&quot;</span> &lt;&lt; f<span class="number">2.</span><span class="built_in">get</span>() &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;f3=&quot;</span> &lt;&lt; f<span class="number">3.</span><span class="built_in">get</span>() &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">    &#125; <span class="built_in">catch</span> (<span class="type">const</span> std::exception&amp; e) &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;caught: &quot;</span> &lt;&lt; e.<span class="built_in">what</span>() &lt;&lt; <span class="string">&quot;\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    pool.<span class="built_in">shutdown</span>(); <span class="comment">// 可省略：析构也会 shutdown</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>流体仿真实战</title>
    <url>//notes/%E9%AB%98%E6%80%A7%E8%83%BDc-%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/12-%E6%B5%81%E4%BD%93%E4%BB%BF%E7%9C%9F%E5%AE%9E%E6%88%98/</url>
    <content><![CDATA[<h1 id="流体仿真实战"><a href="#流体仿真实战" class="headerlink" title="流体仿真实战"></a>流体仿真实战</h1><p>时间：2026&#x2F;04&#x2F;09</p>
<blockquote>
<p>关键词：Eulerian grid、advection、diffusion、projection、pressure solve、ping-pong、texture object、CUDA<br>核心目标：从工程角度理解一个实时流体模拟在 GPU 上通常由哪些步骤组成，以及每一步为什么这样设计。</p>
</blockquote>
<hr>
<h2 id="1-先建立问题模型"><a href="#1-先建立问题模型" class="headerlink" title="1. 先建立问题模型"></a>1. 先建立问题模型</h2><p>实时流体仿真通常不是在“跟踪每一个分子”，而是在求解离散化后的场：</p>
<ul>
<li>速度场 <code>u(x, y[, z])</code></li>
<li>压力场 <code>p</code></li>
<li>密度&#x2F;烟雾浓度 <code>d</code></li>
<li>温度、外力等附加场</li>
</ul>
<p>在图形学实时仿真里，最常见的是 <strong>Eulerian 网格法</strong>：</p>
<ul>
<li>空间被划分成规则网格</li>
<li>每个网格单元保存局部状态</li>
<li>关注的是“这个位置上的流体状态”，而不是“某个粒子去哪了”</li>
</ul>
<hr>
<h2 id="2-一帧流体模拟通常做什么"><a href="#2-一帧流体模拟通常做什么" class="headerlink" title="2. 一帧流体模拟通常做什么"></a>2. 一帧流体模拟通常做什么</h2><p>一个经典的 2D&#x2F;3D 实时烟雾模拟步骤，通常长这样：</p>
<ol>
<li>对速度场做平流（advection）</li>
<li>对密度&#x2F;温度做平流</li>
<li>加入外力（force &#x2F; impulse）</li>
<li>计算散度（divergence）</li>
<li>解压强泊松方程（pressure solve）</li>
<li>用压力梯度做投影（projection）</li>
<li>得到无散速度场</li>
<li>渲染密度或其他可视化结果</li>
</ol>
<p>这条管线里最关键的思想是：</p>
<ul>
<li>平流负责“把东西搬着走”</li>
<li>压力投影负责“让速度场满足不可压缩约束”</li>
</ul>
<hr>
<h2 id="3-为什么很多实时流体用-Stable-Fluids"><a href="#3-为什么很多实时流体用-Stable-Fluids" class="headerlink" title="3. 为什么很多实时流体用 Stable Fluids"></a>3. 为什么很多实时流体用 Stable Fluids</h2><p>Jos Stam 的 Stable Fluids 思想之所以经典，是因为它强调：</p>
<ul>
<li>数值上更稳定</li>
<li>能容忍较大的时间步长</li>
<li>非常适合图形实时场景</li>
</ul>
<p>它的代价通常是：</p>
<ul>
<li>会更粘、更耗散</li>
<li>细节可能被抹平</li>
</ul>
<p>所以工程上通常要在：</p>
<ul>
<li>稳定性</li>
<li>速度</li>
<li>视觉效果</li>
</ul>
<p>之间做平衡。</p>
<hr>
<h2 id="4-网格、边界与数据布局"><a href="#4-网格、边界与数据布局" class="headerlink" title="4. 网格、边界与数据布局"></a>4. 网格、边界与数据布局</h2><h3 id="4-1-最常见的数据"><a href="#4-1-最常见的数据" class="headerlink" title="4.1 最常见的数据"></a>4.1 最常见的数据</h3><p>二维时经常有：</p>
<ul>
<li><code>vel_x</code></li>
<li><code>vel_y</code></li>
<li><code>pressure</code></li>
<li><code>divergence</code></li>
<li><code>density</code></li>
</ul>
<h3 id="4-2-常见存储方式"><a href="#4-2-常见存储方式" class="headerlink" title="4.2 常见存储方式"></a>4.2 常见存储方式</h3><p>对 GPU 更友好的方式通常是：</p>
<ul>
<li>扁平连续数组</li>
<li>纹理&#x2F;表面对象</li>
<li>ping-pong 双缓冲</li>
</ul>
<p>例如二维数组常展平成：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">idx = y * width + x;</span><br></pre></td></tr></table></figure>

<h3 id="4-3-为什么常用-SoA-思路"><a href="#4-3-为什么常用-SoA-思路" class="headerlink" title="4.3 为什么常用 SoA 思路"></a>4.3 为什么常用 SoA 思路</h3><p>不同字段通常分开存：</p>
<ul>
<li>一个数组&#x2F;纹理存 <code>vel_x</code></li>
<li>一个数组&#x2F;纹理存 <code>vel_y</code></li>
<li>一个数组&#x2F;纹理存 <code>pressure</code></li>
</ul>
<p>这比把所有字段塞进一个大结构更容易：</p>
<ul>
<li>控制带宽</li>
<li>做批量 kernel</li>
<li>针对单一字段优化访问</li>
</ul>
<hr>
<h2 id="5-平流（Advection）是什么"><a href="#5-平流（Advection）是什么" class="headerlink" title="5. 平流（Advection）是什么"></a>5. 平流（Advection）是什么</h2><p>直觉上，平流就是：</p>
<blockquote>
<p>按速度场把某个量“搬运”到下一个时刻。</p>
</blockquote>
<p>对于密度场来说，可以理解成：</p>
<ul>
<li>烟雾随着流速移动</li>
</ul>
<p>对于速度场本身来说：</p>
<ul>
<li>速度也会被自身流动搬运</li>
</ul>
<h3 id="5-1-半拉格朗日（Semi-Lagrangian）平流"><a href="#5-1-半拉格朗日（Semi-Lagrangian）平流" class="headerlink" title="5.1 半拉格朗日（Semi-Lagrangian）平流"></a>5.1 半拉格朗日（Semi-Lagrangian）平流</h3><p>实时图形里很常见的做法是“反向追踪”：</p>
<ul>
<li>对当前网格点 <code>(x, y)</code></li>
<li>沿速度反方向回溯到上一个时刻的位置</li>
<li>从旧场中插值取样</li>
</ul>
<p>公式直觉：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">phi_new(x) = phi_old(x - dt * u(x))</span><br></pre></td></tr></table></figure>

<h3 id="5-2-为什么它适合-GPU"><a href="#5-2-为什么它适合-GPU" class="headerlink" title="5.2 为什么它适合 GPU"></a>5.2 为什么它适合 GPU</h3><p>因为每个网格点都能独立算：</p>
<ul>
<li>回溯位置</li>
<li>插值取样</li>
<li>写回新值</li>
</ul>
<p>这非常适合 data-parallel kernel。</p>
<hr>
<h2 id="6-为什么纹理对象在流体里常见"><a href="#6-为什么纹理对象在流体里常见" class="headerlink" title="6. 为什么纹理对象在流体里常见"></a>6. 为什么纹理对象在流体里常见</h2><p>流体平流特别需要“按浮点坐标插值采样旧场”。<br>这正是纹理对象很擅长的事情。</p>
<h3 id="6-1-纹理对象的实际优势"><a href="#6-1-纹理对象的实际优势" class="headerlink" title="6.1 纹理对象的实际优势"></a>6.1 纹理对象的实际优势</h3><ul>
<li>支持硬件插值</li>
<li>支持边界寻址模式</li>
<li>访问接口清晰</li>
<li>对二维&#x2F;三维场很自然</li>
</ul>
<p>因此在 CUDA 流体项目里，经常会看到：</p>
<ul>
<li>旧场绑定成 texture</li>
<li>新场写到 output buffer &#x2F; surface</li>
</ul>
<h3 id="6-2-一个常见工程模式"><a href="#6-2-一个常见工程模式" class="headerlink" title="6.2 一个常见工程模式"></a>6.2 一个常见工程模式</h3><ul>
<li><code>src</code> 作为只读纹理</li>
<li><code>dst</code> 作为可写 buffer</li>
<li>kernel 结束后交换 <code>src/dst</code></li>
</ul>
<p>这就是典型的 ping-pong。</p>
<hr>
<h2 id="7-Ping-Pong-双缓冲为什么几乎是标配"><a href="#7-Ping-Pong-双缓冲为什么几乎是标配" class="headerlink" title="7. Ping-Pong 双缓冲为什么几乎是标配"></a>7. Ping-Pong 双缓冲为什么几乎是标配</h2><p>平流、扩散、压力迭代这类步骤，通常不能一边读旧值一边原地覆盖写新值，否则会污染后续计算。</p>
<p>最常见做法：</p>
<ul>
<li><code>A</code> 存旧场</li>
<li><code>B</code> 存新场</li>
<li>一步计算后交换 <code>A/B</code></li>
</ul>
<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">advect:    vel0 -&gt; vel1</span><br><span class="line">swap</span><br><span class="line">project:   vel1 -&gt; vel0</span><br><span class="line">swap</span><br></pre></td></tr></table></figure>

<p>双缓冲的代价是多占一份显存，但好处是：</p>
<ul>
<li>读写依赖清晰</li>
<li>kernel 更容易写对</li>
<li>更适合并行</li>
</ul>
<hr>
<h2 id="8-散度（Divergence）为什么要算"><a href="#8-散度（Divergence）为什么要算" class="headerlink" title="8. 散度（Divergence）为什么要算"></a>8. 散度（Divergence）为什么要算</h2><p>不可压缩流体通常要求：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">div(u) = 0</span><br></pre></td></tr></table></figure>

<p>但经过加力、平流等步骤后，速度场往往不再满足这个条件。<br>所以需要先计算散度，看看当前速度场“有多不守恒”。</p>
<p>二维中心差分的直觉形式：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">div = (u_right - u_left + v_up - v_down) * 0.5 / h</span><br></pre></td></tr></table></figure>

<p>算出来的散度场会成为后面压力方程的右端项。</p>
<hr>
<h2 id="9-压力求解（Pressure-Solve）是核心瓶颈之一"><a href="#9-压力求解（Pressure-Solve）是核心瓶颈之一" class="headerlink" title="9. 压力求解（Pressure Solve）是核心瓶颈之一"></a>9. 压力求解（Pressure Solve）是核心瓶颈之一</h2><p>为了让速度场重新无散，通常要解一个泊松方程：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">∇²p = div</span><br></pre></td></tr></table></figure>

<p>这里：</p>
<ul>
<li><code>p</code> 是压力</li>
<li><code>div</code> 是上一阶段算出的散度</li>
</ul>
<h3 id="9-1-为什么这是难点"><a href="#9-1-为什么这是难点" class="headerlink" title="9.1 为什么这是难点"></a>9.1 为什么这是难点</h3><p>因为它不是一个局部一步完成的更新，而是一个全局耦合问题。<br>实时实现中常用近似迭代法，例如：</p>
<ul>
<li>Jacobi</li>
<li>Gauss-Seidel</li>
<li>Red-Black Gauss-Seidel</li>
<li>Multigrid（更复杂也更强）</li>
</ul>
<h3 id="9-2-为什么很多教程先用-Jacobi"><a href="#9-2-为什么很多教程先用-Jacobi" class="headerlink" title="9.2 为什么很多教程先用 Jacobi"></a>9.2 为什么很多教程先用 Jacobi</h3><p>因为它简单、规则、容易并行：</p>
<ul>
<li>每次迭代只读旧压力</li>
<li>写新压力</li>
<li>适合 GPU 大规模并行</li>
</ul>
<p>代价是收敛速度不算快。</p>
<hr>
<h2 id="10-投影（Projection）在做什么"><a href="#10-投影（Projection）在做什么" class="headerlink" title="10. 投影（Projection）在做什么"></a>10. 投影（Projection）在做什么</h2><p>解出压力场后，用压力梯度修正速度：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">u&#x27; = u - ∇p</span><br></pre></td></tr></table></figure>

<p>直觉上就是：</p>
<ul>
<li>从原速度里减掉“造成体积膨胀&#x2F;压缩”的那部分</li>
</ul>
<p>投影之后，速度场更接近无散状态，也就更符合不可压缩流体假设。</p>
<p>这一步通常是：</p>
<ol>
<li>读取速度</li>
<li>读取相邻压力</li>
<li>做梯度差分</li>
<li>写回新速度</li>
</ol>
<hr>
<h2 id="11-边界条件不能忽略"><a href="#11-边界条件不能忽略" class="headerlink" title="11. 边界条件不能忽略"></a>11. 边界条件不能忽略</h2><p>很多新手代码“能跑但看起来不对”，问题往往在边界。</p>
<p>常见边界处理包括：</p>
<ul>
<li>固壁边界</li>
<li>开放边界</li>
<li>周期边界</li>
<li>障碍物边界</li>
</ul>
<p>例如固壁边界时，速度在法向方向通常要满足不穿透约束。</p>
<p>如果边界没处理好，可能会出现：</p>
<ul>
<li>烟雾穿墙</li>
<li>边缘数值爆炸</li>
<li>压力解不稳定</li>
</ul>
<hr>
<h2 id="12-CUDA-上通常如何拆-kernel"><a href="#12-CUDA-上通常如何拆-kernel" class="headerlink" title="12. CUDA 上通常如何拆 kernel"></a>12. CUDA 上通常如何拆 kernel</h2><p>一个典型实时流体项目不会把全部逻辑塞进一个 kernel，而是拆成多步：</p>
<ul>
<li><code>advect_velocity</code></li>
<li><code>advect_density</code></li>
<li><code>add_force</code></li>
<li><code>compute_divergence</code></li>
<li><code>jacobi_pressure</code></li>
<li><code>project_velocity</code></li>
<li><code>render_density</code></li>
</ul>
<p>这样做的好处：</p>
<ul>
<li>每步逻辑清晰</li>
<li>便于调试</li>
<li>便于替换某一步算法</li>
</ul>
<p>代价是：</p>
<ul>
<li>kernel launch 次数变多</li>
<li>中间结果要反复读写全局内存</li>
</ul>
<p>所以工程优化通常是在“模块清晰”和“减少中间带宽”之间折中。</p>
<hr>
<h2 id="13-一个典型的-CUDA-数据流"><a href="#13-一个典型的-CUDA-数据流" class="headerlink" title="13. 一个典型的 CUDA 数据流"></a>13. 一个典型的 CUDA 数据流</h2><p>可以把一帧想成这样：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">velocity_src --advect--&gt; velocity_dst</span><br><span class="line">swap</span><br><span class="line"></span><br><span class="line">density_src --advect--&gt; density_dst</span><br><span class="line">swap</span><br><span class="line"></span><br><span class="line">velocity_src --add_force--&gt; velocity_dst</span><br><span class="line">swap</span><br><span class="line"></span><br><span class="line">velocity_src --divergence--&gt; divergence</span><br><span class="line"></span><br><span class="line">pressure_src --jacobi--&gt; pressure_dst</span><br><span class="line">swap</span><br><span class="line">... 迭代多次 ...</span><br><span class="line"></span><br><span class="line">velocity_src + pressure_src --project--&gt; velocity_dst</span><br><span class="line">swap</span><br><span class="line"></span><br><span class="line">density_src --render--&gt; framebuffer</span><br></pre></td></tr></table></figure>

<p>其中压力求解阶段往往会迭代很多轮，是核心带宽热点之一。</p>
<hr>
<h2 id="14-纹理对象在这里最常见的落点"><a href="#14-纹理对象在这里最常见的落点" class="headerlink" title="14. 纹理对象在这里最常见的落点"></a>14. 纹理对象在这里最常见的落点</h2><h3 id="14-1-平流时"><a href="#14-1-平流时" class="headerlink" title="14.1 平流时"></a>14.1 平流时</h3><p>因为需要按浮点坐标回溯并插值取样旧场。</p>
<h3 id="14-2-可视化时"><a href="#14-2-可视化时" class="headerlink" title="14.2 可视化时"></a>14.2 可视化时</h3><p>例如把密度场映射到图像。</p>
<h3 id="14-3-有时不用纹理也能做"><a href="#14-3-有时不用纹理也能做" class="headerlink" title="14.3 有时不用纹理也能做"></a>14.3 有时不用纹理也能做</h3><p>完全可以手写双线性插值，但纹理对象通常更方便，也更贴近 GPU 图像管线思维。</p>
<hr>
<h2 id="15-性能瓶颈通常在哪"><a href="#15-性能瓶颈通常在哪" class="headerlink" title="15. 性能瓶颈通常在哪"></a>15. 性能瓶颈通常在哪</h2><h3 id="15-1-不一定是算术，而是带宽"><a href="#15-1-不一定是算术，而是带宽" class="headerlink" title="15.1 不一定是算术，而是带宽"></a>15.1 不一定是算术，而是带宽</h3><p>流体很多步骤都是 stencil &#x2F; 邻域访问：</p>
<ul>
<li>读周围多个点</li>
<li>写一个点</li>
</ul>
<p>因此常见瓶颈是：</p>
<ul>
<li>全局内存带宽</li>
<li>多次中间场读写</li>
</ul>
<h3 id="15-2-压力迭代轮数"><a href="#15-2-压力迭代轮数" class="headerlink" title="15.2 压力迭代轮数"></a>15.2 压力迭代轮数</h3><p>Jacobi 每多迭代一轮，都意味着：</p>
<ul>
<li>再扫一遍整个压力场</li>
</ul>
<p>所以视觉质量和帧率经常直接在这里博弈。</p>
<h3 id="15-3-边界和条件判断"><a href="#15-3-边界和条件判断" class="headerlink" title="15.3 边界和条件判断"></a>15.3 边界和条件判断</h3><p>边界格子处理如果分支太多，也会影响 warp 执行效率。</p>
<hr>
<h2 id="16-常见优化方向"><a href="#16-常见优化方向" class="headerlink" title="16. 常见优化方向"></a>16. 常见优化方向</h2><h3 id="16-1-减少全局内存往返"><a href="#16-1-减少全局内存往返" class="headerlink" title="16.1 减少全局内存往返"></a>16.1 减少全局内存往返</h3><p>例如：</p>
<ul>
<li>更少的中间缓冲</li>
<li>更合理的 kernel 融合</li>
</ul>
<h3 id="16-2-用-shared-memory-做局部邻域缓存"><a href="#16-2-用-shared-memory-做局部邻域缓存" class="headerlink" title="16.2 用 shared memory 做局部邻域缓存"></a>16.2 用 shared memory 做局部邻域缓存</h3><p>对于 stencil 类计算，局部 tile 常能减少重复全局加载。</p>
<h3 id="16-3-让访问尽量规则"><a href="#16-3-让访问尽量规则" class="headerlink" title="16.3 让访问尽量规则"></a>16.3 让访问尽量规则</h3><p>例如：</p>
<ul>
<li>扁平数组</li>
<li>合理线程块布局</li>
<li>连续内存访问</li>
</ul>
<h3 id="16-4-区分“视觉上够用”和“数值上更准”"><a href="#16-4-区分“视觉上够用”和“数值上更准”" class="headerlink" title="16.4 区分“视觉上够用”和“数值上更准”"></a>16.4 区分“视觉上够用”和“数值上更准”</h3><p>实时图形里很多时候不是求学术最优，而是：</p>
<ul>
<li>足够稳定</li>
<li>足够快</li>
<li>视觉上自然</li>
</ul>
<hr>
<h2 id="17-为什么这类项目特别适合做综合练习"><a href="#17-为什么这类项目特别适合做综合练习" class="headerlink" title="17. 为什么这类项目特别适合做综合练习"></a>17. 为什么这类项目特别适合做综合练习</h2><p>流体仿真把很多高性能主题都串起来了：</p>
<ul>
<li>数据布局</li>
<li>邻域访存</li>
<li>ping-pong 双缓冲</li>
<li>GPU kernel 设计</li>
<li>纹理对象</li>
<li>迭代求解器</li>
<li>可视化与物理折中</li>
</ul>
<p>所以它既是一个图形学题目，也是一个很典型的并行计算工程题。</p>
<hr>
<h2 id="18-初学实现建议"><a href="#18-初学实现建议" class="headerlink" title="18. 初学实现建议"></a>18. 初学实现建议</h2><p>如果你第一次做，比较稳的路线是：</p>
<ol>
<li>先做 2D，不要一开始上 3D</li>
<li>先做密度平流，再加速度场</li>
<li>再补散度、压力、投影</li>
<li>压力求解先用 Jacobi 跑通</li>
<li>最后再考虑纹理对象、shared memory、性能优化</li>
</ol>
<p>因为最难的不是“写出一个 kernel”，而是：</p>
<ul>
<li>整条数据流逻辑正确</li>
<li>每步边界一致</li>
<li>数值上稳定</li>
</ul>
<hr>
<h2 id="19-一页总结"><a href="#19-一页总结" class="headerlink" title="19. 一页总结"></a>19. 一页总结</h2><p>实时流体仿真的工程骨架，可以压缩成下面几句：</p>
<ol>
<li>用规则网格存速度、压力和密度场</li>
<li>用平流把量沿速度场搬运</li>
<li>用散度和压力解修正速度场</li>
<li>用投影得到近似不可压缩流体</li>
<li>用 ping-pong 双缓冲保证每一步读写分离</li>
<li>用纹理对象和连续布局提高 GPU 访问效率</li>
</ol>
<p>如果只记一个最核心的工程关系，那就是：</p>
<blockquote>
<p>平流负责“搬”，压力投影负责“稳”。</p>
</blockquote>
<hr>
<h2 id="20-建议继续补充的相关主题"><a href="#20-建议继续补充的相关主题" class="headerlink" title="20. 建议继续补充的相关主题"></a>20. 建议继续补充的相关主题</h2><p>和本篇衔接最紧密的内容：</p>
<ol>
<li>CUDA texture &#x2F; surface object 细节</li>
<li>Staggered grid（MAC grid）</li>
<li>Jacobi &#x2F; Gauss-Seidel &#x2F; Multigrid 对比</li>
<li>Vorticity confinement 与视觉细节增强</li>
<li>3D 烟雾与体渲染</li>
</ol>
<hr>
<h2 id="21-参考资料"><a href="#21-参考资料" class="headerlink" title="21. 参考资料"></a>21. 参考资料</h2><ol>
<li><p>Jos Stam, Stable Fluids<br><a href="https://www.dgp.toronto.edu/public_user/stam/reality/Research/pdf/GDC03.pdf">https://www.dgp.toronto.edu/public_user/stam/reality/Research/pdf/GDC03.pdf</a></p>
</li>
<li><p>GPU Gems: Fast Fluid Dynamics Simulation on the GPU<br><a href="https://developer.nvidia.com/gpugems/gpugems/part-vi-beyond-triangles/chapter-38-fast-fluid-dynamics-simulation-gpu">https://developer.nvidia.com/gpugems/gpugems/part-vi-beyond-triangles/chapter-38-fast-fluid-dynamics-simulation-gpu</a></p>
</li>
</ol>
]]></content>
      <categories>
        <category>学习</category>
        <category>高性能C++并行编程</category>
      </categories>
  </entry>
  <entry>
    <title>盲超分</title>
    <url>/2025/11/28/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/%E8%B6%85%E5%88%86/%E7%9B%B2%E8%B6%85%E5%88%86/</url>
    <content><![CDATA[<h1 id="盲超分"><a href="#盲超分" class="headerlink" title="盲超分"></a>盲超分</h1><p>退化核？<br>综述：“看清”世界——从盲超分到真实超分 <a href="https://zhuanlan.zhihu.com/p/657729852">https://zhuanlan.zhihu.com/p/657729852</a></p>
<p>自监督：<a href="https://ieeexplore.ieee.org/document/10868396">https://ieeexplore.ieee.org/document/10868396</a><br>自监督：单一遥感高光谱图像的盲超分辨率<a href="https://github.com/zhiyuan0112/TSBSR">https://github.com/zhiyuan0112/TSBSR</a></p>
]]></content>
      <categories>
        <category>深度学习</category>
        <category>超分</category>
      </categories>
      <tags>
        <tag>超分辨率</tag>
        <tag>盲超分</tag>
        <tag>论文笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>Masked Autoencoders Are Scalable Vision Learners</title>
    <url>/2025/12/09/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/%E6%89%A9%E6%95%A3/%5B%E4%BD%95%E6%81%BA%E6%98%8E%5DMasked%20Autoencoders%20Are%20Scalable%20Vision%20Learners/</url>
    <content><![CDATA[<h1 id="Masked-Autoencoders-Are-Scalable-Vision-Learners"><a href="#Masked-Autoencoders-Are-Scalable-Vision-Learners" class="headerlink" title="Masked Autoencoders Are Scalable Vision Learners"></a>Masked Autoencoders Are Scalable Vision Learners</h1><p>论文地址：<a href="https://arxiv.org/pdf/2111.06377%E3%80%82">https://arxiv.org/pdf/2111.06377。</a><br>作者：何凯明<br>机构: Facebook</p>
<h2 id="摘要"><a href="#摘要" class="headerlink" title="摘要"></a>摘要</h2><p>带掩码的自编码器是一个可拓展的视觉学习器<br>MAE基于两个核心设计，可以高效的训练大模型：<br>1、非对称的encoder-decoder架构编码器只作用在可见的patch中，对于丢掉的patch，编码器不会对它进行编码，这样能够节省一定的计算时间。解码器是一个比较轻量的解码器，拿到编码器的输出之后，重构被遮挡住的块。<br>2、使用较高的mask比例将mask比例设置为75%，迫使模型得到一个较好的自监督训练效果。如果只是遮住几块的话，只需要进行插值就可以出来了，模型可能学不到特别的东西。编码器只编码1&#x2F;4大小的图片，可以降低计算量，训练加速3倍或者以上模型结构。最终作者只使用一个最简单的ViT-Huge模型（模型结构来自ViT这篇文章），在ImageNet-1K（100万张图片）上预训练，准确率能达到87.8%；这个模型主要是用来做迁移学习，它证明了在迁移学习的其他任务上表现也非常好注：在ViT这篇文章的最后一段有提到过怎样做自监督学习，作者说效果并不是很好，结论是：用有标号的模型和大的训练集可以得到很好的效果。本文亮点是，只使用小的数据集（ImageNet-1k，100万张图片），而且是通过自监督，就能够做到跟之前可能效果一样好的模型了。</p>
<h2 id="网络结构"><a href="#网络结构" class="headerlink" title="网络结构"></a>网络结构</h2><p><img src="/img/notes/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/%E6%89%A9%E6%95%A3/%E5%9B%BE%E5%BA%93/MAE/%E7%BD%91%E7%BB%9C%E7%BB%93%E6%9E%84.png" alt="网络图"></p>
]]></content>
      <categories>
        <category>深度学习</category>
        <category>扩散</category>
      </categories>
      <tags>
        <tag>MAE</tag>
        <tag>自监督学习</tag>
        <tag>Vision Transformer</tag>
      </tags>
  </entry>
  <entry>
    <title>让扩散模型回归“去噪”本质</title>
    <url>/2025/11/21/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/%E6%89%A9%E6%95%A3/%E4%BD%95%E5%87%AF%E6%98%8E%E8%AE%A9%E6%89%A9%E6%95%A3%E6%A8%A1%E5%9E%8B%E5%9B%9E%E5%BD%92%E2%80%9C%E5%8E%BB%E5%99%AA%E2%80%9D%E6%9C%AC%E8%B4%A8/</url>
    <content><![CDATA[<h1 id="让扩散模型回归“去噪”本质"><a href="#让扩散模型回归“去噪”本质" class="headerlink" title="让扩散模型回归“去噪”本质"></a>让扩散模型回归“去噪”本质</h1><p>论文标题: Back to Basics: Let Denoising Generative Models Denoise<br>作者: Tianhong Li(黎天鸿), Kaiming He(何恺明)<br>机构: 麻省理工学院 (MIT)<br>论文地址: <a href="https://arxiv.org/abs/2511.13720">https://arxiv.org/abs/2511.13720</a><br>代码仓库: <a href="https://github.com/LTH14/JiT">https://github.com/LTH14/JiT</a><br>总结：本文指出了一个反常识问题，就是传统的去噪模型每步预测的唯噪声，而噪声在高维度中的分布是很难观察的，但是如果将预测噪声改为预测真实纯洁的图像就能改变这一问题。</p>
]]></content>
      <categories>
        <category>深度学习</category>
        <category>扩散</category>
      </categories>
      <tags>
        <tag>论文笔记</tag>
        <tag>扩散模型</tag>
        <tag>生成模型</tag>
      </tags>
  </entry>
  <entry>
    <title>Chunk 学习笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/chunk%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="Chunk-学习笔记"><a href="#Chunk-学习笔记" class="headerlink" title="Chunk 学习笔记"></a>Chunk 学习笔记</h1><h2 id="1-什么是-Chunk"><a href="#1-什么是-Chunk" class="headerlink" title="1. 什么是 Chunk"></a>1. 什么是 Chunk</h2><p>在 embedding、检索和 RAG 场景里，<code>chunk</code> 指的是把原始文档切成一段一段可独立编码、可独立检索的小文本单元。</p>
<p>对应的过程通常叫：</p>
<ul>
<li>chunking</li>
<li>文本切块</li>
<li>文档切分</li>
</ul>
<p>可以先把 chunk 看成检索流程里的最小工作单元。它不是随便切一刀，而是既要保留语义完整性，也要方便后续编码和检索。</p>
<hr>
<h2 id="2-为什么要做-Chunk"><a href="#2-为什么要做-Chunk" class="headerlink" title="2. 为什么要做 Chunk"></a>2. 为什么要做 Chunk</h2><p>原始文档通常不适合整篇直接送去做 embedding 或检索，原因一般有三个：</p>
<ul>
<li>太长，超过模型输入限制</li>
<li>粒度太粗，检索结果不精准</li>
<li>一篇文档往往包含多个主题，整篇编码会让语义混在一起</li>
</ul>
<p>切块的目的，是让每一块：</p>
<ul>
<li>尽量只表达一个相对完整的意思</li>
<li>长度不要太长，避免语义被稀释</li>
<li>长度也不要太短，避免上下文不足</li>
</ul>
<hr>
<h2 id="3-Chunk-对检索效果为什么这么关键"><a href="#3-Chunk-对检索效果为什么这么关键" class="headerlink" title="3. Chunk 对检索效果为什么这么关键"></a>3. Chunk 对检索效果为什么这么关键</h2><p>检索效果不好时，很多人的第一反应是换 embedding 模型，但工程里更常见的问题其实出在 chunk 策略。</p>
<p>实际项目里，chunk 策略会直接影响：</p>
<ul>
<li>向量质量</li>
<li>召回命中率</li>
<li>rerank 效果</li>
<li>最终 LLM 回答质量</li>
</ul>
<p>常见问题包括：</p>
<ul>
<li>chunk 太大，相关信息被埋在长文本里，召回不准</li>
<li>chunk 太碎，单块语义不完整，召回上来后也不好用</li>
<li>切断了标题和正文的关系，导致上下文丢失</li>
<li>overlap 设置不合理，重复太多或连续性不够</li>
</ul>
<p>所以可以把它理解成：</p>
<blockquote>
<p>embedding 决定“怎么表示”，chunking 决定“切得对不对”。</p>
</blockquote>
<hr>
<h2 id="4-一个好-Chunk-的目标"><a href="#4-一个好-Chunk-的目标" class="headerlink" title="4. 一个好 Chunk 的目标"></a>4. 一个好 Chunk 的目标</h2><p>好的 chunk 通常要满足这几个条件：</p>
<ul>
<li>语义尽量完整</li>
<li>长度尽量稳定</li>
<li>有明确边界</li>
<li>便于加标题、来源、页码等元数据</li>
<li>便于后续召回、去重、引用和展示</li>
</ul>
<p>如果一个 chunk 满足下面任意一种情况，通常都说明切得不够好：</p>
<ul>
<li>只有一句残缺的话</li>
<li>标题和正文被拆开</li>
<li>一个 chunk 同时塞了多个不相关主题</li>
<li>文本大部分是模板、导航、页脚、噪声内容</li>
</ul>
<hr>
<h2 id="5-常见的-Chunk-策略"><a href="#5-常见的-Chunk-策略" class="headerlink" title="5. 常见的 Chunk 策略"></a>5. 常见的 Chunk 策略</h2><h3 id="5-1-固定长度切块"><a href="#5-1-固定长度切块" class="headerlink" title="5.1 固定长度切块"></a>5.1 固定长度切块</h3><p>例如每 <code>300</code> 字、每 <code>500</code> tokens 切一块。</p>
<p>优点：</p>
<ul>
<li>简单</li>
<li>稳定</li>
<li>容易批量处理</li>
</ul>
<p>缺点：</p>
<ul>
<li>可能把完整段落或完整论点切断</li>
</ul>
<p>适合：</p>
<ul>
<li>初版 baseline</li>
<li>结构不稳定的原始文本</li>
</ul>
<h3 id="5-2-按段落切块"><a href="#5-2-按段落切块" class="headerlink" title="5.2 按段落切块"></a>5.2 按段落切块</h3><p>按照自然段来切。</p>
<p>优点：</p>
<ul>
<li>语义边界自然</li>
<li>可读性通常更好</li>
</ul>
<p>缺点：</p>
<ul>
<li>长度不稳定</li>
<li>某些段落可能过长或过短</li>
</ul>
<p>适合：</p>
<ul>
<li>普通说明文档</li>
<li>笔记</li>
<li>博客文章</li>
</ul>
<h3 id="5-3-按标题和小节切块"><a href="#5-3-按标题和小节切块" class="headerlink" title="5.3 按标题和小节切块"></a>5.3 按标题和小节切块</h3><p>按照文档层级来切，例如：</p>
<ul>
<li>一级标题</li>
<li>二级标题</li>
<li>小节内容</li>
</ul>
<p>优点：</p>
<ul>
<li>结构清晰</li>
<li>更适合知识库和技术文档</li>
</ul>
<p>缺点：</p>
<ul>
<li>某些小节可能太长，还需要二次细分</li>
</ul>
<p>适合：</p>
<ul>
<li>API 文档</li>
<li>产品文档</li>
<li>教程型内容</li>
</ul>
<h3 id="5-4-混合切块"><a href="#5-4-混合切块" class="headerlink" title="5.4 混合切块"></a>5.4 混合切块</h3><p>先按标题或段落切，再对过长部分按固定长度继续细分。</p>
<p>这是实际项目里很常见的一种方案，因为它兼顾了：</p>
<ul>
<li>语义完整性</li>
<li>长度可控</li>
<li>工程实现难度</li>
</ul>
<hr>
<h2 id="6-什么是-Overlap"><a href="#6-什么是-Overlap" class="headerlink" title="6. 什么是 Overlap"></a>6. 什么是 Overlap</h2><p><code>overlap</code> 是相邻 chunk 之间保留的一部分重复内容。</p>
<p>作用：</p>
<ul>
<li>避免关键信息刚好被切断</li>
<li>提高上下文连续性</li>
<li>减少边界句子丢信息的问题</li>
</ul>
<p>例如：</p>
<ul>
<li>chunk1: 第 <code>1~300</code> 字</li>
<li>chunk2: 第 <code>260~560</code> 字</li>
</ul>
<p>这里就有 <code>40</code> 字重叠。</p>
<p>但 overlap 也不是越大越好。</p>
<p>如果 overlap 太大，会带来：</p>
<ul>
<li>存储冗余</li>
<li>检索结果重复</li>
<li>top-k 被相似 chunk 挤占</li>
</ul>
<p>因此，overlap 要处理的是下面这组平衡：</p>
<ul>
<li>上下文连续性</li>
<li>检索去重成本</li>
</ul>
<hr>
<h2 id="7-Chunk-Size-怎么定"><a href="#7-Chunk-Size-怎么定" class="headerlink" title="7. Chunk Size 怎么定"></a>7. Chunk Size 怎么定</h2><p>chunk 大小没有全场景通用的固定答案，应该结合下面几件事一起看：</p>
<ul>
<li>文档类型</li>
<li>模型最大输入长度</li>
<li>用户 query 的粒度</li>
<li>是否有 rerank</li>
<li>LLM 最终要吃多少上下文</li>
</ul>
<h3 id="7-1-中文场景一个常见起点"><a href="#7-1-中文场景一个常见起点" class="headerlink" title="7.1 中文场景一个常见起点"></a>7.1 中文场景一个常见起点</h3><p>如果是中文知识库、普通笔记或教程类文本，可以先从下面这个 baseline 开始：</p>
<ul>
<li>每块 <code>300~500</code> 字</li>
<li>overlap 取 <code>10%~20%</code></li>
<li>优先按标题和段落切，超长再二次切分</li>
</ul>
<p>这不是固定标准，但很适合作为第一版默认策略。</p>
<h3 id="7-2-用-token-还是字数"><a href="#7-2-用-token-还是字数" class="headerlink" title="7.2 用 token 还是字数"></a>7.2 用 token 还是字数</h3><p>更严格的工程实现通常按 token 控制，因为模型真正受限的是 token 数。</p>
<p>但在中文笔记场景里：</p>
<ul>
<li>用字数做初版规则更直观</li>
<li>用 token 做最终线上控制更稳妥</li>
</ul>
<hr>
<h2 id="8-不同文档类型怎么切"><a href="#8-不同文档类型怎么切" class="headerlink" title="8. 不同文档类型怎么切"></a>8. 不同文档类型怎么切</h2><h3 id="8-1-FAQ"><a href="#8-1-FAQ" class="headerlink" title="8.1 FAQ"></a>8.1 FAQ</h3><p>优先按一问一答切。</p>
<p>原因：</p>
<ul>
<li>每条 FAQ 本身就是天然语义单元</li>
<li>非常适合检索</li>
</ul>
<h3 id="8-2-API-文档"><a href="#8-2-API-文档" class="headerlink" title="8.2 API 文档"></a>8.2 API 文档</h3><p>优先按接口、参数、返回值、错误码分块。</p>
<p>原因：</p>
<ul>
<li>用户检索时往往是精确问题</li>
<li>结构化切分更利于命中</li>
</ul>
<h3 id="8-3-教程和博客"><a href="#8-3-教程和博客" class="headerlink" title="8.3 教程和博客"></a>8.3 教程和博客</h3><p>优先按标题和段落切，再对过长部分二次细分。</p>
<p>原因：</p>
<ul>
<li>这类文档有明显章节结构</li>
<li>语义常围绕小节展开</li>
</ul>
<h3 id="8-4-法规、合同、制度文档"><a href="#8-4-法规、合同、制度文档" class="headerlink" title="8.4 法规、合同、制度文档"></a>8.4 法规、合同、制度文档</h3><p>优先按条款、章节切，再保留章节编号。</p>
<p>原因：</p>
<ul>
<li>编号本身很重要</li>
<li>后续引用和回溯都依赖这些元数据</li>
</ul>
<hr>
<h2 id="9-Chunk-不只是文本，还要带元数据"><a href="#9-Chunk-不只是文本，还要带元数据" class="headerlink" title="9. Chunk 不只是文本，还要带元数据"></a>9. Chunk 不只是文本，还要带元数据</h2><p>实际工程里，向量库里存的往往不只是 chunk 文本本身，还包括一组元数据。</p>
<p>常见字段：</p>
<ul>
<li><code>chunk_id</code></li>
<li><code>doc_id</code></li>
<li><code>title</code></li>
<li><code>section</code></li>
<li><code>source</code></li>
<li><code>page</code></li>
<li><code>url</code></li>
<li><code>create_time</code></li>
</ul>
<p>这些元数据很重要，因为它们会影响：</p>
<ul>
<li>过滤</li>
<li>去重</li>
<li>排序</li>
<li>页面展示</li>
<li>来源追踪</li>
</ul>
<p>一句话说：</p>
<blockquote>
<p>没有元数据的 chunk，很难在真实系统里长期维护。</p>
</blockquote>
<hr>
<h2 id="10-Chunk-怎么评估好不好"><a href="#10-Chunk-怎么评估好不好" class="headerlink" title="10. Chunk 怎么评估好不好"></a>10. Chunk 怎么评估好不好</h2><p>chunk 没有一个单独的万能评分，通常要放到检索链路里一起评估。</p>
<p>最常见的评估方式有：</p>
<ul>
<li>看 Recall@K 是否提升</li>
<li>看 badcase 是否减少</li>
<li>看召回结果是否更少重复</li>
<li>看 LLM 最终回答是否更稳定</li>
</ul>
<p>排查时，通常先看这几个问题：</p>
<ol>
<li>召回不到，是不是 chunk 太大</li>
<li>召回到了但答不好，是不是 chunk 太碎</li>
<li>top-k 很重复，是不是 overlap 太大</li>
<li>标题命中了但正文没进来，是不是标题和内容被拆开了</li>
</ol>
<hr>
<h2 id="11-常见坑"><a href="#11-常见坑" class="headerlink" title="11. 常见坑"></a>11. 常见坑</h2><h3 id="11-1-只按固定长度切，不看语义边界"><a href="#11-1-只按固定长度切，不看语义边界" class="headerlink" title="11.1 只按固定长度切，不看语义边界"></a>11.1 只按固定长度切，不看语义边界</h3><p>这样最容易把一句完整的话切断。</p>
<h3 id="11-2-标题和正文分离"><a href="#11-2-标题和正文分离" class="headerlink" title="11.2 标题和正文分离"></a>11.2 标题和正文分离</h3><p>很多技术文档里，标题本身就是关键检索信号，和正文拆开后效果通常会明显变差。</p>
<h3 id="11-3-chunk-太碎"><a href="#11-3-chunk-太碎" class="headerlink" title="11.3 chunk 太碎"></a>11.3 chunk 太碎</h3><p>如果一个 chunk 只剩一句短句，虽然向量容易算，但语义往往不完整。</p>
<h3 id="11-4-chunk-太大"><a href="#11-4-chunk-太大" class="headerlink" title="11.4 chunk 太大"></a>11.4 chunk 太大</h3><p>如果一块里塞进多个主题，检索时很容易语义发散。</p>
<h3 id="11-5-overlap-太大"><a href="#11-5-overlap-太大" class="headerlink" title="11.5 overlap 太大"></a>11.5 overlap 太大</h3><p>会导致召回结果里出现很多几乎一样的片段，影响 top-k 质量。</p>
<h3 id="11-6-没做清洗就直接切"><a href="#11-6-没做清洗就直接切" class="headerlink" title="11.6 没做清洗就直接切"></a>11.6 没做清洗就直接切</h3><p>页眉、页脚、导航栏、版权信息、空白符噪声，都会污染 chunk 质量。</p>
<hr>
<h2 id="12-一个实用的默认方案"><a href="#12-一个实用的默认方案" class="headerlink" title="12. 一个实用的默认方案"></a>12. 一个实用的默认方案</h2><p>如果你是第一次给中文知识库做 chunk，可以先这样做：</p>
<ol>
<li>先清洗无关噪声</li>
<li>按标题和段落切</li>
<li>过长段落按 <code>300~500</code> 字继续切</li>
<li>overlap 先取 <code>10%~15%</code></li>
<li>保留 <code>doc_id</code>、<code>title</code>、<code>section</code>、<code>source</code></li>
<li>离线抽样检查 top-k 检索效果</li>
<li>根据 badcase 再回调 chunk size 和 overlap</li>
</ol>
<p>这套方案不是最优，但通常足够当第一版生产 baseline。</p>
<hr>
<h2 id="13-Chunk、Embedding、RAG-的关系"><a href="#13-Chunk、Embedding、RAG-的关系" class="headerlink" title="13. Chunk、Embedding、RAG 的关系"></a>13. Chunk、Embedding、RAG 的关系</h2><p>可以把三者简单理解成：</p>
<ul>
<li>chunking 决定“怎么切”</li>
<li>embedding 决定“怎么表示”</li>
<li>retrieval &#x2F; rerank 决定“怎么找”</li>
<li>LLM 决定“怎么答”</li>
</ul>
<p>如果 chunk 切得不对，后面的 embedding、rerank、LLM 往往都只能被动补救。</p>
<hr>
<h2 id="14-一句话总结"><a href="#14-一句话总结" class="headerlink" title="14. 一句话总结"></a>14. 一句话总结</h2><p>Chunk 的核心不是“把文档切小”，而是：</p>
<blockquote>
<p>把文档切成既适合向量表示、又适合检索召回、还能支撑最终回答的语义单元。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>句子嵌入模型</category>
      </categories>
  </entry>
  <entry>
    <title>LoRA 微调训练笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/lora%E5%BE%AE%E8%B0%83%E8%AE%AD%E7%BB%83/</url>
    <content><![CDATA[<h1 id="LoRA-微调训练笔记"><a href="#LoRA-微调训练笔记" class="headerlink" title="LoRA 微调训练笔记"></a>LoRA 微调训练笔记</h1><h2 id="1-什么是-LoRA"><a href="#1-什么是-LoRA" class="headerlink" title="1. 什么是 LoRA"></a>1. 什么是 LoRA</h2><p>LoRA（Low-Rank Adaptation）是一种参数高效微调方法。</p>
<p>它的核心思路不是更新整个基座模型，而是：</p>
<ul>
<li>冻结原始预训练权重</li>
<li>只在部分线性层旁边插入可训练的低秩矩阵</li>
<li>训练时只更新这部分新增参数</li>
</ul>
<p>对句子嵌入模型来说，LoRA 特别适合下面几类场景：</p>
<ul>
<li>显存有限，无法稳定做全量微调</li>
<li>想快速做领域适配，例如法律、医疗、电商、代码检索</li>
<li>希望保留同一个基座模型，同时维护多个领域 adapter</li>
<li>想减少模型发布体积，只保存 adapter 权重</li>
</ul>
<p>可以先把 LoRA 看成一种轻量改造：不大动原来的基座模型，只额外训练一小部分增量参数。和全量微调相比，它更省显存，也更适合快速做领域适配。</p>
<hr>
<h2 id="2-LoRA-的算法原理"><a href="#2-LoRA-的算法原理" class="headerlink" title="2. LoRA 的算法原理"></a>2. LoRA 的算法原理</h2><p>假设原始线性层权重是 <code>W</code>，全量微调要学的是一个完整的权重更新量 <code>ΔW</code>：</p>
<p><code>W&#39; = W + ΔW</code></p>
<p>LoRA 认为这个更新量通常可以被一个低秩分解近似表示：</p>
<p><code>ΔW ≈ sBA</code></p>
<p>其中：</p>
<ul>
<li><code>A</code> 的形状通常可写作 <code>r x d_in</code></li>
<li><code>B</code> 的形状通常可写作 <code>d_out x r</code></li>
<li><code>r</code> 是秩，远小于 <code>d_in</code> 和 <code>d_out</code></li>
<li><code>s</code> 是缩放因子，常见写法是 <code>alpha / r</code></li>
</ul>
<p>因此 LoRA 后的线性层可写成：</p>
<p><code>W&#39; = W + sBA</code></p>
<p>训练时：</p>
<ul>
<li><code>W</code> 冻结不动</li>
<li>只训练 <code>A</code> 和 <code>B</code></li>
</ul>
<p>这样做的直接收益是：</p>
<ul>
<li>可训练参数从 <code>d_out x d_in</code> 降到 <code>r x (d_in + d_out)</code></li>
<li>反向传播只覆盖 LoRA 参数，显存占用更低</li>
<li>一个基座模型可以挂多个 adapter，便于多任务和多领域切换</li>
</ul>
<p>常见初始化方式还会让 <code>B</code> 初始为 0，这样刚插入 LoRA 时输出和原模型基本一致，训练过程就相当于逐步学出一个增量更新。</p>
<hr>
<h2 id="3-LoRA-在句子嵌入训练中的位置"><a href="#3-LoRA-在句子嵌入训练中的位置" class="headerlink" title="3. LoRA 在句子嵌入训练中的位置"></a>3. LoRA 在句子嵌入训练中的位置</h2><p>LoRA 只改变“哪些参数参与训练”，但不会改变 embedding 任务本身。</p>
<p>通常不变的东西有：</p>
<ul>
<li>训练数据格式</li>
<li>loss 设计</li>
<li>pooling 方式</li>
<li>向量归一化策略</li>
<li>评估指标</li>
</ul>
<p>真正变化的是：</p>
<ul>
<li>原来训练全部 encoder 参数</li>
<li>现在只训练插入到 attention 或线性层里的 LoRA 参数</li>
</ul>
<p>所以如果你原本是这样训练 embedding：</p>
<ul>
<li><code>query-positive</code> 对配合对比学习</li>
<li><code>query-positive-negative</code> 三元组配合 triplet loss</li>
<li>STS 打分数据配合回归 loss</li>
</ul>
<p>换成 LoRA 后，训练主流程通常不用重写，主要改的是参数更新范围。</p>
<hr>
<h2 id="4-一个适合-embedding-的-LoRA-训练流程"><a href="#4-一个适合-embedding-的-LoRA-训练流程" class="headerlink" title="4. 一个适合 embedding 的 LoRA 训练流程"></a>4. 一个适合 embedding 的 LoRA 训练流程</h2><p>检索类句子嵌入任务里，比较实用的一条路线通常是：</p>
<ol>
<li>选一个已有 embedding 基座，例如 BGE、E5、GTE、MiniLM、MPNet</li>
<li>保持原有文本预处理、prompt 前缀、pooling 逻辑不变</li>
<li>给编码器的部分线性层插入 LoRA</li>
<li>用 <code>query-positive</code> 或 <code>query-positive-negative</code> 数据做对比学习</li>
<li>优先使用 batch 内负样本，再配合 hard negative</li>
<li>用 Recall@K、MRR、NDCG 做离线评估</li>
<li>只保存 adapter，部署时加载到相同基座模型上</li>
<li>如有需要再把 LoRA 权重 merge 回基座权重</li>
</ol>
<p>关键点要记住：</p>
<ul>
<li>LoRA 解决的是“训练成本”问题</li>
<li>数据质量、难例挖掘、线上预处理一致性，依然决定最终效果</li>
</ul>
<hr>
<h2 id="5-LoRA-训练时重点关注哪些超参数"><a href="#5-LoRA-训练时重点关注哪些超参数" class="headerlink" title="5. LoRA 训练时重点关注哪些超参数"></a>5. LoRA 训练时重点关注哪些超参数</h2><h3 id="5-1-r（rank）"><a href="#5-1-r（rank）" class="headerlink" title="5.1 r（rank）"></a>5.1 <code>r</code>（rank）</h3><ul>
<li>决定低秩矩阵容量</li>
<li>常见起点：<code>8 / 16 / 32 / 64</code></li>
<li><code>r</code> 越大，表达能力越强，但参数量和显存也会上升</li>
</ul>
<h3 id="5-2-lora-alpha"><a href="#5-2-lora-alpha" class="headerlink" title="5.2 lora_alpha"></a>5.2 <code>lora_alpha</code></h3><ul>
<li>决定 LoRA 更新量的缩放强度</li>
<li>常见会设置成 <code>r</code> 的 2 倍或 4 倍</li>
</ul>
<h3 id="5-3-lora-dropout"><a href="#5-3-lora-dropout" class="headerlink" title="5.3 lora_dropout"></a>5.3 <code>lora_dropout</code></h3><ul>
<li>常见取值：<code>0.0 ~ 0.1</code></li>
<li>数据量较小或容易过拟合时，可以适当加一点</li>
</ul>
<h3 id="5-4-target-modules"><a href="#5-4-target-modules" class="headerlink" title="5.4 target_modules"></a>5.4 <code>target_modules</code></h3><ul>
<li>BERT &#x2F; MiniLM 一类 encoder，常见目标层是 <code>query</code>、<code>value</code></li>
<li>Llama &#x2F; Qwen 一类 Transformer，常见目标层是 <code>q_proj</code>、<code>v_proj</code></li>
<li>如果不想按架构手写模块名，也可以考虑 <code>all-linear</code></li>
</ul>
<h3 id="5-5-learning-rate"><a href="#5-5-learning-rate" class="headerlink" title="5.5 learning rate"></a>5.5 learning rate</h3><ul>
<li>LoRA 通常可以比全量微调用更大的学习率</li>
<li>工程上常从 <code>1e-4</code> 或 <code>2e-4</code> 开始试，再根据验证集回调</li>
</ul>
<p>此外，embedding 任务原本重要的参数依旧重要：</p>
<ul>
<li>batch size</li>
<li>max length</li>
<li>hard negative 数量</li>
<li>warmup ratio</li>
<li>训练轮数</li>
</ul>
<hr>
<h2 id="6-一个常见实现方式：Sentence-Transformers-PEFT"><a href="#6-一个常见实现方式：Sentence-Transformers-PEFT" class="headerlink" title="6. 一个常见实现方式：Sentence Transformers + PEFT"></a>6. 一个常见实现方式：Sentence Transformers + PEFT</h2><p>如果本来就在做句子嵌入，最直接的工程组合通常是：</p>
<ul>
<li><code>Sentence Transformers</code> 负责 embedding 训练流程</li>
<li><code>PEFT</code> 负责 LoRA adapter 注入与保存</li>
</ul>
<p>一个常见写法如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> datasets <span class="keyword">import</span> load_dataset</span><br><span class="line"><span class="keyword">from</span> peft <span class="keyword">import</span> LoraConfig, TaskType</span><br><span class="line"><span class="keyword">from</span> sentence_transformers <span class="keyword">import</span> (</span><br><span class="line">    SentenceTransformer,</span><br><span class="line">    SentenceTransformerTrainer,</span><br><span class="line">    SentenceTransformerTrainingArguments,</span><br><span class="line">)</span><br><span class="line"><span class="keyword">from</span> sentence_transformers.losses <span class="keyword">import</span> MultipleNegativesRankingLoss</span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 选择基座模型</span></span><br><span class="line">model = SentenceTransformer(<span class="string">&quot;BAAI/bge-small-zh-v1.5&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 配置 LoRA</span></span><br><span class="line">peft_config = LoraConfig(</span><br><span class="line">    task_type=TaskType.FEATURE_EXTRACTION,</span><br><span class="line">    inference_mode=<span class="literal">False</span>,</span><br><span class="line">    r=<span class="number">32</span>,</span><br><span class="line">    lora_alpha=<span class="number">64</span>,</span><br><span class="line">    lora_dropout=<span class="number">0.05</span>,</span><br><span class="line">    target_modules=[<span class="string">&quot;query&quot;</span>, <span class="string">&quot;value&quot;</span>],</span><br><span class="line">)</span><br><span class="line">model.add_adapter(peft_config)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 准备数据</span></span><br><span class="line">train_dataset = load_dataset(<span class="string">&quot;json&quot;</span>, data_files=<span class="string">&quot;train.jsonl&quot;</span>, split=<span class="string">&quot;train&quot;</span>)</span><br><span class="line">eval_dataset = load_dataset(<span class="string">&quot;json&quot;</span>, data_files=<span class="string">&quot;dev.jsonl&quot;</span>, split=<span class="string">&quot;train&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 定义 loss</span></span><br><span class="line">loss = MultipleNegativesRankingLoss(model)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 训练参数</span></span><br><span class="line">args = SentenceTransformerTrainingArguments(</span><br><span class="line">    output_dir=<span class="string">&quot;outputs/bge-small-zh-lora&quot;</span>,</span><br><span class="line">    num_train_epochs=<span class="number">1</span>,</span><br><span class="line">    per_device_train_batch_size=<span class="number">128</span>,</span><br><span class="line">    per_device_eval_batch_size=<span class="number">128</span>,</span><br><span class="line">    learning_rate=<span class="number">1e-4</span>,</span><br><span class="line">    warmup_ratio=<span class="number">0.1</span>,</span><br><span class="line">    fp16=<span class="literal">True</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 6. 启动训练</span></span><br><span class="line">trainer = SentenceTransformerTrainer(</span><br><span class="line">    model=model,</span><br><span class="line">    args=args,</span><br><span class="line">    train_dataset=train_dataset,</span><br><span class="line">    eval_dataset=eval_dataset,</span><br><span class="line">    loss=loss,</span><br><span class="line">)</span><br><span class="line">trainer.train()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 7. 保存 adapter</span></span><br><span class="line">model.save_pretrained(<span class="string">&quot;outputs/bge-small-zh-lora/final&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>如果你的数据是检索三元组，也可以把 loss 换成：</p>
<ul>
<li><code>TripletLoss</code></li>
<li><code>CachedMultipleNegativesRankingLoss</code></li>
<li><code>MultipleNegativesRankingLoss</code></li>
</ul>
<p>经验上：</p>
<ul>
<li>检索召回优先，通常先试 <code>MultipleNegativesRankingLoss</code></li>
<li>batch 很大时，可优先考虑 <code>CachedMultipleNegativesRankingLoss</code></li>
<li>有明确三元组时，可尝试 <code>TripletLoss</code></li>
</ul>
<hr>
<h2 id="7-LoRA-层到底做了什么"><a href="#7-LoRA-层到底做了什么" class="headerlink" title="7. LoRA 层到底做了什么"></a>7. LoRA 层到底做了什么</h2><p>如果原来的线性层输出写成：</p>
<p><code>y = Wx</code></p>
<p>那么加上 LoRA 后，输出就变成：</p>
<p><code>y = Wx + sBAx</code></p>
<p>也就是说：</p>
<ul>
<li>主干路径还是原始的 <code>Wx</code></li>
<li>LoRA 只是在旁边额外加了一条低秩增量路径</li>
</ul>
<p>训练完成后，通常有两种部署方式。</p>
<h3 id="7-1-保留-adapter"><a href="#7-1-保留-adapter" class="headerlink" title="7.1 保留 adapter"></a>7.1 保留 adapter</h3><p>优点：</p>
<ul>
<li>体积小</li>
<li>方便多领域切换</li>
<li>基座模型只保留一份</li>
</ul>
<p>缺点：</p>
<ul>
<li>部署时需要同时管理 base model 和 adapter</li>
</ul>
<h3 id="7-2-merge-回基座权重"><a href="#7-2-merge-回基座权重" class="headerlink" title="7.2 merge 回基座权重"></a>7.2 merge 回基座权重</h3><p>优点：</p>
<ul>
<li>部署形态更像普通模型</li>
<li>推理链路更简单</li>
</ul>
<p>缺点：</p>
<ul>
<li>不方便在同一个基座上频繁切换多个 adapter</li>
</ul>
<hr>
<h2 id="8-LoRA-适合什么，不适合什么"><a href="#8-LoRA-适合什么，不适合什么" class="headerlink" title="8. LoRA 适合什么，不适合什么"></a>8. LoRA 适合什么，不适合什么</h2><p>LoRA 更适合：</p>
<ul>
<li>中小规模领域适配</li>
<li>训练资源紧张</li>
<li>想快速验证方向</li>
<li>同一个基座服务多个垂类任务</li>
</ul>
<p>LoRA 不一定最优的情况：</p>
<ul>
<li>域迁移特别大，任务和原始预训练差异非常明显</li>
<li>你追求极限指标，并且训练资源足够</li>
<li>需要同时修改很多非线性模块或特殊组件</li>
</ul>
<p>这时更适合的思路可能是：</p>
<ul>
<li>全量微调</li>
<li>LoRA + 更强数据构造</li>
<li>LoRA 的变体，例如 DoRA、rsLoRA、QLoRA</li>
</ul>
<hr>
<h2 id="9-一份可直接抄的-LoRA-检查清单"><a href="#9-一份可直接抄的-LoRA-检查清单" class="headerlink" title="9. 一份可直接抄的 LoRA 检查清单"></a>9. 一份可直接抄的 LoRA 检查清单</h2><ol>
<li>基座模型是否适合你的语种和场景</li>
<li>训练和推理的文本预处理是否完全一致</li>
<li>pooling 方式是否与原模型保持一致</li>
<li><code>target_modules</code> 是否匹配当前模型架构</li>
<li>loss 是否与任务目标一致</li>
<li>是否准备了足够强的 hard negative</li>
<li>是否有稳定的验证集和 badcase 集</li>
<li>保存的是 adapter，还是 merge 后模型</li>
<li>部署时是否保证 base model 版本完全一致</li>
<li>线上是否监控向量分布、召回率和 badcase</li>
</ol>
<hr>
<h2 id="10-论文、官方文档与博客参考"><a href="#10-论文、官方文档与博客参考" class="headerlink" title="10. 论文、官方文档与博客参考"></a>10. 论文、官方文档与博客参考</h2><p>论文与官方资料：</p>
<ul>
<li>LoRA 原始论文：<a href="https://arxiv.org/abs/2106.09685">https://arxiv.org/abs/2106.09685</a></li>
<li>Hugging Face PEFT LoRA 文档：<a href="https://huggingface.co/docs/peft/en/package_reference/lora">https://huggingface.co/docs/peft/en/package_reference/lora</a></li>
<li>Hugging Face PEFT LoRA 使用指南：<a href="https://huggingface.co/docs/peft/en/developer_guides/lora">https://huggingface.co/docs/peft/en/developer_guides/lora</a></li>
<li>Sentence Transformers PEFT &#x2F; LoRA 文档：<a href="https://www.sbert.net/examples/sentence_transformer/training/peft/README.html">https://www.sbert.net/examples/sentence_transformer/training/peft/README.html</a></li>
<li>Sentence Transformers 官方 LoRA 训练脚本：<a href="https://github.com/huggingface/sentence-transformers/blob/main/examples/sentence_transformer/training/peft/training_gooaq_lora.py">https://github.com/huggingface/sentence-transformers/blob/main/examples/sentence_transformer/training/peft/training_gooaq_lora.py</a></li>
</ul>
<p>博客参考：</p>
<ul>
<li>Hugging Face: Parameter-Efficient Fine-Tuning using PEFT<br><a href="https://huggingface.co/blog/peft">https://huggingface.co/blog/peft</a></li>
<li>Hugging Face: 用 Sentence Transformers v3 训练和微调嵌入模型<br><a href="https://huggingface.co/blog/zh/train-sentence-transformers">https://huggingface.co/blog/zh/train-sentence-transformers</a></li>
<li>Sebastian Raschka: Improving LoRA - Implementing Weight-Decomposed Low-Rank Adaptation (DoRA) from Scratch<br><a href="https://magazine.sebastianraschka.com/p/lora-and-dora-from-scratch">https://magazine.sebastianraschka.com/p/lora-and-dora-from-scratch</a></li>
</ul>
<hr>
<h2 id="11-一句话总结"><a href="#11-一句话总结" class="headerlink" title="11. 一句话总结"></a>11. 一句话总结</h2><p>LoRA 用在句子嵌入模型上，归根到底做的是：</p>
<blockquote>
<p>保留原来的 embedding 训练目标，只把全量参数更新改成低秩增量更新。</p>
</blockquote>
<p>重点依然是数据质量、难例构造和线上一致性。</p>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>句子嵌入模型</category>
      </categories>
  </entry>
  <entry>
    <title>黄山</title>
    <url>/2025/11/16/%E7%94%9F%E6%B4%BB/%E6%97%85%E6%B8%B8%E6%94%BB%E7%95%A5/%E9%BB%84%E5%B1%B1/</url>
    <content><![CDATA[<h1 id="黄山"><a href="#黄山" class="headerlink" title="黄山"></a>黄山</h1><p><a href="https://www.nipic.com/show/50091343.html?utm_source=chatgpt.com"><img src="https://tse4.mm.bing.net/th/id/OIP.a92obHwocDrNicbuQrWrswHaJ1?cb=ucfimg2&pid=Api&ucfimg=1" alt="AI创作图黄山 奇松 山谷 云海 松树 绿色 自然 风景 旅行 摄影 中国 安徽省 地理 生态 宁静 壮丽nipic.com-昵图网"></a></p>
<h2 id="一、整体思路（预算版）"><a href="#一、整体思路（预算版）" class="headerlink" title="一、整体思路（预算版）"></a>一、整体思路（预算版）</h2><ul>
<li><p><strong>出行方式</strong>：广州 → 黄山北 高铁往返，二等座一般单程 520–600 元左右，往返约 1100–1200 元。(<a href="https://trains.ctrip.com/TrainBooking/guangzhounan-huangshan/gaotie/?utm_source=chatgpt.com" title="广州南到黄山高铁查询">携程火车票</a>)</p>
</li>
<li><p><strong>住宿策略</strong>：</p>
<ul>
<li>不住山上，<strong>两晚都住山下（汤口或者屯溪）</strong>，选 100–150&#x2F;晚的经济型酒店&#x2F;客栈。</li>
</ul>
</li>
<li><p><strong>景点选择</strong>：</p>
<ul>
<li>D1–D2：黄山一日半：走索道+步行的轻量版线路</li>
<li>D3：宏村半日 + 黄山市区随便逛，视预算可选</li>
</ul>
</li>
<li><p><strong>控制花费的小技巧</strong>：</p>
<ul>
<li>索道只坐一段（上山坐，视体力下山走部分路）。</li>
<li>山上的水&#x2F;零食尽量自带一些。</li>
<li>宏村可以跟当地一日游&#x2F;拼车，省心也省一点车费。</li>
</ul>
</li>
</ul>
<hr>
<h2 id="二、详细行程规划"><a href="#二、详细行程规划" class="headerlink" title="二、详细行程规划"></a>二、详细行程规划</h2><h3 id="▶-D1：广州-→-黄山北-→-汤口-屯溪（轻松抵达日）"><a href="#▶-D1：广州-→-黄山北-→-汤口-屯溪（轻松抵达日）" class="headerlink" title="▶ D1：广州 → 黄山北 → 汤口&#x2F;屯溪（轻松抵达日）"></a>▶ D1：广州 → 黄山北 → 汤口&#x2F;屯溪（轻松抵达日）</h3><p><strong>上午 &#x2F; 中午</strong></p>
<ul>
<li><p>从 <strong>广州南&#x2F;广州东&#x2F;广州新塘 → 黄山北</strong> 坐高铁，尽量选中午前出发、下午两三点到黄山北的车次（车程约 5 小时）。(<a href="https://tw.trip.com/trains/china/route/guangzhou-to-huangshan/?utm_source=chatgpt.com" title="從廣州到黃山的高鐵預訂、票價及時刻表">Trip.com</a>)</p>
</li>
<li><p>黄山北出站后：</p>
<ul>
<li>若第二天一早上黄山：建议直接去 <strong>汤口镇（黄山南大门）</strong>，大巴或拼车约 1–1.5 小时。</li>
<li>若想市区逛一逛：可以先住 <strong>屯溪老街附近</strong>，晚上吃吃逛逛。</li>
</ul>
</li>
</ul>
<p><strong>下午 &#x2F; 晚上</strong></p>
<ul>
<li>办理入住（控制在 <strong>100–150 元&#x2F;晚</strong> 的快捷酒店&#x2F;客栈）。</li>
<li>在屯溪的话：逛 <strong>屯溪老街</strong>，吃一顿徽菜（毛豆腐、臭鳜鱼、刀板香等）。</li>
<li>在汤口的话：熟悉第二天上山的大巴、售票处位置，早点休息。</li>
</ul>
<blockquote>
<p>建议：预算紧 &amp; 主要目标是黄山 → <strong>D1 晚上住汤口</strong> 会更高效，第二天可以早早上山。</p>
</blockquote>
<hr>
<h3 id="▶-D2：黄山一日游（索道-精华步行，不住山上）"><a href="#▶-D2：黄山一日游（索道-精华步行，不住山上）" class="headerlink" title="▶ D2：黄山一日游（索道 + 精华步行，不住山上）"></a>▶ D2：黄山一日游（索道 + 精华步行，不住山上）</h3><blockquote>
<p>预算版选择：<strong>黄山一日“精华速刷版”</strong>，不上主峰也能感受到黄山特色。</p>
</blockquote>
<p><strong>早上</strong></p>
<ol>
<li><p>早起从酒店 → 汤口新国线换乘中心</p>
</li>
<li><p>买票：</p>
<ul>
<li>景区门票：平旺季 <strong>190 元&#x2F;人</strong>(<a href="https://hf.bendibao.com/tour/2023331/91141.shtm?utm_source=chatgpt.com" title="黄山门票多少钱2025？附索道价格">hf.bendibao.com</a>)</li>
<li>景区大巴：单程 19 元，上下山合计约 38 元。(<a href="https://wap.huangshan.com.cn/hsdetail/ticket/park2017122114525726?addressLabel=04&utm_source=chatgpt.com" title="黄山风景区-门票索道详情">wap.huangshan.com.cn</a>)</li>
</ul>
</li>
<li><p>坐大巴到 <strong>云谷寺（后山口）</strong> → 再坐 <strong>云谷索道上山</strong>，单程 80 元左右（平旺季）。(<a href="https://hf.bendibao.com/tour/2023331/91141.shtm?utm_source=chatgpt.com" title="黄山门票多少钱2025？附索道价格">hf.bendibao.com</a>)</p>
</li>
</ol>
<p><strong>上山后推荐线路（强度中等）：</strong></p>
<ul>
<li>索道上站（白鹅岭） → 始信峰（看奇松、怪石）</li>
<li>北海景区：清凉台、猴子观海 等观景点</li>
<li>喜欢走一点路的：可以往 西海入口 方向走一段，再折返，不进大峡谷深处（省体力）。</li>
<li>中午在北海&#x2F;光明顶附近简单吃点自带干粮或景区快餐。</li>
</ul>
<p><strong>下午下山</strong></p>
<ul>
<li><p>体力不错：</p>
<ul>
<li>从北海 → 白鹅岭 → 步行下到 <strong>云谷寺</strong>（路程较长但一路风景不错）。</li>
</ul>
</li>
<li><p>体力一般：</p>
<ul>
<li>返回云谷索道上站 → 再坐索道下山（再花 1×80 元）。</li>
</ul>
</li>
</ul>
<p>然后：云谷寺 → 大巴 → 汤口 → 回酒店休息。</p>
<blockquote>
<p>不上天都峰&#x2F;莲花峰，可以大幅度降低体力和时间消耗，一天玩得比较从容。</p>
</blockquote>
<hr>
<h3 id="▶-D3：宏村半日游-回广州"><a href="#▶-D3：宏村半日游-回广州" class="headerlink" title="▶ D3：宏村半日游 + 回广州"></a>▶ D3：宏村半日游 + 回广州</h3><p><strong>早上：汤口&#x2F;屯溪 → 宏村</strong></p>
<ul>
<li>从汤口&#x2F;黄山市区报一个去 <strong>宏村的拼车&#x2F;一日游</strong>，或者去汽车站坐车，车程约 1–1.5 小时。</li>
<li>宏村门票：成人约 <strong>90–100 元左右</strong>。(<a href="https://touch.piao.qunar.com/touch/detail.htm?cat=from_area=ts_sku&from_index=4&from_value=%25E5%2590%2588%25E8%2582%25A5%25E5%25BF%2585%25E6%25B8%25B8&dist_city=%25E6%25B7%25AE%25E5%258C%2597&id=11072&utm_source=chatgpt.com" title="宏村景区门票 - 景点门票预订- 去哪儿网">去哪儿网</a>)</li>
</ul>
<p><strong>宏村游玩重点：</strong></p>
<ul>
<li>南湖、月沼：徽派建筑 + 水面倒影，拍照很好看。</li>
<li>巷子里慢慢走走，找几家咖啡&#x2F;小吃店坐坐，体验古村氛围。</li>
</ul>
<p><strong>下午</strong></p>
<ul>
<li>午后从宏村 → 返回黄山北 &#x2F; 屯溪。</li>
<li>预留足够时间，傍晚或晚上坐高铁 <strong>黄山北 → 广州</strong>。</li>
</ul>
<hr>
<h2 id="三、人均费用粗算（按“2000-左右”来配）"><a href="#三、人均费用粗算（按“2000-左右”来配）" class="headerlink" title="三、人均费用粗算（按“2000 左右”来配）"></a>三、人均费用粗算（按“2000 左右”来配）</h2><blockquote>
<p>下表是<strong>大致估算</strong>，实际以你订到的车票&#x2F;酒店为准。</p>
</blockquote>
<h3 id="1-大头：往返高铁"><a href="#1-大头：往返高铁" class="headerlink" title="1. 大头：往返高铁"></a>1. 大头：往返高铁</h3><ul>
<li>广州 → 黄山北 二等座：约 520–600 元</li>
<li>黄山北 → 广州 二等座：约 520–600 元<br>👉 合计按 <strong>1100–1200 元</strong> 预估。(<a href="https://trains.ctrip.com/TrainBooking/guangzhounan-huangshan/gaotie/?utm_source=chatgpt.com" title="广州南到黄山高铁查询">携程火车票</a>)</li>
</ul>
<h3 id="2-景区门票-索道-大巴"><a href="#2-景区门票-索道-大巴" class="headerlink" title="2. 景区门票 + 索道 + 大巴"></a>2. 景区门票 + 索道 + 大巴</h3><ul>
<li><p>黄山门票：190</p>
</li>
<li><p>黄山景区大巴：38（往返）</p>
</li>
<li><p>索道：</p>
<ul>
<li>只坐上行一次：80</li>
<li>若上下都坐：160</li>
</ul>
</li>
<li><p>宏村门票：约 94（按网上成人价估算）(<a href="https://touch.piao.qunar.com/touch/detail.htm?cat=from_area=ts_sku&from_index=4&from_value=%25E5%2590%2588%25E8%2582%25A5%25E5%25BF%2585%25E6%25B8%25B8&dist_city=%25E6%25B7%25AE%25E5%258C%2597&id=11072&utm_source=chatgpt.com" title="宏村景区门票 - 景点门票预订- 去哪儿网">去哪儿网</a>)</p>
</li>
</ul>
<p><strong>控制版（只坐单程索道）合计：</strong><br><code>190 + 38 + 80 + 94 ≈ 402 元</code></p>
<p>如果你觉得体力一般，上下都坐索道：<br><code>190 + 38 + 160 + 94 ≈ 482 元</code></p>
<h3 id="3-住宿"><a href="#3-住宿" class="headerlink" title="3. 住宿"></a>3. 住宿</h3><ul>
<li>D1：汤口&#x2F;屯溪经济型酒店 100–150</li>
<li>D2：同上，再 100–150<br>👉 住宿预估：<strong>200–300 元</strong></li>
</ul>
<h3 id="4-吃饭-当地交通"><a href="#4-吃饭-当地交通" class="headerlink" title="4. 吃饭 + 当地交通"></a>4. 吃饭 + 当地交通</h3><ul>
<li>吃饭按 <strong>80–100 元&#x2F;天</strong> 算，3 天 ≈ 240–300 元</li>
<li>市内&#x2F;镇内小交通、拼车到宏村等：预留 <strong>100–150 元</strong>。</li>
</ul>
<hr>
<h3 id="5-总体汇总（偏省钱方案）"><a href="#5-总体汇总（偏省钱方案）" class="headerlink" title="5. 总体汇总（偏省钱方案）"></a>5. 总体汇总（偏省钱方案）</h3><p>按较省的方式估算（高铁 1100、索道只坐一次、酒店便宜点）：</p>
<ul>
<li>高铁：1100</li>
<li>黄山 + 宏村门票&#x2F;车&#x2F;索道：约 400</li>
<li>住宿：约 250</li>
<li>吃饭 + 小交通：约 350</li>
</ul>
<p><strong>合计 ≈ 2100 元左右</strong></p>
<blockquote>
<p>也就是说：<strong>“两千左右”是可以做到的，但会略微紧张</strong>，主要看你高铁能不能抢到稍便宜的车次、酒店选多便宜的、索道坐几次。<br>如果你想更轻松一点（多坐索道、吃好一点），建议心里预留到 <strong>2200–2300</strong> 更舒服。</p>
</blockquote>
<hr>
<h2 id="四、给你一个能直接照抄的简短行程版"><a href="#四、给你一个能直接照抄的简短行程版" class="headerlink" title="四、给你一个能直接照抄的简短行程版"></a>四、给你一个能直接照抄的简短行程版</h2><ul>
<li><strong>D1</strong>：广州 → 黄山北（高铁）→ 汤口，入住；晚上简单逛逛、准备上山物资。</li>
<li><strong>D2</strong>：汤口 → 黄山（云谷索道上 → 北海 → 始信峰 → 光明顶附近 → 视体力部分步行下山&#x2F;坐索道下），晚上回汤口 &#x2F; 屯溪休息。</li>
<li><strong>D3</strong>：早上去宏村（南湖、月沼拍照闲逛），下午返回黄山北，傍晚&#x2F;晚上高铁回广州。</li>
</ul>
<p>如果你愿意告诉我<strong>大概出行月份 + 更偏“省钱”还是“轻松”</strong>，我可以帮你把上面每一天细到：大概几点出门、建议坐的车次时段、黄山具体走哪条步行路线，给你一个“可直接发给同伴&#x2F;直接订票用”的终版行程单。</p>
]]></content>
      <categories>
        <category>生活</category>
        <category>旅游攻略</category>
      </categories>
      <tags>
        <tag>旅游</tag>
        <tag>黄山</tag>
        <tag>攻略</tag>
      </tags>
  </entry>
  <entry>
    <title>句子嵌入模型开发与部署全流程</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/</url>
    <content><![CDATA[<h1 id="句子嵌入模型开发与部署全流程"><a href="#句子嵌入模型开发与部署全流程" class="headerlink" title="句子嵌入模型开发与部署全流程"></a>句子嵌入模型开发与部署全流程</h1><h2 id="补充专题笔记"><a href="#补充专题笔记" class="headerlink" title="补充专题笔记"></a>补充专题笔记</h2><ul>
<li><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/lora%E5%BE%AE%E8%B0%83%E8%AE%AD%E7%BB%83/">LoRA微调训练</a></li>
<li><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/chunk%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/">Chunk学习笔记</a></li>
<li><a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/%E6%95%B0%E6%8D%AE%E5%A4%84%E7%90%86%E4%B8%8E%E6%95%B0%E6%8D%AE%E9%9B%86%E8%AF%84%E4%BC%B0/">数据处理与数据集评估</a></li>
</ul>
<h2 id="1-什么是句子嵌入模型"><a href="#1-什么是句子嵌入模型" class="headerlink" title="1. 什么是句子嵌入模型"></a>1. 什么是句子嵌入模型</h2><p>句子嵌入模型（Sentence Embedding Model）做的事情很直接：</p>
<ul>
<li>把一句话、一个 query、一个标题、一个段落，编码成一个固定长度的向量</li>
<li>让语义相近的句子，在向量空间里距离更近</li>
<li>让语义无关或相反的句子，在向量空间里距离更远</li>
</ul>
<p>它最常见的用途是：</p>
<ul>
<li>语义检索</li>
<li>问答召回</li>
<li>相似句匹配</li>
<li>去重与聚类</li>
<li>推荐系统召回</li>
<li>RAG 检索</li>
</ul>
<p>可以把它理解成一种语义编码器：它把句子的意思压缩成后续可比较、可检索、可排序的向量表示。</p>
<hr>
<h2 id="2-在开发前，先明确业务目标"><a href="#2-在开发前，先明确业务目标" class="headerlink" title="2. 在开发前，先明确业务目标"></a>2. 在开发前，先明确业务目标</h2><p>做句子嵌入模型，第一步不是训练，而是先确定你到底要解决什么问题。</p>
<p>常见目标有三类：</p>
<h3 id="2-1-语义相似度"><a href="#2-1-语义相似度" class="headerlink" title="2.1 语义相似度"></a>2.1 语义相似度</h3><p>例如：</p>
<ul>
<li>“怎么学好 C++”</li>
<li>“如何提升 C++ 学习效果”</li>
</ul>
<p>这类任务关注的是两句话意思是否接近。</p>
<p>适合场景：</p>
<ul>
<li>相似问题匹配</li>
<li>FAQ 匹配</li>
<li>文本去重</li>
</ul>
<h3 id="2-2-检索召回"><a href="#2-2-检索召回" class="headerlink" title="2.2 检索召回"></a>2.2 检索召回</h3><p>例如：</p>
<ul>
<li>用户输入 query</li>
<li>系统从知识库、商品库、帖子库中召回最相关内容</li>
</ul>
<p>适合场景：</p>
<ul>
<li>搜索</li>
<li>RAG</li>
<li>推荐系统第一阶段召回</li>
</ul>
<h3 id="2-3-分类辅助表示"><a href="#2-3-分类辅助表示" class="headerlink" title="2.3 分类辅助表示"></a>2.3 分类辅助表示</h3><p>把句子向量作为下游模型输入，做：</p>
<ul>
<li>分类</li>
<li>聚类</li>
<li>异常检测</li>
</ul>
<p>这三类任务虽然都用 embedding，但数据组织方式、训练目标、评估指标并不完全相同。</p>
<p>所以第一件事是确定：</p>
<ul>
<li>输入是什么</li>
<li>输出要服务什么业务</li>
<li>最重要的指标是什么</li>
<li>延迟、成本、吞吐量限制是多少</li>
</ul>
<h3 id="2-4-为什么做句子嵌入还要懂-BM25"><a href="#2-4-为什么做句子嵌入还要懂-BM25" class="headerlink" title="2.4 为什么做句子嵌入还要懂 BM25"></a>2.4 为什么做句子嵌入还要懂 BM25</h3><p>BM25 是经典的稀疏检索算法，核心还是依赖 query 和 document 的词项匹配来打分。</p>
<p>它主要考虑三件事：</p>
<ul>
<li>某个词在当前文档里出现了多少次</li>
<li>这个词在整个语料库里是否稀有</li>
<li>当前文档是否过长，需要做长度归一化</li>
</ul>
<p>可以把它简单理解成：</p>
<blockquote>
<p>一个词越能代表当前文档、又越不常见，它对相关性分数的贡献就越大。</p>
</blockquote>
<p>BM25 的优点是：</p>
<ul>
<li>不需要训练，冷启动快</li>
<li>对专有名词、品牌名、版本号、报错信息、函数名这类精确词特别敏感</li>
<li>可解释性强，容易分析为什么命中或没命中</li>
</ul>
<p>BM25 的局限是：</p>
<ul>
<li>不擅长理解同义表达</li>
<li>query 和文档不共享关键词时，很难召回</li>
<li>对深层语义和改写表达支持较弱</li>
</ul>
<p>而句子嵌入模型更擅长的是：</p>
<ul>
<li>语义相近但字面不同的表达</li>
<li>口语化 query 和正式文档之间的匹配</li>
<li>跨表述方式的泛化召回</li>
</ul>
<p>所以在搜索、知识库检索、RAG 里，BM25 和 embedding 往往不是替代关系，而是互补关系：</p>
<ul>
<li>BM25 更擅长“字面相关”</li>
<li>embedding 更擅长“语义相关”</li>
</ul>
<p>多数线上系统里，更常见的不是二选一，而是：</p>
<ul>
<li>BM25 召回一批结果</li>
<li>向量检索再召回一批结果</li>
<li>合并、去重、融合</li>
<li>最后交给 rerank 或大模型进一步处理</li>
</ul>
<hr>
<h2 id="3-一个完整的工程流程"><a href="#3-一个完整的工程流程" class="headerlink" title="3. 一个完整的工程流程"></a>3. 一个完整的工程流程</h2><p>句子嵌入模型从立项到上线，通常可以拆成下面几个阶段：</p>
<ol>
<li>定义任务与指标</li>
<li>准备训练数据与评估集</li>
<li>选择基座模型</li>
<li>设计训练目标与样本组织方式</li>
<li>训练与调参</li>
<li>离线评估与 badcase 分析</li>
<li>导出模型并构建推理服务</li>
<li>构建 BM25、向量索引或在线召回链路</li>
<li>灰度上线</li>
<li>监控、回流、持续迭代</li>
</ol>
<p>如果只记一个主线，可以记成：</p>
<blockquote>
<p>先定义业务，再准备数据，再训练，再评估，再部署，再根据线上反馈迭代。</p>
</blockquote>
<hr>
<h2 id="4-数据准备的详细过程"><a href="#4-数据准备的详细过程" class="headerlink" title="4. 数据准备的详细过程"></a>4. 数据准备的详细过程</h2><p>数据准备通常决定了句子嵌入模型最后 70% 以上的效果上限。</p>
<h3 id="4-1-明确训练样本的基本单位"><a href="#4-1-明确训练样本的基本单位" class="headerlink" title="4.1 明确训练样本的基本单位"></a>4.1 明确训练样本的基本单位</h3><p>不同任务，对数据格式的要求不同。</p>
<h4 id="相似度任务"><a href="#相似度任务" class="headerlink" title="相似度任务"></a>相似度任务</h4><p>通常需要句子对：</p>
<ul>
<li><code>sentence_a</code></li>
<li><code>sentence_b</code></li>
<li><code>label</code></li>
</ul>
<p>其中 <code>label</code> 可以是：</p>
<ul>
<li>二分类标签：相似 &#x2F; 不相似</li>
<li>打分标签：0 到 1，或 0 到 5</li>
</ul>
<p>示例：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;sentence_a&quot;</span><span class="punctuation">:</span><span class="string">&quot;怎么学习操作系统&quot;</span><span class="punctuation">,</span><span class="attr">&quot;sentence_b&quot;</span><span class="punctuation">:</span><span class="string">&quot;如何系统学习操作系统&quot;</span><span class="punctuation">,</span><span class="attr">&quot;label&quot;</span><span class="punctuation">:</span><span class="number">1</span><span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;sentence_a&quot;</span><span class="punctuation">:</span><span class="string">&quot;苹果手机怎么截图&quot;</span><span class="punctuation">,</span><span class="attr">&quot;sentence_b&quot;</span><span class="punctuation">:</span><span class="string">&quot;线性代数怎么入门&quot;</span><span class="punctuation">,</span><span class="attr">&quot;label&quot;</span><span class="punctuation">:</span><span class="number">0</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h4 id="检索任务"><a href="#检索任务" class="headerlink" title="检索任务"></a>检索任务</h4><p>通常需要三元组或多元组：</p>
<ul>
<li><code>query</code></li>
<li><code>positive</code></li>
<li><code>negative</code> 或 <code>hard_negative</code></li>
</ul>
<p>示例：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;query&quot;</span><span class="punctuation">:</span><span class="string">&quot;C++ 线程池实现&quot;</span><span class="punctuation">,</span><span class="attr">&quot;positive&quot;</span><span class="punctuation">:</span><span class="string">&quot;一个使用条件变量和任务队列实现线程池的教程&quot;</span><span class="punctuation">,</span><span class="attr">&quot;hard_negative&quot;</span><span class="punctuation">:</span><span class="string">&quot;进程池和线程池的区别说明&quot;</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<h4 id="聚类或无监督任务"><a href="#聚类或无监督任务" class="headerlink" title="聚类或无监督任务"></a>聚类或无监督任务</h4><p>这类任务可以只准备原始文本，但效果往往不如有监督数据稳定。</p>
<h3 id="4-2-数据来源"><a href="#4-2-数据来源" class="headerlink" title="4.2 数据来源"></a>4.2 数据来源</h3><p>句子嵌入模型的数据，常见来源有：</p>
<ul>
<li>搜索日志</li>
<li>点击日志</li>
<li>问答系统日志</li>
<li>FAQ 数据</li>
<li>商品标题和类目</li>
<li>用户查询与文档标题</li>
<li>人工标注数据</li>
<li>现有分类数据集</li>
<li>开源语义匹配数据集</li>
</ul>
<p>工程上最常见的做法是：</p>
<ul>
<li>先用业务日志构造弱监督数据</li>
<li>再配合少量高质量人工标注数据做校正</li>
</ul>
<h3 id="4-3-正样本如何构造"><a href="#4-3-正样本如何构造" class="headerlink" title="4.3 正样本如何构造"></a>4.3 正样本如何构造</h3><p>正样本的核心是：语义上应该接近，并且符合业务目标。</p>
<p>常见构造方式：</p>
<h4 id="来自点击或转化日志"><a href="#来自点击或转化日志" class="headerlink" title="来自点击或转化日志"></a>来自点击或转化日志</h4><p>例如：</p>
<ul>
<li>用户 query -&gt; 点击文档</li>
<li>用户 query -&gt; 点击商品</li>
<li>用户 query -&gt; 最终停留或转化内容</li>
</ul>
<p>这类数据往往最贴近真实业务。</p>
<p>但要注意：</p>
<ul>
<li>点击不一定等于相关</li>
<li>位置偏置会污染标签</li>
<li>热门内容可能被误当作高相关内容</li>
</ul>
<p>所以一般需要加过滤规则，例如：</p>
<ul>
<li>去掉极短停留</li>
<li>去掉误点击</li>
<li>去掉曝光高但满意度低的样本</li>
</ul>
<h4 id="来自同义改写"><a href="#来自同义改写" class="headerlink" title="来自同义改写"></a>来自同义改写</h4><p>例如把一句话做：</p>
<ul>
<li>同义改写</li>
<li>问法改写</li>
<li>标题与正文摘要配对</li>
</ul>
<p>这类方法适合构造句子相似度训练集。</p>
<h4 id="来自结构化业务关系"><a href="#来自结构化业务关系" class="headerlink" title="来自结构化业务关系"></a>来自结构化业务关系</h4><p>例如：</p>
<ul>
<li>问题 -&gt; 标准答案标题</li>
<li>商品标题 -&gt; 标准类目</li>
<li>文档标题 -&gt; 文档摘要</li>
<li>代码问题 -&gt; 解决方案描述</li>
</ul>
<p>如果业务里本身就存在稳定映射关系，这类数据通常质量较高。</p>
<h3 id="4-4-负样本如何构造"><a href="#4-4-负样本如何构造" class="headerlink" title="4.4 负样本如何构造"></a>4.4 负样本如何构造</h3><p>负样本质量直接决定模型区分能力。</p>
<p>负样本通常分三层：</p>
<h4 id="随机负样本"><a href="#随机负样本" class="headerlink" title="随机负样本"></a>随机负样本</h4><p>从语料里随机采样无关文本。</p>
<p>优点：</p>
<ul>
<li>简单</li>
<li>容易构造</li>
</ul>
<p>缺点：</p>
<ul>
<li>太容易</li>
<li>对模型提升有限</li>
</ul>
<h4 id="同域负样本"><a href="#同域负样本" class="headerlink" title="同域负样本"></a>同域负样本</h4><p>从相同主题、相同类目、相同场景里采样不匹配内容。</p>
<p>例如：</p>
<ul>
<li>query 是“C++ 线程池”</li>
<li>负样本不是“美食做法”</li>
<li>而是“C++ 协程原理”</li>
</ul>
<p>这种负样本更接近真实线上干扰项。</p>
<h4 id="困难负样本（Hard-Negative）"><a href="#困难负样本（Hard-Negative）" class="headerlink" title="困难负样本（Hard Negative）"></a>困难负样本（Hard Negative）</h4><p>这是效果提升最关键的一类数据。</p>
<p>所谓困难负样本，就是：</p>
<ul>
<li>表面很像</li>
<li>关键词很接近</li>
<li>但真实标签不匹配</li>
</ul>
<p>例如：</p>
<ul>
<li>query：“Python 多线程和多进程区别”</li>
<li>positive：“Python 中多线程与多进程的对比说明”</li>
<li>hard negative：“Python 线程池使用教程”</li>
</ul>
<p>困难负样本常见构造方式：</p>
<ul>
<li>用 BM25 检索出的高分非正样本</li>
<li>用旧 embedding 模型召回出的相似错例</li>
<li>用交叉编码器打分后筛出的高混淆样本</li>
<li>人工从 badcase 中挑选</li>
</ul>
<p>一句话总结：</p>
<blockquote>
<p>句子嵌入模型真正拉开差距，往往靠的不是更多随机负样本，而是更好的困难负样本。</p>
</blockquote>
<h3 id="4-5-数据清洗"><a href="#4-5-数据清洗" class="headerlink" title="4.5 数据清洗"></a>4.5 数据清洗</h3><p>原始数据通常很脏，必须清洗。</p>
<p>重点包括：</p>
<ul>
<li>去重</li>
<li>去除乱码</li>
<li>去除过短文本</li>
<li>去除极长文本</li>
<li>去除纯模板文本</li>
<li>去除广告、脏词、无意义符号</li>
<li>统一全角半角</li>
<li>统一大小写</li>
<li>统一空白符</li>
</ul>
<p>如果是中文场景，还要注意：</p>
<ul>
<li>简繁统一</li>
<li>标点统一</li>
<li>URL、邮箱、手机号是否脱敏</li>
<li>时间、数字、金额是否要做归一化</li>
</ul>
<h3 id="4-6-数据切分"><a href="#4-6-数据切分" class="headerlink" title="4.6 数据切分"></a>4.6 数据切分</h3><p>至少要分成三份：</p>
<ul>
<li>训练集</li>
<li>验证集</li>
<li>测试集</li>
</ul>
<p>常见比例：</p>
<ul>
<li>8:1:1</li>
<li>9:0.5:0.5</li>
</ul>
<p>重点不是比例，而是避免数据泄漏。</p>
<p>例如：</p>
<ul>
<li>同一问题的改写版本，不要同时出现在训练和测试里</li>
<li>同一文档的多个片段，不要一部分在训练一部分在测试</li>
<li>同一用户会话，不要跨集合乱分</li>
</ul>
<p>否则评估结果会虚高。</p>
<h3 id="4-7-标注规范"><a href="#4-7-标注规范" class="headerlink" title="4.7 标注规范"></a>4.7 标注规范</h3><p>如果有人工标注，一定要先写标注规则。</p>
<p>至少要明确：</p>
<ul>
<li>什么叫相似</li>
<li>什么叫部分相关</li>
<li>什么叫不相关</li>
<li>遇到歧义怎么处理</li>
<li>长文本与短 query 如何判断</li>
</ul>
<p>建议使用三档或四档标签：</p>
<ul>
<li>2：强相关</li>
<li>1：部分相关</li>
<li>0：不相关</li>
</ul>
<p>这样后面既可以做分类，也可以映射成回归分数。</p>
<h3 id="4-8-构建评估集"><a href="#4-8-构建评估集" class="headerlink" title="4.8 构建评估集"></a>4.8 构建评估集</h3><p>训练集和评估集不要混在一起。</p>
<p>评估集应当满足：</p>
<ul>
<li>规模不必特别大，但质量一定要高</li>
<li>覆盖核心业务场景</li>
<li>覆盖热门 query 和长尾 query</li>
<li>覆盖容易混淆的 case</li>
<li>尽量有人工校验</li>
</ul>
<p>一个实用做法是：</p>
<ul>
<li>先准备一份标准评估集</li>
<li>再维护一份 badcase 集</li>
</ul>
<p>badcase 集是后续迭代最重要的资产之一。</p>
<h3 id="4-9-数据处理与数据集评估专题"><a href="#4-9-数据处理与数据集评估专题" class="headerlink" title="4.9 数据处理与数据集评估专题"></a>4.9 数据处理与数据集评估专题</h3><p>数据质量很大程度上决定模型效果上限，这部分详细内容已拆分到独立笔记：<a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/%E6%95%B0%E6%8D%AE%E5%A4%84%E7%90%86%E4%B8%8E%E6%95%B0%E6%8D%AE%E9%9B%86%E8%AF%84%E4%BC%B0/">数据处理与数据集评估</a>。</p>
<p>这里先记 4 个最重要的结论：</p>
<ul>
<li>数据处理不只是“清洗脏数据”，还包括样本分布分析、标签质量检查、数据切分和泄漏排查</li>
<li>训练集大不代表有效，重复样本、简单负样本、分布失衡和标签噪声都会拉低训练质量</li>
<li>评估集不是越大越好，而是要覆盖核心场景、长尾 query、困难样本和真实线上分布</li>
<li>如果没有稳定的数据快照、评估集和 badcase 集，模型迭代通常很难定位问题</li>
</ul>
<hr>
<h2 id="5-模型开发的详细过程"><a href="#5-模型开发的详细过程" class="headerlink" title="5. 模型开发的详细过程"></a>5. 模型开发的详细过程</h2><h3 id="5-1-选择模型架构"><a href="#5-1-选择模型架构" class="headerlink" title="5.1 选择模型架构"></a>5.1 选择模型架构</h3><p>句子嵌入最常见的是双塔编码结构（Bi-Encoder）：</p>
<ul>
<li>query 单独编码</li>
<li>document 单独编码</li>
<li>通过向量相似度做匹配</li>
</ul>
<p>优点：</p>
<ul>
<li>可提前离线编码文档</li>
<li>适合大规模检索</li>
<li>在线速度快</li>
</ul>
<p>缺点：</p>
<ul>
<li>交互建模较弱</li>
<li>极细粒度匹配不如交叉编码器</li>
</ul>
<p>如果业务是海量召回，优先考虑双塔。</p>
<p>如果业务是小规模精排，可以在召回后增加 Cross-Encoder 做二阶段排序。</p>
<h3 id="5-2-选择基座模型"><a href="#5-2-选择基座模型" class="headerlink" title="5.2 选择基座模型"></a>5.2 选择基座模型</h3><p>一般不会从零训练，而是基于现有预训练 Transformer 做微调。</p>
<p>常见选择思路：</p>
<ul>
<li>中文任务优先选中文或中英双语基座</li>
<li>长文本场景关注最大长度</li>
<li>在线低延迟场景优先小模型</li>
<li>追求效果可以用更强基座，但成本更高</li>
</ul>
<p>选择时要看：</p>
<ul>
<li>语种支持</li>
<li>最大输入长度</li>
<li>向量维度</li>
<li>推理速度</li>
<li>显存占用</li>
<li>是否方便导出 ONNX</li>
<li>是否便于量化</li>
</ul>
<h3 id="5-3-文本预处理策略"><a href="#5-3-文本预处理策略" class="headerlink" title="5.3 文本预处理策略"></a>5.3 文本预处理策略</h3><p>训练前要统一训练和推理时的文本处理逻辑。</p>
<p>例如：</p>
<ul>
<li>是否截断到固定长度</li>
<li>是否保留标点</li>
<li>是否保留换行</li>
<li>标题和正文是否拼接</li>
<li>query 前是否加任务前缀</li>
</ul>
<p>如果训练时用了：</p>
<ul>
<li><code>query: xxx</code></li>
<li><code>passage: xxx</code></li>
</ul>
<p>那部署时也必须保持一致。</p>
<p>训练和服务预处理不一致，是最常见的效果回退原因之一。</p>
<h3 id="5-4-训练目标如何选择"><a href="#5-4-训练目标如何选择" class="headerlink" title="5.4 训练目标如何选择"></a>5.4 训练目标如何选择</h3><p>不同任务常用不同 loss。</p>
<h4 id="对比学习"><a href="#对比学习" class="headerlink" title="对比学习"></a>对比学习</h4><p>目标是：</p>
<ul>
<li>拉近正样本</li>
<li>拉远负样本</li>
</ul>
<p>常见形式：</p>
<ul>
<li>Contrastive Loss</li>
<li>InfoNCE</li>
<li>Multiple Negatives Ranking Loss</li>
</ul>
<p>适合：</p>
<ul>
<li>检索</li>
<li>匹配</li>
<li>召回</li>
</ul>
<h4 id="Triplet-Loss"><a href="#Triplet-Loss" class="headerlink" title="Triplet Loss"></a>Triplet Loss</h4><p>输入一般是：</p>
<ul>
<li>anchor</li>
<li>positive</li>
<li>negative</li>
</ul>
<p>要求 anchor 更接近 positive，远离 negative。</p>
<p>适合有明确三元组数据的场景。</p>
<h4 id="Cosine-Regression-MSE"><a href="#Cosine-Regression-MSE" class="headerlink" title="Cosine Regression &#x2F; MSE"></a>Cosine Regression &#x2F; MSE</h4><p>适合有相似度分数标签的数据，例如：</p>
<ul>
<li>STS 数据</li>
<li>0 到 1 或 0 到 5 的语义打分</li>
</ul>
<h4 id="蒸馏"><a href="#蒸馏" class="headerlink" title="蒸馏"></a>蒸馏</h4><p>用更强 teacher 模型指导 student 模型学习。</p>
<p>常见方式：</p>
<ul>
<li>用 Cross-Encoder 对句子对打分</li>
<li>用 teacher embedding 作为软标签</li>
</ul>
<p>蒸馏适合：</p>
<ul>
<li>小模型加速上线</li>
<li>域内数据不足但有较强 teacher 时</li>
</ul>
<h3 id="5-5-一个常见训练配方"><a href="#5-5-一个常见训练配方" class="headerlink" title="5.5 一个常见训练配方"></a>5.5 一个常见训练配方</h3><p>检索类任务里，常见流程是：</p>
<ol>
<li>选择一个已有 embedding 基座</li>
<li>用 query-positive 对做对比学习</li>
<li>在 batch 内把其他样本当作负样本</li>
<li>再额外加入 hard negative</li>
<li>周期性评估 recall@k、mrr、ndcg</li>
<li>通过 badcase 继续补数据</li>
</ol>
<p>这类方法比单纯随机负样本更接近真实检索场景。</p>
<h3 id="5-6-训练时需要关注的超参数"><a href="#5-6-训练时需要关注的超参数" class="headerlink" title="5.6 训练时需要关注的超参数"></a>5.6 训练时需要关注的超参数</h3><p>重点关注：</p>
<ul>
<li>batch size</li>
<li>learning rate</li>
<li>max length</li>
<li>warmup steps</li>
<li>temperature</li>
<li>dropout</li>
<li>hard negative 数量</li>
<li>训练轮数</li>
</ul>
<p>经验上：</p>
<ul>
<li>batch 太小，对比学习效果往往受影响</li>
<li>max length 太短，会截断关键信息</li>
<li>训练太久，容易过拟合特定表达方式</li>
</ul>
<h3 id="5-7-是否需要做难例挖掘"><a href="#5-7-是否需要做难例挖掘" class="headerlink" title="5.7 是否需要做难例挖掘"></a>5.7 是否需要做难例挖掘</h3><p>大多数业务场景都需要。</p>
<p>常见做法：</p>
<ol>
<li>先训练一个初版模型</li>
<li>用这个模型去召回相似结果</li>
<li>从错召回结果中挑选 hard negative</li>
<li>回灌重新训练</li>
</ol>
<p>这就是一个标准的闭环：</p>
<ul>
<li>训练</li>
<li>召回</li>
<li>找错例</li>
<li>再训练</li>
</ul>
<p>很多效果提升，都是靠这个闭环慢慢做出来的。</p>
<h3 id="5-8-LoRA-微调"><a href="#5-8-LoRA-微调" class="headerlink" title="5.8 LoRA 微调"></a>5.8 LoRA 微调</h3><p>LoRA 详细内容已拆分到独立笔记：<a href="/notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/lora%E5%BE%AE%E8%B0%83%E8%AE%AD%E7%BB%83/">LoRA微调训练</a>。</p>
<p>这里先记 3 个最重要的结论：</p>
<ul>
<li>LoRA 的本质是冻结基座权重，只训练低秩增量参数</li>
<li>对 embedding 任务来说，通常不改数据格式、loss 和 pooling，只改参数更新范围</li>
<li>LoRA 解决的是训练成本问题，最终效果依然主要取决于数据质量、hard negative 和训练部署一致性</li>
</ul>
<hr>
<h2 id="6-评估与验收"><a href="#6-评估与验收" class="headerlink" title="6. 评估与验收"></a>6. 评估与验收</h2><h3 id="6-1-离线评估指标"><a href="#6-1-离线评估指标" class="headerlink" title="6.1 离线评估指标"></a>6.1 离线评估指标</h3><p>不同任务看不同指标。</p>
<h4 id="相似度任务-1"><a href="#相似度任务-1" class="headerlink" title="相似度任务"></a>相似度任务</h4><p>常见指标：</p>
<ul>
<li>Spearman</li>
<li>Pearson</li>
<li>Accuracy</li>
<li>F1</li>
</ul>
<h4 id="检索任务-1"><a href="#检索任务-1" class="headerlink" title="检索任务"></a>检索任务</h4><p>常见指标：</p>
<ul>
<li>Recall@K</li>
<li>Precision@K</li>
<li>MRR</li>
<li>MAP</li>
<li>NDCG</li>
</ul>
<p>如果模型主要用于线上召回，最应关注的通常是：</p>
<ul>
<li>Recall@10</li>
<li>Recall@50</li>
<li>MRR@10</li>
</ul>
<h3 id="6-2-线上指标"><a href="#6-2-线上指标" class="headerlink" title="6.2 线上指标"></a>6.2 线上指标</h3><p>离线指标好，不代表线上一定好。</p>
<p>线上更关注：</p>
<ul>
<li>点击率</li>
<li>转化率</li>
<li>用户停留</li>
<li>问题命中率</li>
<li>RAG 回答满意度</li>
<li>首次召回正确率</li>
</ul>
<h3 id="6-3-Badcase-分析"><a href="#6-3-Badcase-分析" class="headerlink" title="6.3 Badcase 分析"></a>6.3 Badcase 分析</h3><p>评估阶段不能只看平均分。</p>
<p>必须看错在哪里。</p>
<p>常见 badcase 类型：</p>
<ul>
<li>同义表达没召回</li>
<li>关键词相近但语义不符</li>
<li>长文本被截断</li>
<li>否定句理解错误</li>
<li>数字、时间、版本号理解错误</li>
<li>专业术语、缩写、别名处理差</li>
</ul>
<p>分析 badcase 时要问三个问题：</p>
<ol>
<li>是数据问题，还是模型问题</li>
<li>是预处理问题，还是标签问题</li>
<li>是召回问题，还是排序问题</li>
</ol>
<hr>
<h2 id="7-部署上线的详细过程"><a href="#7-部署上线的详细过程" class="headerlink" title="7. 部署上线的详细过程"></a>7. 部署上线的详细过程</h2><p>训练完成后，不是直接上线模型文件，而是要把它接入完整推理链路。</p>
<h3 id="7-1-部署前先确定线上形态"><a href="#7-1-部署前先确定线上形态" class="headerlink" title="7.1 部署前先确定线上形态"></a>7.1 部署前先确定线上形态</h3><p>常见有三种：</p>
<h4 id="离线编码-在线检索"><a href="#离线编码-在线检索" class="headerlink" title="离线编码 + 在线检索"></a>离线编码 + 在线检索</h4><p>最常见。</p>
<p>流程：</p>
<ol>
<li>文档库离线批量生成 embedding</li>
<li>写入向量库或索引</li>
<li>用户 query 在线编码</li>
<li>与向量索引做近邻检索，必要时并行走 BM25</li>
<li>合并结果后返回 topK</li>
</ol>
<p>适合：</p>
<ul>
<li>搜索</li>
<li>RAG</li>
<li>推荐召回</li>
</ul>
<h4 id="在线双边编码"><a href="#在线双边编码" class="headerlink" title="在线双边编码"></a>在线双边编码</h4><p>query 和候选内容都在线编码。</p>
<p>适合候选集较小的场景，但成本会高很多。</p>
<h4 id="召回-精排双阶段"><a href="#召回-精排双阶段" class="headerlink" title="召回 + 精排双阶段"></a>召回 + 精排双阶段</h4><p>流程：</p>
<ol>
<li>用句子嵌入模型做第一阶段召回</li>
<li>用更强的排序模型做第二阶段重排</li>
</ol>
<p>这是实际业务里非常常见的方案。</p>
<h3 id="7-2-模型导出"><a href="#7-2-模型导出" class="headerlink" title="7.2 模型导出"></a>7.2 模型导出</h3><p>训练好后通常需要把模型导出成便于部署的格式。</p>
<p>常见形态：</p>
<ul>
<li>PyTorch 原始权重</li>
<li>ONNX</li>
<li>TensorRT</li>
</ul>
<p>选择原则：</p>
<ul>
<li>如果研发环境灵活，直接用 PyTorch 也可以</li>
<li>如果强调推理速度和跨语言部署，ONNX 很常见</li>
<li>如果是 GPU 高吞吐场景，可进一步做 TensorRT 优化</li>
</ul>
<h3 id="7-3-推理服务设计"><a href="#7-3-推理服务设计" class="headerlink" title="7.3 推理服务设计"></a>7.3 推理服务设计</h3><p>一个 embedding 服务通常要解决下面几个问题：</p>
<ul>
<li>输入文本预处理</li>
<li>tokenizer 编码</li>
<li>模型推理</li>
<li>pooling</li>
<li>向量归一化</li>
<li>批处理</li>
<li>结果返回</li>
</ul>
<p>其中 pooling 很关键，常见方式有：</p>
<ul>
<li>CLS pooling</li>
<li>Mean pooling</li>
<li>Masked mean pooling</li>
</ul>
<p>如果训练时用的是 mean pooling，部署时也必须保持一致。</p>
<h3 id="7-4-向量归一化"><a href="#7-4-向量归一化" class="headerlink" title="7.4 向量归一化"></a>7.4 向量归一化</h3><p>很多检索系统会对向量做 L2 normalization。</p>
<p>这样在使用余弦相似度时更稳定，也更方便用点积近似计算。</p>
<p>注意：</p>
<ul>
<li>训练时是否归一化</li>
<li>离线建库时是否归一化</li>
<li>在线 query 是否归一化</li>
</ul>
<p>三者必须一致。</p>
<h3 id="7-5-向量索引与存储"><a href="#7-5-向量索引与存储" class="headerlink" title="7.5 向量索引与存储"></a>7.5 向量索引与存储</h3><p>常见选择：</p>
<ul>
<li>FAISS</li>
<li>Milvus</li>
<li>pgvector</li>
<li>Elasticsearch 向量检索</li>
<li>Weaviate</li>
</ul>
<p>核心不是工具名字，而是要考虑：</p>
<ul>
<li>数据规模</li>
<li>延迟要求</li>
<li>更新频率</li>
<li>是否需要过滤条件</li>
<li>是否要混合检索</li>
</ul>
<p>如果数据量很大，通常会用 ANN 检索。</p>
<p>常见索引类型：</p>
<ul>
<li>IVF</li>
<li>HNSW</li>
<li>PQ</li>
</ul>
<p>它们本质是在召回速度、内存占用、召回率之间做权衡。</p>
<h4 id="BM25-与混合检索"><a href="#BM25-与混合检索" class="headerlink" title="BM25 与混合检索"></a>BM25 与混合检索</h4><p>如果线上只有向量检索，常见漏召回场景包括：</p>
<ul>
<li>专有名词</li>
<li>产品型号和版本号</li>
<li>报错码、函数名、接口名</li>
<li>语料里很稀有但业务上非常关键的关键词</li>
</ul>
<p>如果线上只有 BM25，也常见下面的问题：</p>
<ul>
<li>同义表达召回不出来</li>
<li>query 更口语化，文档更书面化</li>
<li>用户问题和文档标题没有共享关键词</li>
</ul>
<p>所以实际工程里很常见的做法是混合检索：</p>
<ol>
<li>用 BM25 召回 topK</li>
<li>用 embedding 召回 topK</li>
<li>按 <code>doc_id</code> 或 <code>chunk_id</code> 去重</li>
<li>用融合策略得到最终候选集</li>
<li>需要时再做 rerank</li>
</ol>
<p>常见融合方式有：</p>
<ul>
<li>Reciprocal Rank Fusion（RRF）</li>
<li>加权分数融合</li>
<li>先并集召回，再交给 Cross-Encoder 或 reranker</li>
</ul>
<p>工程上要注意：</p>
<ul>
<li>BM25 分数和向量相似度分数通常不在同一量纲，不能直接生硬相加</li>
<li>chunk 切分策略会同时影响 BM25 和向量召回效果</li>
<li>过滤条件、权限控制、时间范围等规则要在两路召回里保持一致</li>
</ul>
<h3 id="7-6-离线建库流程"><a href="#7-6-离线建库流程" class="headerlink" title="7.6 离线建库流程"></a>7.6 离线建库流程</h3><p>文档侧通常这样做：</p>
<ol>
<li>清洗原始文本</li>
<li>切块或抽取标题摘要</li>
<li>生成 doc_id 与元数据</li>
<li>批量编码生成 embedding</li>
<li>写入向量库</li>
<li>建索引</li>
<li>做抽样质检</li>
</ol>
<p>元数据通常包括：</p>
<ul>
<li>doc_id</li>
<li>title</li>
<li>source</li>
<li>category</li>
<li>create_time</li>
<li>version</li>
</ul>
<p>这些字段后面常用于过滤、排序和回溯。</p>
<h3 id="7-7-在线查询流程"><a href="#7-7-在线查询流程" class="headerlink" title="7.7 在线查询流程"></a>7.7 在线查询流程</h3><p>一个典型请求链路通常是：</p>
<ol>
<li>用户输入 query</li>
<li>文本清洗与标准化</li>
<li>生成 query embedding</li>
<li>并行走 BM25 和向量召回</li>
<li>合并、去重并取候选 topK</li>
<li>按业务规则过滤</li>
<li>如有需要再做重排</li>
<li>返回结果或交给大模型生成答案</li>
</ol>
<h3 id="7-8-版本控制"><a href="#7-8-版本控制" class="headerlink" title="7.8 版本控制"></a>7.8 版本控制</h3><p>部署时一定要给下面内容做版本号：</p>
<ul>
<li>模型版本</li>
<li>tokenizer 版本</li>
<li>预处理规则版本</li>
<li>索引版本</li>
<li>数据快照版本</li>
</ul>
<p>否则一旦效果回退，很难定位问题。</p>
<h3 id="7-9-灰度上线"><a href="#7-9-灰度上线" class="headerlink" title="7.9 灰度上线"></a>7.9 灰度上线</h3><p>不要全量直接切换。</p>
<p>建议：</p>
<ul>
<li>先影子流量验证</li>
<li>再小流量灰度</li>
<li>再按业务指标逐步放量</li>
</ul>
<p>重点观察：</p>
<ul>
<li>延迟</li>
<li>超时率</li>
<li>召回结果质量</li>
<li>点击和转化变化</li>
</ul>
<hr>
<h2 id="8-上线后的监控与持续迭代"><a href="#8-上线后的监控与持续迭代" class="headerlink" title="8. 上线后的监控与持续迭代"></a>8. 上线后的监控与持续迭代</h2><p>句子嵌入模型不是“训完一次就结束”，而是需要长期维护。</p>
<h3 id="8-1-监控哪些指标"><a href="#8-1-监控哪些指标" class="headerlink" title="8.1 监控哪些指标"></a>8.1 监控哪些指标</h3><h4 id="服务指标"><a href="#服务指标" class="headerlink" title="服务指标"></a>服务指标</h4><ul>
<li>P50 &#x2F; P95 &#x2F; P99 延迟</li>
<li>QPS</li>
<li>错误率</li>
<li>超时率</li>
<li>GPU &#x2F; CPU 利用率</li>
</ul>
<h4 id="召回指标"><a href="#召回指标" class="headerlink" title="召回指标"></a>召回指标</h4><ul>
<li>topK 命中率</li>
<li>空结果率</li>
<li>重复结果率</li>
<li>query 平均召回分数</li>
</ul>
<h4 id="业务指标"><a href="#业务指标" class="headerlink" title="业务指标"></a>业务指标</h4><ul>
<li>点击率</li>
<li>转化率</li>
<li>用户满意度</li>
<li>RAG 答案采纳率</li>
</ul>
<h3 id="8-2-监控向量分布"><a href="#8-2-监控向量分布" class="headerlink" title="8.2 监控向量分布"></a>8.2 监控向量分布</h3><p>还要关注 embedding 自身是否异常。</p>
<p>例如：</p>
<ul>
<li>向量范数是否突然漂移</li>
<li>相似度分布是否异常集中</li>
<li>不同版本的向量是否不兼容</li>
</ul>
<p>如果向量分布明显漂移，通常意味着：</p>
<ul>
<li>预处理变了</li>
<li>tokenizer 变了</li>
<li>pooling 变了</li>
<li>模型加载错了</li>
</ul>
<h3 id="8-3-数据回流"><a href="#8-3-数据回流" class="headerlink" title="8.3 数据回流"></a>8.3 数据回流</h3><p>线上数据要不断回流到训练系统。</p>
<p>可回流的内容包括：</p>
<ul>
<li>新 query</li>
<li>新点击</li>
<li>新 badcase</li>
<li>人工纠错</li>
<li>用户拒绝或不满意样本</li>
</ul>
<p>这会形成一个持续优化闭环：</p>
<ol>
<li>上线模型</li>
<li>收集线上日志</li>
<li>发现 badcase</li>
<li>更新数据集</li>
<li>重新训练和评估</li>
<li>再上线</li>
</ol>
<hr>
<h2 id="9-一个最常见的落地方案"><a href="#9-一个最常见的落地方案" class="headerlink" title="9. 一个最常见的落地方案"></a>9. 一个最常见的落地方案</h2><p>如果是做中文知识库检索或 RAG，最常见的工程方案往往是：</p>
<h3 id="第一步：准备文档数据"><a href="#第一步：准备文档数据" class="headerlink" title="第一步：准备文档数据"></a>第一步：准备文档数据</h3><ul>
<li>清洗文档</li>
<li>分段切块</li>
<li>补充标题、来源、标签</li>
</ul>
<h3 id="第二步：准备-query-doc-训练对"><a href="#第二步：准备-query-doc-训练对" class="headerlink" title="第二步：准备 query-doc 训练对"></a>第二步：准备 query-doc 训练对</h3><p>来源可以是：</p>
<ul>
<li>搜索点击日志</li>
<li>FAQ 问题与答案标题</li>
<li>人工构造的问法改写</li>
</ul>
<h3 id="第三步：构造-hard-negative"><a href="#第三步：构造-hard-negative" class="headerlink" title="第三步：构造 hard negative"></a>第三步：构造 hard negative</h3><ul>
<li>用 BM25 召回看起来相关但实际不对的文档</li>
<li>用旧模型召回错例</li>
</ul>
<h3 id="第四步：微调双塔模型"><a href="#第四步：微调双塔模型" class="headerlink" title="第四步：微调双塔模型"></a>第四步：微调双塔模型</h3><ul>
<li>query 和 passage 分开编码</li>
<li>用对比学习训练</li>
<li>周期性评估 Recall@K</li>
</ul>
<h3 id="第五步：离线编码知识库"><a href="#第五步：离线编码知识库" class="headerlink" title="第五步：离线编码知识库"></a>第五步：离线编码知识库</h3><ul>
<li>把每个 chunk 编码成向量</li>
<li>写入向量数据库</li>
<li>同步建立 BM25 倒排索引</li>
</ul>
<h3 id="第六步：部署在线查询服务"><a href="#第六步：部署在线查询服务" class="headerlink" title="第六步：部署在线查询服务"></a>第六步：部署在线查询服务</h3><ul>
<li>用户 query 实时编码</li>
<li>并行执行 BM25 召回和向量召回</li>
<li>合并去重后取 topK</li>
</ul>
<h3 id="第七步：加一个-rerank-或-LLM"><a href="#第七步：加一个-rerank-或-LLM" class="headerlink" title="第七步：加一个 rerank 或 LLM"></a>第七步：加一个 rerank 或 LLM</h3><ul>
<li>检索类场景可加 rerank</li>
<li>RAG 场景可把 topK chunk 交给大模型</li>
</ul>
<p>这条路线属于最常见、最实用、最容易先做出效果的一种方案。</p>
<hr>
<h2 id="10-常见问题与坑"><a href="#10-常见问题与坑" class="headerlink" title="10. 常见问题与坑"></a>10. 常见问题与坑</h2><h3 id="10-1-只换更大的模型，但不改数据"><a href="#10-1-只换更大的模型，但不改数据" class="headerlink" title="10.1 只换更大的模型，但不改数据"></a>10.1 只换更大的模型，但不改数据</h3><p>很多时候效果上不去，不是模型不够大，而是：</p>
<ul>
<li>正样本质量差</li>
<li>负样本太简单</li>
<li>标注不一致</li>
</ul>
<h3 id="10-2-训练和部署处理不一致"><a href="#10-2-训练和部署处理不一致" class="headerlink" title="10.2 训练和部署处理不一致"></a>10.2 训练和部署处理不一致</h3><p>例如：</p>
<ul>
<li>训练时有前缀，线上没有</li>
<li>训练时做归一化，线上没做</li>
<li>训练时 mean pooling，线上用了 CLS</li>
</ul>
<p>这会直接造成效果明显下降。</p>
<h3 id="10-3-只看平均指标，不看-badcase"><a href="#10-3-只看平均指标，不看-badcase" class="headerlink" title="10.3 只看平均指标，不看 badcase"></a>10.3 只看平均指标，不看 badcase</h3><p>平均分提升，不代表关键场景真的提升。</p>
<p>很多严重问题只能从 badcase 中看出来。</p>
<h3 id="10-4-离线效果好，线上效果差"><a href="#10-4-离线效果好，线上效果差" class="headerlink" title="10.4 离线效果好，线上效果差"></a>10.4 离线效果好，线上效果差</h3><p>常见原因：</p>
<ul>
<li>评估集不真实</li>
<li>数据分布漂移</li>
<li>线上 query 更短、更口语化</li>
<li>文档切块方式不合理</li>
<li>召回后没有做业务过滤和重排</li>
</ul>
<h3 id="10-5-文本太长却直接截断"><a href="#10-5-文本太长却直接截断" class="headerlink" title="10.5 文本太长却直接截断"></a>10.5 文本太长却直接截断</h3><p>句子嵌入模型虽然叫“句子”，但很多业务里实际输入是：</p>
<ul>
<li>标题 + 摘要</li>
<li>query + 上下文</li>
<li>文档 chunk</li>
</ul>
<p>如果文本经常超长，必须重新设计：</p>
<ul>
<li>切块策略</li>
<li>标题拼接策略</li>
<li>最大长度</li>
</ul>
<h3 id="10-6-误以为-BM25-可以被-embedding-完全替代"><a href="#10-6-误以为-BM25-可以被-embedding-完全替代" class="headerlink" title="10.6 误以为 BM25 可以被 embedding 完全替代"></a>10.6 误以为 BM25 可以被 embedding 完全替代</h3><p>很多团队刚做向量检索时，会默认认为 embedding 上线后就不再需要 BM25。</p>
<p>这通常不成立。</p>
<p>在下面这些场景里，BM25 往往仍然很重要：</p>
<ul>
<li>搜索词里包含产品名、错误码、版本号</li>
<li>用户就是想找某个精确术语</li>
<li>业务要求结果必须可解释</li>
<li>冷启动阶段还没有足够训练数据</li>
</ul>
<p>更稳妥的思路通常是：</p>
<ul>
<li>先保留 BM25</li>
<li>再增加向量召回</li>
<li>最后通过融合和重排把两者优势结合起来</li>
</ul>
<hr>
<h2 id="11-一份可执行的开发清单"><a href="#11-一份可执行的开发清单" class="headerlink" title="11. 一份可执行的开发清单"></a>11. 一份可执行的开发清单</h2><p>如果从零开始做一个句子嵌入项目，可以按下面清单推进：</p>
<ol>
<li>明确业务任务是相似度、检索还是召回</li>
<li>确定线上目标指标和延迟预算</li>
<li>收集原始文本和日志数据</li>
<li>构造正样本、负样本、hard negative</li>
<li>清洗数据并切分 train &#x2F; valid &#x2F; test</li>
<li>建立标准评估集和 badcase 集</li>
<li>选择基座模型并定义预处理规则</li>
<li>选择 loss，开始训练第一版模型</li>
<li>做离线评估和 badcase 分析</li>
<li>导出模型并实现推理服务</li>
<li>建设 BM25 &#x2F; 向量索引和离线建库流程</li>
<li>小流量灰度上线</li>
<li>监控线上指标并回流数据</li>
<li>持续做难例挖掘和版本迭代</li>
</ol>
<hr>
<h2 id="12-一句话总结"><a href="#12-一句话总结" class="headerlink" title="12. 一句话总结"></a>12. 一句话总结</h2><p>句子嵌入模型项目要想真正落地，核心不是“把模型跑起来”，而是把下面几件事串成闭环：</p>
<ul>
<li>用业务目标定义任务</li>
<li>用高质量数据决定效果上限</li>
<li>用 hard negative 提升区分能力</li>
<li>用稳定推理链路保证线上一致性</li>
<li>用监控和数据回流持续迭代</li>
</ul>
<p>所以从工程角度看：</p>
<blockquote>
<p>句子嵌入模型 &#x3D; 数据构造能力 + 训练能力 + 检索部署能力 + 持续迭代能力。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>句子嵌入模型</category>
      </categories>
  </entry>
  <entry>
    <title>禅与摩托车维修艺术</title>
    <url>/2025/12/09/%E6%9D%82%E4%B8%83%E6%9D%82%E5%85%AB/%E8%AF%BB%E4%B9%A6%E6%9C%89%E6%84%9F/%E7%A6%85%E4%B8%8E%E6%91%A9%E6%89%98%E8%BD%A6%E7%BB%B4%E4%BF%AE%E8%89%BA%E6%9C%AF/</url>
    <content><![CDATA[<h1 id="良质"><a href="#良质" class="headerlink" title="良质"></a>良质</h1>]]></content>
      <categories>
        <category>杂七杂八</category>
        <category>读书有感</category>
      </categories>
      <tags>
        <tag>读书笔记</tag>
        <tag>哲学</tag>
        <tag>禅与摩托车维修艺术</tag>
      </tags>
  </entry>
  <entry>
    <title>LLM 部署学习笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2/llm/</url>
    <content><![CDATA[<h1 id="LLM-部署学习笔记"><a href="#LLM-部署学习笔记" class="headerlink" title="LLM 部署学习笔记"></a>LLM 部署学习笔记</h1><h2 id="1-先抓住部署的核心矛盾"><a href="#1-先抓住部署的核心矛盾" class="headerlink" title="1. 先抓住部署的核心矛盾"></a>1. 先抓住部署的核心矛盾</h2><p>部署大模型，本质上一直在解决 3 个问题：</p>
<ul>
<li>模型权重怎么加载进来</li>
<li>有限的 GPU &#x2F; CPU &#x2F; 磁盘内存怎么分配</li>
<li>多张卡之间怎么协调，才能兼顾“能跑、跑得稳、跑得快”</li>
</ul>
<p>如果把它再压缩成一句话：</p>
<blockquote>
<p>大模型部署，本质就是“权重放哪、缓存放哪、计算在哪做、卡和卡之间怎么通信”。</p>
</blockquote>
<hr>
<h2 id="2-模型加载：模型到底是怎么被搬进来的"><a href="#2-模型加载：模型到底是怎么被搬进来的" class="headerlink" title="2. 模型加载：模型到底是怎么被搬进来的"></a>2. 模型加载：模型到底是怎么被搬进来的</h2><h3 id="2-1-传统加载流程"><a href="#2-1-传统加载流程" class="headerlink" title="2.1 传统加载流程"></a>2.1 传统加载流程</h3><p>普通 PyTorch 模型通常这样加载：</p>
<ol>
<li>先创建模型结构</li>
<li>再把权重文件读进内存</li>
<li>再把权重拷贝进模型</li>
<li>再把模型搬到 GPU</li>
</ol>
<p>小模型这样没问题，但大模型会有明显问题：</p>
<ul>
<li>创建模型时，会先生成一份“空模型参数”</li>
<li>读取 <code>state_dict</code> 时，又会在 CPU 内存里出现第二份权重</li>
<li>最后再拷到 GPU，还会产生一次设备搬运</li>
</ul>
<p>所以大模型加载时，最容易遇到的第一个问题不是算不动，而是：</p>
<blockquote>
<p>还没开始推理，CPU 内存或显存就先爆了。</p>
</blockquote>
<hr>
<h3 id="2-2-为什么大模型加载难"><a href="#2-2-为什么大模型加载难" class="headerlink" title="2.2 为什么大模型加载难"></a>2.2 为什么大模型加载难</h3><p>大模型加载难，主要难在下面几件事：</p>
<ul>
<li>参数量太大，权重本身就已经很占内存</li>
<li>传统加载流程会出现“双份内存峰值”</li>
<li>单卡显存不一定放得下完整模型</li>
<li>就算 GPU 放不下，CPU 内存也不一定足够</li>
<li>真正上线时还要给 KV Cache、临时 buffer、CUDA runtime 留空间</li>
</ul>
<p>例如只看权重本体：</p>
<ul>
<li><code>7B</code> 参数，<code>FP16/BF16</code> 大约需要 <code>14GB</code></li>
<li><code>70B</code> 参数，<code>FP16/BF16</code> 大约需要 <code>140GB</code></li>
</ul>
<p>这还只是权重，不包含：</p>
<ul>
<li>KV Cache</li>
<li>中间激活</li>
<li>CUDA allocator 预留空间</li>
<li>框架工作区和临时 buffer</li>
</ul>
<hr>
<h3 id="2-3-更合理的大模型加载流程"><a href="#2-3-更合理的大模型加载流程" class="headerlink" title="2.3 更合理的大模型加载流程"></a>2.3 更合理的大模型加载流程</h3><p>大模型部署里，更常见的是这种思路：</p>
<ol>
<li>先只读取配置，不真实分配完整参数</li>
<li>先构建一个“空壳模型”</li>
<li>提前规划每一层该放到哪块设备</li>
<li>按分片逐步加载权重</li>
<li>加载完后再做 warmup 和运行时缓存初始化</li>
</ol>
<p>这就是很多框架里常见的：</p>
<ul>
<li><code>meta device</code></li>
<li><code>init_empty_weights</code></li>
<li><code>device_map</code></li>
<li><code>load_checkpoint_and_dispatch</code></li>
<li><code>low_cpu_mem_usage=True</code></li>
</ul>
<p>一句话理解：</p>
<blockquote>
<p>先规划，再搬运；先知道每层去哪，再真正把权重加载进去。</p>
</blockquote>
<hr>
<h3 id="2-4-典型加载手段"><a href="#2-4-典型加载手段" class="headerlink" title="2.4 典型加载手段"></a>2.4 典型加载手段</h3><h4 id="1）分片权重"><a href="#1）分片权重" class="headerlink" title="1）分片权重"></a>1）分片权重</h4><p>大模型通常不会保存成一个超大的单文件，而是拆成多个 shard。</p>
<p>这样做的好处：</p>
<ul>
<li>单次读入的文件更小</li>
<li>CPU 峰值内存更容易控制</li>
<li>可以边读边放到目标设备</li>
</ul>
<p>常见格式有：</p>
<ul>
<li><code>pytorch_model.bin</code></li>
<li><code>model-00001-of-000xx.safetensors</code></li>
</ul>
<p>现在更推荐 <code>safetensors</code>，因为它更适合做安全加载和高效读取。</p>
<h4 id="2）空壳初始化"><a href="#2）空壳初始化" class="headerlink" title="2）空壳初始化"></a>2）空壳初始化</h4><p>先在 <code>meta</code> 设备上把模型结构搭出来，只保留：</p>
<ul>
<li>shape</li>
<li>dtype</li>
<li>层结构信息</li>
</ul>
<p>此时不真正占用完整参数内存。</p>
<p>这一步的意义很大，因为只有先得到“模型骨架”，后面才能：</p>
<ul>
<li>估算每层参数量</li>
<li>计算 device map</li>
<li>决定哪些层放 GPU、哪些层放 CPU、哪些层甚至落盘</li>
</ul>
<h4 id="3）自动或手动-device-map"><a href="#3）自动或手动-device-map" class="headerlink" title="3）自动或手动 device_map"></a>3）自动或手动 <code>device_map</code></h4><p>加载前先决定每一层该去哪。</p>
<p>典型策略：</p>
<ul>
<li><code>device_map=&quot;auto&quot;</code>：框架自动做层到设备的映射</li>
<li>手动指定：自己定义哪些层放 <code>cuda:0</code>、<code>cuda:1</code>、<code>cpu</code></li>
</ul>
<p>这一步解决的是：</p>
<blockquote>
<p>不是把整个模型整体搬到一个设备上，而是按层切开后分别放。</p>
</blockquote>
<h4 id="4）CPU-NVMe-Offload"><a href="#4）CPU-NVMe-Offload" class="headerlink" title="4）CPU &#x2F; NVMe Offload"></a>4）CPU &#x2F; NVMe Offload</h4><p>如果 GPU 还是装不下，就把一部分权重放到：</p>
<ul>
<li>CPU 内存</li>
<li>NVMe 磁盘</li>
</ul>
<p>推理时再按层搬到 GPU。</p>
<p>这类方案的本质是：</p>
<blockquote>
<p>用更便宜、更大的存储层，换取“能跑起来”的可能性。</p>
</blockquote>
<p>代价也很明显：</p>
<ul>
<li>延迟会上升</li>
<li>PCIe 传输会成为瓶颈</li>
<li>更适合吞吐优先，不一定适合极低延迟</li>
</ul>
<hr>
<h3 id="2-5-一个常见加载示例"><a href="#2-5-一个常见加载示例" class="headerlink" title="2.5 一个常见加载示例"></a>2.5 一个常见加载示例</h3><p>如果是 Hugging Face Transformers，最常见的写法会是：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> transformers <span class="keyword">import</span> AutoModelForCausalLM, AutoTokenizer</span><br><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"></span><br><span class="line">model_id = <span class="string">&quot;meta-llama/Meta-Llama-3-8B-Instruct&quot;</span></span><br><span class="line"></span><br><span class="line">tokenizer = AutoTokenizer.from_pretrained(model_id)</span><br><span class="line">model = AutoModelForCausalLM.from_pretrained(</span><br><span class="line">    model_id,</span><br><span class="line">    torch_dtype=torch.bfloat16,</span><br><span class="line">    device_map=<span class="string">&quot;auto&quot;</span>,</span><br><span class="line">    low_cpu_mem_usage=<span class="literal">True</span>,</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>这里几项的意义分别是：</p>
<ul>
<li><code>torch_dtype=torch.bfloat16</code>：减小权重占用</li>
<li><code>device_map=&quot;auto&quot;</code>：自动把层分配到多个设备</li>
<li><code>low_cpu_mem_usage=True</code>：尽量降低 CPU 加载峰值</li>
</ul>
<p>如果模型更大，还可能配合：</p>
<ul>
<li><code>load_checkpoint_and_dispatch</code></li>
<li><code>max_memory</code></li>
<li><code>offload_folder</code></li>
</ul>
<hr>
<h3 id="2-6-服务真正启动时发生了什么"><a href="#2-6-服务真正启动时发生了什么" class="headerlink" title="2.6 服务真正启动时发生了什么"></a>2.6 服务真正启动时发生了什么</h3><p>部署系统真正启动时，通常不只是“把模型读进来”，还会顺手做这些事：</p>
<ul>
<li>初始化 CUDA context</li>
<li>创建 tokenizer</li>
<li>加载权重并分配设备</li>
<li>预留 runtime workspace</li>
<li>预分配或按需分配 KV Cache</li>
<li>执行一次 warmup，让 kernel、graph、memory pool 进入稳定状态</li>
</ul>
<p>所以线上冷启动慢，往往不只是“模型文件大”，而是整套运行时都在初始化。</p>
<hr>
<h2 id="3-内存分配：显存到底都花到哪里了"><a href="#3-内存分配：显存到底都花到哪里了" class="headerlink" title="3. 内存分配：显存到底都花到哪里了"></a>3. 内存分配：显存到底都花到哪里了</h2><h3 id="3-1-推理时内存的主要组成"><a href="#3-1-推理时内存的主要组成" class="headerlink" title="3.1 推理时内存的主要组成"></a>3.1 推理时内存的主要组成</h3><p>推理阶段最重要的几部分内存通常是：</p>
<table>
<thead>
<tr>
<th>内存项</th>
<th>作用</th>
<th>特点</th>
</tr>
</thead>
<tbody><tr>
<td>权重 <code>weights</code></td>
<td>存模型参数</td>
<td>静态为主，启动时确定</td>
</tr>
<tr>
<td>激活 <code>activations</code></td>
<td>当前 forward 的中间结果</td>
<td>瞬时占用，prefill 更明显</td>
</tr>
<tr>
<td>KV Cache</td>
<td>保存历史 token 的 key&#x2F;value</td>
<td>会随上下文和并发增长</td>
</tr>
<tr>
<td>Workspace &#x2F; 临时 buffer</td>
<td>kernel、attention、通信缓冲</td>
<td>容易被忽略</td>
</tr>
<tr>
<td>CUDA allocator 保留区</td>
<td>运行时内存池</td>
<td>看起来“没用完”，实际已被预留</td>
</tr>
</tbody></table>
<p>部署里最常见的误区之一：</p>
<blockquote>
<p>以为显存主要都花在权重上，但很多时候真正把服务顶爆的是 KV Cache。</p>
</blockquote>
<hr>
<h3 id="3-2-权重占用怎么估算"><a href="#3-2-权重占用怎么估算" class="headerlink" title="3.2 权重占用怎么估算"></a>3.2 权重占用怎么估算</h3><p>最基础的估算公式：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">权重内存 ≈ 参数量 × 每个参数字节数</span><br></pre></td></tr></table></figure>

<p>常见精度：</p>
<table>
<thead>
<tr>
<th>精度</th>
<th>每参数字节数</th>
</tr>
</thead>
<tbody><tr>
<td>FP32</td>
<td>4 Bytes</td>
</tr>
<tr>
<td>FP16</td>
<td>2 Bytes</td>
</tr>
<tr>
<td>BF16</td>
<td>2 Bytes</td>
</tr>
<tr>
<td>INT8</td>
<td>1 Byte</td>
</tr>
<tr>
<td>4-bit</td>
<td>约 0.5 Byte，外加量化元数据</td>
</tr>
</tbody></table>
<p>所以只看权重大致可以这么记：</p>
<ul>
<li><code>7B @ FP16/BF16</code> 约 <code>14GB</code></li>
<li><code>13B @ FP16/BF16</code> 约 <code>26GB</code></li>
<li><code>70B @ FP16/BF16</code> 约 <code>140GB</code></li>
</ul>
<p>注意这只是“裸权重体积”，实际部署还要留余量。</p>
<hr>
<h3 id="3-3-KV-Cache-为什么这么重要"><a href="#3-3-KV-Cache-为什么这么重要" class="headerlink" title="3.3 KV Cache 为什么这么重要"></a>3.3 KV Cache 为什么这么重要</h3><p>自回归推理不是每次都从头算完整上下文，而是把历史 token 的注意力结果缓存起来。</p>
<p>这个缓存就是 KV Cache。</p>
<p>它的近似规模可以理解为：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">KV Cache ≈ batch × seq_len × layers × num_kv_heads × head_dim × 2 × bytes</span><br></pre></td></tr></table></figure>

<p>这里的 <code>2</code> 是因为要存：</p>
<ul>
<li>Key</li>
<li>Value</li>
</ul>
<p>几个关键结论：</p>
<ul>
<li>上下文越长，KV Cache 越大</li>
<li>并发请求越多，KV Cache 越大</li>
<li>层数越多，KV Cache 越大</li>
<li><code>GQA / MQA</code> 会减少 <code>num_kv_heads</code>，因此能降低 KV Cache 压力</li>
</ul>
<p>所以部署时经常会出现这种现象：</p>
<ul>
<li>模型能加载成功</li>
<li>一跑高并发或长上下文就 OOM</li>
</ul>
<p>这通常不是权重问题，而是 KV Cache 膨胀了。</p>
<hr>
<h3 id="3-4-Prefill-和-Decode-的内存特征不同"><a href="#3-4-Prefill-和-Decode-的内存特征不同" class="headerlink" title="3.4 Prefill 和 Decode 的内存特征不同"></a>3.4 Prefill 和 Decode 的内存特征不同</h3><p>大模型推理通常可以分成两个阶段：</p>
<h4 id="Prefill"><a href="#Prefill" class="headerlink" title="Prefill"></a>Prefill</h4><p>也就是先把整段输入 prompt 跑一遍。</p>
<p>特点：</p>
<ul>
<li>计算量大</li>
<li>激活更明显</li>
<li>更偏“算力瓶颈”</li>
</ul>
<h4 id="Decode"><a href="#Decode" class="headerlink" title="Decode"></a>Decode</h4><p>也就是一 token 一 token 地生成。</p>
<p>特点：</p>
<ul>
<li>每步计算量没那么大</li>
<li>但 KV Cache 要一直留着</li>
<li>更容易受显存和内存带宽影响</li>
</ul>
<p>一句话记忆：</p>
<blockquote>
<p>Prefill 更像“重算一遍输入”，Decode 更像“带着缓存持续往前推”。</p>
</blockquote>
<hr>
<h3 id="3-5-为什么会有显存碎片"><a href="#3-5-为什么会有显存碎片" class="headerlink" title="3.5 为什么会有显存碎片"></a>3.5 为什么会有显存碎片</h3><p>在真实服务里，请求长度通常不一样：</p>
<ul>
<li>有的 prompt 很短</li>
<li>有的输出很长</li>
<li>有的请求很快结束</li>
<li>有的请求会长期占着上下文</li>
</ul>
<p>如果 KV Cache 按大块连续内存预留，就容易出现：</p>
<ul>
<li>过度预分配</li>
<li>空洞</li>
<li>碎片</li>
<li>大量“看起来空着，但其实不好再利用”的显存</li>
</ul>
<p>这也是为什么 vLLM 的 <code>PagedAttention</code> 很重要：</p>
<blockquote>
<p>它把 KV Cache 按 block 管理，像操作系统分页一样按需分配，而不是死板地整段连续申请。</p>
</blockquote>
<hr>
<h3 id="3-6-常见的内存优化手段"><a href="#3-6-常见的内存优化手段" class="headerlink" title="3.6 常见的内存优化手段"></a>3.6 常见的内存优化手段</h3><h4 id="1）降低权重精度"><a href="#1）降低权重精度" class="headerlink" title="1）降低权重精度"></a>1）降低权重精度</h4><p>常见选择：</p>
<ul>
<li><code>BF16 / FP16</code></li>
<li><code>INT8</code></li>
<li><code>4-bit</code></li>
</ul>
<p>作用：</p>
<ul>
<li>直接压缩权重内存</li>
<li>往往也能降低带宽压力</li>
</ul>
<p>但要注意：</p>
<blockquote>
<p>权重量化通常主要解决“模型本体太大”，不一定直接解决 KV Cache 过大。</p>
</blockquote>
<h4 id="2）KV-Cache-分块管理"><a href="#2）KV-Cache-分块管理" class="headerlink" title="2）KV Cache 分块管理"></a>2）KV Cache 分块管理</h4><p>这类思路常见于：</p>
<ul>
<li><code>PagedAttention</code></li>
<li>paged KV cache</li>
<li>block-based cache allocator</li>
</ul>
<p>作用：</p>
<ul>
<li>降低碎片</li>
<li>提高 cache 复用率</li>
<li>提升 batch 能力</li>
</ul>
<h4 id="3）Continuous-Batching-In-flight-Batching"><a href="#3）Continuous-Batching-In-flight-Batching" class="headerlink" title="3）Continuous Batching &#x2F; In-flight Batching"></a>3）Continuous Batching &#x2F; In-flight Batching</h4><p>不是等整个 batch 全结束才接新请求，而是：</p>
<ul>
<li>谁先结束就先释放</li>
<li>空出来的位置立即塞新请求</li>
</ul>
<p>这样可以让 GPU 持续保持高利用率。</p>
<h4 id="4）Offload"><a href="#4）Offload" class="headerlink" title="4）Offload"></a>4）Offload</h4><p>把一部分数据放到：</p>
<ul>
<li>CPU</li>
<li>NVMe</li>
</ul>
<p>适合：</p>
<ul>
<li>显存不够</li>
<li>可以接受额外延迟</li>
</ul>
<h4 id="5）控制上下文和并发"><a href="#5）控制上下文和并发" class="headerlink" title="5）控制上下文和并发"></a>5）控制上下文和并发</h4><p>线上很多时候最有效的办法非常朴素：</p>
<ul>
<li>限制 <code>max_model_len</code></li>
<li>限制 <code>max_num_seqs</code></li>
<li>限制 <code>max_new_tokens</code></li>
</ul>
<p>因为这些参数直接决定了 KV Cache 的上限。</p>
<hr>
<h2 id="4-多卡协调：多张-GPU-是怎么配合的"><a href="#4-多卡协调：多张-GPU-是怎么配合的" class="headerlink" title="4. 多卡协调：多张 GPU 是怎么配合的"></a>4. 多卡协调：多张 GPU 是怎么配合的</h2><h3 id="4-1-为什么需要多卡"><a href="#4-1-为什么需要多卡" class="headerlink" title="4.1 为什么需要多卡"></a>4.1 为什么需要多卡</h3><p>需要多卡，通常有 3 个原因：</p>
<ul>
<li>单卡放不下模型</li>
<li>单卡吞吐不够</li>
<li>希望用更多卡降低单请求延迟或提升整体并发</li>
</ul>
<p>但“多卡”不是一个方案，而是一组不同的并行方式。</p>
<hr>
<h3 id="4-2-数据并行-Data-Parallel"><a href="#4-2-数据并行-Data-Parallel" class="headerlink" title="4.2 数据并行 Data Parallel"></a>4.2 数据并行 Data Parallel</h3><p>最容易理解的一种方式：</p>
<ul>
<li>每张 GPU 放一整份完整模型</li>
<li>不同请求分发到不同 GPU</li>
</ul>
<p>优点：</p>
<ul>
<li>最简单</li>
<li>卡间通信最少</li>
<li>很适合模型能完整放入单卡时做水平扩展</li>
</ul>
<p>缺点：</p>
<ul>
<li>每张卡都要复制完整权重</li>
<li>模型太大时根本放不下</li>
</ul>
<p>适合场景：</p>
<ul>
<li>单卡能装下模型</li>
<li>主要目标是提高 QPS</li>
</ul>
<hr>
<h3 id="4-3-张量并行-Tensor-Parallel"><a href="#4-3-张量并行-Tensor-Parallel" class="headerlink" title="4.3 张量并行 Tensor Parallel"></a>4.3 张量并行 Tensor Parallel</h3><p>张量并行的思路是：</p>
<ul>
<li>不复制整层</li>
<li>而是把一层里的大矩阵切到多张卡上</li>
</ul>
<p>例如一个大的线性层，可以按列切、按行切，让多张 GPU 同时算一层。</p>
<p>优点：</p>
<ul>
<li>单卡装不下时还能跑</li>
<li>对超大模型很常见</li>
</ul>
<p>代价：</p>
<ul>
<li>几乎每一层都会发生通信</li>
<li>对 GPU 间互联非常敏感</li>
</ul>
<p>典型通信包括：</p>
<ul>
<li><code>AllReduce</code></li>
<li><code>AllGather</code></li>
<li><code>ReduceScatter</code></li>
</ul>
<p>一句话理解：</p>
<blockquote>
<p>Tensor Parallel 是“同一层一起算”，所以卡和卡之间必须频繁交换部分结果。</p>
</blockquote>
<hr>
<h3 id="4-4-流水线并行-Pipeline-Parallel"><a href="#4-4-流水线并行-Pipeline-Parallel" class="headerlink" title="4.4 流水线并行 Pipeline Parallel"></a>4.4 流水线并行 Pipeline Parallel</h3><p>流水线并行是把模型按层切成几段：</p>
<ul>
<li>前几层在 GPU0</li>
<li>中间层在 GPU1</li>
<li>后几层在 GPU2</li>
</ul>
<p>数据像流水线一样一段段往后传。</p>
<p>优点：</p>
<ul>
<li>每张卡只放一部分层</li>
<li>适合层数很多、模型特别深的情况</li>
</ul>
<p>缺点：</p>
<ul>
<li>会有 pipeline bubble</li>
<li>微批次设计更复杂</li>
<li>延迟不一定最优</li>
</ul>
<p>一句话理解：</p>
<blockquote>
<p>Pipeline Parallel 是“不同卡负责不同楼层”，输入像坐电梯一样逐层往上走。</p>
</blockquote>
<hr>
<h3 id="4-5-FSDP-ZeRO"><a href="#4-5-FSDP-ZeRO" class="headerlink" title="4.5 FSDP &#x2F; ZeRO"></a>4.5 FSDP &#x2F; ZeRO</h3><p>这类方案的核心不是“把一层切开一起算”，而是：</p>
<blockquote>
<p>把参数、梯度、优化器状态分片，谁需要时再临时聚合。</p>
</blockquote>
<p>最典型的是训练或微调场景。</p>
<p>FSDP 背后的核心动作可以这样理解：</p>
<ul>
<li>平时每张卡只保留自己那一份 shard</li>
<li>前向计算到某层时，先 <code>all-gather</code> 出完整参数</li>
<li>算完后，把不需要的部分释放</li>
<li>反向时再做 <code>reduce-scatter</code></li>
</ul>
<p>它解决的是：</p>
<ul>
<li>大模型训练时显存冗余过高</li>
<li>DDP 每卡都存一整份参数太浪费</li>
</ul>
<p>在部署里要注意：</p>
<ul>
<li>纯在线低延迟推理，主流更常见的是 <code>Tensor Parallel</code></li>
<li>FSDP &#x2F; ZeRO 更常用于训练、SFT、继续预训练</li>
<li>但 ZeRO-Inference 这类方案会把“分片和 offload”思路带到推理场景</li>
</ul>
<hr>
<h3 id="4-6-多卡通信时到底在传什么"><a href="#4-6-多卡通信时到底在传什么" class="headerlink" title="4.6 多卡通信时到底在传什么"></a>4.6 多卡通信时到底在传什么</h3><p>多卡协同时，常见通信原语如下：</p>
<table>
<thead>
<tr>
<th>通信原语</th>
<th>常见用途</th>
</tr>
</thead>
<tbody><tr>
<td><code>Broadcast</code></td>
<td>把参数或控制信息同步给所有卡</td>
</tr>
<tr>
<td><code>AllReduce</code></td>
<td>汇总每张卡的部分结果并让所有卡都拿到</td>
</tr>
<tr>
<td><code>AllGather</code></td>
<td>把各卡持有的分片拼回完整结果</td>
</tr>
<tr>
<td><code>ReduceScatter</code></td>
<td>先归约再分片返回</td>
</tr>
<tr>
<td><code>Send / Recv</code></td>
<td>流水线并行中前后 stage 传递激活</td>
</tr>
</tbody></table>
<p>底层常见通信库：</p>
<ul>
<li><code>NCCL</code></li>
<li><code>RCCL</code></li>
</ul>
<p>如果是部署视角，需要记住一个关键点：</p>
<blockquote>
<p>多卡不只是“卡多了”，而是“通信也多了”。如果通信成本压不住，卡越多不一定越快。</p>
</blockquote>
<hr>
<h3 id="4-7-什么时候选哪种多卡方案"><a href="#4-7-什么时候选哪种多卡方案" class="headerlink" title="4.7 什么时候选哪种多卡方案"></a>4.7 什么时候选哪种多卡方案</h3><table>
<thead>
<tr>
<th>场景</th>
<th>更常见的选择</th>
</tr>
</thead>
<tbody><tr>
<td>模型能放进单卡，只想提 QPS</td>
<td>Data Parallel</td>
</tr>
<tr>
<td>模型单卡放不下，但在同机多卡内运行</td>
<td>Tensor Parallel</td>
</tr>
<tr>
<td>模型很深，想把层按 stage 切开</td>
<td>Pipeline Parallel</td>
</tr>
<tr>
<td>训练 &#x2F; SFT &#x2F; 继续预训练，显存压力极大</td>
<td>FSDP &#x2F; ZeRO</td>
</tr>
<tr>
<td>显存不够，但可以接受更高延迟</td>
<td>CPU &#x2F; NVMe Offload</td>
</tr>
</tbody></table>
<p>很多真实系统是混合方案，例如：</p>
<ul>
<li><code>TP + PP</code></li>
<li><code>TP + Data Parallel</code></li>
<li><code>ZeRO + Offload</code></li>
</ul>
<hr>
<h3 id="4-8-多卡部署里最真实的瓶颈"><a href="#4-8-多卡部署里最真实的瓶颈" class="headerlink" title="4.8 多卡部署里最真实的瓶颈"></a>4.8 多卡部署里最真实的瓶颈</h3><p>线上真正会卡住的地方，通常不是论文里那一句“支持多卡”，而是这些细节：</p>
<ul>
<li>GPU 之间是 <code>NVLink</code> 还是只有 <code>PCIe</code></li>
<li>通信拓扑是否对称</li>
<li>小 batch 下通信时间会不会盖过计算时间</li>
<li>scheduler 是否能把动态长度请求组织好</li>
<li>KV Cache 是否会在高并发下失控</li>
<li>某一张卡是否变成热点卡</li>
</ul>
<p>所以多卡性能调优，本质是在看：</p>
<ul>
<li>算得快不快</li>
<li>传得快不快</li>
<li>分得均不均</li>
</ul>
<hr>
<h2 id="5-把三件事串起来：一条完整的部署链路"><a href="#5-把三件事串起来：一条完整的部署链路" class="headerlink" title="5. 把三件事串起来：一条完整的部署链路"></a>5. 把三件事串起来：一条完整的部署链路</h2><p>从服务启动到返回 token，大致会经历这条路径：</p>
<ol>
<li>读取模型配置和 tokenizer</li>
<li>创建空壳模型，估算权重规模</li>
<li>计算 <code>device_map</code> 或并行策略</li>
<li>分片加载权重到 GPU &#x2F; CPU &#x2F; NVMe</li>
<li>初始化 runtime buffer 和 KV Cache 管理器</li>
<li>收到请求后，scheduler 做 batch 编排</li>
<li>先做 prefill</li>
<li>再进入 decode，持续使用 KV Cache</li>
<li>如果是多卡，则在层间或 stage 间做通信</li>
<li>请求结束后释放对应 KV block</li>
</ol>
<p>这条链路里：</p>
<ul>
<li>模型加载决定“能不能启动”</li>
<li>内存分配决定“能不能稳定跑”</li>
<li>多卡协调决定“吞吐和延迟能不能达标”</li>
</ul>
<hr>
<h2 id="6-高频结论"><a href="#6-高频结论" class="headerlink" title="6. 高频结论"></a>6. 高频结论</h2><ul>
<li>大模型加载难，首先难在内存峰值，而不是算力。</li>
<li>推理阶段最核心的两块内存通常是“权重 + KV Cache”。</li>
<li>权重决定你能否把模型装进系统，KV Cache 决定你能支撑多少上下文和并发。</li>
<li><code>4-bit / 8-bit</code> 量化主要缓解权重压力，不自动等于 KV Cache 压力消失。</li>
<li>模型能跑起来，不代表服务能稳定支撑高并发。</li>
<li>多卡不总是更快；如果互联差、通信重，延迟可能更差。</li>
<li>部署里最常见的几类手段是：分片加载、精度压缩、KV Cache 优化、动态 batching、多卡并行。</li>
</ul>
<hr>
<h2 id="7-延伸阅读：推荐几篇博客"><a href="#7-延伸阅读：推荐几篇博客" class="headerlink" title="7. 延伸阅读：推荐几篇博客"></a>7. 延伸阅读：推荐几篇博客</h2><p>下面这几篇我按“加载 -&gt; 内存 -&gt; 多卡”这条线推荐，基本都是官方博客或官方文档，适合继续往下读。</p>
<h3 id="1）模型加载"><a href="#1）模型加载" class="headerlink" title="1）模型加载"></a>1）模型加载</h3><ul>
<li><a href="https://huggingface.co/blog/accelerate-large-models">How Accelerate runs very large models thanks to PyTorch</a><br>2022-09-27，Hugging Face。<br>这篇很适合用来理解 <code>meta device</code>、空壳初始化、<code>device_map</code>、分片加载到底解决了什么问题。</li>
</ul>
<h3 id="2）内存分配-KV-Cache"><a href="#2）内存分配-KV-Cache" class="headerlink" title="2）内存分配 &#x2F; KV Cache"></a>2）内存分配 &#x2F; KV Cache</h3><ul>
<li><a href="https://vllm.ai/blog/vllm">vLLM: Easy, Fast, and Cheap LLM Serving with PagedAttention</a><br>2023-06-20，vLLM 官方博客。<br>这篇非常值得看，重点就是为什么 LLM 服务的瓶颈经常在内存，尤其是 KV Cache 的碎片和过度预留问题。</li>
</ul>
<h3 id="3）FSDP-和分片思想"><a href="#3）FSDP-和分片思想" class="headerlink" title="3）FSDP 和分片思想"></a>3）FSDP 和分片思想</h3><ul>
<li><a href="https://huggingface.co/blog/pytorch-fsdp">Accelerate Large Model Training using PyTorch Fully Sharded Data Parallel</a><br>2022-05-02，Hugging Face。<br>适合理解 FSDP 的核心动作：为什么要分片、什么时候 <code>all-gather</code>、什么时候 <code>reduce-scatter</code>。</li>
</ul>
<h3 id="4）Offload-和异构内存"><a href="#4）Offload-和异构内存" class="headerlink" title="4）Offload 和异构内存"></a>4）Offload 和异构内存</h3><ul>
<li><a href="https://www.deepspeed.ai/2022/09/09/zero-inference.html">ZeRO-Inference: Democratizing massive model inference</a><br>2022-09-09，DeepSpeed。<br>如果你想理解“GPU 装不下怎么办”，这篇很有代表性，重点讲 CPU &#x2F; NVMe offload、层预取、多 GPU 拉取权重。</li>
</ul>
<h3 id="5）多卡通信"><a href="#5）多卡通信" class="headerlink" title="5）多卡通信"></a>5）多卡通信</h3><ul>
<li><a href="https://developer.nvidia.com/blog/3x-faster-allreduce-with-nvswitch-and-tensorrt-llm-multishot/">3x Faster AllReduce with NVSwitch and TensorRT-LLM MultiShot</a><br>2024-11-01，NVIDIA Technical Blog。<br>这篇适合在你已经理解 Tensor Parallel 之后继续看，能更具体地理解多卡通信为什么会成为低延迟推理瓶颈。</li>
</ul>
<hr>
<h2 id="8-建议的阅读顺序"><a href="#8-建议的阅读顺序" class="headerlink" title="8. 建议的阅读顺序"></a>8. 建议的阅读顺序</h2><p>如果你想按一条比较顺的路径学习，我建议：</p>
<ol>
<li>先看 Hugging Face 的大模型加载文章</li>
<li>再看 vLLM 的 PagedAttention</li>
<li>再看 DeepSpeed 的 ZeRO-Inference</li>
<li>再看 NVIDIA 那篇多卡通信博客</li>
<li>最后补 FSDP，把“分片和通信”的训练视角补齐</li>
</ol>
<hr>
<h2 id="9-一句话总结"><a href="#9-一句话总结" class="headerlink" title="9. 一句话总结"></a>9. 一句话总结</h2><p>大模型部署的核心，不是“把模型跑起来”这么简单，而是：</p>
<blockquote>
<p>用尽量少的显存和通信开销，把权重、缓存和计算稳定地组织起来。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>大模型部署</category>
      </categories>
  </entry>
  <entry>
    <title>数据处理与数据集评估笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%8F%A5%E5%AD%90%E5%B5%8C%E5%85%A5%E6%A8%A1%E5%9E%8B/%E6%95%B0%E6%8D%AE%E5%A4%84%E7%90%86%E4%B8%8E%E6%95%B0%E6%8D%AE%E9%9B%86%E8%AF%84%E4%BC%B0/</url>
    <content><![CDATA[<h1 id="数据处理与数据集评估笔记"><a href="#数据处理与数据集评估笔记" class="headerlink" title="数据处理与数据集评估笔记"></a>数据处理与数据集评估笔记</h1><h2 id="1-为什么数据决定模型上限"><a href="#1-为什么数据决定模型上限" class="headerlink" title="1. 为什么数据决定模型上限"></a>1. 为什么数据决定模型上限</h2><p>对句子嵌入、检索召回和 RAG 场景来说，模型当然重要，但很多时候真正决定上限的是数据。</p>
<p>常见原因有：</p>
<ul>
<li>正样本质量决定模型学到什么叫“相关”</li>
<li>负样本质量决定模型能否真正学会区分</li>
<li>样本分布决定模型上线后是否能泛化</li>
<li>标签噪声决定训练信号是否稳定</li>
<li>评估集质量决定你是否能看清真实问题</li>
</ul>
<p>先记住一个判断：模型是在数据里学规律。如果数据本身混乱、偏斜或失真，训练出来的结果大概率也会带着同样的问题。</p>
<hr>
<h2 id="2-数据处理到底包括什么"><a href="#2-数据处理到底包括什么" class="headerlink" title="2. 数据处理到底包括什么"></a>2. 数据处理到底包括什么</h2><p>很多人一提数据处理，只想到“删乱码、去空值”，但在 embedding 项目里，数据处理通常至少包括下面这些部分：</p>
<ul>
<li>数据采集</li>
<li>样本构造</li>
<li>数据清洗</li>
<li>去重和归一化</li>
<li>标签治理</li>
<li>样本分布分析</li>
<li>训练 &#x2F; 验证 &#x2F; 测试切分</li>
<li>评估集构建</li>
<li>数据版本管理</li>
</ul>
<p>换个更完整的说法：</p>
<blockquote>
<p>数据处理 &#x3D; 数据工程 + 数据质量控制 + 数据评估设计。</p>
</blockquote>
<hr>
<h2 id="3-先定义样本单位，再谈处理策略"><a href="#3-先定义样本单位，再谈处理策略" class="headerlink" title="3. 先定义样本单位，再谈处理策略"></a>3. 先定义样本单位，再谈处理策略</h2><p>在做数据处理之前，第一件事不是清洗，而是先明确“一个样本到底是什么”。</p>
<p>不同任务里，样本单位并不一样：</p>
<h3 id="3-1-相似度任务"><a href="#3-1-相似度任务" class="headerlink" title="3.1 相似度任务"></a>3.1 相似度任务</h3><p>通常是句子对：</p>
<ul>
<li><code>sentence_a</code></li>
<li><code>sentence_b</code></li>
<li><code>label</code></li>
</ul>
<h3 id="3-2-检索任务"><a href="#3-2-检索任务" class="headerlink" title="3.2 检索任务"></a>3.2 检索任务</h3><p>通常是：</p>
<ul>
<li><code>query</code></li>
<li><code>positive</code></li>
<li><code>negative</code> 或 <code>hard_negative</code></li>
</ul>
<h3 id="3-3-文档向量库构建"><a href="#3-3-文档向量库构建" class="headerlink" title="3.3 文档向量库构建"></a>3.3 文档向量库构建</h3><p>通常是：</p>
<ul>
<li><code>doc_id</code></li>
<li><code>chunk_id</code></li>
<li><code>text</code></li>
<li><code>metadata</code></li>
</ul>
<p>如果样本单位没定义清楚，后面很多统计都会失真。<br>比如你以“句子对”为单位去算分布，和以“query”为单位去算分布，结论会完全不同。</p>
<hr>
<h2 id="4-数据处理的主线流程"><a href="#4-数据处理的主线流程" class="headerlink" title="4. 数据处理的主线流程"></a>4. 数据处理的主线流程</h2><p>一个比较稳的流程通常是：</p>
<ol>
<li>明确任务目标和样本单位</li>
<li>采集原始数据</li>
<li>构造正样本、负样本、hard negative</li>
<li>做文本清洗和标准化</li>
<li>做去重、去模板、去噪</li>
<li>分析样本分布和标签分布</li>
<li>切分 train &#x2F; valid &#x2F; test</li>
<li>构建标准评估集和 badcase 集</li>
<li>训练第一版模型</li>
<li>用 badcase 反推数据问题，再回流修正</li>
</ol>
<p>这条链路里，最容易被忽视的不是清洗，而是：</p>
<ul>
<li>分布分析</li>
<li>泄漏排查</li>
<li>评估集设计</li>
</ul>
<hr>
<h2 id="5-数据清洗要做什么"><a href="#5-数据清洗要做什么" class="headerlink" title="5. 数据清洗要做什么"></a>5. 数据清洗要做什么</h2><h3 id="5-1-基础清洗"><a href="#5-1-基础清洗" class="headerlink" title="5.1 基础清洗"></a>5.1 基础清洗</h3><p>最基础的一层通常包括：</p>
<ul>
<li>去空文本</li>
<li>去乱码</li>
<li>去无意义符号</li>
<li>去极短文本</li>
<li>去极长文本</li>
<li>统一空白符</li>
<li>统一全角半角</li>
<li>统一大小写</li>
</ul>
<p>如果是中文场景，还经常要考虑：</p>
<ul>
<li>简繁统一</li>
<li>标点统一</li>
<li>URL、邮箱、手机号脱敏</li>
<li>时间、数字、金额是否归一化</li>
</ul>
<h3 id="5-2-噪声清洗"><a href="#5-2-噪声清洗" class="headerlink" title="5.2 噪声清洗"></a>5.2 噪声清洗</h3><p>很多业务数据不是“脏”，而是“有大量无效结构”。</p>
<p>例如：</p>
<ul>
<li>页眉页脚</li>
<li>导航栏</li>
<li>广告词</li>
<li>模板开场白</li>
<li>重复免责声明</li>
<li>OCR 识别错误文本</li>
</ul>
<p>这些内容如果不清掉，会让模型学到大量无关模式。</p>
<h3 id="5-3-去重"><a href="#5-3-去重" class="headerlink" title="5.3 去重"></a>5.3 去重</h3><p>去重非常重要，因为重复样本会让训练和评估都失真。</p>
<p>常见去重对象：</p>
<ul>
<li>完全重复文本</li>
<li>近重复文本</li>
<li>同一 query 的重复点击对</li>
<li>同一文档切块后的重复片段</li>
</ul>
<p>如果不做去重，常见后果有：</p>
<ul>
<li>模型过度拟合高频表达</li>
<li>评估结果虚高</li>
<li>数据规模看起来很大，但有效信息量并不高</li>
</ul>
<hr>
<h2 id="6-样本分布为什么必须看"><a href="#6-样本分布为什么必须看" class="headerlink" title="6. 样本分布为什么必须看"></a>6. 样本分布为什么必须看</h2><p>很多训练失败，不是模型没学会，而是数据分布本身有问题。</p>
<p>至少要看下面几类分布。</p>
<h3 id="6-1-文本长度分布"><a href="#6-1-文本长度分布" class="headerlink" title="6.1 文本长度分布"></a>6.1 文本长度分布</h3><p>要看：</p>
<ul>
<li>query 长度分布</li>
<li>document &#x2F; passage 长度分布</li>
<li>chunk 长度分布</li>
</ul>
<p>如果训练集里大部分文本很短，但线上是长 query 或长段落，模型上线后就容易掉点。</p>
<h3 id="6-2-标签分布"><a href="#6-2-标签分布" class="headerlink" title="6.2 标签分布"></a>6.2 标签分布</h3><p>例如：</p>
<ul>
<li>正负样本比例</li>
<li><code>2 / 1 / 0</code> 多档标签比例</li>
<li>query 是否只有正样本没有负样本</li>
</ul>
<p>如果标签极度失衡，模型往往会学到偏置策略，而不是学到真正的语义边界。</p>
<h3 id="6-3-场景分布"><a href="#6-3-场景分布" class="headerlink" title="6.3 场景分布"></a>6.3 场景分布</h3><p>要看数据是否覆盖不同业务场景，例如：</p>
<ul>
<li>FAQ</li>
<li>搜索</li>
<li>商品检索</li>
<li>知识库问答</li>
<li>代码检索</li>
</ul>
<p>如果训练样本只覆盖热门场景，长尾场景通常会明显掉效果。</p>
<h3 id="6-4-热门与长尾分布"><a href="#6-4-热门与长尾分布" class="headerlink" title="6.4 热门与长尾分布"></a>6.4 热门与长尾分布</h3><p>要区分：</p>
<ul>
<li>高频 query</li>
<li>长尾 query</li>
<li>高频文档</li>
<li>稀有文档</li>
</ul>
<p>如果训练集被热门 query 主导，模型可能对高频表达很强，但对长尾表达泛化很差。</p>
<h3 id="6-5-正样本来源分布"><a href="#6-5-正样本来源分布" class="headerlink" title="6.5 正样本来源分布"></a>6.5 正样本来源分布</h3><p>正样本如果同时来自多种来源，例如：</p>
<ul>
<li>点击日志</li>
<li>人工标注</li>
<li>FAQ 映射</li>
<li>改写生成</li>
</ul>
<p>最好分别统计占比，因为不同来源噪声强度不一样。</p>
<h3 id="6-6-负样本难度分布"><a href="#6-6-负样本难度分布" class="headerlink" title="6.6 负样本难度分布"></a>6.6 负样本难度分布</h3><p>不能只看负样本数量，还要看难度结构。</p>
<p>通常可以分成：</p>
<ul>
<li>随机负样本</li>
<li>同域负样本</li>
<li>hard negative</li>
</ul>
<p>如果全是简单负样本，模型离线 loss 可能很好看，但上线区分能力依然不够。</p>
<hr>
<h2 id="7-标签质量怎么检查"><a href="#7-标签质量怎么检查" class="headerlink" title="7. 标签质量怎么检查"></a>7. 标签质量怎么检查</h2><p>数据量大不代表标签质量高。</p>
<p>标签问题通常来自：</p>
<ul>
<li>点击不等于相关</li>
<li>曝光位置偏置</li>
<li>人工标注标准不一致</li>
<li>自动构造规则太粗糙</li>
<li>历史系统的错误被继承进新数据</li>
</ul>
<p>可以从下面几方面检查：</p>
<h3 id="7-1-抽样人工复核"><a href="#7-1-抽样人工复核" class="headerlink" title="7.1 抽样人工复核"></a>7.1 抽样人工复核</h3><p>最直接也最有效。</p>
<p>重点抽样：</p>
<ul>
<li>高频 query</li>
<li>长尾 query</li>
<li>高相似但错召回样本</li>
<li>模型和标签明显冲突的样本</li>
</ul>
<h3 id="7-2-看标签一致性"><a href="#7-2-看标签一致性" class="headerlink" title="7.2 看标签一致性"></a>7.2 看标签一致性</h3><p>例如同一个 query 是否出现下面这种情况：</p>
<ul>
<li>同一文档有时标正样本</li>
<li>有时又标成负样本</li>
</ul>
<p>这种冲突标签会直接污染训练信号。</p>
<h3 id="7-3-看模型反常样本"><a href="#7-3-看模型反常样本" class="headerlink" title="7.3 看模型反常样本"></a>7.3 看模型反常样本</h3><p>如果一个样本长期表现为：</p>
<ul>
<li>标签是正样本，但模型始终给很低分</li>
<li>标签是负样本，但模型和人工都觉得很相关</li>
</ul>
<p>那就要怀疑数据或标签本身有问题，而不是先怀疑模型。</p>
<hr>
<h2 id="8-正样本和负样本要怎么治理"><a href="#8-正样本和负样本要怎么治理" class="headerlink" title="8. 正样本和负样本要怎么治理"></a>8. 正样本和负样本要怎么治理</h2><h3 id="8-1-正样本治理"><a href="#8-1-正样本治理" class="headerlink" title="8.1 正样本治理"></a>8.1 正样本治理</h3><p>正样本要优先保证“真相关”。</p>
<p>对于日志数据，常见过滤规则包括：</p>
<ul>
<li>去掉极短停留</li>
<li>去掉误点击</li>
<li>去掉曝光高但满意度低的内容</li>
<li>去掉跳出率特别高的点击对</li>
</ul>
<p>如果是结构化映射数据，还要检查：</p>
<ul>
<li>映射关系是否过期</li>
<li>标题和正文是否错配</li>
<li>类目标签是否长期维护</li>
</ul>
<h3 id="8-2-负样本治理"><a href="#8-2-负样本治理" class="headerlink" title="8.2 负样本治理"></a>8.2 负样本治理</h3><p>负样本的核心不是“越多越好”，而是“越像线上干扰项越好”。</p>
<p>常见做法：</p>
<ul>
<li>先用随机负样本保证基础区分</li>
<li>再补同域负样本</li>
<li>最后重点挖 hard negative</li>
</ul>
<h3 id="8-3-Hard-Negative-是最值得花时间的部分"><a href="#8-3-Hard-Negative-是最值得花时间的部分" class="headerlink" title="8.3 Hard Negative 是最值得花时间的部分"></a>8.3 Hard Negative 是最值得花时间的部分</h3><p>hard negative 常见来源：</p>
<ul>
<li>BM25 高分误召回</li>
<li>旧 embedding 模型的错召回</li>
<li>Cross-Encoder 高混淆样本</li>
<li>人工 badcase 回流</li>
</ul>
<p>很多时候，模型的真实提升不来自更多数据，而来自更强的 hard negative。</p>
<hr>
<h2 id="9-训练集、验证集、测试集怎么切"><a href="#9-训练集、验证集、测试集怎么切" class="headerlink" title="9. 训练集、验证集、测试集怎么切"></a>9. 训练集、验证集、测试集怎么切</h2><p>切分数据时，重点不是比例，而是避免数据泄漏。</p>
<h3 id="9-1-常见切分比例"><a href="#9-1-常见切分比例" class="headerlink" title="9.1 常见切分比例"></a>9.1 常见切分比例</h3><ul>
<li><code>8:1:1</code></li>
<li><code>9:0.5:0.5</code></li>
</ul>
<h3 id="9-2-更重要的是避免泄漏"><a href="#9-2-更重要的是避免泄漏" class="headerlink" title="9.2 更重要的是避免泄漏"></a>9.2 更重要的是避免泄漏</h3><p>例如：</p>
<ul>
<li>同一问题的改写版本，不要分到不同集合</li>
<li>同一文档的多个 chunk，不要乱落到 train 和 test</li>
<li>同一用户会话，不要一部分在训练、一部分在测试</li>
<li>同一商品不同标题变体，不要跨集合乱分</li>
</ul>
<p>如果泄漏存在，评估结果通常会明显虚高。</p>
<h3 id="9-3-切分方式最好按业务实体来做"><a href="#9-3-切分方式最好按业务实体来做" class="headerlink" title="9.3 切分方式最好按业务实体来做"></a>9.3 切分方式最好按业务实体来做</h3><p>例如按下面这些维度切，通常更稳：</p>
<ul>
<li>按 <code>query_id</code></li>
<li>按 <code>doc_id</code></li>
<li>按会话</li>
<li>按用户</li>
</ul>
<p>而不是简单随机切行。</p>
<hr>
<h2 id="10-评估集应该怎么设计"><a href="#10-评估集应该怎么设计" class="headerlink" title="10. 评估集应该怎么设计"></a>10. 评估集应该怎么设计</h2><p>评估集不是简单从训练数据里抽一点出来，而是要刻意设计。</p>
<p>一个好的评估集通常要满足：</p>
<ul>
<li>覆盖核心业务场景</li>
<li>覆盖热门 query 和长尾 query</li>
<li>覆盖高混淆样本</li>
<li>尽量有人工校验</li>
<li>分布尽量接近线上真实请求</li>
</ul>
<h3 id="10-1-标准评估集"><a href="#10-1-标准评估集" class="headerlink" title="10.1 标准评估集"></a>10.1 标准评估集</h3><p>这是每次训练都要跑的稳定基线。</p>
<p>要求：</p>
<ul>
<li>版本稳定</li>
<li>标注质量高</li>
<li>用来做横向对比</li>
</ul>
<h3 id="10-2-Badcase-集"><a href="#10-2-Badcase-集" class="headerlink" title="10.2 Badcase 集"></a>10.2 Badcase 集</h3><p>这是最重要的补充集。</p>
<p>来源通常是：</p>
<ul>
<li>线上错召回</li>
<li>用户投诉</li>
<li>人工复核发现的高价值错误</li>
<li>新业务新术语</li>
</ul>
<p>badcase 集的作用不是看平均分，而是防止关键错误反复出现。</p>
<h3 id="10-3-分层评估"><a href="#10-3-分层评估" class="headerlink" title="10.3 分层评估"></a>10.3 分层评估</h3><p>如果场景复杂，建议把评估集再分层：</p>
<ul>
<li>热门 query 集</li>
<li>长尾 query 集</li>
<li>专业术语集</li>
<li>否定表达集</li>
<li>数字、时间、版本号敏感集</li>
</ul>
<p>这样你才能知道模型到底是“整体提升”，还是只在某一类场景提升。</p>
<hr>
<h2 id="11-数据集评估到底评什么"><a href="#11-数据集评估到底评什么" class="headerlink" title="11. 数据集评估到底评什么"></a>11. 数据集评估到底评什么</h2><p>除了看模型指标，还应该单独评估数据集本身。</p>
<p>至少可以看下面几类指标。</p>
<h3 id="11-1-规模指标"><a href="#11-1-规模指标" class="headerlink" title="11.1 规模指标"></a>11.1 规模指标</h3><ul>
<li>样本总量</li>
<li>唯一 query 数</li>
<li>唯一 doc 数</li>
<li>平均每个 query 的正样本数</li>
<li>平均每个 query 的负样本数</li>
</ul>
<h3 id="11-2-分布指标"><a href="#11-2-分布指标" class="headerlink" title="11.2 分布指标"></a>11.2 分布指标</h3><ul>
<li>文本长度分布</li>
<li>标签分布</li>
<li>场景分布</li>
<li>来源分布</li>
<li>热门 &#x2F; 长尾分布</li>
</ul>
<h3 id="11-3-质量指标"><a href="#11-3-质量指标" class="headerlink" title="11.3 质量指标"></a>11.3 质量指标</h3><ul>
<li>重复率</li>
<li>近重复率</li>
<li>空文本比例</li>
<li>模板文本比例</li>
<li>乱码比例</li>
<li>标签冲突率</li>
</ul>
<h3 id="11-4-难度指标"><a href="#11-4-难度指标" class="headerlink" title="11.4 难度指标"></a>11.4 难度指标</h3><ul>
<li>hard negative 占比</li>
<li>高混淆样本占比</li>
<li>多跳或长文本样本占比</li>
</ul>
<p>如果这些指标长期不看，训练往往会变成“看 loss 猜问题”。</p>
<hr>
<h2 id="12-一个很实用的数据诊断思路"><a href="#12-一个很实用的数据诊断思路" class="headerlink" title="12. 一个很实用的数据诊断思路"></a>12. 一个很实用的数据诊断思路</h2><p>如果模型效果不好，可以先按下面顺序排查：</p>
<ol>
<li>看训练和测试是否泄漏</li>
<li>看是否有大量重复样本</li>
<li>看标签是否冲突</li>
<li>看负样本是不是太简单</li>
<li>看热门样本是否占比过高</li>
<li>看长尾 query 是否覆盖不足</li>
<li>看评估集是否真的接近线上</li>
</ol>
<p>这条排查顺序很实用，因为很多问题根本不是模型结构导致的。</p>
<hr>
<h2 id="13-数据版本管理为什么重要"><a href="#13-数据版本管理为什么重要" class="headerlink" title="13. 数据版本管理为什么重要"></a>13. 数据版本管理为什么重要</h2><p>如果你每次训练都在用“最新数据”，但没有快照和版本号，后面几乎一定会遇到这类问题：</p>
<ul>
<li>效果变好了，不知道是哪批数据带来的</li>
<li>效果变差了，不知道是哪次清洗规则改坏了</li>
<li>badcase 修好了，但下个版本又回来了</li>
</ul>
<p>所以至少要给下面这些东西做版本管理：</p>
<ul>
<li>原始数据快照</li>
<li>清洗规则版本</li>
<li>样本构造规则版本</li>
<li>hard negative 生成版本</li>
<li>评估集版本</li>
<li>badcase 集版本</li>
</ul>
<hr>
<h2 id="14-一个适合落地的默认做法"><a href="#14-一个适合落地的默认做法" class="headerlink" title="14. 一个适合落地的默认做法"></a>14. 一个适合落地的默认做法</h2><p>如果你现在要从零做一套数据处理和数据集评估流程，可以先按这个 baseline 走：</p>
<ol>
<li>先定义样本单位和标签规范</li>
<li>从业务日志和结构化关系里构造正样本</li>
<li>补随机负样本、同域负样本和 hard negative</li>
<li>做基础清洗、去重和归一化</li>
<li>统计长度分布、标签分布、场景分布和来源分布</li>
<li>按 <code>query_id</code> 或 <code>doc_id</code> 切 train &#x2F; valid &#x2F; test</li>
<li>建一份小而稳定的标准评估集</li>
<li>再维护一份持续追加的 badcase 集</li>
<li>每次训练都固定跑离线评估和分层评估</li>
<li>把线上 badcase 持续回流</li>
</ol>
<p>这套流程不花哨，但很稳，也最容易持续迭代。</p>
<hr>
<h2 id="15-一份可直接抄的检查清单"><a href="#15-一份可直接抄的检查清单" class="headerlink" title="15. 一份可直接抄的检查清单"></a>15. 一份可直接抄的检查清单</h2><ol>
<li>是否明确了样本单位</li>
<li>是否定义了标注规则</li>
<li>是否做了基础清洗和噪声清洗</li>
<li>是否做了完全去重和近重复检查</li>
<li>是否统计了长度分布和标签分布</li>
<li>是否区分了热门和长尾样本</li>
<li>是否统计了正样本和负样本来源</li>
<li>是否有足够强的 hard negative</li>
<li>是否避免了 train &#x2F; test 泄漏</li>
<li>是否有稳定的评估集和 badcase 集</li>
<li>是否给数据和规则做了版本管理</li>
</ol>
<hr>
<h2 id="16-一句话总结"><a href="#16-一句话总结" class="headerlink" title="16. 一句话总结"></a>16. 一句话总结</h2><p>数据处理和数据集评估的核心不是“把脏数据洗干净”，而是：</p>
<blockquote>
<p>让训练数据更真实、分布更合理、标签更稳定、评估更可信。</p>
</blockquote>
<p>对 embedding 项目来说，很多时候真正决定模型上限的，不是你换了多大的模型，而是你把数据治理做到了什么程度。</p>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>句子嵌入模型</category>
      </categories>
  </entry>
  <entry>
    <title>Drogon 学习笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/drogon/</url>
    <content><![CDATA[<h1 id="Drogon-学习笔记"><a href="#Drogon-学习笔记" class="headerlink" title="Drogon 学习笔记"></a>Drogon 学习笔记</h1><h2 id="1-Drogon-是什么"><a href="#1-Drogon-是什么" class="headerlink" title="1. Drogon 是什么"></a>1. Drogon 是什么</h2><p>Drogon 是一个基于 C++ 的现代 Web 应用框架，适合用来开发：</p>
<ul>
<li>高性能 HTTP API 服务</li>
<li>C++ 网关服务</li>
<li>需要低延迟的业务接口</li>
<li>WebSocket 服务</li>
<li>需要接 MySQL &#x2F; PostgreSQL &#x2F; Redis 的后端系统</li>
</ul>
<p>它的几个核心特点：</p>
<ul>
<li>性能高，底层是非阻塞 IO</li>
<li>支持异步编程</li>
<li>支持路由、控制器、过滤器、中间件</li>
<li>支持数据库、Redis、Session、文件上传</li>
<li>支持 HTTP Client，也能调用其他内部服务</li>
<li>支持 C++20 协程</li>
</ul>
<p>如果先抓核心区别，可以这样记：FastAPI 更偏 Python 生态里的高效 API 框架，Drogon 则更像适合高性能 C++ 服务的全功能 Web 框架。</p>
<h2 id="2-为什么很多后端项目会用-Drogon"><a href="#2-为什么很多后端项目会用-Drogon" class="headerlink" title="2. 为什么很多后端项目会用 Drogon"></a>2. 为什么很多后端项目会用 Drogon</h2><p>如果你的系统有这些特点：</p>
<ul>
<li>接口并发高</li>
<li>对延迟敏感</li>
<li>业务层想用 C++ 实现</li>
<li>需要把网络、数据库、缓存、会话统一放在一个服务里</li>
</ul>
<p>那 Drogon 会很合适。</p>
<p>尤其是在你现在这类 AI 项目架构里，Drogon 很适合承担：</p>
<ul>
<li>对外 API 网关</li>
<li>会话和鉴权层</li>
<li>请求转发层</li>
<li>MySQL 持久化层</li>
<li>Redis 状态层</li>
</ul>
<p>而 Python 生态仍然更适合承担：</p>
<ul>
<li>模型推理</li>
<li>文档解析</li>
<li>RAG 流程</li>
<li>Celery 异步任务</li>
</ul>
<p>因此，很多混合架构会拆成：</p>
<ul>
<li>Drogon：对外服务、状态治理、性能敏感层</li>
<li>FastAPI：内部模型服务、任务接口</li>
<li>Celery：后台异步执行</li>
</ul>
<h2 id="3-安装和工程初始化"><a href="#3-安装和工程初始化" class="headerlink" title="3. 安装和工程初始化"></a>3. 安装和工程初始化</h2><p>Drogon 的安装方式比 FastAPI 重一些，因为它是 C++ 框架，需要编译环境和依赖。</p>
<p>常见方式有：</p>
<ul>
<li><code>vcpkg</code></li>
<li>源码编译</li>
<li>Docker</li>
</ul>
<p>如果你想先快速上手，最常见的是源码安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/drogonframework/drogon.git</span><br><span class="line"><span class="built_in">cd</span> drogon</span><br><span class="line">git submodule update --init</span><br><span class="line"><span class="built_in">mkdir</span> build</span><br><span class="line"><span class="built_in">cd</span> build</span><br><span class="line">cmake ..</span><br><span class="line">make -j4</span><br><span class="line"><span class="built_in">sudo</span> make install</span><br></pre></td></tr></table></figure>

<p>安装完成后，通常还会有一个命令行工具：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">drogon_ctl</span><br></pre></td></tr></table></figure>

<p>它常用来：</p>
<ul>
<li>创建项目骨架</li>
<li>创建 Controller</li>
<li>创建 Filter</li>
<li>生成 ORM Model</li>
</ul>
<p>例如创建一个项目：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">drogon_ctl create project demo</span><br></pre></td></tr></table></figure>

<h2 id="4-最小可运行示例"><a href="#4-最小可运行示例" class="headerlink" title="4. 最小可运行示例"></a>4. 最小可运行示例</h2><p>和 FastAPI 的 <code>main.py</code> 一样，Drogon 也可以先从一个最小服务开始。</p>
<h3 id="4-1-CMakeLists-txt"><a href="#4-1-CMakeLists-txt" class="headerlink" title="4.1 CMakeLists.txt"></a>4.1 <code>CMakeLists.txt</code></h3><figure class="highlight cmake"><table><tr><td class="code"><pre><span class="line"><span class="keyword">cmake_minimum_required</span>(VERSION <span class="number">3.16</span>)</span><br><span class="line"><span class="keyword">project</span>(drogon_demo)</span><br><span class="line"></span><br><span class="line"><span class="keyword">set</span>(CMAKE_CXX_STANDARD <span class="number">17</span>)</span><br><span class="line"><span class="keyword">set</span>(CMAKE_CXX_STANDARD_REQUIRED <span class="keyword">ON</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">find_package</span>(Drogon CONFIG REQUIRED)</span><br><span class="line"></span><br><span class="line"><span class="keyword">add_executable</span>(<span class="variable">$&#123;PROJECT_NAME&#125;</span> main.cc)</span><br><span class="line"><span class="keyword">target_link_libraries</span>(<span class="variable">$&#123;PROJECT_NAME&#125;</span> PRIVATE Drogon::Drogon)</span><br></pre></td></tr></table></figure>

<h3 id="4-2-main-cc"><a href="#4-2-main-cc" class="headerlink" title="4.2 main.cc"></a>4.2 <code>main.cc</code></h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;drogon/drogon.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> drogon;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">app</span>().<span class="built_in">registerHandler</span>(</span><br><span class="line">        <span class="string">&quot;/&quot;</span>,</span><br><span class="line">        [](<span class="type">const</span> HttpRequestPtr &amp;req,</span><br><span class="line">           std::function&lt;<span class="built_in">void</span>(<span class="type">const</span> HttpResponsePtr &amp;)&gt; &amp;&amp;callback) &#123;</span><br><span class="line">            Json::Value json;</span><br><span class="line">            json[<span class="string">&quot;message&quot;</span>] = <span class="string">&quot;hello drogon&quot;</span>;</span><br><span class="line">            <span class="built_in">callback</span>(HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json));</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;Get&#125;);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">app</span>().<span class="built_in">addListener</span>(<span class="string">&quot;0.0.0.0&quot;</span>, <span class="number">8080</span>);</span><br><span class="line">    <span class="built_in">app</span>().<span class="built_in">setThreadNum</span>(<span class="number">1</span>);</span><br><span class="line">    <span class="built_in">app</span>().<span class="built_in">run</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>编译和运行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cmake -S . -B build</span><br><span class="line">cmake --build build</span><br><span class="line">./build/drogon_demo</span><br></pre></td></tr></table></figure>

<p>访问：</p>
<ul>
<li><code>http://127.0.0.1:8080/</code></li>
</ul>
<p>如果对照 FastAPI，可以这样看：</p>
<ul>
<li><code>app = FastAPI()</code> 对应 <code>app()</code></li>
<li><code>@app.get(&quot;/&quot;)</code> 对应 <code>registerHandler(&quot;/&quot;, ...)</code></li>
<li><code>return &#123;...&#125;</code> 对应 <code>callback(HttpResponse::newHttpJsonResponse(...))</code></li>
</ul>
<h2 id="5-Drogon-的基本工作方式"><a href="#5-Drogon-的基本工作方式" class="headerlink" title="5. Drogon 的基本工作方式"></a>5. Drogon 的基本工作方式</h2><p>可以把一次请求理解成下面这条链路：</p>
<ol>
<li>客户端发来 HTTP 请求</li>
<li>Drogon 根据路径和方法匹配路由</li>
<li>先经过中间件和过滤器</li>
<li>进入 Handler 或 Controller 方法</li>
<li>业务代码处理请求</li>
<li>构造 <code>HttpResponse</code></li>
<li>通过 <code>callback</code> 返回给客户端</li>
</ol>
<p>和 FastAPI 很像，但有一个非常重要的区别：</p>
<blockquote>
<p>Drogon 不会像 FastAPI 那样自动把 Python 函数返回值转成 JSON，你需要更显式地构造响应对象。</p>
</blockquote>
<h2 id="6-直接注册-Handler"><a href="#6-直接注册-Handler" class="headerlink" title="6. 直接注册 Handler"></a>6. 直接注册 Handler</h2><p>最简单的方式就是直接在 <code>main()</code> 里注册路由。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">app</span>().<span class="built_in">registerHandler</span>(</span><br><span class="line">    <span class="string">&quot;/health&quot;</span>,</span><br><span class="line">    [](<span class="type">const</span> HttpRequestPtr &amp;req,</span><br><span class="line">       std::function&lt;<span class="built_in">void</span>(<span class="type">const</span> HttpResponsePtr &amp;)&gt; &amp;&amp;callback) &#123;</span><br><span class="line">        Json::Value json;</span><br><span class="line">        json[<span class="string">&quot;status&quot;</span>] = <span class="string">&quot;ok&quot;</span>;</span><br><span class="line">        <span class="built_in">callback</span>(HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json));</span><br><span class="line">    &#125;,</span><br><span class="line">    &#123;Get&#125;);</span><br></pre></td></tr></table></figure>

<p>适合：</p>
<ul>
<li>学习阶段</li>
<li>很小的 demo</li>
<li>临时测试接口</li>
</ul>
<p>但项目一大，通常更推荐 Controller。</p>
<h2 id="7-三种常见-Controller"><a href="#7-三种常见-Controller" class="headerlink" title="7. 三种常见 Controller"></a>7. 三种常见 Controller</h2><p>Drogon 常见的控制器主要有三类：</p>
<ul>
<li><code>HttpSimpleController</code></li>
<li><code>HttpController</code></li>
<li><code>WebSocketController</code></li>
</ul>
<h3 id="7-1-HttpSimpleController"><a href="#7-1-HttpSimpleController" class="headerlink" title="7.1 HttpSimpleController"></a>7.1 <code>HttpSimpleController</code></h3><p>适合单个简单接口。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;drogon/HttpSimpleController.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Ping</span> : <span class="keyword">public</span> drogon::HttpSimpleController&lt;Ping&gt;</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">public</span>:</span><br><span class="line">    <span class="function">PATH_LIST_BEGIN</span></span><br><span class="line"><span class="function">    <span class="title">PATH_ADD</span><span class="params">(<span class="string">&quot;/ping&quot;</span>, drogon::Get)</span></span>;</span><br><span class="line">    <span class="function">PATH_LIST_END</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">    <span class="type">void</span> <span class="title">asyncHandleHttpRequest</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="type">const</span> drogon::HttpRequestPtr &amp;req,</span></span></span><br><span class="line"><span class="params"><span class="function">        std::function&lt;<span class="type">void</span>(<span class="type">const</span> drogon::HttpResponsePtr &amp;)&gt; &amp;&amp;callback)</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        Json::Value json;</span><br><span class="line">        json[<span class="string">&quot;message&quot;</span>] = <span class="string">&quot;pong&quot;</span>;</span><br><span class="line">        <span class="built_in">callback</span>(drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="7-2-HttpController"><a href="#7-2-HttpController" class="headerlink" title="7.2 HttpController"></a>7.2 <code>HttpController</code></h3><p>适合一组 REST 风格接口，功能更完整。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;drogon/HttpController.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> api::v1</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">User</span> : <span class="keyword">public</span> drogon::HttpController&lt;User&gt;</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">public</span>:</span><br><span class="line">    <span class="function">METHOD_LIST_BEGIN</span></span><br><span class="line"><span class="function">    <span class="title">ADD_METHOD_TO</span><span class="params">(User::getUser, <span class="string">&quot;/api/v1/users/&#123;id&#125;&quot;</span>, drogon::Get)</span></span>;</span><br><span class="line">    <span class="built_in">ADD_METHOD_TO</span>(User::createUser, <span class="string">&quot;/api/v1/users&quot;</span>, drogon::Post);</span><br><span class="line">    <span class="function">METHOD_LIST_END</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">    <span class="type">void</span> <span class="title">getUser</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="type">const</span> drogon::HttpRequestPtr &amp;req,</span></span></span><br><span class="line"><span class="params"><span class="function">        std::function&lt;<span class="type">void</span>(<span class="type">const</span> drogon::HttpResponsePtr &amp;)&gt; &amp;&amp;callback,</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="type">int</span> id)</span> <span class="type">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        Json::Value json;</span><br><span class="line">        json[<span class="string">&quot;id&quot;</span>] = id;</span><br><span class="line">        <span class="built_in">callback</span>(drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">createUser</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        <span class="type">const</span> drogon::HttpRequestPtr &amp;req,</span></span></span><br><span class="line"><span class="params"><span class="function">        std::function&lt;<span class="type">void</span>(<span class="type">const</span> drogon::HttpResponsePtr &amp;)&gt; &amp;&amp;callback)</span> <span class="type">const</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">auto</span> jsonPtr = req-&gt;<span class="built_in">getJsonObject</span>();</span><br><span class="line"></span><br><span class="line">        Json::Value json;</span><br><span class="line">        json[<span class="string">&quot;name&quot;</span>] = jsonPtr ? (*jsonPtr)[<span class="string">&quot;name&quot;</span>].<span class="built_in">asString</span>() : <span class="string">&quot;&quot;</span>;</span><br><span class="line">        <span class="built_in">callback</span>(drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line">&#125;  <span class="comment">// namespace api::v1</span></span><br></pre></td></tr></table></figure>

<h3 id="7-3-WebSocketController"><a href="#7-3-WebSocketController" class="headerlink" title="7.3 WebSocketController"></a>7.3 <code>WebSocketController</code></h3><p>适合：</p>
<ul>
<li>实时聊天</li>
<li>实时协作</li>
<li>推送类业务</li>
</ul>
<p>如果你后面要做双向实时通信，可以重点看这一类；如果只是 AI 输出流式结果，很多时候 SSE 就够了，不一定非要 WebSocket。</p>
<h2 id="8-路径参数、查询参数、JSON-请求体"><a href="#8-路径参数、查询参数、JSON-请求体" class="headerlink" title="8. 路径参数、查询参数、JSON 请求体"></a>8. 路径参数、查询参数、JSON 请求体</h2><p>这部分可以类比 FastAPI 的：</p>
<ul>
<li>路径参数</li>
<li>查询参数</li>
<li>请求体</li>
</ul>
<h3 id="8-1-路径参数"><a href="#8-1-路径参数" class="headerlink" title="8.1 路径参数"></a>8.1 路径参数</h3><p>在 Drogon 里，路径参数可以直接映射到函数参数。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ADD_METHOD_TO</span>(BookController::getBook, <span class="string">&quot;/api/v1/books/&#123;book_id&#125;&quot;</span>, drogon::Get);</span><br></pre></td></tr></table></figure>

<p>对应的处理函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">getBook</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="type">const</span> drogon::HttpRequestPtr &amp;req,</span></span></span><br><span class="line"><span class="params"><span class="function">    std::function&lt;<span class="type">void</span>(<span class="type">const</span> drogon::HttpResponsePtr &amp;)&gt; &amp;&amp;callback,</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="type">int</span> bookId)</span> <span class="type">const</span></span>;</span><br></pre></td></tr></table></figure>

<p>放到 FastAPI 的语境里，大致对应：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/books/&#123;book_id&#125;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_book</span>(<span class="params">book_id: <span class="built_in">int</span></span>):</span><br><span class="line">    ...</span><br></pre></td></tr></table></figure>

<h3 id="8-2-查询参数"><a href="#8-2-查询参数" class="headerlink" title="8.2 查询参数"></a>8.2 查询参数</h3><p>查询参数通常从 <code>req</code> 里读取：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> keyword = req-&gt;<span class="built_in">getParameter</span>(<span class="string">&quot;keyword&quot;</span>);</span><br><span class="line"><span class="keyword">auto</span> page = req-&gt;<span class="built_in">getParameter</span>(<span class="string">&quot;page&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">/api/v1/search?keyword=llm&amp;page=1</span><br></pre></td></tr></table></figure>

<h3 id="8-3-JSON-请求体"><a href="#8-3-JSON-请求体" class="headerlink" title="8.3 JSON 请求体"></a>8.3 JSON 请求体</h3><p>如果客户端发的是 JSON，请求体常见写法是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> jsonPtr = req-&gt;<span class="built_in">getJsonObject</span>();</span><br><span class="line"><span class="keyword">if</span> (!jsonPtr)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">auto</span> resp = drogon::HttpResponse::<span class="built_in">newHttpResponse</span>();</span><br><span class="line">    resp-&gt;<span class="built_in">setStatusCode</span>(drogon::k400BadRequest);</span><br><span class="line">    <span class="built_in">callback</span>(resp);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">std::string name = (*jsonPtr)[<span class="string">&quot;name&quot;</span>].<span class="built_in">asString</span>();</span><br></pre></td></tr></table></figure>

<p>结论：</p>
<ul>
<li>路径参数：函数参数映射</li>
<li>查询参数：<code>req-&gt;getParameter()</code></li>
<li>JSON 请求体：<code>req-&gt;getJsonObject()</code></li>
</ul>
<h2 id="9-JSON-响应和状态码"><a href="#9-JSON-响应和状态码" class="headerlink" title="9. JSON 响应和状态码"></a>9. JSON 响应和状态码</h2><p>Drogon 里最常见的响应方式是手动构造 JSON。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">Json::Value json;</span><br><span class="line">json[<span class="string">&quot;message&quot;</span>] = <span class="string">&quot;created&quot;</span>;</span><br><span class="line">json[<span class="string">&quot;id&quot;</span>] = <span class="number">1001</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">auto</span> resp = drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json);</span><br><span class="line">resp-&gt;<span class="built_in">setStatusCode</span>(drogon::k201Created);</span><br><span class="line"><span class="built_in">callback</span>(resp);</span><br></pre></td></tr></table></figure>

<p>这和 FastAPI 的差异在于：</p>
<ul>
<li>FastAPI 更自动</li>
<li>Drogon 更显式</li>
</ul>
<p>显式的好处是：</p>
<ul>
<li>更清楚知道返回了什么</li>
<li>更方便做细粒度控制</li>
<li>更符合 C++ 项目风格</li>
</ul>
<h2 id="10-配置文件-config-json"><a href="#10-配置文件-config-json" class="headerlink" title="10. 配置文件 config.json"></a>10. 配置文件 <code>config.json</code></h2><p>很多 Drogon 项目不会把所有配置都写死在 <code>main()</code> 里，而是放到配置文件。</p>
<p>最常见的主程序写法是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;drogon/drogon.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    drogon::<span class="built_in">app</span>().<span class="built_in">loadConfigFile</span>(<span class="string">&quot;config.json&quot;</span>);</span><br><span class="line">    drogon::<span class="built_in">app</span>().<span class="built_in">run</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一个简化版配置示意：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;listeners&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;address&quot;</span><span class="punctuation">:</span> <span class="string">&quot;0.0.0.0&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;port&quot;</span><span class="punctuation">:</span> <span class="number">8080</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;db_clients&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;default&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;rdbms&quot;</span><span class="punctuation">:</span> <span class="string">&quot;mysql&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;host&quot;</span><span class="punctuation">:</span> <span class="string">&quot;127.0.0.1&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;port&quot;</span><span class="punctuation">:</span> <span class="number">3306</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;dbname&quot;</span><span class="punctuation">:</span> <span class="string">&quot;demo&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;user&quot;</span><span class="punctuation">:</span> <span class="string">&quot;root&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;passwd&quot;</span><span class="punctuation">:</span> <span class="string">&quot;123456&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;is_fast&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">false</span></span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;number_of_connections&quot;</span><span class="punctuation">:</span> <span class="number">4</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;redis_clients&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">    <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;default&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;host&quot;</span><span class="punctuation">:</span> <span class="string">&quot;127.0.0.1&quot;</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;port&quot;</span><span class="punctuation">:</span> <span class="number">6379</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;number_of_connections&quot;</span><span class="punctuation">:</span> <span class="number">2</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;app&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;number_of_threads&quot;</span><span class="punctuation">:</span> <span class="number">4</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;enable_session&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;upload_path&quot;</span><span class="punctuation">:</span> <span class="string">&quot;uploads&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;client_max_body_size&quot;</span><span class="punctuation">:</span> <span class="string">&quot;50M&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>这里最重要的点是：</p>
<ul>
<li><code>listeners</code>：监听地址和端口</li>
<li><code>db_clients</code>：数据库连接</li>
<li><code>redis_clients</code>：Redis 连接</li>
<li><code>app</code>：线程数、Session、上传路径等全局设置</li>
</ul>
<h2 id="11-过滤器和中间件"><a href="#11-过滤器和中间件" class="headerlink" title="11. 过滤器和中间件"></a>11. 过滤器和中间件</h2><p>这部分很像 FastAPI 的依赖、鉴权逻辑、中间件，但 Drogon 拆得更明确。</p>
<p>简单理解：</p>
<ul>
<li>Filter：更像“接口前置校验”</li>
<li>Middleware：更像“围绕请求处理链做前后处理”</li>
</ul>
<p>例如做一个简单鉴权 Filter：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;drogon/HttpFilter.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">AuthFilter</span> : <span class="keyword">public</span> drogon::HttpFilter&lt;AuthFilter&gt;</span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">doFilter</span><span class="params">(<span class="type">const</span> drogon::HttpRequestPtr &amp;req,</span></span></span><br><span class="line"><span class="params"><span class="function">                  FilterCallback &amp;&amp;fcb,</span></span></span><br><span class="line"><span class="params"><span class="function">                  FilterChainCallback &amp;&amp;fccb)</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">auto</span> token = req-&gt;<span class="built_in">getHeader</span>(<span class="string">&quot;x-api-key&quot;</span>);</span><br><span class="line">        <span class="keyword">if</span> (token != <span class="string">&quot;secret&quot;</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            Json::Value json;</span><br><span class="line">            json[<span class="string">&quot;error&quot;</span>] = <span class="string">&quot;unauthorized&quot;</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">auto</span> resp = drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json);</span><br><span class="line">            resp-&gt;<span class="built_in">setStatusCode</span>(drogon::k401Unauthorized);</span><br><span class="line">            <span class="built_in">fcb</span>(resp);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">fccb</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>挂到某个接口上：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ADD_METHOD_TO</span>(User::getUser, <span class="string">&quot;/api/v1/users/&#123;id&#125;&quot;</span>, drogon::Get, <span class="string">&quot;AuthFilter&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>适合抽出来做的逻辑通常有：</p>
<ul>
<li>登录校验</li>
<li>API Key 校验</li>
<li>频率限制</li>
<li>内网访问限制</li>
<li>统一日志</li>
</ul>
<h2 id="12-Session"><a href="#12-Session" class="headerlink" title="12. Session"></a>12. Session</h2><p>Drogon 内置了 Session 支持，但默认不是强制开启的。</p>
<p>它适合做：</p>
<ul>
<li>登录态</li>
<li>短期会话数据</li>
<li>访问频率控制</li>
<li>一些轻量服务端状态</li>
</ul>
<p>开启方式可以在配置文件里写：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;app&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;enable_session&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>或者代码里显式开启：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">drogon::<span class="built_in">app</span>().<span class="built_in">enableSession</span>(<span class="number">1200</span>);</span><br></pre></td></tr></table></figure>

<p>使用示例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">req-&gt;<span class="built_in">session</span>()-&gt;<span class="built_in">insert</span>(<span class="string">&quot;user_id&quot;</span>, <span class="number">1001</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (req-&gt;<span class="built_in">session</span>()-&gt;<span class="built_in">find</span>(<span class="string">&quot;user_id&quot;</span>))</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">auto</span> userId = req-&gt;<span class="built_in">session</span>()-&gt;<span class="built_in">get</span>&lt;<span class="type">int</span>&gt;(<span class="string">&quot;user_id&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意：</p>
<blockquote>
<p>Session 依赖 Cookie。如果客户端不保存 Cookie，每次请求都可能被当成新会话。</p>
</blockquote>
<h2 id="13-文件上传"><a href="#13-文件上传" class="headerlink" title="13. 文件上传"></a>13. 文件上传</h2><p>在 AI 项目里，文件上传非常常见，比如：</p>
<ul>
<li>上传 PDF</li>
<li>上传图片</li>
<li>上传音频</li>
<li>上传数据文件</li>
</ul>
<p>Drogon 常用 <code>MultiPartParser</code> 来处理 multipart 上传。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;drogon/drogon.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">upload</span><span class="params">(<span class="type">const</span> drogon::HttpRequestPtr &amp;req,</span></span></span><br><span class="line"><span class="params"><span class="function">            std::function&lt;<span class="type">void</span>(<span class="type">const</span> drogon::HttpResponsePtr &amp;)&gt; &amp;&amp;callback)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    drogon::MultiPartParser parser;</span><br><span class="line">    <span class="keyword">if</span> (parser.<span class="built_in">parse</span>(req) != <span class="number">0</span> || parser.<span class="built_in">getFiles</span>().<span class="built_in">empty</span>())</span><br><span class="line">    &#123;</span><br><span class="line">        Json::Value json;</span><br><span class="line">        json[<span class="string">&quot;error&quot;</span>] = <span class="string">&quot;no file&quot;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">auto</span> resp = drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json);</span><br><span class="line">        resp-&gt;<span class="built_in">setStatusCode</span>(drogon::k400BadRequest);</span><br><span class="line">        <span class="built_in">callback</span>(resp);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> file = parser.<span class="built_in">getFiles</span>()[<span class="number">0</span>];</span><br><span class="line">    file.<span class="built_in">save</span>(<span class="string">&quot;./uploads&quot;</span>);</span><br><span class="line"></span><br><span class="line">    Json::Value json;</span><br><span class="line">    json[<span class="string">&quot;filename&quot;</span>] = file.<span class="built_in">getFileName</span>();</span><br><span class="line">    <span class="keyword">auto</span> resp = drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json);</span><br><span class="line">    <span class="built_in">callback</span>(resp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>实际项目里要特别注意：</p>
<ul>
<li>不要直接信任原始文件名</li>
<li>要限制文件大小</li>
<li>要校验扩展名和 MIME 类型</li>
<li>最好自己生成保存文件名</li>
</ul>
<p>对于 AI 系统，更推荐的流程通常是：</p>
<ol>
<li>Drogon 接收上传</li>
<li>保存到磁盘或对象存储</li>
<li>写一条数据库记录</li>
<li>调内部 FastAPI &#x2F; Celery 提交处理任务</li>
</ol>
<h2 id="14-数据库访问：DbClient-和-ORM"><a href="#14-数据库访问：DbClient-和-ORM" class="headerlink" title="14. 数据库访问：DbClient 和 ORM"></a>14. 数据库访问：<code>DbClient</code> 和 ORM</h2><p>Drogon 自带数据库支持，常见关系型数据库包括：</p>
<ul>
<li>MySQL</li>
<li>PostgreSQL</li>
<li>SQLite3</li>
</ul>
<p>最常见的是通过 <code>DbClient</code> 执行 SQL。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> client = drogon::<span class="built_in">app</span>().<span class="built_in">getDbClient</span>();</span><br><span class="line"></span><br><span class="line">client-&gt;<span class="built_in">execSqlAsync</span>(</span><br><span class="line">    <span class="string">&quot;select id, name from users where id = ?&quot;</span>,</span><br><span class="line">    [callback](<span class="type">const</span> drogon::orm::Result &amp;result) &#123;</span><br><span class="line">        Json::Value json;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (result.<span class="built_in">empty</span>())</span><br><span class="line">        &#123;</span><br><span class="line">            json[<span class="string">&quot;error&quot;</span>] = <span class="string">&quot;not found&quot;</span>;</span><br><span class="line">            <span class="keyword">auto</span> resp = drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json);</span><br><span class="line">            resp-&gt;<span class="built_in">setStatusCode</span>(drogon::k404NotFound);</span><br><span class="line">            <span class="built_in">callback</span>(resp);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        json[<span class="string">&quot;id&quot;</span>] = result[<span class="number">0</span>][<span class="string">&quot;id&quot;</span>].<span class="built_in">as</span>&lt;<span class="type">int</span>&gt;();</span><br><span class="line">        json[<span class="string">&quot;name&quot;</span>] = result[<span class="number">0</span>][<span class="string">&quot;name&quot;</span>].<span class="built_in">as</span>&lt;std::string&gt;();</span><br><span class="line">        <span class="built_in">callback</span>(drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json));</span><br><span class="line">    &#125;,</span><br><span class="line">    [callback](<span class="type">const</span> drogon::orm::DrogonDbException &amp;e) &#123;</span><br><span class="line">        Json::Value json;</span><br><span class="line">        json[<span class="string">&quot;error&quot;</span>] = e.<span class="built_in">base</span>().<span class="built_in">what</span>();</span><br><span class="line">        <span class="keyword">auto</span> resp = drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json);</span><br><span class="line">        resp-&gt;<span class="built_in">setStatusCode</span>(drogon::k500InternalServerError);</span><br><span class="line">        <span class="built_in">callback</span>(resp);</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="number">1001</span>);</span><br></pre></td></tr></table></figure>

<p>要点：</p>
<ul>
<li>MySQL 占位符通常用 <code>?</code></li>
<li>PostgreSQL 占位符通常用 <code>$1</code>、<code>$2</code></li>
<li>异步接口更符合 Drogon 的整体风格</li>
<li>不要在回调里做很重的阻塞操作</li>
</ul>
<p>如果你不想手写太多 SQL，还可以用 <code>drogon_ctl</code> 根据表结构生成 ORM Model。</p>
<p>适合：</p>
<ul>
<li>表结构比较稳定</li>
<li>想减少手写样板代码</li>
<li>想把数据库访问写得更结构化</li>
</ul>
<h2 id="15-Redis-支持"><a href="#15-Redis-支持" class="headerlink" title="15. Redis 支持"></a>15. Redis 支持</h2><p>Drogon 也支持 Redis，而且同样是异步风格。</p>
<p>如果已经在 <code>config.json</code> 里配置了 <code>redis_clients</code>，运行后就可以获取客户端：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> redisClient = drogon::<span class="built_in">app</span>().<span class="built_in">getRedisClient</span>();</span><br></pre></td></tr></table></figure>

<p>执行命令示例：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">redisClient-&gt;<span class="built_in">execCommandAsync</span>(</span><br><span class="line">    [](<span class="type">const</span> drogon::nosql::RedisResult &amp;r) &#123;</span><br><span class="line">        LOG_INFO &lt;&lt; <span class="string">&quot;redis ok: &quot;</span> &lt;&lt; r.<span class="built_in">asString</span>();</span><br><span class="line">    &#125;,</span><br><span class="line">    [](<span class="type">const</span> std::exception &amp;err) &#123;</span><br><span class="line">        LOG_ERROR &lt;&lt; <span class="string">&quot;redis error: &quot;</span> &lt;&lt; err.<span class="built_in">what</span>();</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">&quot;get session:%s&quot;</span>,</span><br><span class="line">    <span class="string">&quot;1001&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>它适合做：</p>
<ul>
<li>缓存</li>
<li>任务状态存储</li>
<li>限流计数器</li>
<li>会话辅助状态</li>
<li>流式输出中间层</li>
</ul>
<p>如果把你现在的整条链路连起来理解，就是：</p>
<ul>
<li>Drogon：对外接口</li>
<li>Redis：缓存 &#x2F; 状态 &#x2F; 消息中间层</li>
<li>FastAPI &#x2F; Celery：后台处理</li>
</ul>
<h2 id="16-C-20-协程-Task"><a href="#16-C-20-协程-Task" class="headerlink" title="16. C++20 协程 Task&lt;&gt;"></a>16. C++20 协程 <code>Task&lt;&gt;</code></h2><p>如果你用的是较新的编译器，Drogon 支持协程，这可以让异步代码更接近同步写法。</p>
<p>例如数据库查询可以写成：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;drogon/drogon.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function">drogon::Task&lt;drogon::HttpResponsePtr&gt; <span class="title">getUserCoro</span><span class="params">(<span class="type">int</span> userId)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> db = drogon::<span class="built_in">app</span>().<span class="built_in">getDbClient</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">auto</span> result = <span class="keyword">co_await</span> db-&gt;<span class="built_in">execSqlCoro</span>(</span><br><span class="line">            <span class="string">&quot;select id, name from users where id = ?&quot;</span>,</span><br><span class="line">            userId);</span><br><span class="line"></span><br><span class="line">        Json::Value json;</span><br><span class="line">        json[<span class="string">&quot;id&quot;</span>] = result[<span class="number">0</span>][<span class="string">&quot;id&quot;</span>].<span class="built_in">as</span>&lt;<span class="type">int</span>&gt;();</span><br><span class="line">        json[<span class="string">&quot;name&quot;</span>] = result[<span class="number">0</span>][<span class="string">&quot;name&quot;</span>].<span class="built_in">as</span>&lt;std::string&gt;();</span><br><span class="line">        <span class="keyword">co_return</span> drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">catch</span> (<span class="type">const</span> drogon::orm::DrogonDbException &amp;e)</span><br><span class="line">    &#123;</span><br><span class="line">        Json::Value json;</span><br><span class="line">        json[<span class="string">&quot;error&quot;</span>] = e.<span class="built_in">base</span>().<span class="built_in">what</span>();</span><br><span class="line">        <span class="keyword">auto</span> resp = drogon::HttpResponse::<span class="built_in">newHttpJsonResponse</span>(json);</span><br><span class="line">        resp-&gt;<span class="built_in">setStatusCode</span>(drogon::k500InternalServerError);</span><br><span class="line">        <span class="keyword">co_return</span> resp;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>它的好处是：</p>
<ul>
<li>少写回调嵌套</li>
<li>控制流更清晰</li>
<li>复杂异步逻辑更好维护</li>
</ul>
<p>但你要注意：</p>
<ul>
<li>需要合适的编译器和 C++20 支持</li>
<li>协程让代码更好写，不代表可以随便阻塞线程</li>
</ul>
<h2 id="17-Drogon-和-FastAPI-怎么选"><a href="#17-Drogon-和-FastAPI-怎么选" class="headerlink" title="17. Drogon 和 FastAPI 怎么选"></a>17. Drogon 和 FastAPI 怎么选</h2><p>这两个框架不是简单的“谁替代谁”，而是各自适合不同层。</p>
<table>
<thead>
<tr>
<th>维度</th>
<th>Drogon</th>
<th>FastAPI</th>
</tr>
</thead>
<tbody><tr>
<td>语言</td>
<td>C++</td>
<td>Python</td>
</tr>
<tr>
<td>性能</td>
<td>更高，适合性能敏感层</td>
<td>足够高，开发更快</td>
</tr>
<tr>
<td>开发效率</td>
<td>相对慢一些</td>
<td>很高</td>
</tr>
<tr>
<td>参数校验</td>
<td>需要自己控制更多细节</td>
<td>自动化更强</td>
</tr>
<tr>
<td>生态</td>
<td>系统层、服务层强</td>
<td>AI、数据、模型生态强</td>
</tr>
<tr>
<td>适合位置</td>
<td>网关、核心服务、低延迟接口</td>
<td>模型服务、管理后台、内部 API</td>
</tr>
</tbody></table>
<p>如果只抓结论，可以这样选：</p>
<ul>
<li>想更快把模型接口和内部服务跑起来，用 FastAPI</li>
<li>想把网关、状态治理和性能敏感层放在 C++ 里，用 Drogon</li>
</ul>
<h2 id="18-Drogon-FastAPI-Celery-的典型配合方式"><a href="#18-Drogon-FastAPI-Celery-的典型配合方式" class="headerlink" title="18. Drogon + FastAPI + Celery 的典型配合方式"></a>18. Drogon + FastAPI + Celery 的典型配合方式</h2><p>这是你当前项目最值得掌握的一块。</p>
<p>一个典型流程可以这样拆：</p>
<ol>
<li>客户端请求先到 Drogon</li>
<li>Drogon 做鉴权、Session、参数检查、落库</li>
<li>Drogon 调用内部 FastAPI 接口提交任务</li>
<li>FastAPI 再把重任务交给 Celery</li>
<li>Celery Worker 真正处理文档、推理、索引构建</li>
<li>结果写回 MySQL &#x2F; Redis</li>
<li>Drogon 再把状态或结果返回给前端</li>
</ol>
<p>你可以把它们理解成固定分工：</p>
<ul>
<li>Drogon：网关和业务编排层</li>
<li>FastAPI：Python 服务入口</li>
<li>Celery：后台任务执行层</li>
<li>Redis：状态、缓存、队列中间层</li>
<li>MySQL：长期持久化</li>
</ul>
<p>这套架构特别适合：</p>
<ul>
<li>文档上传处理</li>
<li>RAG 建库</li>
<li>长耗时模型推理</li>
<li>流式输出聊天系统</li>
</ul>
<h2 id="19-一个比较常见的项目结构"><a href="#19-一个比较常见的项目结构" class="headerlink" title="19. 一个比较常见的项目结构"></a>19. 一个比较常见的项目结构</h2><p>如果用 Drogon 做 C++ 网关，一个比较实用的结构可以是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">cpp_gateway/</span><br><span class="line">├── CMakeLists.txt</span><br><span class="line">├── config.json</span><br><span class="line">├── main.cc</span><br><span class="line">├── controllers/</span><br><span class="line">│   ├── HealthController.h</span><br><span class="line">│   ├── HealthController.cc</span><br><span class="line">│   ├── DocumentsController.h</span><br><span class="line">│   └── DocumentsController.cc</span><br><span class="line">├── filters/</span><br><span class="line">│   ├── AuthFilter.h</span><br><span class="line">│   └── AuthFilter.cc</span><br><span class="line">├── services/</span><br><span class="line">│   ├── TaskService.h</span><br><span class="line">│   ├── TaskService.cc</span><br><span class="line">│   ├── PythonClient.h</span><br><span class="line">│   └── PythonClient.cc</span><br><span class="line">├── repositories/</span><br><span class="line">│   ├── DocumentRepo.h</span><br><span class="line">│   └── DocumentRepo.cc</span><br><span class="line">└── models/</span><br></pre></td></tr></table></figure>

<p>建议职责划分：</p>
<ul>
<li><code>controllers/</code>：只处理 HTTP 层</li>
<li><code>filters/</code>：鉴权、限流、前置校验</li>
<li><code>services/</code>：业务编排、调内部服务</li>
<li><code>repositories/</code>：数据库读写</li>
<li><code>models/</code>：ORM 生成模型或业务数据结构</li>
</ul>
<p>也就是说：</p>
<blockquote>
<p>不要把所有逻辑都堆在 Controller 里。</p>
</blockquote>
<h2 id="20-常见坑"><a href="#20-常见坑" class="headerlink" title="20. 常见坑"></a>20. 常见坑</h2><h3 id="20-1-在-Handler-里做阻塞重活"><a href="#20-1-在-Handler-里做阻塞重活" class="headerlink" title="20.1 在 Handler 里做阻塞重活"></a>20.1 在 Handler 里做阻塞重活</h3><p>例如：</p>
<ul>
<li>大文件解析</li>
<li>模型推理</li>
<li>长时间等待第三方服务</li>
</ul>
<p>这些都不应该直接堵在 Drogon 的请求处理线程里。</p>
<p>更合理的做法是：</p>
<ul>
<li>Drogon 负责接请求</li>
<li>重活交给 FastAPI &#x2F; Celery</li>
</ul>
<h3 id="20-2-误以为性能高就应该把所有东西都写进-C"><a href="#20-2-误以为性能高就应该把所有东西都写进-C" class="headerlink" title="20.2 误以为性能高就应该把所有东西都写进 C++"></a>20.2 误以为性能高就应该把所有东西都写进 C++</h3><p>不是。</p>
<p>对于 AI 项目：</p>
<ul>
<li>模型生态、向量库、文档处理库，大部分都在 Python 侧更成熟</li>
<li>Drogon 更适合做高性能 API 和状态治理</li>
</ul>
<h3 id="20-3-忽视文件上传安全"><a href="#20-3-忽视文件上传安全" class="headerlink" title="20.3 忽视文件上传安全"></a>20.3 忽视文件上传安全</h3><p>例如：</p>
<ul>
<li>直接使用原始文件名保存</li>
<li>不限制 body 大小</li>
<li>不校验扩展名</li>
</ul>
<p>这在上传接口里很危险。</p>
<h3 id="20-4-Session-开了但客户端不带-Cookie"><a href="#20-4-Session-开了但客户端不带-Cookie" class="headerlink" title="20.4 Session 开了但客户端不带 Cookie"></a>20.4 Session 开了但客户端不带 Cookie</h3><p>这样会导致服务端不断创建新 Session，白白浪费资源。</p>
<h3 id="20-5-数据库回调里继续做阻塞操作"><a href="#20-5-数据库回调里继续做阻塞操作" class="headerlink" title="20.5 数据库回调里继续做阻塞操作"></a>20.5 数据库回调里继续做阻塞操作</h3><p>这会拖慢整个异步链路。</p>
<h3 id="20-6-Controller-过胖"><a href="#20-6-Controller-过胖" class="headerlink" title="20.6 Controller 过胖"></a>20.6 Controller 过胖</h3><p>如果 Controller 里同时写：</p>
<ul>
<li>参数解析</li>
<li>SQL</li>
<li>Redis</li>
<li>调内部 HTTP</li>
<li>业务编排</li>
</ul>
<p>后面会非常难维护。</p>
<h2 id="21-一套比较实用的学习顺序"><a href="#21-一套比较实用的学习顺序" class="headerlink" title="21. 一套比较实用的学习顺序"></a>21. 一套比较实用的学习顺序</h2><p>如果你是为了做 AI 项目里的 C++ 网关，推荐这样学：</p>
<ol>
<li>先跑通最小 Drogon 服务</li>
<li>学会 <code>registerHandler()</code> 和 <code>HttpController</code></li>
<li>学会 JSON 请求和响应</li>
<li>学会 <code>config.json</code></li>
<li>学会 Filter 和 Session</li>
<li>学会文件上传</li>
<li>学会 <code>DbClient</code> 和 Redis</li>
<li>最后再接 FastAPI 和 Celery</li>
</ol>
<h2 id="22-总结"><a href="#22-总结" class="headerlink" title="22. 总结"></a>22. 总结</h2><p>Drogon 的核心价值在于：</p>
<ul>
<li>用 C++ 写高性能 Web 服务</li>
<li>把 HTTP、数据库、Redis、Session、文件上传放到一套框架里</li>
<li>适合做网关层、业务层、低延迟接口层</li>
</ul>
<p>对于 AI 项目，可以把它理解成：</p>
<ul>
<li>对外 API 入口</li>
<li>会话治理层</li>
<li>状态协调层</li>
<li>数据持久化入口</li>
<li>调用 Python 服务的编排层</li>
</ul>
<p>如果你把前面几份笔记连起来看，可以把它们理解成一条链：</p>
<ul>
<li>Drogon：对外网关 &#x2F; C++ 服务层</li>
<li>FastAPI：Python API 层</li>
<li>Redis：缓存 &#x2F; 状态 &#x2F; 中间层</li>
<li>Celery：后台异步任务层</li>
<li>MySQL：持久化数据层</li>
</ul>
<h2 id="23-参考"><a href="#23-参考" class="headerlink" title="23. 参考"></a>23. 参考</h2><ul>
<li>Drogon GitHub: <a href="https://github.com/drogonframework/drogon">https://github.com/drogonframework/drogon</a></li>
<li>Drogon Wiki: <a href="https://github.com/drogonframework/drogon/wiki">https://github.com/drogonframework/drogon/wiki</a></li>
<li>Drogon Overview: <a href="https://github.com/drogonframework/drogon/wiki/ENG-01-Overview">https://github.com/drogonframework/drogon/wiki/ENG-01-Overview</a></li>
<li>Drogon Installation: <a href="https://github.com/drogonframework/drogon/wiki/ENG-02-Installation">https://github.com/drogonframework/drogon/wiki/ENG-02-Installation</a></li>
<li>Drogon HttpController: <a href="https://github.com/drogonframework/drogon/wiki/ENG-04-2-Controller-HttpController">https://github.com/drogonframework/drogon/wiki/ENG-04-2-Controller-HttpController</a></li>
<li>Drogon Middleware and Filter: <a href="https://github.com/drogonframework/drogon/wiki/ENG-05-Middleware-and-Filter">https://github.com/drogonframework/drogon/wiki/ENG-05-Middleware-and-Filter</a></li>
<li>Drogon Session: <a href="https://github.com/drogonframework/drogon/wiki/ENG-07-Session">https://github.com/drogonframework/drogon/wiki/ENG-07-Session</a></li>
<li>Drogon DbClient: <a href="https://github.com/drogonframework/drogon/wiki/ENG-08-1-Database-DbClient">https://github.com/drogonframework/drogon/wiki/ENG-08-1-Database-DbClient</a></li>
<li>Drogon ORM: <a href="https://github.com/drogonframework/drogon/wiki/ENG-08-3-Database-ORM">https://github.com/drogonframework/drogon/wiki/ENG-08-3-Database-ORM</a></li>
<li>Drogon File Handler: <a href="https://github.com/drogonframework/drogon/wiki/ENG-09-1-File-Handler">https://github.com/drogonframework/drogon/wiki/ENG-09-1-File-Handler</a></li>
<li>Drogon Configuration File: <a href="https://github.com/drogonframework/drogon/wiki/ENG-11-Configuration-File">https://github.com/drogonframework/drogon/wiki/ENG-11-Configuration-File</a></li>
<li>Drogon drogon_ctl: <a href="https://github.com/drogonframework/drogon/wiki/ENG-12-drogon_ctl-Command">https://github.com/drogonframework/drogon/wiki/ENG-12-drogon_ctl-Command</a></li>
<li>Drogon Coroutines: <a href="https://github.com/drogonframework/drogon/wiki/ENG-17-Coroutines">https://github.com/drogonframework/drogon/wiki/ENG-17-Coroutines</a></li>
<li>Drogon Redis: <a href="https://github.com/drogonframework/drogon/wiki/ENG-18-Redis">https://github.com/drogonframework/drogon/wiki/ENG-18-Redis</a></li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>框架</category>
      </categories>
  </entry>
  <entry>
    <title>Celery 学习笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/celery/</url>
    <content><![CDATA[<h1 id="Celery-学习笔记"><a href="#Celery-学习笔记" class="headerlink" title="Celery 学习笔记"></a>Celery 学习笔记</h1><h2 id="1-Celery-是什么"><a href="#1-Celery-是什么" class="headerlink" title="1. Celery 是什么"></a>1. Celery 是什么</h2><p>Celery 是一个基于 Python 的分布式任务队列，用来把“耗时任务”从主业务流程里拆出去异步执行。</p>
<p>典型场景：</p>
<ul>
<li>发送邮件、短信、消息通知</li>
<li>文件转码、图片处理、PDF 解析</li>
<li>大模型推理、批量数据处理</li>
<li>定时任务、周期性任务</li>
<li>需要失败重试的后台任务</li>
</ul>
<p>先记住最实用的分工：FastAPI 负责接请求和返回结果，Celery 负责把耗时任务放到后台慢慢执行。</p>
<h2 id="2-为什么要用-Celery"><a href="#2-为什么要用-Celery" class="headerlink" title="2. 为什么要用 Celery"></a>2. 为什么要用 Celery</h2><p>如果一个接口内部要做很久的事情，比如：</p>
<ul>
<li>调第三方接口要 10 秒</li>
<li>处理一批文件要 2 分钟</li>
<li>模型推理要占满 CPU &#x2F; GPU</li>
</ul>
<p>那就不适合一直阻塞 HTTP 请求。更合理的做法是：</p>
<ol>
<li>FastAPI 收到请求</li>
<li>把任务丢给 Celery</li>
<li>立刻返回一个 <code>task_id</code></li>
<li>前端或调用方再通过 <code>task_id</code> 查询执行结果</li>
</ol>
<p>这样接口响应会更快，系统也更容易扩展。</p>
<h2 id="3-Celery-核心架构"><a href="#3-Celery-核心架构" class="headerlink" title="3. Celery 核心架构"></a>3. Celery 核心架构</h2><p>Celery 主要由 3 个角色组成：</p>
<h3 id="3-1-Broker"><a href="#3-1-Broker" class="headerlink" title="3.1 Broker"></a>3.1 Broker</h3><p>消息中间件，负责存放“待执行任务”。</p>
<p>常见选择：</p>
<ul>
<li>Redis</li>
<li>RabbitMQ</li>
</ul>
<p>它就是任务排队和等待消费的地方。</p>
<h3 id="3-2-Worker"><a href="#3-2-Worker" class="headerlink" title="3.2 Worker"></a>3.2 Worker</h3><p>执行任务的工作进程。</p>
<p>它会不断从 Broker 中取任务，然后真正执行 Python 函数。</p>
<h3 id="3-3-Result-Backend"><a href="#3-3-Result-Backend" class="headerlink" title="3.3 Result Backend"></a>3.3 Result Backend</h3><p>结果存储，用来保存任务状态和返回值。</p>
<p>常见选择：</p>
<ul>
<li>Redis</li>
<li>Database</li>
<li>RPC</li>
</ul>
<p>如果你只关心“任务有没有执行”，不关心返回结果，也可以不配置结果后端；但实际项目里通常还是会配置，方便查状态。</p>
<h2 id="4-工作流程"><a href="#4-工作流程" class="headerlink" title="4. 工作流程"></a>4. 工作流程</h2><p>Celery 的完整链路可以这样理解：</p>
<ol>
<li>客户端或 FastAPI 调用一个 Celery 任务</li>
<li>任务消息被发送到 Broker</li>
<li>Worker 从 Broker 取出任务</li>
<li>Worker 执行任务函数</li>
<li>执行结果和状态写入 Result Backend</li>
<li>业务侧通过 <code>task_id</code> 查询任务状态和结果</li>
</ol>
<p>对应的状态通常有：</p>
<ul>
<li><code>PENDING</code>：任务还没开始，或者还查不到</li>
<li><code>STARTED</code>：任务已开始执行</li>
<li><code>SUCCESS</code>：执行成功</li>
<li><code>FAILURE</code>：执行失败</li>
<li><code>RETRY</code>：正在重试</li>
<li><code>REVOKED</code>：任务被撤销</li>
</ul>
<p>说明：</p>
<ul>
<li>默认不一定会看到 <code>STARTED</code></li>
<li>如果想更明确地看到运行中状态，可以开启 <code>task_track_started=True</code></li>
</ul>
<h2 id="5-安装"><a href="#5-安装" class="headerlink" title="5. 安装"></a>5. 安装</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install celery redis</span><br></pre></td></tr></table></figure>

<p>如果使用 Redis 作为 Broker 和 Result Backend，需要先启动 Redis。</p>
<h2 id="6-最小可运行示例"><a href="#6-最小可运行示例" class="headerlink" title="6. 最小可运行示例"></a>6. 最小可运行示例</h2><h3 id="6-1-编写-Celery-应用"><a href="#6-1-编写-Celery-应用" class="headerlink" title="6.1 编写 Celery 应用"></a>6.1 编写 Celery 应用</h3><p>文件：<code>celery_app.py</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> celery <span class="keyword">import</span> Celery</span><br><span class="line"></span><br><span class="line">app = Celery(</span><br><span class="line">    <span class="string">&quot;demo&quot;</span>,</span><br><span class="line">    broker=<span class="string">&quot;redis://127.0.0.1:6379/1&quot;</span>,</span><br><span class="line">    backend=<span class="string">&quot;redis://127.0.0.1:6379/2&quot;</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">app.conf.update(</span><br><span class="line">    task_track_started=<span class="literal">True</span>,</span><br><span class="line">    timezone=<span class="string">&quot;Asia/Shanghai&quot;</span>,</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><code>/1</code> 表示 Redis 的第 1 个逻辑库，用来当 Broker</li>
<li><code>/2</code> 表示 Redis 的第 2 个逻辑库，用来存任务结果</li>
<li>Broker 和 Backend 分开写更清晰，排查问题也更方便</li>
</ul>
<h3 id="6-2-定义任务"><a href="#6-2-定义任务" class="headerlink" title="6.2 定义任务"></a>6.2 定义任务</h3><p>文件：<code>tasks.py</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> celery_app <span class="keyword">import</span> app</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.task</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">a: <span class="built_in">int</span>, b: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">    time.sleep(<span class="number">3</span>)</span><br><span class="line">    <span class="keyword">return</span> a + b</span><br></pre></td></tr></table></figure>

<p>这里的 <code>add</code> 虽然是普通 Python 函数，但加了 <code>@app.task</code> 之后，它就成了 Celery 任务。</p>
<h3 id="6-3-启动-Worker"><a href="#6-3-启动-Worker" class="headerlink" title="6.3 启动 Worker"></a>6.3 启动 Worker</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">celery -A tasks worker -l info</span><br></pre></td></tr></table></figure>

<p>含义：</p>
<ul>
<li><code>-A tasks</code>：表示 Celery 应用和任务定义在 <code>tasks.py</code></li>
<li><code>worker</code>：启动 worker 进程</li>
<li><code>-l info</code>：日志级别为 <code>info</code></li>
</ul>
<h3 id="6-4-提交任务"><a href="#6-4-提交任务" class="headerlink" title="6.4 提交任务"></a>6.4 提交任务</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> tasks <span class="keyword">import</span> add</span><br><span class="line"></span><br><span class="line">result = add.delay(<span class="number">3</span>, <span class="number">5</span>)</span><br><span class="line"><span class="built_in">print</span>(result.<span class="built_in">id</span>)</span><br></pre></td></tr></table></figure>

<p>注意：</p>
<ul>
<li><code>delay()</code> 只是“提交任务”</li>
<li>它不会在当前进程里同步执行</li>
<li>返回的是 <code>AsyncResult</code></li>
<li>真正执行任务的是独立的 Worker</li>
</ul>
<h3 id="6-5-查询任务结果"><a href="#6-5-查询任务结果" class="headerlink" title="6.5 查询任务结果"></a>6.5 查询任务结果</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> celery.result <span class="keyword">import</span> AsyncResult</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> celery_app <span class="keyword">import</span> app</span><br><span class="line"></span><br><span class="line">task = AsyncResult(<span class="string">&quot;任务ID&quot;</span>, app=app)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(task.status)</span><br><span class="line"><span class="built_in">print</span>(task.successful())</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> task.successful():</span><br><span class="line">    <span class="built_in">print</span>(task.get())</span><br></pre></td></tr></table></figure>

<p>常用属性和方法：</p>
<ul>
<li><code>task.id</code>：任务 ID</li>
<li><code>task.status</code>：任务状态</li>
<li><code>task.successful()</code>：是否执行成功</li>
<li><code>task.failed()</code>：是否执行失败</li>
<li><code>task.ready()</code>：任务是否结束</li>
<li><code>task.get()</code>：获取结果，可能会阻塞</li>
</ul>
<h2 id="7-delay-和-apply-async-的区别"><a href="#7-delay-和-apply-async-的区别" class="headerlink" title="7. delay() 和 apply_async() 的区别"></a>7. <code>delay()</code> 和 <code>apply_async()</code> 的区别</h2><h3 id="7-1-delay"><a href="#7-1-delay" class="headerlink" title="7.1 delay()"></a>7.1 <code>delay()</code></h3><p>最简单的调用方式，其实就是 <code>apply_async()</code> 的简写。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">add.delay(<span class="number">3</span>, <span class="number">5</span>)</span><br></pre></td></tr></table></figure>

<p>适合：</p>
<ul>
<li>普通异步提交</li>
<li>不需要额外参数时</li>
</ul>
<h3 id="7-2-apply-async"><a href="#7-2-apply-async" class="headerlink" title="7.2 apply_async()"></a>7.2 <code>apply_async()</code></h3><p>更灵活，支持倒计时、定时执行、路由到指定队列等。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">add.apply_async(args=[<span class="number">3</span>, <span class="number">5</span>], countdown=<span class="number">10</span>)</span><br></pre></td></tr></table></figure>

<p>常见参数：</p>
<ul>
<li><code>args</code> &#x2F; <code>kwargs</code>：任务参数</li>
<li><code>countdown=10</code>：10 秒后执行</li>
<li><code>eta=...</code>：指定某个时间点执行</li>
<li><code>expires=60</code>：60 秒后任务过期</li>
<li><code>queue=&quot;email&quot;</code>：指定进入哪个队列</li>
<li><code>routing_key=&quot;task.email&quot;</code>：指定路由键</li>
</ul>
<p>结论：</p>
<ul>
<li>简单任务用 <code>delay()</code></li>
<li>有调度需求时用 <code>apply_async()</code></li>
</ul>
<h2 id="8-任务重试"><a href="#8-任务重试" class="headerlink" title="8. 任务重试"></a>8. 任务重试</h2><p>很多后台任务依赖外部资源，比如：</p>
<ul>
<li>第三方 API</li>
<li>数据库</li>
<li>对象存储</li>
<li>模型服务</li>
</ul>
<p>这些服务不稳定时，直接失败往往不合理，更适合自动重试。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> celery_app <span class="keyword">import</span> app</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.task(<span class="params">bind=<span class="literal">True</span>, max_retries=<span class="number">3</span>, default_retry_delay=<span class="number">5</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">fetch_remote_data</span>(<span class="params">self, url: <span class="built_in">str</span></span>):</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="comment"># 这里假设会调用外部接口</span></span><br><span class="line">        <span class="keyword">raise</span> RuntimeError(<span class="string">&quot;temporary error&quot;</span>)</span><br><span class="line">    <span class="keyword">except</span> Exception <span class="keyword">as</span> exc:</span><br><span class="line">        <span class="keyword">raise</span> <span class="variable language_">self</span>.retry(exc=exc)</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><code>bind=True</code> 后，任务第一个参数会变成 <code>self</code></li>
<li><code>self.retry(...)</code> 可以重新入队</li>
<li><code>max_retries=3</code> 表示最多重试 3 次</li>
<li><code>default_retry_delay=5</code> 表示默认每次间隔 5 秒</li>
</ul>
<p>适合有“短暂失败可能恢复”的任务，不适合参数本身就错误的任务。</p>
<h2 id="9-队列与路由"><a href="#9-队列与路由" class="headerlink" title="9. 队列与路由"></a>9. 队列与路由</h2><p>当任务类型越来越多时，通常不会把所有任务都丢到一个队列里。</p>
<p>常见拆法：</p>
<ul>
<li><code>default</code>：普通任务</li>
<li><code>email</code>：发邮件</li>
<li><code>file</code>：文件处理</li>
<li><code>gpu</code>：模型推理</li>
</ul>
<p>这样可以让不同 Worker 只处理自己擅长的任务。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> celery <span class="keyword">import</span> Celery</span><br><span class="line"><span class="keyword">from</span> kombu <span class="keyword">import</span> Exchange, Queue</span><br><span class="line"></span><br><span class="line">app = Celery(<span class="string">&quot;demo&quot;</span>)</span><br><span class="line"></span><br><span class="line">app.conf.task_queues = (</span><br><span class="line">    Queue(<span class="string">&quot;default&quot;</span>, Exchange(<span class="string">&quot;default&quot;</span>), routing_key=<span class="string">&quot;default&quot;</span>),</span><br><span class="line">    Queue(<span class="string">&quot;file&quot;</span>, Exchange(<span class="string">&quot;file&quot;</span>), routing_key=<span class="string">&quot;file.process&quot;</span>),</span><br><span class="line">    Queue(<span class="string">&quot;gpu&quot;</span>, Exchange(<span class="string">&quot;gpu&quot;</span>), routing_key=<span class="string">&quot;gpu.infer&quot;</span>),</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">app.conf.task_routes = &#123;</span><br><span class="line">    <span class="string">&quot;tasks.process_pdf&quot;</span>: &#123;</span><br><span class="line">        <span class="string">&quot;queue&quot;</span>: <span class="string">&quot;file&quot;</span>,</span><br><span class="line">        <span class="string">&quot;routing_key&quot;</span>: <span class="string">&quot;file.process&quot;</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">&quot;tasks.run_inference&quot;</span>: &#123;</span><br><span class="line">        <span class="string">&quot;queue&quot;</span>: <span class="string">&quot;gpu&quot;</span>,</span><br><span class="line">        <span class="string">&quot;routing_key&quot;</span>: <span class="string">&quot;gpu.infer&quot;</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>启动指定队列的 Worker：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">celery -A tasks worker -Q file -l info</span><br><span class="line">celery -A tasks worker -Q gpu -l info</span><br></pre></td></tr></table></figure>

<p>这样做的好处：</p>
<ul>
<li>避免耗时任务阻塞普通任务</li>
<li>可以按机器能力拆分 Worker</li>
<li>GPU 任务和 CPU 任务可以隔离</li>
</ul>
<h2 id="10-定时任务"><a href="#10-定时任务" class="headerlink" title="10. 定时任务"></a>10. 定时任务</h2><p>Celery 可以配合 <code>celery beat</code> 做周期任务。</p>
<p>例如：</p>
<ul>
<li>每天凌晨清理临时文件</li>
<li>每小时刷新缓存</li>
<li>每 10 分钟同步一次数据</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> celery.schedules <span class="keyword">import</span> crontab</span><br><span class="line"></span><br><span class="line">app.conf.beat_schedule = &#123;</span><br><span class="line">    <span class="string">&quot;cleanup-temp-files&quot;</span>: &#123;</span><br><span class="line">        <span class="string">&quot;task&quot;</span>: <span class="string">&quot;tasks.cleanup_temp_files&quot;</span>,</span><br><span class="line">        <span class="string">&quot;schedule&quot;</span>: crontab(hour=<span class="number">3</span>, minute=<span class="number">0</span>),</span><br><span class="line">    &#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>启动：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">celery -A tasks beat -l info</span><br></pre></td></tr></table></figure>

<p>如果要实际执行任务，除了 <code>beat</code> 还要有 <code>worker</code> 在跑。</p>
<h2 id="11-FastAPI-和-Celery-怎么配合"><a href="#11-FastAPI-和-Celery-怎么配合" class="headerlink" title="11. FastAPI 和 Celery 怎么配合"></a>11. FastAPI 和 Celery 怎么配合</h2><p>最常见的配合方式是：</p>
<ol>
<li>FastAPI 暴露 HTTP 接口</li>
<li>接口中调用 Celery 提交任务</li>
<li>Celery Worker 在后台执行</li>
<li>再通过一个查询接口返回任务状态&#x2F;结果</li>
</ol>
<h3 id="11-1-什么时候用-BackgroundTasks，什么时候用-Celery"><a href="#11-1-什么时候用-BackgroundTasks，什么时候用-Celery" class="headerlink" title="11.1 什么时候用 BackgroundTasks，什么时候用 Celery"></a>11.1 什么时候用 <code>BackgroundTasks</code>，什么时候用 Celery</h3><p>FastAPI 自带 <code>BackgroundTasks</code>，但它和 Celery 不是一类东西。</p>
<p><code>BackgroundTasks</code> 更适合：</p>
<ul>
<li>很轻量的后台操作</li>
<li>跟当前服务进程强绑定</li>
<li>不要求失败重试</li>
<li>不要求多机器扩展</li>
</ul>
<p>Celery 更适合：</p>
<ul>
<li>任务耗时长</li>
<li>任务量大</li>
<li>需要重试</li>
<li>需要队列隔离</li>
<li>需要多个 Worker 扩展</li>
<li>需要独立于 Web 进程运行</li>
</ul>
<p>一句话：</p>
<blockquote>
<p>轻任务用 <code>BackgroundTasks</code>，重任务用 Celery。</p>
</blockquote>
<h2 id="12-FastAPI-Celery-案例-1：提交任务并查询状态"><a href="#12-FastAPI-Celery-案例-1：提交任务并查询状态" class="headerlink" title="12. FastAPI + Celery 案例 1：提交任务并查询状态"></a>12. FastAPI + Celery 案例 1：提交任务并查询状态</h2><p>这是最常见的模式。</p>
<h3 id="12-1-目录结构"><a href="#12-1-目录结构" class="headerlink" title="12.1 目录结构"></a>12.1 目录结构</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">project/</span><br><span class="line">├── main.py</span><br><span class="line">├── celery_app.py</span><br><span class="line">└── tasks.py</span><br></pre></td></tr></table></figure>

<h3 id="12-2-celery-app-py"><a href="#12-2-celery-app-py" class="headerlink" title="12.2 celery_app.py"></a>12.2 <code>celery_app.py</code></h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> celery <span class="keyword">import</span> Celery</span><br><span class="line"></span><br><span class="line">celery_app = Celery(</span><br><span class="line">    <span class="string">&quot;fastapi_demo&quot;</span>,</span><br><span class="line">    broker=<span class="string">&quot;redis://127.0.0.1:6379/1&quot;</span>,</span><br><span class="line">    backend=<span class="string">&quot;redis://127.0.0.1:6379/2&quot;</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">celery_app.conf.update(</span><br><span class="line">    task_track_started=<span class="literal">True</span>,</span><br><span class="line">    result_expires=<span class="number">3600</span>,</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<h3 id="12-3-tasks-py"><a href="#12-3-tasks-py" class="headerlink" title="12.3 tasks.py"></a>12.3 <code>tasks.py</code></h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> celery_app <span class="keyword">import</span> celery_app</span><br><span class="line"></span><br><span class="line"><span class="meta">@celery_app.task(<span class="params">name=<span class="string">&quot;tasks.long_time_add&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">long_time_add</span>(<span class="params">a: <span class="built_in">int</span>, b: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">    time.sleep(<span class="number">10</span>)</span><br><span class="line">    <span class="keyword">return</span> a + b</span><br></pre></td></tr></table></figure>

<h3 id="12-4-main-py"><a href="#12-4-main-py" class="headerlink" title="12.4 main.py"></a>12.4 <code>main.py</code></h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"><span class="keyword">from</span> celery.result <span class="keyword">import</span> AsyncResult</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> celery_app <span class="keyword">import</span> celery_app</span><br><span class="line"><span class="keyword">from</span> tasks <span class="keyword">import</span> long_time_add</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/tasks/add&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_add_task</span>(<span class="params">a: <span class="built_in">int</span>, b: <span class="built_in">int</span></span>):</span><br><span class="line">    task = long_time_add.delay(a, b)</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;message&quot;</span>: <span class="string">&quot;task submitted&quot;</span>,</span><br><span class="line">        <span class="string">&quot;task_id&quot;</span>: task.<span class="built_in">id</span>,</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/tasks/&#123;task_id&#125;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_task_result</span>(<span class="params">task_id: <span class="built_in">str</span></span>):</span><br><span class="line">    task = AsyncResult(task_id, app=celery_app)</span><br><span class="line"></span><br><span class="line">    response = &#123;</span><br><span class="line">        <span class="string">&quot;task_id&quot;</span>: task.<span class="built_in">id</span>,</span><br><span class="line">        <span class="string">&quot;status&quot;</span>: task.status,</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> task.successful():</span><br><span class="line">        response[<span class="string">&quot;result&quot;</span>] = task.result</span><br><span class="line">    <span class="keyword">elif</span> task.failed():</span><br><span class="line">        response[<span class="string">&quot;error&quot;</span>] = <span class="built_in">str</span>(task.result)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> response</span><br></pre></td></tr></table></figure>

<h3 id="12-5-启动方式"><a href="#12-5-启动方式" class="headerlink" title="12.5 启动方式"></a>12.5 启动方式</h3><p>启动 FastAPI：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uvicorn main:app --reload</span><br></pre></td></tr></table></figure>

<p>启动 Worker：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">celery -A tasks worker -l info</span><br></pre></td></tr></table></figure>

<p>请求流程：</p>
<ol>
<li>调 <code>POST /tasks/add?a=3&amp;b=5</code></li>
<li>接口立即返回 <code>task_id</code></li>
<li>再调 <code>GET /tasks/&#123;task_id&#125;</code></li>
<li>看任务是 <code>PENDING</code>、<code>STARTED</code> 还是 <code>SUCCESS</code></li>
</ol>
<p>这个模式非常适合前后端分离项目。</p>
<h2 id="13-FastAPI-Celery-案例-2：文件处理任务"><a href="#13-FastAPI-Celery-案例-2：文件处理任务" class="headerlink" title="13. FastAPI + Celery 案例 2：文件处理任务"></a>13. FastAPI + Celery 案例 2：文件处理任务</h2><p>这个场景很常见，比如：</p>
<ul>
<li>上传 PDF 后做文本提取</li>
<li>上传图片后做缩略图</li>
<li>上传音频后做转写</li>
</ul>
<h3 id="13-1-设计原则"><a href="#13-1-设计原则" class="headerlink" title="13.1 设计原则"></a>13.1 设计原则</h3><p>不要把 <code>UploadFile</code>、数据库连接、请求对象直接传给 Celery。</p>
<p>原因：</p>
<ul>
<li>Celery 任务参数通常需要能序列化</li>
<li>这些对象往往不能直接被序列化</li>
<li>跨进程后对象本身也失效了</li>
</ul>
<p>正确做法：</p>
<ol>
<li>FastAPI 先把文件保存到磁盘或对象存储</li>
<li>把 <code>file_path</code> 或 <code>file_url</code> 传给 Celery</li>
<li>Worker 根据路径再去处理文件</li>
</ol>
<h3 id="13-2-示例"><a href="#13-2-示例" class="headerlink" title="13.2 示例"></a>13.2 示例</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pathlib <span class="keyword">import</span> Path</span><br><span class="line"><span class="keyword">import</span> shutil</span><br><span class="line"><span class="keyword">import</span> uuid</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI, File, UploadFile</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> tasks <span class="keyword">import</span> process_pdf</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line">UPLOAD_DIR = Path(<span class="string">&quot;uploads&quot;</span>)</span><br><span class="line">UPLOAD_DIR.mkdir(exist_ok=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/files/pdf&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">upload_pdf</span>(<span class="params">file: UploadFile = File(<span class="params">...</span>)</span>):</span><br><span class="line">    file_id = <span class="string">f&quot;<span class="subst">&#123;uuid.uuid4()&#125;</span>_<span class="subst">&#123;file.filename&#125;</span>&quot;</span></span><br><span class="line">    save_path = UPLOAD_DIR / file_id</span><br><span class="line"></span><br><span class="line">    <span class="keyword">with</span> save_path.<span class="built_in">open</span>(<span class="string">&quot;wb&quot;</span>) <span class="keyword">as</span> buffer:</span><br><span class="line">        shutil.copyfileobj(file.file, buffer)</span><br><span class="line"></span><br><span class="line">    task = process_pdf.delay(<span class="built_in">str</span>(save_path))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;file_path&quot;</span>: <span class="built_in">str</span>(save_path),</span><br><span class="line">        <span class="string">&quot;task_id&quot;</span>: task.<span class="built_in">id</span>,</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>对应的 Celery 任务：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> celery_app <span class="keyword">import</span> celery_app</span><br><span class="line"></span><br><span class="line"><span class="meta">@celery_app.task(<span class="params">name=<span class="string">&quot;tasks.process_pdf&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">process_pdf</span>(<span class="params">file_path: <span class="built_in">str</span></span>):</span><br><span class="line">    <span class="comment"># 这里做 PDF 文本抽取、切分、向量化等操作</span></span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;file_path&quot;</span>: file_path,</span><br><span class="line">        <span class="string">&quot;message&quot;</span>: <span class="string">&quot;processed&quot;</span>,</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>这个思路很适合 AI 项目里的文档解析链路。</p>
<h2 id="14-FastAPI-Celery-案例-3：模型推理任务"><a href="#14-FastAPI-Celery-案例-3：模型推理任务" class="headerlink" title="14. FastAPI + Celery 案例 3：模型推理任务"></a>14. FastAPI + Celery 案例 3：模型推理任务</h2><p>对于 AI 模型开发，这个场景更有代表性。</p>
<p>例如：</p>
<ul>
<li>文本摘要</li>
<li>图片分类</li>
<li>批量 embedding</li>
<li>长文本抽取</li>
</ul>
<h3 id="14-1-为什么适合-Celery"><a href="#14-1-为什么适合-Celery" class="headerlink" title="14.1 为什么适合 Celery"></a>14.1 为什么适合 Celery</h3><p>因为模型推理通常具备这些特点：</p>
<ul>
<li>耗时不稳定</li>
<li>资源消耗高</li>
<li>可能需要排队</li>
<li>可能要分 CPU &#x2F; GPU Worker</li>
</ul>
<h3 id="14-2-一个简化版例子"><a href="#14-2-一个简化版例子" class="headerlink" title="14.2 一个简化版例子"></a>14.2 一个简化版例子</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> tasks <span class="keyword">import</span> run_inference</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/infer&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_inference_task</span>(<span class="params">text: <span class="built_in">str</span></span>):</span><br><span class="line">    task = run_inference.apply_async(</span><br><span class="line">        kwargs=&#123;<span class="string">&quot;text&quot;</span>: text&#125;,</span><br><span class="line">        queue=<span class="string">&quot;gpu&quot;</span>,</span><br><span class="line">        routing_key=<span class="string">&quot;gpu.infer&quot;</span>,</span><br><span class="line">    )</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;task_id&quot;</span>: task.<span class="built_in">id</span>&#125;</span><br></pre></td></tr></table></figure>

<p>对应任务：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> celery_app <span class="keyword">import</span> celery_app</span><br><span class="line"></span><br><span class="line"><span class="meta">@celery_app.task(<span class="params">name=<span class="string">&quot;tasks.run_inference&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">run_inference</span>(<span class="params">text: <span class="built_in">str</span></span>):</span><br><span class="line">    time.sleep(<span class="number">5</span>)</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;text&quot;</span>: text,</span><br><span class="line">        <span class="string">&quot;label&quot;</span>: <span class="string">&quot;positive&quot;</span>,</span><br><span class="line">        <span class="string">&quot;score&quot;</span>: <span class="number">0.98</span>,</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>这里的重点不在模型代码本身，而在架构思路：</p>
<ul>
<li>API 层只负责接收请求</li>
<li>推理层在 Worker 中执行</li>
<li>GPU 任务路由到专门队列</li>
<li>可以单独扩容推理 Worker</li>
</ul>
<p>如果将来一张 GPU 卡只跑一个 Worker，也很好管理。</p>
<h2 id="15-实战里很重要的细节"><a href="#15-实战里很重要的细节" class="headerlink" title="15. 实战里很重要的细节"></a>15. 实战里很重要的细节</h2><h3 id="15-1-任务参数尽量简单"><a href="#15-1-任务参数尽量简单" class="headerlink" title="15.1 任务参数尽量简单"></a>15.1 任务参数尽量简单</h3><p>建议只传这些：</p>
<ul>
<li>字符串</li>
<li>数字</li>
<li>布尔值</li>
<li>列表 &#x2F; 字典</li>
<li>文件路径</li>
<li>数据库主键 ID</li>
</ul>
<p>不建议直接传：</p>
<ul>
<li>数据库 Session</li>
<li>请求对象 <code>Request</code></li>
<li><code>UploadFile</code></li>
<li>大模型实例</li>
<li>打开的文件句柄</li>
</ul>
<p>原则：</p>
<blockquote>
<p>任务参数最好是“可序列化、可重建、可追踪”的。</p>
</blockquote>
<h3 id="15-2-任务尽量幂等"><a href="#15-2-任务尽量幂等" class="headerlink" title="15.2 任务尽量幂等"></a>15.2 任务尽量幂等</h3><p>幂等的意思是：同一个任务重复执行多次，结果最好一致，或者至少不会造成严重副作用。</p>
<p>因为这些情况都可能导致重复执行：</p>
<ul>
<li>Worker 崩溃</li>
<li>消息重复投递</li>
<li>任务重试</li>
<li>手动补偿</li>
</ul>
<p>例如发通知、写数据库、扣费等场景，幂等性非常重要。</p>
<h3 id="15-3-不要在-API-进程里执行重活"><a href="#15-3-不要在-API-进程里执行重活" class="headerlink" title="15.3 不要在 API 进程里执行重活"></a>15.3 不要在 API 进程里执行重活</h3><p>FastAPI 本身应该尽量保持：</p>
<ul>
<li>响应快</li>
<li>逻辑薄</li>
<li>只做参数校验、鉴权、任务分发</li>
</ul>
<p>重计算、长耗时 IO、批处理，尽量放 Worker。</p>
<h3 id="15-4-结果不是必须永久保存"><a href="#15-4-结果不是必须永久保存" class="headerlink" title="15.4 结果不是必须永久保存"></a>15.4 结果不是必须永久保存</h3><p>很多项目会犯一个问题：任务结果一直存 Redis，时间一久就堆积很多垃圾数据。</p>
<p>可以设置：</p>
<ul>
<li><code>result_expires</code></li>
<li>任务完成后主动清理</li>
<li>只保留必要结果，不保留大对象</li>
</ul>
<p>如果结果很大，不要直接把大块内容塞进 Redis，建议落库或存文件，再返回引用地址。</p>
<h3 id="15-5-错误处理不要只靠日志"><a href="#15-5-错误处理不要只靠日志" class="headerlink" title="15.5 错误处理不要只靠日志"></a>15.5 错误处理不要只靠日志</h3><p>建议至少做到：</p>
<ul>
<li>任务失败后能看到 <code>FAILURE</code></li>
<li>能根据 <code>task_id</code> 查到失败原因</li>
<li>关键任务开启自动重试</li>
<li>必要时记录业务日志或落库</li>
</ul>
<h3 id="15-6-CPU-GPU-IO-任务分开"><a href="#15-6-CPU-GPU-IO-任务分开" class="headerlink" title="15.6 CPU &#x2F; GPU &#x2F; IO 任务分开"></a>15.6 CPU &#x2F; GPU &#x2F; IO 任务分开</h3><p>这点在 AI 项目里尤其重要。</p>
<p>建议：</p>
<ul>
<li>文件下载、上传、请求第三方 API 放一个队列</li>
<li>CPU 预处理放一个队列</li>
<li>GPU 推理放一个队列</li>
</ul>
<p>这样不会出现“一个长推理任务把所有普通任务都堵住”的问题。</p>
<h3 id="15-7-Worker-并发要按任务类型调"><a href="#15-7-Worker-并发要按任务类型调" class="headerlink" title="15.7 Worker 并发要按任务类型调"></a>15.7 Worker 并发要按任务类型调</h3><p>Celery 的并发不是越大越好，要看任务类型。</p>
<p>一般经验：</p>
<ul>
<li>IO 密集型任务可以适当提高并发</li>
<li>CPU 密集型任务并发通常不要超过 CPU 核心数太多</li>
<li>GPU 推理任务通常一个 Worker 进程绑定一张卡，甚至一个队列只开 <code>--concurrency=1</code></li>
</ul>
<p>例如 GPU Worker 常见写法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">celery -A tasks worker -Q gpu --concurrency=1 -l info</span><br></pre></td></tr></table></figure>

<p>如果推理模型本身已经很吃显存，再盲目开高并发，反而容易把机器打爆。</p>
<h2 id="16-常见坑"><a href="#16-常见坑" class="headerlink" title="16. 常见坑"></a>16. 常见坑</h2><h3 id="16-1-任务提交成功，但一直不执行"><a href="#16-1-任务提交成功，但一直不执行" class="headerlink" title="16.1 任务提交成功，但一直不执行"></a>16.1 任务提交成功，但一直不执行</h3><p>通常排查：</p>
<ul>
<li>Redis 是否启动</li>
<li>Worker 是否启动</li>
<li><code>-A</code> 指向的模块是否正确</li>
<li>任务有没有注册到当前 Worker</li>
<li>是否路由到了某个没人消费的队列</li>
</ul>
<h3 id="16-2-能查到任务-ID，但拿不到结果"><a href="#16-2-能查到任务-ID，但拿不到结果" class="headerlink" title="16.2 能查到任务 ID，但拿不到结果"></a>16.2 能查到任务 ID，但拿不到结果</h3><p>通常是：</p>
<ul>
<li>没配置 <code>backend</code></li>
<li><code>task_id</code> 查错了</li>
<li>结果过期被清掉了</li>
</ul>
<h3 id="16-3-任务函数里引用了-Web-层对象"><a href="#16-3-任务函数里引用了-Web-层对象" class="headerlink" title="16.3 任务函数里引用了 Web 层对象"></a>16.3 任务函数里引用了 Web 层对象</h3><p>比如：</p>
<ul>
<li>直接把 <code>Request</code> 传进任务</li>
<li>把 SQLAlchemy Session 传进任务</li>
<li>把上传文件对象传进任务</li>
</ul>
<p>这类设计一般都会有问题。任务应该依赖“可序列化参数”，不要依赖当前请求上下文。</p>
<h3 id="16-4-把-Celery-当成同步-RPC-用"><a href="#16-4-把-Celery-当成同步-RPC-用" class="headerlink" title="16.4 把 Celery 当成同步 RPC 用"></a>16.4 把 Celery 当成同步 RPC 用</h3><p>有些人虽然用了 Celery，但提交完任务后立刻 <code>.get()</code> 等待结果，这样就又退回同步调用了。</p>
<p>如果接口本身必须立刻拿到结果，那就要重新评估：</p>
<ul>
<li>这个任务是否真的适合放到 Celery</li>
<li>是否应该走同步推理接口</li>
<li>是否应该拆成“提交任务 + 轮询结果”</li>
</ul>
<h2 id="17-一套比较实用的学习顺序"><a href="#17-一套比较实用的学习顺序" class="headerlink" title="17. 一套比较实用的学习顺序"></a>17. 一套比较实用的学习顺序</h2><p>建议按这个顺序掌握：</p>
<ol>
<li>先跑通最小示例</li>
<li>理解 <code>delay()</code>、<code>AsyncResult</code></li>
<li>学会查任务状态</li>
<li>学会重试</li>
<li>学会队列与路由</li>
<li>再接入 FastAPI</li>
<li>最后再做定时任务、监控、扩容</li>
</ol>
<h2 id="18-总结"><a href="#18-总结" class="headerlink" title="18. 总结"></a>18. 总结</h2><p>Celery 的本质就是：</p>
<ul>
<li>把耗时任务从主线程 &#x2F; 主服务里剥离出去</li>
<li>通过消息队列异步执行</li>
<li>通过 Worker 扩展执行能力</li>
<li>通过结果后端查询状态和结果</li>
</ul>
<p>对于 FastAPI 项目，可以把它理解成一组固定分工：</p>
<ul>
<li>FastAPI：对外提供 API</li>
<li>Redis &#x2F; RabbitMQ：负责排队</li>
<li>Celery Worker：真正执行任务</li>
<li>Result Backend：保存状态和结果</li>
</ul>
<p>如果项目里已经出现“接口慢、任务重、需要重试、需要排队、需要异步处理”的问题，那 Celery 基本就是很自然的选择。</p>
<h2 id="19-参考"><a href="#19-参考" class="headerlink" title="19. 参考"></a>19. 参考</h2><ul>
<li>Celery First Steps: <a href="https://docs.celeryq.dev/en/main/getting-started/first-steps-with-celery.html">https://docs.celeryq.dev/en/main/getting-started/first-steps-with-celery.html</a></li>
<li>Celery Calling Tasks: <a href="https://docs.celeryq.dev/en/main/userguide/calling.html">https://docs.celeryq.dev/en/main/userguide/calling.html</a></li>
<li>Celery Tasks Guide: <a href="https://docs.celeryq.dev/en/main/userguide/tasks.html">https://docs.celeryq.dev/en/main/userguide/tasks.html</a></li>
<li>Celery Routing Tasks: <a href="https://docs.celeryq.dev/en/main/userguide/routing.html">https://docs.celeryq.dev/en/main/userguide/routing.html</a></li>
<li>Celery Configuration: <a href="https://docs.celeryq.dev/en/main/userguide/configuration.html">https://docs.celeryq.dev/en/main/userguide/configuration.html</a></li>
<li>FastAPI Background Tasks: <a href="https://fastapi.tiangolo.com/tutorial/background-tasks/">https://fastapi.tiangolo.com/tutorial/background-tasks/</a></li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>框架</category>
      </categories>
  </entry>
  <entry>
    <title>Qwen 模型演进笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E9%83%A8%E7%BD%B2/qwen%E6%A8%A1%E5%9E%8B%E6%BC%94%E8%BF%9B/</url>
    <content><![CDATA[<h1 id="Qwen-模型演进笔记"><a href="#Qwen-模型演进笔记" class="headerlink" title="Qwen 模型演进笔记"></a>Qwen 模型演进笔记</h1><h2 id="1-先说明“最新模型”指的是谁"><a href="#1-先说明“最新模型”指的是谁" class="headerlink" title="1. 先说明“最新模型”指的是谁"></a>1. 先说明“最新模型”指的是谁</h2><p>截至 <code>2026-04-20</code>，如果说“Qwen 最新模型”，其实要分两类看：</p>
<ul>
<li>最新通用 API 旗舰模型：<code>Qwen3.6-Plus</code><br>发布时间：<code>2026-04-02</code></li>
<li>最新开源主力模型：<code>Qwen3.6-35B-A3B</code><br>发布时间：<code>2026-04-17</code></li>
</ul>
<p>如果你更关心：</p>
<ul>
<li>云端调用、Agent、编程能力：重点看 <code>Qwen3.6-Plus</code></li>
<li>本地部署、开源权重、性价比：重点看 <code>Qwen3.6-35B-A3B</code></li>
</ul>
<hr>
<h2 id="2-Qwen2-5-是什么"><a href="#2-Qwen2-5-是什么" class="headerlink" title="2. Qwen2.5 是什么"></a>2. Qwen2.5 是什么</h2><p><code>Qwen2.5</code> 是 Qwen 在 <code>2024-09-19</code> 发布的一代重要开源模型系列。</p>
<p>这一代的定位可以理解成：</p>
<blockquote>
<p>把“通用聊天模型”真正做到了更稳、更全、更适合工程落地。</p>
</blockquote>
<p>Qwen2.5 这一代包含：</p>
<ul>
<li>通用语言模型：<code>Qwen2.5</code></li>
<li>代码模型：<code>Qwen2.5-Coder</code></li>
<li>数学模型：<code>Qwen2.5-Math</code></li>
</ul>
<p>开源尺寸覆盖比较广：</p>
<ul>
<li><code>0.5B</code></li>
<li><code>1.5B</code></li>
<li><code>3B</code></li>
<li><code>7B</code></li>
<li><code>14B</code></li>
<li><code>32B</code></li>
<li><code>72B</code></li>
</ul>
<hr>
<h2 id="3-Qwen2-5-的主要改进点"><a href="#3-Qwen2-5-的主要改进点" class="headerlink" title="3. Qwen2.5 的主要改进点"></a>3. Qwen2.5 的主要改进点</h2><p>根据官方介绍，Qwen2.5 相比 Qwen2 的核心提升主要在下面几块：</p>
<h3 id="3-1-知识、代码、数学能力更强"><a href="#3-1-知识、代码、数学能力更强" class="headerlink" title="3.1 知识、代码、数学能力更强"></a>3.1 知识、代码、数学能力更强</h3><p>官方提到 Qwen2.5 基于最多 <code>18T tokens</code> 的大规模数据训练，相比 Qwen2：</p>
<ul>
<li>知识能力更强</li>
<li>代码能力提升明显</li>
<li>数学能力提升明显</li>
</ul>
<p>这说明 Qwen2.5 不只是“能聊天”，而是开始更像一个泛用底座模型。</p>
<h3 id="3-2-指令跟随更稳"><a href="#3-2-指令跟随更稳" class="headerlink" title="3.2 指令跟随更稳"></a>3.2 指令跟随更稳</h3><p>Qwen2.5 一个很实用的提升是：</p>
<ul>
<li>更能听懂要求</li>
<li>更能按要求输出</li>
<li>对不同 system prompt 更稳</li>
</ul>
<p>这在工程里很重要，因为很多模型不是“不会做”，而是：</p>
<ul>
<li>格式不稳</li>
<li>角色设定容易跑偏</li>
<li>提示词一换风格就漂</li>
</ul>
<p>Qwen2.5 在这方面明显更适合做产品。</p>
<h3 id="3-3-结构化输出更好"><a href="#3-3-结构化输出更好" class="headerlink" title="3.3 结构化输出更好"></a>3.3 结构化输出更好</h3><p>Qwen2.5 官方特别强调了这几点：</p>
<ul>
<li>更理解结构化数据</li>
<li>更擅长表格理解</li>
<li>更擅长输出结构化结果</li>
<li>特别是 <code>JSON</code> 输出更可靠</li>
</ul>
<p>这个优点对工程很关键，因为很多业务不是只要一段自然语言，而是要：</p>
<ul>
<li>JSON</li>
<li>表单字段</li>
<li>工具调用参数</li>
<li>SQL &#x2F; API 请求体</li>
</ul>
<h3 id="3-4-长文本生成更实用"><a href="#3-4-长文本生成更实用" class="headerlink" title="3.4 长文本生成更实用"></a>3.4 长文本生成更实用</h3><p>Qwen2.5 支持：</p>
<ul>
<li>最长 <code>128K</code> 上下文</li>
<li>最多 <code>8K</code> 生成长度</li>
</ul>
<p>对长文总结、长对话、多轮问答、文档问答更友好。</p>
<h3 id="3-5-多语言能力更强"><a href="#3-5-多语言能力更强" class="headerlink" title="3.5 多语言能力更强"></a>3.5 多语言能力更强</h3><p>Qwen2.5 支持 <code>29+</code> 种语言，继续保持中英双强，同时兼顾多语种能力。</p>
<h3 id="3-6-专项模型更成熟"><a href="#3-6-专项模型更成熟" class="headerlink" title="3.6 专项模型更成熟"></a>3.6 专项模型更成熟</h3><p>这一代不只是通用模型升级，专项模型也更成体系：</p>
<ul>
<li><code>Qwen2.5-Coder</code>：代码相关数据更多，适合编程助手</li>
<li><code>Qwen2.5-Math</code>：支持中英文数学问题，并引入 <code>CoT</code>、<code>PoT</code>、<code>TIR</code></li>
</ul>
<p>这意味着：</p>
<blockquote>
<p>Qwen2.5 开始不是“一个模型打天下”，而是形成了通用底座 + 专项模型的完整产品线。</p>
</blockquote>
<hr>
<h2 id="4-Qwen2-5-的优点"><a href="#4-Qwen2-5-的优点" class="headerlink" title="4. Qwen2.5 的优点"></a>4. Qwen2.5 的优点</h2><p>如果从“开发者和部署者”的角度看，Qwen2.5 的优点主要有这些：</p>
<ul>
<li>尺寸覆盖广，小到 <code>0.5B</code>，大到 <code>72B</code>，方便按机器选型</li>
<li>作为 <code>dense model</code>，结构相对直观，部署门槛低</li>
<li><code>128K</code> 上下文对很多 RAG 和长文场景已经够用</li>
<li>指令跟随和 JSON 输出更稳，适合业务接入</li>
<li><code>Qwen2.5-Coder</code>、<code>Qwen2.5-Math</code> 让专项任务更好选型</li>
<li>已经很好适配 <code>Transformers</code>、<code>vLLM</code>、<code>Ollama</code></li>
</ul>
<p>一句话概括：</p>
<blockquote>
<p>Qwen2.5 的强项是“稳、全、好落地”，非常适合作为通用开源基座。</p>
</blockquote>
<hr>
<h2 id="5-从-Qwen2-5-到最新模型，发生了什么变化"><a href="#5-从-Qwen2-5-到最新模型，发生了什么变化" class="headerlink" title="5. 从 Qwen2.5 到最新模型，发生了什么变化"></a>5. 从 Qwen2.5 到最新模型，发生了什么变化</h2><p>如果把后续几代串起来看，Qwen 的升级路线很清楚：</p>
<h3 id="Qwen2-5"><a href="#Qwen2-5" class="headerlink" title="Qwen2.5"></a>Qwen2.5</h3><ul>
<li>重点是把通用基础能力做强</li>
<li>强化结构化输出、长文本、代码、数学</li>
</ul>
<h3 id="Qwen3"><a href="#Qwen3" class="headerlink" title="Qwen3"></a>Qwen3</h3><ul>
<li>重点是把“推理模式 + 快速模式”融合</li>
<li>开始明显强化 agent 和 reasoning 路线</li>
</ul>
<h3 id="Qwen3-5"><a href="#Qwen3-5" class="headerlink" title="Qwen3.5"></a>Qwen3.5</h3><ul>
<li>重点转向“原生多模态 Agent”</li>
<li>不再只是文本模型，而是朝着文字、图像、音频、视频统一理解发展</li>
</ul>
<h3 id="Qwen3-6"><a href="#Qwen3-6" class="headerlink" title="Qwen3.6"></a>Qwen3.6</h3><ul>
<li>重点是把 Agent 真正往实战推进</li>
<li>明显强化编程 Agent、工具调用、长链路任务执行、多模态推理</li>
</ul>
<p>一句话看演进：</p>
<blockquote>
<p>Qwen2.5 更像一个强通用模型，Qwen3.x 更像朝“能思考、能看、能调用工具、能完成任务”的 Agent 底座在走。</p>
</blockquote>
<hr>
<h2 id="6-Qwen3：相对-Qwen2-5-的关键升级"><a href="#6-Qwen3：相对-Qwen2-5-的关键升级" class="headerlink" title="6. Qwen3：相对 Qwen2.5 的关键升级"></a>6. Qwen3：相对 Qwen2.5 的关键升级</h2><p><code>Qwen3</code> 发布于 <code>2025-04-29</code>，它是 Qwen 从“强聊天模型”走向“推理模型 + Agent 模型”的关键一代。</p>
<h3 id="6-1-Hybrid-Thinking-Modes"><a href="#6-1-Hybrid-Thinking-Modes" class="headerlink" title="6.1 Hybrid Thinking Modes"></a>6.1 Hybrid Thinking Modes</h3><p>Qwen3 引入了一个很重要的设计：</p>
<ul>
<li><code>Thinking Mode</code></li>
<li><code>Non-Thinking Mode</code></li>
</ul>
<p>也就是：</p>
<ul>
<li>复杂问题时，可以多想一会</li>
<li>简单问题时，可以直接快速回答</li>
</ul>
<p>这个设计的意义很大，因为它开始把“推理预算”显式化。</p>
<h3 id="6-2-训练数据规模显著提升"><a href="#6-2-训练数据规模显著提升" class="headerlink" title="6.2 训练数据规模显著提升"></a>6.2 训练数据规模显著提升</h3><p>Qwen3 官方提到：</p>
<ul>
<li>Qwen2.5：约 <code>18T tokens</code></li>
<li>Qwen3：约 <code>36T tokens</code></li>
</ul>
<p>也就是差不多翻倍。</p>
<h3 id="6-3-多语言支持明显扩展"><a href="#6-3-多语言支持明显扩展" class="headerlink" title="6.3 多语言支持明显扩展"></a>6.3 多语言支持明显扩展</h3><p>Qwen3 支持 <code>119</code> 种语言和方言，相比 Qwen2.5 的 <code>29+</code> 更进一步。</p>
<h3 id="6-4-Agent-和编码能力更强"><a href="#6-4-Agent-和编码能力更强" class="headerlink" title="6.4 Agent 和编码能力更强"></a>6.4 Agent 和编码能力更强</h3><p>官方明确提到：</p>
<ul>
<li>强化了 coding 和 agentic capabilities</li>
<li>增强了对 <code>MCP</code> 的支持</li>
</ul>
<p>这说明 Qwen3 不只是问答模型，而是开始面向：</p>
<ul>
<li>工具调用</li>
<li>环境交互</li>
<li>多步任务执行</li>
</ul>
<h3 id="6-5-MoE-带来更好的效率"><a href="#6-5-MoE-带来更好的效率" class="headerlink" title="6.5 MoE 带来更好的效率"></a>6.5 MoE 带来更好的效率</h3><p>Qwen3 这一代不仅有 dense 模型，也有 MoE 模型：</p>
<ul>
<li><code>Qwen3-30B-A3B</code></li>
<li><code>Qwen3-235B-A22B</code></li>
</ul>
<p>MoE 的意义是：</p>
<blockquote>
<p>总参数很大，但每次推理只激活一部分参数，从而兼顾能力和成本。</p>
</blockquote>
<p>官方还提到：</p>
<ul>
<li><code>Qwen3-4B</code> 可以逼近 <code>Qwen2.5-72B-Instruct</code></li>
<li>Qwen3 的 MoE 模型可以用更少激活参数达到接近 Qwen2.5 dense 模型的效果</li>
</ul>
<hr>
<h2 id="7-Qwen3-5：相对-Qwen2-5-Qwen3-的关键升级"><a href="#7-Qwen3-5：相对-Qwen2-5-Qwen3-的关键升级" class="headerlink" title="7. Qwen3.5：相对 Qwen2.5 &#x2F; Qwen3 的关键升级"></a>7. Qwen3.5：相对 Qwen2.5 &#x2F; Qwen3 的关键升级</h2><p><code>Qwen3.5</code> 发布于 <code>2026-02-17</code>。</p>
<p>这一代最重要的变化不是单点指标，而是方向变了：</p>
<blockquote>
<p>从文本大模型，进一步走向“原生多模态 Agent”。</p>
</blockquote>
<h3 id="7-1-原生多模态"><a href="#7-1-原生多模态" class="headerlink" title="7.1 原生多模态"></a>7.1 原生多模态</h3><p>官方将 <code>Qwen3.5-397B-A17B</code> 定义为：</p>
<ul>
<li>native vision-language model</li>
</ul>
<p>也就是说它不是简单地“LLM 外接视觉模块”，而是更强调模型本身对多模态信息的统一处理。</p>
<h3 id="7-2-架构更先进"><a href="#7-2-架构更先进" class="headerlink" title="7.2 架构更先进"></a>7.2 架构更先进</h3><p>Qwen3.5 采用：</p>
<ul>
<li><code>linear attention</code></li>
<li><code>Gated Delta Networks</code></li>
<li><code>sparse MoE</code></li>
</ul>
<p>官方给出的核心信息是：</p>
<ul>
<li>总参数 <code>397B</code></li>
<li>每次前向仅激活 <code>17B</code></li>
</ul>
<p>这类架构的目标很明确：</p>
<ul>
<li>降低推理成本</li>
<li>提高长上下文和多模态处理效率</li>
</ul>
<h3 id="7-3-语言覆盖更大"><a href="#7-3-语言覆盖更大" class="headerlink" title="7.3 语言覆盖更大"></a>7.3 语言覆盖更大</h3><p>Qwen3.5 把语言和方言支持从 <code>119</code> 扩展到 <code>201</code></p>
<p>这意味着它更适合：</p>
<ul>
<li>全球化产品</li>
<li>多语检索</li>
<li>多语客服</li>
<li>多语 Agent</li>
</ul>
<h3 id="7-4-Hosted-版本更适合做-Agent"><a href="#7-4-Hosted-版本更适合做-Agent" class="headerlink" title="7.4 Hosted 版本更适合做 Agent"></a>7.4 Hosted 版本更适合做 Agent</h3><p>官方提到 <code>Qwen3.5-Plus</code> 具备：</p>
<ul>
<li>默认 <code>1M</code> context window</li>
<li>官方内置工具</li>
<li>adaptive tool use</li>
</ul>
<p>这和 Qwen2.5 的差别已经非常明显了。</p>
<p>Qwen2.5 还是：</p>
<ul>
<li>更偏“问答 &#x2F; 聊天 &#x2F; 通用推理”</li>
</ul>
<p>而 Qwen3.5-Plus 开始变成：</p>
<ul>
<li>更偏“能理解环境、能调用工具、能在长上下文里持续工作”</li>
</ul>
<hr>
<h2 id="8-Qwen3-6-Plus：截至-2026-04-20-的最新-API-旗舰"><a href="#8-Qwen3-6-Plus：截至-2026-04-20-的最新-API-旗舰" class="headerlink" title="8. Qwen3.6-Plus：截至 2026-04-20 的最新 API 旗舰"></a>8. Qwen3.6-Plus：截至 2026-04-20 的最新 API 旗舰</h2><p><code>Qwen3.6-Plus</code> 发布于 <code>2026-04-02</code>。</p>
<p>如果你问“现在最强的 Qwen 云端 Agent 模型是谁”，答案基本就是它。</p>
<h3 id="8-1-相比-Qwen3-5-Plus-的主要改进"><a href="#8-1-相比-Qwen3-5-Plus-的主要改进" class="headerlink" title="8.1 相比 Qwen3.5-Plus 的主要改进"></a>8.1 相比 Qwen3.5-Plus 的主要改进</h3><p>官方给出的核心升级点非常明确：</p>
<ul>
<li>编程 Agent 能力显著增强</li>
<li>多模态感知和推理更强</li>
<li>默认 <code>1M</code> 上下文</li>
<li>更稳定、更可靠，更面向真实开发者场景</li>
</ul>
<h3 id="8-2-它最强的地方是什么"><a href="#8-2-它最强的地方是什么" class="headerlink" title="8.2 它最强的地方是什么"></a>8.2 它最强的地方是什么</h3><p>Qwen3.6-Plus 最大的变化不是“普通聊天更会聊”，而是：</p>
<ul>
<li>更会写代码</li>
<li>更会改代码</li>
<li>更会处理仓库级任务</li>
<li>更会终端操作和工具调用</li>
<li>更适合长链路、多步骤任务</li>
</ul>
<p>官方原话对应的方向大致可以概括为：</p>
<ul>
<li>reasoning</li>
<li>memory</li>
<li>execution</li>
</ul>
<p>这三个能力被整合得更紧了。</p>
<h3 id="8-3-它的优点"><a href="#8-3-它的优点" class="headerlink" title="8.3 它的优点"></a>8.3 它的优点</h3><p>从实际使用角度看，Qwen3.6-Plus 的优点主要是：</p>
<ul>
<li>更强的 Agentic Coding</li>
<li>更适合复杂仓库理解</li>
<li>更适合前端开发和自动化任务</li>
<li>更适合长任务规划</li>
<li>更适合工具调用和多轮任务执行</li>
<li>更适合多模态输入场景</li>
</ul>
<p>如果你要做的是：</p>
<ul>
<li>AI 编程助手</li>
<li>自动化开发 Agent</li>
<li>Repo 级代码分析</li>
<li>多步工作流 Agent</li>
</ul>
<p>那 Qwen3.6-Plus 的定位已经明显高于 Qwen2.5。</p>
<hr>
<h2 id="9-Qwen3-6-35B-A3B：截至-2026-04-20-的最新开源主力"><a href="#9-Qwen3-6-35B-A3B：截至-2026-04-20-的最新开源主力" class="headerlink" title="9. Qwen3.6-35B-A3B：截至 2026-04-20 的最新开源主力"></a>9. Qwen3.6-35B-A3B：截至 2026-04-20 的最新开源主力</h2><p><code>Qwen3.6-35B-A3B</code> 发布于 <code>2026-04-17</code>。</p>
<p>这是一款很值得关注的模型，因为它很能代表现在开源模型的方向：</p>
<blockquote>
<p>用更少的激活参数，做出更强的 Agent 和编码能力。</p>
</blockquote>
<h3 id="9-1-它的核心特点"><a href="#9-1-它的核心特点" class="headerlink" title="9.1 它的核心特点"></a>9.1 它的核心特点</h3><ul>
<li><code>35B</code> 总参数</li>
<li><code>3B</code> 激活参数</li>
<li>MoE 架构</li>
<li>原生多模态</li>
<li>兼顾 <code>thinking</code> 和 <code>non-thinking</code></li>
</ul>
<h3 id="9-2-相比上一代的改进"><a href="#9-2-相比上一代的改进" class="headerlink" title="9.2 相比上一代的改进"></a>9.2 相比上一代的改进</h3><p>官方强调它：</p>
<ul>
<li>大幅超过 <code>Qwen3.5-35B-A3B</code></li>
<li>在 agentic coding 上提升明显</li>
<li>能与更大的 dense 模型竞争</li>
</ul>
<p>尤其适合：</p>
<ul>
<li>本地部署</li>
<li>开源私有化</li>
<li>成本敏感场景</li>
<li>想要较强代码 Agent 能力，但又不想上超大模型</li>
</ul>
<h3 id="9-3-它的优点"><a href="#9-3-它的优点" class="headerlink" title="9.3 它的优点"></a>9.3 它的优点</h3><ul>
<li>开源，适合自部署</li>
<li>激活参数低，推理成本相对更友好</li>
<li>代码 Agent 能力强</li>
<li>多模态能力也不错</li>
<li>能对接 <code>OpenClaw</code>、<code>Claude Code</code>、<code>Qwen Code</code></li>
<li>支持 <code>preserve_thinking</code>，更适合连续 Agent 任务</li>
</ul>
<p>一句话概括：</p>
<blockquote>
<p>如果 Qwen3.6-Plus 是云端旗舰，那么 Qwen3.6-35B-A3B 更像是“开源部署里很有竞争力的一把刀”。</p>
</blockquote>
<hr>
<h2 id="10-Qwen2-5-和最新模型怎么对比"><a href="#10-Qwen2-5-和最新模型怎么对比" class="headerlink" title="10. Qwen2.5 和最新模型怎么对比"></a>10. Qwen2.5 和最新模型怎么对比</h2><table>
<thead>
<tr>
<th>维度</th>
<th>Qwen2.5</th>
<th>Qwen3.6-Plus &#x2F; Qwen3.6-35B-A3B</th>
</tr>
</thead>
<tbody><tr>
<td>主要定位</td>
<td>通用基础模型</td>
<td>Agent 化、编程化、多模态化</td>
</tr>
<tr>
<td>核心优势</td>
<td>稳定、通用、结构化输出好</td>
<td>会思考、会调用工具、会执行复杂任务</td>
</tr>
<tr>
<td>上下文</td>
<td>最高 <code>128K</code></td>
<td>Hosted 版默认可到 <code>1M</code></td>
</tr>
<tr>
<td>输出风格</td>
<td>更适合问答、结构化响应</td>
<td>更适合多步任务、复杂工作流</td>
</tr>
<tr>
<td>代码能力</td>
<td>强，尤其 <code>Qwen2.5-Coder</code></td>
<td>更强调 Agentic Coding 和仓库级任务</td>
</tr>
<tr>
<td>多模态</td>
<td>不是这一代的核心重点</td>
<td>已经是核心方向之一</td>
</tr>
<tr>
<td>部署选择</td>
<td>适合通用本地部署</td>
<td>最新开源版更适合高级 Agent 部署</td>
</tr>
<tr>
<td>成本效率</td>
<td>dense 结构，直观易懂</td>
<td>MoE 更强调“能力 &#x2F; 成本”比</td>
</tr>
</tbody></table>
<hr>
<h2 id="11-怎么选"><a href="#11-怎么选" class="headerlink" title="11. 怎么选"></a>11. 怎么选</h2><p>如果你现在要选型，可以这样理解：</p>
<h3 id="选-Qwen2-5-的场景"><a href="#选-Qwen2-5-的场景" class="headerlink" title="选 Qwen2.5 的场景"></a>选 Qwen2.5 的场景</h3><ul>
<li>想找成熟稳定的通用开源模型</li>
<li>更关注文本问答、RAG、结构化输出</li>
<li>本地部署机器一般，希望模型链路简单</li>
<li>需要一套通用模型 + coder&#x2F;math 专项模型组合</li>
</ul>
<h3 id="选-Qwen3-6-35B-A3B-的场景"><a href="#选-Qwen3-6-35B-A3B-的场景" class="headerlink" title="选 Qwen3.6-35B-A3B 的场景"></a>选 Qwen3.6-35B-A3B 的场景</h3><ul>
<li>想自部署最新开源模型</li>
<li>重点是代码 Agent、任务执行、复杂工作流</li>
<li>希望性能强，但推理成本不要太夸张</li>
<li>希望兼顾多模态能力</li>
</ul>
<h3 id="选-Qwen3-6-Plus-的场景"><a href="#选-Qwen3-6-Plus-的场景" class="headerlink" title="选 Qwen3.6-Plus 的场景"></a>选 Qwen3.6-Plus 的场景</h3><ul>
<li>不执着于开源权重</li>
<li>更看重当前最强云端能力</li>
<li>想做编程 Agent、工具调用 Agent、长链路任务规划</li>
<li>需要默认 <code>1M</code> 上下文和更强的实际执行能力</li>
</ul>
<hr>
<h2 id="12-高频结论"><a href="#12-高频结论" class="headerlink" title="12. 高频结论"></a>12. 高频结论</h2><ul>
<li><code>Qwen2.5</code> 的核心价值是“稳、全、好落地”。</li>
<li><code>Qwen3</code> 开始，Qwen 明显往 reasoning + agent 方向转。</li>
<li><code>Qwen3.5</code> 是“原生多模态 Agent”方向的重要节点。</li>
<li><code>Qwen3.6-Plus</code> 是截至 <code>2026-04-20</code> 的最新 API 旗舰，更偏真实世界 Agent。</li>
<li><code>Qwen3.6-35B-A3B</code> 是截至 <code>2026-04-20</code> 的最新开源主力，更适合自部署和高性价比 Agent 场景。</li>
</ul>
<hr>
<h2 id="13-推荐阅读"><a href="#13-推荐阅读" class="headerlink" title="13. 推荐阅读"></a>13. 推荐阅读</h2><p>建议按下面顺序看：</p>
<ol>
<li><code>Qwen2.5</code>：先理解它为什么是一个成熟通用底座</li>
<li><code>Qwen3</code>：看它如何把 thinking &#x2F; non-thinking 合到一起</li>
<li><code>Qwen3.5</code>：看它怎么走向原生多模态 Agent</li>
<li><code>Qwen3.6-Plus</code>：看最新 Agent 旗舰在“编码 + 工具 + 多模态”上怎么继续升级</li>
<li><code>Qwen3.6-35B-A3B</code>：看开源版本如何在成本和能力之间做平衡</li>
</ol>
<p>官方文章：</p>
<ul>
<li><a href="https://qwenlm.github.io/blog/qwen2.5/">Qwen2.5: A Party of Foundation Models!</a></li>
<li><a href="https://qwenlm.github.io/blog/qwen3/">Qwen3: Think Deeper, Act Faster</a></li>
<li><a href="https://www.alibabacloud.com/blog/602894">Qwen3.5: Towards Native Multimodal Agents</a></li>
<li><a href="https://www.alibabacloud.com/blog/603005">Qwen3.6-Plus: Towards Real World Agents</a></li>
<li><a href="https://www.alibabacloud.com/blog/603043">Qwen3.6-35B-A3B: Agentic Coding Power, Now Open to All</a></li>
</ul>
<hr>
<h2 id="14-一句话总结"><a href="#14-一句话总结" class="headerlink" title="14. 一句话总结"></a>14. 一句话总结</h2><p>Qwen2.5 的关键词是“强通用底座”，而最新的 Qwen3.6 系列关键词已经变成了：</p>
<blockquote>
<p>原生多模态、Agent、编程执行、长上下文、高效率 MoE。</p>
</blockquote>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>大模型部署</category>
      </categories>
  </entry>
  <entry>
    <title>Ray 学习笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/ray/</url>
    <content><![CDATA[<h1 id="Ray-学习笔记"><a href="#Ray-学习笔记" class="headerlink" title="Ray 学习笔记"></a>Ray 学习笔记</h1><p>截至 2026-03-30，这份笔记主要参考：</p>
<ul>
<li>Ray 官方文档 <code>2.54.x</code></li>
<li>Celery 官方稳定版文档 <code>5.6.x</code></li>
</ul>
<p>如果当前目标是做 AI 模型开发，可以先抓住这个区分：</p>
<blockquote>
<p>Celery 更像“任务队列和后台调度器”，Ray 更像“分布式计算和 GPU 资源调度层”。</p>
</blockquote>
<hr>
<h2 id="1-Ray-是什么"><a href="#1-Ray-是什么" class="headerlink" title="1. Ray 是什么"></a>1. Ray 是什么</h2><p>Ray 是一个 Python 分布式计算框架，用来把普通 Python 代码扩展到：</p>
<ul>
<li>多进程</li>
<li>多机</li>
<li>多 GPU</li>
<li>分布式训练</li>
<li>批量推理</li>
<li>在线推理服务</li>
</ul>
<p>它的核心不是“消息队列”，而是“把 Python 函数和类，变成可调度的分布式任务”。</p>
<p>Ray 真正提供的是一套调度能力：普通的 Python 函数和类，加上 <code>@ray.remote</code> 之后，就能被当成分布式任务或远程 actor 来运行。</p>
<hr>
<h2 id="2-Ray-适合什么场景"><a href="#2-Ray-适合什么场景" class="headerlink" title="2. Ray 适合什么场景"></a>2. Ray 适合什么场景</h2><p>Ray 典型适合这些场景：</p>
<ul>
<li>批量 embedding 生成</li>
<li>多卡模型推理</li>
<li>大规模数据预处理</li>
<li>分布式训练</li>
<li>超参搜索</li>
<li>需要把模型常驻在 GPU 上的服务</li>
<li>复杂 Python 工作流并行化</li>
</ul>
<p>如果只是这些场景，通常不一定要用 Ray：</p>
<ul>
<li>发送短信、邮件、通知</li>
<li>定时任务</li>
<li>普通后台异步任务</li>
<li>只需要重试、延迟执行、任务链</li>
</ul>
<p>这些更偏 Celery 的强项。</p>
<hr>
<h2 id="3-Ray-的核心概念"><a href="#3-Ray-的核心概念" class="headerlink" title="3. Ray 的核心概念"></a>3. Ray 的核心概念</h2><p>Ray 入门先掌握 4 个东西：</p>
<h3 id="3-1-Task"><a href="#3-1-Task" class="headerlink" title="3.1 Task"></a>3.1 Task</h3><p>Task 就是“远程函数”。</p>
<p>特点：</p>
<ul>
<li>无状态</li>
<li>适合并行执行独立任务</li>
<li>用 <code>@ray.remote</code> 标记</li>
</ul>
<h3 id="3-2-Actor"><a href="#3-2-Actor" class="headerlink" title="3.2 Actor"></a>3.2 Actor</h3><p>Actor 就是“远程对象”。</p>
<p>特点：</p>
<ul>
<li>有状态</li>
<li>适合模型常驻内存 &#x2F; GPU</li>
<li>适合连接池、模型池、缓存、参数服务</li>
</ul>
<h3 id="3-3-ObjectRef"><a href="#3-3-ObjectRef" class="headerlink" title="3.3 ObjectRef"></a>3.3 ObjectRef</h3><p>Ray 不会马上把结果返回给你，而是先返回一个“对象引用”。</p>
<ul>
<li><code>xxx.remote()</code> 返回的通常是 <code>ObjectRef</code></li>
<li>用 <code>ray.get()</code> 才真正取回结果</li>
</ul>
<p>这点很像 <code>Future</code>。</p>
<h3 id="3-4-Object-Store"><a href="#3-4-Object-Store" class="headerlink" title="3.4 Object Store"></a>3.4 Object Store</h3><p>Ray 有自己的对象存储。</p>
<ul>
<li>task &#x2F; actor 的返回值会放到对象存储里</li>
<li>大对象可以先 <code>ray.put()</code></li>
<li>后续多个任务共享这个引用，避免重复拷贝</li>
</ul>
<hr>
<h2 id="4-最小入门示例"><a href="#4-最小入门示例" class="headerlink" title="4. 最小入门示例"></a>4. 最小入门示例</h2><h3 id="4-1-安装"><a href="#4-1-安装" class="headerlink" title="4.1 安装"></a>4.1 安装</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install -U <span class="string">&quot;ray[default]&quot;</span></span><br></pre></td></tr></table></figure>

<p>如果你还要用训练或数据处理：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install -U <span class="string">&quot;ray[train]&quot;</span> <span class="string">&quot;ray[data]&quot;</span> torch</span><br></pre></td></tr></table></figure>

<h3 id="4-2-启动本地-Ray"><a href="#4-2-启动本地-Ray" class="headerlink" title="4.2 启动本地 Ray"></a>4.2 启动本地 Ray</h3><p>最简单的方式：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">ray.init()</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><code>ray.init()</code> 会连接已有本地 Ray，或者启动一个新的本地实例</li>
<li>如果你是 Celery worker 或生产环境进程，通常更建议显式连接已有集群：<code>ray.init(address=&quot;auto&quot;)</code></li>
</ul>
<h3 id="4-3-第一个-Task"><a href="#4-3-第一个-Task" class="headerlink" title="4.3 第一个 Task"></a>4.3 第一个 Task</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">ray.init()</span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">square</span>(<span class="params">x: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">    <span class="keyword">return</span> x * x</span><br><span class="line"></span><br><span class="line">refs = [square.remote(i) <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">8</span>)]</span><br><span class="line">results = ray.get(refs)</span><br><span class="line"><span class="built_in">print</span>(results)</span><br></pre></td></tr></table></figure>

<p>你要关注的是：</p>
<ul>
<li><code>@ray.remote</code>：把普通函数变成远程函数</li>
<li><code>square.remote(i)</code>：不是本地调用，而是提交任务</li>
<li><code>ray.get(refs)</code>：拿回结果</li>
</ul>
<h3 id="4-4-第一个-Actor"><a href="#4-4-第一个-Actor" class="headerlink" title="4.4 第一个 Actor"></a>4.4 第一个 Actor</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">ray.init()</span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Counter</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="variable language_">self</span>.value = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">incr</span>(<span class="params">self, n: <span class="built_in">int</span> = <span class="number">1</span></span>):</span><br><span class="line">        <span class="variable language_">self</span>.value += n</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.value</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">get</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.value</span><br><span class="line"></span><br><span class="line">counter = Counter.remote()</span><br><span class="line"></span><br><span class="line">ray.get(counter.incr.remote())</span><br><span class="line">ray.get(counter.incr.remote(<span class="number">10</span>))</span><br><span class="line"><span class="built_in">print</span>(ray.get(counter.get.remote()))</span><br></pre></td></tr></table></figure>

<p>什么时候该用 Actor：</p>
<ul>
<li>模型加载很慢，不想每次任务都重新加载</li>
<li>需要在内存里保留状态</li>
<li>要做 GPU 常驻推理</li>
</ul>
<h3 id="4-5-共享大对象"><a href="#4-5-共享大对象" class="headerlink" title="4.5 共享大对象"></a>4.5 共享大对象</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">ray.init()</span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">compute_sum</span>(<span class="params">arr</span>):</span><br><span class="line">    <span class="keyword">return</span> arr.<span class="built_in">sum</span>()</span><br><span class="line"></span><br><span class="line">big_array_ref = ray.put(np.ones((<span class="number">10000</span>, <span class="number">1000</span>)))</span><br><span class="line">result = ray.get(compute_sum.remote(big_array_ref))</span><br><span class="line"><span class="built_in">print</span>(result)</span><br></pre></td></tr></table></figure>

<p>经验：</p>
<ul>
<li>小参数直接传</li>
<li>大对象尽量 <code>ray.put()</code></li>
</ul>
<hr>
<h2 id="5-Ray-常用-API"><a href="#5-Ray-常用-API" class="headerlink" title="5. Ray 常用 API"></a>5. Ray 常用 API</h2><h3 id="5-1-ray-init"><a href="#5-1-ray-init" class="headerlink" title="5.1 ray.init()"></a>5.1 <code>ray.init()</code></h3><p>用于启动或连接 Ray。</p>
<p>常见写法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ray.init()</span><br><span class="line">ray.init(address=<span class="string">&quot;auto&quot;</span>)</span><br><span class="line">ray.init(address=<span class="string">&quot;ray://&lt;head-ip&gt;:10001&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>含义：</p>
<ul>
<li><code>ray.init()</code>：本地开发最方便</li>
<li><code>address=&quot;auto&quot;</code>：只连接已有集群，如果没集群会报错，适合生产环境</li>
<li><code>ray://...</code>：Ray Client 方式连接远端集群</li>
</ul>
<h3 id="5-2-ray-get"><a href="#5-2-ray-get" class="headerlink" title="5.2 ray.get()"></a>5.2 <code>ray.get()</code></h3><p>取结果。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">value = ray.get(ref)</span><br><span class="line">values = ray.get(refs)</span><br></pre></td></tr></table></figure>

<h3 id="5-3-ray-wait"><a href="#5-3-ray-wait" class="headerlink" title="5.3 ray.wait()"></a>5.3 <code>ray.wait()</code></h3><p>适合任务很多时，先消费一部分结果，避免一次性堆太多任务或结果。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ready, pending = ray.wait(refs, num_returns=<span class="number">1</span>)</span><br><span class="line">result = ray.get(ready[<span class="number">0</span>])</span><br></pre></td></tr></table></figure>

<h3 id="5-4-ray-put"><a href="#5-4-ray-put" class="headerlink" title="5.4 ray.put()"></a>5.4 <code>ray.put()</code></h3><p>把大对象放进 Ray 对象存储。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">obj_ref = ray.put(big_object)</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="6-Task-和-Actor-怎么选"><a href="#6-Task-和-Actor-怎么选" class="headerlink" title="6. Task 和 Actor 怎么选"></a>6. Task 和 Actor 怎么选</h2><table>
<thead>
<tr>
<th>场景</th>
<th>更适合</th>
</tr>
</thead>
<tbody><tr>
<td>每个任务彼此独立、无状态</td>
<td>Task</td>
</tr>
<tr>
<td>模型要常驻内存 &#x2F; GPU</td>
<td>Actor</td>
</tr>
<tr>
<td>批量推理 worker 池</td>
<td>Actor</td>
</tr>
<tr>
<td>简单 map 并行</td>
<td>Task</td>
</tr>
<tr>
<td>连接池 &#x2F; 缓存 &#x2F; 参数服务</td>
<td>Actor</td>
</tr>
</tbody></table>
<p>最常见的 AI 场景经验是：</p>
<ul>
<li>CPU 并行清洗数据：Task</li>
<li>模型推理：Actor</li>
<li>分布式训练：优先用 Ray Train，而不是自己手搓 Ray Core</li>
</ul>
<hr>
<h2 id="7-从本地到集群"><a href="#7-从本地到集群" class="headerlink" title="7. 从本地到集群"></a>7. 从本地到集群</h2><h3 id="7-1-单机开发"><a href="#7-1-单机开发" class="headerlink" title="7.1 单机开发"></a>7.1 单机开发</h3><p>直接：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ray.init()</span><br></pre></td></tr></table></figure>

<h3 id="7-2-手动启动单机-多机集群"><a href="#7-2-手动启动单机-多机集群" class="headerlink" title="7.2 手动启动单机 &#x2F; 多机集群"></a>7.2 手动启动单机 &#x2F; 多机集群</h3><p>头节点：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ray start --<span class="built_in">head</span> --port=6379</span><br></pre></td></tr></table></figure>

<p>工作节点：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ray start --address=&lt;head-node-ip&gt;:6379</span><br></pre></td></tr></table></figure>

<p>Python 里连接：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">ray.init(address=<span class="string">&quot;auto&quot;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="7-3-GPU-机器上限制可见显卡"><a href="#7-3-GPU-机器上限制可见显卡" class="headerlink" title="7.3 GPU 机器上限制可见显卡"></a>7.3 GPU 机器上限制可见显卡</h3><p>如果你只想让 Ray 看到部分 GPU：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">CUDA_VISIBLE_DEVICES=1,3 ray start --<span class="built_in">head</span> --num-gpus=2</span><br></pre></td></tr></table></figure>

<p>这在一台机器上做资源隔离时很有用。</p>
<hr>
<h2 id="8-多张显卡怎么用"><a href="#8-多张显卡怎么用" class="headerlink" title="8. 多张显卡怎么用"></a>8. 多张显卡怎么用</h2><p>这部分是 Ray 很强的一块。</p>
<p>你先记住结论：</p>
<ul>
<li>单个任务 &#x2F; Actor 用几张卡，就声明几张</li>
<li>多个任务并行吃多卡，让 Ray 调度</li>
<li>训练任务优先用 Ray Train</li>
<li>批量推理优先用 Actor 池或 Ray Data</li>
</ul>
<h3 id="8-1-一张卡对应一个-Actor"><a href="#8-1-一张卡对应一个-Actor" class="headerlink" title="8.1 一张卡对应一个 Actor"></a>8.1 一张卡对应一个 Actor</h3><p>这是最常见的“多卡推理”写法。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">ray.init(num_gpus=<span class="number">4</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote(<span class="params">num_gpus=<span class="number">1</span></span>)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GPUWorker</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">info</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> &#123;</span><br><span class="line">            <span class="string">&quot;gpu_ids&quot;</span>: ray.get_runtime_context().get_accelerator_ids()[<span class="string">&quot;GPU&quot;</span>],</span><br><span class="line">            <span class="string">&quot;cuda_visible_devices&quot;</span>: os.environ.get(<span class="string">&quot;CUDA_VISIBLE_DEVICES&quot;</span>),</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">workers = [GPUWorker.remote() <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>)]</span><br><span class="line"><span class="built_in">print</span>(ray.get([worker.info.remote() <span class="keyword">for</span> worker <span class="keyword">in</span> workers]))</span><br></pre></td></tr></table></figure>

<p>效果：</p>
<ul>
<li>每个 Actor 占 1 张 GPU</li>
<li>Ray 会把不同 Actor 调度到不同卡</li>
<li>大多数深度学习框架会自动遵守 <code>CUDA_VISIBLE_DEVICES</code></li>
</ul>
<h3 id="8-2-单个任务使用多张卡"><a href="#8-2-单个任务使用多张卡" class="headerlink" title="8.2 单个任务使用多张卡"></a>8.2 单个任务使用多张卡</h3><p>如果一个任务本身就要吃多卡：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">ray.init(num_gpus=<span class="number">4</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote(<span class="params">num_gpus=<span class="number">2</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">train_one_job</span>():</span><br><span class="line">    <span class="keyword">import</span> os</span><br><span class="line">    <span class="keyword">return</span> os.environ.get(<span class="string">&quot;CUDA_VISIBLE_DEVICES&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(ray.get(train_one_job.remote()))</span><br></pre></td></tr></table></figure>

<p>这适合：</p>
<ul>
<li>单个训练任务需要 2 卡 &#x2F; 4 卡</li>
<li>单个推理进程要占多卡</li>
</ul>
<h3 id="8-3-分数-GPU"><a href="#8-3-分数-GPU" class="headerlink" title="8.3 分数 GPU"></a>8.3 分数 GPU</h3><p>如果模型很小，也可以共享 GPU：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"></span><br><span class="line">ray.init(num_gpus=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote(<span class="params">num_gpus=<span class="number">0.25</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">small_infer</span>(<span class="params">x</span>):</span><br><span class="line">    time.sleep(<span class="number">1</span>)</span><br><span class="line">    <span class="keyword">return</span> x</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(ray.get([small_infer.remote(i) <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>)]))</span><br></pre></td></tr></table></figure>

<p>注意：</p>
<ul>
<li>分数 GPU 只是“调度上的份额”</li>
<li>显存控制还是你自己负责</li>
<li>小模型、embedding、小批量推理更适合这样做</li>
</ul>
<h3 id="8-4-多卡训练优先用-Ray-Train"><a href="#8-4-多卡训练优先用-Ray-Train" class="headerlink" title="8.4 多卡训练优先用 Ray Train"></a>8.4 多卡训练优先用 Ray Train</h3><p>如果你在做训练，不建议直接自己拼很多 <code>@ray.remote</code>。</p>
<p>更推荐：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> ray.train <span class="keyword">import</span> ScalingConfig</span><br><span class="line"><span class="keyword">from</span> ray.train.torch <span class="keyword">import</span> TorchTrainer, get_device</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">train_loop</span>(<span class="params">config</span>):</span><br><span class="line">    <span class="keyword">import</span> torch</span><br><span class="line"></span><br><span class="line">    device = get_device()</span><br><span class="line">    model = torch.nn.Linear(<span class="number">10</span>, <span class="number">1</span>).to(device)</span><br><span class="line">    x = torch.randn(<span class="number">32</span>, <span class="number">10</span>, device=device)</span><br><span class="line">    y = model(x)</span><br><span class="line">    loss = y.mean()</span><br><span class="line">    loss.backward()</span><br><span class="line"></span><br><span class="line">trainer = TorchTrainer(</span><br><span class="line">    train_loop,</span><br><span class="line">    scaling_config=ScalingConfig(</span><br><span class="line">        num_workers=<span class="number">4</span>,</span><br><span class="line">        use_gpu=<span class="literal">True</span>,</span><br><span class="line">    ),</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">trainer.fit()</span><br></pre></td></tr></table></figure>

<p>这表示：</p>
<ul>
<li>4 个 worker</li>
<li>每个 worker 1 张 GPU</li>
<li>总共 4 张 GPU</li>
</ul>
<h3 id="8-5-一个-worker-吃多张卡"><a href="#8-5-一个-worker-吃多张卡" class="headerlink" title="8.5 一个 worker 吃多张卡"></a>8.5 一个 worker 吃多张卡</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> ray.train <span class="keyword">import</span> ScalingConfig</span><br><span class="line"><span class="keyword">from</span> ray.train.torch <span class="keyword">import</span> TorchTrainer, get_devices</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">train_loop</span>(<span class="params">config</span>):</span><br><span class="line">    <span class="keyword">import</span> torch</span><br><span class="line"></span><br><span class="line">    devices = get_devices()</span><br><span class="line">    <span class="built_in">print</span>(devices)</span><br><span class="line"></span><br><span class="line">trainer = TorchTrainer(</span><br><span class="line">    train_loop,</span><br><span class="line">    scaling_config=ScalingConfig(</span><br><span class="line">        num_workers=<span class="number">2</span>,</span><br><span class="line">        use_gpu=<span class="literal">True</span>,</span><br><span class="line">        resources_per_worker=&#123;<span class="string">&quot;GPU&quot;</span>: <span class="number">2</span>&#125;,</span><br><span class="line">    ),</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">trainer.fit()</span><br></pre></td></tr></table></figure>

<p>这表示：</p>
<ul>
<li>2 个 worker</li>
<li>每个 worker 2 张 GPU</li>
<li>总共 4 张 GPU</li>
</ul>
<p>适合：</p>
<ul>
<li>每个 worker 本身要做张量并行</li>
<li>每个训练进程要 2 卡或更多</li>
</ul>
<h3 id="8-6-需要成组预留资源时，用-Placement-Group"><a href="#8-6-需要成组预留资源时，用-Placement-Group" class="headerlink" title="8.6 需要成组预留资源时，用 Placement Group"></a>8.6 需要成组预留资源时，用 Placement Group</h3><p>有些任务需要“成组占住资源”，比如：</p>
<ul>
<li>2 个 actor 必须同时启动</li>
<li>一个训练 job 要一次性拿到 <code>2 CPU + 2 GPU</code></li>
<li>希望资源尽量在同一台机器</li>
</ul>
<p>这时用 Placement Group。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"><span class="keyword">from</span> ray.util.placement_group <span class="keyword">import</span> placement_group</span><br><span class="line"><span class="keyword">from</span> ray.util.scheduling_strategies <span class="keyword">import</span> PlacementGroupSchedulingStrategy</span><br><span class="line"></span><br><span class="line">ray.init(num_gpus=<span class="number">2</span>, num_cpus=<span class="number">8</span>)</span><br><span class="line"></span><br><span class="line">pg = placement_group(</span><br><span class="line">    [&#123;<span class="string">&quot;CPU&quot;</span>: <span class="number">2</span>, <span class="string">&quot;GPU&quot;</span>: <span class="number">1</span>&#125;, &#123;<span class="string">&quot;CPU&quot;</span>: <span class="number">2</span>, <span class="string">&quot;GPU&quot;</span>: <span class="number">1</span>&#125;],</span><br><span class="line">    strategy=<span class="string">&quot;STRICT_PACK&quot;</span>,</span><br><span class="line">)</span><br><span class="line">ray.get(pg.ready())</span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote(<span class="params">num_cpus=<span class="number">2</span>, num_gpus=<span class="number">1</span></span>)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TrainerWorker</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">ready</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;ok&quot;</span></span><br><span class="line"></span><br><span class="line">workers = [</span><br><span class="line">    TrainerWorker.options(</span><br><span class="line">        scheduling_strategy=PlacementGroupSchedulingStrategy(</span><br><span class="line">            placement_group=pg,</span><br><span class="line">            placement_group_bundle_index=i,</span><br><span class="line">        )</span><br><span class="line">    ).remote()</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">2</span>)</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(ray.get([w.ready.remote() <span class="keyword">for</span> w <span class="keyword">in</span> workers]))</span><br></pre></td></tr></table></figure>

<p><code>STRICT_PACK</code> 的意思是：</p>
<ul>
<li>所有 bundle 必须放在同一台节点上</li>
<li>如果放不下，就直接失败</li>
</ul>
<p>这对多卡训练很常见。</p>
<hr>
<h2 id="9-AI-场景下更实用的写法"><a href="#9-AI-场景下更实用的写法" class="headerlink" title="9. AI 场景下更实用的写法"></a>9. AI 场景下更实用的写法</h2><h3 id="9-1-模型常驻-GPU-的-Actor"><a href="#9-1-模型常驻-GPU-的-Actor" class="headerlink" title="9.1 模型常驻 GPU 的 Actor"></a>9.1 模型常驻 GPU 的 Actor</h3><p>如果你做 embedding、rerank、分类、OCR、ASR，常见需求是：</p>
<ul>
<li>模型初始化很慢</li>
<li>不想每个请求都重新加载模型</li>
<li>想让不同 worker 常驻不同 GPU</li>
</ul>
<p>这时最实用的方案往往是：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">ray.init(num_gpus=<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote(<span class="params">num_gpus=<span class="number">1</span></span>)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">EmbeddingWorker</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, model_name: <span class="built_in">str</span></span>):</span><br><span class="line">        <span class="keyword">from</span> sentence_transformers <span class="keyword">import</span> SentenceTransformer</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.model = SentenceTransformer(model_name, device=<span class="string">&quot;cuda&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">encode</span>(<span class="params">self, texts: <span class="built_in">list</span>[<span class="built_in">str</span>]</span>):</span><br><span class="line">        vectors = <span class="variable language_">self</span>.model.encode(texts, normalize_embeddings=<span class="literal">True</span>)</span><br><span class="line">        <span class="keyword">return</span> vectors.tolist()</span><br><span class="line"></span><br><span class="line">workers = [</span><br><span class="line">    EmbeddingWorker.remote(<span class="string">&quot;BAAI/bge-small-zh-v1.5&quot;</span>)</span><br><span class="line">    <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">2</span>)</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>优势：</p>
<ul>
<li>模型只加载一次</li>
<li>Ray 自动把 worker 分到不同 GPU</li>
<li>适合批量任务和服务化场景</li>
</ul>
<h3 id="9-2-批量推理更推荐-Ray-Data"><a href="#9-2-批量推理更推荐-Ray-Data" class="headerlink" title="9.2 批量推理更推荐 Ray Data"></a>9.2 批量推理更推荐 Ray Data</h3><p>如果是大批量离线推理，比如：</p>
<ul>
<li>给 100 万条文本做 embedding</li>
<li>大量图片跑分类 &#x2F; OCR</li>
<li>多卡批量推理</li>
</ul>
<p>Ray Data 一般比你自己手写 <code>for + remote()</code> 更省心。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">ray.init()</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">EmbeddingPredictor</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, model_name: <span class="built_in">str</span></span>):</span><br><span class="line">        <span class="keyword">from</span> sentence_transformers <span class="keyword">import</span> SentenceTransformer</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.model = SentenceTransformer(model_name, device=<span class="string">&quot;cuda&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__call__</span>(<span class="params">self, batch: pd.DataFrame</span>) -&gt; pd.DataFrame:</span><br><span class="line">        batch = batch.copy()</span><br><span class="line">        batch[<span class="string">&quot;embedding&quot;</span>] = <span class="variable language_">self</span>.model.encode(batch[<span class="string">&quot;text&quot;</span>].tolist()).tolist()</span><br><span class="line">        <span class="keyword">return</span> batch</span><br><span class="line"></span><br><span class="line">ds = ray.data.from_items(</span><br><span class="line">    [&#123;<span class="string">&quot;text&quot;</span>: <span class="string">f&quot;sample <span class="subst">&#123;i&#125;</span>&quot;</span>&#125; <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1000</span>)]</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">result_ds = ds.map_batches(</span><br><span class="line">    EmbeddingPredictor,</span><br><span class="line">    fn_constructor_args=(<span class="string">&quot;BAAI/bge-small-zh-v1.5&quot;</span>,),</span><br><span class="line">    batch_size=<span class="number">64</span>,</span><br><span class="line">    batch_format=<span class="string">&quot;pandas&quot;</span>,</span><br><span class="line">    compute=ray.data.ActorPoolStrategy(size=<span class="number">4</span>),</span><br><span class="line">    num_gpus=<span class="number">1</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(result_ds.take(<span class="number">2</span>))</span><br></pre></td></tr></table></figure>

<p>什么时候优先考虑 Ray Data：</p>
<ul>
<li>数据量大</li>
<li>希望自动做并行读取和批处理</li>
<li>想用 actor pool 跑多卡离线推理</li>
</ul>
<hr>
<h2 id="10-Ray-和-Celery-的区别"><a href="#10-Ray-和-Celery-的区别" class="headerlink" title="10. Ray 和 Celery 的区别"></a>10. Ray 和 Celery 的区别</h2><p>很多人第一次会问：</p>
<blockquote>
<p>我已经有 Celery 了，还要不要 Ray？</p>
</blockquote>
<p>答案通常是：</p>
<blockquote>
<p>不是替代关系，而是职责不同。</p>
</blockquote>
<h3 id="10-1-一张表看区别"><a href="#10-1-一张表看区别" class="headerlink" title="10.1 一张表看区别"></a>10.1 一张表看区别</h3><table>
<thead>
<tr>
<th>维度</th>
<th>Celery</th>
<th>Ray</th>
</tr>
</thead>
<tbody><tr>
<td>核心定位</td>
<td>分布式任务队列</td>
<td>分布式计算框架</td>
</tr>
<tr>
<td>擅长</td>
<td>异步任务、重试、延迟、队列路由</td>
<td>并行计算、状态 Actor、GPU 调度</td>
</tr>
<tr>
<td>GPU 感知</td>
<td>很弱</td>
<td>很强</td>
</tr>
<tr>
<td>模型常驻</td>
<td>不擅长</td>
<td>很擅长</td>
</tr>
<tr>
<td>大对象共享</td>
<td>一般</td>
<td>很强</td>
</tr>
<tr>
<td>训练 &#x2F; 多卡 &#x2F; 集群算力</td>
<td>不擅长</td>
<td>强项</td>
</tr>
<tr>
<td>业务工作流编排</td>
<td>强</td>
<td>一般</td>
</tr>
</tbody></table>
<h3 id="10-2-什么时候只用-Celery"><a href="#10-2-什么时候只用-Celery" class="headerlink" title="10.2 什么时候只用 Celery"></a>10.2 什么时候只用 Celery</h3><ul>
<li>发通知</li>
<li>发邮件</li>
<li>定时任务</li>
<li>任务重试</li>
<li>后台轻量业务任务</li>
</ul>
<h3 id="10-3-什么时候只用-Ray"><a href="#10-3-什么时候只用-Ray" class="headerlink" title="10.3 什么时候只用 Ray"></a>10.3 什么时候只用 Ray</h3><ul>
<li>分布式训练</li>
<li>多 GPU 推理</li>
<li>批量 embedding</li>
<li>大规模离线数据处理</li>
<li>需要模型常驻内存 &#x2F; GPU</li>
</ul>
<h3 id="10-4-什么时候-Ray-Celery-联合"><a href="#10-4-什么时候-Ray-Celery-联合" class="headerlink" title="10.4 什么时候 Ray + Celery 联合"></a>10.4 什么时候 Ray + Celery 联合</h3><p>这是很常见、也很推荐的工程做法：</p>
<ul>
<li>Celery 负责“接住业务异步任务”</li>
<li>Ray 负责“真正吃 CPU &#x2F; GPU 算力”</li>
</ul>
<p>最典型架构：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">FastAPI / Django</span><br><span class="line">    -&gt; 提交 Celery 任务</span><br><span class="line">    -&gt; Celery worker 收到任务</span><br><span class="line">    -&gt; Celery worker 把重计算任务提交给 Ray</span><br><span class="line">    -&gt; Ray task / actor 在 CPU / GPU 上执行</span><br><span class="line">    -&gt; Celery 记录状态、重试、结果</span><br></pre></td></tr></table></figure>

<p>这时职责很清晰：</p>
<ul>
<li>Celery：任务排队、重试、路由、延迟调度、业务集成</li>
<li>Ray：并行计算、多机、多卡、模型常驻</li>
</ul>
<hr>
<h2 id="11-Ray-Celery-推荐落地方式"><a href="#11-Ray-Celery-推荐落地方式" class="headerlink" title="11. Ray + Celery 推荐落地方式"></a>11. Ray + Celery 推荐落地方式</h2><p>这一节不是某个“官方一键集成组件”的说明，而是基于 Ray 官方能力和 Celery 官方能力整理出来的工程实践。</p>
<h3 id="11-1-推荐原则"><a href="#11-1-推荐原则" class="headerlink" title="11.1 推荐原则"></a>11.1 推荐原则</h3><p>推荐这样设计：</p>
<ul>
<li>不要让 Celery 自己直接“管理 GPU”</li>
<li>不要每个 Celery 任务里都临时启动一个 Ray 集群</li>
<li>Celery worker 只负责连接已有 Ray 集群并下发任务</li>
<li>模型尽量放到 Ray Actor 里常驻</li>
</ul>
<p>一句话：</p>
<blockquote>
<p>Celery 做任务层，Ray 做算力层。</p>
</blockquote>
<h3 id="11-2-最实用的方案：Celery-任务内部调用-Ray-Actor"><a href="#11-2-最实用的方案：Celery-任务内部调用-Ray-Actor" class="headerlink" title="11.2 最实用的方案：Celery 任务内部调用 Ray Actor"></a>11.2 最实用的方案：Celery 任务内部调用 Ray Actor</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> celery <span class="keyword">import</span> Celery</span><br><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">celery_app = Celery(</span><br><span class="line">    <span class="string">&quot;demo&quot;</span>,</span><br><span class="line">    broker=<span class="string">&quot;redis://127.0.0.1:6379/1&quot;</span>,</span><br><span class="line">    backend=<span class="string">&quot;redis://127.0.0.1:6379/2&quot;</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">ensure_ray</span>():</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> ray.is_initialized():</span><br><span class="line">        ray.init(address=<span class="string">&quot;auto&quot;</span>, namespace=<span class="string">&quot;ml&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote(<span class="params">num_gpus=<span class="number">1</span></span>)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">EmbeddingWorker</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, model_name: <span class="built_in">str</span></span>):</span><br><span class="line">        <span class="keyword">from</span> sentence_transformers <span class="keyword">import</span> SentenceTransformer</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.model = SentenceTransformer(model_name, device=<span class="string">&quot;cuda&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">encode</span>(<span class="params">self, texts: <span class="built_in">list</span>[<span class="built_in">str</span>]</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.model.encode(texts).tolist()</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_embedding_worker</span>():</span><br><span class="line">    ensure_ray()</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">return</span> ray.get_actor(<span class="string">&quot;embedding_worker&quot;</span>)</span><br><span class="line">    <span class="keyword">except</span> ValueError:</span><br><span class="line">        <span class="keyword">return</span> EmbeddingWorker.options(</span><br><span class="line">            name=<span class="string">&quot;embedding_worker&quot;</span>,</span><br><span class="line">            lifetime=<span class="string">&quot;detached&quot;</span>,</span><br><span class="line">            get_if_exists=<span class="literal">True</span>,</span><br><span class="line">        ).remote(<span class="string">&quot;BAAI/bge-small-zh-v1.5&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@celery_app.task(<span class="params"></span></span></span><br><span class="line"><span class="params"><span class="meta">    bind=<span class="literal">True</span>,</span></span></span><br><span class="line"><span class="params"><span class="meta">    autoretry_for=(<span class="params">Exception,</span>),</span></span></span><br><span class="line"><span class="params"><span class="meta">    retry_backoff=<span class="literal">True</span>,</span></span></span><br><span class="line"><span class="params"><span class="meta">    max_retries=<span class="number">3</span>,</span></span></span><br><span class="line"><span class="params"><span class="meta"></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">build_embeddings</span>(<span class="params">self, texts: <span class="built_in">list</span>[<span class="built_in">str</span>]</span>):</span><br><span class="line">    worker = get_embedding_worker()</span><br><span class="line">    ref = worker.encode.remote(texts)</span><br><span class="line">    vectors = ray.get(ref)</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;count&quot;</span>: <span class="built_in">len</span>(vectors),</span><br><span class="line">        <span class="string">&quot;vectors&quot;</span>: vectors,</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>这套写法的好处：</p>
<ul>
<li>Celery 有重试和任务状态</li>
<li>Ray Actor 让模型只加载一次</li>
<li>GPU 真正由 Ray 调度</li>
<li>Celery 不直接碰 <code>CUDA_VISIBLE_DEVICES</code></li>
</ul>
<h3 id="11-3-为什么这里要用-Named-Actor"><a href="#11-3-为什么这里要用-Named-Actor" class="headerlink" title="11.3 为什么这里要用 Named Actor"></a>11.3 为什么这里要用 Named Actor</h3><p>Named Actor 很适合下面这种需求：</p>
<ul>
<li>多个 Celery 任务都要访问同一个模型 worker</li>
<li>模型很大，不想重复加载</li>
<li>希望 worker 独立于单次任务生命周期存在</li>
</ul>
<p>这里用到的 3 个点都很重要：</p>
<ul>
<li><code>name=&quot;embedding_worker&quot;</code>：给 actor 命名</li>
<li><code>get_if_exists=True</code>：如果已有就复用</li>
<li><code>lifetime=&quot;detached&quot;</code>：Celery 任务结束后 actor 依然活着</li>
</ul>
<p>补充：</p>
<ul>
<li>Detached Actor 不会自动回收</li>
<li>不再需要时要手动 <code>ray.kill(actor_handle)</code>，否则会一直占资源</li>
</ul>
<h3 id="11-4-如果要做多卡模型池"><a href="#11-4-如果要做多卡模型池" class="headerlink" title="11.4 如果要做多卡模型池"></a>11.4 如果要做多卡模型池</h3><p>把一个 Named Actor 扩展成多个即可：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">workers = [</span><br><span class="line">    EmbeddingWorker.options(</span><br><span class="line">        name=<span class="string">f&quot;embedding_worker_<span class="subst">&#123;i&#125;</span>&quot;</span>,</span><br><span class="line">        lifetime=<span class="string">&quot;detached&quot;</span>,</span><br><span class="line">        get_if_exists=<span class="literal">True</span>,</span><br><span class="line">    ).remote(<span class="string">&quot;BAAI/bge-small-zh-v1.5&quot;</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>)</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>然后 Celery 里：</p>
<ul>
<li>轮询选一个</li>
<li>按队列分流</li>
<li>或者再包一个调度 Actor</li>
</ul>
<h3 id="11-5-Celery-队列怎么配合"><a href="#11-5-Celery-队列怎么配合" class="headerlink" title="11.5 Celery 队列怎么配合"></a>11.5 Celery 队列怎么配合</h3><p>重任务建议单独队列，比如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">build_embeddings.apply_async(args=[texts], queue=<span class="string">&quot;gpu&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>对应 worker：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">celery -A app worker -l info -Q gpu --concurrency=4</span><br></pre></td></tr></table></figure>

<p>这样做的意义：</p>
<ul>
<li>把 GPU 相关任务跟普通业务任务隔离</li>
<li>Celery 侧也能做业务级限流</li>
</ul>
<p>注意：</p>
<ul>
<li>Celery 的 <code>--concurrency</code> 不等于 GPU 数</li>
<li>真正的 GPU 分配还是 Ray 决定</li>
</ul>
<hr>
<h2 id="12-一个更合理的职责分层建议"><a href="#12-一个更合理的职责分层建议" class="headerlink" title="12. 一个更合理的职责分层建议"></a>12. 一个更合理的职责分层建议</h2><p>如果你的项目同时有 Web、异步任务、模型推理，我更建议这样分：</p>
<table>
<thead>
<tr>
<th>层</th>
<th>推荐工具</th>
</tr>
</thead>
<tbody><tr>
<td>Web 接口层</td>
<td>FastAPI &#x2F; Django</td>
</tr>
<tr>
<td>任务编排层</td>
<td>Celery</td>
</tr>
<tr>
<td>分布式算力层</td>
<td>Ray</td>
</tr>
<tr>
<td>在线模型服务层</td>
<td>Ray Serve</td>
</tr>
</tbody></table>
<p>这意味着：</p>
<ul>
<li>后台异步批处理：<code>Celery + Ray</code></li>
<li>在线低延迟模型服务：优先 <code>Ray Serve</code></li>
</ul>
<p>如果你把“在线推理 API”也硬塞进 Celery，一般不是最优解。</p>
<hr>
<h2 id="13-常见坑"><a href="#13-常见坑" class="headerlink" title="13. 常见坑"></a>13. 常见坑</h2><h3 id="13-1-不要在循环里一边提交一边-ray-get"><a href="#13-1-不要在循环里一边提交一边-ray-get" class="headerlink" title="13.1 不要在循环里一边提交一边 ray.get()"></a>13.1 不要在循环里一边提交一边 <code>ray.get()</code></h3><p>错误思路：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> x <span class="keyword">in</span> items:</span><br><span class="line">    result = ray.get(task.remote(x))</span><br></pre></td></tr></table></figure>

<p>这样会把并行写成串行。</p>
<p>更合理：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">refs = [task.remote(x) <span class="keyword">for</span> x <span class="keyword">in</span> items]</span><br><span class="line">results = ray.get(refs)</span><br></pre></td></tr></table></figure>

<h3 id="13-2-任务别切得太细"><a href="#13-2-任务别切得太细" class="headerlink" title="13.2 任务别切得太细"></a>13.2 任务别切得太细</h3><p>如果单个任务非常轻，Ray 的调度开销会吃掉收益。</p>
<p>经验：</p>
<ul>
<li>小任务尽量 batch 化</li>
<li>推理尽量做批处理</li>
</ul>
<h3 id="13-3-大对象别重复按值传"><a href="#13-3-大对象别重复按值传" class="headerlink" title="13.3 大对象别重复按值传"></a>13.3 大对象别重复按值传</h3><p>错误思路：</p>
<ul>
<li>每个 task 都传一份很大的模型参数</li>
</ul>
<p>更合理：</p>
<ul>
<li><code>ray.put()</code></li>
<li>或者放进 Actor</li>
</ul>
<h3 id="13-4-GPU-task-默认可能不复用-worker"><a href="#13-4-GPU-task-默认可能不复用-worker" class="headerlink" title="13.4 GPU task 默认可能不复用 worker"></a>13.4 GPU task 默认可能不复用 worker</h3><p>Ray 文档提到：</p>
<ul>
<li>某些 GPU 任务可能会残留显存占用</li>
<li>为避免资源泄漏，Ray 默认不会复用执行过 GPU task 的 worker 进程</li>
</ul>
<p>如果你遇到 GPU 调度开销偏大，需要再回头看 worker 复用策略。</p>
<p>如果你确认自己的任务不会泄漏 GPU 资源，可以显式开启复用：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ray.remote(<span class="params">num_gpus=<span class="number">1</span>, max_calls=<span class="number">0</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">gpu_task</span>():</span><br><span class="line">    ...</span><br></pre></td></tr></table></figure>

<h3 id="13-5-Celery-worker-不要偷偷起本地-Ray"><a href="#13-5-Celery-worker-不要偷偷起本地-Ray" class="headerlink" title="13.5 Celery worker 不要偷偷起本地 Ray"></a>13.5 Celery worker 不要偷偷起本地 Ray</h3><p>在生产环境里，Celery 进程里更推荐：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ray.init(address=<span class="string">&quot;auto&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>而不是：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ray.init()</span><br></pre></td></tr></table></figure>

<p>原因：</p>
<ul>
<li><code>ray.init()</code> 可能在没有集群时直接起一个本地 Ray</li>
<li>这样每台 Celery 机器都可能误起自己的小集群</li>
<li>排查起来很麻烦</li>
</ul>
<hr>
<h2 id="14-一个现实中的选型建议"><a href="#14-一个现实中的选型建议" class="headerlink" title="14. 一个现实中的选型建议"></a>14. 一个现实中的选型建议</h2><p>如果你的任务是下面这些，我建议这样选：</p>
<h3 id="14-1-批量-embedding-rerank-OCR-ASR"><a href="#14-1-批量-embedding-rerank-OCR-ASR" class="headerlink" title="14.1 批量 embedding &#x2F; rerank &#x2F; OCR &#x2F; ASR"></a>14.1 批量 embedding &#x2F; rerank &#x2F; OCR &#x2F; ASR</h3><p>优先：</p>
<ul>
<li><code>Celery + Ray Actor</code></li>
<li>数据量特别大时用 <code>Ray Data</code></li>
</ul>
<h3 id="14-2-分布式训练"><a href="#14-2-分布式训练" class="headerlink" title="14.2 分布式训练"></a>14.2 分布式训练</h3><p>优先：</p>
<ul>
<li><code>Ray Train</code></li>
</ul>
<h3 id="14-3-在线模型-API"><a href="#14-3-在线模型-API" class="headerlink" title="14.3 在线模型 API"></a>14.3 在线模型 API</h3><p>优先：</p>
<ul>
<li><code>Ray Serve</code></li>
</ul>
<h3 id="14-4-只是后台异步业务"><a href="#14-4-只是后台异步业务" class="headerlink" title="14.4 只是后台异步业务"></a>14.4 只是后台异步业务</h3><p>优先：</p>
<ul>
<li><code>Celery</code></li>
</ul>
<hr>
<h2 id="15-我建议你的学习顺序"><a href="#15-我建议你的学习顺序" class="headerlink" title="15. 我建议你的学习顺序"></a>15. 我建议你的学习顺序</h2><p>如果你现在刚入门 Ray，可以按这个顺序学：</p>
<ol>
<li><code>ray.init()</code>、<code>@ray.remote</code>、<code>ray.get()</code></li>
<li>Task 和 Actor 的区别</li>
<li><code>ray.put()</code>、<code>ray.wait()</code></li>
<li>GPU 调度：<code>num_gpus</code></li>
<li>Named Actor &#x2F; Detached Actor</li>
<li>Placement Group</li>
<li>Ray Data 批量推理</li>
<li>Ray Train 多卡训练</li>
<li>再考虑和 Celery 联合</li>
</ol>
<hr>
<h2 id="16-官方文档和官方博客"><a href="#16-官方文档和官方博客" class="headerlink" title="16. 官方文档和官方博客"></a>16. 官方文档和官方博客</h2><p>下面这些我认为最值得先看。</p>
<h3 id="16-1-Ray-官方文档"><a href="#16-1-Ray-官方文档" class="headerlink" title="16.1 Ray 官方文档"></a>16.1 Ray 官方文档</h3><ul>
<li>Ray Core Walkthrough: <a href="https://docs.ray.io/en/latest/ray-core/walkthrough.html">https://docs.ray.io/en/latest/ray-core/walkthrough.html</a></li>
<li>Starting Ray: <a href="https://docs.ray.io/en/latest/ray-core/starting-ray.html">https://docs.ray.io/en/latest/ray-core/starting-ray.html</a></li>
<li><code>ray.init</code> API: <a href="https://docs.ray.io/en/latest/ray-core/api/doc/ray.init.html">https://docs.ray.io/en/latest/ray-core/api/doc/ray.init.html</a></li>
<li>Accelerator Support: <a href="https://docs.ray.io/en/latest/ray-core/scheduling/accelerators.html">https://docs.ray.io/en/latest/ray-core/scheduling/accelerators.html</a></li>
<li>Placement Groups: <a href="https://docs.ray.io/en/latest/ray-core/scheduling/placement-group.html">https://docs.ray.io/en/latest/ray-core/scheduling/placement-group.html</a></li>
<li>Named Actors: <a href="https://docs.ray.io/en/latest/ray-core/actors/named-actors.html">https://docs.ray.io/en/latest/ray-core/actors/named-actors.html</a></li>
<li>Ray Train GPU Guide: <a href="https://docs.ray.io/en/latest/train/user-guides/using-gpus.html">https://docs.ray.io/en/latest/train/user-guides/using-gpus.html</a></li>
<li>Ray Data <code>map_batches</code>: <a href="https://docs.ray.io/en/latest/data/api/doc/ray.data.Dataset.map_batches.html">https://docs.ray.io/en/latest/data/api/doc/ray.data.Dataset.map_batches.html</a></li>
<li>Offline Batch Inference: <a href="https://docs.ray.io/en/latest/data/batch_inference.html">https://docs.ray.io/en/latest/data/batch_inference.html</a></li>
</ul>
<h3 id="16-2-Celery-官方文档"><a href="#16-2-Celery-官方文档" class="headerlink" title="16.2 Celery 官方文档"></a>16.2 Celery 官方文档</h3><ul>
<li>Calling Tasks: <a href="https://docs.celeryq.dev/en/stable/userguide/calling.html">https://docs.celeryq.dev/en/stable/userguide/calling.html</a></li>
<li>Tasks: <a href="https://docs.celeryq.dev/en/stable/userguide/tasks.html">https://docs.celeryq.dev/en/stable/userguide/tasks.html</a></li>
<li>Workers Guide: <a href="https://docs.celeryq.dev/en/stable/userguide/workers.html">https://docs.celeryq.dev/en/stable/userguide/workers.html</a></li>
</ul>
<h3 id="16-3-官方博客-官方活动"><a href="#16-3-官方博客-官方活动" class="headerlink" title="16.3 官方博客 &#x2F; 官方活动"></a>16.3 官方博客 &#x2F; 官方活动</h3><ul>
<li>Scaling Model Batch Inference in Ray: <a href="https://www.anyscale.com/blog/scaling-model-batch-inference-in-ray-using-actors-actorpool-and-ray-data">https://www.anyscale.com/blog/scaling-model-batch-inference-in-ray-using-actors-actorpool-and-ray-data</a></li>
<li>Ray Data: Scalable Data Processing for AI Workloads: <a href="https://www.anyscale.com/blog/ray-data-scalable-data-processing-for-ai-workloads">https://www.anyscale.com/blog/ray-data-scalable-data-processing-for-ai-workloads</a></li>
<li>Introduction to Ray Core and its Ecosystem: <a href="https://www.anyscale.com/events/2022/02/03/introduction-to-ray-core-and-its-ecosystem">https://www.anyscale.com/events/2022/02/03/introduction-to-ray-core-and-its-ecosystem</a></li>
<li>Ray Core Tutorial: <a href="https://www.anyscale.com/events/2021/06/24/ray-core-tutorial">https://www.anyscale.com/events/2021/06/24/ray-core-tutorial</a></li>
</ul>
<hr>
<h2 id="17-最后总结"><a href="#17-最后总结" class="headerlink" title="17. 最后总结"></a>17. 最后总结</h2><p>你可以先把 Ray 理解成：</p>
<blockquote>
<p>一个能把 Python 函数、对象、GPU、集群资源统一调度起来的分布式计算框架。</p>
</blockquote>
<p>如果放到工程里：</p>
<ul>
<li>Celery 解决“任务怎么排队、重试、延迟、编排”</li>
<li>Ray 解决“任务怎么真正并行地吃 CPU &#x2F; GPU”</li>
</ul>
<p>如果你后面是做 AI 系统，尤其是：</p>
<ul>
<li>多卡推理</li>
<li>批量 embedding</li>
<li>大模型训练 &#x2F; 推理</li>
</ul>
<p>那 Ray 很值得认真学。</p>
<hr>
<h2 id="18-FastAPI-Celery-Ray-4-张-GPU-最小可运行项目"><a href="#18-FastAPI-Celery-Ray-4-张-GPU-最小可运行项目" class="headerlink" title="18. FastAPI + Celery + Ray + 4 张 GPU 最小可运行项目"></a>18. <code>FastAPI + Celery + Ray + 4 张 GPU</code> 最小可运行项目</h2><p>下面给你一份“单机 4 张 GPU”的最小结构。</p>
<p>目标：</p>
<ul>
<li>FastAPI 负责接请求</li>
<li>Celery 负责异步任务和状态查询</li>
<li>Ray 负责 4 张 GPU 的模型调度</li>
<li>每张 GPU 常驻 1 个 embedding worker</li>
</ul>
<h3 id="18-1-适用场景"><a href="#18-1-适用场景" class="headerlink" title="18.1 适用场景"></a>18.1 适用场景</h3><p>这个结构很适合：</p>
<ul>
<li>批量生成 embedding</li>
<li>OCR &#x2F; ASR &#x2F; rerank 这类 GPU 推理任务</li>
<li>Web 请求不想阻塞</li>
<li>想把“业务异步”和“GPU 算力”分层</li>
</ul>
<p>不太适合：</p>
<ul>
<li>极低延迟在线推理</li>
</ul>
<p>如果你追求在线低延迟，优先考虑 <code>Ray Serve</code>。</p>
<h3 id="18-2-目录结构"><a href="#18-2-目录结构" class="headerlink" title="18.2 目录结构"></a>18.2 目录结构</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">ray_embedding_demo/</span><br><span class="line">├── app/</span><br><span class="line">│   ├── __init__.py</span><br><span class="line">│   ├── celery_app.py</span><br><span class="line">│   ├── main.py</span><br><span class="line">│   ├── ray_actors.py</span><br><span class="line">│   ├── ray_runtime.py</span><br><span class="line">│   └── tasks.py</span><br><span class="line">└── requirements.txt</span><br></pre></td></tr></table></figure>

<h3 id="18-3-requirements-txt"><a href="#18-3-requirements-txt" class="headerlink" title="18.3 requirements.txt"></a>18.3 <code>requirements.txt</code></h3><figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">fastapi</span><br><span class="line">uvicorn[standard]</span><br><span class="line">celery</span><br><span class="line">redis</span><br><span class="line">ray[default]</span><br><span class="line">sentence-transformers</span><br><span class="line">torch</span><br><span class="line">numpy</span><br></pre></td></tr></table></figure>

<h3 id="18-4-app-celery-app-py"><a href="#18-4-app-celery-app-py" class="headerlink" title="18.4 app/celery_app.py"></a>18.4 <code>app/celery_app.py</code></h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> celery <span class="keyword">import</span> Celery</span><br><span class="line"></span><br><span class="line">celery_app = Celery(</span><br><span class="line">    <span class="string">&quot;ray_embedding_demo&quot;</span>,</span><br><span class="line">    broker=<span class="string">&quot;redis://127.0.0.1:6379/1&quot;</span>,</span><br><span class="line">    backend=<span class="string">&quot;redis://127.0.0.1:6379/2&quot;</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">celery_app.conf.update(</span><br><span class="line">    imports=(<span class="string">&quot;app.tasks&quot;</span>,),</span><br><span class="line">    task_track_started=<span class="literal">True</span>,</span><br><span class="line">    task_default_queue=<span class="string">&quot;gpu&quot;</span>,</span><br><span class="line">    timezone=<span class="string">&quot;Asia/Shanghai&quot;</span>,</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<h3 id="18-5-app-ray-runtime-py"><a href="#18-5-app-ray-runtime-py" class="headerlink" title="18.5 app/ray_runtime.py"></a>18.5 <code>app/ray_runtime.py</code></h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line">RAY_NAMESPACE = <span class="string">&quot;embedding-demo&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">ensure_ray</span>():</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> ray.is_initialized():</span><br><span class="line">        ray.init(address=<span class="string">&quot;auto&quot;</span>, namespace=RAY_NAMESPACE)</span><br></pre></td></tr></table></figure>

<h3 id="18-6-app-ray-actors-py"><a href="#18-6-app-ray-actors-py" class="headerlink" title="18.6 app/ray_actors.py"></a>18.6 <code>app/ray_actors.py</code></h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> __future__ <span class="keyword">import</span> annotations</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> app.ray_runtime <span class="keyword">import</span> ensure_ray</span><br><span class="line"></span><br><span class="line">MODEL_NAME = <span class="string">&quot;BAAI/bge-small-zh-v1.5&quot;</span></span><br><span class="line">WORKER_COUNT = <span class="number">4</span></span><br><span class="line">WORKER_PREFIX = <span class="string">&quot;embedding_worker&quot;</span></span><br><span class="line">ROUTER_NAME = <span class="string">&quot;embedding_router&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">worker_name</span>(<span class="params">worker_id: <span class="built_in">int</span></span>) -&gt; <span class="built_in">str</span>:</span><br><span class="line">    <span class="keyword">return</span> <span class="string">f&quot;<span class="subst">&#123;WORKER_PREFIX&#125;</span>_<span class="subst">&#123;worker_id&#125;</span>&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote(<span class="params">num_gpus=<span class="number">1</span></span>)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">EmbeddingWorker</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, model_name: <span class="built_in">str</span>, worker_id: <span class="built_in">int</span></span>):</span><br><span class="line">        <span class="keyword">from</span> sentence_transformers <span class="keyword">import</span> SentenceTransformer</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.worker_id = worker_id</span><br><span class="line">        <span class="variable language_">self</span>.model = SentenceTransformer(model_name, device=<span class="string">&quot;cuda&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">encode</span>(<span class="params">self, texts: <span class="built_in">list</span>[<span class="built_in">str</span>]</span>) -&gt; <span class="built_in">list</span>[<span class="built_in">list</span>[<span class="built_in">float</span>]]:</span><br><span class="line">        vectors = <span class="variable language_">self</span>.model.encode(</span><br><span class="line">            texts,</span><br><span class="line">            batch_size=<span class="number">32</span>,</span><br><span class="line">            normalize_embeddings=<span class="literal">True</span>,</span><br><span class="line">        )</span><br><span class="line">        <span class="keyword">return</span> vectors.tolist()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">info</span>(<span class="params">self</span>) -&gt; <span class="built_in">dict</span>:</span><br><span class="line">        <span class="keyword">return</span> &#123;</span><br><span class="line">            <span class="string">&quot;worker_id&quot;</span>: <span class="variable language_">self</span>.worker_id,</span><br><span class="line">            <span class="string">&quot;gpu_ids&quot;</span>: ray.get_runtime_context().get_accelerator_ids()[<span class="string">&quot;GPU&quot;</span>],</span><br><span class="line">            <span class="string">&quot;cuda_visible_devices&quot;</span>: os.environ.get(<span class="string">&quot;CUDA_VISIBLE_DEVICES&quot;</span>),</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@ray.remote</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">EmbeddingRouter</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, worker_names: <span class="built_in">list</span>[<span class="built_in">str</span>]</span>):</span><br><span class="line">        <span class="variable language_">self</span>.worker_names = worker_names</span><br><span class="line">        <span class="variable language_">self</span>.index = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">encode</span>(<span class="params">self, texts: <span class="built_in">list</span>[<span class="built_in">str</span>]</span>) -&gt; <span class="built_in">list</span>[<span class="built_in">list</span>[<span class="built_in">float</span>]]:</span><br><span class="line">        current_name = <span class="variable language_">self</span>.worker_names[<span class="variable language_">self</span>.index]</span><br><span class="line">        <span class="variable language_">self</span>.index = (<span class="variable language_">self</span>.index + <span class="number">1</span>) % <span class="built_in">len</span>(<span class="variable language_">self</span>.worker_names)</span><br><span class="line">        worker = ray.get_actor(current_name)</span><br><span class="line">        <span class="keyword">return</span> ray.get(worker.encode.remote(texts))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">state</span>(<span class="params">self</span>) -&gt; <span class="built_in">dict</span>:</span><br><span class="line">        <span class="keyword">return</span> &#123;</span><br><span class="line">            <span class="string">&quot;worker_names&quot;</span>: <span class="variable language_">self</span>.worker_names,</span><br><span class="line">            <span class="string">&quot;next_index&quot;</span>: <span class="variable language_">self</span>.index,</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">ensure_embedding_cluster</span>():</span><br><span class="line">    ensure_ray()</span><br><span class="line"></span><br><span class="line">    worker_names = []</span><br><span class="line">    <span class="keyword">for</span> worker_id <span class="keyword">in</span> <span class="built_in">range</span>(WORKER_COUNT):</span><br><span class="line">        name = worker_name(worker_id)</span><br><span class="line">        EmbeddingWorker.options(</span><br><span class="line">            name=name,</span><br><span class="line">            lifetime=<span class="string">&quot;detached&quot;</span>,</span><br><span class="line">            get_if_exists=<span class="literal">True</span>,</span><br><span class="line">        ).remote(MODEL_NAME, worker_id)</span><br><span class="line">        worker_names.append(name)</span><br><span class="line"></span><br><span class="line">    EmbeddingRouter.options(</span><br><span class="line">        name=ROUTER_NAME,</span><br><span class="line">        lifetime=<span class="string">&quot;detached&quot;</span>,</span><br><span class="line">        get_if_exists=<span class="literal">True</span>,</span><br><span class="line">    ).remote(worker_names)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_router</span>():</span><br><span class="line">    ensure_embedding_cluster()</span><br><span class="line">    <span class="keyword">return</span> ray.get_actor(ROUTER_NAME)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">list_workers</span>() -&gt; <span class="built_in">list</span>[<span class="built_in">dict</span>]:</span><br><span class="line">    ensure_embedding_cluster()</span><br><span class="line">    handles = [ray.get_actor(worker_name(i)) <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(WORKER_COUNT)]</span><br><span class="line">    <span class="keyword">return</span> ray.get([handle.info.remote() <span class="keyword">for</span> handle <span class="keyword">in</span> handles])</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><code>EmbeddingWorker</code>：每个 actor 绑定 1 张 GPU</li>
<li><code>EmbeddingRouter</code>：做一个最简单的轮询分发</li>
<li><code>lifetime=&quot;detached&quot;</code>：worker 不跟着单个任务退出</li>
<li><code>get_if_exists=True</code>：多进程启动时避免重复创建</li>
</ul>
<h3 id="18-7-app-tasks-py"><a href="#18-7-app-tasks-py" class="headerlink" title="18.7 app/tasks.py"></a>18.7 <code>app/tasks.py</code></h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> ray</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> app.celery_app <span class="keyword">import</span> celery_app</span><br><span class="line"><span class="keyword">from</span> app.ray_actors <span class="keyword">import</span> get_router</span><br><span class="line"></span><br><span class="line"><span class="meta">@celery_app.task(<span class="params"></span></span></span><br><span class="line"><span class="params"><span class="meta">    bind=<span class="literal">True</span>,</span></span></span><br><span class="line"><span class="params"><span class="meta">    autoretry_for=(<span class="params">Exception,</span>),</span></span></span><br><span class="line"><span class="params"><span class="meta">    retry_backoff=<span class="literal">True</span>,</span></span></span><br><span class="line"><span class="params"><span class="meta">    retry_jitter=<span class="literal">True</span>,</span></span></span><br><span class="line"><span class="params"><span class="meta">    max_retries=<span class="number">3</span>,</span></span></span><br><span class="line"><span class="params"><span class="meta"></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">build_embeddings</span>(<span class="params">self, texts: <span class="built_in">list</span>[<span class="built_in">str</span>]</span>) -&gt; <span class="built_in">dict</span>:</span><br><span class="line">    router = get_router()</span><br><span class="line">    vectors = ray.get(router.encode.remote(texts))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;count&quot;</span>: <span class="built_in">len</span>(vectors),</span><br><span class="line">        <span class="string">&quot;dim&quot;</span>: <span class="built_in">len</span>(vectors[<span class="number">0</span>]) <span class="keyword">if</span> vectors <span class="keyword">else</span> <span class="number">0</span>,</span><br><span class="line">        <span class="string">&quot;vectors&quot;</span>: vectors,</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="18-8-app-main-py"><a href="#18-8-app-main-py" class="headerlink" title="18.8 app/main.py"></a>18.8 <code>app/main.py</code></h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> contextlib <span class="keyword">import</span> asynccontextmanager</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> celery.result <span class="keyword">import</span> AsyncResult</span><br><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"><span class="keyword">from</span> pydantic <span class="keyword">import</span> BaseModel, Field</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> app.celery_app <span class="keyword">import</span> celery_app</span><br><span class="line"><span class="keyword">from</span> app.ray_actors <span class="keyword">import</span> ensure_embedding_cluster, list_workers</span><br><span class="line"><span class="keyword">from</span> app.tasks <span class="keyword">import</span> build_embeddings</span><br><span class="line"></span><br><span class="line"><span class="meta">@asynccontextmanager</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">lifespan</span>(<span class="params">app: FastAPI</span>):</span><br><span class="line">    ensure_embedding_cluster()</span><br><span class="line">    <span class="keyword">yield</span></span><br><span class="line"></span><br><span class="line">app = FastAPI(lifespan=lifespan)</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">EmbeddingRequest</span>(<span class="title class_ inherited__">BaseModel</span>):</span><br><span class="line">    texts: <span class="built_in">list</span>[<span class="built_in">str</span>] = Field(min_length=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/embeddings/submit&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">submit_embeddings</span>(<span class="params">payload: EmbeddingRequest</span>):</span><br><span class="line">    task = build_embeddings.apply_async(args=[payload.texts], queue=<span class="string">&quot;gpu&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;task_id&quot;</span>: task.<span class="built_in">id</span>,</span><br><span class="line">        <span class="string">&quot;count&quot;</span>: <span class="built_in">len</span>(payload.texts),</span><br><span class="line">        <span class="string">&quot;status&quot;</span>: <span class="string">&quot;submitted&quot;</span>,</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/tasks/&#123;task_id&#125;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_task_status</span>(<span class="params">task_id: <span class="built_in">str</span></span>):</span><br><span class="line">    result = AsyncResult(task_id, app=celery_app)</span><br><span class="line">    data = &#123;</span><br><span class="line">        <span class="string">&quot;task_id&quot;</span>: task_id,</span><br><span class="line">        <span class="string">&quot;status&quot;</span>: result.status,</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> result.successful():</span><br><span class="line">        data[<span class="string">&quot;result&quot;</span>] = result.result</span><br><span class="line">    <span class="keyword">elif</span> result.failed():</span><br><span class="line">        data[<span class="string">&quot;error&quot;</span>] = <span class="built_in">str</span>(result.result)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> data</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/ray/workers&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_ray_workers</span>():</span><br><span class="line">    <span class="keyword">return</span> list_workers()</span><br></pre></td></tr></table></figure>

<h3 id="18-9-启动顺序"><a href="#18-9-启动顺序" class="headerlink" title="18.9 启动顺序"></a>18.9 启动顺序</h3><p>先安装依赖：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install -r requirements.txt</span><br></pre></td></tr></table></figure>

<p>启动 Redis：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-server</span><br></pre></td></tr></table></figure>

<p>启动 Ray 集群。</p>
<p>注意：</p>
<ul>
<li>Redis 默认也用 <code>6379</code></li>
<li>Ray head 默认端口也常见写成 <code>6379</code></li>
<li>同机演示时最好显式给 Ray 换一个端口，避免冲突</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ray stop</span><br><span class="line">ray start --<span class="built_in">head</span> --port=9339 --dashboard-port=8265 --num-gpus=4</span><br></pre></td></tr></table></figure>

<p>启动 Celery worker：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">celery -A app.celery_app:celery_app worker -l info -Q gpu --concurrency=4</span><br></pre></td></tr></table></figure>

<p>启动 FastAPI：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload</span><br></pre></td></tr></table></figure>

<h3 id="18-10-试一下"><a href="#18-10-试一下" class="headerlink" title="18.10 试一下"></a>18.10 试一下</h3><p>提交任务：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -X POST <span class="string">&quot;http://127.0.0.1:8000/embeddings/submit&quot;</span> \</span><br><span class="line">  -H <span class="string">&quot;Content-Type: application/json&quot;</span> \</span><br><span class="line">  -d <span class="string">&#x27;&#123;</span></span><br><span class="line"><span class="string">    &quot;texts&quot;: [&quot;你好&quot;, &quot;Ray 和 Celery 可以联合使用&quot;, &quot;多卡 embedding 服务&quot;]</span></span><br><span class="line"><span class="string">  &#125;&#x27;</span></span><br></pre></td></tr></table></figure>

<p>返回类似：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;task_id&quot;</span><span class="punctuation">:</span> <span class="string">&quot;c9e2b7c1-xxxx-xxxx-xxxx-xxxxxxxxxxxx&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;count&quot;</span><span class="punctuation">:</span> <span class="number">3</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;status&quot;</span><span class="punctuation">:</span> <span class="string">&quot;submitted&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>查状态：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl <span class="string">&quot;http://127.0.0.1:8000/tasks/c9e2b7c1-xxxx-xxxx-xxxx-xxxxxxxxxxxx&quot;</span></span><br></pre></td></tr></table></figure>

<p>查看 4 个 GPU worker：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl <span class="string">&quot;http://127.0.0.1:8000/ray/workers&quot;</span></span><br></pre></td></tr></table></figure>

<p>你应该能看到类似结果：</p>
<ul>
<li><code>worker_id=0</code> 对应 1 张卡</li>
<li><code>worker_id=1</code> 对应 1 张卡</li>
<li><code>worker_id=2</code> 对应 1 张卡</li>
<li><code>worker_id=3</code> 对应 1 张卡</li>
</ul>
<hr>
<h2 id="19-“4-张显卡做-embedding-服务”-应该怎么理解"><a href="#19-“4-张显卡做-embedding-服务”-应该怎么理解" class="headerlink" title="19. “4 张显卡做 embedding 服务” 应该怎么理解"></a>19. “4 张显卡做 embedding 服务” 应该怎么理解</h2><p>上面的示例，实际做的是这件事：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">客户端请求</span><br><span class="line">    -&gt; FastAPI</span><br><span class="line">    -&gt; Celery 异步提交</span><br><span class="line">    -&gt; Celery 调用 Ray Router</span><br><span class="line">    -&gt; Router 把请求轮询发给 4 个 GPU Actor</span><br><span class="line">    -&gt; 对应 Actor 在各自 GPU 上推理</span><br><span class="line">    -&gt; Celery 返回任务结果</span><br></pre></td></tr></table></figure>

<p>这意味着：</p>
<ul>
<li>4 张卡不是给 Celery 的</li>
<li>4 张卡是给 Ray Actor 池的</li>
<li>Celery 只是任务入口，不负责 GPU 资源分配</li>
</ul>
<h3 id="19-1-为什么是一张卡一个-Actor"><a href="#19-1-为什么是一张卡一个-Actor" class="headerlink" title="19.1 为什么是一张卡一个 Actor"></a>19.1 为什么是一张卡一个 Actor</h3><p>这是最容易稳定运行的方式。</p>
<p>优点：</p>
<ul>
<li>模型只加载一次</li>
<li>每张卡有自己的常驻进程</li>
<li>不容易互相抢显存</li>
<li>问题定位更清晰</li>
</ul>
<p>适合：</p>
<ul>
<li>embedding</li>
<li>rerank</li>
<li>OCR</li>
<li>语音识别</li>
<li>图像分类</li>
</ul>
<h3 id="19-2-吞吐量怎么提高"><a href="#19-2-吞吐量怎么提高" class="headerlink" title="19.2 吞吐量怎么提高"></a>19.2 吞吐量怎么提高</h3><p>如果你觉得吞吐还不够，优先调这几个地方：</p>
<ol>
<li>增大 <code>self.model.encode(..., batch_size=32)</code> 的 <code>batch_size</code></li>
<li>一次提交多条文本，而不是每次只送 1 条</li>
<li>把路由器改成按“负载”调度，而不是简单轮询</li>
<li>小模型场景下尝试分数 GPU，比如 <code>num_gpus=0.5</code></li>
</ol>
<p>最有效的通常不是“开更多 Celery 进程”，而是：</p>
<ul>
<li>增大单次批量</li>
<li>让 GPU 更满</li>
</ul>
<h3 id="19-3-什么时候改成-Ray-Data"><a href="#19-3-什么时候改成-Ray-Data" class="headerlink" title="19.3 什么时候改成 Ray Data"></a>19.3 什么时候改成 <code>Ray Data</code></h3><p>如果你的场景从“在线异步任务”变成：</p>
<ul>
<li>一次处理几十万条</li>
<li>离线批量跑 embedding</li>
<li>数据已经在文件、表、对象存储里</li>
</ul>
<p>那通常不该继续走 <code>FastAPI -&gt; Celery</code> 这条链路，而应该直接上：</p>
<ul>
<li><code>Ray Data</code></li>
<li><code>map_batches</code></li>
<li>actor pool</li>
</ul>
<h3 id="19-4-什么时候改成-Ray-Serve"><a href="#19-4-什么时候改成-Ray-Serve" class="headerlink" title="19.4 什么时候改成 Ray Serve"></a>19.4 什么时候改成 <code>Ray Serve</code></h3><p>如果你的目标是：</p>
<ul>
<li>每个请求都想低延迟返回</li>
<li>希望直接暴露模型 API</li>
<li>想做副本扩缩容</li>
<li>想配合在线服务治理</li>
</ul>
<p>那比起 <code>FastAPI + Celery + Ray</code>，更适合直接用：</p>
<ul>
<li><code>Ray Serve</code></li>
</ul>
<p>一句话：</p>
<ul>
<li>批处理 &#x2F; 后台任务：<code>Celery + Ray</code></li>
<li>在线模型服务：<code>Ray Serve</code></li>
</ul>
<h3 id="19-5-这个示例有哪些简化"><a href="#19-5-这个示例有哪些简化" class="headerlink" title="19.5 这个示例有哪些简化"></a>19.5 这个示例有哪些简化</h3><p>为了易懂，这里故意省略了一些生产细节：</p>
<ul>
<li>没有把结果写数据库</li>
<li>没有做请求级限流</li>
<li>没有做任务取消</li>
<li>没有做 GPU worker 健康检查和自动重建</li>
<li>没有做模型热更新</li>
</ul>
<p>但核心链路已经是对的：</p>
<ul>
<li>Celery 编排任务</li>
<li>Ray 管 GPU</li>
<li>模型常驻 Actor</li>
</ul>
<h3 id="19-6-真上生产时我建议再补-5-个点"><a href="#19-6-真上生产时我建议再补-5-个点" class="headerlink" title="19.6 真上生产时我建议再补 5 个点"></a>19.6 真上生产时我建议再补 5 个点</h3><ol>
<li>结果不要直接塞进 Celery backend，尤其是 embedding 向量很大时</li>
<li>结果存数据库、对象存储或向量库，Celery 只回传元信息</li>
<li>给不同模型建不同的 Ray Actor 池，不要混在一个池里</li>
<li>给 GPU 任务单独 Celery 队列，避免和普通业务任务互相影响</li>
<li>增加一个“模型路由层”，按模型名、batch 大小、GPU 余量去分配</li>
</ol>
<h3 id="19-7-如果从单机-4-卡扩到多机"><a href="#19-7-如果从单机-4-卡扩到多机" class="headerlink" title="19.7 如果从单机 4 卡扩到多机"></a>19.7 如果从单机 4 卡扩到多机</h3><p>这套代码基本不用大改。</p>
<p>你主要改的是部署：</p>
<p>头节点：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ray start --<span class="built_in">head</span> --port=9339 --dashboard-port=8265</span><br></pre></td></tr></table></figure>

<p>工作节点：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ray start --address=&lt;head-ip&gt;:9339 --num-gpus=4</span><br></pre></td></tr></table></figure>

<p>Python 代码仍然可以继续用：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">ray.init(address=<span class="string">&quot;auto&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>这也是 Ray 很实用的一点：</p>
<ul>
<li>先单机写通</li>
<li>再平移到多机</li>
</ul>
<hr>
<h2 id="20-一份更现实的落地建议"><a href="#20-一份更现实的落地建议" class="headerlink" title="20. 一份更现实的落地建议"></a>20. 一份更现实的落地建议</h2><p>如果你当前项目是 RAG 或 AI 应用后台，我建议优先这么落：</p>
<ol>
<li>Web 请求进入 FastAPI</li>
<li>文件解析、embedding、rerank 任务交给 Celery</li>
<li>Celery 只做任务编排和状态更新</li>
<li>具体模型推理交给 Ray Actor 池</li>
<li>大批量离线任务再单独引入 <code>Ray Data</code></li>
</ol>
<p>这个拆法的优点是：</p>
<ul>
<li>业务代码和算力代码边界清晰</li>
<li>GPU 不会被 Celery 乱占</li>
<li>后续扩展到多机、多模型也更稳</li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>框架</category>
      </categories>
  </entry>
  <entry>
    <title>FAISS 学习笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/faiss/</url>
    <content><![CDATA[<h1 id="FAISS-学习笔记"><a href="#FAISS-学习笔记" class="headerlink" title="FAISS 学习笔记"></a>FAISS 学习笔记</h1><h2 id="1-FAISS-是什么"><a href="#1-FAISS-是什么" class="headerlink" title="1. FAISS 是什么"></a>1. FAISS 是什么</h2><p>FAISS，全称 Facebook AI Similarity Search，是一个用于高效向量相似度搜索和聚类的库。</p>
<p>它最常见的用途是：</p>
<ul>
<li>语义检索</li>
<li>图片检索</li>
<li>推荐系统召回</li>
<li>RAG 文档检索</li>
<li>大规模 embedding 向量查找</li>
</ul>
<p>可以先这样理解：</p>
<blockquote>
<p>Embedding 模型负责把文本变成向量，FAISS 负责在一堆向量里快速找到最相似的几个。</p>
</blockquote>
<p>在 RAG 里，FAISS 通常位于这条链路中：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">文档</span><br><span class="line">  -&gt; 切分 chunk</span><br><span class="line">  -&gt; embedding 模型生成向量</span><br><span class="line">  -&gt; FAISS 建索引</span><br><span class="line">  -&gt; 用户问题生成 query embedding</span><br><span class="line">  -&gt; FAISS Top-K 检索</span><br><span class="line">  -&gt; 把检索结果交给 LLM 生成回答</span><br></pre></td></tr></table></figure>

<p>FAISS 本身不是完整数据库，它主要管理向量索引。文档标题、chunk 内容、文件路径、页码、用户 ID、权限等元数据，通常还要放在 MySQL、PostgreSQL、MongoDB 或其他业务数据库里。</p>
<h2 id="2-为什么需要-FAISS"><a href="#2-为什么需要-FAISS" class="headerlink" title="2. 为什么需要 FAISS"></a>2. 为什么需要 FAISS</h2><p>如果有 10 个向量，直接遍历计算相似度就够了。</p>
<p>但如果有：</p>
<ul>
<li>10 万个 chunk</li>
<li>100 万个商品向量</li>
<li>1000 万张图片特征</li>
</ul>
<p>每次查询都全量遍历会很慢。</p>
<p>FAISS 解决的是：</p>
<ul>
<li>怎么快速找到最近邻</li>
<li>怎么在速度、召回率、内存之间做取舍</li>
<li>怎么支持百万级、千万级甚至更大规模的向量检索</li>
</ul>
<p>在小规模 RAG 项目里，<code>IndexFlat</code> 就能跑通完整链路；数据量上来后，再考虑 IVF、HNSW、PQ 等索引结构。</p>
<h2 id="3-FAISS-的核心概念"><a href="#3-FAISS-的核心概念" class="headerlink" title="3. FAISS 的核心概念"></a>3. FAISS 的核心概念</h2><h3 id="3-1-向量"><a href="#3-1-向量" class="headerlink" title="3.1 向量"></a>3.1 向量</h3><p>FAISS 处理的是固定维度的向量。</p>
<p>例如一个 embedding 模型输出 768 维向量：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[0.12, -0.03, 0.88, ..., 0.41]</span><br></pre></td></tr></table></figure>

<p>那么 FAISS 里的所有向量都必须是 768 维。</p>
<p>如果数据库向量是 768 维，而查询向量是 1024 维，就不能放在同一个 index 里检索。</p>
<h3 id="3-2-d"><a href="#3-2-d" class="headerlink" title="3.2 d"></a>3.2 <code>d</code></h3><p><code>d</code> 表示向量维度。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">d = <span class="number">768</span></span><br><span class="line">index = faiss.IndexFlatL2(d)</span><br></pre></td></tr></table></figure>

<p>这个 <code>d</code> 必须和 embedding 模型输出维度一致。</p>
<h3 id="3-3-xb-和-xq"><a href="#3-3-xb-和-xq" class="headerlink" title="3.3 xb 和 xq"></a>3.3 <code>xb</code> 和 <code>xq</code></h3><p>FAISS 官方示例里经常用两个名字：</p>
<ul>
<li><code>xb</code>：database vectors，要被索引的向量集合</li>
<li><code>xq</code>：query vectors，查询向量</li>
</ul>
<p>它们通常都是 NumPy 数组：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">xb.shape == (num_vectors, dimension)</span><br><span class="line">xq.shape == (num_queries, dimension)</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">xb: 10000 个文档 chunk 向量，每个 768 维</span><br><span class="line">xq: 1 个用户问题向量，每个 768 维</span><br></pre></td></tr></table></figure>

<h3 id="3-4-Index"><a href="#3-4-Index" class="headerlink" title="3.4 Index"></a>3.4 Index</h3><p>FAISS 的核心对象是 <code>Index</code>。</p>
<p>Index 可以理解成：</p>
<blockquote>
<p>存放向量并支持相似度搜索的数据结构。</p>
</blockquote>
<p>常用操作：</p>
<ul>
<li><code>add()</code>：添加向量</li>
<li><code>search()</code>：查询最近的 Top-K 向量</li>
<li><code>train()</code>：训练索引，部分索引需要</li>
<li><code>ntotal</code>：当前索引中有多少条向量</li>
<li><code>is_trained</code>：索引是否已经训练完成</li>
</ul>
<h3 id="3-5-D-和-I"><a href="#3-5-D-和-I" class="headerlink" title="3.5 D 和 I"></a>3.5 <code>D</code> 和 <code>I</code></h3><p>FAISS 搜索结果通常返回两个数组：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">D, I = index.search(query_vectors, k)</span><br></pre></td></tr></table></figure>

<p>含义：</p>
<ul>
<li><code>D</code>：距离或相似度分数</li>
<li><code>I</code>：检索到的向量 ID 或内部下标</li>
</ul>
<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">I = [[12, 88, 102]]</span><br><span class="line">D = [[0.13, 0.20, 0.35]]</span><br></pre></td></tr></table></figure>

<p>表示当前查询最相关的 3 个结果分别是第 12、88、102 个向量。</p>
<p>注意：</p>
<ul>
<li>L2 距离下，<code>D</code> 越小越相似。</li>
<li>Inner Product 下，<code>D</code> 越大越相似。</li>
</ul>
<h2 id="4-安装"><a href="#4-安装" class="headerlink" title="4. 安装"></a>4. 安装</h2><p>CPU 版本：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install faiss-cpu</span><br></pre></td></tr></table></figure>

<p>GPU 版本通常需要匹配 CUDA 环境，安装方式会和系统、CUDA、Python 版本有关。初学和普通 RAG 后端项目，先用 CPU 版本就够。</p>
<h2 id="5-最小可运行示例"><a href="#5-最小可运行示例" class="headerlink" title="5. 最小可运行示例"></a>5. 最小可运行示例</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> faiss</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line">d = <span class="number">4</span></span><br><span class="line">xb = np.array(</span><br><span class="line">    [</span><br><span class="line">        [<span class="number">1.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>],</span><br><span class="line">        [<span class="number">0.9</span>, <span class="number">0.1</span>, <span class="number">0.0</span>, <span class="number">0.0</span>],</span><br><span class="line">        [<span class="number">0.0</span>, <span class="number">1.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>],</span><br><span class="line">        [<span class="number">0.0</span>, <span class="number">0.0</span>, <span class="number">1.0</span>, <span class="number">0.0</span>],</span><br><span class="line">    ],</span><br><span class="line">    dtype=<span class="string">&quot;float32&quot;</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">xq = np.array([[<span class="number">1.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>]], dtype=<span class="string">&quot;float32&quot;</span>)</span><br><span class="line"></span><br><span class="line">index = faiss.IndexFlatL2(d)</span><br><span class="line">index.add(xb)</span><br><span class="line"></span><br><span class="line">k = <span class="number">2</span></span><br><span class="line">D, I = index.search(xq, k)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(D)</span><br><span class="line"><span class="built_in">print</span>(I)</span><br></pre></td></tr></table></figure>

<p>这段代码做了几件事：</p>
<ol>
<li>准备一批数据库向量 <code>xb</code></li>
<li>准备查询向量 <code>xq</code></li>
<li>创建 L2 距离索引 <code>IndexFlatL2</code></li>
<li>把数据库向量加入索引</li>
<li>查询最相近的 2 个向量</li>
</ol>
<h2 id="6-数据格式要求"><a href="#6-数据格式要求" class="headerlink" title="6. 数据格式要求"></a>6. 数据格式要求</h2><p>FAISS 对输入数据有几个常见要求：</p>
<ul>
<li>NumPy 数组通常要是 <code>float32</code></li>
<li>shape 要是二维：<code>(n, d)</code></li>
<li>数据最好是连续内存</li>
<li>所有向量维度必须一致</li>
</ul>
<p>推荐这样处理：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">vectors = np.asarray(vectors, dtype=<span class="string">&quot;float32&quot;</span>)</span><br><span class="line">vectors = np.ascontiguousarray(vectors)</span><br></pre></td></tr></table></figure>

<p>如果只有一个查询向量，不要传一维数组：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 不推荐</span></span><br><span class="line">query.shape == (<span class="number">768</span>,)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 推荐</span></span><br><span class="line">query.shape == (<span class="number">1</span>, <span class="number">768</span>)</span><br></pre></td></tr></table></figure>

<p>可以这样修正：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">query = np.asarray(query, dtype=<span class="string">&quot;float32&quot;</span>).reshape(<span class="number">1</span>, -<span class="number">1</span>)</span><br></pre></td></tr></table></figure>

<h2 id="7-距离和相似度"><a href="#7-距离和相似度" class="headerlink" title="7. 距离和相似度"></a>7. 距离和相似度</h2><p>FAISS 里最常见的两种检索方式：</p>
<ul>
<li>L2 距离</li>
<li>Inner Product 内积</li>
</ul>
<h3 id="7-1-L2-距离"><a href="#7-1-L2-距离" class="headerlink" title="7.1 L2 距离"></a>7.1 L2 距离</h3><p>L2 距离就是欧氏距离。</p>
<p>FAISS 的 <code>METRIC_L2</code> 返回的是平方 L2 距离，不开根号。排序结果不受影响，因为平方距离和真实欧氏距离是单调一致的。</p>
<p>使用方式：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.IndexFlatL2(d)</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li><code>D</code> 越小越相似</li>
<li>适合直接按距离找最近邻</li>
</ul>
<h3 id="7-2-Inner-Product"><a href="#7-2-Inner-Product" class="headerlink" title="7.2 Inner Product"></a>7.2 Inner Product</h3><p>Inner Product 是内积，常用于最大内积搜索。</p>
<p>使用方式：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.IndexFlatIP(d)</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li><code>D</code> 越大越相似</li>
<li>常用于已经归一化的 embedding 检索</li>
</ul>
<h3 id="7-3-余弦相似度怎么做"><a href="#7-3-余弦相似度怎么做" class="headerlink" title="7.3 余弦相似度怎么做"></a>7.3 余弦相似度怎么做</h3><p>很多文本 embedding 检索习惯使用余弦相似度。</p>
<p>FAISS 里常见做法是：</p>
<ol>
<li>使用 <code>IndexFlatIP</code></li>
<li>添加向量前先做 L2 归一化</li>
<li>查询向量也做 L2 归一化</li>
</ol>
<p>代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> faiss</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line">d = <span class="number">768</span></span><br><span class="line">xb = np.asarray(xb, dtype=<span class="string">&quot;float32&quot;</span>)</span><br><span class="line">xq = np.asarray(xq, dtype=<span class="string">&quot;float32&quot;</span>)</span><br><span class="line"></span><br><span class="line">faiss.normalize_L2(xb)</span><br><span class="line">faiss.normalize_L2(xq)</span><br><span class="line"></span><br><span class="line">index = faiss.IndexFlatIP(d)</span><br><span class="line">index.add(xb)</span><br><span class="line"></span><br><span class="line">D, I = index.search(xq, k=<span class="number">5</span>)</span><br></pre></td></tr></table></figure>

<p>注意：</p>
<blockquote>
<p>如果只归一化文档向量，不归一化查询向量，或者反过来，只归一化查询向量不归一化文档向量，结果都会不一致。</p>
</blockquote>
<h2 id="8-常见索引类型"><a href="#8-常见索引类型" class="headerlink" title="8. 常见索引类型"></a>8. 常见索引类型</h2><p>FAISS 有很多索引。初学阶段先掌握这几类就够。</p>
<h3 id="8-1-IndexFlatL2"><a href="#8-1-IndexFlatL2" class="headerlink" title="8.1 IndexFlatL2"></a>8.1 <code>IndexFlatL2</code></h3><p>精确 L2 检索。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.IndexFlatL2(d)</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li>不需要训练</li>
<li>暴力搜索，结果精确</li>
<li>数据量小到中等时很好用</li>
<li>内存占用约等于 <code>向量数量 * 维度 * 4 字节</code></li>
</ul>
<p>适合：</p>
<ul>
<li>本地 demo</li>
<li>小规模 RAG</li>
<li>先验证业务链路</li>
<li>作为评测近似索引 recall 的 ground truth</li>
</ul>
<h3 id="8-2-IndexFlatIP"><a href="#8-2-IndexFlatIP" class="headerlink" title="8.2 IndexFlatIP"></a>8.2 <code>IndexFlatIP</code></h3><p>精确内积检索。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.IndexFlatIP(d)</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li>不需要训练</li>
<li>暴力搜索，结果精确</li>
<li>搭配向量归一化后，常用于余弦相似度检索</li>
</ul>
<p>适合：</p>
<ul>
<li>文本 embedding 语义检索</li>
<li>RAG 原型</li>
<li>中小规模知识库</li>
</ul>
<h3 id="8-3-IndexHNSWFlat"><a href="#8-3-IndexHNSWFlat" class="headerlink" title="8.3 IndexHNSWFlat"></a>8.3 <code>IndexHNSWFlat</code></h3><p>HNSW 是一种基于图的近似最近邻搜索方法。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.IndexHNSWFlat(d, <span class="number">32</span>)</span><br></pre></td></tr></table></figure>

<p>第二个参数通常叫 <code>M</code>，可以理解成图里每个节点连接的邻居数量。</p>
<p>特点：</p>
<ul>
<li>不需要像 IVF 那样单独训练</li>
<li>搜索速度快</li>
<li>召回率通常不错</li>
<li>内存比 Flat 更高，因为要额外存图结构</li>
</ul>
<p>常见调参：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index.hnsw.efSearch = <span class="number">64</span></span><br><span class="line">index.hnsw.efConstruction = <span class="number">200</span></span><br></pre></td></tr></table></figure>

<p>简单理解：</p>
<ul>
<li><code>M</code> 越大，图更密，召回更好，内存更高。</li>
<li><code>efSearch</code> 越大，搜索更认真，召回更好，速度更慢。</li>
<li><code>efConstruction</code> 越大，建图更慢，但图质量可能更好。</li>
</ul>
<p>适合：</p>
<ul>
<li>希望查询快</li>
<li>数据规模比 Flat 更大</li>
<li>可以接受近似结果</li>
</ul>
<h3 id="8-4-IndexIVFFlat"><a href="#8-4-IndexIVFFlat" class="headerlink" title="8.4 IndexIVFFlat"></a>8.4 <code>IndexIVFFlat</code></h3><p>IVF，全称 Inverted File，可以理解成“先粗分类，再局部搜索”。</p>
<p>基本思路：</p>
<ol>
<li>先把向量空间分成很多个簇</li>
<li>添加向量时，把每个向量放进对应簇里</li>
<li>查询时，只搜索最相关的几个簇</li>
</ol>
<p>代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> faiss</span><br><span class="line"></span><br><span class="line">d = <span class="number">768</span></span><br><span class="line">nlist = <span class="number">100</span></span><br><span class="line"></span><br><span class="line">quantizer = faiss.IndexFlatIP(d)</span><br><span class="line">index = faiss.IndexIVFFlat(</span><br><span class="line">    quantizer,</span><br><span class="line">    d,</span><br><span class="line">    nlist,</span><br><span class="line">    faiss.METRIC_INNER_PRODUCT,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">faiss.normalize_L2(train_vectors)</span><br><span class="line">faiss.normalize_L2(database_vectors)</span><br><span class="line">faiss.normalize_L2(query_vectors)</span><br><span class="line"></span><br><span class="line">index.train(train_vectors)</span><br><span class="line">index.add(database_vectors)</span><br><span class="line"></span><br><span class="line">index.nprobe = <span class="number">10</span></span><br><span class="line">D, I = index.search(query_vectors, k=<span class="number">5</span>)</span><br></pre></td></tr></table></figure>

<p>关键参数：</p>
<ul>
<li><code>nlist</code>：聚类中心数量，也就是分多少个桶</li>
<li><code>nprobe</code>：查询时搜索多少个桶</li>
</ul>
<p>简单理解：</p>
<ul>
<li><code>nlist</code> 越大，桶越多，每个桶里数据越少，但训练和管理更复杂。</li>
<li><code>nprobe</code> 越大，搜的桶越多，召回更高，速度更慢。</li>
</ul>
<p>注意：</p>
<blockquote>
<p>IVF 类索引通常需要先 <code>train()</code>，再 <code>add()</code>。</p>
</blockquote>
<p>适合：</p>
<ul>
<li>数据量较大</li>
<li>希望比 Flat 更快</li>
<li>能接受近似检索</li>
</ul>
<h3 id="8-5-IndexIVFPQ"><a href="#8-5-IndexIVFPQ" class="headerlink" title="8.5 IndexIVFPQ"></a>8.5 <code>IndexIVFPQ</code></h3><p>PQ，全称 Product Quantization，核心目的是压缩向量。</p>
<p><code>IndexIVFPQ</code> 可以理解成：</p>
<blockquote>
<p>IVF 负责缩小搜索范围，PQ 负责压缩每个向量，降低内存占用。</p>
</blockquote>
<p>特点：</p>
<ul>
<li>内存占用明显降低</li>
<li>查询速度可以更快</li>
<li>结果是近似的</li>
<li>实现和调参比 Flat、HNSW、IVFFlat 更复杂</li>
</ul>
<p>适合：</p>
<ul>
<li>向量数量很大</li>
<li>内存压力明显</li>
<li>可以接受召回率下降</li>
</ul>
<p>对于普通 RAG 项目，不建议一开始就用 IVFPQ。先用 <code>IndexFlatIP</code> 或 <code>IndexHNSWFlat</code> 跑通业务，再根据数据量和性能瓶颈升级。</p>
<h2 id="9-index-factory"><a href="#9-index-factory" class="headerlink" title="9. index_factory"></a>9. <code>index_factory</code></h2><p>FAISS 提供 <code>index_factory</code> 用字符串快速创建索引。</p>
<p>例如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.index_factory(d, <span class="string">&quot;Flat&quot;</span>, faiss.METRIC_INNER_PRODUCT)</span><br></pre></td></tr></table></figure>

<p>HNSW：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.index_factory(d, <span class="string">&quot;HNSW32,Flat&quot;</span>, faiss.METRIC_INNER_PRODUCT)</span><br></pre></td></tr></table></figure>

<p>IVF：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.index_factory(d, <span class="string">&quot;IVF100,Flat&quot;</span>, faiss.METRIC_INNER_PRODUCT)</span><br></pre></td></tr></table></figure>

<p>IVFPQ：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.index_factory(d, <span class="string">&quot;IVF100,PQ16&quot;</span>, faiss.METRIC_INNER_PRODUCT)</span><br></pre></td></tr></table></figure>

<p><code>index_factory</code> 的好处是：</p>
<ul>
<li>创建复合索引更方便</li>
<li>配置可以字符串化</li>
<li>适合写到配置文件里</li>
</ul>
<p>缺点是：</p>
<ul>
<li>初学时不如显式构造容易理解</li>
<li>写错字符串时排查成本更高</li>
</ul>
<p>建议：</p>
<ul>
<li>学习阶段先显式构造</li>
<li>熟悉索引类型后再用 <code>index_factory</code></li>
</ul>
<h2 id="10-向量-ID-和业务-ID"><a href="#10-向量-ID-和业务-ID" class="headerlink" title="10. 向量 ID 和业务 ID"></a>10. 向量 ID 和业务 ID</h2><h3 id="10-1-默认-ID"><a href="#10-1-默认-ID" class="headerlink" title="10.1 默认 ID"></a>10.1 默认 ID</h3><p>如果直接这样添加：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index.add(vectors)</span><br></pre></td></tr></table></figure>

<p>FAISS 会使用内部连续 ID：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">0, 1, 2, 3, ...</span><br></pre></td></tr></table></figure>

<p>这适合简单 demo，但真实项目里通常不够。</p>
<p>因为 RAG 里你真正关心的是：</p>
<ul>
<li>这个向量属于哪个文档</li>
<li>是第几个 chunk</li>
<li>对应 MySQL 里的哪条记录</li>
<li>原文内容是什么</li>
<li>页码、标题、来源是什么</li>
</ul>
<h3 id="10-2-使用-IndexIDMap"><a href="#10-2-使用-IndexIDMap" class="headerlink" title="10.2 使用 IndexIDMap"></a>10.2 使用 <code>IndexIDMap</code></h3><p>可以给向量指定自己的 ID：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> faiss</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line">base_index = faiss.IndexFlatIP(d)</span><br><span class="line">index = faiss.IndexIDMap(base_index)</span><br><span class="line"></span><br><span class="line">vectors = np.asarray(vectors, dtype=<span class="string">&quot;float32&quot;</span>)</span><br><span class="line">ids = np.asarray([<span class="number">101</span>, <span class="number">102</span>, <span class="number">103</span>], dtype=<span class="string">&quot;int64&quot;</span>)</span><br><span class="line"></span><br><span class="line">faiss.normalize_L2(vectors)</span><br><span class="line">index.add_with_ids(vectors, ids)</span><br><span class="line"></span><br><span class="line">D, I = index.search(query, k=<span class="number">3</span>)</span><br></pre></td></tr></table></figure>

<p>这里返回的 <code>I</code> 就是你传入的业务 ID。</p>
<h3 id="10-3-RAG-项目里的-ID-设计"><a href="#10-3-RAG-项目里的-ID-设计" class="headerlink" title="10.3 RAG 项目里的 ID 设计"></a>10.3 RAG 项目里的 ID 设计</h3><p>推荐做法：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">MySQL chunk 表：</span><br><span class="line">- id: chunk_id</span><br><span class="line">- document_id</span><br><span class="line">- chunk_index</span><br><span class="line">- content</span><br><span class="line">- page</span><br><span class="line">- metadata</span><br><span class="line">- embedding_model</span><br><span class="line">- created_at</span><br><span class="line"></span><br><span class="line">FAISS：</span><br><span class="line">- 向量 ID 使用 chunk_id</span><br></pre></td></tr></table></figure>

<p>搜索后：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">FAISS 返回 chunk_id</span><br><span class="line">  -&gt; 用 chunk_id 查 MySQL</span><br><span class="line">  -&gt; 取 content、document_id、page、source</span><br><span class="line">  -&gt; 组装 prompt 和 citation</span><br></pre></td></tr></table></figure>

<p>不要把大量元数据硬塞进 FAISS。FAISS 适合管向量，不适合替代业务数据库。</p>
<h2 id="11-索引保存和加载"><a href="#11-索引保存和加载" class="headerlink" title="11. 索引保存和加载"></a>11. 索引保存和加载</h2><p>FAISS index 可以保存到磁盘。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">faiss.write_index(index, <span class="string">&quot;docs.index&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>加载：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.read_index(<span class="string">&quot;docs.index&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>RAG 项目里常见做法：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">data/</span><br><span class="line">└── indexes/</span><br><span class="line">    ├── kb_1.index</span><br><span class="line">    ├── kb_2.index</span><br><span class="line">    └── kb_3.index</span><br></pre></td></tr></table></figure>

<p>同时在数据库里记录：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">knowledge_base_id</span><br><span class="line">index_path</span><br><span class="line">embedding_model</span><br><span class="line">embedding_dim</span><br><span class="line">metric_type</span><br><span class="line">index_type</span><br><span class="line">chunk_count</span><br><span class="line">version</span><br><span class="line">updated_at</span><br></pre></td></tr></table></figure>

<p>这样做的好处：</p>
<ul>
<li>服务重启后可以直接加载 index</li>
<li>可以知道 index 对应哪个 embedding 模型</li>
<li>模型切换后能判断旧索引是否需要重建</li>
</ul>
<h2 id="12-RAG-项目中的-FAISS-封装"><a href="#12-RAG-项目中的-FAISS-封装" class="headerlink" title="12. RAG 项目中的 FAISS 封装"></a>12. RAG 项目中的 FAISS 封装</h2><p>在项目里不要把 FAISS 操作散落在 router 或 service 里，建议封装成 <code>VectorStore</code>。</p>
<p>示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pathlib <span class="keyword">import</span> Path</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> faiss</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">FaissVectorStore</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, dim: <span class="built_in">int</span>, index_path: <span class="built_in">str</span> | <span class="literal">None</span> = <span class="literal">None</span></span>):</span><br><span class="line">        <span class="variable language_">self</span>.dim = dim</span><br><span class="line">        <span class="variable language_">self</span>.index_path = Path(index_path) <span class="keyword">if</span> index_path <span class="keyword">else</span> <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>.index_path <span class="keyword">and</span> <span class="variable language_">self</span>.index_path.exists():</span><br><span class="line">            <span class="variable language_">self</span>.index = faiss.read_index(<span class="built_in">str</span>(<span class="variable language_">self</span>.index_path))</span><br><span class="line">            <span class="keyword">if</span> <span class="variable language_">self</span>.index.d != dim:</span><br><span class="line">                <span class="keyword">raise</span> ValueError(<span class="string">&quot;index dimension mismatch&quot;</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            base_index = faiss.IndexFlatIP(dim)</span><br><span class="line">            <span class="variable language_">self</span>.index = faiss.IndexIDMap(base_index)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">self, vectors: np.ndarray, ids: np.ndarray</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">        vectors = np.asarray(vectors, dtype=<span class="string">&quot;float32&quot;</span>)</span><br><span class="line">        vectors = np.ascontiguousarray(vectors)</span><br><span class="line">        ids = np.asarray(ids, dtype=<span class="string">&quot;int64&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> vectors.ndim != <span class="number">2</span> <span class="keyword">or</span> vectors.shape[<span class="number">1</span>] != <span class="variable language_">self</span>.dim:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">&quot;invalid vector shape&quot;</span>)</span><br><span class="line"></span><br><span class="line">        faiss.normalize_L2(vectors)</span><br><span class="line">        <span class="variable language_">self</span>.index.add_with_ids(vectors, ids)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">search</span>(<span class="params">self, query: np.ndarray, top_k: <span class="built_in">int</span> = <span class="number">5</span></span>):</span><br><span class="line">        query = np.asarray(query, dtype=<span class="string">&quot;float32&quot;</span>).reshape(<span class="number">1</span>, -<span class="number">1</span>)</span><br><span class="line">        query = np.ascontiguousarray(query)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> query.shape[<span class="number">1</span>] != <span class="variable language_">self</span>.dim:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">&quot;invalid query dimension&quot;</span>)</span><br><span class="line"></span><br><span class="line">        faiss.normalize_L2(query)</span><br><span class="line">        scores, ids = <span class="variable language_">self</span>.index.search(query, top_k)</span><br><span class="line">        <span class="keyword">return</span> scores[<span class="number">0</span>], ids[<span class="number">0</span>]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">save</span>(<span class="params">self</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> <span class="variable language_">self</span>.index_path:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">&quot;index_path is required&quot;</span>)</span><br><span class="line">        <span class="variable language_">self</span>.index_path.parent.mkdir(parents=<span class="literal">True</span>, exist_ok=<span class="literal">True</span>)</span><br><span class="line">        faiss.write_index(<span class="variable language_">self</span>.index, <span class="built_in">str</span>(<span class="variable language_">self</span>.index_path))</span><br></pre></td></tr></table></figure>

<p>这层封装负责：</p>
<ul>
<li>统一检查向量维度</li>
<li>统一转 <code>float32</code></li>
<li>统一做归一化</li>
<li>统一保存和加载 index</li>
<li>对上层隐藏 FAISS 细节</li>
</ul>
<h2 id="13-在-FastAPI-Celery-FAISS-中的位置"><a href="#13-在-FastAPI-Celery-FAISS-中的位置" class="headerlink" title="13. 在 FastAPI + Celery + FAISS 中的位置"></a>13. 在 FastAPI + Celery + FAISS 中的位置</h2><p>结合你的 RAG 项目，可以这样拆：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">FastAPI：</span><br><span class="line">- 接收上传请求</span><br><span class="line">- 创建 document 和 task 记录</span><br><span class="line">- 返回 task_id</span><br><span class="line"></span><br><span class="line">Celery：</span><br><span class="line">- 解析文档</span><br><span class="line">- chunk 切分</span><br><span class="line">- 调 embedding 模型</span><br><span class="line">- 调 VectorStore.add()</span><br><span class="line">- 保存 FAISS index</span><br><span class="line">- 更新任务状态</span><br><span class="line"></span><br><span class="line">FastAPI chat 接口：</span><br><span class="line">- 接收用户问题</span><br><span class="line">- 调 embedding 模型生成 query vector</span><br><span class="line">- 调 VectorStore.search()</span><br><span class="line">- 根据 chunk_id 查 MySQL</span><br><span class="line">- 拼接上下文</span><br><span class="line">- 调 LLM</span><br><span class="line">- 保存 message 和 citation</span><br></pre></td></tr></table></figure>

<p>一个简化链路：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">upload document</span><br><span class="line">  -&gt; Celery ingest task</span><br><span class="line">  -&gt; chunks</span><br><span class="line">  -&gt; embeddings</span><br><span class="line">  -&gt; FAISS index</span><br><span class="line">  -&gt; MySQL chunk metadata</span><br><span class="line"></span><br><span class="line">chat question</span><br><span class="line">  -&gt; query embedding</span><br><span class="line">  -&gt; FAISS top-k chunk_id</span><br><span class="line">  -&gt; MySQL load chunk content</span><br><span class="line">  -&gt; LLM answer</span><br></pre></td></tr></table></figure>

<h2 id="14-单文档索引和知识库索引"><a href="#14-单文档索引和知识库索引" class="headerlink" title="14. 单文档索引和知识库索引"></a>14. 单文档索引和知识库索引</h2><h3 id="14-1-单文档索引"><a href="#14-1-单文档索引" class="headerlink" title="14.1 单文档索引"></a>14.1 单文档索引</h3><p>每个文档一个 FAISS index。</p>
<p>优点：</p>
<ul>
<li>实现简单</li>
<li>删除文档方便</li>
<li>文件和索引一一对应</li>
</ul>
<p>缺点：</p>
<ul>
<li>多文档检索麻烦</li>
<li>查询多个文档时要搜多个 index</li>
<li>不适合知识库级检索</li>
</ul>
<h3 id="14-2-知识库索引"><a href="#14-2-知识库索引" class="headerlink" title="14.2 知识库索引"></a>14.2 知识库索引</h3><p>一个知识库一个 FAISS index，里面包含多个文档的 chunk。</p>
<p>优点：</p>
<ul>
<li>支持多文档统一检索</li>
<li>更接近真实 RAG 系统</li>
<li>检索流程简单</li>
</ul>
<p>缺点：</p>
<ul>
<li>删除单个文档更麻烦</li>
<li>索引版本管理更重要</li>
<li>需要维护 chunk_id 到文档元数据的映射</li>
</ul>
<p>推荐：</p>
<ul>
<li>demo 阶段可以单文档索引</li>
<li>简历项目和真实项目更建议做知识库索引</li>
</ul>
<h2 id="15-更新和删除"><a href="#15-更新和删除" class="headerlink" title="15. 更新和删除"></a>15. 更新和删除</h2><p>FAISS 不是传统数据库，更新和删除要谨慎设计。</p>
<h3 id="15-1-添加"><a href="#15-1-添加" class="headerlink" title="15.1 添加"></a>15.1 添加</h3><p>添加新 chunk 比较简单：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index.add_with_ids(new_vectors, new_ids)</span><br><span class="line">faiss.write_index(index, index_path)</span><br></pre></td></tr></table></figure>

<h3 id="15-2-删除"><a href="#15-2-删除" class="headerlink" title="15.2 删除"></a>15.2 删除</h3><p>部分索引支持 <code>remove_ids()</code>，但工程上仍然要考虑：</p>
<ul>
<li>删除后 MySQL 元数据是否同步</li>
<li>index 文件是否需要重新保存</li>
<li>删除大量数据后索引质量是否下降</li>
<li>多进程读写是否冲突</li>
</ul>
<p>更稳妥的做法：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">少量删除：</span><br><span class="line">- 数据库标记 chunk/document 为 deleted</span><br><span class="line">- 检索后过滤 deleted 结果</span><br><span class="line"></span><br><span class="line">大量删除或周期整理：</span><br><span class="line">- 根据有效 chunk 重新构建 index</span><br><span class="line">- 原子替换旧 index 文件</span><br></pre></td></tr></table></figure>

<h3 id="15-3-更新"><a href="#15-3-更新" class="headerlink" title="15.3 更新"></a>15.3 更新</h3><p>更新文档一般等价于：</p>
<ol>
<li>删除旧文档对应 chunk</li>
<li>重新解析文档</li>
<li>重新生成 embedding</li>
<li>重新加入 index</li>
</ol>
<p>如果更新频率高，就要认真设计索引重建策略。</p>
<h2 id="16-检索参数怎么选"><a href="#16-检索参数怎么选" class="headerlink" title="16. 检索参数怎么选"></a>16. 检索参数怎么选</h2><h3 id="16-1-top-k"><a href="#16-1-top-k" class="headerlink" title="16.1 top_k"></a>16.1 <code>top_k</code></h3><p><code>top_k</code> 表示返回几个候选 chunk。</p>
<p>RAG 常见取值：</p>
<ul>
<li><code>top_k = 3</code></li>
<li><code>top_k = 5</code></li>
<li><code>top_k = 10</code></li>
</ul>
<p>取太小：</p>
<ul>
<li>可能漏掉关键信息</li>
</ul>
<p>取太大：</p>
<ul>
<li>prompt 变长</li>
<li>噪声变多</li>
<li>LLM 成本上升</li>
</ul>
<p>可以先用 <code>top_k=5</code>，后续根据实际效果调。</p>
<h3 id="16-2-nprobe"><a href="#16-2-nprobe" class="headerlink" title="16.2 nprobe"></a>16.2 <code>nprobe</code></h3><p>IVF 索引里最重要的搜索参数是 <code>nprobe</code>。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index.nprobe = <span class="number">10</span></span><br></pre></td></tr></table></figure>

<p>简单理解：</p>
<ul>
<li><code>nprobe</code> 小：速度快，可能漏结果</li>
<li><code>nprobe</code> 大：召回高，速度慢</li>
</ul>
<p>调参时要看：</p>
<ul>
<li>查询耗时</li>
<li>recall</li>
<li>最终回答质量</li>
</ul>
<h3 id="16-3-HNSW-参数"><a href="#16-3-HNSW-参数" class="headerlink" title="16.3 HNSW 参数"></a>16.3 HNSW 参数</h3><p>HNSW 常看：</p>
<ul>
<li><code>M</code></li>
<li><code>efConstruction</code></li>
<li><code>efSearch</code></li>
</ul>
<p>经验理解：</p>
<ul>
<li><code>M</code> 控制图连接密度</li>
<li><code>efConstruction</code> 控制建图质量</li>
<li><code>efSearch</code> 控制查询时探索范围</li>
</ul>
<p>如果检索结果质量不够，先尝试调大 <code>efSearch</code>。</p>
<h2 id="17-如何评估-FAISS-检索效果"><a href="#17-如何评估-FAISS-检索效果" class="headerlink" title="17. 如何评估 FAISS 检索效果"></a>17. 如何评估 FAISS 检索效果</h2><p>只看“能不能返回结果”是不够的。</p>
<p>RAG 项目里至少可以做几个简单评估：</p>
<h3 id="17-1-Recall-K"><a href="#17-1-Recall-K" class="headerlink" title="17.1 Recall@K"></a>17.1 Recall@K</h3><p>如果你有标准答案 chunk，可以看：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Recall@K = 正确 chunk 是否出现在 Top-K 结果里</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<ul>
<li><code>Recall@1</code></li>
<li><code>Recall@3</code></li>
<li><code>Recall@5</code></li>
</ul>
<h3 id="17-2-MRR"><a href="#17-2-MRR" class="headerlink" title="17.2 MRR"></a>17.2 MRR</h3><p>MRR 关注正确结果排在第几位。</p>
<p>如果正确 chunk 总是排在第一名，效果就很好。</p>
<h3 id="17-3-人工问题集"><a href="#17-3-人工问题集" class="headerlink" title="17.3 人工问题集"></a>17.3 人工问题集</h3><p>最实用的方式是为每份文档准备一批问题：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">问题：论文里使用的数据集是什么？</span><br><span class="line">期望命中的 chunk：chunk_id = 123</span><br></pre></td></tr></table></figure>

<p>然后统计：</p>
<ul>
<li>是否命中</li>
<li>排名第几</li>
<li>分数是多少</li>
<li>LLM 最终回答是否引用了正确片段</li>
</ul>
<h3 id="17-4-检索耗时"><a href="#17-4-检索耗时" class="headerlink" title="17.4 检索耗时"></a>17.4 检索耗时</h3><p>RAG 链路里建议记录：</p>
<ul>
<li>embedding 耗时</li>
<li>FAISS search 耗时</li>
<li>MySQL 查 chunk 耗时</li>
<li>LLM 生成耗时</li>
</ul>
<p>这样才能知道瓶颈在哪里。</p>
<h2 id="18-FAISS-和向量数据库的区别"><a href="#18-FAISS-和向量数据库的区别" class="headerlink" title="18. FAISS 和向量数据库的区别"></a>18. FAISS 和向量数据库的区别</h2><p>FAISS 更像一个向量检索引擎或向量索引库。</p>
<p>向量数据库通常还提供：</p>
<ul>
<li>数据持久化管理</li>
<li>元数据过滤</li>
<li>分布式存储</li>
<li>多租户</li>
<li>权限管理</li>
<li>在线增删改查</li>
<li>服务化 API</li>
</ul>
<p>常见向量数据库：</p>
<ul>
<li>Milvus</li>
<li>Qdrant</li>
<li>Weaviate</li>
<li>pgvector</li>
</ul>
<p>对比：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">FAISS：</span><br><span class="line">- 轻量</span><br><span class="line">- 本地库</span><br><span class="line">- 性能强</span><br><span class="line">- 灵活</span><br><span class="line">- 需要自己做元数据和服务封装</span><br><span class="line"></span><br><span class="line">向量数据库：</span><br><span class="line">- 功能完整</span><br><span class="line">- 更偏生产服务</span><br><span class="line">- 自带元数据过滤和管理能力</span><br><span class="line">- 部署和维护成本更高</span><br></pre></td></tr></table></figure>

<p>你的 RAG 项目如果是学习和简历展示，用 FAISS 很合适。它能让你真正理解向量检索、索引、召回率、Top-K、元数据映射这些底层概念。</p>
<h2 id="19-常见坑"><a href="#19-常见坑" class="headerlink" title="19. 常见坑"></a>19. 常见坑</h2><h3 id="19-1-忘记转-float32"><a href="#19-1-忘记转-float32" class="headerlink" title="19.1 忘记转 float32"></a>19.1 忘记转 <code>float32</code></h3><p>很多 embedding 模型或 Python 处理流程会给出 <code>float64</code>。</p>
<p>FAISS 通常期望 <code>float32</code>。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">vectors = vectors.astype(<span class="string">&quot;float32&quot;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="19-2-向量维度不一致"><a href="#19-2-向量维度不一致" class="headerlink" title="19.2 向量维度不一致"></a>19.2 向量维度不一致</h3><p>比如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">索引维度：768</span><br><span class="line">查询向量：1024</span><br></pre></td></tr></table></figure>

<p>这通常是因为：</p>
<ul>
<li>embedding 模型换了</li>
<li>文档向量和查询向量用了不同模型</li>
<li>reshape 错了</li>
</ul>
<p>解决：</p>
<ul>
<li>数据库记录 <code>embedding_model</code> 和 <code>embedding_dim</code></li>
<li>加载 index 时检查配置</li>
<li>查询前检查 query shape</li>
</ul>
<h3 id="19-3-做余弦检索但忘记归一化"><a href="#19-3-做余弦检索但忘记归一化" class="headerlink" title="19.3 做余弦检索但忘记归一化"></a>19.3 做余弦检索但忘记归一化</h3><p>如果用 <code>IndexFlatIP</code> 模拟余弦相似度，一定要：</p>
<ul>
<li>入库向量归一化</li>
<li>查询向量归一化</li>
</ul>
<p>否则内积会受到向量长度影响。</p>
<h3 id="19-4-IVF-忘记-train"><a href="#19-4-IVF-忘记-train" class="headerlink" title="19.4 IVF 忘记 train()"></a>19.4 IVF 忘记 <code>train()</code></h3><p>IVF、PQ 这类索引通常需要训练。</p>
<p>错误流程：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index = faiss.IndexIVFFlat(...)</span><br><span class="line">index.add(vectors)</span><br></pre></td></tr></table></figure>

<p>正确流程：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">index.train(train_vectors)</span><br><span class="line">index.add(vectors)</span><br></pre></td></tr></table></figure>

<p>可以检查：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(index.is_trained)</span><br></pre></td></tr></table></figure>

<h3 id="19-5-只保存-FAISS，不保存元数据"><a href="#19-5-只保存-FAISS，不保存元数据" class="headerlink" title="19.5 只保存 FAISS，不保存元数据"></a>19.5 只保存 FAISS，不保存元数据</h3><p>如果只保存 index 文件，不保存 chunk 元数据，搜索结果只会给你 ID，无法还原原文。</p>
<p>正确做法：</p>
<ul>
<li>FAISS 存向量</li>
<li>MySQL 存 chunk 内容和元数据</li>
<li>FAISS ID 对应 MySQL chunk_id</li>
</ul>
<h3 id="19-6-多进程同时写-index"><a href="#19-6-多进程同时写-index" class="headerlink" title="19.6 多进程同时写 index"></a>19.6 多进程同时写 index</h3><p>如果 FastAPI 和多个 Celery worker 同时写同一个 index 文件，容易出问题。</p>
<p>建议：</p>
<ul>
<li>写操作集中到单个任务队列</li>
<li>使用文件锁或数据库状态锁</li>
<li>写新文件后原子替换旧文件</li>
<li>查询服务读取稳定版本</li>
</ul>
<h3 id="19-7-把-FAISS-当数据库用"><a href="#19-7-把-FAISS-当数据库用" class="headerlink" title="19.7 把 FAISS 当数据库用"></a>19.7 把 FAISS 当数据库用</h3><p>FAISS 适合相似度搜索，不适合做复杂业务查询。</p>
<p>这些事情应该交给业务数据库：</p>
<ul>
<li>用户权限</li>
<li>文档状态</li>
<li>删除标记</li>
<li>文件来源</li>
<li>页码</li>
<li>文本内容</li>
<li>citation 信息</li>
</ul>
<h2 id="20-RAG-项目里的推荐方案"><a href="#20-RAG-项目里的推荐方案" class="headerlink" title="20. RAG 项目里的推荐方案"></a>20. RAG 项目里的推荐方案</h2><p>如果是你的当前 RAG 文档检索项目，我建议这样选：</p>
<h3 id="20-1-初版"><a href="#20-1-初版" class="headerlink" title="20.1 初版"></a>20.1 初版</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">索引：IndexIDMap(IndexFlatIP)</span><br><span class="line">相似度：归一化向量 + Inner Product</span><br><span class="line">元数据：MySQL chunk 表</span><br><span class="line">持久化：faiss.write_index()</span><br><span class="line">任务：Celery 构建索引</span><br></pre></td></tr></table></figure>

<p>优点：</p>
<ul>
<li>简单</li>
<li>准确</li>
<li>方便调试</li>
<li>很适合简历项目说明</li>
</ul>
<h3 id="20-2-数据量变大后"><a href="#20-2-数据量变大后" class="headerlink" title="20.2 数据量变大后"></a>20.2 数据量变大后</h3><p>如果 Flat 查询变慢，可以升级：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">IndexHNSWFlat</span><br></pre></td></tr></table></figure>

<p>或者：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">IndexIVFFlat</span><br></pre></td></tr></table></figure>

<p>建议顺序：</p>
<ol>
<li>先用 Flat 建基准</li>
<li>再上 HNSW 或 IVF</li>
<li>用 Recall@K 对比召回损失</li>
<li>记录查询耗时变化</li>
</ol>
<h3 id="20-3-简历里可以怎么讲"><a href="#20-3-简历里可以怎么讲" class="headerlink" title="20.3 简历里可以怎么讲"></a>20.3 简历里可以怎么讲</h3><p>可以写成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">使用 FAISS 构建文档 chunk 向量索引，采用 chunk_id 作为向量 ID，并在 MySQL 中维护文档、chunk、索引版本和 citation 元数据；查询阶段对用户问题生成 embedding 后执行 Top-K 召回，再根据 chunk_id 回表获取原文内容并拼接上下文供 LLM 生成回答。</span><br></pre></td></tr></table></figure>

<p>如果你做了评测，还可以加：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">基于人工问题集评估 Recall@K 和检索耗时，对比 IndexFlatIP、HNSW、IVFFlat 在召回率、延迟和内存占用上的差异。</span><br></pre></td></tr></table></figure>

<h2 id="21-一个完整的-RAG-检索示例"><a href="#21-一个完整的-RAG-检索示例" class="headerlink" title="21. 一个完整的 RAG 检索示例"></a>21. 一个完整的 RAG 检索示例</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> faiss</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">SimpleRagIndex</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, dim: <span class="built_in">int</span></span>):</span><br><span class="line">        <span class="variable language_">self</span>.dim = dim</span><br><span class="line">        <span class="variable language_">self</span>.index = faiss.IndexIDMap(faiss.IndexFlatIP(dim))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">add_chunks</span>(<span class="params">self, embeddings: <span class="built_in">list</span>[<span class="built_in">list</span>[<span class="built_in">float</span>]], chunk_ids: <span class="built_in">list</span>[<span class="built_in">int</span>]</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">        vectors = np.asarray(embeddings, dtype=<span class="string">&quot;float32&quot;</span>)</span><br><span class="line">        vectors = np.ascontiguousarray(vectors)</span><br><span class="line"></span><br><span class="line">        ids = np.asarray(chunk_ids, dtype=<span class="string">&quot;int64&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> vectors.ndim != <span class="number">2</span>:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">&quot;embeddings must be 2D&quot;</span>)</span><br><span class="line">        <span class="keyword">if</span> vectors.shape[<span class="number">1</span>] != <span class="variable language_">self</span>.dim:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">&quot;embedding dimension mismatch&quot;</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">len</span>(ids) != <span class="built_in">len</span>(vectors):</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">&quot;ids length mismatch&quot;</span>)</span><br><span class="line"></span><br><span class="line">        faiss.normalize_L2(vectors)</span><br><span class="line">        <span class="variable language_">self</span>.index.add_with_ids(vectors, ids)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">search</span>(<span class="params">self, query_embedding: <span class="built_in">list</span>[<span class="built_in">float</span>], top_k: <span class="built_in">int</span> = <span class="number">5</span></span>):</span><br><span class="line">        query = np.asarray(query_embedding, dtype=<span class="string">&quot;float32&quot;</span>).reshape(<span class="number">1</span>, -<span class="number">1</span>)</span><br><span class="line">        query = np.ascontiguousarray(query)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> query.shape[<span class="number">1</span>] != <span class="variable language_">self</span>.dim:</span><br><span class="line">            <span class="keyword">raise</span> ValueError(<span class="string">&quot;query dimension mismatch&quot;</span>)</span><br><span class="line"></span><br><span class="line">        faiss.normalize_L2(query)</span><br><span class="line">        scores, chunk_ids = <span class="variable language_">self</span>.index.search(query, top_k)</span><br><span class="line"></span><br><span class="line">        results = []</span><br><span class="line">        <span class="keyword">for</span> score, chunk_id <span class="keyword">in</span> <span class="built_in">zip</span>(scores[<span class="number">0</span>], chunk_ids[<span class="number">0</span>]):</span><br><span class="line">            <span class="keyword">if</span> chunk_id == -<span class="number">1</span>:</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            results.append(</span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="string">&quot;chunk_id&quot;</span>: <span class="built_in">int</span>(chunk_id),</span><br><span class="line">                    <span class="string">&quot;score&quot;</span>: <span class="built_in">float</span>(score),</span><br><span class="line">                &#125;</span><br><span class="line">            )</span><br><span class="line">        <span class="keyword">return</span> results</span><br><span class="line"></span><br><span class="line">rag_index = SimpleRagIndex(dim=<span class="number">4</span>)</span><br><span class="line"></span><br><span class="line">rag_index.add_chunks(</span><br><span class="line">    embeddings=[</span><br><span class="line">        [<span class="number">1.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>],</span><br><span class="line">        [<span class="number">0.9</span>, <span class="number">0.1</span>, <span class="number">0.0</span>, <span class="number">0.0</span>],</span><br><span class="line">        [<span class="number">0.0</span>, <span class="number">1.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>],</span><br><span class="line">    ],</span><br><span class="line">    chunk_ids=[<span class="number">1001</span>, <span class="number">1002</span>, <span class="number">1003</span>],</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">results = rag_index.search([<span class="number">1.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>, <span class="number">0.0</span>], top_k=<span class="number">2</span>)</span><br><span class="line"><span class="built_in">print</span>(results)</span><br></pre></td></tr></table></figure>

<p>真实项目里，拿到 <code>chunk_id</code> 后再去 MySQL 查：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">chunk_id -&gt; chunk content -&gt; document metadata -&gt; citation</span><br></pre></td></tr></table></figure>

<h2 id="22-学习顺序"><a href="#22-学习顺序" class="headerlink" title="22. 学习顺序"></a>22. 学习顺序</h2><p>建议按这个顺序学：</p>
<ol>
<li>理解 embedding 和向量相似度</li>
<li>跑通 <code>IndexFlatL2</code></li>
<li>跑通 <code>IndexFlatIP + normalize_L2</code></li>
<li>学会 <code>add()</code>、<code>search()</code>、<code>D/I</code></li>
<li>学会 <code>IndexIDMap</code> 绑定业务 ID</li>
<li>学会 <code>write_index()</code> 和 <code>read_index()</code></li>
<li>接入 RAG 项目：chunk_id 回表查 MySQL</li>
<li>数据量变大后再学 HNSW、IVF、PQ</li>
<li>用 Recall@K 和耗时评估索引效果</li>
</ol>
<h2 id="23-总结"><a href="#23-总结" class="headerlink" title="23. 总结"></a>23. 总结</h2><p>FAISS 的核心价值是：</p>
<ul>
<li>快速做向量相似度搜索</li>
<li>支持精确检索和近似检索</li>
<li>支持不同索引结构，在速度、内存、召回率之间取舍</li>
<li>非常适合 RAG 项目里的 Top-K chunk 召回</li>
</ul>
<p>初学时先记住：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Embedding 负责把文本变成向量</span><br><span class="line">FAISS 负责从向量里找相似内容</span><br><span class="line">MySQL 负责保存 chunk 内容和元数据</span><br><span class="line">LLM 负责基于检索结果生成回答</span><br></pre></td></tr></table></figure>

<p>对于你的 RAG 项目，最实用的起点是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">IndexIDMap(IndexFlatIP) + normalize_L2 + chunk_id 回表查 MySQL</span><br></pre></td></tr></table></figure>

<p>等数据量、延迟和内存真的成为问题，再升级 HNSW、IVF 或 PQ。</p>
<h2 id="24-参考"><a href="#24-参考" class="headerlink" title="24. 参考"></a>24. 参考</h2><ul>
<li>FAISS GitHub: <a href="https://github.com/facebookresearch/faiss">https://github.com/facebookresearch/faiss</a></li>
<li>FAISS Wiki: <a href="https://github.com/facebookresearch/faiss/wiki">https://github.com/facebookresearch/faiss/wiki</a></li>
<li>FAISS Getting Started: <a href="https://github.com/facebookresearch/faiss/wiki/Getting-started">https://github.com/facebookresearch/faiss/wiki/Getting-started</a></li>
<li>FAISS Indexes: <a href="https://github.com/facebookresearch/faiss/wiki/Faiss-indexes">https://github.com/facebookresearch/faiss/wiki/Faiss-indexes</a></li>
<li>FAISS MetricType and distances: <a href="https://github.com/facebookresearch/faiss/wiki/MetricType-and-distances">https://github.com/facebookresearch/faiss/wiki/MetricType-and-distances</a></li>
<li>FAISS Index Factory: <a href="https://github.com/facebookresearch/faiss/wiki/The-index-factory">https://github.com/facebookresearch/faiss/wiki/The-index-factory</a></li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>框架</category>
      </categories>
  </entry>
  <entry>
    <title>FastAPI 学习笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/fasthapi/</url>
    <content><![CDATA[<h1 id="FastAPI-学习笔记"><a href="#FastAPI-学习笔记" class="headerlink" title="FastAPI 学习笔记"></a>FastAPI 学习笔记</h1><h2 id="1-FastAPI-是什么"><a href="#1-FastAPI-是什么" class="headerlink" title="1. FastAPI 是什么"></a>1. FastAPI 是什么</h2><p>FastAPI 是一个基于 Python 类型注解构建的 Web API 框架，适合用来开发：</p>
<ul>
<li>后端接口服务</li>
<li>AI 模型推理接口</li>
<li>文件上传和处理接口</li>
<li>内部微服务</li>
<li>需要自动生成接口文档的项目</li>
</ul>
<p>它的几个核心特点：</p>
<ul>
<li>开发效率高，写法接近普通 Python 函数</li>
<li>自动参数校验</li>
<li>自动生成 OpenAPI 文档</li>
<li>支持异步</li>
<li>和 Pydantic 配合很好，适合结构化数据处理</li>
</ul>
<p>FastAPI 最实用的地方在于：你写的是普通 Python 函数，但它会顺手帮你把 HTTP 请求、参数校验和接口文档这些事情一起处理掉。</p>
<h2 id="2-为什么很多-AI-项目喜欢用-FastAPI"><a href="#2-为什么很多-AI-项目喜欢用-FastAPI" class="headerlink" title="2. 为什么很多 AI 项目喜欢用 FastAPI"></a>2. 为什么很多 AI 项目喜欢用 FastAPI</h2><p>在 AI 项目里，FastAPI 很常见，因为它很适合做这些事情：</p>
<ul>
<li>暴露模型推理接口</li>
<li>上传文档、图片、音频</li>
<li>提供任务提交和状态查询接口</li>
<li>给前端或其他服务提供统一 API</li>
<li>和 Celery、Redis、数据库组合成一套服务</li>
</ul>
<p>尤其是下面这种场景：</p>
<ol>
<li>前端上传文件</li>
<li>FastAPI 接收请求</li>
<li>把耗时任务交给 Celery</li>
<li>再通过 FastAPI 提供任务状态查询接口</li>
</ol>
<p>这类架构在 RAG、推理平台、数据处理平台里非常常见。</p>
<h2 id="3-安装"><a href="#3-安装" class="headerlink" title="3. 安装"></a>3. 安装</h2><p>最基础的安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install fastapi uvicorn</span><br></pre></td></tr></table></figure>

<p>如果要做文件上传，还需要：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install python-multipart</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><code>fastapi</code>：框架本身</li>
<li><code>uvicorn</code>：ASGI Server，用来启动服务</li>
<li><code>python-multipart</code>：处理表单和文件上传</li>
</ul>
<h2 id="4-最小可运行示例"><a href="#4-最小可运行示例" class="headerlink" title="4. 最小可运行示例"></a>4. 最小可运行示例</h2><p>文件：<code>main.py</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">read_root</span>():</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;message&quot;</span>: <span class="string">&quot;hello fastapi&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>启动命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uvicorn main:app --reload</span><br></pre></td></tr></table></figure>

<p>含义：</p>
<ul>
<li><code>main</code>：Python 文件名 <code>main.py</code></li>
<li><code>app</code>：文件中的 FastAPI 实例</li>
<li><code>--reload</code>：代码变更后自动重启，适合开发环境</li>
</ul>
<p>启动后可以访问：</p>
<ul>
<li><code>http://127.0.0.1:8000/</code></li>
<li><code>http://127.0.0.1:8000/docs</code></li>
<li><code>http://127.0.0.1:8000/redoc</code></li>
</ul>
<p>其中：</p>
<ul>
<li><code>/docs</code>：Swagger UI</li>
<li><code>/redoc</code>：ReDoc 文档页面</li>
</ul>
<h2 id="5-FastAPI-的基本工作方式"><a href="#5-FastAPI-的基本工作方式" class="headerlink" title="5. FastAPI 的基本工作方式"></a>5. FastAPI 的基本工作方式</h2><p>可以把一个接口理解成：</p>
<ol>
<li>定义一个路径</li>
<li>指定请求方法，比如 <code>GET</code>、<code>POST</code></li>
<li>声明这个接口需要哪些参数</li>
<li>FastAPI 自动帮你解析参数并做校验</li>
<li>函数返回值自动转换为 JSON 响应</li>
</ol>
<p>示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/hello&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">hello</span>(<span class="params">name: <span class="built_in">str</span></span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;message&quot;</span>: <span class="string">f&quot;hello <span class="subst">&#123;name&#125;</span>&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>访问：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">/hello?name=tom</span><br></pre></td></tr></table></figure>

<p>返回：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="string">&quot;hello tom&quot;</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>这里的 <code>name: str</code> 会被 FastAPI 识别为查询参数。</p>
<h2 id="6-路由基础"><a href="#6-路由基础" class="headerlink" title="6. 路由基础"></a>6. 路由基础</h2><p>FastAPI 支持常见的 HTTP 方法：</p>
<ul>
<li><code>@app.get()</code></li>
<li><code>@app.post()</code></li>
<li><code>@app.put()</code></li>
<li><code>@app.delete()</code></li>
<li><code>@app.patch()</code></li>
</ul>
<p>示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/users&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_users</span>():</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;action&quot;</span>: <span class="string">&quot;list users&quot;</span>&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/users&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_user</span>():</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;action&quot;</span>: <span class="string">&quot;create user&quot;</span>&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/users/&#123;user_id&#125;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_user</span>(<span class="params">user_id: <span class="built_in">int</span></span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;user_id&quot;</span>: user_id&#125;</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><code>/users</code> 通常表示资源集合</li>
<li><code>/users/&#123;user_id&#125;</code> 表示某个具体资源</li>
<li><code>user_id: int</code> 会自动做类型校验</li>
</ul>
<p>如果路径参数类型不对，FastAPI 会直接返回校验错误。</p>
<h2 id="7-路径参数、查询参数、请求体"><a href="#7-路径参数、查询参数、请求体" class="headerlink" title="7. 路径参数、查询参数、请求体"></a>7. 路径参数、查询参数、请求体</h2><p>这是 FastAPI 最重要的基础之一。</p>
<h3 id="7-1-路径参数"><a href="#7-1-路径参数" class="headerlink" title="7.1 路径参数"></a>7.1 路径参数</h3><p>路径里写在 <code>&#123;&#125;</code> 中的内容就是路径参数。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/items/&#123;item_id&#125;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_item</span>(<span class="params">item_id: <span class="built_in">int</span></span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;item_id&quot;</span>: item_id&#125;</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">/items/100</span><br></pre></td></tr></table></figure>

<p>这里的 <code>item_id</code> 就是路径参数。</p>
<h3 id="7-2-查询参数"><a href="#7-2-查询参数" class="headerlink" title="7.2 查询参数"></a>7.2 查询参数</h3><p>函数里那些不在路径中的普通基础类型参数，通常会被当成查询参数。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/items&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">list_items</span>(<span class="params">page: <span class="built_in">int</span> = <span class="number">1</span>, size: <span class="built_in">int</span> = <span class="number">10</span></span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;page&quot;</span>: page, <span class="string">&quot;size&quot;</span>: size&#125;</span><br></pre></td></tr></table></figure>

<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">/items?page=2&amp;size=20</span><br></pre></td></tr></table></figure>

<h3 id="7-3-请求体"><a href="#7-3-请求体" class="headerlink" title="7.3 请求体"></a>7.3 请求体</h3><p>如果参数是 Pydantic 模型，FastAPI 会把它当成请求体。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pydantic <span class="keyword">import</span> BaseModel</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UserCreate</span>(<span class="title class_ inherited__">BaseModel</span>):</span><br><span class="line">    name: <span class="built_in">str</span></span><br><span class="line">    age: <span class="built_in">int</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/users&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_user</span>(<span class="params">user: UserCreate</span>):</span><br><span class="line">    <span class="keyword">return</span> user</span><br></pre></td></tr></table></figure>

<p>请求：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;alice&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;age&quot;</span><span class="punctuation">:</span> <span class="number">18</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>结论：</p>
<ul>
<li>路径里的参数是路径参数</li>
<li>简单类型参数通常是查询参数</li>
<li>Pydantic 模型通常是请求体</li>
</ul>
<h2 id="8-Pydantic-模型"><a href="#8-Pydantic-模型" class="headerlink" title="8. Pydantic 模型"></a>8. Pydantic 模型</h2><p>FastAPI 的数据校验非常依赖 Pydantic。</p>
<p>最常见的用途：</p>
<ul>
<li>定义请求体结构</li>
<li>定义返回体结构</li>
<li>做字段校验</li>
<li>约束字段类型</li>
</ul>
<h3 id="8-1-基本示例"><a href="#8-1-基本示例" class="headerlink" title="8.1 基本示例"></a>8.1 基本示例</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pydantic <span class="keyword">import</span> BaseModel, Field</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ArticleCreate</span>(<span class="title class_ inherited__">BaseModel</span>):</span><br><span class="line">    title: <span class="built_in">str</span> = Field(min_length=<span class="number">1</span>, max_length=<span class="number">100</span>)</span><br><span class="line">    content: <span class="built_in">str</span></span><br><span class="line">    author: <span class="built_in">str</span></span><br></pre></td></tr></table></figure>

<p>这里的好处是：</p>
<ul>
<li>字段缺失会报错</li>
<li>类型不对会报错</li>
<li>长度不满足约束会报错</li>
</ul>
<h3 id="8-2-作为返回模型"><a href="#8-2-作为返回模型" class="headerlink" title="8.2 作为返回模型"></a>8.2 作为返回模型</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"><span class="keyword">from</span> pydantic <span class="keyword">import</span> BaseModel</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UserOut</span>(<span class="title class_ inherited__">BaseModel</span>):</span><br><span class="line">    <span class="built_in">id</span>: <span class="built_in">int</span></span><br><span class="line">    name: <span class="built_in">str</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/users/&#123;user_id&#125;&quot;</span>, response_model=UserOut</span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_user</span>(<span class="params">user_id: <span class="built_in">int</span></span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;id&quot;</span>: user_id, <span class="string">&quot;name&quot;</span>: <span class="string">&quot;tom&quot;</span>, <span class="string">&quot;password&quot;</span>: <span class="string">&quot;secret&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>返回给客户端时，<code>password</code> 这类不在 <code>response_model</code> 中的字段会被过滤掉。</p>
<p>这非常适合做：</p>
<ul>
<li>隐藏敏感字段</li>
<li>固定接口返回结构</li>
<li>保证前后端契约一致</li>
</ul>
<h2 id="9-def-和-async-def-怎么选"><a href="#9-def-和-async-def-怎么选" class="headerlink" title="9. def 和 async def 怎么选"></a>9. <code>def</code> 和 <code>async def</code> 怎么选</h2><p>这是 FastAPI 初学时很容易混淆的点。</p>
<h3 id="9-1-用-async-def-的情况"><a href="#9-1-用-async-def-的情况" class="headerlink" title="9.1 用 async def 的情况"></a>9.1 用 <code>async def</code> 的情况</h3><p>适合：</p>
<ul>
<li>调用异步数据库客户端</li>
<li>调用异步 HTTP 客户端</li>
<li>WebSocket</li>
<li>其他真正支持异步的 IO 场景</li>
</ul>
<h3 id="9-2-用普通-def-的情况"><a href="#9-2-用普通-def-的情况" class="headerlink" title="9.2 用普通 def 的情况"></a>9.2 用普通 <code>def</code> 的情况</h3><p>适合：</p>
<ul>
<li>普通同步逻辑</li>
<li>CPU 密集型计算</li>
<li>调用同步库</li>
<li>本来就不是异步的代码</li>
</ul>
<p>注意：</p>
<blockquote>
<p><code>async def</code> 不是“更快”的意思，它只是更适合异步 IO。</p>
</blockquote>
<p>如果你在 <code>async def</code> 里直接跑很重的同步任务，照样会阻塞。</p>
<p>对于重任务，通常应该交给：</p>
<ul>
<li>Celery</li>
<li>任务队列</li>
<li>独立 Worker</li>
</ul>
<h2 id="10-请求参数的更明确写法"><a href="#10-请求参数的更明确写法" class="headerlink" title="10. 请求参数的更明确写法"></a>10. 请求参数的更明确写法</h2><p>FastAPI 虽然能自动推断参数来源，但实际项目里更推荐写明确一些。</p>
<p>可以使用：</p>
<ul>
<li><code>Path</code></li>
<li><code>Query</code></li>
<li><code>Body</code></li>
</ul>
<p>示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> Body, FastAPI, Path, Query</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/items/&#123;item_id&#125;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">update_item</span>(<span class="params"></span></span><br><span class="line"><span class="params">    item_id: <span class="built_in">int</span> = Path(<span class="params">..., ge=<span class="number">1</span></span>),</span></span><br><span class="line"><span class="params">    keyword: <span class="built_in">str</span> | <span class="literal">None</span> = Query(<span class="params">default=<span class="literal">None</span>, min_length=<span class="number">1</span></span>),</span></span><br><span class="line"><span class="params">    data: <span class="built_in">dict</span> = Body(<span class="params">...</span>),</span></span><br><span class="line"><span class="params"></span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;item_id&quot;</span>: item_id,</span><br><span class="line">        <span class="string">&quot;keyword&quot;</span>: keyword,</span><br><span class="line">        <span class="string">&quot;data&quot;</span>: data,</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>这里：</p>
<ul>
<li><code>Path(..., ge=1)</code> 表示路径参数必须大于等于 1</li>
<li><code>Query(...)</code> 用来描述查询参数校验</li>
<li><code>Body(...)</code> 明确说明这个参数来自请求体</li>
</ul>
<p>这种写法更清晰，也更适合团队协作。</p>
<h2 id="11-APIRouter-路由拆分"><a href="#11-APIRouter-路由拆分" class="headerlink" title="11. APIRouter 路由拆分"></a>11. APIRouter 路由拆分</h2><p>项目一大，通常不会把所有接口都写在一个 <code>main.py</code> 里。</p>
<p>FastAPI 推荐使用 <code>APIRouter</code> 拆模块。</p>
<h3 id="11-1-基本示例"><a href="#11-1-基本示例" class="headerlink" title="11.1 基本示例"></a>11.1 基本示例</h3><p>文件：<code>routers/health.py</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> APIRouter</span><br><span class="line"></span><br><span class="line">router = APIRouter(prefix=<span class="string">&quot;/health&quot;</span>, tags=[<span class="string">&quot;health&quot;</span>])</span><br><span class="line"></span><br><span class="line"><span class="meta">@router.get(<span class="params"><span class="string">&quot;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">health_check</span>():</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;status&quot;</span>: <span class="string">&quot;ok&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>主文件：<code>main.py</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> routers.health <span class="keyword">import</span> router <span class="keyword">as</span> health_router</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line">app.include_router(health_router)</span><br></pre></td></tr></table></figure>

<p>访问路径：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">/health</span><br></pre></td></tr></table></figure>

<h3 id="11-2-为什么要拆-Router"><a href="#11-2-为什么要拆-Router" class="headerlink" title="11.2 为什么要拆 Router"></a>11.2 为什么要拆 Router</h3><p>好处：</p>
<ul>
<li>按业务模块组织代码</li>
<li>主入口文件更简洁</li>
<li>适合多人协作</li>
<li>后续做版本管理更方便</li>
</ul>
<p>常见拆分方式：</p>
<ul>
<li><code>routers/user.py</code></li>
<li><code>routers/auth.py</code></li>
<li><code>routers/files.py</code></li>
<li><code>routers/tasks.py</code></li>
</ul>
<h2 id="12-路由前缀和-API-版本管理"><a href="#12-路由前缀和-API-版本管理" class="headerlink" title="12. 路由前缀和 API 版本管理"></a>12. 路由前缀和 API 版本管理</h2><p>你原来的笔记重点写的是这部分，这里整理成更清晰的版本。</p>
<h3 id="12-1-统一加前缀"><a href="#12-1-统一加前缀" class="headerlink" title="12.1 统一加前缀"></a>12.1 统一加前缀</h3><p>如果整个项目都希望走 <code>/api/v1</code> 前缀，可以在 <code>include_router()</code> 时统一加上。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> routers.health <span class="keyword">import</span> router <span class="keyword">as</span> health_router</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line">root_prefix = <span class="string">&quot;/api/v1&quot;</span></span><br><span class="line">app.include_router(health_router, prefix=root_prefix)</span><br></pre></td></tr></table></figure>

<p>如果 <code>health_router</code> 本身是：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">router = APIRouter(prefix=<span class="string">&quot;/health&quot;</span>, tags=[<span class="string">&quot;health&quot;</span>])</span><br></pre></td></tr></table></figure>

<p>那么最终路径就是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">/api/v1/health</span><br></pre></td></tr></table></figure>

<h3 id="12-2-在-Router-上直接带业务前缀"><a href="#12-2-在-Router-上直接带业务前缀" class="headerlink" title="12.2 在 Router 上直接带业务前缀"></a>12.2 在 Router 上直接带业务前缀</h3><p>例如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">router = APIRouter(prefix=<span class="string">&quot;/users&quot;</span>, tags=[<span class="string">&quot;users&quot;</span>])</span><br></pre></td></tr></table></figure>

<p>再在主应用里挂上版本前缀：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">app.include_router(user_router, prefix=<span class="string">&quot;/api/v1&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>最后效果就是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">/api/v1/users</span><br></pre></td></tr></table></figure>

<h3 id="12-3-多版本应用挂载"><a href="#12-3-多版本应用挂载" class="headerlink" title="12.3 多版本应用挂载"></a>12.3 多版本应用挂载</h3><p>如果你想同时保留多个版本，也可以挂多个 FastAPI 子应用。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line">app_v1 = FastAPI(title=<span class="string">&quot;API V1&quot;</span>)</span><br><span class="line">app_v2 = FastAPI(title=<span class="string">&quot;API V2&quot;</span>)</span><br><span class="line"></span><br><span class="line">app.mount(<span class="string">&quot;/api/v1&quot;</span>, app_v1)</span><br><span class="line">app.mount(<span class="string">&quot;/api/v2&quot;</span>, app_v2)</span><br></pre></td></tr></table></figure>

<p>这种方式适合：</p>
<ul>
<li>两个版本差异非常大</li>
<li>想彻底隔离文档和路由</li>
<li>历史接口需要长期兼容</li>
</ul>
<p>但如果只是小规模版本演进，通常直接用：</p>
<ul>
<li><code>include_router(..., prefix=&quot;/api/v1&quot;)</code></li>
<li><code>include_router(..., prefix=&quot;/api/v2&quot;)</code></li>
</ul>
<p>就够了。</p>
<h3 id="12-4-版本管理建议"><a href="#12-4-版本管理建议" class="headerlink" title="12.4 版本管理建议"></a>12.4 版本管理建议</h3><p>实际项目里一般这样做：</p>
<ul>
<li>先按模块拆 Router</li>
<li>再统一加版本前缀</li>
<li>不要一开始就把版本设计得过于复杂</li>
</ul>
<p>多数项目初期使用：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">/api/v1/xxx</span><br></pre></td></tr></table></figure>

<p>已经足够。</p>
<h2 id="13-依赖注入-Depends"><a href="#13-依赖注入-Depends" class="headerlink" title="13. 依赖注入 Depends"></a>13. 依赖注入 <code>Depends</code></h2><p><code>Depends</code> 是 FastAPI 很重要的能力。</p>
<p>它的核心作用是：</p>
<ul>
<li>把公共逻辑抽出来复用</li>
<li>给接口注入数据库连接、用户信息、分页参数等</li>
<li>让路由函数更干净</li>
</ul>
<h3 id="13-1-基本示例"><a href="#13-1-基本示例" class="headerlink" title="13.1 基本示例"></a>13.1 基本示例</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> Depends, FastAPI</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">common_pagination</span>(<span class="params">page: <span class="built_in">int</span> = <span class="number">1</span>, size: <span class="built_in">int</span> = <span class="number">10</span></span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;page&quot;</span>: page, <span class="string">&quot;size&quot;</span>: size&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/articles&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">list_articles</span>(<span class="params">pagination=Depends(<span class="params">common_pagination</span>)</span>):</span><br><span class="line">    <span class="keyword">return</span> pagination</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><code>page</code> 和 <code>size</code> 可以在多个接口中复用</li>
<li>路由函数里只拿最终整理好的结果</li>
</ul>
<h3 id="13-2-常见依赖场景"><a href="#13-2-常见依赖场景" class="headerlink" title="13.2 常见依赖场景"></a>13.2 常见依赖场景</h3><ul>
<li>获取当前登录用户</li>
<li>获取数据库 Session</li>
<li>提取分页参数</li>
<li>权限校验</li>
<li>校验请求头</li>
</ul>
<p>例如认证场景：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> Depends, FastAPI, Header, HTTPException</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">verify_token</span>(<span class="params">x_token: <span class="built_in">str</span> = Header(<span class="params">...</span>)</span>):</span><br><span class="line">    <span class="keyword">if</span> x_token != <span class="string">&quot;demo-token&quot;</span>:</span><br><span class="line">        <span class="keyword">raise</span> HTTPException(status_code=<span class="number">401</span>, detail=<span class="string">&quot;invalid token&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> x_token</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/secure&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">secure_api</span>(<span class="params">token: <span class="built_in">str</span> = Depends(<span class="params">verify_token</span>)</span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;token&quot;</span>: token&#125;</span><br></pre></td></tr></table></figure>

<h2 id="14-异常处理"><a href="#14-异常处理" class="headerlink" title="14. 异常处理"></a>14. 异常处理</h2><p>FastAPI 常用 <code>HTTPException</code> 抛业务错误。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI, HTTPException</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/users/&#123;user_id&#125;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_user</span>(<span class="params">user_id: <span class="built_in">int</span></span>):</span><br><span class="line">    <span class="keyword">if</span> user_id != <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">raise</span> HTTPException(status_code=<span class="number">404</span>, detail=<span class="string">&quot;user not found&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;id&quot;</span>: <span class="number">1</span>, <span class="string">&quot;name&quot;</span>: <span class="string">&quot;tom&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>适合用在：</p>
<ul>
<li>资源不存在</li>
<li>权限不足</li>
<li>参数不合法</li>
<li>业务状态不满足要求</li>
</ul>
<p>补充理解：</p>
<ul>
<li>参数校验错误通常由 FastAPI 自动处理</li>
<li>业务逻辑错误通常由你主动抛 <code>HTTPException</code></li>
</ul>
<h2 id="15-文件上传"><a href="#15-文件上传" class="headerlink" title="15. 文件上传"></a>15. 文件上传</h2><p>这在 AI 项目里很常见，比如：</p>
<ul>
<li>上传 PDF</li>
<li>上传图片</li>
<li>上传音频</li>
<li>上传知识库文档</li>
</ul>
<h3 id="15-1-单文件上传"><a href="#15-1-单文件上传" class="headerlink" title="15.1 单文件上传"></a>15.1 单文件上传</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI, File, UploadFile</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/upload&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">upload_file</span>(<span class="params">file: UploadFile = File(<span class="params">...</span>)</span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;filename&quot;</span>: file.filename,</span><br><span class="line">        <span class="string">&quot;content_type&quot;</span>: file.content_type,</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="15-2-保存到本地"><a href="#15-2-保存到本地" class="headerlink" title="15.2 保存到本地"></a>15.2 保存到本地</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pathlib <span class="keyword">import</span> Path</span><br><span class="line"><span class="keyword">import</span> shutil</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI, File, UploadFile</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line">UPLOAD_DIR = Path(<span class="string">&quot;uploads&quot;</span>)</span><br><span class="line">UPLOAD_DIR.mkdir(exist_ok=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/upload&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">upload_file</span>(<span class="params">file: UploadFile = File(<span class="params">...</span>)</span>):</span><br><span class="line">    save_path = UPLOAD_DIR / file.filename</span><br><span class="line"></span><br><span class="line">    <span class="keyword">with</span> save_path.<span class="built_in">open</span>(<span class="string">&quot;wb&quot;</span>) <span class="keyword">as</span> buffer:</span><br><span class="line">        shutil.copyfileobj(file.file, buffer)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;path&quot;</span>: <span class="built_in">str</span>(save_path)&#125;</span><br></pre></td></tr></table></figure>

<h3 id="15-3-实战建议"><a href="#15-3-实战建议" class="headerlink" title="15.3 实战建议"></a>15.3 实战建议</h3><ul>
<li>不要直接相信上传文件名</li>
<li>最好自己生成唯一文件名</li>
<li>大文件处理不要阻塞接口太久</li>
<li>上传成功后可以把处理任务交给 Celery</li>
</ul>
<h2 id="16-表单与文件混合提交"><a href="#16-表单与文件混合提交" class="headerlink" title="16. 表单与文件混合提交"></a>16. 表单与文件混合提交</h2><p>有时接口既要文件，也要普通字段，比如：</p>
<ul>
<li>文件 + 文档类型</li>
<li>图片 + 用户 ID</li>
<li>音频 + 语言参数</li>
</ul>
<p>示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI, File, Form, UploadFile</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/documents&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_document</span>(<span class="params"></span></span><br><span class="line"><span class="params">    file: UploadFile = File(<span class="params">...</span>),</span></span><br><span class="line"><span class="params">    doc_type: <span class="built_in">str</span> = Form(<span class="params">...</span>),</span></span><br><span class="line"><span class="params"></span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="string">&quot;filename&quot;</span>: file.filename,</span><br><span class="line">        <span class="string">&quot;doc_type&quot;</span>: doc_type,</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="17-中间件和-CORS"><a href="#17-中间件和-CORS" class="headerlink" title="17. 中间件和 CORS"></a>17. 中间件和 CORS</h2><h3 id="17-1-中间件是什么"><a href="#17-1-中间件是什么" class="headerlink" title="17.1 中间件是什么"></a>17.1 中间件是什么</h3><p>中间件是在请求进入路由前、响应返回客户端前统一插入的一层处理逻辑。</p>
<p>常见用途：</p>
<ul>
<li>记录请求日志</li>
<li>统计耗时</li>
<li>增加追踪 ID</li>
<li>统一鉴权</li>
</ul>
<h3 id="17-2-CORS"><a href="#17-2-CORS" class="headerlink" title="17.2 CORS"></a>17.2 CORS</h3><p>如果前后端分离，浏览器经常会遇到跨域问题。</p>
<p>FastAPI 通常这样加：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"><span class="keyword">from</span> fastapi.middleware.cors <span class="keyword">import</span> CORSMiddleware</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line">app.add_middleware(</span><br><span class="line">    CORSMiddleware,</span><br><span class="line">    allow_origins=[<span class="string">&quot;http://localhost:3000&quot;</span>],</span><br><span class="line">    allow_credentials=<span class="literal">True</span>,</span><br><span class="line">    allow_methods=[<span class="string">&quot;*&quot;</span>],</span><br><span class="line">    allow_headers=[<span class="string">&quot;*&quot;</span>],</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>开发环境常见，生产环境一般要更严格地限制来源。</p>
<h2 id="18-生命周期事件"><a href="#18-生命周期事件" class="headerlink" title="18. 生命周期事件"></a>18. 生命周期事件</h2><p>很多项目需要在服务启动时做初始化，比如：</p>
<ul>
<li>建立连接池</li>
<li>加载配置</li>
<li>初始化日志</li>
<li>加载模型</li>
</ul>
<p>FastAPI 支持应用生命周期管理。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> contextlib <span class="keyword">import</span> asynccontextmanager</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line"><span class="meta">@asynccontextmanager</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">lifespan</span>(<span class="params">app: FastAPI</span>):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;service starting&quot;</span>)</span><br><span class="line">    <span class="keyword">yield</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;service stopping&quot;</span>)</span><br><span class="line"></span><br><span class="line">app = FastAPI(lifespan=lifespan)</span><br></pre></td></tr></table></figure>

<p>如果项目里有大模型、向量库客户端、数据库连接池，这一层会比较重要。</p>
<h2 id="19-BackgroundTasks-和-Celery-的区别"><a href="#19-BackgroundTasks-和-Celery-的区别" class="headerlink" title="19. BackgroundTasks 和 Celery 的区别"></a>19. <code>BackgroundTasks</code> 和 Celery 的区别</h2><p>这点和你刚整理的 <code>Celery.md</code> 是连着的。</p>
<h3 id="19-1-BackgroundTasks"><a href="#19-1-BackgroundTasks" class="headerlink" title="19.1 BackgroundTasks"></a>19.1 <code>BackgroundTasks</code></h3><p>适合：</p>
<ul>
<li>很轻量的后台操作</li>
<li>和当前 Web 进程生命周期绑定</li>
<li>不需要重试</li>
<li>不需要独立扩容</li>
</ul>
<p>示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> BackgroundTasks, FastAPI</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">write_log</span>(<span class="params">message: <span class="built_in">str</span></span>):</span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(<span class="string">&quot;app.log&quot;</span>, <span class="string">&quot;a&quot;</span>, encoding=<span class="string">&quot;utf-8&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        f.write(message + <span class="string">&quot;\n&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/notify&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">notify</span>(<span class="params">background_tasks: BackgroundTasks</span>):</span><br><span class="line">    background_tasks.add_task(write_log, <span class="string">&quot;new request&quot;</span>)</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;message&quot;</span>: <span class="string">&quot;accepted&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<h3 id="19-2-Celery"><a href="#19-2-Celery" class="headerlink" title="19.2 Celery"></a>19.2 Celery</h3><p>更适合：</p>
<ul>
<li>长耗时任务</li>
<li>文件处理</li>
<li>模型推理</li>
<li>批量任务</li>
<li>需要失败重试</li>
<li>需要多机扩展</li>
</ul>
<p>一句话：</p>
<blockquote>
<p>轻任务用 <code>BackgroundTasks</code>，重任务用 Celery。</p>
</blockquote>
<h2 id="20-FastAPI-和-Celery-的典型配合方式"><a href="#20-FastAPI-和-Celery-的典型配合方式" class="headerlink" title="20. FastAPI 和 Celery 的典型配合方式"></a>20. FastAPI 和 Celery 的典型配合方式</h2><p>在 AI 项目里，比较常见的结构是：</p>
<ol>
<li>FastAPI 负责接收请求</li>
<li>接口做参数校验和鉴权</li>
<li>把耗时任务提交给 Celery</li>
<li>返回 <code>task_id</code></li>
<li>前端轮询任务状态</li>
</ol>
<p>例如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> tasks <span class="keyword">import</span> process_document</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/tasks/documents&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_document_task</span>(<span class="params">file_path: <span class="built_in">str</span></span>):</span><br><span class="line">    task = process_document.delay(file_path)</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;task_id&quot;</span>: task.<span class="built_in">id</span>&#125;</span><br></pre></td></tr></table></figure>

<p>这个思路尤其适合：</p>
<ul>
<li>文档解析</li>
<li>embedding 计算</li>
<li>批量摘要</li>
<li>图片处理</li>
<li>音频转写</li>
</ul>
<h2 id="21-响应模型和状态码"><a href="#21-响应模型和状态码" class="headerlink" title="21. 响应模型和状态码"></a>21. 响应模型和状态码</h2><p>建议在正式项目里尽量把返回结构固定下来。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI, status</span><br><span class="line"><span class="keyword">from</span> pydantic <span class="keyword">import</span> BaseModel</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UserCreate</span>(<span class="title class_ inherited__">BaseModel</span>):</span><br><span class="line">    name: <span class="built_in">str</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">UserOut</span>(<span class="title class_ inherited__">BaseModel</span>):</span><br><span class="line">    <span class="built_in">id</span>: <span class="built_in">int</span></span><br><span class="line">    name: <span class="built_in">str</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.post(<span class="params"><span class="string">&quot;/users&quot;</span>, response_model=UserOut, status_code=status.HTTP_201_CREATED</span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_user</span>(<span class="params">user: UserCreate</span>):</span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;id&quot;</span>: <span class="number">1</span>, <span class="string">&quot;name&quot;</span>: user.name&#125;</span><br></pre></td></tr></table></figure>

<p>这样做的好处：</p>
<ul>
<li>文档更清晰</li>
<li>前端更好联调</li>
<li>返回结构更稳定</li>
</ul>
<h2 id="22-测试"><a href="#22-测试" class="headerlink" title="22. 测试"></a>22. 测试</h2><p>FastAPI 支持用 <code>TestClient</code> 做接口测试。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi.testclient <span class="keyword">import</span> TestClient</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> main <span class="keyword">import</span> app</span><br><span class="line"></span><br><span class="line">client = TestClient(app)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">test_read_root</span>():</span><br><span class="line">    response = client.get(<span class="string">&quot;/&quot;</span>)</span><br><span class="line">    <span class="keyword">assert</span> response.status_code == <span class="number">200</span></span><br><span class="line">    <span class="keyword">assert</span> response.json() == &#123;<span class="string">&quot;message&quot;</span>: <span class="string">&quot;hello fastapi&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<p>这个对接口开发很有用，特别是：</p>
<ul>
<li>改接口时防止回归</li>
<li>验证参数校验</li>
<li>验证权限逻辑</li>
</ul>
<h2 id="23-FastAPI-项目的工程结构"><a href="#23-FastAPI-项目的工程结构" class="headerlink" title="23. FastAPI 项目的工程结构"></a>23. FastAPI 项目的工程结构</h2><p>当项目从 demo 变成一个后端服务时，最重要的不是目录多，而是每一层职责清楚。</p>
<p>FastAPI 官方文档里会用 <code>app/main.py</code>、<code>app/dependencies.py</code>、<code>app/routers/</code> 这种结构说明大型应用拆分。实际项目里可以在这个基础上继续细分成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">project/</span><br><span class="line">├── app/</span><br><span class="line">│   ├── main.py</span><br><span class="line">│   ├── api/</span><br><span class="line">│   │   └── v1/</span><br><span class="line">│   │       ├── router.py</span><br><span class="line">│   │       └── endpoints/</span><br><span class="line">│   │           ├── health.py</span><br><span class="line">│   │           ├── documents.py</span><br><span class="line">│   │           ├── chat.py</span><br><span class="line">│   │           └── tasks.py</span><br><span class="line">│   ├── core/</span><br><span class="line">│   │   ├── config.py</span><br><span class="line">│   │   ├── logging.py</span><br><span class="line">│   │   ├── security.py</span><br><span class="line">│   │   └── exceptions.py</span><br><span class="line">│   ├── schemas/</span><br><span class="line">│   │   ├── document.py</span><br><span class="line">│   │   ├── chat.py</span><br><span class="line">│   │   └── task.py</span><br><span class="line">│   ├── models/</span><br><span class="line">│   │   ├── document.py</span><br><span class="line">│   │   ├── chunk.py</span><br><span class="line">│   │   └── message.py</span><br><span class="line">│   ├── crud/</span><br><span class="line">│   │   ├── document.py</span><br><span class="line">│   │   └── task.py</span><br><span class="line">│   ├── services/</span><br><span class="line">│   │   ├── document_service.py</span><br><span class="line">│   │   ├── rag_service.py</span><br><span class="line">│   │   ├── embedding_service.py</span><br><span class="line">│   │   └── llm_service.py</span><br><span class="line">│   ├── clients/</span><br><span class="line">│   │   ├── redis_client.py</span><br><span class="line">│   │   ├── vector_store.py</span><br><span class="line">│   │   └── llm_client.py</span><br><span class="line">│   ├── db/</span><br><span class="line">│   │   ├── session.py</span><br><span class="line">│   │   └── base.py</span><br><span class="line">│   └── workers/</span><br><span class="line">│       ├── celery_app.py</span><br><span class="line">│       └── tasks.py</span><br><span class="line">├── tests/</span><br><span class="line">│   ├── test_documents.py</span><br><span class="line">│   └── test_chat.py</span><br><span class="line">└── requirements.txt</span><br></pre></td></tr></table></figure>

<p>不一定每个项目都要这么全。目录结构应该跟项目复杂度匹配，小项目可以先从 <code>main.py + routers + schemas + services</code> 开始。</p>
<h3 id="23-1-各目录负责什么"><a href="#23-1-各目录负责什么" class="headerlink" title="23.1 各目录负责什么"></a>23.1 各目录负责什么</h3><ul>
<li><code>main.py</code>：应用入口，只负责创建 <code>FastAPI</code> 实例、注册中间件、注册路由、声明生命周期逻辑。</li>
<li><code>api/v1/router.py</code>：汇总某个 API 版本下的所有 router，例如 <code>/documents</code>、<code>/chat</code>、<code>/tasks</code>。</li>
<li><code>api/v1/endpoints/</code>：HTTP 接口层，只处理请求参数、依赖注入、状态码和响应模型，不直接堆复杂业务。</li>
<li><code>schemas/</code>：Pydantic 模型，定义请求体、返回体和接口契约。</li>
<li><code>models/</code>：数据库 ORM 模型，对应 MySQL 表结构。</li>
<li><code>crud/</code>：数据库读写封装，只做增删改查，不写复杂业务流程。</li>
<li><code>services/</code>：业务逻辑层，负责组织多个 <code>crud</code>、<code>client</code>、算法模块完成一个完整业务动作。</li>
<li><code>clients/</code>：外部服务或基础设施客户端，例如 Redis、向量库、LLM 服务、对象存储。</li>
<li><code>core/</code>：全局配置、日志、安全、异常处理、公共常量。</li>
<li><code>workers/</code>：Celery 应用和后台任务，处理文档解析、embedding、索引构建等耗时流程。</li>
<li><code>tests/</code>：接口测试、业务测试、E2E 测试。</li>
</ul>
<p>最关键的原则：</p>
<blockquote>
<p>Router 不写重业务，Service 不关心 HTTP，CRUD 不关心业务流程。</p>
</blockquote>
<h3 id="23-2-一次请求在结构里的流动"><a href="#23-2-一次请求在结构里的流动" class="headerlink" title="23.2 一次请求在结构里的流动"></a>23.2 一次请求在结构里的流动</h3><p>以创建文档任务为例：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Client</span><br><span class="line">  -&gt; FastAPI middleware</span><br><span class="line">  -&gt; api/v1/endpoints/documents.py</span><br><span class="line">  -&gt; Depends 获取配置、数据库 Session、用户信息</span><br><span class="line">  -&gt; schemas/document.py 校验请求体</span><br><span class="line">  -&gt; services/document_service.py 组织业务</span><br><span class="line">  -&gt; crud/document.py 写入文档记录</span><br><span class="line">  -&gt; workers/tasks.py 提交 Celery 任务</span><br><span class="line">  -&gt; 返回 task_id</span><br></pre></td></tr></table></figure>

<p>接口函数应该尽量薄，例如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> APIRouter, Depends, UploadFile</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> app.db.session <span class="keyword">import</span> get_db</span><br><span class="line"><span class="keyword">from</span> app.schemas.document <span class="keyword">import</span> DocumentUploadResponse</span><br><span class="line"><span class="keyword">from</span> app.services.document_service <span class="keyword">import</span> DocumentService</span><br><span class="line"></span><br><span class="line">router = APIRouter(prefix=<span class="string">&quot;/documents&quot;</span>, tags=[<span class="string">&quot;documents&quot;</span>])</span><br><span class="line"></span><br><span class="line"><span class="meta">@router.post(<span class="params"><span class="string">&quot;&quot;</span>, response_model=DocumentUploadResponse</span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">upload_document</span>(<span class="params">file: UploadFile, db=Depends(<span class="params">get_db</span>)</span>):</span><br><span class="line">    service = DocumentService(db)</span><br><span class="line">    <span class="keyword">return</span> service.create_ingest_task(file)</span><br></pre></td></tr></table></figure>

<p>这个接口只做三件事：</p>
<ul>
<li>接收 HTTP 请求</li>
<li>通过 <code>Depends</code> 拿到依赖</li>
<li>调用 service 返回结果</li>
</ul>
<p>文件保存、数据库写入、任务提交、错误处理都应该下沉到 service 或更底层。</p>
<h3 id="23-3-main-py-应该保持很薄"><a href="#23-3-main-py-应该保持很薄" class="headerlink" title="23.3 main.py 应该保持很薄"></a>23.3 <code>main.py</code> 应该保持很薄</h3><p><code>main.py</code> 更像“装配入口”，不要把业务逻辑都塞进去。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"><span class="keyword">from</span> fastapi.middleware.cors <span class="keyword">import</span> CORSMiddleware</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> app.api.v1.router <span class="keyword">import</span> api_router</span><br><span class="line"><span class="keyword">from</span> app.core.config <span class="keyword">import</span> settings</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">create_app</span>() -&gt; FastAPI:</span><br><span class="line">    app = FastAPI(title=settings.app_name)</span><br><span class="line"></span><br><span class="line">    app.add_middleware(</span><br><span class="line">        CORSMiddleware,</span><br><span class="line">        allow_origins=settings.cors_origins,</span><br><span class="line">        allow_credentials=<span class="literal">True</span>,</span><br><span class="line">        allow_methods=[<span class="string">&quot;*&quot;</span>],</span><br><span class="line">        allow_headers=[<span class="string">&quot;*&quot;</span>],</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    app.include_router(api_router, prefix=<span class="string">&quot;/api/v1&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> app</span><br><span class="line"></span><br><span class="line">app = create_app()</span><br></pre></td></tr></table></figure>

<p>好处：</p>
<ul>
<li>应用创建逻辑清楚</li>
<li>测试时可以复用 <code>create_app()</code></li>
<li>业务模块不会和框架入口互相缠住</li>
</ul>
<h3 id="23-4-API-Router-汇总方式"><a href="#23-4-API-Router-汇总方式" class="headerlink" title="23.4 API Router 汇总方式"></a>23.4 API Router 汇总方式</h3><p>可以用一个 <code>api/v1/router.py</code> 统一挂载各业务模块：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> APIRouter</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> app.api.v1.endpoints <span class="keyword">import</span> chat, documents, health, tasks</span><br><span class="line"></span><br><span class="line">api_router = APIRouter()</span><br><span class="line">api_router.include_router(health.router)</span><br><span class="line">api_router.include_router(documents.router)</span><br><span class="line">api_router.include_router(tasks.router)</span><br><span class="line">api_router.include_router(chat.router)</span><br></pre></td></tr></table></figure>

<p>这样 <code>main.py</code> 只需要引入一个 <code>api_router</code>，不会随着接口数量变多而越来越乱。</p>
<h3 id="23-5-配置层-core-config-py"><a href="#23-5-配置层-core-config-py" class="headerlink" title="23.5 配置层 core/config.py"></a>23.5 配置层 <code>core/config.py</code></h3><p>正式项目不要把数据库地址、Redis 地址、模型地址直接写死在业务代码里。更好的做法是用配置对象统一管理。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pydantic_settings <span class="keyword">import</span> BaseSettings, SettingsConfigDict</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Settings</span>(<span class="title class_ inherited__">BaseSettings</span>):</span><br><span class="line">    model_config = SettingsConfigDict(env_file=<span class="string">&quot;.env&quot;</span>)</span><br><span class="line"></span><br><span class="line">    app_name: <span class="built_in">str</span> = <span class="string">&quot;rag-api&quot;</span></span><br><span class="line">    mysql_url: <span class="built_in">str</span></span><br><span class="line">    redis_url: <span class="built_in">str</span></span><br><span class="line">    llm_base_url: <span class="built_in">str</span></span><br><span class="line">    embedding_model: <span class="built_in">str</span> = <span class="string">&quot;KaLM-Embedding-0.5B&quot;</span></span><br><span class="line">    cors_origins: <span class="built_in">list</span>[<span class="built_in">str</span>] = [<span class="string">&quot;http://localhost:3000&quot;</span>]</span><br><span class="line"></span><br><span class="line">settings = Settings()</span><br></pre></td></tr></table></figure>

<p>这样可以做到：</p>
<ul>
<li>本地、测试、生产环境分开配置</li>
<li>敏感信息不写进代码</li>
<li>配置类型可以被 Pydantic 校验</li>
</ul>
<h3 id="23-6-结合-RAG-项目的推荐结构"><a href="#23-6-结合-RAG-项目的推荐结构" class="headerlink" title="23.6 结合 RAG 项目的推荐结构"></a>23.6 结合 RAG 项目的推荐结构</h3><p>如果是你的 RAG 文档检索项目，可以这样对应：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">接口层：</span><br><span class="line">- documents.py：上传文档、查询文档列表、删除文档</span><br><span class="line">- tasks.py：查询 Celery 任务状态和进度</span><br><span class="line">- chat.py：创建会话、提交问题、返回回答和 citations</span><br><span class="line">- health.py：健康检查和依赖状态检查</span><br><span class="line"></span><br><span class="line">业务层：</span><br><span class="line">- document_service.py：保存文件、创建文档记录、提交 ingest 任务</span><br><span class="line">- rag_service.py：Top-K 检索、上下文拼接、调用 LLM、保存 message/citation</span><br><span class="line">- embedding_service.py：文本向量化、批量 embedding</span><br><span class="line">- index_service.py：FAISS 索引构建、加载、保存</span><br><span class="line"></span><br><span class="line">基础设施层：</span><br><span class="line">- db/session.py：MySQL Session</span><br><span class="line">- clients/redis_client.py：Redis 连接</span><br><span class="line">- clients/llm_client.py：OpenAI-compatible LLM 调用</span><br><span class="line">- clients/vector_store.py：FAISS 检索封装</span><br><span class="line">- workers/tasks.py：文档解析、chunk、embedding、建索引</span><br></pre></td></tr></table></figure>

<p>RAG 链路可以这样记：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">上传接口</span><br><span class="line">  -&gt; document_service 创建文档和任务</span><br><span class="line">  -&gt; Celery worker 解析文档</span><br><span class="line">  -&gt; chunk 切片</span><br><span class="line">  -&gt; embedding_service 生成向量</span><br><span class="line">  -&gt; index_service 写入 FAISS</span><br><span class="line">  -&gt; task 状态更新</span><br><span class="line"></span><br><span class="line">问答接口</span><br><span class="line">  -&gt; rag_service 接收问题</span><br><span class="line">  -&gt; vector_store Top-K 检索</span><br><span class="line">  -&gt; 拼接上下文和 Prompt</span><br><span class="line">  -&gt; llm_client 调用模型</span><br><span class="line">  -&gt; 保存 answer 和 citation</span><br><span class="line">  -&gt; 返回给前端</span><br></pre></td></tr></table></figure>

<h3 id="23-7-什么时候需要拆得更细"><a href="#23-7-什么时候需要拆得更细" class="headerlink" title="23.7 什么时候需要拆得更细"></a>23.7 什么时候需要拆得更细</h3><p>如果项目只有几个接口：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">main.py</span><br><span class="line">routers/</span><br><span class="line">schemas/</span><br><span class="line">services/</span><br></pre></td></tr></table></figure>

<p>就已经够用。</p>
<p>当出现下面情况时，再继续拆：</p>
<ul>
<li>接口超过十几个</li>
<li>多个接口复用同一批业务逻辑</li>
<li>数据库表开始变多</li>
<li>出现 Celery、Redis、LLM、向量库等外部依赖</li>
<li>测试变得难写</li>
<li><code>main.py</code> 或某个 router 文件超过几百行</li>
</ul>
<p>不要为了“看起来专业”一开始就堆很多目录。目录结构的目的不是复杂，而是让业务边界清楚。</p>
<h2 id="24-常见坑"><a href="#24-常见坑" class="headerlink" title="24. 常见坑"></a>24. 常见坑</h2><h3 id="24-1-把所有代码都写在一个文件里"><a href="#24-1-把所有代码都写在一个文件里" class="headerlink" title="24.1 把所有代码都写在一个文件里"></a>24.1 把所有代码都写在一个文件里</h3><p>初学时能跑，项目一大就很难维护。</p>
<p>建议尽快学会：</p>
<ul>
<li><code>APIRouter</code></li>
<li>模块拆分</li>
<li>业务逻辑下沉到 service 层</li>
</ul>
<h3 id="24-2-在-async-def-里跑重同步任务"><a href="#24-2-在-async-def-里跑重同步任务" class="headerlink" title="24.2 在 async def 里跑重同步任务"></a>24.2 在 <code>async def</code> 里跑重同步任务</h3><p>这会阻塞事件循环。</p>
<p>如果是：</p>
<ul>
<li>长时间计算</li>
<li>文件处理</li>
<li>模型推理</li>
</ul>
<p>通常不应该直接堆在接口函数里。</p>
<h3 id="24-3-请求体验证和返回结构不统一"><a href="#24-3-请求体验证和返回结构不统一" class="headerlink" title="24.3 请求体验证和返回结构不统一"></a>24.3 请求体验证和返回结构不统一</h3><p>如果不定义 Pydantic 模型，接口会越来越乱。</p>
<p>建议：</p>
<ul>
<li>请求体尽量用模型</li>
<li>返回值尽量用 <code>response_model</code></li>
</ul>
<h3 id="24-4-忽略文件上传安全问题"><a href="#24-4-忽略文件上传安全问题" class="headerlink" title="24.4 忽略文件上传安全问题"></a>24.4 忽略文件上传安全问题</h3><p>例如：</p>
<ul>
<li>直接使用用户传上来的文件名</li>
<li>不限制文件类型</li>
<li>不限制文件大小</li>
</ul>
<p>这些在正式项目里都容易出问题。</p>
<h3 id="24-5-把-FastAPI-当成“万能后台线程框架”"><a href="#24-5-把-FastAPI-当成“万能后台线程框架”" class="headerlink" title="24.5 把 FastAPI 当成“万能后台线程框架”"></a>24.5 把 FastAPI 当成“万能后台线程框架”</h3><p>FastAPI 主要是 Web API 框架，不是专门的任务调度系统。</p>
<p>如果任务已经明显是：</p>
<ul>
<li>重任务</li>
<li>批处理</li>
<li>需要排队</li>
<li>需要重试</li>
</ul>
<p>就应该交给 Celery 之类的任务系统。</p>
<h2 id="25-一套比较实用的学习顺序"><a href="#25-一套比较实用的学习顺序" class="headerlink" title="25. 一套比较实用的学习顺序"></a>25. 一套比较实用的学习顺序</h2><p>建议按这个顺序掌握：</p>
<ol>
<li>先跑通最小示例</li>
<li>理解路由、路径参数、查询参数</li>
<li>学会用 Pydantic 定义请求体和返回体</li>
<li>学会拆 <code>APIRouter</code></li>
<li>学会 <code>Depends</code></li>
<li>学会文件上传</li>
<li>学会 <code>BackgroundTasks</code></li>
<li>最后再接入 Celery、数据库、鉴权</li>
</ol>
<h2 id="26-总结"><a href="#26-总结" class="headerlink" title="26. 总结"></a>26. 总结</h2><p>FastAPI 的核心价值在于：</p>
<ul>
<li>用很少的代码快速暴露 API</li>
<li>自动完成参数解析和校验</li>
<li>自动生成接口文档</li>
<li>很适合和 Pydantic、Celery、Redis、数据库组合</li>
</ul>
<p>对于 AI 项目，可以把它理解成：</p>
<ul>
<li>对外接口入口</li>
<li>请求校验层</li>
<li>文件上传入口</li>
<li>任务分发入口</li>
<li>状态查询入口</li>
</ul>
<p>如果你后面要继续整理 AI 模型开发相关知识点，FastAPI、Redis、Celery 这三份笔记其实可以看成一条链：</p>
<ul>
<li>FastAPI：接请求</li>
<li>Redis：做缓存 &#x2F; 消息中间件</li>
<li>Celery：处理后台任务</li>
</ul>
<h2 id="27-参考"><a href="#27-参考" class="headerlink" title="27. 参考"></a>27. 参考</h2><ul>
<li>FastAPI Official Docs: <a href="https://fastapi.tiangolo.com/">https://fastapi.tiangolo.com/</a></li>
<li>FastAPI Path Params: <a href="https://fastapi.tiangolo.com/tutorial/path-params/">https://fastapi.tiangolo.com/tutorial/path-params/</a></li>
<li>FastAPI Query Params: <a href="https://fastapi.tiangolo.com/tutorial/query-params/">https://fastapi.tiangolo.com/tutorial/query-params/</a></li>
<li>FastAPI Request Body: <a href="https://fastapi.tiangolo.com/tutorial/body/">https://fastapi.tiangolo.com/tutorial/body/</a></li>
<li>FastAPI APIRouter: <a href="https://fastapi.tiangolo.com/tutorial/bigger-applications/">https://fastapi.tiangolo.com/tutorial/bigger-applications/</a></li>
<li>FastAPI Dependencies: <a href="https://fastapi.tiangolo.com/tutorial/dependencies/">https://fastapi.tiangolo.com/tutorial/dependencies/</a></li>
<li>FastAPI File Upload: <a href="https://fastapi.tiangolo.com/tutorial/request-files/">https://fastapi.tiangolo.com/tutorial/request-files/</a></li>
<li>FastAPI Background Tasks: <a href="https://fastapi.tiangolo.com/tutorial/background-tasks/">https://fastapi.tiangolo.com/tutorial/background-tasks/</a></li>
<li>FastAPI Testing: <a href="https://fastapi.tiangolo.com/tutorial/testing/">https://fastapi.tiangolo.com/tutorial/testing/</a></li>
<li>FastAPI Middleware: <a href="https://fastapi.tiangolo.com/tutorial/middleware/">https://fastapi.tiangolo.com/tutorial/middleware/</a></li>
<li>FastAPI Lifespan Events: <a href="https://fastapi.tiangolo.com/advanced/events/">https://fastapi.tiangolo.com/advanced/events/</a></li>
<li>FastAPI Settings: <a href="https://fastapi.tiangolo.com/advanced/settings/">https://fastapi.tiangolo.com/advanced/settings/</a></li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>框架</category>
      </categories>
  </entry>
  <entry>
    <title>Embedding 入门笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/embedding/</url>
    <content><![CDATA[<h1 id="Embedding-入门笔记"><a href="#Embedding-入门笔记" class="headerlink" title="Embedding 入门笔记"></a>Embedding 入门笔记</h1><h2 id="1-什么是-Embedding"><a href="#1-什么是-Embedding" class="headerlink" title="1. 什么是 Embedding"></a>1. 什么是 Embedding</h2><p>Embedding 做的事情可以先这样看：</p>
<ul>
<li>把文本、图片、代码等内容，转换成一串数字</li>
<li>这串数字不是随便生成的，而是尽量保留原内容的“语义信息”</li>
<li>语义越接近的内容，转成的向量通常越接近</li>
</ul>
<p>更直接一点说，Embedding 是把人能理解的内容，变成机器更容易比较和计算的向量表示。</p>
<hr>
<h2 id="2-为什么需要-Embedding"><a href="#2-为什么需要-Embedding" class="headerlink" title="2. 为什么需要 Embedding"></a>2. 为什么需要 Embedding</h2><p>计算机不直接理解“意思”，它更擅长处理数字。</p>
<p>例如这两句话：</p>
<ul>
<li>今天天气很好</li>
<li>今天阳光不错</li>
</ul>
<p>如果只看关键词，它们并不完全一样；<br>但如果转成 embedding，它们在向量空间里通常会比较近，因为语义接近。</p>
<p>所以 embedding 的核心价值是：</p>
<ul>
<li>不只看字面是否一样</li>
<li>更关注内容“意思像不像”</li>
</ul>
<hr>
<h2 id="3-Embedding-的直观类比"><a href="#3-Embedding-的直观类比" class="headerlink" title="3. Embedding 的直观类比"></a>3. Embedding 的直观类比</h2><p>可以把 embedding 想成“给每段内容找一个坐标”。</p>
<ul>
<li>语义相近的内容，坐标位置更近</li>
<li>语义差别大的内容，坐标位置更远</li>
</ul>
<p>例如：</p>
<ul>
<li>“苹果手机”</li>
<li>“iPhone”</li>
</ul>
<p>它们可能在向量空间里很接近。</p>
<p>而：</p>
<ul>
<li>“苹果手机”</li>
<li>“高等数学”</li>
</ul>
<p>它们通常会相距很远。</p>
<hr>
<h2 id="4-Embedding-的输出长什么样"><a href="#4-Embedding-的输出长什么样" class="headerlink" title="4. Embedding 的输出长什么样"></a>4. Embedding 的输出长什么样</h2><p>Embedding 的结果一般是一个高维向量，例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">[0.021, -0.184, 0.337, ..., 0.092]</span><br></pre></td></tr></table></figure>

<p>特点：</p>
<ul>
<li>从形式上看，就是一串浮点数组成的列表</li>
<li>维度可能是几百到几千</li>
<li>人看不懂具体数值，但模型和检索系统可以利用它做计算</li>
</ul>
<p>你不需要记住每个数值，只需要理解：</p>
<ul>
<li>向量本身没有直观意义</li>
<li>向量之间的“距离&#x2F;相似度”有意义</li>
</ul>
<hr>
<h2 id="5-Embedding-主要解决什么问题"><a href="#5-Embedding-主要解决什么问题" class="headerlink" title="5. Embedding 主要解决什么问题"></a>5. Embedding 主要解决什么问题</h2><h3 id="5-1-语义检索"><a href="#5-1-语义检索" class="headerlink" title="5.1 语义检索"></a>5.1 语义检索</h3><p>最常见用途。</p>
<p>用户搜索：</p>
<ul>
<li>“怎么提高学习效率”</li>
</ul>
<p>文档里写的是：</p>
<ul>
<li>“如何建立高效的学习习惯”</li>
</ul>
<p>虽然字面不完全一致，但 embedding 检索可能仍然能找到这段内容。</p>
<h3 id="5-2-推荐系统"><a href="#5-2-推荐系统" class="headerlink" title="5.2 推荐系统"></a>5.2 推荐系统</h3><ul>
<li>给用户推荐相似文章</li>
<li>推荐相似商品</li>
<li>推荐相似视频</li>
</ul>
<h3 id="5-3-聚类"><a href="#5-3-聚类" class="headerlink" title="5.3 聚类"></a>5.3 聚类</h3><p>把意思接近的内容自动分到一组。</p>
<h3 id="5-4-分类"><a href="#5-4-分类" class="headerlink" title="5.4 分类"></a>5.4 分类</h3><p>把新文本和已有标签进行相似度比较，辅助分类。</p>
<h3 id="5-5-RAG"><a href="#5-5-RAG" class="headerlink" title="5.5 RAG"></a>5.5 RAG</h3><p>这是现在最常见的 AI 应用场景之一。</p>
<p>流程通常是：</p>
<ol>
<li>先把知识库文本切块</li>
<li>每个块生成 embedding</li>
<li>用户提问时，也生成问题的 embedding</li>
<li>用相似度检索最相关的文本块</li>
<li>把检索结果交给大模型生成回答</li>
</ol>
<hr>
<h2 id="6-Embedding-的基本工作流程"><a href="#6-Embedding-的基本工作流程" class="headerlink" title="6. Embedding 的基本工作流程"></a>6. Embedding 的基本工作流程</h2><p>以文本为例：</p>
<h3 id="第一步：准备文本"><a href="#第一步：准备文本" class="headerlink" title="第一步：准备文本"></a>第一步：准备文本</h3><p>原始文档通常不会整篇直接做 embedding，而是先切成多个小块（chunk）。</p>
<p>原因：</p>
<ul>
<li>太长会超过模型限制</li>
<li>粒度太大不利于精确检索</li>
<li>分块后更适合做 RAG</li>
</ul>
<h3 id="第二步：调用-embedding-模型"><a href="#第二步：调用-embedding-模型" class="headerlink" title="第二步：调用 embedding 模型"></a>第二步：调用 embedding 模型</h3><p>把每个文本块输入 embedding 模型，得到一个向量。</p>
<h3 id="第三步：存储向量"><a href="#第三步：存储向量" class="headerlink" title="第三步：存储向量"></a>第三步：存储向量</h3><p>常见存储方式：</p>
<ul>
<li>向量数据库</li>
<li>FAISS</li>
<li>pgvector</li>
<li>Milvus</li>
<li>Weaviate</li>
</ul>
<h3 id="第四步：用户提问"><a href="#第四步：用户提问" class="headerlink" title="第四步：用户提问"></a>第四步：用户提问</h3><p>把用户问题也转成 embedding。</p>
<h3 id="第五步：计算相似度"><a href="#第五步：计算相似度" class="headerlink" title="第五步：计算相似度"></a>第五步：计算相似度</h3><p>把“问题向量”和“文本块向量”做相似度计算，找到最相近的几个结果。</p>
<h3 id="第六步：返回结果或交给大模型"><a href="#第六步：返回结果或交给大模型" class="headerlink" title="第六步：返回结果或交给大模型"></a>第六步：返回结果或交给大模型</h3><ul>
<li>可以直接返回检索结果</li>
<li>也可以把结果交给 LLM 生成更完整的回答</li>
</ul>
<hr>
<h2 id="7-常见的相似度计算方式"><a href="#7-常见的相似度计算方式" class="headerlink" title="7. 常见的相似度计算方式"></a>7. 常见的相似度计算方式</h2><p>Embedding 常见比较方式有：</p>
<ul>
<li>余弦相似度（Cosine Similarity）</li>
<li>点积（Dot Product）</li>
<li>欧氏距离（Euclidean Distance）</li>
</ul>
<p>初学阶段重点理解一个结论：</p>
<ul>
<li>两个向量越相似，通常说明两段内容语义越接近</li>
</ul>
<p>实际工程里，很多系统常用余弦相似度。</p>
<hr>
<h2 id="8-Embedding-和关键词搜索的区别"><a href="#8-Embedding-和关键词搜索的区别" class="headerlink" title="8. Embedding 和关键词搜索的区别"></a>8. Embedding 和关键词搜索的区别</h2><h3 id="关键词搜索"><a href="#关键词搜索" class="headerlink" title="关键词搜索"></a>关键词搜索</h3><p>优点：</p>
<ul>
<li>简单直接</li>
<li>精确匹配效果好</li>
</ul>
<p>缺点：</p>
<ul>
<li>容易受措辞影响</li>
<li>同义词、近义表达处理较弱</li>
</ul>
<h3 id="Embedding-搜索"><a href="#Embedding-搜索" class="headerlink" title="Embedding 搜索"></a>Embedding 搜索</h3><p>优点：</p>
<ul>
<li>更关注语义</li>
<li>能处理“说法不同但意思接近”的情况</li>
</ul>
<p>缺点：</p>
<ul>
<li>成本更高</li>
<li>结果不一定完全可解释</li>
<li>有时会召回“看起来像，但其实不对”的内容</li>
</ul>
<p>结论：</p>
<ul>
<li>关键词搜索适合精确匹配</li>
<li>embedding 搜索适合语义检索</li>
<li>很多实际系统会把两者结合起来</li>
</ul>
<hr>
<h2 id="9-Embedding-在-RAG-里的位置"><a href="#9-Embedding-在-RAG-里的位置" class="headerlink" title="9. Embedding 在 RAG 里的位置"></a>9. Embedding 在 RAG 里的位置</h2><p>RAG 可以粗略理解成：</p>
<ul>
<li>Embedding 负责“找资料”</li>
<li>大模型负责“组织答案”</li>
</ul>
<p>也就是说：</p>
<ul>
<li>Embedding 决定你能不能找到相关上下文</li>
<li>LLM 决定你能不能把上下文回答得清楚</li>
</ul>
<p>如果 embedding 检索不到有用内容，那么后面的生成效果通常也会变差。</p>
<p>所以在 RAG 里，embedding 不是配角，而是基础能力。</p>
<hr>
<h2 id="10-初学者最容易混淆的几个点"><a href="#10-初学者最容易混淆的几个点" class="headerlink" title="10. 初学者最容易混淆的几个点"></a>10. 初学者最容易混淆的几个点</h2><h3 id="10-1-Embedding-不是生成答案"><a href="#10-1-Embedding-不是生成答案" class="headerlink" title="10.1 Embedding 不是生成答案"></a>10.1 Embedding 不是生成答案</h3><p>Embedding 主要做“表示”和“检索”，不是直接生成自然语言回答。</p>
<h3 id="10-2-Embedding-不等于关键词匹配"><a href="#10-2-Embedding-不等于关键词匹配" class="headerlink" title="10.2 Embedding 不等于关键词匹配"></a>10.2 Embedding 不等于关键词匹配</h3><p>它更偏向语义空间中的相似性，而不是简单的词面重合。</p>
<h3 id="10-3-向量维度不是越大越好"><a href="#10-3-向量维度不是越大越好" class="headerlink" title="10.3 向量维度不是越大越好"></a>10.3 向量维度不是越大越好</h3><p>维度更高不一定代表效果一定更强，还要考虑：</p>
<ul>
<li>模型质量</li>
<li>成本</li>
<li>存储空间</li>
<li>检索速度</li>
</ul>
<h3 id="10-4-文本切块很重要"><a href="#10-4-文本切块很重要" class="headerlink" title="10.4 文本切块很重要"></a>10.4 文本切块很重要</h3><p>很多检索效果差，不一定是 embedding 模型不行，可能是：</p>
<ul>
<li>切块太大</li>
<li>切块太碎</li>
<li>重叠设置不合理</li>
<li>清洗文本质量差</li>
</ul>
<hr>
<h2 id="11-一个最小例子"><a href="#11-一个最小例子" class="headerlink" title="11. 一个最小例子"></a>11. 一个最小例子</h2><p>假设知识库里有三段文本：</p>
<ol>
<li>Python 是一种编程语言</li>
<li>篮球是一项团队运动</li>
<li>机器学习是人工智能的重要分支</li>
</ol>
<p>用户问题是：</p>
<blockquote>
<p>什么是 AI 的一个重要领域？</p>
</blockquote>
<p>系统流程：</p>
<ol>
<li>先把这三段文本转成 embedding</li>
<li>再把用户问题转成 embedding</li>
<li>比较问题与三段文本的相似度</li>
<li>第 3 段大概率最相近</li>
<li>系统返回第 3 段，或者交给大模型生成最终答案</li>
</ol>
<p>这就是 embedding 在检索中的基本逻辑。</p>
<hr>
<h2 id="12-学习-Embedding-时建议先掌握的关键词"><a href="#12-学习-Embedding-时建议先掌握的关键词" class="headerlink" title="12. 学习 Embedding 时建议先掌握的关键词"></a>12. 学习 Embedding 时建议先掌握的关键词</h2><ul>
<li>向量（vector）</li>
<li>维度（dimension）</li>
<li>语义相似度（semantic similarity）</li>
<li>文本切块（chunking）</li>
<li>向量检索（vector search）</li>
<li>召回（recall）</li>
<li>重排（rerank）</li>
<li>RAG</li>
</ul>
<hr>
<h2 id="13-初学者学习路线"><a href="#13-初学者学习路线" class="headerlink" title="13. 初学者学习路线"></a>13. 初学者学习路线</h2><h3 id="第一阶段：先懂概念"><a href="#第一阶段：先懂概念" class="headerlink" title="第一阶段：先懂概念"></a>第一阶段：先懂概念</h3><p>重点搞清楚：</p>
<ul>
<li>embedding 是什么</li>
<li>为什么能做语义检索</li>
<li>它和关键词搜索有什么区别</li>
</ul>
<h3 id="第二阶段：能跑一个小-demo"><a href="#第二阶段：能跑一个小-demo" class="headerlink" title="第二阶段：能跑一个小 demo"></a>第二阶段：能跑一个小 demo</h3><p>例如：</p>
<ul>
<li>准备几段文本</li>
<li>生成 embedding</li>
<li>输入一个问题</li>
<li>找出最相似的文本</li>
</ul>
<h3 id="第三阶段：结合-RAG-理解"><a href="#第三阶段：结合-RAG-理解" class="headerlink" title="第三阶段：结合 RAG 理解"></a>第三阶段：结合 RAG 理解</h3><p>重点理解：</p>
<ul>
<li>文档如何切块</li>
<li>向量如何存储</li>
<li>如何检索 top-k</li>
<li>为什么还需要 rerank</li>
</ul>
<h3 id="第四阶段：再看工程优化"><a href="#第四阶段：再看工程优化" class="headerlink" title="第四阶段：再看工程优化"></a>第四阶段：再看工程优化</h3><p>例如：</p>
<ul>
<li>检索召回率</li>
<li>多语言效果</li>
<li>成本控制</li>
<li>延迟优化</li>
</ul>
<hr>
<h2 id="14-一句话总结"><a href="#14-一句话总结" class="headerlink" title="14. 一句话总结"></a>14. 一句话总结</h2><p>Embedding 的本质，是把内容变成可以计算“语义距离”的向量表示。<br>它是语义检索、推荐系统、聚类分析和 RAG 的基础能力之一。</p>
<hr>
<h2 id="15-Embedding-模型怎么选"><a href="#15-Embedding-模型怎么选" class="headerlink" title="15. Embedding 模型怎么选"></a>15. Embedding 模型怎么选</h2><p>初学者选 embedding 模型时，重点不要只盯着“排行榜”，而要先看是否适合自己的场景。</p>
<p>可以先看 4 个维度：</p>
<h3 id="15-1-语言"><a href="#15-1-语言" class="headerlink" title="15.1 语言"></a>15.1 语言</h3><ul>
<li>主要是中文，就优先选中文效果好的模型</li>
<li>中英混合，就选多语言模型</li>
<li>如果业务是代码、医学、法律等领域，最好找更贴近领域语料的模型</li>
</ul>
<h3 id="15-2-成本和速度"><a href="#15-2-成本和速度" class="headerlink" title="15.2 成本和速度"></a>15.2 成本和速度</h3><ul>
<li>在线 API：接入快，但按量付费</li>
<li>本地开源模型：可控性高，但需要本地算力和部署成本</li>
</ul>
<h3 id="15-3-向量维度"><a href="#15-3-向量维度" class="headerlink" title="15.3 向量维度"></a>15.3 向量维度</h3><ul>
<li>维度更高，不代表一定更好</li>
<li>维度越高，通常存储成本和检索开销也会更高</li>
</ul>
<h3 id="15-4-是否适合检索任务"><a href="#15-4-是否适合检索任务" class="headerlink" title="15.4 是否适合检索任务"></a>15.4 是否适合检索任务</h3><p>有些模型更偏“通用表征”，有些模型更偏“检索优化”。</p>
<p>做 RAG 时，优先选择明确用于 embedding &#x2F; retrieval 的模型，而不是随便拿一个生成模型来代替。</p>
<h3 id="15-5-初学者建议"><a href="#15-5-初学者建议" class="headerlink" title="15.5 初学者建议"></a>15.5 初学者建议</h3><p>如果你只是入门，建议先用一个成熟模型把流程跑通，不要一开始就在模型上反复切换。</p>
<p>可以按这个顺序来理解：</p>
<ul>
<li>第一阶段先验证流程能跑通</li>
<li>第二阶段再比较不同模型的效果</li>
</ul>
<hr>
<h2 id="16-文本切块怎么做"><a href="#16-文本切块怎么做" class="headerlink" title="16. 文本切块怎么做"></a>16. 文本切块怎么做</h2><p>Embedding 效果好不好，很多时候不只是模型问题，切块策略同样重要。</p>
<h3 id="16-1-为什么要切块"><a href="#16-1-为什么要切块" class="headerlink" title="16.1 为什么要切块"></a>16.1 为什么要切块</h3><p>因为原始文档通常太长，不适合直接拿整篇去做检索。</p>
<p>切块的目标是：</p>
<ul>
<li>每块内容尽量表达一个相对完整的意思</li>
<li>不能太长，否则检索不精准</li>
<li>也不能太短，否则上下文不足</li>
</ul>
<h3 id="16-2-常见切块方式"><a href="#16-2-常见切块方式" class="headerlink" title="16.2 常见切块方式"></a>16.2 常见切块方式</h3><h4 id="固定长度切块"><a href="#固定长度切块" class="headerlink" title="固定长度切块"></a>固定长度切块</h4><p>比如每 300 字或每 500 tokens 切一块。</p>
<p>优点：</p>
<ul>
<li>简单</li>
<li>容易实现</li>
</ul>
<p>缺点：</p>
<ul>
<li>可能把完整段落切断</li>
</ul>
<h4 id="按段落-标题切块"><a href="#按段落-标题切块" class="headerlink" title="按段落 &#x2F; 标题切块"></a>按段落 &#x2F; 标题切块</h4><p>根据自然段、标题、小节来切。</p>
<p>优点：</p>
<ul>
<li>语义更完整</li>
</ul>
<p>缺点：</p>
<ul>
<li>长度不稳定</li>
</ul>
<h4 id="混合切块"><a href="#混合切块" class="headerlink" title="混合切块"></a>混合切块</h4><p>先按标题和段落切，再对过长部分按长度继续细分。</p>
<p>这是实际项目里很常见的方式。</p>
<h3 id="16-3-什么是-overlap"><a href="#16-3-什么是-overlap" class="headerlink" title="16.3 什么是 overlap"></a>16.3 什么是 overlap</h3><p><code>overlap</code> 就是相邻 chunk 之间保留一部分重复内容。</p>
<p>作用：</p>
<ul>
<li>避免关键信息刚好被切断</li>
<li>提高上下文连续性</li>
</ul>
<p>例如：</p>
<ul>
<li>chunk1: 第 1~300 字</li>
<li>chunk2: 第 260~560 字</li>
</ul>
<p>这里就有 40 字重叠。</p>
<h3 id="16-4-初学者的默认思路"><a href="#16-4-初学者的默认思路" class="headerlink" title="16.4 初学者的默认思路"></a>16.4 初学者的默认思路</h3><p>可以先从下面这个简单策略开始：</p>
<ul>
<li>中文笔记或普通文档：每块 300~500 字</li>
<li>overlap：10%~20%</li>
<li>优先按标题和段落切，长度超了再二次切分</li>
</ul>
<p>这不是固定标准，只是适合作为第一版 baseline。</p>
<hr>
<h2 id="17-向量数据库和-FAISS-是什么关系"><a href="#17-向量数据库和-FAISS-是什么关系" class="headerlink" title="17. 向量数据库和 FAISS 是什么关系"></a>17. 向量数据库和 FAISS 是什么关系</h2><p>很多初学者会把 embedding、FAISS、向量数据库混在一起，其实它们不是一回事。</p>
<h3 id="17-1-Embedding-模型负责什么"><a href="#17-1-Embedding-模型负责什么" class="headerlink" title="17.1 Embedding 模型负责什么"></a>17.1 Embedding 模型负责什么</h3><p>负责把文本转成向量。</p>
<h3 id="17-2-FAISS-负责什么"><a href="#17-2-FAISS-负责什么" class="headerlink" title="17.2 FAISS 负责什么"></a>17.2 FAISS 负责什么</h3><p>FAISS 是一个向量检索库，重点是：</p>
<ul>
<li>存向量</li>
<li>建索引</li>
<li>做近似或精确检索</li>
</ul>
<p>它更像“本地检索引擎”，适合：</p>
<ul>
<li>学习</li>
<li>单机实验</li>
<li>本地部署</li>
</ul>
<h3 id="17-3-向量数据库负责什么"><a href="#17-3-向量数据库负责什么" class="headerlink" title="17.3 向量数据库负责什么"></a>17.3 向量数据库负责什么</h3><p>向量数据库除了向量检索，还经常提供：</p>
<ul>
<li>元数据过滤</li>
<li>持久化</li>
<li>集群能力</li>
<li>权限和服务化接口</li>
</ul>
<h3 id="17-4-怎么选"><a href="#17-4-怎么选" class="headerlink" title="17.4 怎么选"></a>17.4 怎么选</h3><ul>
<li>只是学习原理：先用 FAISS</li>
<li>已经有 PostgreSQL：可以考虑 <code>pgvector</code></li>
<li>需要完整服务化能力：再考虑 Milvus、Weaviate、Pinecone 一类方案</li>
</ul>
<p>职责可以这样拆开看：embedding 负责生成向量，FAISS 或向量数据库负责保存、索引和检索这些向量。</p>
<hr>
<h2 id="18-为什么检索后还需要-Rerank"><a href="#18-为什么检索后还需要-Rerank" class="headerlink" title="18. 为什么检索后还需要 Rerank"></a>18. 为什么检索后还需要 Rerank</h2><p>很多 RAG 系统不是“检索完直接交给大模型”，而是会多一步 rerank。</p>
<h3 id="18-1-检索和-rerank-的区别"><a href="#18-1-检索和-rerank-的区别" class="headerlink" title="18.1 检索和 rerank 的区别"></a>18.1 检索和 rerank 的区别</h3><p>第一阶段检索：</p>
<ul>
<li>用 embedding 先快速找出 top-k 候选片段</li>
<li>目标是“尽量别漏掉相关内容”</li>
</ul>
<p>第二阶段 rerank：</p>
<ul>
<li>对候选片段重新打分排序</li>
<li>目标是“把最相关的排到最前面”</li>
</ul>
<h3 id="18-2-为什么要这样做"><a href="#18-2-为什么要这样做" class="headerlink" title="18.2 为什么要这样做"></a>18.2 为什么要这样做</h3><p>因为 embedding 检索更像“粗筛”，速度快，但有时不够精细。<br>rerank 更像“精筛”，速度慢一些，但相关性通常更好。</p>
<p>常见做法是：</p>
<ol>
<li>embedding 先召回 top 10 或 top 20</li>
<li>rerank 再选出 top 3 或 top 5</li>
<li>把这几个最相关片段交给 LLM</li>
</ol>
<h3 id="18-3-初学者什么时候需要-rerank"><a href="#18-3-初学者什么时候需要-rerank" class="headerlink" title="18.3 初学者什么时候需要 rerank"></a>18.3 初学者什么时候需要 rerank</h3><p>如果你的知识库还比较小，可以先不加 rerank。<br>当你发现“明明检索到了相关内容，但排序不理想”时，再加 rerank 会更有感觉。</p>
<hr>
<h2 id="19-RAG-的完整工作流"><a href="#19-RAG-的完整工作流" class="headerlink" title="19. RAG 的完整工作流"></a>19. RAG 的完整工作流</h2><p>把 embedding 放到 RAG 里，可以拆成两条链路来看。</p>
<h3 id="19-1-入库链路"><a href="#19-1-入库链路" class="headerlink" title="19.1 入库链路"></a>19.1 入库链路</h3><ol>
<li>读取原始文档</li>
<li>清洗文本</li>
<li>按规则切块</li>
<li>为每个 chunk 生成 embedding</li>
<li>保存 chunk 文本、元数据和向量</li>
<li>建立向量索引</li>
</ol>
<p>这里通常还会一起保存：</p>
<ul>
<li><code>doc_id</code></li>
<li><code>chunk_id</code></li>
<li><code>source</code></li>
<li><code>title</code></li>
<li><code>section</code></li>
<li><code>text</code></li>
</ul>
<p>这样后面才能做引用返回和结果定位。</p>
<h3 id="19-2-查询链路"><a href="#19-2-查询链路" class="headerlink" title="19.2 查询链路"></a>19.2 查询链路</h3><ol>
<li>用户输入问题</li>
<li>把问题转成 embedding</li>
<li>到向量库里召回 top-k chunks</li>
<li>如果有 rerank，再重排一次</li>
<li>把相关 chunks 拼进 prompt</li>
<li>交给 LLM 生成答案</li>
<li>返回答案和引用来源</li>
</ol>
<h3 id="19-3-在这个流程里谁最关键"><a href="#19-3-在这个流程里谁最关键" class="headerlink" title="19.3 在这个流程里谁最关键"></a>19.3 在这个流程里谁最关键</h3><ul>
<li>chunking 决定“切得好不好”</li>
<li>embedding 决定“能不能找对”</li>
<li>rerank 决定“排得准不准”</li>
<li>LLM 决定“答得顺不顺”</li>
</ul>
<p>所以 RAG 的质量不是只靠一个大模型决定的。</p>
<hr>
<h2 id="20-一个最小可运行示例"><a href="#20-一个最小可运行示例" class="headerlink" title="20. 一个最小可运行示例"></a>20. 一个最小可运行示例</h2><p>下面这个例子演示：</p>
<ul>
<li>先把几段文本转成 embedding</li>
<li>用 FAISS 建索引</li>
<li>再用问题去检索最相近的内容</li>
</ul>
<p>安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install sentence-transformers faiss-cpu numpy</span><br></pre></td></tr></table></figure>

<p>示例代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> faiss</span><br><span class="line"><span class="keyword">from</span> sentence_transformers <span class="keyword">import</span> SentenceTransformer</span><br><span class="line"></span><br><span class="line">docs = [</span><br><span class="line">    <span class="string">&quot;Python 是一种通用编程语言。&quot;</span>,</span><br><span class="line">    <span class="string">&quot;Celery 用来处理异步任务。&quot;</span>,</span><br><span class="line">    <span class="string">&quot;Embedding 可以把文本表示成向量。&quot;</span>,</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line">query = <span class="string">&quot;什么技术可以把文本变成向量？&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 这里只是示例，模型名可以按你的语言和场景替换</span></span><br><span class="line">model = SentenceTransformer(<span class="string">&quot;BAAI/bge-small-zh-v1.5&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># normalize_embeddings=True 时，常配合内积检索</span></span><br><span class="line">doc_vectors = model.encode(docs, normalize_embeddings=<span class="literal">True</span>)</span><br><span class="line">doc_vectors = np.asarray(doc_vectors, dtype=<span class="string">&quot;float32&quot;</span>)</span><br><span class="line"></span><br><span class="line">index = faiss.IndexFlatIP(doc_vectors.shape[<span class="number">1</span>])</span><br><span class="line">index.add(doc_vectors)</span><br><span class="line"></span><br><span class="line">query_vector = model.encode([query], normalize_embeddings=<span class="literal">True</span>)</span><br><span class="line">query_vector = np.asarray(query_vector, dtype=<span class="string">&quot;float32&quot;</span>)</span><br><span class="line"></span><br><span class="line">scores, ids = index.search(query_vector, k=<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> score, idx <span class="keyword">in</span> <span class="built_in">zip</span>(scores[<span class="number">0</span>], ids[<span class="number">0</span>]):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;score=<span class="subst">&#123;score:<span class="number">.4</span>f&#125;</span> text=<span class="subst">&#123;docs[idx]&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>

<p>如果结果里把：</p>
<ul>
<li><code>Embedding 可以把文本表示成向量。</code></li>
</ul>
<p>排在最前面，就说明最基本的语义检索已经跑通了。</p>
<hr>
<h2 id="21-检索效果不好时怎么排查"><a href="#21-检索效果不好时怎么排查" class="headerlink" title="21. 检索效果不好时怎么排查"></a>21. 检索效果不好时怎么排查</h2><p>很多时候不是“模型不行”，而是流程某一环出了问题。</p>
<p>建议按这个顺序排查：</p>
<h3 id="21-1-先看-chunk"><a href="#21-1-先看-chunk" class="headerlink" title="21.1 先看 chunk"></a>21.1 先看 chunk</h3><ul>
<li>是否切得太碎</li>
<li>是否一块里塞了太多主题</li>
<li>标题和正文是否被拆散</li>
</ul>
<h3 id="21-2-再看文本清洗"><a href="#21-2-再看文本清洗" class="headerlink" title="21.2 再看文本清洗"></a>21.2 再看文本清洗</h3><ul>
<li>是否有大量乱码</li>
<li>是否混入无关模板文字</li>
<li>是否把表格、代码块、换行结构破坏了</li>
</ul>
<h3 id="21-3-再看相似度设置"><a href="#21-3-再看相似度设置" class="headerlink" title="21.3 再看相似度设置"></a>21.3 再看相似度设置</h3><ul>
<li>向量是否做了归一化</li>
<li>模型推荐的是 cosine、dot product 还是 L2</li>
<li>索引方式是否和模型假设一致</li>
</ul>
<h3 id="21-4-再看召回数量"><a href="#21-4-再看召回数量" class="headerlink" title="21.4 再看召回数量"></a>21.4 再看召回数量</h3><ul>
<li><code>top_k</code> 太小可能漏召回</li>
<li><code>top_k</code> 太大又可能引入太多噪声</li>
</ul>
<h3 id="21-5-最后再考虑-rerank-和模型替换"><a href="#21-5-最后再考虑-rerank-和模型替换" class="headerlink" title="21.5 最后再考虑 rerank 和模型替换"></a>21.5 最后再考虑 rerank 和模型替换</h3><p>先把 chunking、清洗、召回这些基础问题处理好，再换模型，效率会更高。</p>
<hr>
<h2 id="22-参考博客-文档"><a href="#22-参考博客-文档" class="headerlink" title="22. 参考博客 &#x2F; 文档"></a>22. 参考博客 &#x2F; 文档</h2><p>适合初学者先看的几篇：</p>
<ul>
<li>OpenAI Embeddings Guide: <a href="https://platform.openai.com/docs/guides/embeddings">https://platform.openai.com/docs/guides/embeddings</a></li>
<li>OpenAI Key Concepts - Embeddings: <a href="https://platform.openai.com/docs/concepts#embeddings">https://platform.openai.com/docs/concepts#embeddings</a></li>
<li>Pinecone - What Are Vector Embeddings?: <a href="https://www.pinecone.io/learn/vector-embeddings/">https://www.pinecone.io/learn/vector-embeddings/</a></li>
<li>Pinecone - Vector Embeddings for Developers: <a href="https://www.pinecone.io/learn/vector-embeddings-for-developers/">https://www.pinecone.io/learn/vector-embeddings-for-developers/</a></li>
<li>Sentence Transformers Documentation: <a href="https://www.sbert.net/">https://www.sbert.net/</a></li>
<li>FAISS Documentation: <a href="https://faiss.ai/">https://faiss.ai/</a></li>
</ul>
<p>继续深入可以看：</p>
<ul>
<li>pgvector: <a href="https://github.com/pgvector/pgvector">https://github.com/pgvector/pgvector</a></li>
<li>Cohere - Reranking Best Practices: <a href="https://docs.cohere.com/docs/reranking-best-practices">https://docs.cohere.com/docs/reranking-best-practices</a></li>
</ul>
<hr>
<h2 id="23-一句话总结"><a href="#23-一句话总结" class="headerlink" title="23. 一句话总结"></a>23. 一句话总结</h2><p>Embedding 不是单独存在的技术点，而是语义检索、向量数据库、rerank 和 RAG 整条链路中的基础环节。<br>真正做项目时，要把它放进“切块 -&gt; 向量化 -&gt; 检索 -&gt; 重排 -&gt; 生成”的完整流程里理解。</p>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>框架</category>
      </categories>
  </entry>
  <entry>
    <title>MySQL 学习笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/mysql/</url>
    <content><![CDATA[<h1 id="MySQL-学习笔记"><a href="#MySQL-学习笔记" class="headerlink" title="MySQL 学习笔记"></a>MySQL 学习笔记</h1><h2 id="1-MySQL-是什么"><a href="#1-MySQL-是什么" class="headerlink" title="1. MySQL 是什么"></a>1. MySQL 是什么</h2><p>MySQL 是一个关系型数据库，用来存储：</p>
<ul>
<li>用户信息</li>
<li>订单数据</li>
<li>文档元数据</li>
<li>任务记录</li>
<li>聊天记录</li>
<li>各种需要长期保存的结构化数据</li>
</ul>
<p>先抓住它和 Redis 的分工差异：</p>
<blockquote>
<p>MySQL 更适合存“长期、结构化、可查询的数据”，Redis 更适合存“高频、临时、快速访问的数据”。</p>
</blockquote>
<h2 id="2-为什么项目里经常会有-MySQL"><a href="#2-为什么项目里经常会有-MySQL" class="headerlink" title="2. 为什么项目里经常会有 MySQL"></a>2. 为什么项目里经常会有 MySQL</h2><p>因为很多业务数据都需要：</p>
<ul>
<li>长期保存</li>
<li>有明确字段结构</li>
<li>支持条件查询</li>
<li>支持关联查询</li>
<li>保证数据一致性</li>
</ul>
<p>例如：</p>
<ul>
<li>一个用户有哪些会话</li>
<li>一个文档属于哪个用户</li>
<li>一条任务记录当前是什么状态</li>
<li>某段时间创建了多少条消息</li>
</ul>
<p>这些都很适合用 MySQL 来存。</p>
<h2 id="3-在-AI-项目里-MySQL-常见用法"><a href="#3-在-AI-项目里-MySQL-常见用法" class="headerlink" title="3. 在 AI 项目里 MySQL 常见用法"></a>3. 在 AI 项目里 MySQL 常见用法</h2><p>在 AI 模型开发场景里，MySQL 常见用途有：</p>
<ul>
<li>存用户表</li>
<li>存会话表</li>
<li>存消息表</li>
<li>存文档表</li>
<li>存任务记录表</li>
<li>存模型调用日志</li>
<li>存权限和配置</li>
</ul>
<p>放回整套系统里看，各组件的分工大致是：</p>
<ul>
<li>FastAPI &#x2F; Drogon：接请求</li>
<li>Redis：做缓存、状态层、中间层</li>
<li>Celery：处理后台任务</li>
<li>MySQL：存长期结构化数据</li>
</ul>
<h2 id="4-MySQL-和-Redis-的区别"><a href="#4-MySQL-和-Redis-的区别" class="headerlink" title="4. MySQL 和 Redis 的区别"></a>4. MySQL 和 Redis 的区别</h2><table>
<thead>
<tr>
<th>维度</th>
<th>MySQL</th>
<th>Redis</th>
</tr>
</thead>
<tbody><tr>
<td>数据模型</td>
<td>关系型表结构</td>
<td>键值型</td>
</tr>
<tr>
<td>持久化</td>
<td>强，适合长期存储</td>
<td>可持久化，但常用于短期数据</td>
</tr>
<tr>
<td>查询能力</td>
<td>强，支持 SQL</td>
<td>主要按 key 操作</td>
</tr>
<tr>
<td>适合场景</td>
<td>用户、订单、文档、消息</td>
<td>缓存、计数器、会话、队列</td>
</tr>
<tr>
<td>事务</td>
<td>支持</td>
<td>支持有限的原子操作，不是传统关系事务</td>
</tr>
</tbody></table>
<p>简单理解：</p>
<ul>
<li>用户资料、文档记录、消息历史更适合 MySQL</li>
<li>缓存结果、限流计数、短期任务状态更适合 Redis</li>
</ul>
<h2 id="5-安装和启动"><a href="#5-安装和启动" class="headerlink" title="5. 安装和启动"></a>5. 安装和启动</h2><p>如果本地已经装好了 MySQL，可以直接启动服务。</p>
<p>如果只是临时学习，Docker 会更省事：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d \</span><br><span class="line">  --name mysql8 \</span><br><span class="line">  -p 3306:3306 \</span><br><span class="line">  -e MYSQL_ROOT_PASSWORD=123456 \</span><br><span class="line">  -e MYSQL_DATABASE=demo \</span><br><span class="line">  mysql:8</span><br></pre></td></tr></table></figure>

<p>进入 MySQL：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql -h 127.0.0.1 -P 3306 -u root -p</span><br></pre></td></tr></table></figure>

<p>查看数据库：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> DATABASES;</span><br></pre></td></tr></table></figure>

<p>选择数据库：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">USE demo;</span><br></pre></td></tr></table></figure>

<h2 id="6-Python-连接-MySQL"><a href="#6-Python-连接-MySQL" class="headerlink" title="6. Python 连接 MySQL"></a>6. Python 连接 MySQL</h2><p>Python 项目里很常见的是 <code>pymysql</code>。</p>
<p>安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install pymysql</span><br></pre></td></tr></table></figure>

<p>最基础的连接方式：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pymysql</span><br><span class="line"></span><br><span class="line">conn = pymysql.connect(</span><br><span class="line">    host=<span class="string">&quot;127.0.0.1&quot;</span>,</span><br><span class="line">    port=<span class="number">3306</span>,</span><br><span class="line">    user=<span class="string">&quot;root&quot;</span>,</span><br><span class="line">    password=<span class="string">&quot;123456&quot;</span>,</span><br><span class="line">    database=<span class="string">&quot;demo&quot;</span>,</span><br><span class="line">    charset=<span class="string">&quot;utf8mb4&quot;</span>,</span><br><span class="line">    autocommit=<span class="literal">False</span>,</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>几个常见参数：</p>
<ul>
<li><code>host</code>：数据库地址</li>
<li><code>port</code>：端口，MySQL 默认通常是 <code>3306</code></li>
<li><code>user</code> &#x2F; <code>password</code>：用户名密码</li>
<li><code>database</code>：要连接的库</li>
<li><code>charset=&quot;utf8mb4&quot;</code>：推荐写上，避免中文和 emoji 编码问题</li>
<li><code>autocommit=False</code>：默认手动提交事务</li>
</ul>
<h2 id="7-连接和游标-cursor"><a href="#7-连接和游标-cursor" class="headerlink" title="7. 连接和游标 cursor"></a>7. 连接和游标 <code>cursor</code></h2><p>你原来笔记里提到“游标对象的具体作用是什么”，可以这样理解：</p>
<blockquote>
<p>连接 <code>conn</code> 负责连数据库，游标 <code>cursor</code> 负责执行 SQL 和取结果。</p>
</blockquote>
<p>最常见写法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">cursor = conn.cursor()</span><br></pre></td></tr></table></figure>

<p>更推荐：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">with</span> conn.cursor() <span class="keyword">as</span> cursor:</span><br><span class="line">    cursor.execute(<span class="string">&quot;SELECT 1&quot;</span>)</span><br><span class="line">    result = cursor.fetchone()</span><br></pre></td></tr></table></figure>

<p><code>cursor</code> 主要负责：</p>
<ul>
<li>执行 SQL</li>
<li>传递参数</li>
<li>获取查询结果</li>
<li>提供 <code>lastrowid</code>、<code>rowcount</code> 等信息</li>
</ul>
<p>常见方法：</p>
<ul>
<li><code>cursor.execute()</code>：执行一条 SQL</li>
<li><code>cursor.executemany()</code>：批量执行</li>
<li><code>cursor.fetchone()</code>：取一条</li>
<li><code>cursor.fetchmany(n)</code>：取前 <code>n</code> 条</li>
<li><code>cursor.fetchall()</code>：取全部</li>
</ul>
<p>如果你希望查询结果按字典返回，而不是元组，可以这样写：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pymysql.cursors</span><br><span class="line"></span><br><span class="line">conn = pymysql.connect(</span><br><span class="line">    host=<span class="string">&quot;127.0.0.1&quot;</span>,</span><br><span class="line">    user=<span class="string">&quot;root&quot;</span>,</span><br><span class="line">    password=<span class="string">&quot;123456&quot;</span>,</span><br><span class="line">    database=<span class="string">&quot;demo&quot;</span>,</span><br><span class="line">    charset=<span class="string">&quot;utf8mb4&quot;</span>,</span><br><span class="line">    cursorclass=pymysql.cursors.DictCursor,</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>这样查出来的数据会更接近：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;<span class="string">&quot;id&quot;</span>: <span class="number">1</span>, <span class="string">&quot;name&quot;</span>: <span class="string">&quot;tom&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<h2 id="8-建表和常见字段类型"><a href="#8-建表和常见字段类型" class="headerlink" title="8. 建表和常见字段类型"></a>8. 建表和常见字段类型</h2><p>MySQL 最基础的 SQL 之一就是建表。</p>
<p>示例：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> users (</span><br><span class="line">    id <span class="type">BIGINT</span> <span class="keyword">PRIMARY</span> KEY AUTO_INCREMENT,</span><br><span class="line">    username <span class="type">VARCHAR</span>(<span class="number">100</span>) <span class="keyword">NOT</span> <span class="keyword">NULL</span> <span class="keyword">UNIQUE</span>,</span><br><span class="line">    email <span class="type">VARCHAR</span>(<span class="number">200</span>) <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span>,</span><br><span class="line">    age <span class="type">INT</span> <span class="keyword">DEFAULT</span> <span class="number">0</span>,</span><br><span class="line">    created_at DATETIME <span class="keyword">NOT</span> <span class="keyword">NULL</span> <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>常见字段类型：</p>
<ul>
<li><code>INT</code>：整数</li>
<li><code>BIGINT</code>：更大的整数</li>
<li><code>VARCHAR(n)</code>：变长字符串</li>
<li><code>TEXT</code>：长文本</li>
<li><code>DATETIME</code>：日期时间</li>
<li><code>DATE</code>：日期</li>
<li><code>DECIMAL(10,2)</code>：精确小数</li>
<li><code>BOOLEAN</code>：布尔，底层通常可看成 <code>TINYINT</code></li>
</ul>
<p>常见约束：</p>
<ul>
<li><code>PRIMARY KEY</code>：主键</li>
<li><code>AUTO_INCREMENT</code>：自增</li>
<li><code>NOT NULL</code>：不能为空</li>
<li><code>UNIQUE</code>：唯一</li>
<li><code>DEFAULT</code>：默认值</li>
<li><code>FOREIGN KEY</code>：外键</li>
</ul>
<h2 id="9-表结构相关-SQL"><a href="#9-表结构相关-SQL" class="headerlink" title="9. 表结构相关 SQL"></a>9. 表结构相关 SQL</h2><p>这部分在实际开发里很常用。</p>
<h3 id="9-1-创建表"><a href="#9-1-创建表" class="headerlink" title="9.1 创建表"></a>9.1 创建表</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> users (</span><br><span class="line">    id <span class="type">BIGINT</span> <span class="keyword">PRIMARY</span> KEY AUTO_INCREMENT,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">100</span>) <span class="keyword">NOT</span> <span class="keyword">NULL</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h3 id="9-2-如果表已存在就跳过"><a href="#9-2-如果表已存在就跳过" class="headerlink" title="9.2 如果表已存在就跳过"></a>9.2 如果表已存在就跳过</h3><p>你前面提到的“表已存在怎么办”，对应的就是这个场景。</p>
<p>如果担心“已经有同名表，再创建会报错”，可以写：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> users (</span><br><span class="line">    id <span class="type">BIGINT</span> <span class="keyword">PRIMARY</span> KEY AUTO_INCREMENT,</span><br><span class="line">    name <span class="type">VARCHAR</span>(<span class="number">100</span>) <span class="keyword">NOT</span> <span class="keyword">NULL</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>这样当表已存在时，会直接跳过，不会因为同名表报错。</p>
<h3 id="9-3-修改表"><a href="#9-3-修改表" class="headerlink" title="9.3 修改表"></a>9.3 修改表</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> users <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> email <span class="type">VARCHAR</span>(<span class="number">200</span>);</span><br></pre></td></tr></table></figure>

<h3 id="9-4-删除表"><a href="#9-4-删除表" class="headerlink" title="9.4 删除表"></a>9.4 删除表</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> users;</span><br></pre></td></tr></table></figure>

<p>如果担心表不存在报错：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> users;</span><br></pre></td></tr></table></figure>

<h3 id="9-5-查看建表语句"><a href="#9-5-查看建表语句" class="headerlink" title="9.5 查看建表语句"></a>9.5 查看建表语句</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SHOW</span> <span class="keyword">CREATE</span> <span class="keyword">TABLE</span> users;</span><br></pre></td></tr></table></figure>

<p>这个命令非常实用，因为它能直接看到当前表的真实结构、索引、字符集等信息。</p>
<h2 id="10-插入数据-INSERT"><a href="#10-插入数据-INSERT" class="headerlink" title="10. 插入数据 INSERT"></a>10. 插入数据 <code>INSERT</code></h2><p>最基础的插入写法：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">INSERT</span> <span class="keyword">INTO</span> users (username, email, age)</span><br><span class="line"><span class="keyword">VALUES</span> (<span class="string">&#x27;tom&#x27;</span>, <span class="string">&#x27;tom@example.com&#x27;</span>, <span class="number">18</span>);</span><br></pre></td></tr></table></figure>

<p>在 Python 里更推荐参数化写法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">with</span> conn.cursor() <span class="keyword">as</span> cursor:</span><br><span class="line">    sql = <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    INSERT INTO users (username, email, age)</span></span><br><span class="line"><span class="string">    VALUES (%s, %s, %s)</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    cursor.execute(sql, (<span class="string">&quot;tom&quot;</span>, <span class="string">&quot;tom@example.com&quot;</span>, <span class="number">18</span>))</span><br><span class="line">    conn.commit()</span><br></pre></td></tr></table></figure>

<p>插入后拿自增主键：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">user_id = cursor.lastrowid</span><br></pre></td></tr></table></figure>

<h2 id="11-查询数据-SELECT"><a href="#11-查询数据-SELECT" class="headerlink" title="11. 查询数据 SELECT"></a>11. 查询数据 <code>SELECT</code></h2><h3 id="11-1-查全部字段"><a href="#11-1-查全部字段" class="headerlink" title="11.1 查全部字段"></a>11.1 查全部字段</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users;</span><br></pre></td></tr></table></figure>

<h3 id="11-2-只查部分字段"><a href="#11-2-只查部分字段" class="headerlink" title="11.2 只查部分字段"></a>11.2 只查部分字段</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, username <span class="keyword">FROM</span> users;</span><br></pre></td></tr></table></figure>

<h3 id="11-3-带条件查询"><a href="#11-3-带条件查询" class="headerlink" title="11.3 带条件查询"></a>11.3 带条件查询</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, username</span><br><span class="line"><span class="keyword">FROM</span> users</span><br><span class="line"><span class="keyword">WHERE</span> age <span class="operator">&gt;=</span> <span class="number">18</span>;</span><br></pre></td></tr></table></figure>

<h3 id="11-4-排序"><a href="#11-4-排序" class="headerlink" title="11.4 排序"></a>11.4 排序</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, username</span><br><span class="line"><span class="keyword">FROM</span> users</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> created_at <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure>

<h3 id="11-5-分页"><a href="#11-5-分页" class="headerlink" title="11.5 分页"></a>11.5 分页</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> id, username</span><br><span class="line"><span class="keyword">FROM</span> users</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> id <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">10</span> <span class="keyword">OFFSET</span> <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p>也常写成：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">LIMIT <span class="number">0</span>, <span class="number">10</span>;</span><br></pre></td></tr></table></figure>

<h2 id="12-更新数据-UPDATE"><a href="#12-更新数据-UPDATE" class="headerlink" title="12. 更新数据 UPDATE"></a>12. 更新数据 <code>UPDATE</code></h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> users</span><br><span class="line"><span class="keyword">SET</span> email <span class="operator">=</span> <span class="string">&#x27;new@example.com&#x27;</span></span><br><span class="line"><span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p>一定要注意：</p>
<blockquote>
<p><code>UPDATE</code> 不写 <code>WHERE</code>，会更新整张表。</p>
</blockquote>
<p>Python 示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">with</span> conn.cursor() <span class="keyword">as</span> cursor:</span><br><span class="line">    sql = <span class="string">&quot;UPDATE users SET email = %s WHERE id = %s&quot;</span></span><br><span class="line">    affected_rows = cursor.execute(sql, (<span class="string">&quot;new@example.com&quot;</span>, <span class="number">1</span>))</span><br><span class="line">    conn.commit()</span><br><span class="line">    <span class="built_in">print</span>(affected_rows)</span><br></pre></td></tr></table></figure>

<h2 id="13-删除数据-DELETE"><a href="#13-删除数据-DELETE" class="headerlink" title="13. 删除数据 DELETE"></a>13. 删除数据 <code>DELETE</code></h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> users</span><br><span class="line"><span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<p>同样要注意：</p>
<blockquote>
<p><code>DELETE</code> 不写 <code>WHERE</code>，会删整张表的数据。</p>
</blockquote>
<p>如果你是想删整张表但保留结构，也可以：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">TRUNCATE</span> <span class="keyword">TABLE</span> users;</span><br></pre></td></tr></table></figure>

<h2 id="14-条件查询、排序、分组"><a href="#14-条件查询、排序、分组" class="headerlink" title="14. 条件查询、排序、分组"></a>14. 条件查询、排序、分组</h2><p>这些是 SQL 里非常高频的能力。</p>
<h3 id="14-1-WHERE"><a href="#14-1-WHERE" class="headerlink" title="14.1 WHERE"></a>14.1 <code>WHERE</code></h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users <span class="keyword">WHERE</span> age <span class="operator">&gt;=</span> <span class="number">18</span>;</span><br></pre></td></tr></table></figure>

<h3 id="14-2-ORDER-BY"><a href="#14-2-ORDER-BY" class="headerlink" title="14.2 ORDER BY"></a>14.2 <code>ORDER BY</code></h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users <span class="keyword">ORDER</span> <span class="keyword">BY</span> id <span class="keyword">DESC</span>;</span><br></pre></td></tr></table></figure>

<h3 id="14-3-LIMIT"><a href="#14-3-LIMIT" class="headerlink" title="14.3 LIMIT"></a>14.3 <code>LIMIT</code></h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure>

<h3 id="14-4-GROUP-BY"><a href="#14-4-GROUP-BY" class="headerlink" title="14.4 GROUP BY"></a>14.4 <code>GROUP BY</code></h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> status, <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> total</span><br><span class="line"><span class="keyword">FROM</span> tasks</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> status;</span><br></pre></td></tr></table></figure>

<h3 id="14-5-HAVING"><a href="#14-5-HAVING" class="headerlink" title="14.5 HAVING"></a>14.5 <code>HAVING</code></h3><p><code>HAVING</code> 通常配合分组结果使用：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> status, <span class="built_in">COUNT</span>(<span class="operator">*</span>) <span class="keyword">AS</span> total</span><br><span class="line"><span class="keyword">FROM</span> tasks</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> status</span><br><span class="line"><span class="keyword">HAVING</span> total <span class="operator">&gt;</span> <span class="number">10</span>;</span><br></pre></td></tr></table></figure>

<h2 id="15-JOIN-关联查询"><a href="#15-JOIN-关联查询" class="headerlink" title="15. JOIN 关联查询"></a>15. JOIN 关联查询</h2><p>这是关系型数据库最重要的优势之一。</p>
<p>假设：</p>
<ul>
<li><code>users</code> 表存用户</li>
<li><code>documents</code> 表存文档</li>
</ul>
<p>查询“每篇文档属于哪个用户”：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> d.id, d.title, u.username</span><br><span class="line"><span class="keyword">FROM</span> documents d</span><br><span class="line"><span class="keyword">JOIN</span> users u <span class="keyword">ON</span> d.user_id <span class="operator">=</span> u.id;</span><br></pre></td></tr></table></figure>

<p>常见 JOIN：</p>
<ul>
<li><code>JOIN</code> &#x2F; <code>INNER JOIN</code>：两边都能匹配到才返回</li>
<li><code>LEFT JOIN</code>：左表全部保留，右表匹配不到则为 <code>NULL</code></li>
</ul>
<p>例如：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> u.id, u.username, d.title</span><br><span class="line"><span class="keyword">FROM</span> users u</span><br><span class="line"><span class="keyword">LEFT</span> <span class="keyword">JOIN</span> documents d <span class="keyword">ON</span> u.id <span class="operator">=</span> d.user_id;</span><br></pre></td></tr></table></figure>

<h2 id="16-索引-INDEX"><a href="#16-索引-INDEX" class="headerlink" title="16. 索引 INDEX"></a>16. 索引 <code>INDEX</code></h2><p>索引的作用可以粗暴理解成：</p>
<blockquote>
<p>让查询更快，但会增加写入成本和占用空间。</p>
</blockquote>
<p>例如给用户名加索引：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> INDEX idx_users_username <span class="keyword">ON</span> users(username);</span><br></pre></td></tr></table></figure>

<p>适合加索引的字段通常有：</p>
<ul>
<li>经常出现在 <code>WHERE</code> 里的字段</li>
<li>经常用于排序的字段</li>
<li>经常用于 JOIN 的字段</li>
<li>唯一约束字段</li>
</ul>
<p>常见误区：</p>
<ul>
<li>不是索引越多越好</li>
<li>很小的表不一定需要索引</li>
<li>更新频繁的字段加太多索引会拖慢写入</li>
</ul>
<h2 id="17-外键-FOREIGN-KEY"><a href="#17-外键-FOREIGN-KEY" class="headerlink" title="17. 外键 FOREIGN KEY"></a>17. 外键 <code>FOREIGN KEY</code></h2><p>外键用于表达表和表之间的引用关系。</p>
<p>例如：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> documents (</span><br><span class="line">    id <span class="type">BIGINT</span> <span class="keyword">PRIMARY</span> KEY AUTO_INCREMENT,</span><br><span class="line">    user_id <span class="type">BIGINT</span> <span class="keyword">NOT</span> <span class="keyword">NULL</span>,</span><br><span class="line">    title <span class="type">VARCHAR</span>(<span class="number">200</span>) <span class="keyword">NOT</span> <span class="keyword">NULL</span>,</span><br><span class="line">    <span class="keyword">CONSTRAINT</span> fk_documents_user</span><br><span class="line">        <span class="keyword">FOREIGN</span> KEY (user_id) <span class="keyword">REFERENCES</span> users(id)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>它的作用是：</p>
<ul>
<li>保证引用关系合法</li>
<li>避免出现“文档指向一个不存在用户”的情况</li>
</ul>
<p>但很多实际项目里，也会选择在业务层保证关系，而不是强依赖数据库外键。</p>
<h2 id="18-事务-commit-和-rollback"><a href="#18-事务-commit-和-rollback" class="headerlink" title="18. 事务 commit 和 rollback"></a>18. 事务 <code>commit</code> 和 <code>rollback</code></h2><p>你原来的笔记里提到“事务提交”，这部分非常重要。</p>
<p>事务适合用于：</p>
<ul>
<li>多条 SQL 必须一起成功</li>
<li>中间某一步失败时要整体撤回</li>
</ul>
<p>示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    <span class="keyword">with</span> conn.cursor() <span class="keyword">as</span> cursor:</span><br><span class="line">        cursor.execute(</span><br><span class="line">            <span class="string">&quot;INSERT INTO accounts (name, balance) VALUES (%s, %s)&quot;</span>,</span><br><span class="line">            (<span class="string">&quot;alice&quot;</span>, <span class="number">1000</span>),</span><br><span class="line">        )</span><br><span class="line">        cursor.execute(</span><br><span class="line">            <span class="string">&quot;INSERT INTO accounts (name, balance) VALUES (%s, %s)&quot;</span>,</span><br><span class="line">            (<span class="string">&quot;bob&quot;</span>, <span class="number">500</span>),</span><br><span class="line">        )</span><br><span class="line">    conn.commit()</span><br><span class="line"><span class="keyword">except</span> Exception:</span><br><span class="line">    conn.rollback()</span><br><span class="line">    <span class="keyword">raise</span></span><br></pre></td></tr></table></figure>

<p>常见规则：</p>
<ul>
<li>成功后 <code>conn.commit()</code></li>
<li>失败时 <code>conn.rollback()</code></li>
</ul>
<p>如果连接时配置：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">autocommit=<span class="literal">True</span></span><br></pre></td></tr></table></figure>

<p>就表示每条 SQL 默认自动提交。</p>
<p>学习阶段可以用，但项目里要清楚：</p>
<blockquote>
<p>一旦涉及多步写操作，通常还是要认真控制事务边界。</p>
</blockquote>
<h2 id="19-查询结果获取"><a href="#19-查询结果获取" class="headerlink" title="19. 查询结果获取"></a>19. 查询结果获取</h2><p>这是你原来笔记里已经提到的内容，这里整理成更清晰的版本。</p>
<h3 id="19-1-fetchone"><a href="#19-1-fetchone" class="headerlink" title="19.1 fetchone()"></a>19.1 <code>fetchone()</code></h3><p>只取一条：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">row = cursor.fetchone()</span><br></pre></td></tr></table></figure>

<h3 id="19-2-fetchmany-n"><a href="#19-2-fetchmany-n" class="headerlink" title="19.2 fetchmany(n)"></a>19.2 <code>fetchmany(n)</code></h3><p>取前 <code>n</code> 条：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">rows = cursor.fetchmany(<span class="number">10</span>)</span><br></pre></td></tr></table></figure>

<h3 id="19-3-fetchall"><a href="#19-3-fetchall" class="headerlink" title="19.3 fetchall()"></a>19.3 <code>fetchall()</code></h3><p>取全部：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">rows = cursor.fetchall()</span><br></pre></td></tr></table></figure>

<h3 id="19-4-lastrowid"><a href="#19-4-lastrowid" class="headerlink" title="19.4 lastrowid"></a>19.4 <code>lastrowid</code></h3><p>拿最后插入的自增 ID：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">new_id = cursor.lastrowid</span><br></pre></td></tr></table></figure>

<h3 id="19-5-rowcount"><a href="#19-5-rowcount" class="headerlink" title="19.5 rowcount"></a>19.5 <code>rowcount</code></h3><p>看本次 SQL 影响了多少行：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">count = cursor.rowcount</span><br></pre></td></tr></table></figure>

<h2 id="20-execute-和-executemany"><a href="#20-execute-和-executemany" class="headerlink" title="20. execute() 和 executemany()"></a>20. <code>execute()</code> 和 <code>executemany()</code></h2><h3 id="20-1-execute"><a href="#20-1-execute" class="headerlink" title="20.1 execute()"></a>20.1 <code>execute()</code></h3><p>执行一条 SQL：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">cursor.execute(</span><br><span class="line">    <span class="string">&quot;INSERT INTO users (username, age) VALUES (%s, %s)&quot;</span>,</span><br><span class="line">    (<span class="string">&quot;tom&quot;</span>, <span class="number">18</span>),</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<h3 id="20-2-executemany"><a href="#20-2-executemany" class="headerlink" title="20.2 executemany()"></a>20.2 <code>executemany()</code></h3><p>批量执行：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">data = [</span><br><span class="line">    (<span class="string">&quot;tom&quot;</span>, <span class="number">18</span>),</span><br><span class="line">    (<span class="string">&quot;alice&quot;</span>, <span class="number">20</span>),</span><br><span class="line">    (<span class="string">&quot;bob&quot;</span>, <span class="number">22</span>),</span><br><span class="line">]</span><br><span class="line"></span><br><span class="line">cursor.executemany(</span><br><span class="line">    <span class="string">&quot;INSERT INTO users (username, age) VALUES (%s, %s)&quot;</span>,</span><br><span class="line">    data,</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>适合：</p>
<ul>
<li>批量插入</li>
<li>批量更新同类数据</li>
</ul>
<h2 id="21-一段比较完整的-PyMySQL-示例"><a href="#21-一段比较完整的-PyMySQL-示例" class="headerlink" title="21. 一段比较完整的 PyMySQL 示例"></a>21. 一段比较完整的 PyMySQL 示例</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pymysql</span><br><span class="line"><span class="keyword">import</span> pymysql.cursors</span><br><span class="line"></span><br><span class="line">conn = pymysql.connect(</span><br><span class="line">    host=<span class="string">&quot;127.0.0.1&quot;</span>,</span><br><span class="line">    port=<span class="number">3306</span>,</span><br><span class="line">    user=<span class="string">&quot;root&quot;</span>,</span><br><span class="line">    password=<span class="string">&quot;123456&quot;</span>,</span><br><span class="line">    database=<span class="string">&quot;demo&quot;</span>,</span><br><span class="line">    charset=<span class="string">&quot;utf8mb4&quot;</span>,</span><br><span class="line">    cursorclass=pymysql.cursors.DictCursor,</span><br><span class="line">    autocommit=<span class="literal">False</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    <span class="keyword">with</span> conn.cursor() <span class="keyword">as</span> cursor:</span><br><span class="line">        cursor.execute(</span><br><span class="line">            <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">            CREATE TABLE IF NOT EXISTS users (</span></span><br><span class="line"><span class="string">                id BIGINT PRIMARY KEY AUTO_INCREMENT,</span></span><br><span class="line"><span class="string">                username VARCHAR(100) NOT NULL UNIQUE,</span></span><br><span class="line"><span class="string">                age INT NOT NULL DEFAULT 0,</span></span><br><span class="line"><span class="string">                created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP</span></span><br><span class="line"><span class="string">            )</span></span><br><span class="line"><span class="string">            &quot;&quot;&quot;</span></span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line">        cursor.execute(</span><br><span class="line">            <span class="string">&quot;INSERT INTO users (username, age) VALUES (%s, %s)&quot;</span>,</span><br><span class="line">            (<span class="string">&quot;tom&quot;</span>, <span class="number">18</span>),</span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line">        new_id = cursor.lastrowid</span><br><span class="line"></span><br><span class="line">        cursor.execute(</span><br><span class="line">            <span class="string">&quot;SELECT id, username, age FROM users WHERE id = %s&quot;</span>,</span><br><span class="line">            (new_id,),</span><br><span class="line">        )</span><br><span class="line">        row = cursor.fetchone()</span><br><span class="line">        <span class="built_in">print</span>(row)</span><br><span class="line"></span><br><span class="line">    conn.commit()</span><br><span class="line"><span class="keyword">except</span> Exception:</span><br><span class="line">    conn.rollback()</span><br><span class="line">    <span class="keyword">raise</span></span><br><span class="line"><span class="keyword">finally</span>:</span><br><span class="line">    conn.close()</span><br></pre></td></tr></table></figure>

<h2 id="22-FastAPI-里怎么用-MySQL"><a href="#22-FastAPI-里怎么用-MySQL" class="headerlink" title="22. FastAPI 里怎么用 MySQL"></a>22. FastAPI 里怎么用 MySQL</h2><p>在 FastAPI 项目里，MySQL 常见用途有：</p>
<ul>
<li>存用户</li>
<li>存任务记录</li>
<li>存会话和消息</li>
<li>存文档元数据</li>
</ul>
<p>最简单的同步示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pymysql</span><br><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_conn</span>():</span><br><span class="line">    <span class="keyword">return</span> pymysql.connect(</span><br><span class="line">        host=<span class="string">&quot;127.0.0.1&quot;</span>,</span><br><span class="line">        user=<span class="string">&quot;root&quot;</span>,</span><br><span class="line">        password=<span class="string">&quot;123456&quot;</span>,</span><br><span class="line">        database=<span class="string">&quot;demo&quot;</span>,</span><br><span class="line">        charset=<span class="string">&quot;utf8mb4&quot;</span>,</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/users/&#123;user_id&#125;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_user</span>(<span class="params">user_id: <span class="built_in">int</span></span>):</span><br><span class="line">    conn = get_conn()</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">with</span> conn.cursor() <span class="keyword">as</span> cursor:</span><br><span class="line">            cursor.execute(</span><br><span class="line">                <span class="string">&quot;SELECT id, username FROM users WHERE id = %s&quot;</span>,</span><br><span class="line">                (user_id,),</span><br><span class="line">            )</span><br><span class="line">            row = cursor.fetchone()</span><br><span class="line">        <span class="keyword">return</span> &#123;<span class="string">&quot;data&quot;</span>: row&#125;</span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        conn.close()</span><br></pre></td></tr></table></figure>

<p>不过实际项目里通常会再往前走一步：</p>
<ul>
<li>使用连接池</li>
<li>抽 Repository &#x2F; Service 层</li>
<li>使用 SQLAlchemy 等更完整的数据库层</li>
</ul>
<h2 id="23-AI-项目里比较常见的表设计"><a href="#23-AI-项目里比较常见的表设计" class="headerlink" title="23. AI 项目里比较常见的表设计"></a>23. AI 项目里比较常见的表设计</h2><p>如果你在做文档问答、RAG、聊天系统，常见会有这些表：</p>
<ul>
<li><code>users</code></li>
<li><code>sessions</code></li>
<li><code>messages</code></li>
<li><code>documents</code></li>
<li><code>document_chunks</code></li>
<li><code>tasks</code></li>
</ul>
<p>例如：</p>
<ul>
<li><code>documents</code>：存文档标题、路径、状态、所属用户</li>
<li><code>tasks</code>：存异步任务 ID、状态、错误信息</li>
<li><code>messages</code>：存对话消息和角色</li>
</ul>
<p>也就是说：</p>
<blockquote>
<p>MySQL 更适合存“事实记录”和“长期业务数据”，不适合拿来做高频缓存层。</p>
</blockquote>
<h2 id="24-常见坑"><a href="#24-常见坑" class="headerlink" title="24. 常见坑"></a>24. 常见坑</h2><h3 id="24-1-用字符串拼-SQL"><a href="#24-1-用字符串拼-SQL" class="headerlink" title="24.1 用字符串拼 SQL"></a>24.1 用字符串拼 SQL</h3><p>错误示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">sql = <span class="string">f&quot;SELECT * FROM users WHERE username = &#x27;<span class="subst">&#123;username&#125;</span>&#x27;&quot;</span></span><br></pre></td></tr></table></figure>

<p>这会有 SQL 注入风险。</p>
<p>正确做法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">cursor.execute(</span><br><span class="line">    <span class="string">&quot;SELECT * FROM users WHERE username = %s&quot;</span>,</span><br><span class="line">    (username,),</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<h3 id="24-2-忘记提交事务"><a href="#24-2-忘记提交事务" class="headerlink" title="24.2 忘记提交事务"></a>24.2 忘记提交事务</h3><p>执行了 <code>INSERT</code> &#x2F; <code>UPDATE</code> &#x2F; <code>DELETE</code> 之后，如果没有 <code>commit()</code>，数据可能不会真正写入。</p>
<h3 id="24-3-UPDATE-DELETE-不带-WHERE"><a href="#24-3-UPDATE-DELETE-不带-WHERE" class="headerlink" title="24.3 UPDATE &#x2F; DELETE 不带 WHERE"></a>24.3 <code>UPDATE</code> &#x2F; <code>DELETE</code> 不带 <code>WHERE</code></h3><p>这会影响整张表。</p>
<h3 id="24-4-滥用-SELECT"><a href="#24-4-滥用-SELECT" class="headerlink" title="24.4 滥用 SELECT *"></a>24.4 滥用 <code>SELECT *</code></h3><p>开发初期问题不大，但项目里更推荐明确字段，避免：</p>
<ul>
<li>取了不需要的数据</li>
<li>表结构变动后影响接口</li>
<li>传输和解析成本增加</li>
</ul>
<h3 id="24-5-不建索引或乱建索引"><a href="#24-5-不建索引或乱建索引" class="headerlink" title="24.5 不建索引或乱建索引"></a>24.5 不建索引或乱建索引</h3><p>都不对。</p>
<p>应该根据查询模式来设计索引。</p>
<h3 id="24-6-字符集没统一"><a href="#24-6-字符集没统一" class="headerlink" title="24.6 字符集没统一"></a>24.6 字符集没统一</h3><p>推荐统一使用：</p>
<ul>
<li>数据库字符集：<code>utf8mb4</code></li>
<li>Python 连接：<code>charset=&quot;utf8mb4&quot;</code></li>
</ul>
<h3 id="24-7-把-MySQL-当缓存用"><a href="#24-7-把-MySQL-当缓存用" class="headerlink" title="24.7 把 MySQL 当缓存用"></a>24.7 把 MySQL 当缓存用</h3><p>这会让数据库承担不适合它的高频压力。</p>
<h2 id="25-一套比较实用的学习顺序"><a href="#25-一套比较实用的学习顺序" class="headerlink" title="25. 一套比较实用的学习顺序"></a>25. 一套比较实用的学习顺序</h2><p>建议这样学：</p>
<ol>
<li>先学会创建库、建表、插入、查询、更新、删除</li>
<li>学会 <code>WHERE</code>、<code>ORDER BY</code>、<code>LIMIT</code></li>
<li>学会 <code>GROUP BY</code> 和 <code>JOIN</code></li>
<li>学会索引和事务</li>
<li>学会用 PyMySQL 连接和执行参数化 SQL</li>
<li>最后再接入 FastAPI、Celery、Redis</li>
</ol>
<h2 id="26-总结"><a href="#26-总结" class="headerlink" title="26. 总结"></a>26. 总结</h2><p>MySQL 的核心价值在于：</p>
<ul>
<li>存长期结构化数据</li>
<li>支持 SQL 查询</li>
<li>支持关联查询</li>
<li>支持事务</li>
<li>适合承载业务主数据</li>
</ul>
<p>对于 AI 项目，可以把它理解成：</p>
<ul>
<li>用户数据存储层</li>
<li>会话和消息持久化层</li>
<li>文档元数据层</li>
<li>任务记录层</li>
</ul>
<p>如果把你前面几份笔记连起来看，可以把它们理解成一条链：</p>
<ul>
<li>FastAPI &#x2F; Drogon：接请求</li>
<li>Redis：缓存 &#x2F; 状态 &#x2F; 中间层</li>
<li>Celery：后台异步任务</li>
<li>MySQL：长期持久化数据</li>
</ul>
<h2 id="27-参考"><a href="#27-参考" class="headerlink" title="27. 参考"></a>27. 参考</h2><ul>
<li>MySQL Official Docs: <a href="https://dev.mysql.com/doc/">https://dev.mysql.com/doc/</a></li>
<li>PyMySQL Docs: <a href="https://pymysql.readthedocs.io/">https://pymysql.readthedocs.io/</a></li>
<li>SQL Syntax Reference: <a href="https://dev.mysql.com/doc/refman/8.0/en/sql-statements.html">https://dev.mysql.com/doc/refman/8.0/en/sql-statements.html</a></li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>框架</category>
      </categories>
  </entry>
  <entry>
    <title>Redis 学习笔记</title>
    <url>//notes/ai%E6%A8%A1%E5%9E%8B%E5%BC%80%E5%8F%91/%E7%9F%A5%E8%AF%86%E7%82%B9%E5%AD%A6%E4%B9%A0/%E6%A1%86%E6%9E%B6/redis/</url>
    <content><![CDATA[<h1 id="Redis-学习笔记"><a href="#Redis-学习笔记" class="headerlink" title="Redis 学习笔记"></a>Redis 学习笔记</h1><h2 id="1-Redis-是什么"><a href="#1-Redis-是什么" class="headerlink" title="1. Redis 是什么"></a>1. Redis 是什么</h2><p>Redis 是一个高性能的内存型键值数据库，常被用来做：</p>
<ul>
<li>缓存</li>
<li>会话存储</li>
<li>计数器</li>
<li>排行榜</li>
<li>消息队列</li>
<li>分布式锁</li>
<li>Celery 的 Broker &#x2F; Result Backend</li>
</ul>
<p>先把分工记住：MySQL 更适合放长期、结构化、需要稳定查询的数据，Redis 更适合放高频访问、生命周期短、对速度敏感的数据。</p>
<h2 id="2-为什么项目里经常会有-Redis"><a href="#2-为什么项目里经常会有-Redis" class="headerlink" title="2. 为什么项目里经常会有 Redis"></a>2. 为什么项目里经常会有 Redis</h2><p>很多系统里都会遇到这些问题：</p>
<ul>
<li>某些数据访问特别频繁</li>
<li>接口需要很快返回</li>
<li>需要临时状态，不想每次都查数据库</li>
<li>需要做限流、验证码、会话缓存</li>
<li>需要一个轻量消息通道</li>
</ul>
<p>这时候 Redis 很适合。</p>
<p>因为它的特点是：</p>
<ul>
<li>读写快</li>
<li>支持多种数据结构</li>
<li>支持过期时间</li>
<li>支持原子操作</li>
<li>既能做缓存，也能做简单消息系统</li>
</ul>
<h2 id="3-在-AI-项目里-Redis-常见用法"><a href="#3-在-AI-项目里-Redis-常见用法" class="headerlink" title="3. 在 AI 项目里 Redis 常见用法"></a>3. 在 AI 项目里 Redis 常见用法</h2><p>在 AI 模型开发场景里，Redis 很常见：</p>
<ul>
<li>缓存热点查询结果</li>
<li>缓存用户会话上下文</li>
<li>给 Celery 当 Broker</li>
<li>给 Celery 当任务结果存储</li>
<li>存任务状态、进度、短期中间结果</li>
<li>做限流</li>
<li>用 Streams 传递流式输出 token</li>
</ul>
<p>和你前面整理的两份笔记连起来理解：</p>
<ul>
<li>FastAPI：接请求</li>
<li>Redis：缓存 &#x2F; 排队 &#x2F; 状态层</li>
<li>Celery：异步处理后台任务</li>
</ul>
<h2 id="4-Redis-和-MySQL-的区别"><a href="#4-Redis-和-MySQL-的区别" class="headerlink" title="4. Redis 和 MySQL 的区别"></a>4. Redis 和 MySQL 的区别</h2><p>可以这样对比理解：</p>
<table>
<thead>
<tr>
<th>维度</th>
<th>Redis</th>
<th>MySQL</th>
</tr>
</thead>
<tbody><tr>
<td>数据类型</td>
<td>键值型，支持多种结构</td>
<td>关系型表结构</td>
</tr>
<tr>
<td>访问速度</td>
<td>很快，主要基于内存</td>
<td>相对慢一些，主要基于磁盘</td>
</tr>
<tr>
<td>适合场景</td>
<td>缓存、短期状态、计数、队列</td>
<td>长期存储、强结构化数据</td>
</tr>
<tr>
<td>查询方式</td>
<td>按 key 取值为主</td>
<td>SQL 查询</td>
</tr>
<tr>
<td>持久化重点</td>
<td>可选持久化，但常用于高频临时数据</td>
<td>天生适合长期持久化</td>
</tr>
</tbody></table>
<p>简单理解：</p>
<ul>
<li>用户资料、订单、文档元数据更适合 MySQL</li>
<li>验证码、登录态、缓存结果、限流计数更适合 Redis</li>
</ul>
<h2 id="5-安装和启动"><a href="#5-安装和启动" class="headerlink" title="5. 安装和启动"></a>5. 安装和启动</h2><p>如果本地已经装好了 Redis，可以直接启动服务。</p>
<p>常见方式：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-server</span><br></pre></td></tr></table></figure>

<p>如果只是临时学习，也可以用 Docker：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d --name redis -p 6379:6379 redis:7</span><br></pre></td></tr></table></figure>

<p>测试服务是否正常：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli ping</span><br></pre></td></tr></table></figure>

<p>如果返回：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">PONG</span><br></pre></td></tr></table></figure>

<p>说明 Redis 正常启动。</p>
<h2 id="6-Python-连接-Redis"><a href="#6-Python-连接-Redis" class="headerlink" title="6. Python 连接 Redis"></a>6. Python 连接 Redis</h2><p>Python 项目里最常用的是 <code>redis-py</code>。</p>
<p>安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install redis</span><br></pre></td></tr></table></figure>

<p>最基础的连接方式：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> redis</span><br><span class="line"></span><br><span class="line">r = redis.Redis(</span><br><span class="line">    host=<span class="string">&quot;127.0.0.1&quot;</span>,</span><br><span class="line">    port=<span class="number">6379</span>,</span><br><span class="line">    db=<span class="number">0</span>,</span><br><span class="line">    decode_responses=<span class="literal">True</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">r.<span class="built_in">set</span>(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;tom&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(r.get(<span class="string">&quot;name&quot;</span>))</span><br></pre></td></tr></table></figure>

<p>说明：</p>
<ul>
<li><code>db=0</code> 表示使用第 0 个逻辑库</li>
<li><code>decode_responses=True</code> 表示自动把结果解码为字符串</li>
</ul>
<p>如果不加 <code>decode_responses=True</code>，很多结果会是 <code>bytes</code>。</p>
<h2 id="7-Redis-的基本概念"><a href="#7-Redis-的基本概念" class="headerlink" title="7. Redis 的基本概念"></a>7. Redis 的基本概念</h2><p>Redis 里最核心的概念就是：</p>
<ul>
<li>key</li>
<li>value</li>
</ul>
<p>所有数据都以 <code>key -&gt; value</code> 的形式存储。</p>
<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">user:1:name -&gt; tom</span><br><span class="line">task:123:status -&gt; SUCCESS</span><br><span class="line">cache:article:100 -&gt; &#123;...&#125;</span><br></pre></td></tr></table></figure>

<p>实际项目里非常重要的一点是：</p>
<blockquote>
<p>key 命名要有规则。</p>
</blockquote>
<p>常见命名方式：</p>
<ul>
<li><code>user:1001</code></li>
<li><code>session:token:abc123</code></li>
<li><code>task:ingest:001</code></li>
<li><code>cache:query:hash_xxx</code></li>
</ul>
<p>这样后续排查和维护会清晰很多。</p>
<h3 id="7-1-逻辑库-db"><a href="#7-1-逻辑库-db" class="headerlink" title="7.1 逻辑库 db"></a>7.1 逻辑库 <code>db</code></h3><p>Redis 默认会提供多个逻辑库，常见写法里的：</p>
<ul>
<li><code>db=0</code></li>
<li><code>redis://127.0.0.1:6379/1</code></li>
<li><code>redis://127.0.0.1:6379/2</code></li>
</ul>
<p>这里最后的数字就是逻辑库编号。</p>
<p>它常用于做基础隔离，例如：</p>
<ul>
<li><code>0</code>：普通缓存</li>
<li><code>1</code>：Celery broker</li>
<li><code>2</code>：Celery backend</li>
</ul>
<p>不过要注意：</p>
<blockquote>
<p>逻辑库只是“轻隔离”，不是严格意义上的多实例隔离。</p>
</blockquote>
<p>如果项目越来越复杂，真正需要资源隔离、权限隔离、性能隔离时，通常还是会考虑拆 Redis 实例。</p>
<h2 id="8-常用通用命令"><a href="#8-常用通用命令" class="headerlink" title="8. 常用通用命令"></a>8. 常用通用命令</h2><h3 id="8-1-设置和获取"><a href="#8-1-设置和获取" class="headerlink" title="8.1 设置和获取"></a>8.1 设置和获取</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SET name tom</span><br><span class="line">GET name</span><br></pre></td></tr></table></figure>

<h3 id="8-2-判断是否存在"><a href="#8-2-判断是否存在" class="headerlink" title="8.2 判断是否存在"></a>8.2 判断是否存在</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">EXISTS name</span><br></pre></td></tr></table></figure>

<h3 id="8-3-删除"><a href="#8-3-删除" class="headerlink" title="8.3 删除"></a>8.3 删除</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">DEL name</span><br></pre></td></tr></table></figure>

<h3 id="8-4-查看过期时间"><a href="#8-4-查看过期时间" class="headerlink" title="8.4 查看过期时间"></a>8.4 查看过期时间</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">TTL name</span><br></pre></td></tr></table></figure>

<h3 id="8-5-给-key-设置过期"><a href="#8-5-给-key-设置过期" class="headerlink" title="8.5 给 key 设置过期"></a>8.5 给 key 设置过期</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">EXPIRE name 60</span><br></pre></td></tr></table></figure>

<p>表示 60 秒后过期。</p>
<h3 id="8-6-直接带过期时间写入"><a href="#8-6-直接带过期时间写入" class="headerlink" title="8.6 直接带过期时间写入"></a>8.6 直接带过期时间写入</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SET code 123456 EX 300</span><br></pre></td></tr></table></figure>

<p>表示写入验证码，并在 300 秒后自动过期。</p>
<h3 id="8-7-查看库里的-key"><a href="#8-7-查看库里的-key" class="headerlink" title="8.7 查看库里的 key"></a>8.7 查看库里的 key</h3><p>学习阶段常见命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">KEYS *</span><br></pre></td></tr></table></figure>

<p>但要注意：</p>
<blockquote>
<p><code>KEYS *</code> 适合本地调试，不适合在线上大库里频繁使用。</p>
</blockquote>
<p>更稳妥的方式通常是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SCAN 0 MATCH user:* COUNT 100</span><br></pre></td></tr></table></figure>

<p><code>SCAN</code> 更适合在数据量较大时逐步遍历。</p>
<h2 id="9-Redis-的核心数据结构"><a href="#9-Redis-的核心数据结构" class="headerlink" title="9. Redis 的核心数据结构"></a>9. Redis 的核心数据结构</h2><p>Redis 不只是简单字符串，它支持很多数据结构。这个是 Redis 很有价值的地方。</p>
<p>最常用的有：</p>
<ul>
<li>String</li>
<li>Hash</li>
<li>List</li>
<li>Set</li>
<li>Sorted Set</li>
<li>Stream</li>
</ul>
<h2 id="10-String"><a href="#10-String" class="headerlink" title="10. String"></a>10. String</h2><p>这是 Redis 最基础、最常用的数据类型。</p>
<p>适合：</p>
<ul>
<li>普通缓存</li>
<li>验证码</li>
<li>token</li>
<li>JSON 字符串</li>
<li>计数器</li>
</ul>
<h3 id="10-1-基本命令"><a href="#10-1-基本命令" class="headerlink" title="10.1 基本命令"></a>10.1 基本命令</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SET username alice</span><br><span class="line">GET username</span><br></pre></td></tr></table></figure>

<h3 id="10-2-自增计数"><a href="#10-2-自增计数" class="headerlink" title="10.2 自增计数"></a>10.2 自增计数</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SET page_views 0</span><br><span class="line">INCR page_views</span><br><span class="line">INCRBY page_views 5</span><br></pre></td></tr></table></figure>

<p>这类操作很适合：</p>
<ul>
<li>接口访问次数统计</li>
<li>点赞数</li>
<li>限流计数</li>
</ul>
<h3 id="10-3-Python-示例"><a href="#10-3-Python-示例" class="headerlink" title="10.3 Python 示例"></a>10.3 Python 示例</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> redis</span><br><span class="line"></span><br><span class="line">r = redis.Redis(host=<span class="string">&quot;127.0.0.1&quot;</span>, port=<span class="number">6379</span>, db=<span class="number">0</span>, decode_responses=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">r.<span class="built_in">set</span>(<span class="string">&quot;article:1:title&quot;</span>, <span class="string">&quot;Redis Intro&quot;</span>)</span><br><span class="line">title = r.get(<span class="string">&quot;article:1:title&quot;</span>)</span><br><span class="line"></span><br><span class="line">r.<span class="built_in">set</span>(<span class="string">&quot;counter:views&quot;</span>, <span class="number">0</span>)</span><br><span class="line">r.incr(<span class="string">&quot;counter:views&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="11-Hash"><a href="#11-Hash" class="headerlink" title="11. Hash"></a>11. Hash</h2><p>Hash 就是一个 key 下挂多组 field-value。</p>
<p>适合：</p>
<ul>
<li>用户对象</li>
<li>配置项</li>
<li>文档元信息</li>
<li>任务状态对象</li>
</ul>
<h3 id="11-1-基本命令"><a href="#11-1-基本命令" class="headerlink" title="11.1 基本命令"></a>11.1 基本命令</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">HSET user:1 name tom age 18 city shanghai</span><br><span class="line">HGET user:1 name</span><br><span class="line">HGETALL user:1</span><br></pre></td></tr></table></figure>

<h3 id="11-2-为什么-Hash-很常用"><a href="#11-2-为什么-Hash-很常用" class="headerlink" title="11.2 为什么 Hash 很常用"></a>11.2 为什么 Hash 很常用</h3><p>因为很多业务对象天然就是字段结构，比如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">user:1</span><br><span class="line">  name -&gt; tom</span><br><span class="line">  age -&gt; 18</span><br><span class="line">  city -&gt; shanghai</span><br></pre></td></tr></table></figure>

<p>相比把整个对象都塞进一个 JSON 字符串里，Hash 在局部更新字段时更方便。</p>
<h3 id="11-3-Python-示例"><a href="#11-3-Python-示例" class="headerlink" title="11.3 Python 示例"></a>11.3 Python 示例</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">r.hset(<span class="string">&quot;user:1&quot;</span>, mapping=&#123;<span class="string">&quot;name&quot;</span>: <span class="string">&quot;tom&quot;</span>, <span class="string">&quot;age&quot;</span>: <span class="number">18</span>, <span class="string">&quot;city&quot;</span>: <span class="string">&quot;shanghai&quot;</span>&#125;)</span><br><span class="line">user = r.hgetall(<span class="string">&quot;user:1&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="12-List"><a href="#12-List" class="headerlink" title="12. List"></a>12. List</h2><p>List 是有序列表。</p>
<p>适合：</p>
<ul>
<li>简单消息队列</li>
<li>待处理任务列表</li>
<li>最近操作记录</li>
</ul>
<h3 id="12-1-基本命令"><a href="#12-1-基本命令" class="headerlink" title="12.1 基本命令"></a>12.1 基本命令</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">LPUSH queue:tasks task1</span><br><span class="line">LPUSH queue:tasks task2</span><br><span class="line">RPOP queue:tasks</span><br></pre></td></tr></table></figure>

<p>直观一点看，就是：</p>
<ul>
<li>左边入队</li>
<li>右边出队</li>
</ul>
<h3 id="12-2-阻塞读取"><a href="#12-2-阻塞读取" class="headerlink" title="12.2 阻塞读取"></a>12.2 阻塞读取</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">BLPOP queue:tasks 0</span><br></pre></td></tr></table></figure>

<p>表示如果队列为空就阻塞等待。</p>
<p>这可以做一个很简单的消费者模型，但它更适合轻量场景，不适合复杂消费确认机制。</p>
<h3 id="12-3-使用建议"><a href="#12-3-使用建议" class="headerlink" title="12.3 使用建议"></a>12.3 使用建议</h3><p>List 可以做简单队列，但如果项目要：</p>
<ul>
<li>消费组</li>
<li>确认机制</li>
<li>多消费者协作</li>
<li>更强的消息语义</li>
</ul>
<p>通常会考虑：</p>
<ul>
<li>Redis Streams</li>
<li>RabbitMQ</li>
<li>Kafka</li>
</ul>
<h2 id="13-Set"><a href="#13-Set" class="headerlink" title="13. Set"></a>13. Set</h2><p>Set 是无序且元素唯一的集合。</p>
<p>适合：</p>
<ul>
<li>去重</li>
<li>标签集合</li>
<li>记录某用户已点赞哪些内容</li>
<li>共同好友 &#x2F; 共同标签等集合计算</li>
</ul>
<h3 id="13-1-基本命令"><a href="#13-1-基本命令" class="headerlink" title="13.1 基本命令"></a>13.1 基本命令</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SADD tags:article:1 redis python database</span><br><span class="line">SMEMBERS tags:article:1</span><br><span class="line">SISMEMBER tags:article:1 redis</span><br></pre></td></tr></table></figure>

<h3 id="13-2-集合运算"><a href="#13-2-集合运算" class="headerlink" title="13.2 集合运算"></a>13.2 集合运算</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SADD set1 a b c</span><br><span class="line">SADD set2 b c d</span><br><span class="line">SINTER set1 set2</span><br><span class="line">SUNION set1 set2</span><br><span class="line">SDIFF set1 set2</span><br></pre></td></tr></table></figure>

<p>这对“标签交集、权限集合、去重集合”这类场景非常方便。</p>
<h2 id="14-Sorted-Set"><a href="#14-Sorted-Set" class="headerlink" title="14. Sorted Set"></a>14. Sorted Set</h2><p>Sorted Set 是带分数、可排序的集合。</p>
<p>适合：</p>
<ul>
<li>排行榜</li>
<li>按时间排序的数据</li>
<li>按热度排序的数据</li>
</ul>
<h3 id="14-1-基本命令"><a href="#14-1-基本命令" class="headerlink" title="14.1 基本命令"></a>14.1 基本命令</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ZADD ranking 100 tom 95 alice 88 bob</span><br><span class="line">ZRANGE ranking 0 -1</span><br><span class="line">ZREVRANGE ranking 0 -1</span><br><span class="line">ZSCORE ranking tom</span><br></pre></td></tr></table></figure>

<p>这里每个成员都有一个 score。</p>
<h3 id="14-2-场景理解"><a href="#14-2-场景理解" class="headerlink" title="14.2 场景理解"></a>14.2 场景理解</h3><p>例如文章热度榜：</p>
<ul>
<li><code>article:100 -&gt; 200</code></li>
<li><code>article:101 -&gt; 150</code></li>
<li><code>article:102 -&gt; 320</code></li>
</ul>
<p>就可以按 score 排序快速拿到前 N 名。</p>
<h2 id="15-Stream"><a href="#15-Stream" class="headerlink" title="15. Stream"></a>15. Stream</h2><p>Streams 是 Redis 较强的一类消息结构。</p>
<p>适合：</p>
<ul>
<li>事件流</li>
<li>多消费者消费</li>
<li>流式 token 传递</li>
<li>日志型消息</li>
</ul>
<p>在 AI 项目里，它很适合做：</p>
<ul>
<li>Worker 持续写出 token</li>
<li>Gateway &#x2F; FastAPI 持续消费 token</li>
<li>流式返回给前端</li>
</ul>
<h3 id="15-1-基本命令"><a href="#15-1-基本命令" class="headerlink" title="15.1 基本命令"></a>15.1 基本命令</h3><p>写入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">XADD chat:stream:1 * token hello</span><br></pre></td></tr></table></figure>

<p>读取：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">XREAD COUNT 10 STREAMS chat:stream:1 0</span><br></pre></td></tr></table></figure>

<h3 id="15-2-消费组"><a href="#15-2-消费组" class="headerlink" title="15.2 消费组"></a>15.2 消费组</h3><p>如果是多个消费者协作，可以用消费组：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">XGROUP CREATE chat:stream:1 group1 0 MKSTREAM</span><br><span class="line">XREADGROUP GROUP group1 consumer1 COUNT 10 STREAMS chat:stream:1 &gt;</span><br></pre></td></tr></table></figure>

<h3 id="15-3-为什么-Streams-比-List-更适合流式场景"><a href="#15-3-为什么-Streams-比-List-更适合流式场景" class="headerlink" title="15.3 为什么 Streams 比 List 更适合流式场景"></a>15.3 为什么 Streams 比 List 更适合流式场景</h3><p>因为它支持：</p>
<ul>
<li>消息 ID</li>
<li>消费组</li>
<li>待确认消息</li>
<li>更像日志流的结构</li>
</ul>
<p>如果后面你做：</p>
<ul>
<li>SSE</li>
<li>token 流输出</li>
<li>异步处理链路</li>
</ul>
<p>Streams 会比简单 List 更稳一些。</p>
<h2 id="16-过期时间和-TTL"><a href="#16-过期时间和-TTL" class="headerlink" title="16. 过期时间和 TTL"></a>16. 过期时间和 TTL</h2><p>Redis 很核心的一点就是支持 key 过期。</p>
<p>这在缓存场景里非常重要。</p>
<h3 id="16-1-设置过期"><a href="#16-1-设置过期" class="headerlink" title="16.1 设置过期"></a>16.1 设置过期</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SET session:token:abc user_1 EX 3600</span><br></pre></td></tr></table></figure>

<p>表示 1 小时后自动过期。</p>
<h3 id="16-2-查看剩余时间"><a href="#16-2-查看剩余时间" class="headerlink" title="16.2 查看剩余时间"></a>16.2 查看剩余时间</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">TTL session:token:abc</span><br></pre></td></tr></table></figure>

<h3 id="16-3-为什么过期时间很重要"><a href="#16-3-为什么过期时间很重要" class="headerlink" title="16.3 为什么过期时间很重要"></a>16.3 为什么过期时间很重要</h3><p>因为很多数据本来就不该永久保存，例如：</p>
<ul>
<li>登录态</li>
<li>验证码</li>
<li>缓存结果</li>
<li>限流计数</li>
<li>任务中间状态</li>
</ul>
<p>如果不设置过期时间，Redis 很容易越堆越大。</p>
<h2 id="17-持久化"><a href="#17-持久化" class="headerlink" title="17. 持久化"></a>17. 持久化</h2><p>Redis 虽然主要是内存数据库，但也支持持久化。</p>
<p>常见方式：</p>
<ul>
<li>RDB</li>
<li>AOF</li>
</ul>
<h3 id="17-1-RDB"><a href="#17-1-RDB" class="headerlink" title="17.1 RDB"></a>17.1 RDB</h3><p>它更像是按时间点保存一份内存快照。</p>
<p>特点：</p>
<ul>
<li>文件紧凑</li>
<li>恢复快</li>
<li>更适合定期备份</li>
</ul>
<h3 id="17-2-AOF"><a href="#17-2-AOF" class="headerlink" title="17.2 AOF"></a>17.2 AOF</h3><p>它的思路是把写命令按顺序追加记录下来。</p>
<p>特点：</p>
<ul>
<li>数据恢复通常更完整</li>
<li>文件可能更大</li>
<li>更强调操作日志式恢复</li>
</ul>
<h3 id="17-3-怎么理解"><a href="#17-3-怎么理解" class="headerlink" title="17.3 怎么理解"></a>17.3 怎么理解</h3><p>如果 Redis 里只是放缓存，丢了还能重建，那持久化要求没那么高。</p>
<p>如果 Redis 里存了：</p>
<ul>
<li>任务状态</li>
<li>流式消息</li>
<li>某些关键临时业务状态</li>
</ul>
<p>那就要更认真考虑持久化策略。</p>
<h2 id="18-Pub-Sub"><a href="#18-Pub-Sub" class="headerlink" title="18. Pub&#x2F;Sub"></a>18. Pub&#x2F;Sub</h2><p>Redis 还支持发布订阅。</p>
<p>基本命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SUBSCRIBE news</span><br><span class="line">PUBLISH news <span class="string">&quot;hello&quot;</span></span><br></pre></td></tr></table></figure>

<p>适合：</p>
<ul>
<li>简单实时通知</li>
<li>临时消息广播</li>
</ul>
<p>但它有一个明显特点：</p>
<blockquote>
<p>如果订阅者不在线，消息一般不会帮你补回来。</p>
</blockquote>
<p>所以在很多需要“可回放、可追踪”的场景里，Streams 通常比 Pub&#x2F;Sub 更合适。</p>
<h2 id="19-事务和-Lua"><a href="#19-事务和-Lua" class="headerlink" title="19. 事务和 Lua"></a>19. 事务和 Lua</h2><p>Redis 也支持事务和 Lua 脚本。</p>
<h3 id="19-1-事务"><a href="#19-1-事务" class="headerlink" title="19.1 事务"></a>19.1 事务</h3><p>基本写法：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">MULTI</span><br><span class="line">SET key1 value1</span><br><span class="line">INCR counter</span><br><span class="line">EXEC</span><br></pre></td></tr></table></figure>

<p>适合把一组命令放在一起执行。</p>
<h3 id="19-2-Lua-脚本"><a href="#19-2-Lua-脚本" class="headerlink" title="19.2 Lua 脚本"></a>19.2 Lua 脚本</h3><p>Lua 常用于：</p>
<ul>
<li>多命令原子操作</li>
<li>分布式锁安全释放</li>
<li>把多个步骤封装成一个服务端操作</li>
</ul>
<p>在实际项目里，Lua 经常是为了解决“先判断再删除”这种竞态问题。</p>
<h2 id="20-分布式锁"><a href="#20-分布式锁" class="headerlink" title="20. 分布式锁"></a>20. 分布式锁</h2><p>Redis 常被用来做分布式锁。</p>
<p>最常见写法是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SET lock:task_1 request_abc NX EX 30</span><br></pre></td></tr></table></figure>

<p>含义：</p>
<ul>
<li><code>NX</code>：只有 key 不存在时才设置成功</li>
<li><code>EX 30</code>：30 秒后自动过期</li>
</ul>
<p>如果返回成功，就表示拿到了锁。</p>
<h3 id="20-1-为什么需要过期时间"><a href="#20-1-为什么需要过期时间" class="headerlink" title="20.1 为什么需要过期时间"></a>20.1 为什么需要过期时间</h3><p>因为如果加锁后进程挂了，没有过期时间的话，锁就可能一直不释放。</p>
<h3 id="20-2-为什么不能直接-DEL"><a href="#20-2-为什么不能直接-DEL" class="headerlink" title="20.2 为什么不能直接 DEL"></a>20.2 为什么不能直接 <code>DEL</code></h3><p>因为可能出现这种情况：</p>
<ol>
<li>线程 A 拿到锁</li>
<li>锁过期</li>
<li>线程 B 拿到同名新锁</li>
<li>线程 A 这时候再执行 <code>DEL</code></li>
<li>把线程 B 的锁删掉了</li>
</ol>
<p>所以更安全的做法是：</p>
<ul>
<li>加锁时写入唯一值</li>
<li>解锁时先判断 value 是否还是自己的</li>
<li>判断和删除要放在一个原子操作里，通常用 Lua</li>
</ul>
<h3 id="20-3-Python-示例"><a href="#20-3-Python-示例" class="headerlink" title="20.3 Python 示例"></a>20.3 Python 示例</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> uuid</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> redis</span><br><span class="line"></span><br><span class="line">r = redis.Redis(host=<span class="string">&quot;127.0.0.1&quot;</span>, port=<span class="number">6379</span>, db=<span class="number">0</span>, decode_responses=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">lock_key = <span class="string">&quot;lock:task:1&quot;</span></span><br><span class="line">lock_value = <span class="built_in">str</span>(uuid.uuid4())</span><br><span class="line"></span><br><span class="line">locked = r.<span class="built_in">set</span>(lock_key, lock_value, nx=<span class="literal">True</span>, ex=<span class="number">30</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> locked:</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;do something&quot;</span>)</span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        unlock_script = <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">        if redis.call(&#x27;GET&#x27;, KEYS[1]) == ARGV[1] then</span></span><br><span class="line"><span class="string">            return redis.call(&#x27;DEL&#x27;, KEYS[1])</span></span><br><span class="line"><span class="string">        else</span></span><br><span class="line"><span class="string">            return 0</span></span><br><span class="line"><span class="string">        end</span></span><br><span class="line"><span class="string">        &quot;&quot;&quot;</span></span><br><span class="line">        r.<span class="built_in">eval</span>(unlock_script, <span class="number">1</span>, lock_key, lock_value)</span><br></pre></td></tr></table></figure>

<h2 id="21-缓存是-Redis-最常见的用途"><a href="#21-缓存是-Redis-最常见的用途" class="headerlink" title="21. 缓存是 Redis 最常见的用途"></a>21. 缓存是 Redis 最常见的用途</h2><h3 id="21-1-最简单的缓存思路"><a href="#21-1-最简单的缓存思路" class="headerlink" title="21.1 最简单的缓存思路"></a>21.1 最简单的缓存思路</h3><p>先查 Redis：</p>
<ul>
<li>有数据就直接返回</li>
<li>没数据再查数据库或调用模型服务</li>
<li>查到结果后再写回 Redis</li>
</ul>
<p>这就是最常见的缓存模式。</p>
<h3 id="21-2-Python-示例"><a href="#21-2-Python-示例" class="headerlink" title="21.2 Python 示例"></a>21.2 Python 示例</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> json</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> redis</span><br><span class="line"></span><br><span class="line">r = redis.Redis(host=<span class="string">&quot;127.0.0.1&quot;</span>, port=<span class="number">6379</span>, db=<span class="number">0</span>, decode_responses=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">get_article</span>(<span class="params">article_id: <span class="built_in">int</span></span>):</span><br><span class="line">    cache_key = <span class="string">f&quot;article:<span class="subst">&#123;article_id&#125;</span>&quot;</span></span><br><span class="line">    cached = r.get(cache_key)</span><br><span class="line">    <span class="keyword">if</span> cached:</span><br><span class="line">        <span class="keyword">return</span> json.loads(cached)</span><br><span class="line"></span><br><span class="line">    data = &#123;<span class="string">&quot;id&quot;</span>: article_id, <span class="string">&quot;title&quot;</span>: <span class="string">&quot;Redis Intro&quot;</span>&#125;</span><br><span class="line">    r.<span class="built_in">set</span>(cache_key, json.dumps(data), ex=<span class="number">300</span>)</span><br><span class="line">    <span class="keyword">return</span> data</span><br></pre></td></tr></table></figure>

<h3 id="21-3-缓存设计里要注意什么"><a href="#21-3-缓存设计里要注意什么" class="headerlink" title="21.3 缓存设计里要注意什么"></a>21.3 缓存设计里要注意什么</h3><ul>
<li>一定要设计 TTL</li>
<li>不要无脑缓存超大对象</li>
<li>key 命名要稳定</li>
<li>更新数据库时要考虑缓存同步</li>
</ul>
<h2 id="22-缓存穿透、击穿、雪崩"><a href="#22-缓存穿透、击穿、雪崩" class="headerlink" title="22. 缓存穿透、击穿、雪崩"></a>22. 缓存穿透、击穿、雪崩</h2><p>这是 Redis 学习里很常见的三个词。</p>
<h3 id="22-1-缓存穿透"><a href="#22-1-缓存穿透" class="headerlink" title="22.1 缓存穿透"></a>22.1 缓存穿透</h3><p>请求的数据本来就不存在：</p>
<ul>
<li>Redis 没有</li>
<li>数据库也没有</li>
<li>每次都打到后端</li>
</ul>
<p>常见处理：</p>
<ul>
<li>参数校验</li>
<li>对空结果也做短期缓存</li>
<li>布隆过滤器</li>
</ul>
<h3 id="22-2-缓存击穿"><a href="#22-2-缓存击穿" class="headerlink" title="22.2 缓存击穿"></a>22.2 缓存击穿</h3><p>某个热点 key 失效瞬间，大量请求同时打到后端。</p>
<p>常见处理：</p>
<ul>
<li>热点 key 不要同时过期</li>
<li>加锁重建缓存</li>
<li>提前刷新</li>
</ul>
<h3 id="22-3-缓存雪崩"><a href="#22-3-缓存雪崩" class="headerlink" title="22.3 缓存雪崩"></a>22.3 缓存雪崩</h3><p>大量 key 在同一时间过期，导致后端压力暴涨。</p>
<p>常见处理：</p>
<ul>
<li>TTL 加随机值</li>
<li>分批过期</li>
<li>多级缓存</li>
</ul>
<h2 id="23-限流"><a href="#23-限流" class="headerlink" title="23. 限流"></a>23. 限流</h2><p>Redis 很适合做接口限流，因为：</p>
<ul>
<li><code>INCR</code> 是原子操作</li>
<li>配合 TTL 很方便</li>
</ul>
<h3 id="23-1-最简单的固定窗口限流"><a href="#23-1-最简单的固定窗口限流" class="headerlink" title="23.1 最简单的固定窗口限流"></a>23.1 最简单的固定窗口限流</h3><p>思路：</p>
<ol>
<li>某个用户请求一次就 <code>INCR</code></li>
<li>第一次请求时设置过期时间</li>
<li>超过阈值就拒绝</li>
</ol>
<p>Python 示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> redis</span><br><span class="line"></span><br><span class="line">r = redis.Redis(host=<span class="string">&quot;127.0.0.1&quot;</span>, port=<span class="number">6379</span>, db=<span class="number">0</span>, decode_responses=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">allow_request</span>(<span class="params">user_id: <span class="built_in">str</span>, limit: <span class="built_in">int</span> = <span class="number">10</span>, window: <span class="built_in">int</span> = <span class="number">60</span></span>) -&gt; <span class="built_in">bool</span>:</span><br><span class="line">    key = <span class="string">f&quot;rate_limit:<span class="subst">&#123;user_id&#125;</span>&quot;</span></span><br><span class="line">    current = r.incr(key)</span><br><span class="line">    <span class="keyword">if</span> current == <span class="number">1</span>:</span><br><span class="line">        r.expire(key, window)</span><br><span class="line">    <span class="keyword">return</span> current &lt;= limit</span><br></pre></td></tr></table></figure>

<p>这个模式在：</p>
<ul>
<li>登录接口</li>
<li>短信发送</li>
<li>AI 推理调用</li>
</ul>
<p>都很常见。</p>
<h2 id="24-Session-登录态"><a href="#24-Session-登录态" class="headerlink" title="24. Session &#x2F; 登录态"></a>24. Session &#x2F; 登录态</h2><p>Redis 也经常用来存登录态或短期会话。</p>
<p>例如：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">session:token:abc123 -&gt; user_id=1001</span><br></pre></td></tr></table></figure>

<p>然后设置：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">EXPIRE session:token:abc123 3600</span><br></pre></td></tr></table></figure>

<p>这样用户 1 小时不活跃就自动失效。</p>
<h2 id="25-FastAPI-里怎么用-Redis"><a href="#25-FastAPI-里怎么用-Redis" class="headerlink" title="25. FastAPI 里怎么用 Redis"></a>25. FastAPI 里怎么用 Redis</h2><p>FastAPI 项目里，Redis 常见用途有：</p>
<ul>
<li>缓存接口结果</li>
<li>存验证码</li>
<li>存 session</li>
<li>做限流</li>
<li>存短期任务状态</li>
</ul>
<h3 id="25-1-一个简单缓存例子"><a href="#25-1-一个简单缓存例子" class="headerlink" title="25.1 一个简单缓存例子"></a>25.1 一个简单缓存例子</h3><p>这里用 <code>redis.asyncio</code> 更符合 FastAPI 的异步风格。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> json</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"><span class="keyword">from</span> redis.asyncio <span class="keyword">import</span> Redis</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line"></span><br><span class="line">redis_client = Redis(</span><br><span class="line">    host=<span class="string">&quot;127.0.0.1&quot;</span>,</span><br><span class="line">    port=<span class="number">6379</span>,</span><br><span class="line">    db=<span class="number">0</span>,</span><br><span class="line">    decode_responses=<span class="literal">True</span>,</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/articles/&#123;article_id&#125;&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">get_article</span>(<span class="params">article_id: <span class="built_in">int</span></span>):</span><br><span class="line">    cache_key = <span class="string">f&quot;article:<span class="subst">&#123;article_id&#125;</span>&quot;</span></span><br><span class="line">    cached = <span class="keyword">await</span> redis_client.get(cache_key)</span><br><span class="line">    <span class="keyword">if</span> cached:</span><br><span class="line">        <span class="keyword">return</span> json.loads(cached)</span><br><span class="line"></span><br><span class="line">    data = &#123;<span class="string">&quot;id&quot;</span>: article_id, <span class="string">&quot;title&quot;</span>: <span class="string">&quot;Redis Intro&quot;</span>&#125;</span><br><span class="line">    <span class="keyword">await</span> redis_client.<span class="built_in">set</span>(cache_key, json.dumps(data), ex=<span class="number">300</span>)</span><br><span class="line">    <span class="keyword">return</span> data</span><br></pre></td></tr></table></figure>

<p>补充：</p>
<ul>
<li><code>redis.asyncio</code> 更适合异步接口</li>
<li>正式项目里通常会统一封装 Redis 客户端</li>
<li>服务关闭时最好把客户端连接关闭掉</li>
</ul>
<h3 id="25-2-一个简单限流例子"><a href="#25-2-一个简单限流例子" class="headerlink" title="25.2 一个简单限流例子"></a>25.2 一个简单限流例子</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI, HTTPException, Request</span><br><span class="line"><span class="keyword">from</span> redis.asyncio <span class="keyword">import</span> Redis</span><br><span class="line"></span><br><span class="line">app = FastAPI()</span><br><span class="line">redis_client = Redis(host=<span class="string">&quot;127.0.0.1&quot;</span>, port=<span class="number">6379</span>, db=<span class="number">0</span>, decode_responses=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.get(<span class="params"><span class="string">&quot;/infer&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">infer</span>(<span class="params">request: Request</span>):</span><br><span class="line">    client_ip = request.client.host</span><br><span class="line">    key = <span class="string">f&quot;rate_limit:<span class="subst">&#123;client_ip&#125;</span>&quot;</span></span><br><span class="line"></span><br><span class="line">    current = <span class="keyword">await</span> redis_client.incr(key)</span><br><span class="line">    <span class="keyword">if</span> current == <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">await</span> redis_client.expire(key, <span class="number">60</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> current &gt; <span class="number">20</span>:</span><br><span class="line">        <span class="keyword">raise</span> HTTPException(status_code=<span class="number">429</span>, detail=<span class="string">&quot;too many requests&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123;<span class="string">&quot;message&quot;</span>: <span class="string">&quot;ok&quot;</span>&#125;</span><br></pre></td></tr></table></figure>

<h3 id="25-3-生命周期里管理-Redis-客户端"><a href="#25-3-生命周期里管理-Redis-客户端" class="headerlink" title="25.3 生命周期里管理 Redis 客户端"></a>25.3 生命周期里管理 Redis 客户端</h3><p>如果项目规模再大一点，通常会把 Redis 放到 FastAPI 生命周期里初始化和关闭。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> contextlib <span class="keyword">import</span> asynccontextmanager</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> fastapi <span class="keyword">import</span> FastAPI</span><br><span class="line"><span class="keyword">from</span> redis.asyncio <span class="keyword">import</span> Redis</span><br><span class="line"></span><br><span class="line"><span class="meta">@asynccontextmanager</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">def</span> <span class="title function_">lifespan</span>(<span class="params">app: FastAPI</span>):</span><br><span class="line">    app.state.redis = Redis(</span><br><span class="line">        host=<span class="string">&quot;127.0.0.1&quot;</span>,</span><br><span class="line">        port=<span class="number">6379</span>,</span><br><span class="line">        db=<span class="number">0</span>,</span><br><span class="line">        decode_responses=<span class="literal">True</span>,</span><br><span class="line">    )</span><br><span class="line">    <span class="keyword">yield</span></span><br><span class="line">    <span class="keyword">await</span> app.state.redis.aclose()</span><br><span class="line"></span><br><span class="line">app = FastAPI(lifespan=lifespan)</span><br></pre></td></tr></table></figure>

<p>这样做的好处：</p>
<ul>
<li>连接初始化位置统一</li>
<li>更方便复用</li>
<li>关闭服务时能正常释放资源</li>
</ul>
<h2 id="26-Celery-为什么经常配-Redis"><a href="#26-Celery-为什么经常配-Redis" class="headerlink" title="26. Celery 为什么经常配 Redis"></a>26. Celery 为什么经常配 Redis</h2><p>你在 <code>Celery.md</code> 里已经接触到了这一点。</p>
<p>Redis 在 Celery 里通常可以做两件事：</p>
<ul>
<li>Broker：存待执行任务消息</li>
<li>Result Backend：存任务状态和结果</li>
</ul>
<p>示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> celery <span class="keyword">import</span> Celery</span><br><span class="line"></span><br><span class="line">app = Celery(</span><br><span class="line">    <span class="string">&quot;demo&quot;</span>,</span><br><span class="line">    broker=<span class="string">&quot;redis://127.0.0.1:6379/1&quot;</span>,</span><br><span class="line">    backend=<span class="string">&quot;redis://127.0.0.1:6379/2&quot;</span>,</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>这里通常会把逻辑库分开：</p>
<ul>
<li><code>/1</code>：Broker</li>
<li><code>/2</code>：Backend</li>
</ul>
<p>这样排查和清理都更方便。</p>
<h3 id="26-1-为什么很多人喜欢-Redis-Celery"><a href="#26-1-为什么很多人喜欢-Redis-Celery" class="headerlink" title="26.1 为什么很多人喜欢 Redis + Celery"></a>26.1 为什么很多人喜欢 Redis + Celery</h3><p>因为它足够简单：</p>
<ul>
<li>环境轻</li>
<li>容易本地启动</li>
<li>Python 项目接入快</li>
</ul>
<p>但也要注意：</p>
<ul>
<li>不要无限保存任务结果</li>
<li>大结果不要直接堆 Redis</li>
<li>任务状态和业务长期数据要区分</li>
</ul>
<h2 id="27-AI-RAG-项目里-Redis-的几个实用位置"><a href="#27-AI-RAG-项目里-Redis-的几个实用位置" class="headerlink" title="27. AI &#x2F; RAG 项目里 Redis 的几个实用位置"></a>27. AI &#x2F; RAG 项目里 Redis 的几个实用位置</h2><p>如果你做的是 RAG 或模型服务，Redis 常放在这些位置：</p>
<h3 id="27-1-热点缓存"><a href="#27-1-热点缓存" class="headerlink" title="27.1 热点缓存"></a>27.1 热点缓存</h3><p>例如：</p>
<ul>
<li>相同 query 的检索结果缓存</li>
<li>embedding 结果缓存</li>
<li>频繁访问的文档元数据缓存</li>
</ul>
<h3 id="27-2-会话上下文缓存"><a href="#27-2-会话上下文缓存" class="headerlink" title="27.2 会话上下文缓存"></a>27.2 会话上下文缓存</h3><p>例如：</p>
<ul>
<li>最近 N 轮聊天记录</li>
<li>临时上下文摘要</li>
<li>会话状态</li>
</ul>
<h3 id="27-3-任务状态层"><a href="#27-3-任务状态层" class="headerlink" title="27.3 任务状态层"></a>27.3 任务状态层</h3><p>例如：</p>
<ul>
<li><code>task:123:status</code></li>
<li><code>task:123:progress</code></li>
<li><code>task:123:result_ref</code></li>
</ul>
<h3 id="27-4-流式输出通道"><a href="#27-4-流式输出通道" class="headerlink" title="27.4 流式输出通道"></a>27.4 流式输出通道</h3><p>例如：</p>
<ul>
<li>Worker 生成 token 后写入 <code>chat:stream:&#123;task_id&#125;</code></li>
<li>Gateway 或 API 服务持续消费</li>
<li>对外通过 SSE 返回</li>
</ul>
<p>这个设计非常适合“生成过程持续输出”的场景。</p>
<h2 id="28-Redis-Streams-在流式输出里的思路"><a href="#28-Redis-Streams-在流式输出里的思路" class="headerlink" title="28. Redis Streams 在流式输出里的思路"></a>28. Redis Streams 在流式输出里的思路</h2><p>一个简化流程可以这样理解：</p>
<ol>
<li>FastAPI 收到对话请求</li>
<li>把任务提交给 Celery</li>
<li>Worker 调模型生成 token</li>
<li>每生成一段 token 就 <code>XADD</code> 到某个 Stream</li>
<li>Gateway 或 API 层 <code>XREAD</code> &#x2F; <code>XREADGROUP</code></li>
<li>持续把内容推给前端</li>
</ol>
<p>例如 Stream key：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">chat:stream:task_1001</span><br></pre></td></tr></table></figure>

<p>这个模式的好处：</p>
<ul>
<li>推理和对外接口解耦</li>
<li>可以观察中间过程</li>
<li>可以做断线恢复或补读</li>
</ul>
<h2 id="29-key-设计建议"><a href="#29-key-设计建议" class="headerlink" title="29. key 设计建议"></a>29. key 设计建议</h2><p>Redis 真正到了项目里，key 设计非常重要。</p>
<p>建议：</p>
<ul>
<li>带业务前缀</li>
<li>带对象类型</li>
<li>带唯一 ID</li>
<li>必要时带版本</li>
</ul>
<p>例如：</p>
<ul>
<li><code>user:1001</code></li>
<li><code>session:token:abc123</code></li>
<li><code>cache:article:100</code></li>
<li><code>task:ingest:001:status</code></li>
<li><code>chat:stream:task_1001</code></li>
</ul>
<p>不要这样写：</p>
<ul>
<li><code>a</code></li>
<li><code>test</code></li>
<li><code>temp1</code></li>
</ul>
<p>这种 key 后期几乎不可维护。</p>
<h2 id="30-Redis-内存管理要有意识"><a href="#30-Redis-内存管理要有意识" class="headerlink" title="30. Redis 内存管理要有意识"></a>30. Redis 内存管理要有意识</h2><p>Redis 很快，但它是以内存为核心的，所以内存管理很重要。</p>
<p>需要注意：</p>
<ul>
<li>大 key 会拖慢操作</li>
<li>无过期时间的缓存容易无限增长</li>
<li>Stream、List、Set 如果不清理会不断膨胀</li>
<li>不要把超大模型结果直接塞进去</li>
</ul>
<p>对 AI 项目尤其重要：</p>
<ul>
<li>大文本结果建议存数据库或文件</li>
<li>Redis 里尽量存引用、状态、索引、小块结果</li>
</ul>
<h3 id="30-1-排查时少用危险大命令"><a href="#30-1-排查时少用危险大命令" class="headerlink" title="30.1 排查时少用危险大命令"></a>30.1 排查时少用危险大命令</h3><p>实际项目里还要注意一些“能用，但别乱用”的命令。</p>
<p>例如：</p>
<ul>
<li><code>KEYS *</code></li>
<li><code>FLUSHDB</code></li>
<li><code>FLUSHALL</code></li>
</ul>
<p>其中：</p>
<ul>
<li><code>KEYS *</code> 在大库里可能带来明显扫描压力</li>
<li><code>FLUSHDB</code> 会清空当前库</li>
<li><code>FLUSHALL</code> 会清空整个 Redis 所有库</li>
</ul>
<p>本地学习可以用，但线上环境要非常谨慎。</p>
<h2 id="31-常见坑"><a href="#31-常见坑" class="headerlink" title="31. 常见坑"></a>31. 常见坑</h2><h3 id="31-1-把-Redis-当永久数据库"><a href="#31-1-把-Redis-当永久数据库" class="headerlink" title="31.1 把 Redis 当永久数据库"></a>31.1 把 Redis 当永久数据库</h3><p>Redis 可以持久化，但它更常见的定位还是：</p>
<ul>
<li>缓存</li>
<li>状态层</li>
<li>高速临时存储</li>
</ul>
<p>长期核心业务数据还是更适合数据库。</p>
<h3 id="31-2-忘记设置-TTL"><a href="#31-2-忘记设置-TTL" class="headerlink" title="31.2 忘记设置 TTL"></a>31.2 忘记设置 TTL</h3><p>这是最常见的问题之一。</p>
<p>很多缓存、验证码、限流 key 如果没有 TTL，会一直堆着不清。</p>
<h3 id="31-3-一个-Redis-库里什么都混着放"><a href="#31-3-一个-Redis-库里什么都混着放" class="headerlink" title="31.3 一个 Redis 库里什么都混着放"></a>31.3 一个 Redis 库里什么都混着放</h3><p>例如：</p>
<ul>
<li>缓存</li>
<li>Celery broker</li>
<li>Celery backend</li>
<li>会话</li>
<li>任务流</li>
</ul>
<p>全部都混在同一个逻辑库里，会让排查和维护变得很乱。</p>
<p>至少应该在命名和逻辑库上做一定隔离。</p>
<h3 id="31-4-把超大对象直接塞进-Redis"><a href="#31-4-把超大对象直接塞进-Redis" class="headerlink" title="31.4 把超大对象直接塞进 Redis"></a>31.4 把超大对象直接塞进 Redis</h3><p>例如：</p>
<ul>
<li>整篇长文档</li>
<li>大块模型输出</li>
<li>大量 embedding</li>
</ul>
<p>这些会明显增加内存压力，也会拖慢网络传输。</p>
<h3 id="31-5-分布式锁写得不安全"><a href="#31-5-分布式锁写得不安全" class="headerlink" title="31.5 分布式锁写得不安全"></a>31.5 分布式锁写得不安全</h3><p>只会 <code>SETNX</code> 或只会 <code>DEL</code>，但没有考虑：</p>
<ul>
<li>锁超时</li>
<li>唯一 value</li>
<li>原子释放</li>
</ul>
<p>这种写法在并发场景里容易出问题。</p>
<h3 id="31-6-用-Pub-Sub-做必须可靠消费的任务"><a href="#31-6-用-Pub-Sub-做必须可靠消费的任务" class="headerlink" title="31.6 用 Pub&#x2F;Sub 做必须可靠消费的任务"></a>31.6 用 Pub&#x2F;Sub 做必须可靠消费的任务</h3><p>Pub&#x2F;Sub 更像广播，不是强消息队列。</p>
<p>如果你要求：</p>
<ul>
<li>消息可追踪</li>
<li>消费可确认</li>
<li>掉线后能补读</li>
</ul>
<p>Streams 通常更合适。</p>
<h2 id="32-一套比较实用的学习顺序"><a href="#32-一套比较实用的学习顺序" class="headerlink" title="32. 一套比较实用的学习顺序"></a>32. 一套比较实用的学习顺序</h2><p>建议按这个顺序掌握：</p>
<ol>
<li>先学会连接 Redis 和 <code>redis-cli</code></li>
<li>掌握 String、Hash、List、Set、Sorted Set</li>
<li>学会 TTL 和过期机制</li>
<li>学会缓存和限流</li>
<li>学会分布式锁的基本思路</li>
<li>再看 Pub&#x2F;Sub 和 Streams</li>
<li>最后再把 Redis 接到 FastAPI 和 Celery</li>
</ol>
<h2 id="33-总结"><a href="#33-总结" class="headerlink" title="33. 总结"></a>33. 总结</h2><p>Redis 的核心价值可以概括成几句话：</p>
<ul>
<li>它很快，适合高频读写</li>
<li>它支持多种数据结构，不只是一个简单缓存</li>
<li>它天然适合存“短期状态”和“可过期数据”</li>
<li>它很适合做 FastAPI 和 Celery 之间的中间层</li>
</ul>
<p>在你这套 AI 模型开发学习链路里，可以这样理解它的位置：</p>
<ul>
<li>FastAPI：提供接口</li>
<li>Redis：缓存、状态、消息中间层</li>
<li>Celery：执行后台异步任务</li>
<li>MySQL：长期结构化数据存储</li>
</ul>
<p>如果后面你继续往“RAG 系统、推理服务、异步任务平台”方向走，Redis 几乎一定会成为一个基础组件。</p>
<h2 id="34-参考"><a href="#34-参考" class="headerlink" title="34. 参考"></a>34. 参考</h2><ul>
<li>Redis Docs: <a href="https://redis.io/docs/">https://redis.io/docs/</a></li>
<li>Redis Data Types: <a href="https://redis.io/docs/latest/develop/data-types/">https://redis.io/docs/latest/develop/data-types/</a></li>
<li>Redis Commands: <a href="https://redis.io/docs/latest/commands/">https://redis.io/docs/latest/commands/</a></li>
<li>Redis EXPIRE: <a href="https://redis.io/docs/latest/commands/expire/">https://redis.io/docs/latest/commands/expire/</a></li>
<li>Redis TTL: <a href="https://redis.io/docs/latest/commands/ttl/">https://redis.io/docs/latest/commands/ttl/</a></li>
<li>Redis Streams: <a href="https://redis.io/docs/latest/develop/data-types/streams/">https://redis.io/docs/latest/develop/data-types/streams/</a></li>
<li>redis-py Guide: <a href="https://redis.io/docs/latest/develop/clients/redis-py/">https://redis.io/docs/latest/develop/clients/redis-py/</a></li>
</ul>
]]></content>
      <categories>
        <category>学习</category>
        <category>AI模型开发</category>
        <category>知识点学习</category>
        <category>框架</category>
      </categories>
  </entry>
</search>
